# P000 six-axis Johnson–Tropical arithmetic integration return

Status: `SUCCESS / EXACT JOINT-INFORMATION CLASSIFICATION / NO NATIVE PROMOTION`

- Task: `RS-P000-SIX-AXIS-JOHNSON-TROPICAL-ARITHMETIC-INTEGRATION`
- Publication: `TP2-156A531524A87ED60C04`
- Researcher: `EM-P000JTAI1-6D4A91`
- Claim: `chatgpt-p000jtai1-20260830-2018-6d4a91`
- Execution record: `ER-089CBC246179905BE89D`
- Accepted Johnson input: `RR-930072E4A6F2E0FD912F`
- Accepted Tropical input: `RR-43B397CD1E8953F613B3`

Hard target:

`P000_JOHNSON_TROPICAL_JOINT_ARITHMETIC_INFORMATION_EXACTLY_CLASSIFIED_OR_REDUNDANCY_BOUNDARY_PROVED`

Terminal disposition:

`P000_JOHNSON_TROPICAL_JOINT_ARITHMETIC_INFORMATION_EXACTLY_CLASSIFIED`.

## 1. Frozen domains and notation

Use the frozen six-coordinate order

`x=(x_AB,x_AC,x_AD,x_BC,x_BD,x_CD) in Z^6`

and the three complementary pairs

`(AB,CD), (AC,BD), (AD,BC)`.

Write

- `t=(t1,t2,t3)=(x_AB*x_CD, x_AC*x_BD, x_AD*x_BC)`;
- `H=(h1,h2,h3)=(x_AB+x_CD, x_AC+x_BD, x_AD+x_BC)`;
- `Q=t1-t2+t3`;
- `Q_orb=sort(S_t-2*t1,S_t-2*t2,S_t-2*t3)`, `S_t=t1+t2+t3`;
- `rho=((x_AB-x_CD) mod2,(x_AC-x_BD) mod2,(x_AD-x_BC) mod2,sum(x) mod3)`.

For a fixed prime `p`, the valuation subdomain remains exactly

`D_p=(Z\{0})^6`.

On `D_p` define

`alpha_p=(v_p(t1),v_p(t2),v_p(t3))`

and classify its minimum as `UNIQUE_MIN`, `TWO_MIN`, or `TRIPLE_MIN`. The scalar gap is
`delta_p=second_min(alpha_p)-min(alpha_p)`.

The accepted Johnson `1+3+2` projector data is retained as a typed, lossless representation reference. Because the three projector components sum back to `x`, I do not count the full projector vectors as a compressed arithmetic invariant; doing so would trivialize every fiber question.

## 2. The first exact collapse: `Q_orb` already contains the full unordered product triple

Let

`q=(q1,q2,q3)=Q_orb`

and let `S_q=q1+q2+q3`. Since the three entries are `S_t-2*t_i` and their sum is `S_t`,

`{t1,t2,t3} = { (S_q-q1)/2, (S_q-q2)/2, (S_q-q3)/2 }`

as an unordered multiset.

So `Q_orb` is not merely a coarse quadratic score. At its accepted exact strength it is equivalent to the **unordered exact complementary-product triple**.

Consequences on every `D_p`:

1. `Q_orb` determines the unordered `alpha_p` triple;
2. hence it determines `min(alpha_p)`;
3. it determines `UNIQUE_MIN/TWO_MIN/TRIPLE_MIN`;
4. it determines `delta_p`.

Therefore the valuation-side Tropical packet

`(unordered alpha_p, delta_p, valuation tie type)`

adds **zero carrier-invariant information** beyond exact `Q_orb`.

Classification:

`W_VP_DELTA_AND_TIE_REDUNDANT_WITH_Q_ORB`.

This does not make raw `W_COORD` redundant. The already accepted matched control remains decisive:

`x=(-2,-2,0,2,1,1)`,
`y=(-2,-1,-1,2,2,0)`

have the same `Q_orb=(-4,0,0)` and the same `rho` carrier-orbit `((0,1,1),0)`, but raw complementary-pair gaps are `0` and `3`. Thus raw pair-sum information is genuinely independent of the Johnson arithmetic packet.

## 3. The second exact collapse: full raw pair sums already contain `rho`

For one complementary pair `(a,b)`,

`a-b == a+b (mod 2)`.

Hence the three mod-2 components of `rho` equal `h_i mod 2`. Also

`sum(x)=h1+h2+h3`,

so the mod-3 component equals `sum(H) mod3`.

Thus labeled `H` determines all of `rho`; after quotienting by the carrier-induced `S3` on complementary pairs, the orbit of `H` determines the corresponding orbit of `rho`.

Classification:

`RHO_REDUNDANT_ON_FULL_RAW_PAIR_SUM_PACKET`.

This is a redundancy statement only at the full-`H` level. A scalar `delta_coord` remains far more lossy than `H`.

## 4. Exact pair-symmetric completion and the missing alignment state

Define the aligned pair packet

`K(x)=multiset{(h_i,t_i): i=1,2,3}`.

For one record `(h,t)`, the original complementary coordinates are the roots of

`z^2-hz+t=0`.

Therefore `(h,t)` is integer-representable exactly when

`D=h^2-4t=d^2 >= 0`

for an integer `d` with `h == d (mod 2)`, and then the unordered pair is

`{(h-d)/2,(h+d)/2}`.

Now let `Gamma` be generated by the frozen unsigned carrier `S4` and the accepted complement involution `C`. Exact permutation generation gives

`|Gamma|=48`

and

`Gamma = C2 wr S3`,

the full symmetry group that permutes the three complementary pairs and independently swaps the two entries inside each pair.

It follows symbolically that:

**Pair-completion theorem.**
`K(x)=K(y)` if and only if `x` and `y` lie in the same `Gamma` orbit.

So `K` is a complete invariant of the declared six-coordinate representation modulo the already accepted carrier+complement symmetry. This is not a new P000 geometry; it is the elementary symmetric reconstruction of each existing complementary pair.

The separate accepted packets do **not** contain the relative matching between the unordered `H` multiset and the unordered `T={t_i}` multiset recovered from `Q_orb`. There are at most `3!=6` matchings, each checked by the discriminant gate above. This bound is sharp.

A small collision already proves alignment is real information:

- `x=(-2,-2,1,0,1,-2)`;
- `y=(-2,-1,-1,0,2,-2)`.

Both have

- `H_multiset=(-4,-1,1)`;
- `T_multiset=(-2,0,4)`;
- `Q_orb=(-6,2,6)`;
- `rho_orbit=(0,1,1,2)`;
- raw `delta_coord=3`;

but

- `K(x)=((-4,4),(-1,-2),(1,0))`;
- `K(y)=((-4,4),(-1,0),(1,-2))`;

and they are in different `Gamma` orbits.

The six-way upper bound is also attained: for

`H=(-3,3,9)`, `T=(-70,-10,0)`

all six relative matchings pass the exact integer-root gate and give six distinct `Gamma` orbits.

Hence the exact finite extra state at the **full-H + Q_orb** level is a relative alignment selector with at most six values. In binary coding this requires at most three bits, and a six-state fiber genuinely occurs.

Classification:

`RELATIVE_ALIGNMENT_IS_THE_ONLY_FINITE_EXTRA_STATE_AT_FULL_H_AND_Q_ORB_LEVEL`.

## 5. 2-primary and 3-primary interaction

### 5.1 Exact 2-primary compatibility

For one complementary pair `(a,b)`, put

`alpha=v_2(ab)`, `r=(a-b) mod2`.

Then exactly:

- `alpha=0 => r=0` (both entries are odd);
- `alpha=1 => r=1` (one entry is odd and the other has exact 2-adic order 1);
- `alpha>=2 => r in {0,1}`, and both values occur.

Sufficiency is explicit: put all 2-adic order on one coordinate for `r=1`; for `alpha>=2`, split positive 2-adic order across both coordinates for `r=0`. By CRT the pair sum modulo `3` can be chosen independently inside every allowed `(alpha,r)` case. Thus the `Z/3` coordinate of `rho` adds no further 2-adic tie restriction.

### 5.2 3-primary residue

At `p=3`, the mod-2 components of `rho` are CRT-independent of 3-adic valuation data. The mod-3 total residue does not select the valuation tie regime either. The deterministic certificate contains explicit states realizing all three total residues `0,1,2` inside each of `UNIQUE_MIN`, `TWO_MIN`, and `TRIPLE_MIN`.

So the real primary interaction is the exact local `p=2` parity law above; there is no analogous tie-selection law coming from the Johnson `Z/3` residue.

## 6. Pfaffian valuation: the exact safe/ambiguous table

The accepted unique-min theorem remains:

`UNIQUE_MIN => v_p(Q)=min(alpha_p)`.

The joint analysis sharpens the `delta_p=0` side by separating two-minimum from triple-minimum cases.

### 6.1 Two minima: ambiguous for every prime

For any prime `p`, choose an even integer `a` divisible by `p`, and set

`b=1`, `c=1-a`.

Define

`x=(a,a,a,a,b,c)`,
`y=(a,a,a,a,c,b)`.

Then `x` and `y` have the same complete `Gamma`-invariant pair packet `K`, hence the same `Q_orb`, `rho` orbit, unordered `alpha_p`, tie type, and all carrier/complement-invariant data classified above. Their valuation pattern is `TWO_MIN`. But

`Q(x)=0`,
`Q(y)=2*a^2 != 0`.

Thus no `Gamma`-invariant joint packet can select the oriented scalar `Q` or its valuation in the two-minimum regime.

### 6.2 Triple minimum at `p=2`: exact, not ambiguous

If all three `v_2(t_i)` equal `m`, factor `2^m`. The three normalized signed summands are odd, and

`odd - odd + odd`

is odd. Therefore

`TRIPLE_MIN at p=2 => v_2(Q)=m`

and in particular `Q != 0`.

This is strictly sharper than the old scalar condition `delta_p=0`.

### 6.3 Triple minimum at odd `p`: ambiguous

For any odd prime not dividing `2`, take

`a=2`, `b=1`, `c=-1`

and the same two states above. All three product valuations are `0`, so both states are `TRIPLE_MIN`, while

`Q(x)=0`,
`Q(y)=8`.

Hence odd-prime triple ties remain cancellation-ambiguous.

The exact table is therefore:

| valuation tie type | `p=2` | odd `p` |
| --- | --- | --- |
| unique minimum | exact `v_p(Q)=m` | exact `v_p(Q)=m` |
| exactly two minima | ambiguous | ambiguous |
| triple minimum | exact `v_2(Q)=m` | ambiguous |

This identifies the smallest extra information needed to recover oriented `Q`: one must add an **orientation/negative-pair selector** beyond the `Gamma`-invariant packet. Generically `Q_orb` contains three candidate scalar values, so this selector is three-valued (at most two binary bits), not a hidden valuation refinement.

## 7. Information-cost atlas

The exact costs are now:

- `Q_orb`: lossless for the unordered complementary-product triple; loses which product occupies which oriented pair/negative slot.
- valuation `delta_p` and tie type: **0 extra cost** once `Q_orb` is retained on `D_p`.
- full raw `H`: genuinely independent from `Q_orb`; it also makes `rho` redundant.
- scalar raw `delta_coord`: lossy compression of `H`, so it cannot replace `H`.
- separate full `H` orbit + `Q_orb`: leaves at most six `Gamma`-orbits; six is sharp.
- relative `H↔T` alignment: a finite at-most-six-state completion; after adding it, `K` is complete modulo `Gamma`.
- oriented scalar `Q`: still not a `Gamma`-invariant observable; selecting it generically needs a three-way negative-pair/orientation state.

Therefore Johnson and Tropical should be kept coupled only at the **raw pair-sum / product-alignment** layer. Their p-adic gap classifiers should not be carried as separate state once exact `Q_orb` is available.

## 8. Verification

Task-local exact checker:

`research_checks/P000_SIX_AXIS_JOHNSON_TROPICAL_ARITHMETIC_INTEGRATION_CHECK_20260830.py`

Fresh local execution:

`LOCAL_DETERMINISTIC_PASS checks=155667`.

The checker verifies:

1. exact `S4` order `24`, `Gamma` order `48`, and `Gamma=C2 wr S3`;
2. `Q_orb` inversion on all `5^6=15625` states in `[-2,2]^6`;
3. `rho` reconstruction from `H` on that census;
4. pair-packet completeness modulo `Gamma` on that census plus the symbolic quadratic-root proof gate;
5. accepted raw-Tropical nonredundancy;
6. the alignment collision and sharp six-orbit fiber;
7. valuation redundancy for `p=2,3,5` on the full nonzero `{-2,-1,1,2}^6` census;
8. unique-min and `p=2` triple-min valuation laws over exact bounded product triples;
9. two-min and odd-triple cancellation counterexamples;
10. the exact 2-primary parity law and 3-primary tie-residue regressions.

Method-harvest disposition:

`RESULT_ONLY / TASK_LOCAL_EXACT_CERTIFICATE`.

No new general-purpose mechanism was created, so the universal tool-reuse gate resolves as `NOT_APPLICABLE_NO_NEW_GENERAL_MECHANISM`.

## 9. Scope guards and terminal recommendation

Nothing here promotes:

- tropical/valuated-matroid language to native P000 geometry;
- `Lambda^2`, Johnson sectors, or the carrier `S4` to native dimension;
- a factorization mechanism;
- a Full-Cell lift;
- `v_p(0)=+infinity` semantics;
- Working Truth or Foundation status.

The hard target is satisfied. The result gives both an exact positive integration (`K` completeness modulo declared symmetry) and exact redundancy/no-go boundaries (`delta_p` redundancy, six-way alignment cost, and tie-dependent cancellation limits).

Next action: Driver review this immutable Result. Do not publish a downstream successor from the researcher lane.
