# P000 Native Mixed Star / Cross-Block Rotation V7 — Research Return

Researcher-ID: `EM-P000NATFCC7-7A01E3`

Task: `RS-P000-L1-NATIVE-CARRIER-CONTACT-BRIDGE`

Publication: `TP2-AA2BF67633F3F44D0D87`

Terminal verdict: `EXACT_NATIVE_PRIMITIVE_OBSTRUCTION_PROVED`

Hard target: `P000_PRIMITIVE_MIXED_STAR_AND_CROSS_BLOCK_FULL_STATE_ROTATION_EXACTLY_CONSTRUCTED_OR_OBSTRUCTED = OBSTRUCTED_IN_CURRENT_NATIVE_SIGNATURE`

## 1. Frozen native semantics

This run did not reopen signed-K4 switching, H1, double-cover classification, carrier word enumeration, or the old passive C2 route.

The recoverable P000 native Cell construction remains blockwise. For each base address
`r=(a,b,c)` with nonnegative coordinates and `min(r)=0`, the established Cell supports are

- `I_A={1,2,3}`,
- `I_B={4,5,6}`.

The six-axis full state may contain both factors, schematically `X6=Cell_A x Cell_B`, but the product is not a constructor whose codomain is one mixed Cell.

The FCC/K4 readout has the exact observation stars

- `J_A={1,2,3}`,
- `J_B={1,4,5}`,
- `J_C={2,4,6}`,
- `J_D={3,5,6}`,

under `1->AB, 2->AC, 3->AD, 4->BC, 5->BD, 6->CD`. This readout is not used as an inverse constructor from carrier data to native geometry.

## 2. Mixed-star obstruction

### Theorem 1 — block-purity invariant

Every Cell-valued term generated by the currently recoverable native constructor language has axis support in `{I_A,I_B}`.

Proof: primitive Cell constructors are A-block or B-block. Address/stack/payload changes preserve that block type. Within-block reindexing preserves each block. Whole-factor exchange swaps the two blocks. Product formation may produce a full state containing both factors, but it does not eliminate the product into a new Cell-valued term. Induction over the available Cell-valued constructors therefore leaves support equal to `I_A` or `I_B`. QED.

Since `J_B={1,4,5}` is neither `I_A` nor `I_B`, it is not a constructible native Cell in the frozen signature.

At carrier level `J_A∩J_B={1}` is exact. At native level the gluing expression is undefined, not false: one input (`J_B`) lacks native Cell typing. Declaring FCC adjacency on `J_B` would therefore be the forbidden step “observation window -> native geometric slice”.

Obstruction class: `MIXED_SLICE_RELATION_MISSING`.

## 3. Cross-block b obstruction

Give the current native/clone-product motion grammar a deliberately generous upper envelope

`W=(S3 x S3) semidirect C2`, `|W|=72`,

where the two S3 factors arbitrarily permute axes inside `I_A` and `I_B`, and C2 may exchange the two blocks as wholes.

The required axis action is

`b=(2 4)(3 5)`, fixing `1,6`.

In zero-based checker form:
`B_TARGET=(0,3,4,1,2,5)`.

It satisfies `b^2=id` and sends the known native star to the mixed observation:
`b(I_A)=J_B`.

### Theorem 2 — no current native b lift

`B_TARGET` is not in `W`.

Proof: every element of W maps `I_A` either to `I_A` or to `I_B`. But `B_TARGET(I_A)=J_B={1,4,5}`, which is neither. Hence `B_TARGET` is outside W. QED.

The deterministic checker also enumerates all 72 elements of W and confirms exact non-membership.

Crucially, the physical K4 carrier S4 action of the vertex transposition `(A B)` induces exactly the same six-axis permutation `(2 4)(3 5)`. Thus:

`carrier b exists exactly`, while `native b lift is not expressible`.

The failure occurs before payload/support/inverse preservation can be checked: there is no typed native transform term with the required base-state axis action. A passive sign/deck bit cannot repair a missing base motion.

Obstruction class: `NO_AXIS_REFINED_CROSS_BLOCK_STATE_TRANSFORM`.

## 4. Lift square and gated relations

Carrier transport gives the exact lower arrow `b(J_A)=J_B`, but the native upper arrow is absent:

`native Cell_A -- R~_b ? --> native mixed Cell`
`      | Phi                   | Phi`
`      v                       v`
`carrier J_A ------ b ------> carrier J_B`.

Therefore this run does not fabricate `R~_a`, `J_C/J_D` native orbit completion, or native-strength relations `R~_a^3`, `R~_b^2`, `(R~_a R~_b)^4`. The taskbook gates those steps on successful native construction of `J_B` and `R~_b`.

`NATIVE_RELATION_RESIDUE = NOT_REACHED`.

## 5. Minimal repair interface

The exact missing information is native, not carrier-level.

A single sufficiently strong new primitive could remove both failures:

`BMix_b`: an axis-refined native full-state primitive whose domain contains the state carrying `Cell_A(r)`, whose axis action is `(2 4)(3 5)`, whose inverse is defined, and whose transport law makes the image of the native `J_A` Cell relation a genuine native Cell relation on `J_B` while preserving payload/support typing.

Equivalently it can be factored into:

1. a mixed Cell constructor/transport relation `mu_r` for local axes `(E1,E4,E5)` with a gluing law on shared `E1`; and
2. a cross-block full-state transform `R~_b` with axis action `(2 4)(3 5)`.

This return does not assert either primitive already exists. It identifies the smallest new native capability that must be derived from deeper stack/handle relations or explicitly added to the native signature.

## 6. Time trace

`t=0`: native `Cell_A(r)`, support `J_A`, readout `Phi(X0)=J_A`.

`t=1`: carrier transposition `(A B)` gives readout `J_B`, but required native `X1=R~_b(X0)` is undefined because the target axis action is outside W and the mixed Cell has no constructor.

Thus there are not yet two native rotation paths to compare.

## 7. Deterministic checker

Path:
`research_checks/P000_NATIVE_MIXED_STAR_CROSS_BLOCK_ROTATION_V7_CHECK_20260829.py`

The checker uses only the Python standard library and exact finite enumeration. It covers:

- pure native Cell positive controls at multiple base addresses;
- mixed `J_B,J_C,J_D` native negative controls;
- exact K4/FCC star readout for `J_B`;
- no-quotient regression;
- all 72 elements of the block/whole-factor envelope W;
- target b non-membership in W and `b^2=id`;
- exact carrier S4 six-edge action and carrier-b identification;
- old whole-factor `rho` outside physical carrier S4;
- `J_A/J_B` readout overlap `{1}` with native gluing type failure;
- frozen split `S4 x C2` regression in the all-negative carrier gauge.

Local execution:

```text
PASS
carrier_JB_valid=True; native_JB_constructible=False
native_transform_envelope_order=72; desired_b_in_envelope=False
desired_b_squared=identity; carrier_b_exists=True; native_b_lift_typechecks=False
overlap_JA_JB_readout={1}; native_overlap=UNDEFINED
minimal_obstruction=MIXED_SLICE_RELATION_MISSING + NO_AXIS_REFINED_CROSS_BLOCK_STATE_TRANSFORM
carrier_split_S4xC2_regression=True; no_quotient_regression=True
```

## 8. Acceptance disposition

A primitive native semantics: PASS.

B `J_B` geometric slice: EXACT OBSTRUCTION.

C cross-block `R~_b`: EXACT OBSTRUCTION.

D `J_A <-> J_B`: EXACT LIFTING FAILURE; carrier only.

E `R~_a` / orbit completion: correctly NOT REACHED.

F native relations: correctly NOT REACHED.

G minimal-obstruction theorem: PASS.

H time trace: PASS.

I deterministic checker: PASS.

## 9. Conclusion

Gen6 showed that carrier/cohomology lifting does not manufacture the missing native motion. Gen7 strengthens that to an expressibility theorem:

`current native Cell language is block-pure`

and even the generous current motion envelope is contained in

`(S3 x S3) semidirect C2`,

whereas `J_B` is mixed and the required `b` is a partial block mixer.

Hence the exact boundary is:

**FCC/K4 already contains the mixed star and the b permutation; the frozen P000 native state semantics do not yet contain the primitive that makes either one geometric.**

This is a constructive obstruction, not a refutation of a richer P000 theory. A successor should derive `BMix_b` from deeper native stack/handle relations or explicitly extend the native signature, then re-run gluing, payload/support, inverse, and native-relation checks. Carrier algebra should remain frozen.
