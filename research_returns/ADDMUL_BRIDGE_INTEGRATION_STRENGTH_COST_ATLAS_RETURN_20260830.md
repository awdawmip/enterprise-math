# ADDMUL Bridge Integration — Strength / Information-Cost / Composability Atlas

Researcher-ID: `EM-AMINT-8E3C72`  
Task: `RS-ADDMUL-BRIDGE-INTEGRATION-STRENGTH-COST-ATLAS`  
Publication: `TP2-970ED4BA261B4270FCB6`  
Execution record: `ER-CDED03A1489613EE6B8C`

## 1. Terminal verdict

`SUCCESS / FINITE_SET_OF_INCOMPARABLE_BRIDGE_CLASSES_CLASSIFIED / OPERATION_SAFE_COMPOSITION_REQUIRES_EXPLICIT_DYNAMIC_STATE_AND_COST_LOWER_BOUND`

Hard target:

`ADDMUL_BRIDGE_STRENGTH_INFORMATION_COST_COMPOSABILITY_ATLAS_CLASSIFIED`.

The accepted first-wave routes do not collapse to one mechanism. The exact integration result is:

1. **A1/A3/A4 form an exact algebraic composition cluster.**
2. **A2 is a prime-indexed defect extension.** At `p=2` it is exactly A1 with sign reversed; for odd `p` the anti-diagonal is a genuine lost-product fiber.
3. **A5 is a dynamic lossy-refinement class.** Exact addition in a compressed valuation state requires unbounded residue-depth refinement.
4. **A6 is an invertible typed spectral-coordinate class, but exact information recovery is not the same as operation intertwining.**

There is therefore a common operation-safe **audit/state grammar**, but no nontrivial fixed finite **common bridge state** across A1–A6.

No Working Truth, Foundation, L4, canonical-theorem, or global-tool promotion is claimed.

## 2. Frozen inputs and reuse gate

Only Driver-accepted A1–A7 returns and `BRIDGE_AUDIT_PACKET_V1` are used.

Existing mechanisms are reused:
- mixed finite differences / A1 cross-effects;
- `src/enterprise_math/precision.py`;
- existing precision-holonomy defect transport;
- existing operation quotient/fiber machinery;
- accepted A5 generation-versus-transport semantics.

No parallel generic precision, quotient, valuation, holonomy, or finite-difference engine is introduced.

Method harvest: `RESULT_ONLY`.

## 3. Common signature

The A7 audit packet is extended by one cost axis, `PRECISION_REFINEMENT`.

Task-local grammar:

`OPERATION_SAFE_BRIDGE_STATE_GRAMMAR_V1`

Fields:
- mechanism / strength ceiling;
- domain / codomain;
- source addition and multiplication transport;
- fiber/kernel/injectivity;
- closure and exceptional locus;
- hidden coordinates;
- partiality;
- error;
- reconstruction rule;
- precision/refinement;
- cost vector
  `DOMAIN / COLLISION / HIDDEN_COORDINATE / PARTIALITY / ERROR / PRECISION_REFINEMENT`.

This is an audit grammar, not a common target algebra.

### A1

`EXACT_DEFECT_RECONSTRUCTION` on `Z`:

\[
Q_2(x+y)-Q_2(x)-Q_2(y)=xy.
\]

Addition stays native and multiplication is exactly reconstructed from one normalized unary quadratic observable `Q_2`. Error is zero. At finite precision, the top arity reuses existing graded monomial carry; lower arities require filtered triangular correction.

### A2

\[
\delta_p(n)=\frac{n-n^p}{p},\qquad
D_p(x,y)=\delta_p(x+y)-\delta_p(x)-\delta_p(y).
\]

At `p=2`,

\[
\delta_2=-Q_2,\qquad D_2(x,y)=-xy.
\]

For odd `p`, `(s=x+y,D_p)` reconstructs `xy` on the semantic image when `s\ne0`, but

\[
D_p(a,-a)=0
\]

for every `a` while the product `-a^2` varies. Thus `s=0` is an infinite product-loss fiber unless extra state or an explicit exceptional branch is retained.

### A3

\[
F_c(x,y)=x+y+cxy,\qquad T_c(x)=1+cx,
\]

with

\[
T_c(F_c(x,y))=T_c(x)T_c(y).
\]

Fibers are exactly `Ann_R(c)`. The monoid law is global; inversion is restricted to the locus where `1+cx` is a unit. Finite formal jets are operation-safe only on the already-declared nilpotent cell; naive polynomial truncation is not accepted.

### A4

For finite divisor-closed `S`, the integral ghost map is triangular and injective. Its valid image is a subring of `Z^S`, so Witt addition and multiplication become componentwise `+` and `*`.

Arbitrary integer ghost packets are not valid. The recursive divisibility/Dwork image gate and divisor-closed locality must remain explicit. Composite indices are irreducible state: changing only `a_6` changes `g_6` by `6` while the prime-power skeleton stays unchanged.

### A5

\[
V(xy)=V(x)+V(y)
\]

is exact, whereas addition is

\[
V(x+y)=\min(V(x),V(y))+K(x,y).
\]

The hidden correction is generated by normalized units at tied-valuation walls. Exact staged operation requires the accepted split

\[
E_{\rm parent}=\tau+\kappa,
\]

where `tau` transports inherited defect and `kappa` generates new cancellation.

The decisive lower bound is

\[
v_p(1)=v_p(p^k-1)=0,\qquad
\kappa_p(1,p^k-1)=k.
\tag{A5-LB}
\]

Cancellation depth is therefore unbounded at one fixed valuation input state.

### A6

After adding the zero atom, the Gauss transition is full rank. Omitting the zero atom is equivalent to restricting to the exact codimension-one Fourier image

\[
\sum_t A_t=0.
\]

But the natural transition does not intertwine multiplicative convolution with additive-convolution Fourier multiplication. Hence

`ZERO_COLLISION != OPERATION_INTERTWINING`.

This is an exact separation between information invertibility and operation transport.

## 4. Exact algebraic cluster

### Theorem 4.1 — A1 equals the `p=2` core of A2 up to sign

For every integer `n`,

\[
\delta_2(n)=\frac{n-n^2}{2}=-Q_2(n).
\]

Therefore

\[
D_2(x,y)=-\bigl(Q_2(x+y)-Q_2(x)-Q_2(y)\bigr)=-xy.
\]

This is the only exact redundancy found inside the first-wave routes.

### Theorem 4.2 — A1 evaluates A3 exactly over `Z`

\[
F_c(x,y)
=x+y+c\bigl(Q_2(x+y)-Q_2(x)-Q_2(y)\bigr).
\]

So native addition plus the A1 quadratic observable evaluates every integer-parameter A3 law exactly.

### Theorem 4.3 — A1 composes exactly with A4

If valid ghost packets are `g=G_S(a)` and `h=G_S(b)`, then A4 gives the componentwise product `gh` for Witt multiplication. Coordinatewise A1 gives

\[
Q_2(g_n+h_n)-Q_2(g_n)-Q_2(h_n)=g_nh_n.
\]

The reconstructed product vector is valid because the A4 ghost image is a subring.

### Theorem 4.4 — A3 composes exactly with A4

For valid `g,h`,

\[
F_c(g,h)=g+h+cgh
\]

is again in the integral ghost image because that image is a subring.

The operation-safe common state grammar can therefore carry A3 and A4 together, but their gates remain typed: `Ann(c)` does not replace the A4 integral-image/locality gate, and the A4 gate does not replace A3's inverse locus.

## 5. A1/A2 versus A5: generated and transported defect

A1 and A2 are unary coboundary defects, so their raw associativity cocycle is exact. A5's `kappa_p` is different: it is generated by hidden unit data on tied-valuation walls and must be transported through later merges.

A task-local common defect grammar is:

`DEFECT_STATE=(BASE, GENERATED_DEFECT, TRANSPORTED_DEFECT, DETAIL_BUDGET, OVERFLOW_TYPE)`.

### Theorem 5.1 — no fixed residue-depth state gives global exact A5 addition

Fix a prime `p` and any finite detail budget `D`. Take

\[
x=1,\qquad y=p^k-1
\]

with `k>=D`. The input valuation state is always `(0,0)`, but the output cancellation excess is `k`.

If normalized units are known only modulo `p^D`, the condition

\[
u+w\equiv0\pmod{p^D}
\]

certifies only `kappa>=D` (or exact cancellation), not the exact result.

Therefore an operation-safe compressed A5 state must either:
1. refine dynamically;
2. expose `DEPTH_AT_LEAST_D / OVERFLOW`; or
3. retain enough full-source information that the collapse is no longer a genuine compression.

A2's finite-log equal-valuation footprint does not pay this A5 cost; A2 and A5 therefore compose only with extra state.

## 6. A6 pressure test

In `F_3`, let `chi_0` and `chi_1` be the two normalized multiplicative characters. Their cross multiplicative convolution is zero by character-idempotent orthogonality:

\[
\chi_0 *_\times \chi_1=0.
\]

At a nonzero additive frequency both additive Fourier columns are nonzero, so their pointwise product is nonzero.

Thus the natural Gauss transition cannot convert multiplicative convolution into additive-convolution Fourier multiplication. Full rank does not alter this obstruction.

The zero atom is nevertheless an exact cost trade:
- keep `delta_0` and get a full `p`-dimensional invertible transform; or
- omit it and keep the codimension-one image constraint `sum_t A_t=0`.

Generic reconstruct-and-reapply Fourier expansion is not counted as `COMPOSES_EXACTLY`; otherwise every invertible basis change would vacuously compose with every formula.

## 7. Pairwise composability matrix

| Pair | Verdict | Reason |
|---|---|---|
| A1–A2 | `COMPOSES_EXACTLY` | `p=2` core is identical up to sign; odd `p` adds defect/valuation data. |
| A1–A3 | `COMPOSES_EXACTLY` | A1 reconstructs the `cxy` term exactly. |
| A1–A4 | `COMPOSES_EXACTLY` | A1 reconstructs componentwise ghost multiplication. |
| A1–A5 | `COMPOSES_WITH_EXTRA_STATE` | A5 needs dynamic unit/residue refinement. |
| A1–A6 | `INCOMPATIBLE_OR_NOT_NATURAL` | A6 operation non-intertwining survives. |
| A2–A3 | `COMPOSES_WITH_EXTRA_STATE` | Odd-prime `s=0` needs an exact product/equivalent branch. |
| A2–A4 | `COMPOSES_WITH_EXTRA_STATE` | Odd-prime singular branches and A4 image/locality gates remain independent. |
| A2–A5 | `COMPOSES_WITH_EXTRA_STATE` | Finite-log data does not replace unbounded cancellation refinement. |
| A2–A6 | `INCOMPATIBLE_OR_NOT_NATURAL` | Semantic-image inversion and natural convolution semantics do not yield one preserved law. |
| A3–A4 | `COMPOSES_EXACTLY` | The valid ghost image is a subring. |
| A3–A5 | `COMPOSES_WITH_EXTRA_STATE` | `v_p(x+y+cxy)` needs unit-residue cancellation data. |
| A3–A6 | `INCOMPATIBLE_OR_NOT_NATURAL` | Pullback along `T_c` does not remove A6's operation obstruction. |
| A4–A5 | `COMPOSES_WITH_EXTRA_STATE` | Composite ghost state and valuation unit state are independent costs. |
| A4–A6 | `INCOMPATIBLE_OR_NOT_NATURAL` | Mod-`p` loses `a_p` when `p` is retained; keeping lifts still gives no natural intertwiner. |
| A5–A6 | `INCOMPATIBLE_OR_NOT_NATURAL` | Finite-field spectral data supplies no canonical valuation cancellation refinement. |

No full route pair is classified as redundant; only the A1/A2(`p=2`) sub-core is.

## 8. Information-cost atlas

The six cost axes are not collapsed to one scalar.

### Exact exchangeable costs

1. **A6:** zero atom ↔ codimension-one image constraint.
2. **A3:** `Ann(c)` collision ↔ restricting `c` to a regular/unit locus.
3. **A2 odd:** anti-diagonal domain exclusion ↔ retaining `xy` or an equivalent exact exceptional-branch coordinate.

### Nonexchangeable costs

1. **A4 composite index:** prime-only refinement cannot recover the `a_6` witness.
2. **A5 dynamic depth:** no fixed residue depth replaces unbounded `kappa`.
3. **A6 operation law:** full rank/injectivity cannot pay the missing convolution-intertwiner cost.

Every A1–A6 route is exact on its declared scope, so `ERROR=0` throughout this atlas. Incomparability comes from domain, collision, hidden state, partiality, and refinement rather than approximation.

## 9. Minimal common kernel theorem

### Positive subset result

A1/A3/A4, with A2 at `p=2`, share the exact algebraic core

`ADDITIVE_CARRIER + NORMALIZED_QUADRATIC_REFINEMENT + TYPED_IMAGE_GATE_WHERE_REQUIRED`.

A4 still requires its own ghost coordinates and image/locality certificate.

### Negative full-pool result

No nontrivial fixed finite state can preserve all accepted strengths across A1–A6:

- A5 forces unbounded dynamic refinement by (A5-LB);
- A4 forces irreducible composite-index information;
- A6 proves that collision-free invertibility does not imply operation transport.

The only universal escape is to retain the full source plus route-specific metadata and reconstruct/reapply every operation. That is a trivial embedding, not a common bridge compression.

Therefore:

\[
\boxed{
\text{COMMON AUDIT GRAMMAR EXISTS;\quad
NONTRIVIAL FIXED FINITE COMMON BRIDGE STATE DOES NOT.}
}
\]

The first-wave classes are:

1. `EXACT_ALGEBRAIC_CLUSTER={A1,A3,A4}`, with A2(`p=2`) overlapping;
2. `PRIME_DEFECT_EXTENSION={A2\ odd\ p}`;
3. `DYNAMIC_LOSSY_REFINEMENT={A5}`;
4. `SPECTRAL_LINEAR_COORDINATE={A6}`.

This is a finite incomparability classification, not a claim that no future typed/dynamic interface can connect the classes.

## 10. Exact certificate

Checker:

`research_checks/ADDMUL_BRIDGE_INTEGRATION_STRENGTH_COST_ATLAS_CHECK_20260830.py`

Structured atlas:

`research_artifacts/ADDMUL_BRIDGE_INTEGRATION_STRENGTH_COST_ATLAS/atlas.json`

Certificate:

`research_artifacts/ADDMUL_BRIDGE_INTEGRATION_STRENGTH_COST_ATLAS/exact_regression_certificate.json`

Deterministic standard-library exact arithmetic result:

`PASS / 22870 exact checks`.

The checker freezes:
- A1/A2 `p=2`;
- A1→A3 and A1/A3 on A4 ghost packets;
- odd-prime A2 anti-diagonal loss;
- A3 annihilator fibers;
- A4 image/composite-index witnesses;
- A5 unbounded cancellation depth and fixed-depth overflow;
- A4→mod-`p` information loss at index `p`;
- the `F_3` A6 operation non-intertwining witness;
- the A6 zero-atom/hyperplane cost trade.

Finite regression is a certificate; the all-domain conclusions above are symbolic and rely on the accepted route theorems.

## 11. Scope firewall and next control-plane step

This return does not claim primitive elimination, a global odd-prime product reconstruction, global group structure for A3, validity of arbitrary ghost packets, valuation-window exact addition, or an A6 convolution-algebra isomorphism.

Do not open six route-specific continuations. A successor is justified only for a concrete downstream consumer needing either:
- `DYNAMIC_DEFECT_STATE`, implemented using existing precision-holonomy machinery; or
- `ALGEBRAIC_CLUSTER_ADAPTER`, consuming A1/A3 on A4 ghost packets while preserving A4 gates.

Do not promote `OPERATION_SAFE_BRIDGE_STATE_GRAMMAR_V1` to a global tool solely by renaming it.
