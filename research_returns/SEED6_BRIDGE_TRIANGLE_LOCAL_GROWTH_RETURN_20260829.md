# Seed-6 Bridge Triangle Local Growth — Research Return

Task: `RS-SEED6-BRIDGE-TRIANGLE-LOCAL-GROWTH`  
Publication: `TP2-262052627FA7DD9E5FF7`  
Researcher: `EM-S6T-A65497`  
Claim: `chatgpt-s6t-20260829-2042-a65497`  
Execution record: `ER-4C84BFAF80DD68EA2839`  
Hard target: `SEED6_LOCAL_BRIDGE_TRIANGLE_GEOMETRY_CLASSIFIED`  
Terminal verdict: `SUCCESS`

## 1. Main classification

For every prime `r>3`, set

\[
T_r=\{6,2r,3r\},\qquad L=6r=2\cdot3\cdot r.
\]

Because `L` is squarefree with exactly three prime atoms `{2,3,r}`, its divisor lattice is the Boolean lattice `B3`. The three elements of `T_r` are exactly its three coatoms:

\[
6=L/r,\qquad 2r=L/3,\qquad 3r=L/2.
\]

Therefore the strongest local structure is not a numerical-distance pattern and not the square-product identity. It is the **rank-2 coatom layer of a three-atom Boolean divisor lattice**.

The pairwise meet/join laws are

\[
\gcd(6,2r)=2,\quad \gcd(6,3r)=3,\quad \gcd(2r,3r)=r,
\]

and

\[
\operatorname{lcm}(6,2r)=\operatorname{lcm}(6,3r)=\operatorname{lcm}(2r,3r)=6r.
\]

This is `EXACT_STRUCTURAL`.

## 2. Two equivalent local models

### A. Object-only gcd-labelled triangle

Vertices are `6,2r,3r`. Each object-object edge is labelled by the gcd of its endpoints:

- `6 -- 2r` carries `2`;
- `6 -- 3r` carries `3`;
- `2r -- 3r` carries `r`.

Inside the frozen family, the edge labels reconstruct each object:

\[
6=2\cdot3,\qquad 2r=2\cdot r,\qquad 3r=3\cdot r.
\]

Hence the exact gcd-edge-labelled triangle and the carrier-incidence type determine one another.

### B. Carrier/coatom Levi incidence graph

Use carrier nodes `{2,3,r}` and object nodes `{6,2r,3r}`; connect `p` to `n` iff `p|n`. The graph is the 6-cycle

\[
2-6-3-3r-r-2r-2.
\]

The carrier/object bipartition must be remembered. Forgetting it adds rank-reversing graph symmetries that are not multiplicative carrier automorphisms.

Model A is the compact interface; Model B is the faithful divisor-lattice interface.

## 3. Valuation-support and integral lattice signature

With carrier rows `(2,3,r)` and object columns `(6,2r,3r)`, the incidence matrix is

\[
A=
\begin{pmatrix}
1&1&0\\
1&0&1\\
0&1&1
\end{pmatrix}.
\]

It has

\[
\det(A)=-2,\qquad \operatorname{SNF}(A)=\operatorname{diag}(1,1,2).
\]

Thus the lattice generated by the three coatom exponent vectors has index `2` in `Z^3`. Since every column has even coordinate sum, that lattice is exactly

\[
\{(a,b,c)\in\mathbb Z^3:a+b+c\equiv0\pmod2\}.
\]

This is `EXACT_STRUCTURAL`. It is a standard algebraic footprint of the incidence pattern, not a novelty claim, but it is a useful integral interface for later gluing.

## 4. Automorphism classification

The local symmetry depends on what data are retained.

| remembered structure | automorphism count |
|---|---:|
| object triangle, unlabeled | `6` |
| object triangle, edge types `fixed/fixed/new` | `2` |
| object triangle, exact gcd labels `2,3,r` | `1` |
| Levi `C6`, rank forgotten | `12` |
| Levi `C6`, carrier/object rank preserved | `6` |
| rank-preserving Levi cell with seed coatom `6` fixed | `2` |
| exact carrier labels fixed | `1` |

So the normalized Seed-6 cell is naturally rooted but not canonically oriented. If the seed carriers `{2,3}` are unordered, the reflection swapping `2<->3` and `2r<->3r` survives. An ordered boundary `(2,3)` is extra structure.

## 5. Candidate invariant audit

1. **Boolean coatom / meet-join signature** — `EXACT_STRUCTURAL`.  
   The three objects are exactly the three coatoms of `B3`; pairwise gcds are the atoms and pairwise lcms are the common top.

2. **gcd carrier-incidence reconstruction** — `EXACT_STRUCTURAL`.  
   Each vertex is the product of its two incident gcd labels.

3. **valuation-support matrix and SNF `(1,1,2)`** — `EXACT_STRUCTURAL`.  
   The support vectors form the three weight-2 vectors in `{0,1}^3` and generate the even-sum sublattice.

4. **fixed/fixed/new edge-type port structure** — `EXACT_STRUCTURAL`.  
   The two seed-incident edges are fixed-carrier ports; the opposite edge is the fresh-carrier port.

5. **`6(2r)(3r)=(6r)^2` as an independent invariant** — `TAUTOLOGICAL`.  
   It follows because each carrier atom occurs in exactly two coatoms. It is a checksum, not additional structure.

6. **canonical orientation without ordering the seed carriers** — `MODEL_DEPENDENT`.  
   The rooted typed cell has a reflection, so no intrinsic left/right choice exists.

7. **numerical `r` recoverable from normalized rooted typed combinatorics** — `COUNTEREXAMPLE_FOUND`.  
   `T_5={6,10,15}` and `T_7={6,14,21}` are numerically different but become typed-isomorphic after replacing the fresh prime by the role symbol `N`.

## 6. Cross-r rigidity

For any primes `r,s>3`, the maps

\[
6\mapsto6,\quad 2r\mapsto2s,\quad 3r\mapsto3s
\]

and

\[
2\mapsto2,\quad3\mapsto3,\quad r\mapsto s
\]

give an isomorphism of rooted typed carrier-incidence cells.

Hence **all isolated prime Seed-6 cells have one normalized local isomorphism type**. Any nontrivial variation across primes must enter through extra arithmetic labels or through relations among multiple cells (gluing, order, cycles, path dependence, etc.), not through the isolated normalized triangle itself.

This is the main negative boundary discovered by the task.

## 7. Exact census

The standard-library exact checker enumerates the first 500 primes `r>3`, from `5` through `3583`.

For all 500 samples it verifies:

- `L=6r` has prime atoms `{2,3,r}` with exponent `1`;
- `T_r` is exactly `{L/2,L/3,L/r}`;
- pairwise gcds are `2,3,r`;
- every pairwise lcm is `L`;
- each vertex is the product of its two incident gcd labels;
- the product-square checksum holds;
- the normalized support/incidence signature is constant;
- exact gcd-labelled triangle automorphism count is `1`;
- fixed/fixed/new typed automorphism count is `2`.

It also verifies the abstract Levi symmetry counts `12,6,2,1`.

No in-domain counterexample exists; the symbolic proof establishes the formulas for every prime `r>3`, so the census is a regression certificate rather than the basis of the proof.

Boundary guards outside scope:

- `r=2` and `r=3`: vertices collide;
- `r=25`: squarefreeness fails;
- `r=35`: `6r` has four prime atoms, so the displayed three objects are not the full `B3` coatom layer.

## 8. Minimal local interface for downstream gluing

Export the rooted rank-aware cell

\[
\mathcal I_r=(P_r,V_r,I_r,s)
\]

with

- `P_r={2,3,r}` carriers;
- `V_r={6,2r,3r}` coatoms;
- `p I_r n` iff `p|n`;
- root `s=6`.

Equivalent boundary description:

- fixed seed spine: `2 -- 6 -- 3`;
- fresh ear: `2 -- 2r -- r -- 3r -- 3`.

For role-normalized work, replace `(2,3,r)` by `(L,R,N)` and state explicitly whether `(L,R)` is ordered. The matrix `A` and SNF `(1,1,2)` may be exported as the integral algebraic companion.

No global topology or holonomy is imposed by this task.

## 9. Hard-target closure

`SEED6_LOCAL_BRIDGE_TRIANGLE_GEOMETRY_CLASSIFIED` is satisfied.

- two natural local graph/incidence models: `PASS`;
- gcd/carrier labels and reconstruction: `PASS`;
- local automorphisms: `PASS`;
- multiple candidate invariant classes: `PASS`;
- required status classification: `PASS`;
- exact 500-prime checker/census and counterexample audit: `PASS`;
- minimal global-gluing interface without prescribing topology: `PASS`.

## 10. Downstream recommendation

The next bridge-rectangle/global-gluing task should consume the local cell as a **rooted Boolean `B3` coatom-incidence block**, not as an arbitrary numerical triangle and not as a square-product coincidence.

The high-leverage downstream question is:

> When many copies of this universal rooted `B3` cell are glued, does any loop/path/global invariant survive after quotienting the local repetition forced by the incidence matrix and its index-2 parity signature?

That is the first place where genuinely new multiplicative geometry can appear.

## Reproducibility

- checker: `research_checks/SEED6_BRIDGE_TRIANGLE_LOCAL_GROWTH_CHECK_20260829.py`
- census certificate: `research_artifacts/SEED6_BRIDGE_TRIANGLE_LOCAL_GROWTH/census_summary.json`
