# CFD continuation: exact route integration boundary and reuse horizon

Task: `RS-CFD-SPECTRAL-HYBRID-20260910`  
Publication: `TP2-C3B717C14153D5AC45BF`  
Researcher: `EM-DIRECT-421478`  
Claim: `CLM-CFD-421478-20260921`, Issue 240 comment `5755123772`  
Activity: `RA-HOURLY-20260921T1140-421478`  
State: `CONTINUATION_REQUIRED / ORIGINAL_HOST_NOT_RUN / NO_SELF_ACCEPTANCE`

## Scope and inherited authority

This return continues the exact handoff frontier from `997981529b76c1b5e46ed43af91617c2c0023bd8:research_returns/RS-CFD-SPECTRAL-HYBRID-20260910_9CFD0F_20260921.md`. It preserves the published exact axis-generator proof, empty-support repair, 8,192 exhaustive seed checks, seven held-out trajectories, warm/cold timing evidence, and dense fallback. It does not relabel the finite-Galerkin harness as spectralDNS and does not mark the task DONE.

The pinned native adapter remains `3fb575716647b3f618bbd1483b7a4439ccca2601:research_artifacts/CFD_SPECTRAL_HYBRID_292BCE_20260917/static_carrier_native_adapter.py` (blob `1436c730189ba1b4f8d84359ff24a39f3ddc2f82`). The exact fast certificate remains `997981529b76c1b5e46ed43af91617c2c0023bd8:research_artifacts/CFD_HOST_9CFD0F_20260921/fast_carrier.py` (blob `d696a89a281a960eb1eb597faa087159b69bf681`).

## BRC gate

Resolution: `EXTEND_EXISTING_TOOL / REUSE_EXECUTED`.

The routing carrier is exact signed Boolean wavevector support only. Complex amplitudes, phase, conjugacy, pair multiplicity, native pressure/diffusion operations, RK linear combinations, and the dense fallback remain outside the support quotient and are not discarded. No coefficient threshold, pruning, absolute-value collapse, or total-only summary is introduced. The diagonal witness `±(1,1,0)` remains the information-loss counterexample preventing a false Cartesian shortcut.

## New bounded implementation checkpoint

`research_artifacts/RS-CFD-SPECTRAL-HYBRID-20260910_421478/trajectory_probe.py` composes the two already justified deltas at the native-adapter boundary:

1. use the axis-generator certificate only when every nonzero coordinate gcd generator is present in the signed seed; otherwise call the frozen general detector;
2. construct the fixed rFFT label array with `.reshape(-1, 3)`, so exact empty support remains a rank-2 `(0,3)` label object instead of the previously failing `(0,)` shape.

The local deterministic probe passed four regressions: exact-empty rank repair; diagonal missing-axis-witness decline to the general detector with closure size 15; exact `xy` axis carrier size 225; and exact `xyz` size 3375 selecting dense fallback without carrier enumeration. The published result is in `research_artifacts/RS-CFD-SPECTRAL-HYBRID-20260910_421478/results.json`.

This is an adapter-boundary checkpoint, not an original-host execution. It does not replace the actual serial spectralDNS/shenfun/FFTW requirement.

## New total-cost result: explicit reuse horizon

The prior checkpoint established that first-JIT cold execution loses badly while warm execution can win. This return turns that qualitative requirement into a declared finite reuse horizon for the two cases that have both cold and warm measurements.

For `n` trajectories in one persistent compiled session, use the descriptive median affine model

`T_route(n) = C_route + (n-1) W_route`,  
`T_fft(n)   = C_fft   + (n-1) W_fft`.

This is an accounting model over the recorded medians, not a confidence interval, hardware-independent law, or host result.

### xy225 nonlinear-active

- cold penalty: `537.956543 - 61.924834 = 476.031709 ms`;
- warm saving per reuse: `55.441 - 18.554 = 36.887 ms`;
- therefore `Δ(n) = 476.031709 - 36.887 (n-1) ms`;
- at `n=13`, the route still loses by `33.387709 ms`;
- at `n=14`, the route leads by `3.499291 ms`.

So the smallest median-model reuse horizon is **14 total trajectories**: one cold trajectory plus thirteen warm reuses.

### xy35 nonlinear-active

- cold penalty: `496.299639 - 61.819903 = 434.479736 ms`;
- warm saving per reuse: `56.098 - 4.541 = 51.557 ms`;
- therefore `Δ(n) = 434.479736 - 51.557 (n-1) ms`;
- at `n=9`, the route still loses by `22.023736 ms`;
- at `n=10`, the route leads by `29.533263 ms`.

So the smallest median-model reuse horizon is **10 total trajectories**: one cold trajectory plus nine warm reuses.

The dense-fallback cases `xyz3375` and `random48` have no positive warm saving versus FFT in the recorded medians, so amortization cannot rescue them under this evidence. The correct route remains dense FFT fallback. The parity/general-detector case may be warm-favorable, but no matching cold measurement exists, so this return does not manufacture a cold reuse horizon for it.

## Current original-host capability check

The current execution environment has Python 3.13.5 with NumPy, SciPy and Numba, but does not expose Python modules `spectralDNS`, `shenfun`, `mpi4py`, `mpi4py_fft` or `pyfftw`; `mpiexec`, `mpirun` and `fftw-wisdom` are absent. System FFTW shared libraries are present, which is insufficient by itself to instantiate the pinned Python host. Therefore the original serial host is not runnable in this environment without the missing host stack.

The complete prior reproduction bundle was located in the project Library at `/进取数论/CFD_9CFD0F_research.bundle` (reported size 34,316 bytes), and its accompanying `REPORT.md` is readable and consistent with the prior checkpoint. However, the current Project file surface does not grant an authorized raw-byte materialization path for that bundle, so this run cannot verify its byte SHA-256, clone it, or mirror its 147 warm raw rows into GitHub. This is an access/capability boundary, not evidence that the bundle is missing.

## Status and smallest unresolved unit

New progress is real but bounded: the support-routing repair is executable, the empty-label boundary remains repaired, and the previously missing compiled-session accounting now has explicit break-even horizons of 14 (`xy225`) and 10 (`xy35`) trajectories under the recorded median model. Dense fallback remains mandatory for observed losing cases.

The task remains `CONTINUATION_REQUIRED`. The smallest unresolved unit is still the actual pinned serial host execution: obtain raw access to the complete reproduction bundle or otherwise reconstruct the exact pinned adapter inputs; provide the spectralDNS/shenfun/FFTW Python host stack; integrate the same exact-axis selector and `(0,3)` repair into that host; run unchanged dense callback, pressure/diffusion and RK4 on the nonlinear-active held-out cases; retain raw setup/allocation/compilation/trajectory timings; then submit the resulting host checkpoint to independent `RS-CFD-TRAJECTORY-VERIFY-20260910` review. No DONE, canonical promotion, universal speedup, industrial claim, continuous-PDE theorem, or native-P000 fluid theorem is asserted here.
