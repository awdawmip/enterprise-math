# GEO6 First-Wave Accepted Prior-Art Synthesis — Research Return

Task: `RS-GEO6-FIRSTWAVE-ACCEPTED-PRIOR-ART-SYNTHESIS`  
Publication: `TP2-D5FC77B38C002D9EA868`  
Researcher-ID: `EM-G6PA-9C72F1`  
Claim: `chatgpt-g6pa-20260830-1459-9c72f1`  
Execution branch: `research/geo6-firstwave-prior-art-synthesis-em-g6pa-9c72f1`  
Date: `2026-08-30`  
Terminal verdict: `AUDIT_COMPLETE`  
Hard target: `GEO6_FIRSTWAVE_ACCEPTED_CLAIMS_PRIOR_ART_EXACTLY_CLASSIFIED`

## Executive result

The shared prior-art gate for the accepted GEO6 Kissing, Falconer and Hadwiger first-wave Results is complete.

Nineteen accepted claims/obstructions were decomposed and classified individually:

- `EXACT_DUPLICATE`: 3
- `STRICT_ANTECEDENT`: 10
- `ADJACENT_METHOD`: 3
- `NO_MATERIAL_MATCH`: 3

The finite combinatorial layer is therefore largely classical. The surviving Enterprise value is not a new finite number; it is the isolation of four P000 semantic selectors that classical antecedents do not decide:

1. `CONTACT_SELECTOR`
2. `LOCALITY_REFINEMENT_SELECTOR`
3. `ROTATION_CLOSURE_SELECTOR`
4. `TRANSLATION_ACTION_SELECTOR`

`NO_MATERIAL_MATCH` is not a novelty certificate. It records only that the searched external theory does not decide the P000-specific semantic statement under equivalent hypotheses.

The complete source-backed claim matrix is frozen at:

`research_artifacts/GEO6_FIRSTWAVE_ACCEPTED_PRIOR_ART_SYNTHESIS/claim_source_matrix.json`

The exact finite regression is frozen at:

`research_checks/GEO6_FIRSTWAVE_ACCEPTED_PRIOR_ART_SYNTHESIS_CHECK_20260830.py`

## Kissing lane

The external E6 `72/20/720` witness is standard E6/root-polytope mathematics (`EXACT_DUPLICATE`). The declared seven-Cell star is the elementary `K_{1,6}` degree calculation (`STRICT_ANTECEDENT`). The four-Cell S4 invariant empty/K4 dichotomy and six-axis capacities `{0,1,4,5}` are special cases of permutation-orbital / Johnson-scheme `J(4,2)` structure (`STRICT_ANTECEDENT`). The `r>=12` residual-label pressure is generic injectivity/pigeonhole reasoning (`STRICT_ANTECEDENT`).

What remains is not the finite census. It is:

`CONTACT_SELECTOR`: P000 must canonically select or derive native Cell contact, possibly with an equivariant hidden residual state beyond the six-axis readout.

## Falconer lane

The gap-free positive distance spectrum `{1,...,diam}` is a general connected graph-metric fact (`STRICT_ANTECEDENT`). The polynomial

`V_6(r)=sum_{j=0}^6 2^j C(6,j) C(r,j)`

is the classical six-dimensional Lee-ball volume formula (`EXACT_DUPLICATE`). The bound `|A|<=V_6(s)` is diameter-ball containment plus that Lee formula (`STRICT_ANTECEDENT`). The balanced-box inequality is AM-GM (`STRICT_ANTECEDENT`). The same-carrier coarse relation is exactly the Hamming graph `H(6,q)`, with `q^6` states and positive distance set `{1,...,6}` (`EXACT_DUPLICATE`).

What remains is:

`LOCALITY_REFINEMENT_SELECTOR`: P000 must supply a native reason to choose local/refinable/bounded-growth adjacency over a coarse Hamming-compatible relation before a cardinality-to-spectrum theorem can become native.

## Hadwiger lane

The signed-shell cover/illumination reduction is adjacent to binary strength-1 covering-array incidence (`ADJACENT_METHOD`). The one-step weight-2 value `3` is the minimum edge-cover number of `K6` (`STRICT_ANTECEDENT`). The composition-closed value `2` is the length-6 even-weight/single-parity-check code containing the all-minus word (`STRICT_ANTECEDENT`). Sign-preserving impossibility is an invariant-subset obstruction (`STRICT_ANTECEDENT`). Passive block-refinement invariance is standard incidence duplication once the refinement semantics are declared (`ADJACENT_METHOD`).

The classical Levi–Hadwiger problem assumes Euclidean convex bodies and translation/homothety/illumination structure, so its `2^6` heuristic is only adjacent, not a numerical antecedent for the signed Cell probe.

What remains is:

- `ROTATION_CLOSURE_SELECTOR`: determine the actual Full-Cell native operation family and whether composition/global inversion is present.
- `TRANSLATION_ACTION_SELECTOR`: freeze ambient translation/homothety and compatible boundary transport before defining any native translation-cover number.

## Continuation kill rules

Do not spend further GEO6 research budget merely re-deriving:

- E6 `72` roots / degree `20` / `720` edges;
- star capacity `6`;
- S4 / `J(4,2)` capacities `0,1,4,5`;
- the Lee `V_6(r)` polynomial;
- the Hamming `H(6,q)` bounded-spectrum family;
- the signed-shell values `3`, `2`, or infinity under the same frozen operation families.

A successor is justified only if it advances one of the four native semantic selectors.

## Exact regression

The task-local integer checker independently verifies:

- `J(4,2)` non-diagonal orbital sizes `3,12` and valencies `1,4`;
- invariant six-axis degrees `0,1,4,5`;
- Lee-ball values `1,13,85,377,1289` for radii `0..4`;
- `H(6,q)` distances `0..6` for `q=2..5`;
- signed-shell separation values elementary=`3`, even=`2`, full signs=`2`, sign-preserving impossible;
- minimum edge-cover size of `K6` equals `3`;
- three disjoint weight-2 sign flips compose to global inversion;
- E6 reflection closure from the frozen integral Gram matrix has 72 norm-2 roots, pairing-1 degree 20 and 720 edges.

The exact checker passes all assertions.

## Terminal disposition

`AUDIT_COMPLETE`.

The hard target `GEO6_FIRSTWAVE_ACCEPTED_CLAIMS_PRIOR_ART_EXACTLY_CLASSIFIED` is met at declared task scope.

Driver recommendation: accept the audit if immutable pins agree; freeze duplicate-continuation kills; route any successor only to `CONTACT_SELECTOR`, `LOCALITY_REFINEMENT_SELECTOR`, `ROTATION_CLOSURE_SELECTOR`, or `TRANSLATION_ACTION_SELECTOR`, with an explicit P000/Full-Cell datum capable of resolving that selector.
