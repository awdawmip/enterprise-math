# P022 Observation-History Arithmetic Core Return — 2026-08-27

Task: `RS-P022-OBSERVATION-HISTORY`  
Publication: `TP2-DE338F269CA11E9BC01B`  
Researcher: `EM-P022OH-540040`  
Claim: `chatgpt-p022oh-20260827-1639`

## Verdict

`PASS / HARD_TARGET_MET_VIA_EXACT_REDUCTION`

The old P022 first-reentry boundary is not closed here by an all-parameter nonvanishing theorem. It is closed at the taskbook's explicitly allowed exact-reduction terminal: the dangerous divisibility is reduced to one fixed-parameter terminating finite-field hypergeometric cancellation identity, and the elementary valuation plus routine terminating-transformation routes are exhausted exactly. No Working Truth, Foundation, or canonical theorem promotion is claimed.

## 1. Frozen boundary and normalization

Replay source: `program/p022-geometry-v2@c07ca4c719117829fe2c6919bbe635a1e97a8c4b`.

The frozen owner lineage supplies the Franel convention

`F_n = sum_{k=0}^n C(n,k)^3`,

the p-Lucas/reflection machinery, the twin-center blackout reduction, and the fact that `q=3r-1` is the first dangerous reflection boundary. Its midpoint obstruction already leaves complete escape only for `q ≡ 17,35 (mod 72)`.

For a nontrivial twin center, `2r-1` and `2r+1` are prime. Modulo 3 this forces `3|r`; primality of `q=3r-1>2` forces `r` even. Hence

`r=6m`, `q=18m-1`,

with twin boundaries `12m-1` and `12m+1`.

## 2. Exact terminating-kernel theorem

MacMahon's exact expansion is

`F_r = sum_{k=0}^{floor(r/2)} 2^(r-2k) (r+k)! / ((r-2k)! (k!)^3)`.

For `r=6m`, the ratio of consecutive normalized summands is

`(6m+k+1)(6m-2k)(6m-2k-1) / (4(k+1)^3)`.

Modulo `q=18m-1`, one has `6m ≡ 1/3`, so the ratio becomes

`(k-1/6)(k+1/3)(k+4/3) / (k+1)^3`.

Therefore

`2^(-6m) F_(6m) ≡ 3F2(-3m,6m,6m+1;1,1;1) (mod q)`.

The `-3m` parameter terminates exactly at `3m`. Define the integer kernel

`S_m = sum_{k=0}^{3m} (-1)^k C(3m,k) C(6m+k-1,k) C(6m+k,k)`.

Then the exact P022 boundary equivalence is

`q | F_(6m)  iff  S_m ≡ 0 (mod q)`, for `q=18m-1`.

This is symbolic, not a finite-scan inference.

## 3. Exact unit obstruction

For every `0 <= k <= 3m`, all factors in

`(-3m)_k (6m)_k (6m+1)_k / (k!)^3`

have absolute representatives strictly below `q`: the numerator ranges stop at `3m`, `9m-1`, and `9m`, while `k!` stops at `3m`; all are `<18m-1`. Thus every kernel term is a q-adic unit.

Consequently the remaining problem is genuine finite-field cancellation. No term vanishes, no unique minimum valuation exists, and no valuation-positive tail can be discarded. The elementary valuation route is fully closed.

## 4. Fixed rational-parameter residual

Reversing a terminating `3F2(-n,a,b;1,1;1)` gives

`(-1)^n (a)_n (b)_n/(n!)^2 * sum_{j=0}^n (-n)_j^3 / ((1-a-n)_j (1-b-n)_j j!)`.

With `n=3m`, `a=6m`, `b=6m+1`, the prefactor is a q-unit and, modulo `q=18m-1`,

`-3m ≡ -1/6`, `1-9m ≡ 1/2`, `-9m ≡ -1/2`.

Hence the smallest explicit residual is the fixed-parameter truncated kernel

`R_m(q) = sum_{j=0}^{3m} (-1/6)_j^3 / ((1/2)_j (-1/2)_j j!)`.

The remaining all-m gate is to prove or refute `R_m(q) != 0 (mod q)` for the admissible P022 boundary constellation.

## 5. Terminating-transform audit

The checker closes the exact affine parameter orbit generated by numerator/denominator permutations and the standard terminating Weber–Erdelyi transform

`3F2(A,B,-N;D,E;1) = (D-A)_N/(D)_N * 3F2(A,E-B,-N;1+A-D-N,E;1)`.

For the specialized start `(-3m,6m,6m+1;1,1)`, the canonical orbit has exactly 12 parameter types. Among them:

- direct numerator/denominator cancellation: 0;
- Saalschutz-balanced members: 0.

So the routine terminating-transform route does not collapse this family to an immediate factorial quotient. This does not exclude deeper finite-field, modular, or geometric transformations.

## 6. Deterministic exact pressure test

The checker enumerates all `q<50000` satisfying

`q=18m-1`, `12m-1 prime`, `12m+1 prime`,

and computes both `F_(6m) mod q` and the terminating kernel exactly.

Results:

- total boundary candidates: 90;
- residue counts mod 72: `17:22`, `35:25`, `53:28`, `71:15`;
- zeros among all 90: 0;
- frozen complete-escape survivors (`17` or `35 mod 72`): 47;
- zeros among those 47: 0.

This is finite evidence only.

A control counterexample prevents an invalid generalization: `149 | F_50`, with `149 ≡ 2 (mod 3)` and `50=(149+1)/3`. Thus mod 3 alone cannot prove the P022 statement; the twin-boundary and surviving mod-72 structure are genuinely load-bearing.

## 7. Frozen outputs and next frontier

Artifacts:

- `scripts/check_p022_observation_history_arithmetic_core.py`;
- `research_artifacts/P022_OBSERVATION_HISTORY_ARITHMETIC_CORE/boundary_census_q_lt_50000.json`;
- this return.

The checker is deterministic and asserts the symbolic kernel congruence on every census candidate, the unit lemma, the 12-member transformation orbit, the exact candidate counts, and the `q=149` control.

Recommended successor, if Driver accepts this return: attack the single fixed kernel `R_m(q)` by finite-field hypergeometric/Jacobi-sum methods or a modular/Cartier interpretation. Do not spend the next task merely extending the finite cutoff.

Prior-art note: MacMahon's Franel expansion and the Jarvis–Verrill-style Franel congruence machinery are prior art; a convenient audited source is Victor J. W. Guo, *Proof of two conjectures of Z.-W. Sun on congruences for Franel numbers*, arXiv:1201.0617.

No all-m nonvanishing theorem is claimed. Driver review is required before integration or promotion.
