# P022 Observation-History Forced-Midpoint Fallback — Final Research Return

Status: `FINAL_FROZEN / BOUNDED_FORCED_MIDPOINT_CAPTURE_PROVED / HARD_TARGET_ACHIEVED`

Date: `2026-08-27`

Task: `RS-P022-OBSERVATION-HISTORY`
Publication: `TP2-D78DBA0243911E0363FA`
Claim: `chatgpt-p022-replay-20260827-1639`
Researcher: `EM-P022-A4FCE7`
Recovered owner head: `program/p022-geometry-v2@603ef1c72245612359f8b59cab7a492de21a9166`

## 1. Verdict

The replay hard target is achieved.

Let `q>5` be prime with

\[
q\equiv 5\ \text{or}\ 23\pmod{24},
\qquad
m=\frac{q-1}{2},
\qquad
B=\frac{q+1}{6}.
\]

Assume the first positive Franel zero modulo `q` is at a nontrivial twin-prime deferral center `r`:

\[
q\mid F_r,\qquad
q\nmid F_j\ (1\le j<r),\qquad
2r-1,\ 2r+1\ \text{prime}.
\]

Then, in the exact range

\[
\boxed{q<6r-1}
\quad\Longleftrightarrow\quad
\boxed{B<r},
\]

the `q`-marker is necessarily detected by a nonzero Franel-defect valuation no later than segment `m`.

More precisely:

- normally `v_q(D_m)>0`;
- only when `q=23 (mod 24)`, `m` is prime, and the one positive dangerous support index `d=(q-3)/4` is itself a Franel zero, capture may move earlier;
- in that exceptional branch `D_d` exists and `v_q(D_d)>0`, with `r+2<=d<2r-1`.

Thus every primitive-twin target in `q<6r-1` is captured by `D_d` or `D_m`.

This does **not** close the parent P022 high-range frontier `q>=6r-1`.

## 2. Exact signed-support audit

Write `A_n=C(2n,n)`. For composite odd boundary `2n-1`, the canonical triangular relation is obtained from

\[
A_n=A_{n-1}\frac{2(2n-1)}{n}.
\]

At `n=m`, because `q=5 (mod 6)`, `B=(q+1)/6` is integral.

### 2.1 `q=24k+5`

Here

\[
m=12k+2,\qquad B=4k+1,\qquad q-2=3(8k+1).
\]

Every central-binomial basis index generated by the integer `q-2` is at most `B`. The same is true for the integer `m=2(6k+1)`. Therefore the only relation support above `B` is the recurrence predecessor:

\[
\boxed{\operatorname{supp}_{>B}(\alpha_m)=\{(m-1,+1)\}}.
\]

### 2.2 `q=24k+23`, composite `m`

Now

\[
m=12k+11,\qquad B=4k+4,\qquad q-2=3(8k+7).
\]

Again every basis index generated by `q-2` is at most `B`. If `m` is composite and odd, every prime factor of `m` has complementary factor at least `3`, hence its induced central-binomial index is also below `B`. Thus

\[
\boxed{\operatorname{supp}_{>B}(\alpha_m)=\{(m-1,+1)\}}.
\]

### 2.3 `q=24k+23`, prime `m`

Put

\[
h=\frac{m+1}{2}=6k+6.
\]

For prime `m`, its exact integer-basis expansion contributes `+A_h-A_{h-1}` before the relation subtraction; subtracting that expansion therefore contributes

\[
(h-1,+1),\qquad(h,-1).
\]

All support recursively generated by the composite integer `h=6(k+1)` lies at or below `B`. Hence the complete high support is exactly

\[
\boxed{
\operatorname{supp}_{>B}(\alpha_m)
=
\{(h-1,+1),(h,-1),(m-1,+1)\}.
}
\]

No high support term is omitted.

## 3. Valuation proof

Let `z_j=v_q(F_j)`.

Because `B<r` and `r` is the first positive Franel zero, every support index `<=B` is a `q`-unit. The forced-midpoint criterion gives `z_m>0`. Adjacent Franel zeros are impossible, so

\[
z_{m-1}=0.
\]

In the ordinary high-support cases this immediately yields

\[
v_q(D_m)=z_m>0.
\]

In the prime-midpoint special case, put

\[
d=h-1=\frac{q-3}{4}.
\]

If `z_d=0`, then the coefficient of `h` is `-1`, so any zero at `h` only increases the defect valuation:

\[
v_q(D_m)=z_m+z_h>0.
\]

If `z_d>0`, primitivity forbids `d<r`. Equality `d=r` is impossible: a nontrivial twin center has `r=0 (mod 3)`, whereas `d=2 (mod 3)` in the class `q=23 (mod 24)`. Hence `d>r`; the same congruence forces `d>=r+2`.

Moreover `q<6r-1` gives

\[
d=\frac{q-3}{4}<\frac{6r-4}{4}<2r-1.
\]

The odd boundary at `d` is

\[
2d-1=m-2,
\]

and for `q=24k+23` this is `3(4k+3)`, a nontrivial composite. Therefore `D_d` exists. Inside the twin blackout `r+2<=d<2r-1`, the exact defect law is

\[
v_q(D_d)=z_d-z_{d-1}.
\]

Adjacent-zero exclusion gives `z_{d-1}=0`, hence

\[
\boxed{v_q(D_d)=z_d>0}.
\]

This proves the bounded capture theorem without finite computation.

## 4. Independent finite falsification

A separate checker rebuilds, without importing the P022 implementation under test:

1. primality by trial division;
2. the exact Franel recurrence
   \[
   n^2F_n=(7n^2-7n+2)F_{n-1}+8(n-1)^2F_{n-2};
   \]
3. the recursive central-binomial integer basis;
4. the composite `A_n` relation and its signed support.

Scope: all target primes `q<50,000` in residues `5,23 (mod 24)`.

Results:

- target primes tested: `1,294`;
- signed-support mismatches: `0`;
- prime-midpoint special-support cases: `193`, mismatches `0`;
- primitive first-zero twin-center cases: `12`;
- cases satisfying `q<6r-1`: `6`;
- bounded captures verified: `6/6`;
- theorem failures: `0`.

The six bounded cases are:

\[
(2351,726),\ (3701,1014),\ (11279,3744),\
(23831,10506),\ (33773,8031),\ (45263,16071),
\]

listed as `(q,r)`.

This scan is regression/falsification evidence only. It is not used in the general proof.

Executable evidence:
- `scripts/check_p022_forced_midpoint_fallback_replay.py`;
- `research_artifacts/P022_OBSERVATION_HISTORY_FORCED_MIDPOINT_FALLBACK/regression_20260827.json`.

## 5. Recovered-source relation

The durable owner source `src/enterprise_math/p022_barlow_forced_midpoint_fallback.py` states the same bounded implication and encodes the signed-support and dangerous-index checks. The replay did not treat the source as self-validating: the support formula and recurrence were independently reconstructed and pressure-tested.

The later owner modules for strict twin transport and general high transfer are not prerequisites for this result.

## 6. Exact residue

The task hard target is closed, but the parent objective is not.

The surviving frontier is

\[
\boxed{q\ge 6r-1}
\]

for primitive Franel primes whose first zero is a nontrivial twin center. The owner branch already contains fixed-transfer/high-digit machinery aimed at that regime. This return makes no all-`q` observability or multiplicative-independence claim.

## 7. Terminal disposition

Hard target:

`P022_FORCED_MIDPOINT_FALLBACK_CAPTURE_EXACT_AND_INDEPENDENTLY_REPLAY_VERIFIED`

Disposition:

`ACHIEVED`

Terminal verdict:

`PASS`

The appropriate next control-plane action is Driver review of this bounded closure. If accepted, this legacy replay generation should be frozen as complete and the parent objective reevaluated against the explicit high-range residue rather than redispatching the same midpoint task.
