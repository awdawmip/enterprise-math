# Research Return — A3 Shell Partial-Move Scale-Coherence Revision

Task-ID: `RS-A3-SHELL-PARTIAL-MOVE-SCALE-COHERENCE-REVISION`  
Publication-ID: `TP2-E6E8A3DC37930B4CF4AA`  
Researcher-ID: `EM-A3SCR-9FE7C5`  
Claim-ID: `chatgpt-a3scr-20260828-2332-9fe7c5`

## Terminal verdict

`PASS / CORRECTED_H4_EXACT / SUPPORT_TRANSITION_PAIR_GROUPOID_CLASSIFICATION / FRAME_ONLY_IFF_REFUTED / NO_FOUNDATION_PROMOTION`

Hard target:

`A3_PARTIAL_MOVE_SCALE_COMMUTATION_AND_RADIAL_DEFECT_EXACTLY_CLASSIFIED`

Disposition:

`SATISFIED_FOR_THE_DECLARED_PREFIX_SUPPORT_SEMANTICS`

## Exact corrected H4

For the frozen prefix-support model, let

\[
U_{n,d}=B_n\setminus B_{n-d}
=\bigcup_{j=n-d+1}^{n} S_j
\]

and let \(D_{n,d}(g)\) apply the faithful sign-twisted frame action \(R_g\) on
\(U_{n,d}\) and fix \(B_{n-d}\). Each \(D_{n,d}\) is an exact group action.

For the scale edge \(n+1\to n\), define

\[
T_{n,d}=S_{n-d+1},
\qquad
O_{n,d}=\bigcup_{j=n-d+2}^{n}S_j,
\]

and write \(\overline D_{n+1,d}\) for the restricted upper-scale action on
\(B_n\). The exact support-transition action is

\[
J_{n,d}(g)
=
D_{n,d}(g)\,\overline D_{n+1,d}(g)^{-1}.
\]

It is \(R_g\) exactly on the transition shell \(T_{n,d}\) and the identity
elsewhere. In particular the missing H4 datum is not another frame phase:
it is the scale-induced support transition.

For a lower aligner \(g_n\) and upper aligner \(g_{n+1}\), the exact
state-level scale defect factors as

\[
\boxed{
F_{n,d}(g_n,g_{n+1})
=
J_{n,d}(g_n)\,
\overline D_{n+1,d}(g_ng_{n+1}^{-1}).
}
\]

The two factors have disjoint radial supports and commute. Equivalently,
\(F_{n,d}\) acts by

\[
F_{n,d}(x)=
\begin{cases}
x,& r(x)\le n-d,\\
R_{g_n}x,& r(x)=n-d+1,\\
R_{g_ng_{n+1}^{-1}}x,& n-d+2\le r(x)\le n.
\end{cases}
\]

Thus the old double-coset defect sees only the relative-frame factor and
forgets the absolute lower-aligner action on the newly exposed transition
shell.

## Operation descent and exact no-go

For every \(n\ge d\ge1\),

\[
\rho_{n+1,n}D_{n+1,d}(g)
=
D_{n,d}(g)\rho_{n+1,n}
\]

on the full raw state language **iff \(g=e\)**.

The forward implication follows because equality forces
\(J_{n,d}(g)=1\); the transition shell contains the scaled \(A_3\) root
directions and the sign-twisted \(S_4\) representation is faithful, so a
nonidentity \(g\) moves some transition-shell point.

The frozen Driver witness is recovered exactly: for depth 2, scales \(3\to2\),
\(g=(23)\), and \(p=(1,-1,0,0)\in S_1\), the old frame defect is \(C_0\), but
the upper route fixes \(p\) while the lower route sends it to
\((-1,0,1,0)\), outside the residual-\(H\) orbit.

A stronger collision is frozen as well. The typed arrows
\((2,2;H,H)\) and \((2,2;Hg,Hg)\) have the same old relative double-coset
projection \(C_0\), but induce different state relations on the transition
shell. Therefore **no classifier that factors only through
\(Hg_ng_{n+1}^{-1}H\) can classify the H4 square for the declared
scale-dependent prefix actions.**

## Correct support-aware groupoid object

For the pointer target, the frozen residual stabilizer is
\(H=\{e,(12)\}\). A reachable alignment family at scale \(n\) is the left
coset \(L_n=Hg_n\).

The corrected deterministic transport arrow is the typed pair-groupoid datum

\[
\Omega_{n,d}
=
(n,d;L_n,L_{n+1}),
\]

and its exact representative-independent state defect relation is

\[
\mathfrak D(\Omega_{n,d})
=
\{
F_{n,d}(u,v):u\in L_n,\ v\in L_{n+1}
\}.
\]

This relation contains exactly the state-level transports generated by all
legal representative choices: there are neither missing nor spurious raw
defect maps.

The previously accepted fixed-\(H\) pair-groupoid/double-coset algebra is
recovered by the projection

\[
\pi_{\rm frame}(\Omega_{n,d})
=
Hg_ng_{n+1}^{-1}H.
\]

It is therefore a genuine quotient/special case. It becomes H4-complete for
an observation quotient \(Q\) only after the support-transition factor is
annihilated on the relevant state language,

\[
Q\circ J_{n,d}(u)=Q
\quad
\text{for every legal lower representative }u.
\]

Without that operation-safe condition the frame projection is provably
insufficient.

## H5/H6 correction

The parent H5 signature must retain \(\Omega_{n,d}\), or equivalently the
full state defect relation \(\mathfrak D\), wherever scale coherence is
queried. A frame-only defect birth radius and a support-transition defect
birth radius may be recorded separately, but stabilization, periodicity and
full defect birth must use the corrected support-aware object.

The old three-radius label example \(B_1\subset B_2\subset B_3\) is not a
uniform depth-2 state-level H4 prototype: the lower edge would require the
undefined action \(D_{1,2}\).

The smallest uniform depth-2 three-radius state-level regression is instead

\[
B_2\subset B_3\subset B_4.
\]

Using the same \(g=(23)\) at all three scales gives old adjacent frame defects
\(C_0,C_0\), while both actual state-level squares fail to commute: the
\(4\to3\) edge exposes the \(S_2\) transition and the \(3\to2\) edge exposes
the \(S_1\) transition. This is a two-edge deterministic regression against
any reinstatement of frame-only H4.

## Exact replay package

Full theorem/proof package:

`research_artifacts/A3_SHELL_PARTIAL_MOVE_SCALE_COHERENCE_REVISION/A3_PARTIAL_MOVE_SCALE_COHERENCE_REVISION_RESULT_20260828.md`

Machine certificate:

`research_artifacts/A3_SHELL_PARTIAL_MOVE_SCALE_COHERENCE_REVISION/certificate.json`

Deterministic pure-stdlib checker:

`scripts/check_a3_shell_partial_move_scale_coherence_revision.py`

Checker terminal verdict:

`A3_PARTIAL_MOVE_SCALE_COHERENCE_REVISION_CHECK=PASS`

The exact replay checks 364032 site-level homomorphism identities for
\(D\), 364032 for \(J\), 364032 site-level defect factorizations, 96
transition-faithfulness group checks, 576 representative-level
pair-groupoid projections, the mandatory Driver witness, and the corrected
\(B_2\subset B_3\subset B_4\) two-edge regression. Finite enumeration is
used only as regression; the theorem above is proved symbolically for all
declared \(n\ge d\ge1\).

## Tool and semantic verdict

`COMPOSE_EXISTING_TOOLS / TASK_LOCAL_SUPPORT_TRANSITION_OPERATOR / NO_NEW_SHARED_FAMILY`

The corrected object composes the existing finite-symmetry,
operation-safe-quotient, relation/BRC and holonomy/groupoid ideas. The only
new ingredient is the task-local scale support-transition operator \(J\).

No physical interpretation, Working Truth, Foundation mutation, or canonical
promotion is claimed.

## Remaining boundary and routing recommendation

There is no unresolved residue **within the declared prefix-support
semantics**.

Still outside this task are genuinely different regimes with
scale-dependent stabilizers \(H_n\), object/state-dependent legal-move
domains, non-conjugate targets, or arbitrary partial-action groupoids. They
must not be inferred from this result.

Recommended next control action:

`DRIVER_REVIEW_AND_CLOSE_REVISION_IF_ACCEPTED`.

A successor is justified only for one of those broader varying-domain /
varying-stabilizer regimes, not for a larger finite \(S_4/H\) census.
