# P000 S4 等变 PF10 / connection 模空间 V21 — Independent Replication Review

Status: `REPLICATION COMPLETE / DRIVER HANDOFF`

Researcher-ID: `EM-P000FCC21R-A6E30E`  
Task-ID: `RS-P000-L1-NATIVE-CARRIER-CONTACT-BRIDGE`  
Publication-ID: `TP2-FBDBDBE1C5BDF65F97A0`  
Claim-ID: `CLAIM-P000FCC21R-20260906T0622Z-CHATGPT-02`  
Execution base: `f2b6a751b82b1bc71389be1527da0f88dbf39117`

Replicated durable frontier: PR `#1051`, frozen Result `RR-6C813A97DB5DA06C9CCF`.

Terminal class preserved:

`NONTRIVIAL_PF10_AND_NONFLAT_CONNECTION_MODULI_COMMON_MODEL_CLASSIFIED`.

## 1. Verdict

The Gen21 mathematical classification reproduces exactly under an independent finite reconstruction.

No theorem-changing mismatch was found.

The strongest justified conclusion remains:

- PF10 has a genuine nonconstant `S4`-equivariant moduli family;
- the frozen typed independent-connection problem has exactly `12` raw legal seeds;
- these give `2` raw flat and `10` raw nonflat connections;
- under the accepted local-`S6` presentation gauge they form exactly `8` gauge classes, `1` flat and `7` nonflat;
- the seven nonflat classes have the frozen holonomy fingerprints and subgroup orders;
- one common Full-Cell carrier model simultaneously carries a nonconstant PF10 family and a nonidentity/nonflat connection;
- no P000 ontology, Gen17 cost, Gen18 full-lift-fiber criterion, `NO_KERNEL_QUOTIENT`, or time-separation rule is changed.

## 2. Independent method

I did not use the frozen Gen21 executable as the derivation engine.

The replication rebuilds the finite model from:

1. the four tetrahedral Cells `A,B,C,D`;
2. six channel labels as the six unordered Cell pairs;
3. the induced `S4` action on those six edges;
4. exact stabilizer orbit enumeration;
5. exact enumeration of all `6! = 720` local channel permutations;
6. the legal seed equations for `t=T_AB`;
7. global connection propagation by carrier conjugation;
8. spanning-tree gauge normalization on `AB,AC,AD`;
9. simultaneous `S6` conjugacy classification of the three non-tree transports;
10. exact triangle holonomy and finite subgroup closure.

Checker:

`research_checks/P000_S4_EQUIVARIANT_PF10_CONNECTION_MODULI_V21_REPLICATION_CHECK_20260906.py`.

Certificate:

`research_artifacts/P000_S4_EQUIVARIANT_PF10_CONNECTION_MODULI_V21_REPLICATION/REPLICATION_CERTIFICATE.json`.

## 3. BRC observer/provenance audit

BRC is applied at the gauge-compression boundary.

Population:

`12` legal raw connection seeds.

Branch identity:

raw seed permutation `t=T_AB`.

Allowed future operations include flatness testing, gauge comparison, holonomy classification, carrier covariance and common-model witness selection.

Therefore the observer cannot collapse the census directly to the scalar statement `8 gauge classes`.

The adequate carrier retained in the certificate is:

`raw seed -> gauge class -> three basis holonomies -> cycle type -> generated holonomy subgroup order -> flat/nonflat`.

This is necessary because:

- raw seeds `0` and `5` are distinct presentations but one flat gauge class;
- classes `C4` and `C5` have the same one-triangle cycle type `2+2+2` but different generated holonomy subgroup orders `2` and `6`.

So total-only compression would erase theorem-relevant structure.

BRC resolution: `REUSE_APPLIED`.

## 4. PF10 moduli replication

Fix Cell `A`.

`Stab_{S4}(A) ~= S3` has exactly two channel-vector orbits:

- star channels `{E1,E2,E3}` incident to `A`;
- opposite-face channels `{E4,E5,E6}`.

On ordered channel pairs it has exactly eight orbits.

Thus the representative PF10 profile has exactly:

`2(I) + 2(O) + 8(M) = 12`

raw stabilizer-fixed parameter slots.

Full local regressions also reproduce:

- full `S4` vector orbits = `1`;
- full `S4` ordered-pair orbits = `3`;
- base Cell vector orbits = `2`;
- base Cell ordered-pair orbits = `8`.

The reconstruction law

`P_x = rho(g) P_A`, for any `g(A)=x`,

is well-defined exactly because `P_A` is fixed by `Stab(A)`; evaluation at `A` is the inverse map. Hence the global equivariant-family classification is complete and duplicate-free at the frozen presentation strength.

The explicit nonconstant witness also reproduces:

- `I_x` = incidence vector of the three channels meeting Cell `x`;
- `O_x=1-I_x`;
- `M_x=I_6`.

The four `I_x` are distinct and transport exactly under all `24` carrier permutations.

## 5. Connection seed locus

For representative oriented edge `A->B`, the pointwise edge stabilizer is generated by `(CD)` and induces

`kappa=(E2 E3)(E4 E5)`.

The edge-reversing carrier swap `(AB)` induces

`lambda=(E2 E4)(E3 E5)`.

The legal seed equations are exactly

`[t,kappa]=1`,

`lambda t lambda = t^-1`.

Because `lambda` commutes with `kappa`, the map

`s=lambda t`

identifies legal seeds with involutions in `C_{S6}(kappa)`.

Independent enumeration gives:

`|C_{S6}(kappa)| = 16`,

`# involutions = 12`,

hence exactly `12` legal seeds.

Their cycle forms reproduce the frozen list in the same canonical channel presentation.

## 6. Gauge quotient and holonomy

Use star spanning tree `AB,AC,AD`.

Tree gauging makes those three transports identity. Residual root gauge is simultaneous conjugation on the remaining triple `(BC,BD,CD)`.

The twelve raw seeds partition exactly as:

`[[0,5],[1],[2],[3,4],[6,11],[7],[8],[9,10]]`.

Therefore there are exactly `8` accepted gauge classes.

The independently reproduced class fingerprints are:

| class | representative | triangle cycle type | holonomy subgroup order | flat |
|---|---|---:|---:|---|
| C0 | `id` | `1+1+1+1+1+1` | 1 | yes |
| C1 | `(E2 E3)(E4 E5)` | `4+2` | 24 | no |
| C2 | `(E2 E4)(E3 E5)` | `2+2+1+1` | 6 | no |
| C3 | `(E2 E4 E3 E5)` | `2+1+1+1+1` | 6 | no |
| C4 | `(E1 E6)` | `2+2+2` | 2 | no |
| C5 | `(E1 E6)(E2 E3)(E4 E5)` | `2+2+2` | 6 | no |
| C6 | `(E1 E6)(E2 E4)(E3 E5)` | `4+1+1` | 24 | no |
| C7 | `(E1 E6)(E2 E4 E3 E5)` | `5+1` | 60 | no |

Raw flat count is exactly `2`; raw nonflat count is `10`. The two raw flat presentations occupy the single flat gauge class `C0`.

For every legal seed, all global oriented-edge transports were reconstructed and the carrier holonomy conjugacy law was exhaustively checked over `24 * 12 * 3 = 864` carrier/seed/basis-loop cases.

## 7. Common nondegenerate model

Take the nonconstant PF10 incidence/complement witness and connection seed

`t=(E1 E6)`.

Carrier propagation gives the rule:

> on each tetrahedral Cell edge, swap that edge-channel with its unique opposite edge-channel.

For all three basis triangles, the based holonomy is

`omega=(E1 E6)(E2 E5)(E3 E4) != id`.

So the same model is simultaneously:

- PF10-nonconstant;
- connection-nonidentity;
- connection-nonflat;
- fully carrier-`S4` equivariant.

With frozen carrier generators `a=(BCD)`, `b=(AB)`, exact permutation computation gives

`a^3=b^2=(ab)^4=id`.

Since both enriched fields were checked equivariant under all `S4`, these relations act consistently on the complete enriched model.

## 8. Boundaries retained

This replication does **not** infer:

- `CARRIER_S4 = COMPLETE_NATIVE_P000_ROTATION_GROUP`;
- local `S6` gauge = native spatial rotation;
- gauge/presentation bits = new native axes;
- quotient by a hidden structural kernel;
- chosen coherent lifts = Gen18 full transparency in a hidden-kernel model.

The common witness deliberately uses faithful visible `G0=S4`; that is a model choice, not a quotient theorem.

## 9. Driver recommendation

Replication verdict: `PASS / REPRODUCED_EXACT`.

Recommended control disposition:

**accept the Gen21 moduli/common-model mathematical closure at its frozen finite K4/tetra carrier strength.**

If accepted, do not reopen the already-closed existence/nonflatness question. Preserve Gen18 hidden-kernel/full-lift-fiber logic, P000/G15/Gen17 costs, `NO_KERNEL_QUOTIENT`, and `TIME_FIXED`, and route the next research work to the next native-P000 bridge/frontier selected by the canonical dispatcher.
