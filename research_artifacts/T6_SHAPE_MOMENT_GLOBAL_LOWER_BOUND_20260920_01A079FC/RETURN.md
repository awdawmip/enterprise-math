# T6 research return: p19 closed; p17 order1 is next

Task: RS-T6-SHAPE-MOMENT-GLOBAL-LOWER-BOUND.
Publication: TP2-34B106D2512A298C9647.
Execution: ER-120BCED768F86DA406CC; claim t6sm-exact-solver-01a079fc-20260920.

The global hard target remains OPEN: this return does not establish T6=2332
and does not supply a cheaper exact collision.

New exact mathematical frontier:

- Maximal p19, order9 split(4,5): all45287075 primitive affine instances cleared.
- New order10 splits(4,6),(5,5), order11(5,6), order12(6,6): all clear.
- After completing those new units, one specifically authorized independent
 15.031-second check establishes the old(3,6) claim over335374 instances. It
 fills the mathematical reproducibility gap with a new checker; it does not
 recover or reproduce the original missing builder.
- Under the Task's previously verified global p19 order0--8 input, consumed
 without replay, these certificates close the entire maximal-p19 layer.
- Maximal p17 q0 is excluded by a saturated lattice and the static exact bound
 min(D_i)>2*2331^2. A second consumer verifies this without the LLL library.

`PROOF.md` supplies the carrier, valuation guards, exact saturation, stronger
separate-side squared-norm bound, strict flatness/tie argument, and the static
p17 zero-coset proof. `coverage_p19.json` binds the six remaining blocks; it is
a coverage-metadata ledger, not a substitute for producer replay.

The entire original BRC shape observer H0,...,H6 remains the mother target.
Mixed congruences are a necessary relaxation; a surviving local kernel would
require full lower-prime and H0 completion. All denominators a=e+1, signed
multiplicities and both side budgets remain exact; no prime-only/composite
sieve or floating near-equality was used as proof evidence. The18- and16-entry
lattices are auxiliary coefficient arrays, not new native spatial dimensions.

Tool reuse: existing BRC arithmetic facade executed for materialized division
and remainder; existing standard saturation/LLL/Gram-Schmidt methodology
composed at this task-specific interface. FLINT0.9.0 proposes an integer basis;
integer determinant, congruence and orthogonal checks establish what is used.
No new global tool family or worldwide novelty is claimed. Native arithmetic
static gates pass for the new calculation sources. Traces are checked on each
call; output retains samples and counts, while full traces require replay.

Actual next action: independently review this frozen research return, then
continue the same existing Task at maximal p17 vertical order1, split(0,1).
Restore the p17 exact basis from its certificate and establish the appropriate
affine search gate; do not transplant the p19 unique-nearest-plane shortcut
without its strict hypothesis. Do not replay verified p19 or T1--T5/L360 work.
Orders1--16 at p17 and the lower maximal primes remain unclosed.

All outcomes here have researcher-proof/exact-computation strength. Source
publication and the result record grant no Driver acceptance, Working Truth,
Foundation promotion, successor Task or global theorem status. The exact
original builder archive gap is preserved as provenance, separately from the
new independent proof of its bounded(3,6) conclusion.

Researcher-ID: EM-T6SM-B134BD / RS-T6-SHAPE-MOMENT-GLOBAL-LOWER-BOUND
Global-Knowledge-Sync: main@6043a5a / GLOBAL_KNOWLEDGE_V1
