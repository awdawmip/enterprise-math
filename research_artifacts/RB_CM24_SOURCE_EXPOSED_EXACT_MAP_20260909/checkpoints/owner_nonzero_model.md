# Owner paper review: nonzero and target-model obligations

This note concerns the frozen source at EM commit `341f36bb53c25d97c3ae533a71c92ad273a7b904`, task `RS-RB-CM24-SOURCE-EXPOSED-EXACT-MAP-CERTIFICATE`. It is source-exposed mathematical paper review by Driver `EM-DVR-01E1D9`, with no new computer execution, blind reconstruction, formal Result disposition, or task acceptance. The researcher must check and bind the reasoning to the completed proof/certificate. Model coordinates R,t,w are not identifications with Enterprise native axes.

## Faithfulness of the coefficient presentation

Let alpha be the selected positive fourth root of 3 and i the selected imaginary unit. Eisenstein at 3 gives degree four for Q(alpha); this field is real, so K0=Q(alpha,i) has degree eight over Q. It is the splitting field of x^4-3. It also has degree four over Q(i), so the map r fixing i and sending alpha to i*alpha defines an automorphism of order four. Complex conjugation s fixes alpha and negates i, and satisfies srs=r^(-1). These eight automorphisms give D4.

The commutator subgroup is generated by r^2. Its fixed field is Q(alpha^2,i), of degree four. Every quadratic subfield of K0 is contained in that fixed field: its degree-two Galois quotient is abelian and therefore kills the commutator. The three quadratic subfields of the biquadratic field Q(sqrt(3),i) are Q(sqrt(3)), Q(i), and Q(sqrt(-3)). None is Q(sqrt(2)). Consequently beta=sqrt(2) is not in K0, and L=K0(beta) has degree sixteen over Q.

It follows that the sixteen monomials alpha^a beta^b i^c, with 0<=a<4 and 0<=b,c<2, are a Q-basis. Thus the script's monic coefficient reduction is faithful under the declared embedding, once this field argument is adopted. A nonzero reduced coefficient vector does not disappear under that embedding. No evaluated root, quotient, numerical fit, or private numerical algebra API is required for this argument.

Over L(R), R^3-3R is not a square, because its zero at R=0 has odd order. Hence t^2=R^3-3R defines a quadratic function-field extension with basis 1,t. This justifies nonzero reduced R,t polynomials as functions. The cubic has distinct roots in characteristic zero and its projective elliptic model is nonsingular. The further F=(R+2)t is not a square on that curve: at (R,t)=(0,0), t has order one and R+2 is a unit. The double cover is therefore connected. Its smooth projective normalization is the appropriate source for all global-map claims.

## Constants and nonconstant X

Put s0=alpha^2. The four elements 1,beta,s0,s0*beta are linearly independent over Q. The frozen vectors

`lambda = 35 + 24 beta - 20 s0 - 14 s0 beta`,

`lambda-1 = 34 + 24 beta - 20 s0 - 14 s0 beta`,

`uN = -3 + 3 beta + 2 s0 - 2 s0 beta`,

`9 + 3 beta + 2 s0 + 4 s0 beta`

are all nonzero. This gives lambda!=0,1, uN!=0 and C4!=0. The identity `k^2=12 beta-10 s0` follows directly from the defining monic relations; its difference from 0 or +/-2 is also a nonzero vector in this biquadratic basis.

At the origin O of the elliptic carrier, ord(R)=-2 and ord(t)=-3. In N, the term `i alpha uN R^3` has order -6 and every other term has greater order. In D0 the term `R^2 t` has order -7 and every other term has greater order. Their coefficients are nonzero, so ord(N)=-6, ord(D0)=-7 and ord(X)=1. Thus both displayed sections are nonzero and X is nonconstant. As sections of L(7O), N vanishes once at O and D0 does not vanish there; O is not a common base point. This calculation does not determine every finite common base point or prove degree six.

In characteristic zero, nonconstant X has nonzero differential. Since 2delta is the carrier derivation, W=2D0^2 delta(X) is nonzero. The denominators t, w and t+k are likewise nonzero as functions. This supplies the nonzero function-field conditions for interpreting the already executed full cleared ODE as the stated rational identity, without claiming that all displayed affine quotients are regular pointwise.

## Exact target model and j

Write C=C4/4 symbolically. Once the target equation is proved, the invertible change x=CX, y=CY puts it into the monic model

`y^2 = x^3 - C(1+lambda)x^2 + C^2 lambda x`.

Its Weierstrass quantities are

`b2=-4C(1+lambda), b4=2C^2 lambda, b6=0, b8=-C^4 lambda^2`,

`c4=16C^2(1-lambda+lambda^2)`,

`Delta=16C^6 lambda^2(1-lambda)^2`.

The proven nonzero constants make Delta nonzero, so the projective target is elliptic. The exact invariant is

`j = 256(1-lambda+lambda^2)^3/[lambda^2(1-lambda)^2]`.

The C twist cancels from this ratio. This is an exact symbolic relation to the frozen model, not numerical fitting. Any further simplified radical expression or identification with a chosen CM period must be separately checked at its stated scope. In particular, this calculation supplies no homology/image index and no absolute period normalization.

## Remaining review boundary

This note and the first ODE checkpoint do not prove all four special-fiber divisors, every common-basepoint cancellation and multiplicity, degree six, the actual geometric unramified twists/half-points or original-field descent. They also do not replace the source-bound tamper checks, global differential proof, full certificate and durable return required by the unchanged eight-gate task.
