# D25 exact bridge: the raw d=3 coefficient digit is the existing finite-Gauss unit core plus explicit deformation/tail corrections

Status: `PROVED_STRICT_SOURCE_BRIDGE / NOT_A_RESULT / UNREVIEWED / LIFT_OPEN`

Task: `RS-ENTERPRISE-BRC-HALF-COUPLING-INERT-PLUS-D24-SECOND-DIGIT-LIFT`

Consumed durable frontier: Source `8fe4a6dbdbd3d7b3acd9458aafca675884605522`, checkpoint `9978dfcc94162d129237`, where

`a_raw(p-1)=p alpha_p`, `TRAW_p=alpha_p^2+Lcomp_p`, and `K_p=tau_p-alpha_p^2-Lcomp_p`.

The live instruction was to compute `alpha_p` from the exact d=3 coefficient before attempting the one-digit-deeper companion. This unit closes that half without identifying the Gauss-Manin coefficient `J_p` with an ordinary polynomial derivative.

## 1. Exact d=3 coefficient and target notation

Every target prime satisfies `p == 13 or 19 (mod 24)`, hence `p=6r+1`. Put `n=2r=(p-1)/3`. At `lambda=1/2`, choose the source branch

`t=(1-sqrt(1-lambda))/2=(2-sqrt(2))/4`,

so `4t(1-t)=1/2`. Work in the unramified quadratic p-adic coefficient ring generated by `sqrt(2)`; branch provenance is retained.

Chisholm--Deines--Long--Nebe--Swisher, Lemma 12, gives the exact source coefficient

`H_{3,p-1}(t)=sum_{j=0}^n C_j t^j`,

`C_j=binom(p-1-j,j) binom(p-1-2j,p-1-3j) / 27^j`.

Define

`A_j=(1/3)_j (2/3)_j/(j!)^2`,

`F_n(t)=sum_{j=0}^n A_j t^j`,

`D_p(t)=sum_{j=0}^n A_j (H_{3j}-H_j)t^j`,

where the harmonic denominators are p-units because `3j<=p-1`.

For every `0<=j<=n`,

`C_j=(p-1-j)!/[27^j (j!)^2 (p-1-3j)!]`

`    =A_j * product_{q=j+1}^{3j}(1-p/q)`.

There are `2j` negative factors in the intermediate product `product(p-q)`, so no sign is lost. Therefore, uniformly in the whole target population,

`C_j = A_j [1-p(H_{3j}-H_j)] (mod p^2)`,

and hence

`BOX 1: H_{3,p-1}(t)=F_n(t)-p D_p(t) (mod p^2)`.

This is an all-prime coefficient identity, not a finite scan.

## 2. One-order-deeper finite quadratic truncation

Use the classical quadratic transformation as a formal power-series identity at the selected branch:

`_2F1(1/3,2/3;1;t)=_2F1(1/6,1/3;1;4t(1-t))`.

Write

`B_k=(1/6)_k(1/3)_k/(k!)^2`,

`G_n(lambda)=sum_{k=0}^n B_k lambda^k`,

`T_p(t)=G_n(4t(1-t))-F_n(t)`.

The formal identity implies every coefficient of `T_p(t)` of t-degree `<=n` is exactly zero: a coefficient of degree `d<=n` cannot see a hypergeometric term with index `k>n`. For a surviving degree `d>n=2r`, one must have `k>d/2>r`. For every `r<k<=2r`, `(1/6)_k` contains exactly the factor `6r+1=p`, while `(1/3)_k`, `k!`, and all other factors are p-units. Thus `v_p(B_k)=1` on the entire contributing high band. Consequently

`T_p(t) in p Z_(p)[t]`.

Define the exact finite quadratic-tail quotient

`E_p(t)=T_p(t)/p`.

Equivalently, its mod-p polynomial is the high-degree part only:

`E_p(t)=sum_{k=r+1}^{2r} (B_k/p) 4^k sum_{u=2r-k+1}^{k} (-1)^u binom(k,u)t^{k+u} (mod p)`.

Therefore

`BOX 2: F_n(t)=G_n(1/2)-p E_p(t) (mod p^2)`

at the frozen target.

## 3. Exact identification with the already-owned finite-Gauss source

The durable cutoff finite-Gauss checkpoint uses

`c_k(a)=(-r+a)_k(-2r+2a)_k/(k!)^2`, evaluated at `a=p/6`, `z=1/2`.

But exactly

`-r+p/6=1/6`, `-2r+p/3=1/3`,

so its source term is

`c_k(p/6)2^{-k}=B_k 2^{-k}`.

The existing unit-band sum is

`S=sum_{k=0}^{2r} c_k(p/6)2^{-k}=G_n(1/2)`,

and its already-owned unit core is `G_T=S/p`. This is equality of the source bytes/algebra, not analogy.

The target is supersingular, so `H_{3,p-1}(t)` and `S` are p-divisible. Divide BOX 1+BOX 2 by p and reduce modulo p. The raw coefficient digit is therefore

`BOX 3: alpha_p = G_T - D_p(t) - E_p(t) (mod p)`.

Thus `alpha_p` is **not** a new independent Frobenius/source scalar. It factors through the finite-Gauss unit core already present in D25 plus two explicit, provenance-preserving correction functionals: the coefficient-deformation harmonic term `D_p` and the finite quadratic-tail term `E_p`.

## 4. Consequence for the only remaining new companion datum

Put `C_p(t)=D_p(t)+E_p(t)`. Combining with the durable companion decomposition gives

`TRAW_p=(G_T-C_p(t))^2+Lcomp_p (mod p)`,

`K_p=tau_p-(G_T-C_p(t))^2-Lcomp_p (mod p)`.

So after this unit, the source-side unknown pair `(alpha_p,Lcomp_p)` reduces to **one genuinely new companion scalar `Lcomp_p`**. The alpha coordinate is paid for by an exact bridge to the already-owned finite-Gauss carrier.

This does not prove LIFT/JT2: `Lcomp_p` still requires one p-adic digit of the normalized Gauss-Manin companion, and the product still requires the already-declared endpoint/finite-Clausen audit before it can be identified with the final LIFT target.

## 5. BRC audit

Population: exact d=3 source coefficient ports `j=0..2r` and the existing finite-Gauss ports `k=0..2r`.

Observer: `alpha_p=H_{3,p-1}(t)/p mod p`, subsequently only through the product-second-digit observer.

Preserved coordinates: branch `t`, p-adic valuation band, harmonic deformation provenance, quadratic-truncation edge `r/2r`, and the existing `G_T` identity.

Safe compression proved here: `alpha_p -> (G_T,D_p,E_p)` at this observer.

Unsafe compression: dropping `D_p` or `E_p`, replacing the quadratic tail by a mod-p-only transformation, or identifying `J_p` with an ordinary derivative.

`BRC_REUSE_RESOLUTION = COMPOSE_APPLIED + STRICT_REDUCTION_APPLIED`.

## 6. Deterministic falsifier/regression

The companion checker works in `(Z/p^2 Z)[s]/(s^2-2)` with `t=(2-s)/4`. For every 166 target primes below 5000 it independently reconstructs the exact source coefficient recurrence, the fixed-parameter F/G recurrences and harmonic correction and verifies: BOX 1; the one-p valuation of the quadratic high band; p-divisibility of the source H and finite-Gauss S; p-divisibility of `G_n-F_n`; and BOX 3. Zero failures were observed. The finite scan is regression/falsification only; the proof is Sections 1-3.

## 7. Narrow next action

Compute `Lcomp_p=(c_p alpha_p J_p-1)/p mod p` from the exact normalized d=3 Gauss-Manin companion one p-adic digit deeper, now substituting the proved `alpha_p=G_T-D_p-E_p`. Seek an exact bridge from that companion scalar to the existing weighted finite-Gauss cutoff jet/endpoint data. Preserve local-coordinate provenance; do not set `J_p=dH/dlambda` unless that coefficient identity is proved.

External source boundary: Chisholm--Deines--Long--Nebe--Swisher, *p-Adic Analogues of Ramanujan Type Formulas for 1/pi*, Mathematics 1 (2013), DOI 10.3390/math1010009, especially Lemma 12, Lemma 18 and the proof of Theorems 1--2.
