# D25 LIFT — universal prefix recurrence completely factors into provenance-preserving first-order transports

Status: PROVED_STRICT_REDUCTION_UNIT / STAGED_UNTIL_CHECKPOINT / LIFT_NOT_YET_CLOSED / NOT_A_RESULT / UNREVIEWED
Task: RS-ENTERPRISE-BRC-HALF-COUPLING-INERT-PLUS-D24-SECOND-DIGIT-LIFT
Publication: TP2-B6F4FC938FF94941C1B7
Date: 2026-09-24

## 0. Consumed frontier and de-duplication

Consume the current canonical Researcher checkpoint that reduces the complete reflected boundary to the low/original-kernel scalar and preserves the endpoint factor a_m*binom(3m,m). Also consume, only as cross-flow progress to avoid duplicate work, the independently persisted Driver observation that the resulting universal rational prefix R_s obeys an order-3 recurrence. The order-3 recurrence itself is not claimed here as a new Researcher contribution.

This unit rederives the prefix algebra from definitions and proves a sharper structural statement: the order-3 recurrence is d'Alembertian and completely factors into three first-order provenance-preserving transports. This changes the next proof target from generic order-3 recurrence matching to one explicit first-order curvature transport plus initial data.

## 1. Universal rational prefix

Let

  t_0=1,
  t_{s+1}/t_s = (2s+1)(3s+1)(3s+2) / [36(s+1)^3],
  w_s=(6s+1)t_s,
  P_s=sum_{h=0}^{s-1} w_h  (s>=1), P_0=0.

Let

  d_s=(1/6)_s s! / [(1/2)_s(2/3)_s],
  c_s=d_s/(6s^2),
  R_s=sum_{j=1}^s c_j P_j,  R_0=0.

At the target cutoff m=(p-1)/6, the previously consumed low/original-kernel identity identifies this R_m with twice the boundary scalar C_m, hence with the universal side of the surviving second-digit certificate. No claim is made here that the p-adic residual already equals R_m for all target primes; that equality remains LIFT.

Define the exact rational ratios

  rho_s := w_{s+1}/w_s
         = (2s+1)(3s+1)(3s+2)(6s+7)
           / [36(s+1)^3(6s+1)],

  lambda_s := c_{s+1}/c_s
            = s^2(6s+1) / [(s+1)(2s+1)(3s+2)].

## 2. First difference and gauged curvature

Define

  U_s := R_s-R_{s-1}=c_s P_s,

and the gauged second-difference coordinate

  V_s := U_{s+1}-lambda_s U_s.

Because P_{s+1}-P_s=w_s, this is not merely a recurrence consequence but an exact source identity:

  BOXED: V_s = c_{s+1} w_s.                              (F1)

Therefore V_s is itself hypergeometric. Its ratio is

  BOXED: V_{s+1}/V_s = mu_s := rho_s lambda_{s+1},       (F2)

where explicitly

  mu_s = (2s+1)(3s+1)(3s+2)(6s+7)^2
         / [36(s+1)(6s+1)(s+2)(2s+3)(3s+5)].

Thus the universal correction is generated by the exact three-stage chain

  R_s  --Delta-->  U_s
       --(E-lambda_s)--> V_s
       --(E-mu_s)--> 0,

with no loss of cutoff orientation or prefix provenance.

## 3. Complete factorization of the Driver order-3 recurrence

Eliminate U,V from (F1)-(F2). Since

  V_s=(R_{s+1}-R_s)-lambda_s(R_s-R_{s-1}),

(F2) is equivalent to

  R_{s+2}-(1+A_s)R_{s+1}+(A_s+B_s)R_s-B_s R_{s-1}=0,   (F3)

where

  A_s=(1+rho_s)lambda_{s+1},
  B_s=rho_s lambda_s lambda_{s+1}.

This is exactly the independently persisted order-3 recurrence, but (F1)-(F2) prove that it completely factors. In operator language, after retaining the labelled first-difference coordinate,

  (E-mu_s)(E-lambda_s) Delta R = 0,

with the usual index shift encoded by the definitions above. The factorization is an identity of rational functions in s; no finite fitting is used.

Initial data are

  R_0=0,  R_1=1/12,  R_2=143/1296,

so

  U_1=1/12,
  V_1=U_2-lambda_1 U_1=49/6480.

## 4. Exact matching criterion for a p-adic cutoff extension

Let Q_s be any source-derived candidate extension of the p-adic residual to adjacent integer cutoffs, over a field in which the displayed denominators are units. Define

  U^Q_s=Q_s-Q_{s-1},
  V^Q_s=U^Q_{s+1}-lambda_s U^Q_s.

Then Q_s=R_s for all s in a connected cutoff interval if and only if the following provenance-labelled data agree at one start point and the curvature transport holds throughout:

  Q_0=R_0,
  U^Q_1=U_1,
  V^Q_1=V_1,
  V^Q_{s+1}=mu_s V^Q_s.                                 (F4)

Proof: (F4) uniquely propagates V; the definition of V then uniquely propagates U through U_{s+1}=lambda_s U_s+V_s; Delta then uniquely propagates Q. The converse is immediate.

Therefore the remaining D25 proof should not search blindly for an irreducible order-3 recurrence. It suffices to construct a legitimate p-adic cutoff extension of the residual and prove one first-order curvature ratio together with the three initial/provenance coordinates. Conversely, merely observing numerical agreement with (F3) at target primes is not a proof because adjacent-cutoff p-adic source semantics have not yet been supplied.

## 5. BRC audit

Observer: the universal low-kernel correction R_m at cutoff m, together with the exact information needed to compare it to a p-adic residual extension.

Preserved coordinates: cutoff s, original weighted prefix P_s, first difference U_s, gauged curvature V_s, and the rational transport ratios lambda_s, rho_s, mu_s.

Safe compression: the scalar order-3 recurrence may be replaced by the labelled first-order chain only together with initial Q/U/V data; the reconstruction above is exact.

Forbidden compression: do not drop U or V and then claim the p-adic residual has been identified; do not define Q_s by R_s itself; do not use target-prime samples as a substitute for deriving Q_s from G_p,h or an equivalent source object.

BRC_REUSE_RESOLUTION=COMPOSE_APPLIED.

## 6. Narrow next action

Build the p-adic cutoff extension from the second p-adic expansion of the source object (G_p h-1)/p or an equivalent creative-microscoping deformation. Compute its labelled U^Q and V^Q before quotienting. The sharp target is now the first-order identity V^Q_{s+1}=mu_s V^Q_s plus Q_0,U^Q_1,V^Q_1. If that first-order transport fails, record the exact defect term; it is then the minimal obstruction to LIFT in this representation.
