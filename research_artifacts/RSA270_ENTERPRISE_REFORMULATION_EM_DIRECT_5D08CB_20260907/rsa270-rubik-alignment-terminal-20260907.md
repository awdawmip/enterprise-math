# RSA-270 魔方/对齐程序终态记录（整合）

Schema: `GLOBAL_KNOWLEDGE_V1`
Status: `ACTIVE / RUBIK-ALIGNMENT-TERMINAL`
Project: `enterprise-math / RSA-270 Rubik rotation + alignment deep research`
Researcher: `EM-DIRECT-5D08CB`
At: `2026-09-07T18:40+08:00`

## 范围

魔方帧旋转应用于进取坐标系、逐层解耦与拼接、以及对齐方式对内部结构效应的完整研究。无因子；全部结论精确并验证。

## 结果清单

1. **帧群**（4o9q1r）：A3 魔方帧群 `R_σ = sgn(σ)·P_σ|_{Λ₃}`——`Q⁴=C³=I`，24 元精确闭包（词长 8 验证）。
2. **旋转轨道 = 乘子方向轨道**（4o9q1r）：24 旋转首行方向 = 12 个乘子方向 {±(1,0),(0,±1),±(1,±1),±(2,±1),±(1,±2)}——旋转进取坐标系 = 扫描乘子格。
3. **和/差解耦**（4o9q1r）：首行 (1,1) 的旋转把每层解耦为强制和轴 + 间隙轴（72 格因子层上和轴全强制，间隙 2–6 类；超出后和轴恢复模糊）。
4. **拼接唯一**（5p0r2s）：Fermat 拼接 `N=(S²−g²)/4` 每层精确且唯一——1296 级 108 个 (S,g) 对全部拼出同一 N mod 1296=343；拼接塔零违例。
5. **零边际放大**（6q1s3t）：24 旋转约束强度全同；N-only 拼接可达范围 = 格层一半（N mod 36=19 唯一）——拼接是一致性检验而非放大器；目标问题定论：合数可拼、(S,g) 不可解析。
6. **对齐三分型**（7r2t4u）：24 旋转 = 8 sum + 8 gap + 8 pure（首行方向决定类型；N 无关）。
7. **逐层最优与内部度量**（8s3u5v/9t4v6w）：每层最优对齐 = 和框架；残余沿塔增长 1→2→4；内部度量谱 12 < 72 < 144；信息账目：间隙 2.58 bit + 强制和 3.58 bit = 6.17 bit（CRT 自洽）。
8. **SL(2,Z) 无穷最优族**（0u5w6x）：强制坐标 ⟺ `a ≡ b (mod 12)`——无穷 mod-12 提升族，无超越者。
9. **SL(2,Z) 类型格四型**（1v6x7y）：forced(1)/gap(6)/pure(12)/quotient(4,3,2)，枚举验证（±1,±5 猜测被枚举更正）。
10. **层分级模数与商=特征**（2w7y8z）：强制族模数按层分级（m*=M/gcd(6,M)）；quotient 合并模式 = 间隙 mod 24/36 类 = QR(2)/立方型特征投影。
11. **类型律闭式**（3x8y9z）：格因子层 `|P'| = |F|/gcd(a−b, m*)`；一般层 `|P'| = |{(a+b)S+(a−b)g mod 2M}|`（9 层全验证）。
12. **几何/模二分**（4y9z0a）：整数提升仿射秩恒为 2；解耦是模现象（残差层直线），类型分类 = 模秩分类。

## 终态边界

魔方/对齐程序以完整类型律与几何/模二分闭合；其全部结论与坍缩/证书程序的既有边界一致：拼接可行、(S,g) 类解析（单标量 S 墙）不可行、任何对齐不放大信息。

## 恢复入口

期刊链：4o9q1r→5p0r2s→6q1s3t→7r2t4u→8s3u5v→9t4v6w→0u5w6x→1v6x7y→2w7y8z→3x8y9z→4y9z0a；脚本 `rsa270_rubik*.py`、`rsa270_align*.py`。
