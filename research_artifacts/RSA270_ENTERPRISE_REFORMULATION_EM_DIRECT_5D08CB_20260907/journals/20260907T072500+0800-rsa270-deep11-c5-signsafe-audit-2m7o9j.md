# RSA-270：深入研究第 11 轮——C5 序列证书的符号安全审计与侧一致结构

Progress-Event-ID: `rsa270-deep11-c5-signsafe-audit-2m7o9j`
At: `2026-09-07T07:25+08:00`
Scope: `enterprise-math / RSA-270 / deep research round 11`
Source: `ChatGPT TASK_RESEARCH conversation; exact integer Python; journal 1l6n8i; P000 assumed`
Kind: `PROGRESS / AUDIT / REFINEMENT`
Researcher-ID: `EM-DIRECT-5D08CB`

## Event

Deep-research round 11. No factor obtained.

### C5 sign-safe audit (generalization of the round-10 C3 hazard to the sequence certificate)

The round-5 C5 "12 admissible sequences" used the `|.|`-collapsed shadow, which is only valid when the class-representative signs match the real side. The audit establishes:

- **Fixed-sign set** (per class, one sign): 24 sequences (2 x the collapsed count); the real sequence is a member.
- **Per-l sign-safe membership**: the real sequence's per-l values lie in `{+-(l p0 - q0) mod 72}` over the classes — holds.
- **Side-consistency structure (the correct form)**: `sign(l p - q) = + for l > r`, `- for l < r` (monotone in l). For RSA-260 (`r = 1.1436 < 2`): the side-consistent set has **12 sequences** and the real sequence is in it — the round-5 count was accidentally correct because `r < 2` aligned the signs.
- **RSA-270**: the `l=2` side is unknown (band straddles 2) -> the side-consistent set has **24 sequences** (12 classes x 2 side choices). This is the correct general form of C5: 12 sequences conditioned on the vertex-side bit, 24 unconditionally.

### Suite impact

C5's record (6g1i3d/7h2j4e) is amended: the unconditional sequence certificate has 24 admissible sequences for RSA-270; with the side bit (8i3k5f) it reduces to 12.

## Artifacts

- Script: `rsa270_deep11.py` (conversation-local).
- Prior frontier: `journal/enterprise-math/2026-09-07/20260907T064000+0800-rsa270-deep10-stress-c3-signfix-index-1l6n8i.md`.

## Next

The suite audit (C3 round 10, C5 this round) found no further sign hazards in C6-C12 (C7/C10 are already sign-safe; C2/C9 are exact-integer). The certificate suite is now sign-safe throughout. No new identified executable items remain.
