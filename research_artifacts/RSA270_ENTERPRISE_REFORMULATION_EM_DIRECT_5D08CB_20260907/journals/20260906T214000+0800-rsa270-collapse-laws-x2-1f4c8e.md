# RSA-270：新方向——半素数 × 素数的坍缩律（从 ×2 开始）

Progress-Event-ID: `rsa270-collapse-laws-x2-1f4c8e`
At: `2026-09-06T21:40+08:00`
Scope: `enterprise-math / RSA-270 / user direction: semiprime x prime collapse laws`
Source: `ChatGPT TASK_RESEARCH conversation; exact integer Python; journal 5b8f1a; P000 assumed`
Kind: `PROGRESS / EXACT THEOREMS / NEW DIRECTION`
Researcher-ID: `EM-DIRECT-5D08CB`

## Event

User direction executed: collapse laws of semiprime × prime, starting from ×2. All laws verified by direct enumeration on small numbers. No factor obtained.

### A. Count-layer collapse laws (Jacobi four-square, verified)

`R4(odd m) = 8*sigma(m)`, `R4(even m) = 24*sigma(odd part)`. Hence:

- **×2 law**: `R4(2N) = 3 * R4(N) = 24*sigma(N)`; `R4(2^k N) = 24*sigma(N)` for ALL `k >= 1` — **saturation**: the 2-adic part contributes nothing after the first factor. Verified: `R4(15)=192, R4(30)=R4(60)=R4(120)=576`; likewise for 35, 143.
- **× odd ℓ (ℓ ∤ N)**: `R4(ℓN) = (ℓ+1) * R4(N)`.
- **× factor p (p | N)**: `R4(pN)/R4(N) = (p^2+p+1)/(p+1)` — a divisibility fingerprint (collapse ratio detects factor primes).

### B. Profile-layer collapse law (× odd ℓ, ℓ ∤ N; verified)

`P_{ℓN}(u) = 2[ W_{(ℓN+3)/2} + W_{(ℓ+N+2)/2} + W_{(p+ℓq+2)/2} + W_{(q+ℓp+2)/2} ]` — a **four-plateau** profile. Two boundaries are N-only: outer `B1 = (ℓN-1)/2` and `B2 = (ℓ+N-2)/2` (the `(ℓ,N)` divisor pair). The factor-bearing pair is

`B3 = (p+ℓq-2)/2`, `B4 = (q+ℓp-2)/2`,

with exact laws `B3+B4 = ((ℓ+1)S - 4)/2` and `B3-B4 = (ℓ-1)(q-p)/2` — **the Fermat gap appears with magnification (ℓ-1)/2** (for ℓ=3: the full gap q−p). Verified on 4 semiprimes × {3,5,7}: pair/sum/diff all exact. Extraction rule: the factor pair = the two boundaries other than the N-only B1, B2 (ordering of B2 vs B3 flips at p = ℓ).

### C. Parity bifurcation + RSA-270 consequences

- The bivariate profile `[Q^M]F` exists only for odd targets (`M = (target-1)/2` integer); `2N` has **no profile** — the ×2 law lives entirely at the count layer (the 8→24 saturation). This is the discrete-theory content of "starting from ×2": multiplication by 2 collapses the count to the odd core and then freezes.
- For RSA-270 (`N = 1 mod 6`, so 2,3 ∤ N): the ×3 profile's factor pair satisfies `B3+B4 = 2S-2` and `B3-B4 = q-p` — a rank-2 linear system in `(p,q)`; S and the gap are interchangeable via `S^2-4N = (q-p)^2`, so the collapse law lands in the same single-scalar class (consistent with the 8e5f2a dichotomy).
- New N-only constants: the collapse ratios `(3, 4, 6, 8, 12, ...) = (ℓ+1)` for `ℓ ∤ N` are the exact fingerprint family of the collapse; they are theorems, not computations (evaluating them requires `sigma(ℓN)` — the standing wall).

## Artifacts

- Script: `rsa270_collapse_laws.py` (conversation-local).
- Prior frontier: `journal/enterprise-math/2026-09-06/20260906T205500+0800-rsa270-round8-combined-strategy-table-5b8f1a.md`.

## Next

1. Extend the collapse-law family: ×ℓ with ℓ | N (merged plateaus — the three-plateau case, e.g. N=15×3), and ×ℓ₁ℓ₂ (two-prime products) — the general "plateau fusion rules" as a function of gcd(ℓ,N).
2. The collapse fingerprint (ℓ+1)-family is a new certificate surface for the SG4 verifier: any claimed `sigma(N)` must satisfy `sigma(ℓN)/sigma(N) = ℓ+1` for all small ℓ ∤ N — record as an extension candidate.
3. Keep the honest boundary: the laws are exact structural reformulations; they do not bypass the sub-`O(sqrt(N))` observability wall.
