# RSA-270：子目标 SG2/SG3/SG4 完成——强制零点族一般定理、组合闭包、指纹验证器

Progress-Event-ID: `rsa270-sg234-combination-closure-2f7a9c`
At: `2026-09-06T18:20+08:00`
Scope: `enterprise-math / RSA-270 / sub-goals SG2 SG3 SG4 + combination step`
Source: `ChatGPT TASK_RESEARCH conversation; exact integer Python; journals 8e5f2a; P000 assumed`
Kind: `PROGRESS / THEOREMS / TOOL`
Researcher-ID: `EM-DIRECT-5D08CB`

## Event

Sub-goals SG2-SG4 completed and verified; the combination step is the closure theorem of SG3.

### SG2 — general value-forced zero family (theorem, verified)

For odd `N` under the construction-family prior: the 2-adic forced depth is `a(N)=3` iff `N=7 mod 8` else `a(N)=2`; the 3-adic forced depth is `b(N)=2` always (`S mod 9` forced, `S mod 27` two classes). The value-forced zero family is exactly

`{ m >= 3 : m | 2^a * 9  AND  B(w_m) = 0 at the forced class S = s_bar mod 2^a*9 }`

with `B(u) = u^((S-N-1)/2)(1-u^(N+1)) + (1-u^S)`. Verified against full admissible-class enumeration for `m <= 100` on 4 synthetic prior-consistent semiprimes (N mod 8 in {1,7}): families `{3,6,9,18}` (a=2) and `{3,4,6,8,9,12,18,36}` (a=3), both exactly reproduced; no members outside the divisors of `2^a*9`. RSA-270's `{3,4,6,8,9,12,18,36}` is the a=3 corollary.

### SG3 — combination closure theorem

The joint observable algebra of all four sub-goal outputs is exactly

`{ S mod 2m residue classes }  UNION  { unobservable S-linear functions }`.

Periodic families (value ladder, zero set, forced lattice, carry automaton) pin `S = 16 mod 72` (forced) plus the two-class split `S mod 144 in {16,88}`; the affine families (SG1 derivative ladder, u=+-1 mass) are S-linear (factoring-equivalent) but require the profile. **The combination adds no new channel**; the N-only computable part of S remains `S = 16 mod 72` (~6.17 bits). This is the combination result the sub-goal program was designed to test: verified, not assumed.

### SG4 — fingerprint verifier (tool, out-of-sample run)

`verify_sigma(S, N)` checks: S even; `A^2 - N` perfect square (`A=S/2`); `p*q == N`; p,q probable prime (MR); `p,q = 2 mod 3`; S matches the N-derived 2-adic forced class; S matches the N-derived prior-forced class mod 72; and the 62-point torsion fingerprint (`m=3..64`, closed-form vs `K_m + 2 g_m(r1)`). Runs:

- RSA-260 real factors: **PASS all 7 checks** (out-of-sample);
- RSA-260 wrong S (+144): fails at the square check, as designed.

The verifier is the reusable packaged form of the entire certificate line (7c4d1f/5d8a2c/6d4e8b/8e5f2a).

## Artifacts

- Script: `rsa270_sg234.py` (conversation-local; contains the SG2 theorem verification and the SG4 verifier).
- Prior frontier: `journal/enterprise-math/2026-09-06/20260906T174000+0800-rsa270-sg1-derivative-affine-ladder-8e5f2a.md`.

## Next

The sub-goal program (SG1-SG4) is complete and the combination is closed. Remaining for the parent objective: an S-affine observable that is ALSO cheaply N-only computable — none is known, and the dichotomy theorem (8e5f2a) states no such family exists among the profile ladders. Next rounds should either (1) await external inputs (Eric Lu RSA-260 method disclosure / RSA DSP generation provenance), or (2) take a new sub-goal only if it introduces a genuinely new observable class beyond the dichotomy.
