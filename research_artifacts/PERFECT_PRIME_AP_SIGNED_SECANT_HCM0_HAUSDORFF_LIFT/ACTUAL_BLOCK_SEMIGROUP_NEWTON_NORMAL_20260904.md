# Perfect Prime AP HCM0 — actual block semigroup / positive-Newton normal

Task: `RS-PERFECT-PRIME-AP-SIGNED-SECANT-HCM0-HAUSDORFF-LIFT`  
Publication: `TP2-7A2D91C5E40B836F19D2`  
Researcher: `EM-HCM0-HL-FB0860`  
Claim: `CLM-HCM0HL-6F8E2D4389B17C04A521`  
Date: 2026-09-04  
Status: **NONTERMINAL ALL-m STRUCTURAL THEOREM — HCM0 REMAINS OPEN**

## 1. Why the correct variables are consecutive block lengths

The actual layer shifts are `c_s=m^2 s`.  After scaling the interpolation grid `Y_j=mj` to `j`, one layer step is polynomial translation by exactly `m`.

Let `E` denote translation by `m` on the quotient

\[
\mathbb P_n/\mathbf 1,\qquad n=m-1,
\]

and put

\[
N=E-I.
\]

Because a translation difference lowers degree, `N` is nilpotent of index at most `n`:

\[
N^n=0.
\tag{1.1}
\]

For an actual gap of `a>=1` layers the transfer is `E^a`, not a generic translation matrix.  Therefore

\[
\boxed{
E^a-I=N\,S_a(E),
\qquad
S_a(E)=I+E+\cdots+E^{a-1}.
}
\tag{1.2}
\]

This is the algebraic reason that the natural parameters for a three-layer configuration

\[
s_0<s_1<s_2
\]

are the two consecutive positive block lengths

\[
a=s_1-s_0,\qquad c=s_2-s_1,
\]

rather than the two absolute translations `M=m a`, `N=m(a+c)`.

## 2. Exact positive Newton expansion

On the quotient, `E=I+N`.  Hence

\[
S_a(E)
=\sum_{h=0}^{a-1}(I+N)^h
=\sum_{\ell=0}^{n-1}
\left(\sum_{h=0}^{a-1}\binom h\ell\right)N^\ell.
\]

The hockey-stick identity gives

\[
\boxed{
S_a(E)=\sum_{\ell=0}^{n-1}\binom a{\ell+1}N^\ell.
}
\tag{2.1}
\]

Thus every coefficient of the actual gap operator is positive in the Newton/binomial basis in the block length `a`.

For the second endpoint,

\[
S_{a+c}(E)
=\sum_{\ell=0}^{n-1}\binom{a+c}{\ell+1}N^\ell,
\]

and Vandermonde gives

\[
\boxed{
\binom{a+c}{k}
=\sum_{p=0}^{k}\binom ap\binom c{k-p}.
}
\tag{2.2}
\]

Therefore every entry of `S_(a+c)(E)` has a positive expansion in the tensor Newton cone generated by

\[
\binom ap\binom cq,
\qquad p,q\ge0.
\]

No analogous positivity is visible if one treats `M` and `N` as unrelated absolute variables.

## 3. Exact adjacent-layer factorization of every actual quotient form

Let

\[
D_s=\operatorname{diag}\left(
(-1)^j\binom nj\mu_{j+ms}
\right)_{j=0}^{n},
\qquad
\mu_r=\int_0^1v^r\,d\nu_m(v).
\]

In the accepted actual-layer value-space normal form,

\[
Q_{s_0,s_0+a}
=(E^a-I)^T D_{s_0+a}(E^a-I)
\]

on the quotient by constants.  Define the **adjacent endpoint form**

\[
A_s=(E-I)^TD_s(E-I).
\tag{3.1}
\]

Then (1.2) gives the all-`m` identity

\[
\boxed{
Q_{s_0,s_0+a}
=S_a(E)^T A_{s_0+a}S_a(E).
}
\tag{3.2}
\]

Thus an arbitrary actual gap consists of exactly two ingredients:

1. a one-layer quotient form evaluated at the terminal layer;
2. a geometric-sum basis operator with the positive Newton expansion (2.1).

The same identity for the third layer is

\[
\boxed{
Q_{s_0,s_0+a+c}
=S_{a+c}(E)^T A_{s_0+a+c}S_{a+c}(E).
}
\tag{3.3}
\]

Moreover

\[
\boxed{
S_{a+c}(E)=S_a(E)+E^aS_c(E),
}
\tag{3.4}
\]

which is the exact operator form of the two consecutive positive block lengths.

## 4. Determinant consequence and the known coalescence factor

Because `E` is unipotent, every eigenvalue of `S_a(E)` equals `a`.  Hence

\[
\boxed{\det S_a(E)=a^n.}
\tag{4.1}
\]

Equation (3.2) therefore yields

\[
\boxed{
\det Q_{s_0,s_0+a}
=a^{2n}\det A_{s_0+a}.
}
\tag{4.2}
\]

Restoring the physical shift `m^2 a`, the fixed one-layer factor inside `A_s` supplies the corresponding power of `m`, and (4.2) is the semigroup version of the already proved two-shift coalescence factor `(c-d)^(2n)`.

This is an internal consistency check, not a new two-shift result.

## 5. Moment progression is governed by the same block variable

The endpoint metric also advances by the same layer count.  From the Hausdorff representation,

\[
\mu_{j+m(s_0+a)}
=\int_0^1v^{j+ms_0}(v^m)^a\,d\nu_m(v).
\tag{5.1}
\]

Thus the same positive integer `a` which appears in the operator power `E^a` appears as the ordinary power of the scalar contraction

\[
z=v^m\in(0,1).
\]

For the second increment,

\[
\mu_{j+m(s_0+a+c)}
=\int_0^1v^{j+ms_0}z^a z^c\,d\nu_m(v).
\tag{5.2}
\]

This is the precise coupling that is absent from the false arbitrary-integer-shift conjecture.

## 6. New reduced three-layer interface

For a genuine actual triple, the base-slice pencil is now

\[
\boxed{
S_a(E)^TA_{s_0+a}S_a(E)
+t\,S_{a+c}(E)^TA_{s_0+a+c}S_{a+c}(E),
\qquad a,c\in\mathbb Z_{>0}.
}
\tag{6.1}
\]

All dependence on the two gaps is forced through:

- the positive Newton expansions in `a,c` from (2.1)-(2.2);
- the endpoint Hausdorff powers `z^a,z^(a+c)` from (5.1)-(5.2);
- one fixed nilpotent one-layer difference operator `N=E-I`.

This is strictly narrower than `M5_BLOCK_SEPARATED_MELLIN_EULER_PENCIL`, which allowed every integer `M>=m`.  The actual Perfect-Prime problem has the stronger semigroup synchronization above.

## 7. Connection with the first dangerous coefficient

The first coefficient that failed below the block scale in exact experiments is

\[
[t^{n-1}]\det(A+tB)=\det(B)\operatorname{tr}(B^{-1}A).
\]

For `m=6`, a symbolic expansion in unrelated absolute variables `M,N` contains negative monomial coefficients, while substituting

\[
M=ma,\qquad N=m(a+c)
\]
removes those observed negative coefficients.  The present theorem explains why this change of variables is structural rather than cosmetic: it is exactly the semigroup/Newton decomposition (2.1)-(3.4).

No all-`m` trace positivity is claimed here.

The next precise target is:

`M6_ACTUAL_TWO_BLOCK_NEWTON_TRACE_POSITIVITY`:

> after extracting the known positive denominators from
> `tr(Q_(s0,s0+a+c)^(-1) Q_(s0,s0+a))`, prove positivity directly in the tensor Newton basis `binom(a,p) binom(c,q)` for all `m>=2`, `a,c>=1`.

A proof of `M6` would settle the full `t^(n-1)` coefficient for every actual three-layer base slice and would be the first all-`m` genuinely three-support coefficient beyond the two-shift theorem.

## 8. Boundary

Proved all-`m` here:

1. actual gap semigroup factorization (1.2);
2. positive Newton expansion (2.1) and tensor expansion (2.2);
3. adjacent endpoint factorization of every actual quotient form (3.2)-(3.3);
4. exact two-increment law (3.4);
5. determinant factor (4.2);
6. synchronization with Hausdorff powers (5.1)-(5.2).

Still open:

- `M6_ACTUAL_TWO_BLOCK_NEWTON_TRACE_POSITIVITY`;
- the entire actual three-support coefficient theorem;
- all-support layer sign regularity;
- HCM0 and the parent determinant theorem.
