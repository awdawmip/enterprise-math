# JT2 bounded BRC interface classification and non-author review packet

Status: `EXISTING_INTERFACE_CLASSIFICATION / NO_NEW_TOOL / MATHEMATICAL_REVIEW_PENDING`.

Driver: `EM-DVR-C777E7`. This supplements the Driver-authored mathematical candidate at source `9e3a41dc9909ad98c789bb8d53a27addd0649eaa`; it does not accept that candidate or alter a current task's hard target.

## 1. Actual coverage boundary

The managed main reference actually observed for this check was `562df4628f1896bd0faca3706efbacdb78f189d0`. Its shipped `tools/enterprise_toolbox.py`, registry, base inventory and 66 addenda were read as 69 exact Git blobs. The real metadata loader returned 107 methods without duplicate IDs. These are observed counts, not permanent expected-size assertions. Two bounded queries were executed:

- `precision valuation quotient division remainder`
- `quotient factor through composition repair future-safe`

The unchanged metadata router's `tool_suggestions` and `method_suggestions` were used with their actual parameterized metadata inputs. No mathematical module or consumer was imported or run; `module_suggestions`/whole executable-source discovery was not called. The raw result is `brc_lookup_receipt.json`, SHA256 `dbab7ee5325888d85c2220166bdb9c92995366718018a6b8b9bb3c2000329401`.

The first `git archive` attempt failed on unavailable promisor object `2aa9450ee598d892d2df7ab3dbe0100ad23102ae`. A separate bounded check found that all 69 selected metadata blobs were already present. Reading those exact blobs directly succeeded; no remote lazy fetch, metadata repair or retry of mathematics occurred. The initial failure and the successful metadata execution are separately recorded.

## 2. Reuse classification

| Existing identity | Legitimate relation to this candidate | Boundary retained |
| --- | --- | --- |
| `T0_BRC` | Structural support/result/provenance discipline: preserve the term/address and discarded precision data before accepting a projected observation. | No weighted-BRC population was executed. Signed coefficient differences are algebraic analysis, not negative native BRC branches. A Boolean match does not recover erased coefficients or p-adic digits. |
| `precision.integer_projection_calculus` / `T5_PRECISION_REFINEMENT` | Existing projection/detail/recomposition language identifies which quotient and lost digit must remain available. Actual APIs include `project_precision(value, coarse, fine)`, `precision_detail(value, coarse, fine)` and `recompose_precision(coarse_value, detail, coarse, fine)`. | These APIs take nonnegative integers and comparable positive scales. `project_precision` is a floor quotient, not automatically the low-digit residue observer on Z_(p). The inspected historical source contains direct `//`, `%`, `divmod` and a direct root helper; no transitive native-runtime compliance or execution reuse is claimed. |
| `quotient.composition_safe_collapse` / `T6_OPERATION_SAFE_QUOTIENT` | Its fiber-constancy criterion is the appropriate existing structural interface for declaring which observations survive a quotient. `fiber_constancy_witness`, `descends_through`, `induced_map` and `coarsest_one_step_repair` consume a finite distinct domain plus total coarse/observed maps. | This finite-state API cannot by one call certify a symbolic family over all primes. The candidate's uniform polynomial proofs remain necessary. No finite enumeration is substituted for them, and no API call was performed. |
| `symmetry.finite_group_action` / `T7_FINITE_SYMMETRY_EQUIVARIANCE` | The labeled involution t→-t is a potential finite symmetry interface for a later concrete observer. | Its method status remains `TOOLBOX_INTEGRATION_CANDIDATE`. The two ports and their defects cannot be erased merely because they belong to one orbit. No enumerator was executed or promoted. |
| `holonomy.precision_defect_transport` / `T9_HOLONOMY_COCOYCLE_GLUING` | A retrieved, related defect/coherence vocabulary. | Sun parity defects have not been identified with a particular implemented cocycle. This is `BOUNDARY_ONLY`, not applied holonomy and not a proof of the missing adjacent-jet elimination. |

Conclusion: reuse existing structural contracts at paper scope; no new tool family or generic rational/modular wrapper is justified by this packet. The exact finite truncation lemmas are a candidate mathematical composition to review. Lexical matches do not independently prove them or certify all their implementations as migrated.

## 3. Retained input/output contract for any later consumer

For each fixed prime p=6m+1, retain the finite indices 0≤k<p, the coefficient numerators and p-unit denominators, the finite polynomial degree/term address, forced p valuations, and the chosen observation horizon. The formal coefficient ring is Z_(p), and the two ports are carried in Z_(p)[t]/(2t²-1). This is not a request to numerically extract a square root.

The proved-candidate outputs are only the two p² polynomial congruences, their typed substitution at u=(1∓t)/2, and the first-digit slope-value consequence under the explicit source hypotheses. Keep the p² tail information before any quotient if a later observation asks for p³. In particular G_p modulo p², h modulo p², R_p, the labeled parity defects and the task's exact QTF3 address cannot be reconstructed from a Boolean UR flag or W_p modulo p².

Two kinds of division must stay distinct. Factorial/2/3 denominators below p are units in Z_(p); division of g by the nonunit p is legal only after the CM0 divisibility proof. Neither condition licenses materializing a quotient with a historical direct operator. The current runtime protocol allows unevaluated DIV/ROOT carriers and integer cross multiplication; any future quotient, remainder, divisibility readout or root evaluation must use the existing BRC facade with the required reconstruction trace. Sign data and explicit denominator/unit witnesses must survive that conversion. No such runtime was invoked here, and this packet neither proposes a new facade nor rewrites old source.

The remaining QTF3/LIFT issue is presently a missing mathematical identification/normalization at p³, not an established inability of BRC to represent the retained state. Calling it a new tool gap would need a specific input/output failure that this bounded check has not found.

## 4. Minimal non-author review assignment

This is an advisory review packet, not a new formal Task-ID, TP2, CLAIM, audit dependency release or Driver disposition. It belongs to the existing JT2 line's candidate-source review. The reviewer must be a non-author and disclose any shared input/context; this author cannot sign the independent outcome.

Frozen controlling candidate input: `9e3a41dc9909ad98c789bb8d53a27addd0649eaa`, under `research_artifacts/JT2_PERSISTENT_LINE_DRIVER_20260909/reviews/ur_reuse_20260909/`:

- `driver_authored_transport_candidate.md`, SHA256 `063ac8f6f31f83f0b9e16a3a3520aebd52527eb8237a28f07a833fa0f6d28eee`.
- `source_manifest.json`, SHA256 `58f5400335b80839a44c25482febb68a3b452dd1e3f4bf4dfcd894e2a205131a`; it pins eight prior sources and four relevant primary references.
- `read_receipt.json`, SHA256 `459236e1a8f9d25c83ae2384c26a3ab38819f690851ded1b95579f3958e852bf`; it separates direct reads, inherited pins and document-access failures.

Review six bounded points: the two finite truncation proofs; legal p division and slope sign; Chisholm d=3/CM/inert/good-reduction specialization; the visually recovered Borwein N=2, s=1/6, b_s/a_s=6 convention; the Sun six-output boundary; and the unchanged QTF3/LIFT precision/address gap. The complete questions are in the source manifest. Reuse the pinned original CM0/SIMPLE proof as an explicitly declared input or identify one exact defect; do not rerun the old 77-prime checks merely for review.

Deliver a short source-pinned paper-review report with actual read scope and either specific corrections or a qualified pass. Stop at this candidate's boundary. Do not solve LIFT, construct a new consumer, supply a fake minimal obstruction, combine the two parent tasks, or infer formal acceptance from source publication. A qualified pass still does not supply Sun outputs 3/6 or the independent final JT2 audit.

## 5. Exact inspected implementation/protocol pins

All three are from the observed main snapshot above. These are static source reads, not selected runtime-gate PASS claims.

| Path | SHA256 | Git blob |
| --- | --- | --- |
| src/enterprise_math/precision.py | `d8a19c7e4a319b5e9b17b66a1b5ef4ac563bd984815c7355bd73784e0bc521d8` | `0f028d00b2126b7a3b13bcfb8db627fc542af195` |
| src/enterprise_math/composition_safe_collapse.py | `a5031cbd860dbb9195e02fa872dc1abbc10e2b27f47a397125749b8078474529` | `384d166f642fb65c53fc7f2431f43dc99880693a` |
| docs/EXACT_ARITHMETIC_BRC_RUNTIME_PROTOCOL.md | `007ecf39bd3d18247d1b80006503390b04d3eecd39e32c399f7f9b4e50532560` | `ccc45e61a1b76dec34d6e779b93e12d1f380724d` |

Driver-ID: EM-DVR-C777E7 / CONTROL_PLANE

Global-Knowledge-Sync: main@5f14819 / GLOBAL_KNOWLEDGE_V1
