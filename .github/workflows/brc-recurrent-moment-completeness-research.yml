name: brc-recurrent-moment-completeness-research

on:
  pull_request:
    paths:
      - 'research_notes/BRC_RECURRENT_BRANCH_MOMENT_COMPLETENESS_20260903.md'
      - 'experiments/brc_recurrent_branch_moment_completeness_check.py'
      - 'research_notes/BRC_RECURRENT_BRANCH_MOMENT_LIFT_20260903.md'
      - 'experiments/brc_recurrent_branch_moment_lift_check.py'
      - '.github/workflows/brc-recurrent-moment-completeness-research.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Exact recurrent branch moment-lift checker
        run: python experiments/brc_recurrent_branch_moment_lift_check.py
      - name: Exact finite branch-moment completeness checker
        run: python experiments/brc_recurrent_branch_moment_completeness_check.py
