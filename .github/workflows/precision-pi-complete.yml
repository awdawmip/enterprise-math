name: precision-pi-complete

on:
  push:
    branches:
      - formalization/precision-pi-paper-ii-k4
    paths:
      - 'EnterpriseMath/PrecisionPi/**'
      - 'EnterpriseMath.lean'
      - 'lakefile.toml'
      - 'lake-manifest.json'
      - 'lean-toolchain'
      - '.github/workflows/precision-pi-complete.yml'
  workflow_dispatch:

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
      - name: Set up Lean
        uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: false
          test: false
          lint: false
          use-github-cache: false
          use-mathlib-cache: false
      - name: Resolve pinned dependencies
        run: lake update
      - name: Fetch mathlib cache
        run: lake exe cache get
      - name: Build repository target with warnings fatal
        run: lake build --wfail -KCI EnterpriseMath
      - name: Compile all current paper-II theorem modules
        run: lake env lean EnterpriseMath/PrecisionPi/PaperIIComplete.lean
