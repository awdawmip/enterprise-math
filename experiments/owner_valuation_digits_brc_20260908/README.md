# Partial digit DIV migration and archived evidence

This source checkpoint applies the independently checked `digits_lsd` substitution in `research_notes/owner_arithmetic_20260907.py`. The module must equal candidate SHA256 `209c5f38dee8dcd0f61b895ef3c2b1e3d4e2b46724bb3bf7326dd210f813b2ce`.

Only the internal quotient/remainder loop now enters the existing natural BRC DIV facade. The helper accepts an optional local trace collector. The public `SignedValuationSpectrum` and `signed_valuation_spectrum` APIs, all carry transitions, signed outputs and resource limits are unchanged. The public caller does not return or persist digit traces. This is **partial runtime evidence**, not certification of the whole historical valuation tool, a new theorem, formal acceptance, or main admission. Primality, histogram/Fraction arithmetic and broad package imports remain separate dependencies.

The original nineteen-method source shard at published commit `909ed4c3c81adca8d5d653994a83df316faf1365` has SHA256 `9b9b1851602200bbb207681dbac444a7e745b2915d6f8851bc999fd3a028f115`. Its exact bytes are retained under `frozen/`. This checkpoint changes only the target method's module pin and appends scoped evidence; the other eighteen method objects remain byte-identical. Old check/result references retain their historical module/source binding and were not rerun.

## Evidence generations

`review.py`, `inputs.json`, `owner_arithmetic_20260907.candidate.py`, `review.json`, `REVIEW.md`, the patch, original tool receipt and eight `frozen/` source files are copied from the original TEMP review without changing bytes. `review.json` is the actual historical run: ten known digit tuples, eight standalone rejections, eleven pre-histogram caller boundaries and 288 native facade evaluations. It must not be read as an execution from the current package location.

The archived script uses the former owner path for its preservation check. That check cannot be used directly after the owner module is changed. For an independent fresh replay, use:

```text
python -B -X utf8 experiments/owner_valuation_digits_brc_20260908/replay.py
```

The entry creates a separate TEMP directory, verifies every archived pin, copies the exact script/candidate/runtime, and changes only `source_worktree` in the **execution copy** of `inputs.json` to point at its eight frozen source files. All original input hashes and computation logic remain unchanged. It records the manifest change, actual command, stdout/stderr hashes and new receipt. It never overwrites this package's historical `review.json`. `--output-dir` may name a new directory outside this package.

`prepare.py` is the historical immutable Git capture script, not the production replay entry. The archived `REVIEW.md`'s direct-replay instruction describes its original TEMP location; the command above is the supported relocated-package entry.

Direct byte inspection found no CRLF in any of the eight frozen files. Their Git-filtered blob IDs equal the original IDs, so this package needs no `.gitattributes` override. All newly authored package files use UTF-8 and LF. No repository-wide attribute is changed.

## Metadata-only source check

```text
python -B -X utf8 experiments/owner_valuation_digits_brc_20260908/validate_source.py --receipt <new-output.json>
```

This verifies the current module pin, the other eighteen raw method objects, unchanged public/carry/output source suffix, scoped evidence pins, one canonical catalog occurrence, the existing English trigger and an exact-method-ID query with the existing Chinese trigger, and the helper's local AST arithmetic boundary. The current loader's ASCII tokenizer does not support a bare Han-only query; neither the loader nor the original triggers were changed. The check imports no `enterprise_math` runtime and does not execute the old full-spectrum regression. Its receipt is a new metadata execution, separate from the historical computational receipt. No canonical acceptance follows from a successful lookup.

Driver-ID: EM-DVR-01E1D9 / CONTROL_PLANE
Global-Knowledge-Sync: main@a624e4d / GLOBAL_KNOWLEDGE_V1
