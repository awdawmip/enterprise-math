# Weighted minimum trade: bounded RESULT_ONLY main candidate

This package selects only `result.x6.weighted_minimum_trade` from the published
owner snapshot [909ed4c3c81adca8d5d653994a83df316faf1365](https://github.com/awdawmip/enterprise-math/commit/909ed4c3c81adca8d5d653994a83df316faf1365).
The original method object is unchanged: `RESULT_ONLY`, `api=[]`, `family_id=null`,
`TOOLBOX_INTEGRATION_CANDIDATE`, and formal acceptance absent. The original
nineteen-entry shard is not copied over main's two-entry shard.

The local composition base is `d4bad54522bc0abf63e11d1ec03977efdb6f57b6` (tree `e49df831bafa0492b6132d380587768eaa9f631b`).
The two papers are published owner sources; this six-file package is a local
main-admission candidate. Its metadata execution does not establish a source or
main publication, formal Task/Result/Driver review, claim, Working Truth, or Lean status.
The receipt's source, authoring snapshot and execution generation remain distinct
after any later owner-controlled publication.

## Closed mathematical scope and remaining frontier

In one common labelled raw X6 chart, take a nonzero finite signed rational
analysis array after aggregation and Jordan cancellation, with all twenty
three-axis marginals zero and exactly eight positive spatial support sites.
The reviewed papers force exactly eight negative sites and one common absolute
weight on all sixteen sites. Four represented independent binary coordinate
types carry parity. The physical coordinates may be constant, repeated or
complemented one-bit columns, with arbitrary distinct integer endpoint dictionaries.
The four bits are combinatorial labels on these dictionaries, not an arbitrary
change of the raw chart; can3 alone cannot replace the raw projections.
Signed subtraction is an analysis ledger between positive populations, not a
primitive negative BRC population.

This classification closes the eight-positive/at-least-nine-negative route and
the proposed delete-light-point route. In this equal-amplitude trade, removing
one positive atom gives the paper ratio `15/20 = 3/4`, and that atom's original
positive-mass share is `1/8`. These are paper statements, not new computations.
The unrestricted optimal stability constant for `p<=7` remains OPEN; no claim
that it equals one, no new general bound, and no native arithmetic consumer is added.

## Exact existing-main support bridge

The support-slicing argument uses the already-main
[`OWNER_X6_STABILITY_20260907.md`, section 3](../../research_notes/OWNER_X6_STABILITY_20260907.md)
at SHA256 `2915e70210dce5bc136c915e45e96a7c8a1f6dd414fae1590a4687f41bff3127`.
Its finite quantitative lemma states that `p(f)<2^k` implies
`||f||_1 <= C(n,k) D_(n,k)(f)` with finite `C(n,k)`.
Thus a nonzero zero-defect array has at least `2^k` positive sites. Apply the
same result to `-f` for the negative sites. For the full array, a one-axis slice,
and a two-axis slice use `(n,k)=(6,3),(5,2),(4,1)` respectively; finite sums of
the zero raw tables give the needed lower-order zero marginals. At order zero,
total zero and nonzero support give at least one site of each sign.
These are spatial-support bounds, not integer trade-volume bounds.

The historical citation `OWNER_FREE_CANDIDATE_20260907.md`, section 3, stays at
its immutable owner URL in the shard's `historical_source_pins`. It is not
silently represented as a main file. The older stability-frontier comparison
and four checker/JSON artifacts also remain immutable historical references.
They are not needed as new local dependencies of this support bridge.

## Evidence and execution boundary

Root and the independent paper reviewer reported complete bounded paper review
PASS in the admission assessment SHA256
`1b5d65adeca6a5fdf5283ca8d9eb494e4fd1c3112c02b1f05399453c7bf8884c`.
The two complete original papers below remain byte-identical:

| Source | SHA256 |
| --- | --- |
| `OWNER_WEIGHTED_TRADE_FRONTIER_20260907.md` | `520a41ee878aeb1d1c546a7c088ae1c86f0acbe8292d7356d00436322b6f20be` |
| `OWNER_WEIGHTED_TRADE_INDEPENDENT_AUDIT_20260907.md` | `bafe071af7d04a7734641410cd5820342ab307ddcef1a0e5cfd89f38bb0d2aa9` |

Historical observations, including 58/3360 checks and edge/rank checks, are
retained as historical evidence only. This package neither reruns those
programs nor certifies their native arithmetic behavior. The original paper
wording and original method object remain unchanged.

The validator reads only metadata and source bytes, uses the canonical loader
and its metadata/text coverage lookup, checks the exact original result object,
and preserves every currently present prior catalog object and source byte.
The observed base had 105 method objects; that is an execution observation,
not a permanent catalog size requirement. The validator also rejects duplicate
IDs, duplicate paper registration under another ID, changed API/status/scope,
and changed required source/dependency bytes. It does not prove mathematics.
Historical remote pin declarations are bound by the shard envelope; their
remote bytes were checked at source capture, not fetched by the validator.

Run from the repository root:

```powershell
python -B -X utf8 experiments/20260908_owner_weighted_trade_result/validator.py
```

The default is read-only. `--write` writes only this package's `validation.json`
with the actual executed validator/shard hashes and observed catalog. A saved
receipt is historical evidence for its own input bytes; it is not overwritten
by the read-only command and does not assert that a later tree was tested.

The first metadata write/read-only runs, eight rejection probes and two positive
fixture probes passed. The separately selected arithmetic-policy check rejected
seven `pathlib` `/` path expressions as forbidden division AST nodes. Those
seven expressions were replaced by equivalent `joinpath` calls; neither the
canonical checker nor mathematical sources were changed. The original failure
logs and first validator/receipt bytes remain in the author evidence package.
The final receipt records an actual run of the revised validator. Only its
affected write/read-only entrypoints and the selected arithmetic gate were
rerun after this path-only change; the earlier ten probe results retain their
original input generation. No historical mathematical execution was added.

Required remaining gates are owner independent admission review, applicable
CI on the eventual published tree, and the single owner-controlled mathematical
L4 admission. Source indexing alone grants no formal mathematical acceptance.

Global-Knowledge-Sync: main@519a3b5 / GLOBAL_KNOWLEDGE_V1
