# 进取数论晶包坐标与地址约束

状态：生效／当前用户明确的坐标接口要求／V1。
生效日期：2026-09-13。
机器权威文件：`coordinate_address_contract.json`。

## 一、最终地址

所有最终展示、对外交换或作为晶包地址持久化的坐标，统一采用六个非负整数字段，并明确编码版本及固定参照系。合法域是已登记编码的像集，不是任意六个自然数组合。六个字段不自动等于六个独立度量分量，也不新增空间维度。

不再默认拆成十二个正负地址栏。编号可以减少，操作可以反向，内部差值可以有符号，这些均不等于最终地址出现负数。未启用字段只能在不丢失完整身份的前提下置0；未知、隐藏、投影丢失的信息不能冒充0。

## 二、原点、轴线和不同类型的零

显示原点与显示数轴是参照对象，不是晶包、路径节点、操作数或物理源点。显示原点可以处于晶包间的空隙，显示轴线不要求通过晶包中心。这里不据此新增物理维度、距离源点或从原点进入晶包的通道。

旧原始坐标中编号为零的晶包保留；零位移、空路径和标量零也保留。原来的有符号整数坐标和代数作为内部数学表示，不直接输出成最终晶包地址。内部锚点不必等于显示原点。层号是观察标签，不是晶包身份，也不自动意味着到某点的单位距离。

## 三、跨区

跨越显示边界不强制加1。允许经过证明的等值换栏，也允许边界表要求的数值调整；不能把加1或纯字段置换当作所有边界的统一规则。

同一晶包换码要求 `D_new(T(a))=D_old(a)`；真实移动 F 要求 `D_new(K(a))=F(D_old(a))`。实际边身份、先后顺序、端口、BRC 重复度及权重全部保留。换码不新增真实边；数值相等也不删除真实的一步。有限域、边界、初态、缓存和更新顺序需要明确迁移。

## 四、已实现范围

登记编码 `three_region_slice_v1` 只覆盖固定两原生方向切片，地址形式是 `(0,b,c,0,0,0)`、`(a,0,c,0,0,0)`、`(a,b,0,0,0,0)`，启用字段为正整数。前三栏唯一的零确定区域，全零六元组不是该编码下的合法晶包。解码规则和已编译通过的 Lean 证明保持不变，证明文件为 `EnterpriseMath/CellAddress/ThreeRegionSlice.lean`。

这不是任意完整六轴状态的通用编码。全项目的非负地址要求生效，但其他编码必须分别给出完整合法域、可逆性和转移证明后登记。不支持的编码、版本、参照系或切片外输入明确拒绝，不能默默丢掉共同深度或其他坐标。

公开 Python 类型 `FinalCellAddress` 不实现整数序列接口，防止直接把地址字段误当原始坐标相减。JSON 规范位于 `schemas/cell_address.schema.json`，执行校验命令为 `python tools/validate_cell_address.py`。运行时拒绝布尔值、浮点数字、重复 JSON 键及未知版本；JSON Schema 对数学整数的语义比运行时允许的整数文本略宽，完整校验必须经过运行时。大整数必须精确解析，不允许先转浮点数。

## 五、优先关系与迁移

本约束取代旧文件中关于最终地址及显示原点的冲突表述，不改变 P000 空间前提、既有反向操作、内部原始坐标、解码后的度量和历史定理范围。旧坐标零点键只作为内部 RAW_CHART_ZERO 的兼容别名，不能据此强迫显示原点落在晶包中心。

当前总说明及路由中的过时坐标摘要统一修正。历史记录、归档、旧条件实验及已成立证明不追溯改写。此次生效的是坐标接口要求；不宣称完整六轴编码、全调用点迁移、全部物理模型或新的基础定理已经验收。
