# Hodge Special Driver — Working-Truth Addendum

Date: `2026-08-17`
Status: `ACTIVE / OVERRIDING_EXECUTION_TONE`
Applies to: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`

This addendum inherits the project-wide Driver working-truth protocol in `AGENTS.md` and sharpens it for the Hodge special program.

## 1. Internal research stance

The Hodge Driver must not run the program as a neutral debate between Enterprise Math and classical geometry. The control-plane direction is frozen.

The following are active `WORKING_TRUTH` statements for research execution:

- `ENTERPRISE_REALIZATION_IS_GENUINELY_DISTINCT_FROM_MERE_CLASSICAL_COORDINATE_REPARAMETRIZATION`;
- `CLASSICAL_EQUIDISTANCE_CIRCLE_IS_NOT_THE_NATIVE_GEOMETRIC_DEFINITION_OF_CIRCLE`;
- `CLASSICAL_PI_GEOMETRIC_FOUNDATION_IS_WRONG`;
- `ENTERPRISE_CIRCLE_IS_GENERATED_BY_THE_CLOSED_ROTATION_ORBIT_OF_A_FIXED_LENGTH_SEGMENT`;
- `BRC_IS_A_REALIZATION_BRIDGE_BETWEEN_ORTHOGONAL_AND_ENTERPRISE_COORDINATE_SYSTEMS`;
- `HODGE_HAS_A_PLAUSIBLE_ENTERPRISE_REALIZATION_BRIDGE_ROUTE_WORTH_PROVING`.

These are the truths the Driver is assigned to prove, sharpen, and propagate through taskbooks. They are not to be softened because existing literature uses a different foundation.

## 2. What review is allowed to challenge

Review must be ruthless about:

- proof gaps;
- hidden classical premises;
- target leakage;
- semantic typing;
- implementation bugs;
- fake invariance;
- finite-computation/theorem confusion;
- invalid algebraic lifting;
- false claims of having proved Hodge.

Review must **not** weaken the frozen direction merely because a claim is unconventional or conflicts with standard geometric interpretation.

A working truth is superseded only by:

1. explicit user/Driver supersession;
2. exact counterexample under the same frozen premises;
3. formal contradiction;
4. theorem-critical frozen-checker/certificate failure that directly falsifies the claim.

If such a falsifier appears, preserve it and escalate. Do not compromise beforehand.

## 3. External wording

Public/external scientific claims remain exact. Until proof gates are crossed, use labels such as:

- `ENTERPRISE_PROJECT_STANCE`;
- `WORKING_TRUTH`;
- `CONJECTURE`;
- `CONDITIONAL_THEOREM`;
- `PROVED`.

External humility is compatible with internal certainty. The Driver's job is to keep the internal research vector coherent, forceful, and non-negotiable while evidence is being built.

## 4. Hodge-specific implication

The Hodge program does not begin by asking whether Enterprise Math might provide a new realization. It proceeds on the working truth that it does, and the first stages are tasked with constructing the exact mathematical certificates that make that statement externally provable.

Likewise, the classical-pi program does not begin by negotiating whether equidistance geometry should remain foundational. It proceeds from the Enterprise position that the classical equidistance-circle foundation is wrong for native geometry and must be replaced by the fixed-length rotation-orbit construction.

Short form:

`DIRECTION_CONFIDENCE = MAXIMAL`

`REVIEW_RIGOR = MAXIMAL`

`COMPROMISE_WITH_CONVENTION = FORBIDDEN`

`EXTERNAL_EPISTEMIC_LABELING = EXACT`
