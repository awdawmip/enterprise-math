# Blind packet — native typed-Cell prime localizer, global incidence islands, and presentation equivariance

Status: `FROZEN_BLIND_REPLICATION_INPUT`

Date: `2026-08-23`

Packet-ID: `PRIME_NATIVE_LOCALIZER_ISLAND_EQUIVARIANCE_BLIND_PACKET_20260823`

## 1. Evidence firewall

Before freezing the independent return, use only this packet, elementary number theory, exact integer computation written by the receiving researcher, and ordinary classical references consulted only after the internal proof checkpoint named below.

Do not read or search for source notes, source scripts, free-research conversations, journal entries, branch diffs, witness lists, residue tables, or later comparisons concerning:

- native triple-prime self-localization;
- typed-Cell prime-incidence islands;
- any claimed sharp component-size bound;
- seam prime-incidence obstructions;
- presentation-stability/equivariance of those results.

This packet deliberately withholds every claimed extremal number, source proof, source witness, source modulus table, and source final classification.

## 2. Frozen typed-Cell carrier and integer allocation

There are three typed affine Cell charts indexed by

`sigma in {0,1,2}`.

On typed shell `s>=0`, use side coordinate

`0<=t<=s`.

Define

`B_s = 1 + 3*s*(s+1)/2`,

`N(s,t,sigma) = B_s + t + sigma*(s+1)`.

This assigns every positive integer exactly once to a typed Cell state. Primality is evaluated only after the allocation is fixed.

For compatibility with the earlier enumeration-layer notation, one may write `r=s+1`, but the Cell shell is `s`.

## 3. Sector-local elementary incidence

Inside a fixed typed sector `sigma`, the two elementary triangle orientations are represented in `(s,t)` coordinates by

- orientation A:
  `(s,t)`, `(s+1,t)`, `(s+2,t+1)`;
- orientation B:
  `(s,t)`, `(s+1,t+1)`, `(s+2,t+1)`.

Use the ordered integer triple `(x,y,z)` obtained by applying `N` to those three Cells in the displayed order.

The receiving researcher must derive, rather than assume:

- all first- and second-difference identities;
- whether orientation A/B can be decoded from `(x,y,z)` alone;
- whether `(s,t,sigma)` can be recovered uniquely;
- the exact failure modes if order, orientation, typing, or boundary assumptions are weakened.

A `fully-prime local incidence triple` is one for which all three integer labels are prime.

## 4. Global implementation carrier for incidence only

For global seam bookkeeping, use the following implementation-lattice embeddings of the three typed charts:

- `C_12(a,b) -> (a,b)`;
- `C_23(b,c) -> (-1-c, b-1-c)`;
- `C_31(a,c) -> (a-c, -1-c)`.

The corresponding typed coordinates are:

- sector 0: `s=a+b`, `t=b`;
- sector 1: `s=b+c`, `t=c`;
- sector 2: `s=a+c`, `t=a`.

The two elementary implementation-carrier triangles are

`A(x,y)={(x,y),(x+1,y),(x+1,y+1)}`,

`B(x,y)={(x,y),(x,y+1),(x+1,y+1)}`.

Use this carrier only to decide which three typed Cells are incident. Do not interpret its Euclidean distance, angles, perpendicularity, or vector relations as native Enterprise metric data.

## 5. Prime-incidence hypergraph and islands

Construct the global hypergraph whose vertices are typed Cell states carrying prime labels and whose hyperedges are elementary carrier triangles whose three labels are prime.

Two prime Cells lie in the same `prime-incidence island` when they are connected in the 2-section of this hypergraph, equivalently by a chain of fully-prime elementary incidence triangles.

The receiving researcher must determine:

1. whether any fully-prime hyperedge can cross a typed-sector seam, including all small-prime anchor cases;
2. the exact local eligibility classes inside each sector;
3. whether every connected component is finite;
4. if finite, the exact sharp maximum component size and a certified attaining witness;
5. if the source-type sharpness picture fails, the smallest counterexample and corrected classification.

A finite search is not a global proof. Any finite maximum claim must be supported by a complete residue/transition obstruction plus an exact witness.

## 6. Six allowed presentations

The typed allocation admits six global presentations generated by:

1. cyclically changing which physical typed sector receives numeric block `0`;
2. globally reversing the side coordinate in every sector, `t -> s-t`.

These are the `C3 x C2` presentation choices of the tri-sector carrier. Physical typed Cell identities and elementary-incidence structure are transported with the presentation.

The receiving researcher must distinguish:

- invariant numerical statements;
- graph-isomorphism/equivariant statements;
- quantities whose sign or chirality reverses;
- claims that fail because they privilege a named block, orientation, or side coordinate.

For any global island bound or witness classification, prove or refute exact transport across all six presentations. A six-run census is regression evidence, not the proof of equivariance.

## 7. Exact research questions

Freeze an internal proof checkpoint before opening classical or withheld source context, then settle:

### L — minimal localizer

- Is the ordered integer triple of one elementary incidence event enough to recover its orientation and complete typed coordinates?
- What is the exact inverse map?
- Does the specialization to three prime values add information, or merely restrict the same exact inverse to fully-prime events?
- What ordering/typing data are indispensable?

### I — global island classification

- Are cross-sector fully-prime bridges possible?
- Are component sizes globally bounded?
- If yes, what is the sharp bound, why is it global, and what exact prime-labelled component attains it?
- Which small primes and boundaries require separate treatment?

### E — presentation equivariance

- Which L/I statements survive cyclic sector relabeling and global side reversal?
- Give the exact transporting isomorphisms or the smallest failure.
- Separate invariant connectivity from presentation-equivariant orientation/chirality.

Return exactly one leading aggregate classification:

- `LOCALIZER_AND_SHARP_GLOBAL_ISLAND_FULLY_RECONSTRUCTED_WITH_EQUIVARIANCE`;
- `LOCALIZER_RECONSTRUCTED_ISLAND_SCOPE_NARROWED`;
- `ISLAND_BOUND_RECONSTRUCTED_LOCALIZER_SCOPE_NARROWED`;
- `PRESENTATION_EQUIVARIANCE_REQUIRES_NARROWING`;
- `MATERIAL_COUNTEREXAMPLE_TO_ONE_OR_MORE_SOURCE_TYPE_CLAIMS`;
- `OPEN_AFTER_CERTIFIED_PARTIAL_PROGRESS`.

## 8. Mandatory counterexample pressure

At minimum test and explain:

- both local triangle orientations;
- all three physical sector types;
- all six global presentations;
- ordered versus unordered triples;
- boundary and origin-anchor triangles;
- every possible use of primes `2,3,5` in an exceptional bridge or long component;
- components that are mod-eligible but not actually prime;
- a deliberately perturbed label/block/reversal rule that breaks the claimed inverse or equivariance;
- the difference between carrier-incidence isomorphism and native metric invariance.

Do not silently identify presentation-equivariant statements with presentation-invariant statements.

## 9. Executable evidence requirements

The independent checker must:

- generate labels and sector-local triangles directly from this packet;
- independently generate the global carrier atlas and seam triangles;
- verify the proposed local inverse on exhaustive finite domains and symbolic identities;
- enumerate every finite residue state used in a global obstruction;
- certify every prime in an extremal witness independently of the bulk sieve;
- replay all six presentations;
- include negative controls for order loss, one perturbed incidence formula, and one inconsistent sector reversal;
- expose bounds, seeds, algorithms, and frozen digests.

Use two independent implementations for at least one critical object, such as chart formulas versus carrier-atlas enumeration.

## 10. Source-comparison boundary

After the independent report, checker, tables, and witness certificates are committed and frozen, stop. The Driver may then compare with the withheld free-research branch. The receiving researcher must not perform that comparison.
