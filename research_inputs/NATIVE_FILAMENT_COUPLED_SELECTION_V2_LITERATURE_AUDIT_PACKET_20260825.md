# Native filament coupled-selection V2 — independent literature-audit packet

Status: `BLIND_LITERATURE_AUDIT_INPUT / STATEMENT_ONLY / NO_SOURCE_PROOFS`

Date: `2026-08-25`

Originating Researcher-ID: `EM-FREE-NEPS-239A6D`

Purpose: assess external novelty of a mathematically verified-with-narrowing theorem package. This packet intentionally contains statement content only. It does not expose PR #627 source proofs or package-specific checkers.

Prior mathematical audit: PR #631 froze verdict

`PACKAGE_VERIFIED_WITH_NARROWING`.

The literature auditor is not asked to re-prove the package except where needed to compare statement strength. The task is theorem-statement overlap / novelty classification.

## 1. Non-novel ingredients already conceded

Do **not** award novelty for any of the following in isolation:

- characteristic polynomials / characteristic quasi-polynomials of integral arrangements;
- arithmetic/G-Tutte frameworks and finite-field complement counting;
- coefficient monotonicity under divisibility of moduli;
- Reed--Solomon / MDS codes;
- CRT;
- elementary quadratic-character sums;
- order-2 cyclotomic numbers / quadratic-residue intersection counts;
- Legendre transforms / conic or parabola duality;
- Mertens/Dirichlet product asymptotics;
- generic profinite products, Haar measure, Cantor-set and Hausdorff-dimension arguments;
- deterministic Miller--Rabin primality testing.

The only candidate novelty is the exact **coupled structured family** below.

## 2. S1 — sector-count provenance

For an abstract cyclic shell allocator with odd sector count `s`, shell `r>=1` has `s` half-open blocks of length `r`, filled consecutively. The unique central block is

`sigma_*=(s-1)/2`.

On the admissible zigzag side coordinate

`t=h+ceil(r/2)`,

the central label simplifies exactly to

`N_s(r)=h+1+(s r^2+eps(r))/2`.

Therefore the odd-curvature coefficient is

`B=s`.

For even sector count, central symmetry lies on a seam; the reflected central-seam labels differ by

`2h+1`,

so two nonexceptional odd primes cannot occupy a reflected pair simultaneously.

Question for literature audit: is this exact odd/even sector-to-quadratic-filament selection mechanism already a named or directly subsumed construction?

## 3. S2 — odd-curvature dynamics / finite quotient

For positive odd `B`, define

`F_B(H,r)=H+(B r^2+eps(r))/2`.

For start shell `R`, put `chi=(-1)^R`, `c=F_B(H,R)` and `V_j=F_B(H,R+j)`. Then

`V_j=c+B R j+(B j^2+chi eps(j))/2`.

Exact dynamics:

`V_j-2V_(j+1)+V_(j+2)=B-chi(-1)^j`,

and

`V_(j+4)-2V_(j+3)+2V_(j+1)-V_j=0`.

For `k>=3`, let `C_(k,B)(M)` be the set of length-k residue words modulo `M` generated by all integer `(H,R)`.

If `M>2`, after fixing the intercept the exact/minimal effective `R` period is

`L_(B,M)=lcm(2,M/gcd(B,M))`,

and

`|C_(k,B)(M)|=M L_(B,M)`.

If `M=2`, after fixing the intercept the exact/minimal effective period is `1`, while the total code has exactly two words.

For prime `q>max(2,k-1)` with `q∤B`, each fixed chirality, after subtraction of the fixed curvature offset, is exactly the affine evaluation family

`(a+bj)_(j=0,...,k-1)`,

hence an affine `[k,2,k-1]` RS/MDS coset.

Question: are the period/cardinality formula and the parity/slope lock already direct instances of a known modular-code or periodic-polynomial sequence theorem beyond generic integral-matrix code periodicity?

## 4. S3 — chirality-dependent dual-parabola tangent arrangement

For `e in {0,1}` and chirality `chi`, define

`Q_e^(chi)(x)=x^2/(2B)-chi e/2`.

The divisibility zero line at index `j`,

`L_j: y=-j x-(B j^2+chi eps(j))/2`,

is tangent at `x=-Bj` to

`Q_(eps(j))^(chi)`.

Let `u,v` be same-parity indices with parity `e`, and let `w` have opposite parity. In the distinct-slope regime `q∤(u-v)`, the three lines are concurrent modulo `q^a` iff

`q^a | B(w-u)(w-v)+chi(1-2e)`.

For a length-k window, `q>k-1` is sufficient for distinct slopes.

In the inherited regime `q` odd, `q∤B`, `q>k-1`, the two-chirality mixed arrangement changes type exactly at prime divisors of the explicit finite product

`mathfrak D_(k,B)=product_T (B^2 A_T^2-1)`,

where `A_T=(w-u)(w-v)`.

For fixed chirality, the q-adic persistence depth is the maximum valuation of the corresponding fixed-chirality obstruction

`B A_T+chi(1-2e_T)`.

Question: is this exact two-parabola sampled-tangent/discriminant family already directly treated in finite-geometry, conic-arrangement, arithmetic-arrangement, or coding literature?

## 5. S4 — transparency and universal-breaker phase

For odd prime `q`, let `tau_B(q)` be the number of transverse classes `H mod q` such that no value `F_B(H,r)` is divisible by `q` for any shell `r`.

Exact local factors:

- if `q|B`, `tau_B(q)=q-2`;
- if `q∤B`,
  `tau_B(q)=[q-3+(B/q)+(-B/q)]/4`.

At `q=2`:

- `B=1 mod4` is a universal breaker;
- `B=3 mod4` leaves one transparent class.

Consequences:

- `q=3` is a breaker iff `3∤B`;
- `q=5` is a breaker iff `(B/5)=-1`;
- no prime `q>=7` is a universal breaker.

Hence the first-breaker phase for positive odd `B` is determined by `B mod60`:

- first breaker 2: `{1,5,9,13,17,21,25,29,33,37,41,45,49,53,57}`;
- first breaker 3: `{7,11,19,23,31,35,43,47,55,59}`;
- first breaker 5: `{3,27}`;
- no finite universal breaker: `{15,39,51}`.

Question: does the exact finite-breaker classification for this alternating quadratic family have a direct antecedent in covering systems, polynomial orbit sieves, deterministic arithmetic percolation, or quadratic sequence divisibility literature?

## 6. S5 — breaker-coprime capacities

For the first-breaker phases, the exact maximal consecutive runs avoiding divisibility by the first breaker are

- breaker `2` -> `1`;
- breaker `3` -> `5`;
- breaker `5` -> `9`.

For breaker `5`, the two normalized extremal transverse classes are exactly

`H=0,2 mod5`.

These are breaker-coprime/divisibility capacities, **not** unrestricted prime-run caps for arbitrary `(H,R)`.

Question: is the general `2q-1` tangency-run mechanism for the relevant universal-breaker cases already standard in modular quadratic sequences?

## 7. S6 — Legendre-dual unification

For odd `q∤B`, define hit sets

`I_0={-B x^2/2 : x in F_q}`,

`I_1={-(B x^2+1)/2 : x in F_q}`.

These are the negative Legendre-dual value sets of the two quadratic branches.

They satisfy

`|I_0|=|I_1|=(q+1)/2`,

`|I_0∩I_1|=[q+1+(B/q)+(-B/q)]/4`,

and

`tau_B(q)=|I_0∩I_1|-1`.

Universal breaking is exactly minimal overlap `|I_0∩I_1|=1`.

The intersection formula itself is conceded classical order-2 cyclotomy. The candidate coupling is that **the same quadratic pair** controls:

- sampled tangent concurrence -> local exceptional characteristics;
- dual-value covering -> global breaker characteristics.

Question: is this exact tangent-exception / dual-covering coupling already a recognized theorem for sampled conics or quadratic code families?

## 8. S7 — native tri-sector selection corollary

The actual native model supplies the scalar `3` before prime testing in three independent ways:

- sector count `3`;
- mean filament curvature `(2+4)/2=3`;
- normalized seven-Cell Poisson source `18/6=3`.

With `B=3`:

- channel 2 is nonbreaking;
- channel 3 is nonbreaking;
- `(3/5)=-1`, hence channel 5 is the first universal breaker;
- the breaker-coprime run capacity is `9`.

Among positive odd sector counts with a finite universal breaker, `s=3` is the smallest sector count attaining the latest possible finite first breaker `5`.

Separately, on the native typed-Cell incidence complex, an independently established theorem gives

`MAX GLOBAL TYPED-CELL PRIME-INCIDENCE ISLAND SIZE = 9`.

Do not conflate the breaker-coprime capacity theorem with the separate actual-prime incidence theorem.

Question: does literature contain an equivalent geometry-selected parameter rule in which local discrete curvature / sector count determines the first universal arithmetic breaker and a finite incidence capacity?

## 9. S8 — high-dimensional transparent-basin phase

For squarefree `M`, exact CRT factorization gives

`Theta_B(M)=product_(q|M) tau_B(q)`.

For primorials `P_d`:

- `B=3` extinguishes at dimension `d=3` because `tau_3(5)=0`;
- no-break classes `B=15,39,51 mod60` remain nonempty for every finite dimension.

For fixed no-break `B`, the survivor density has asymptotic form

`Theta_B(P_d)/P_d ~ C_B 4^(-d)/(log p_d)^3`, `C_B>0`.

In the squarefree profinite carrier with the primorial ultrametric, the compatible survivor product is compact, perfect, uncountable, Haar-null and Hausdorff dimension `1`; every finite subsystem has an ordinary integer CRT lift, while the full diagonal ordinary-integer intersection is empty.

All general CRT/product/profinite ingredients are conceded classical. Question: is the extinction-vs-sparse-expanding phase attached to this exact alternating quadratic family already directly classified in sieve/profinite literature?

## 10. S9 — control witness

The no-break comparator `B=15` has a deterministically verified run of twelve consecutive prime values below `2^64`.

This rules out the interpretation that the native value `9` is a universal prime-run cap of all quadratic-plus-parity filaments.

This witness is evidence/control, not a novelty claim.

## 11. Required novelty classifications

For each S1--S8, assign exactly one primary verdict:

- `KNOWN_DIRECT_THEOREM` — an existing theorem directly subsumes essentially the full statement;
- `KNOWN_IMMEDIATE_COROLLARY` — follows with only routine substitution from a known theorem;
- `KNOWN_COMPONENTS_ONLY` — ingredients are known but no direct theorem matching the coupling was found;
- `PARTIAL_OVERLAP_REQUIRES_NARROWING` — literature covers a substantive fraction and the research statement must be narrowed;
- `NO_DIRECT_MATCH_FOUND` — after a serious search no theorem-level match was found; this is **not** proof of novelty.

For the full coupled chain S1->S8, also assign one package verdict from the same set.

Every `KNOWN_DIRECT_THEOREM`, `KNOWN_IMMEDIATE_COROLLARY`, or `PARTIAL_OVERLAP_REQUIRES_NARROWING` verdict must cite the exact source and explain the statement mapping.

`NO_DIRECT_MATCH_FOUND` must list the search areas, terms, and closest sources checked.

No claim of publication-level novelty is authorized by this packet alone.
