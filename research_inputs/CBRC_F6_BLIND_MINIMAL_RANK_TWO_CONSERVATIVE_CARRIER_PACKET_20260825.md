# CBRC F6 Blind Input — Minimal Rank-Two Conservative Carrier Frontier

Status: `DRIVER_FROZEN_BLIND_INPUT`
Date: `2026-08-25`
Driver-ID: `EM-DVR-CBRC-F0-7C3A21`

This packet opens the first rank-two stage only after the working-extension rank-one no-go has been closed. It is a carrier/unary-transport classification task. It is not a ring, norm, phase, square-law, splitter, or wave task.

## 1. Epistemic boundary

The following are **Coherent-BRC working-extension** facts, not native Foundation truths:

1. `A0 = ELEMENTARY_OLD_REFINING_BRANCH_PROJECTION_NONDEGENERACY`: an authorized elementary two-branch refinement of one embedded old occurrence may declare both outputs active old-refining branches only if each output has nonzero old signed projection.
2. `FREE_PROJECTION_ZERO_SEPARATION`: for the canonical old signed retraction `pi:C->Z e`, `pi(z) != 0 => q(z)>0`.
3. Together with accepted balanced reversible scalar-conservation semantics, these imply at issued scope `torsion_free_rank(C) >= 2`.

Accepted Driver reviews:

- F5AR branch ontology review at commit `0c983a5c98456a4d9c4b6be29b9a988631984842`;
- F5B positive-separation review at commit `010a1b09f0f79e2484ea56b94bdc7d414b8e0f11`.

Do not promote either working axiom to `CANONICAL_FOUNDATION`.

## 2. Accepted upstream additive/non-sign layer to preserve

The accepted F1/F2 carrier is

`C1 = Z e ⊕ <tau | 3 tau = 0>`.

Unary maps:

`R(n,a)=(n,a+n)`,
`J(n,a)=(-n,-a)`,
`S(n,a)=(n,-a)`.

Relations:

`R^3=id`, `J^2=id`, `S^2=id`, `J R = R J`, `S R S^-1 = R^-1`.

Old signed retraction:

`pi1(n,a)=n e`.

Relative recoalescence witness:

`e + J e = 0`, while `e + J R e = -tau != 0`.

These facts are frozen here so F6 need not read the historical F1 review, whose torsion-free counterfactual is intentionally withheld from discovery.

They do not imply a complex carrier, phase algebra, ring, norm, inner product or square law.

## 3. Rank-two continuation object

F6 may consider a finitely generated additive carrier `C` of torsion-free rank exactly `2` with:

- an embedded copy of the accepted F1 carrier `j:C1 -> C`;
- the old signed generator still primitive;
- a canonical additive retraction `pi:C->Z e` extending `pi1` on `C1`;
- no new relation on the embedded `e` or `tau`;
- sufficient structure to extend/realize the accepted unary `R,J,S` relations on the embedded C1 layer.

F6 must classify, not assume, whether a retraction `r:C->C1` is forced/needed for the least conservative model. If a weaker conservative embedding notion admits strictly smaller or inequivalent carriers, report it rather than silently imposing a split extension.

## 4. What the rank-two lower bound does NOT provide

The working-extension theorem gives only a lower bound on torsion-free rank. It does not select:

- a second basis vector;
- a finite-order free-plane rotation;
- multiplication, unit, ring, field, or extra module structure;
- a quadratic integer system;
- a phase group;
- a norm, inner product, positive form, p-norm or square law;
- a two-slot mixing matrix;
- a readout law;
- a continuum/wave interpretation.

All such structures remain unavailable unless derived in later authorized stages.

## 5. Minimality order for F6

At fixed torsion-free rank `2`, use the target-independent lexicographic order:

1. preserve all accepted upstream additive relations and typed unary semantics;
2. minimize additional torsion beyond the already accepted order-three `tau`;
3. minimize new additive generators/relations beyond the one rank increase already forced;
4. minimize additional unary structure/data needed to extend `R,J,S`;
5. prefer canonical/equivariant descriptions over basis-dependent presentations when equivalent.

Do not use resemblance to known number systems as a tie-breaker.

## 6. Firewall

Before raw freeze do not read/use:

- the full historical F1 torsion-free counterfactual section;
- R063/R064/R065/FQ mathematics;
- downstream coherent-BRC/wave free research;
- external quantum mechanics, Hilbert spaces, Born rules, path integrals, quantum walks, gauge theory or wave equations;
- complex numbers as target carrier;
- Gaussian/Eisenstein/quadratic integers as target;
- roots of unity / finite phase groups as selector;
- rings, fields or arbitrary coefficient multiplication;
- norms, inner products, quadratic forms, square laws;
- Hadamard/Fourier/splitter matrices;
- any known downstream rank-two answer.

F6 must stop before classifying two-slot mixing or scalar laws on rank two.
