# CBRC F5A Input — Branch Ontology / Old-Support Faithfulness Admission Gate

Status: `DRIVER_FROZEN_INPUT`
Date: `2026-08-25`
Driver-ID: `EM-DVR-CBRC-F0-7C3A21`

This packet isolates a candidate branch-ontology axiom after F5R independently proved that the candidate is not derivable from current BRC semantics.

It is not a rank-two, amplitude, phase, norm or wave task.

## 1. Accepted canonical boundary

The canonical component-typed BRC bridge distinguishes:

1. concrete Path-formal witnesses, each retaining word, prefix trajectory, placement and terminal;
2. formal coefficients attached to those witnesses;
3. natural-number augmentation;
4. Boolean support;
5. later enriched coefficient carriers and marked slots used before marker erasure/recoalescence.

A concrete Path-formal witness and a later marked coefficient slot are not currently identified by a canonical total correspondence.

Canonical sources:

- `definitions/ENTERPRISE_BRC_MULTIPATH_ENRICHMENT_BRIDGE_20260821.md@6ec0d73a19e28ec586c59a97d24f5798c9119771`;
- `definitions/ENTERPRISE_NATIVE_LINE_TRACE_FORMULA_20260821.md@b631242db84c5bd3640e6dc554b19a1d04d464f3`.

## 2. Accepted F5R semantic result

Accepted F5R owner head:

`research/cbrc-f5r-independent-forgetful-branch-semantics@f7532b27e2d3e38486b52f8ae24a78734b5a0053`.

Accepted Driver review:

`driver_reviews/CBRC_F5R_INDEPENDENT_FORGETFUL_BRANCH_SEMANTICS_DRIVER_REVIEW_20260825.md@6e40f56745c405042ad2216d1f62b110312ffb83`.

Let

`pi : C -> Z e`

be the conservative additive retraction to the old signed occurrence coordinate. For an elementary two-branch refinement/mixing

`M(e,0)=(x,y)`, define

`FORGETFUL_BRANCH_NONDEGENERACY`

iff

`pi(x) != 0 and pi(y) != 0`.

F5R established:

1. the condition is compatible with current semantics;
2. it is not forced by current semantics;
3. an exact candidate-false model exists on the bookkeeping carrier `Z x Z/2` with a reversible kernel-only second branch;
4. total old-coordinate conservation implies only
   `pi(x) != 0 or pi(y) != 0`;
5. the stronger per-branch condition requires an additional semantic rule;
6. any rank lower bound derived from it is currently only `CONDITIONAL_ON_NEW_AXIOM`.

## 3. Accepted independence witness

Take

`C = Z x Z/2`,

`e=(1,0)`,

`pi(n,t)=n e`,

and

`M_B((n,t),(m,s))=((n,t),(m,s+n mod 2))`.

Then `M_B` is additive and involutive, preserves total old projection and old Boolean support, and maps the elementary input to

`((1,0),(0,1))`.

Both enriched outputs are nonzero, but the second has zero old projection.

This model is accepted only as a semantic independence witness. The torsion tag has no phase, metric, norm, probability or physical interpretation.

## 4. Candidate axiom family to classify

F5A must not assume that the strongest formulation is appropriate. It must classify the following formulations and any strictly intermediate exact formulation it discovers.

### A0 — elementary projection nondegeneracy

For every authorized elementary two-branch refinement of one embedded old occurrence,

`M(e,0)=(x,y)` implies `pi(x)!=0` and `pi(y)!=0`.

### A1 — typed branch-to-old-support faithfulness

Every retained marked branch declared to refine an old concrete Path-formal occurrence must carry a nonempty old witness support and a nonzero old signed coefficient after forgetting enrichment.

This applies to branch records generated by refinement, not to arbitrary coefficient states in `C`.

### A2 — descendant-family faithfulness

For every original concrete Path-formal witness, the total projection of its currently retained descendant branch family is nonzero. Individual descendant slots may have zero projection.

### A3 — global support-reflecting retraction

For every enriched coefficient state,

`z!=0 => pi(z)!=0`.

This removes every nonzero kernel state and is a deliberately strong control, not a preferred axiom.

### A4 — leafwise refinement faithfulness

At every finite refinement depth, every active retained leaf descending from an old occurrence has nonzero old projection.

This is stronger than A0 unless composition makes them equivalent.

## 5. Admission questions

The next stage must determine:

1. which candidate is the weakest exact rule that closes the F4 pure-enrichment branch loophole;
2. whether that rule is consistent with canonical Path-formal branching, signed cancellation and same-terminal recoalescence;
3. whether it is stable under composition, repeated refinement, marker relabeling, translation and forgetful maps;
4. whether it forbids legitimate enrichment states that are not themselves declared branches;
5. whether it is conservative over existing canonical Boolean/N/Path-formal BRC theorems;
6. whether it is definitional, admissible as a new axiom, too strong, or still underdetermined;
7. what exact weaker/stronger alternatives survive each ablation.

## 6. Non-negotiable distinctions

The stage must keep distinct:

- nonzero enriched state;
- nonzero old projection;
- nonempty concrete witness support;
- nonzero signed aggregate;
- Boolean support after final recoalescence;
- branch identity before marker erasure;
- arbitrary carrier states versus states currently typed as retained branches.

In particular, destructive cancellation between two individually faithful branches must remain possible. An admission rule may not silently forbid final aggregate zero merely because each pre-collapse branch is nondegenerate.

## 7. Conditional F4 consequence — not a selector

The accepted F4 boundary says that under `GLOBAL_ZERO_SEPARATION`, every successful torsion-free-rank-one model has a signed-permutation free quotient block.

If an admitted branch ontology forces both elementary output projections nonzero, then a future theorem may conclude only

`torsion_free_rank(C) >= 2`.

This consequence must not be used to choose the axiom. No rank-two carrier may be constructed or selected in the admission stage.

## 8. Firewall

Do not read/use in the admission stage:

- downstream coherent-BRC/wave free research;
- R063/R064/R065/FQ mathematics;
- external quantum mechanics, Hilbert spaces, Born rules, path integrals, quantum walks, gauge theory or wave equations;
- preselected complex/quadratic integer carriers;
- finite phase groups;
- norms, inner products, positive quadratic forms or square laws;
- Hadamard/Fourier/splitter targets;
- any proposed rank-two answer.

The admission decision must be made solely from BRC/path/refinement semantics, logical conservativity and exact countermodels.