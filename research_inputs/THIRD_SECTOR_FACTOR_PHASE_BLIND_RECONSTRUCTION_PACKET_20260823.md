# Blind packet — third-sector multiplicative factor phase versus two-square cells

Status: `FROZEN_BLIND_RECONSTRUCTION_INPUT`

Date: `2026-08-23`

Packet-ID: `THIRD_SECTOR_FACTOR_PHASE_BLIND_RECONSTRUCTION_PACKET_20260823`

## 1. Evidence firewall

This packet contains the complete primitive question but no source formulas for the claimed bridge.

Before freezing the independent return, do not read or search for the GLOBAL_KNOWLEDGE event concerning a third-sector factor phase, its source conversation, any source checker, any source enumeration range, or any stated factor-pair parameterization of two-square representations.

Use elementary number theory, Gaussian-integer methods reconstructed independently, and independently authored exact computation.

## 2. Auxiliary 2+1 overlay

This is an experimental arithmetic overlay. It does not modify the native Enterprise metric, topology, or canonical coordinate ontology.

For `n>=0`, define the additive square-cell set

`S_2(n)={(a,b) in N_0^2 : a^2+b^2=n}`.

The receiving researcher must choose and state whether cells are ordered, unordered, sign-quotiented, or symmetry-quotiented at every theorem step. Silent changes of quotient are invalid.

For `n>1`, define the third-sector multiplicative fiber

`M(n)={(x,y) in N_0^2 : (x+1)(y+1)=n}`.

The ordered fiber is primary. Any unordered factor-pair quotient must be declared separately.

## 3. Elementary fiber questions

Prove, narrow, or refute exact characterizations of:

1. prime fibers;
2. composite fibers;
3. fibers meeting the diagonal `x=y`;
4. fiber cardinality in terms of a standard arithmetic function;
5. the extremal interior point that reads the least prime factor, if such a canonical point exists.

These elementary statements are controls. They are not sufficient to close the task.

## 4. Main reconstruction problem

Determine whether the full set of nonnegative two-square cells of `n` admits an exact parameterization by a factor object canonically visible in `M(n)` or in a multiplicatively defined core extracted from `n`.

A successful reconstruction must answer all of the following without being given the source formulas:

1. the exact existence criterion for `S_2(n)`;
2. the exact number of unordered nonnegative square cells;
3. an explicit bijection, if one exists, between square cells and a quotient of factor choices;
4. the treatment of the prime `2`, primes `1 mod 4`, and primes `3 mod 4` with all exponent cases;
5. fixed points of any involution on the factor choices and their axis/diagonal meaning;
6. an exact formula relating common additive scale `gcd(a,b)` to the corresponding factor choice;
7. a factor-driven recursive generator that reconstructs every square cell and no spurious cell.

Do not count a restatement of the classical two-square existence theorem as a reconstruction of items 3, 6, and 7.

## 5. Reverse bridge from two additive states

For two distinct primitive square cells of the same norm,

`a^2+b^2=c^2+d^2=n`,

investigate whether bilinear combinations

`ac+bd`, `ac-bd`, `ad+bc`, `ad-bc`

and their gcds with `n` recover a nontrivial complementary factor partition of the multiplicative core.

Classify exactly:

- the necessary parity normalization;
- unit and conjugation ambiguity;
- when the recovered factors are coprime;
- when the method returns only trivial factors;
- whether the product of the recovered factors equals a canonical split core;
- all failures caused by nonprimitive cells or repeated representations.

The packet intentionally gives no target gcd formula.

## 6. Mandatory small and degenerate cases

Separate and test:

- `n=1,2`;
- prime powers;
- pure `3 mod 4` square factors;
- powers of `2`;
- all split-prime exponents even;
- exactly one versus multiple primitive representations;
- axis and diagonal cells;
- ordered versus unordered cells;
- two additive cells related only by swap or sign/conjugation.

## 7. Executable evidence

The independent checker must have three logically distinct modules:

1. direct enumeration of square cells;
2. factor-driven generation without consulting direct enumeration;
3. reverse recovery from pairs of primitive square cells.

The modules may be compared only after each produces its own normalized output. Record exact ranges, mismatch lists, and frozen hashes. Include negative controls obtained by removing the `2`-adic normalization and by treating an odd `3 mod 4` exponent as admissible.

## 8. Claim boundary

Classical Gaussian factorization, Fermat's two-square theorem, and the Brahmagupta-Fibonacci identity must be credited as classical infrastructure.

The task may establish an exact experimental-overlay package or a convenient bijective presentation. It may not call the third-sector readout native, foundational, or historically new without a separate evidence-backed audit.

## 9. Source-comparison boundary

After the independent return is frozen, the Driver may compare it with withheld GLOBAL_KNOWLEDGE source event `f0f59174a29b25ff541da5738ed1539c7df7cf78`. The receiving researcher must stop before that comparison.
