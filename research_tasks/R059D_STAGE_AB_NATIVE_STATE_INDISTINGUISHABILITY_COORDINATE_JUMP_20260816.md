# R059D Stage AB — SUPERSEDED

Task-ID: `RS-R059D-STAGE-AB-NATIVE-STATE-INDISTINGUISHABILITY-COORDINATE-JUMP`
Status: `SUPERSEDED_DO_NOT_EXECUTE`
Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`
Date: `2026-08-16`

## Supersession reason

The user supplied a simpler direct main-coordinate construction after Stage AA review:

- choose the `u` positive direction as `0°`;
- one `0°` lattice step from the origin has coordinate increment `(1,0,0)`;
- define the `+60°` lattice direction as **进取垂直** (`ENTERPRISE_ORTHOGONAL`);
- one `+60°` step has coordinate increment `(0,-1,1)`;
- therefore from `(1,0,0)`, one `+60°` step reaches `(1,-1,1)`.

This shows that the prior Stage-AB premise `Gamma_u(n)=(n,-a_n,-a_n)` is not the intended main-coordinate semantics. The AA/X/Y/Z transverse staircase remains a valid historical collapse/readout experiment but is not the main lattice-address generator.

Do not execute the twin-world coordinate-jump lower-bound task as the next main-coordinate stage.

The replacement Stage AB is the direct six-direction / Enterprise-Orthogonal coordinate-generation audit, which freezes and proves the two-generator lattice address law before any further BRC interpretation.
