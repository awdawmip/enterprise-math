# HODGE Stage H0E — Algebraic Source Instantiation of Robust Attributed R2

Date: `2026-08-17`
Status: `ACTIVE / DRIVER-ISSUED TASKBOOK`
Driver: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`
Task ID: `RS-HODGE-H0E-ALGEBRAIC-SOURCE-INSTANTIATION-ATTRIBUTED-R2`
identity_policy: `AUTO_RESOLVE_OR_ALLOCATE`
identity_lane: `HODGE-H0E-ALGEBRAIC-SOURCE-R2`
owner branch: `research/hodge-h0e-algebraic-source-instantiation`
control branch: `research/hodge-special-control-plane`

## 0. Driver acceptance of H0D

H0D is accepted as the first robust transform-attributed operational R2 in the Hodge-special program.

Frozen H0D owner head:

`102f6c73a099a97a412e72c810f8e63d2c370234`

Frozen H0D disposition:

`H0D_ROBUST_TRANSFORM_ATTRIBUTED_R2_FOUND`

Hard target result:

`MULTISTEP_ENTERPRISE_REALIZATION_REACHES_TRANSFORM_ATTRIBUTED_R2 = PASS`

Load-bearing H0D component:

`D1_RECURSIVE_FULL_SUFFIX_QUOTIENT`

On the declared finite layered source, complete remaining-suffix signatures define a coarsest reusable quotient with descended actions and exact preservation of every suffix output. The predeclared reusable-interface count drops:

`18 -> 11`

while passing both `B_raw` and `B_std` attribution audits.

H0D also freezes an important boundary:

- D1 novelty status is `CLASSICAL_PRIOR_ART / PROJECT_EXISTING_REPACKAGING`;
- novelty is not a negative R2 gate;
- the credited leverage is the transform-attributed reduction of the reusable recursive interface.

However H0D's load-bearing transition table is a finite algebraic-style surrogate. Its `P^1` chart test is only a sourcing/naturality stress and does not generate the D1 transition table.

Therefore H0D is **not** yet an R3 seed and does not open H1.

---

## 1. Mission

The single hard target is:

`ACTUAL_ALGEBRAIC_SOURCE_INSTANTIATES_ROBUST_ATTRIBUTED_R2`

H0E must replace H0D's hand-declared layered transition table by a finite exact multi-step source **generated from genuine algebraic data** on a smooth projective complex algebraic toy object.

The preferred primary source is:

`X = P^1_C x P^1_C`

with its standard algebraic affine-chart square and exact chart/overlap transition formulas.

The preferred theorem-critical finite language is built from bounded Laurent-monomial / local-frame section data for a small line bundle `O(a,b)` or an equivalent explicitly algebraic local-section carrier.

The exact local-state and transition tables must be *derived from algebraic formulas*, not selected by hand to reproduce H0D's quotient pattern.

H0E asks whether H0D's D1 mechanism survives this sourcing upgrade:

`actual algebraic local data`

`-> exact multi-step local/overlap continuation system`

`-> complete remaining-behavior / suffix-signature realization`

`-> smaller reusable quotient with descended operations`

with robust transform attribution against fair source baselines.

Do **not** attempt the Hodge conjecture.
Do **not** start H1/cohomology.
Do **not** use known Hodge numbers or cycle representatives.
Do **not** hand-design a finite automaton first and retrofit algebraic labels afterward.

---

## 2. Frozen authority / minimum packet

Read only the smallest sufficient packet:

1. `AGENTS.md`
2. `docs/GITHUB_INTERACTION_BUDGET.md`
3. `research_common_surface.json`
4. `driver_handoffs/HODGE_SPECIAL_DRIVER_HANDOFF_20260817.md`
5. `driver_handoffs/HODGE_SPECIAL_DRIVER_PI_GEOMETRY_ADDENDUM_20260817.md`
6. H0A0 Criterion V2 at `f4e6cf84eb191e0b7442913e018e1f6347e9584e`:
   - `research_results/HODGE_H0A0_REALIZATION_CLASSIFIER_V2.json`
   - `research_results/HODGE_H0A0_PROOF_LEVERAGE_CERTIFICATE_SPEC.json`
7. H0D0 attribution addendum at `96e79629b822a8cb3bc11be1cec8abe319e4cd20`:
   - `research_results/HODGE_H0D0_SOURCE_BASELINE_SPEC.json`
   - `research_results/HODGE_H0D0_LEVERAGE_ATTRIBUTION_CERTIFICATE_SPEC.json`
   - `research_results/HODGE_H0D0_FOUR_AXIS_MODEL.json`
8. H0D frozen artifacts at `102f6c73a099a97a412e72c810f8e63d2c370234`:
   - `research_results/HODGE_H0D_MULTISTEP_SOURCE_REGISTRY.json`
   - `research_results/HODGE_H0D_SOURCE_BASELINE_SANDWICH.json`
   - `research_results/HODGE_H0D_SUFFIX_QUOTIENT_REGISTRY.json`
   - `research_results/HODGE_H0D_PROOF_LEVERAGE_CERTIFICATE_REGISTRY.json`
   - `research_results/HODGE_H0D_ATTRIBUTION_CERTIFICATE_REGISTRY.json`
   - `research_results/HODGE_H0D_HODGE_R3_PREINTERFACE.json`
   - `research_results/HODGE_H0D_SEMANTIC_CHECKPOINT.md`
9. `native_semantics_admissibility.json`
10. `FOUNDATIONAL_LOGIC.md` / `foundational_logic.json` only as needed for semantic typing.

Historical H0D remains read-only.

---

## 3. Criterion semantics remain frozen

H0E uses all four axes:

1. information/recoverability;
2. H0A0 operational R0/R1/R2/R3;
3. H0D0 leverage attribution;
4. novelty/prior-art status.

Required invariants:

`PRIOR_ART_IS_NOT_A_NEGATIVE_R2_GATE`

`NOVELTY_STATUS_DOES_NOT_DETERMINE_OPERATIONAL_RANK`

`SOURCE_INHERITED_LEVERAGE_DOES_NOT_EARN_TRANSFORM_ATTRIBUTION`

`BASELINE_MUST_BE_PREDECLARED`

`POST_HOC_WEAKENING_OF_SOURCE_BASELINE_IS_FORBIDDEN`

H0E hard-target PASS requires:

- operational `R2_NONTRIVIAL_OPERATIONAL_REALIZATION`;
- `ROBUST_TRANSFORM_ATTRIBUTED` against both declared baselines;
- actual algebraic source generation;
- no target leakage;
- sufficient naturality at the claimed algebraic scope.

External novelty may remain zero.

---

## 4. Primary algebraic source — freeze the default

Use:

`X = P^1_C x P^1_C`.

Default affine-chart family:

`U_00, U_10, U_01, U_11`

corresponding to the two standard affine charts in each factor.

Use exact algebraic coordinate transitions generated by inversion in the changed factor. The researcher must freeze an explicit coordinate/frame convention before any quotient counts are examined.

Preferred local object:

- a small line bundle `O(a,b)` with explicit local frames and monomial transition factors; or
- if the exact finite implementation is materially cleaner, bounded Laurent-monomial local section data whose regularity/transport across the four affine charts is generated directly by the coordinate-ring transitions.

The final choice must be algebraically explicit and must state exactly what is source algebraic data versus finite truncation/readout data.

### 4.1 Deterministic parameter registry — no success-driven tuning

Use a small predeclared parameter sweep rather than selecting one lucky example after inspection.

At minimum include:

- exponent/window bounds `B = 1,2,3` where meaningful;
- at least two small line-bundle degree choices if line-bundle frames are used, preferably including `(1,0)` and `(1,1)` or another equally explicit pair;
- all chart moves/queries in the declared finite language.

If a parameter choice is impossible or degenerate, record why; do not silently drop it.

Hard-target evidence must not rely on a single post-selected parameter instance.

---

## 5. Algebraic source-generation gate

Produce an exact source-generation registry proving that every finite transition/restriction/continuation relation used by H0E is generated from:

- the chosen algebraic variety;
- its declared affine coordinate rings / localization maps;
- declared line-bundle transition functions or equivalent algebraic local-section rules;
- the predeclared finite exponent/section window;
- explicitly typed sink/reject states if totalization is required.

Forbidden:

- hand-writing the desired finite transition table and then assigning algebraic names;
- using H0D's `MS3_BINARY_OVERLAP_ASSEMBLY` table as generator data;
- selecting state identifications because they are known to minimize the future language;
- using known Hodge/cycle answers;
- importing a classical cohomology complex as generator.

Required replay property:

`ALGEBRAIC_FORMULAS -> GENERATED_FINITE_SOURCE_TABLE`

must be deterministic and independently reproducible.

The generated source may be functional or relational. If genuinely relational/partial, do **not** force determinism without typing the totalization or generalizing D1 correctly.

---

## 6. Multi-step requirement

The load-bearing algebraic source must be genuinely multi-step.

At minimum:

- at least three nonfinal cuts/stages or an equivalent recursive depth >= 3;
- at least two distinct continuation operations/overlap moves where algebraically meaningful;
- at least four distinct declared remaining queries/words at an early cut;
- at least one branching/diamond/revisit pattern if supported by the chart graph;
- at least one accepting/extendable and one rejecting/nonextendable source state/query.

A disguised one-step classifier does not count.

If the chart graph naturally revisits charts, repeated algebraic transport must replay exactly and identity loops must return the algebraically correct state up to the declared frame equivalence.

---

## 7. Relational generalization gate

H0D D1 is proved for finite layered deterministic total systems.

If the actual algebraic source is relational/nondeterministic, H0E must either:

1. prove an exact relational analogue using complete remaining **observation-support signatures**; or
2. provide a mathematically justified deterministic state expansion whose added state is source-generated and whose cost is included in both baselines.

For a relational analogue, seek:

- complete suffix-support signature `Sigma_i(s)`;
- quotient by equality of complete future observation-support behavior;
- well-defined descended relation/action semantics;
- soundness/completeness for every declared remaining query;
- coarsest sufficient quotient theorem on the declared finite carrier or strongest exact statement.

Generic automata/bisimulation/minimization mathematics receives zero novelty credit but may still qualify operationally if attribution passes.

---

## 8. Double source-baseline sandwich — mandatory

Before quotient counts or success evaluation, freeze:

### `B_raw^alg`

The direct algebraic local-state representation with exact generated restrictions/transitions and explicit local/intermediate states.

### `B_std^alg`

A stronger fair algebraic baseline. It must allow all source-side operations that a competent algebraic proof would naturally use at the declared finite scope, including as applicable:

- composition of restriction/transition maps;
- localization and exact coordinate substitution;
- elimination of explicitly quantified intermediate local states;
- direct image/preimage of finite relation carriers;
- ordinary simplification of Laurent monomials / local-frame exponents;
- source-native equality/regularity checks;
- obvious source-side symmetry/relabeling reductions;
- any predeclared algebraic normal form already available without constructing the candidate future-behavior quotient.

Do not make `B_std^alg` artificially weak merely to preserve attribution.

The candidate behavioral/suffix quotient itself is the transform being tested and need not be inserted into the source baseline by definition. But if an equivalent smaller reusable recursive interface is already available from standard source-side algebraic structure without that transform, attribution is not robust.

Mandatory gaming control:

include at least one candidate that appears advantageous against `B_raw^alg` but is defeated by `B_std^alg`.

---

## 9. D1 instantiation gate

Construct the actual algebraically sourced analogue of D1:

`q_i : S_i^alg -> Q_i^alg`

where classes are determined by the complete declared remaining algebraic continuation/observation language.

Required exact properties:

1. independent definition before success evaluation;
2. every declared future observation factors through `q_i`;
3. next algebraic continuation operations descend to `Q_i^alg` or the correct relational analogue;
4. quotient execution reproduces source execution on the full finite registry;
5. the quotient is coarsest sufficient for the declared future language on the finite carrier, or the strongest exact theorem is stated;
6. a reusable-interface/dependency measure is predeclared and strictly improves;
7. `LEVERAGE_ATTRIBUTION_CERTIFICATE` passes against both `B_raw^alg` and `B_std^alg`.

Hard-target attribution must be:

`ROBUST_TRANSFORM_ATTRIBUTED`.

`BASELINE_SENSITIVE_ATTRIBUTION` does not pass.

---

## 10. Proof-leverage requirement

At least one strict H0A0 leverage class must pass, preferably:

- `DEPENDENCY_REDUCTION`;
- `COMPOSITIONAL_FACTORING`;
- `NORMAL_FORM`;
- `FINITE_OBSTRUCTION_BASIS`.

The preferred primary measure is still a reusable recursive interface measure, but it must include all state introduced by algebraic totalization/relational expansion.

Do not count:

- a reduction that already exists in `B_std^alg`;
- dropping algebraically relevant data that changes future outputs;
- query-by-query precomputation with no reusable quotient interface;
- state-count reduction produced by arbitrary truncation rather than the realization transform.

---

## 11. Algebraic naturality / presentation subgate

H0E must go beyond H0D's finite label permutation test.

At minimum test:

- swapping the two standard charts in one `P^1` factor;
- swapping the two factors of `P^1 x P^1` when the chosen source structure permits it;
- equivalent local-frame conventions for `O(a,b)` if line-bundle frames are used;
- one refinement or alternative cover presentation if feasible within the declared finite source.

Required outcome for a global toy-object claim:

- quotient structures transport by exact isomorphism/equivalence under the declared algebraic presentation change; or
- the claim is explicitly restricted to the chosen cover/presentation.

Do not resurrect the historical all-presentations H0A mainline. Use only the naturality needed by the actual candidate.

---

## 12. Functoriality stress

Include at least one nontrivial algebraic morphism or automorphism compatible with the chosen source family.

Preferred examples:

- factor swap on `P^1 x P^1`;
- a simple product morphism induced by homogeneous polynomial maps on one factor;
- another exact morphism for which local algebraic pullback/restriction is explicit.

State whether the generated finite source and quotient commute with this map at the claimed scope.

Finite evidence is not a universal theorem.

---

## 13. Hodge-adjacent but no Hodge leakage

The source family may be chosen to be p=1-adjacent (local sections / line-bundle / Cartier-style data) because the later Hodge program must eventually confront divisor/line-bundle algebraicity.

However H0E must **not** use as generator:

- Lefschetz `(1,1)` theorem;
- the exponential sequence proof;
- known Picard/Hodge identification as the target answer;
- known divisor representatives selected from a Hodge class;
- Hodge numbers;
- harmonic/Kahler representatives.

Classical line-bundle/descent mathematics may be used as source semantics/control with exact prior-art labeling.

No result in H0E is called Lefschetz `(1,1)` or Hodge.

---

## 14. Rational coefficient boundary

If the algebraic source uses integer exponents or discrete local states, state explicitly how this does and does not interact with the eventual rational Hodge coefficient problem.

H0E does not need a `C_H -> Q_i^alg` map.

But it must not infer that integer-first local state implies integral Hodge classes.

---

## 15. R3 readiness preinterface

H0E does not need to reach R3.

If robust attributed R2 survives actual algebraic sourcing, produce a Driver-facing preinterface identifying the next missing maps:

`C_H(X)`

`-> actual algebraically sourced multi-step source`

`-> attributed quotient operational constraint`

`-> algebraic-cycle / Chow lifting obligations`.

The preinterface must make clear which arrow is now real and which remains missing.

Do not open H1 automatically.

---

## 16. Required controls

Negative controls:

1. hand-coded transition table with algebraic labels attached afterward;
2. quotient chosen from known future answers rather than independently defined full behavior;
3. source-side algebraic composition that wins only against `B_raw^alg` but not `B_std^alg`;
4. arbitrary state truncation that reduces count while changing the future language;
5. presentation-dependent quotient with no declared transport law.

Positive controls:

1. H0D D1 may be replayed read-only as the abstract attributed-R2 control;
2. H0D0 DFT remains the prior-art + transform-attributed control;
3. one source-inherited algebraic simplification should be included and correctly denied attribution credit.

---

## 17. Classification

Freeze exactly one strongest disposition:

### `H0E_R2_ALGEBRAICALLY_SOURCED_ROBUST_ATTRIBUTED`

A genuine algebraic source generates the multi-step system and D1-style realization retains operational R2 with `ROBUST_TRANSFORM_ATTRIBUTED` leverage against both algebraic baselines.

### `H0E_R3_ALGEBRAIC_SOURCE_HODGE_PRESEED`

H0E R2 plus an unusually strong typed Hodge-side preinterface is obtained. This does not prove Hodge and still returns to Driver review.

### `H0E_R1_ALGEBRAIC_SOURCE_REALIZED_NO_ROBUST_ATTRIBUTION`

The algebraic source and quotient are mathematically valid, but the strict leverage is source-inherited/baseline-sensitive or no strict reduction survives.

### `H0E_FAIL_ALGEBRAIC_SOURCE_GENERATION`

The intended multi-step carrier cannot be generated from permitted algebraic data without hand-coding/target leakage.

### `H0E_FAIL_PRESENTATION_NATURALITY`

A claimed global toy-object result depends essentially on presentation and no adequate transport theorem is supplied.

### `H0E_INSUFFICIENT`

A concrete theorem-critical object remains unresolved; state it exactly.

No other disposition without Driver review.

---

## 18. H1 firewall

`H1_ADMISSIBLE = false` throughout H0E unless a later Driver explicitly overrides after review.

Do not construct full Enterprise cohomology.
Do not start H1 automatically even if H0E reaches R2/R3-preseed.

---

## 19. Required artifacts

At minimum produce:

1. `research_results/HODGE_H0E_ALGEBRAIC_SOURCE_SPEC.json`
2. `research_results/HODGE_H0E_ALGEBRAIC_GENERATION_REPLAY.json`
3. `research_results/HODGE_H0E_PARAMETER_REGISTRY.json`
4. `research_results/HODGE_H0E_MULTISTEP_SOURCE_REGISTRY.json`
5. `research_results/HODGE_H0E_ALGEBRAIC_BASELINE_SANDWICH.json`
6. `research_results/HODGE_H0E_SUFFIX_OR_BEHAVIOR_QUOTIENT_REGISTRY.json`
7. `research_results/HODGE_H0E_COMPARISON_THEOREM_REGISTRY.json`
8. `research_results/HODGE_H0E_PROOF_LEVERAGE_CERTIFICATE_REGISTRY.json`
9. `research_results/HODGE_H0E_ATTRIBUTION_CERTIFICATE_REGISTRY.json`
10. `research_results/HODGE_H0E_BASELINE_GAMING_CONTROL.json`
11. `research_results/HODGE_H0E_PRESENTATION_NATURALITY_LEDGER.json`
12. `research_results/HODGE_H0E_FUNCTORIALITY_LEDGER.json`
13. `research_results/HODGE_H0E_PRIOR_ART_NOVELTY_LEDGER.json`
14. `research_results/HODGE_H0E_TARGET_LEAKAGE_LEDGER.json`
15. `research_results/HODGE_H0E_RATIONAL_BOUNDARY.json`
16. `research_results/HODGE_H0E_HODGE_R3_PREINTERFACE.json`
17. `research_results/HODGE_H0E_CLASSIFICATION.json`
18. `research_results/HODGE_H0E_SEMANTIC_CHECKPOINT.md`
19. deterministic checker + checker output
20. manifest with SHA-256 digests.

Checker PASS is protocol consistency only and must not imply R2/R3 unless classification says so.

---

## 20. Mandatory checker gates

At minimum verify:

- H0D frozen head/source consumed exactly;
- generated source table is reproducible from algebraic formulas;
- no hand-coded H0D table is used as generator;
- parameter sweep is predeclared and complete;
- multi-step depth requirement passes;
- `B_raw^alg` and `B_std^alg` are frozen before success counts;
- baseline gaming control is rejected;
- quotient is independently defined;
- all declared future outputs are preserved;
- descended operations are well-defined;
- strict leverage metric is predeclared;
- any hard-target R2 has `ROBUST_TRANSFORM_ATTRIBUTED` status against both baselines;
- novelty is reported but not used as R2 gate;
- presentation/naturality scope is explicit;
- target leakage PASS;
- no Hodge/Lefschetz proof claim;
- H1 remains blocked.

---

## 21. Completion estimates / advancement vector

Before H0E:

- Hodge conceptual route: `~39%`;
- Criterion V2 + attribution addendum: `FROZEN`;
- abstract robust attributed R2: `FOUND at H0D`;
- actual algebraic sourcing of that R2: `OPEN`;
- R3 Hodge bridge: `NOT FOUND`;
- Enterprise cohomology: `0%`;
- algebraic lifting theorem: `0%`;
- unknown Hodge attack readiness: `0%`.

If H0E passes robust algebraic sourcing:

- Hodge conceptual route: `~47%`;
- algebraically instantiated operational R2: `QUALIFIED ON TOY SOURCE`;
- R3 bridge search: `NEXT ADMISSIBLE RESEARCH TARGET`;
- H1 still `NOT_ADMISSIBLE` pending Driver review.

Advancement vector:

`algebraic-sourcing +40 / attributed-R2-instantiation +30 / presentation-naturality +20 / functoriality +15 / R3-readiness +15 / H1 +0 / Hodge-proof +0`.

---

## 22. Scientific meaning

H0D proved that a reusable future-behavior quotient can create real proof/interface leverage on a finite multi-step source.

H0E asks whether this mechanism survives contact with **actual algebraic geometry**, rather than a source table designed independently of an algebraic object.

Success means:

> an exact algebraically generated local-to-global source admits a smaller, reusable, transform-attributed Enterprise operational interface while preserving the declared theorem-critical future language.

That would be the first realization component in this program that is simultaneously:

- operationally nontrivial;
- transform-attributed;
- algebraically sourced;
- presentation-audited;
- still free of Hodge target leakage.

It still would not prove Hodge.

Driver return target:

`EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`

Do not auto-start the next stage.