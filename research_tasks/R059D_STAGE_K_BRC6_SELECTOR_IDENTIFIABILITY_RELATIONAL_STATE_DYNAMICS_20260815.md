# R059D Stage K — BRC6 Selector Identifiability / True Relational State Dynamics

Task-ID: `RS-R059D-STAGE-K-BRC6-SELECTOR-IDENTIFIABILITY-RELATIONAL-STATE-DYNAMICS`
Generation: `R059D`
Status: `DRIVER_APPROVED_TASKBOOK`
Date: `2026-08-15`
Driver: `EM-DVR-R0457K / CONTROL_PLANE`
Researcher-ID: `EM-R059D-9C6B2A`
Owner branch: `research/r059d-stage-k-brc6-selector-robustness-dynamics`

## 0. Frozen parent / identity correction

Consume Stage-J BRC6 only from frozen owner head:

`9f2b70d6cca5ccd66a46cc6dd18730f40a6add72`

Frozen provenance parent:

`03650b38df5950b86cb2636db9e43094683b1bc8`

Stage-J BRC6 artifacts are immutable.

The frozen artifacts contain stale embedded Researcher-ID `EM-R059D-4C7E21` because the BRC6 researcher had not yet been given a separate ID while the old graded-relay Stage-J researcher was still running.

Driver control-plane identity map:

`research_controls/R059D_STAGE_J_PARALLEL_RESEARCHER_IDENTITY_MAP_20260815.md`

From Stage K forward, the authoritative BRC6 researcher identity is:

`EM-R059D-9C6B2A`.

Do not consume the old graded-relay Stage-J branch/results. It remains a separate superseded side lane owned by `EM-R059D-4C7E21`.

## 1. Driver acceptance of Stage J

Freeze at exactly the following strength:

- `ALIGNED_SEGMENT_CELL_COUNT=L`; in the frozen positive Stage-J registry `L=4` and all six candidate segments have identical L.
- L is not a BRC6 selector input, score, tie-break, coefficient, perturbation criterion, or large-N threshold.
- C6 is six relational channel labels only.
- candidate launch count `A_d=O[d]+M[i,d]`.
- exact continuation quotient `C_n^d=A_d+n I[d]` for frozen `n=0,1,2,3`.
- `Z_d=(C_0^d,C_1^d,C_2^d,C_3^d)`.
- F1 gives a valid exact cyclic-label-equivariant partial selector by unique lexicographic maximum.
- exact nontrivial cyclic stabilizer states are correctly left `BRC6_UNRESOLVED_BY_CURRENT_SIGNATURE`; this is evaluator verdict only, never a seventh output.
- `BRC6_SIX_OUTCOME_SURJECTIVITY_ON_FROZEN_REGISTRY` is established by one asymmetric witness plus six cyclic relabelings.
- huge N around `10^36` was tested only as common system-scale count background; no length threshold was used.
- perturbation registries include exact six-way output tables.

Driver retypes the positive result as:

`BRC6_CYCLIC_EQUIVARIANT_PARTIAL_SELECTOR_ESTABLISHED`.

Do **not** promote the following:

`BRC6_TOTAL_SELECTOR = NOT_ESTABLISHED`

`BRC6_SELECTOR_CANONICALITY = NOT_ESTABLISHED`

Reason: F1 is a valid pre-frozen coefficient-free comparator, but the Stage-J F2 box proves that other exact label-blind shared expressions can select different channels on the same witness. F1 is therefore a construction, not yet a theorem that the native algebra uniquely forces lexicographic priority.

Also freeze:

`STAGE_J_PROFILE_REUSE_DYNAMICS = DIAGNOSTIC_ONLY`

The Stage-J period-1/3/6 sequences reused a normalized signature profile after every decision. They are valid channel-label diagnostics but are not yet true state evolution derived from the actual selected transitions.

## 2. Core Stage-K questions

Stage K asks two separate questions.

### K-Q1 — Selector identifiability

Which BRC6 outputs are forced/robust by the exact channel-count state, and which depend on the chosen label-blind comparator?

### K-Q2 — True BRC6 state dynamics

If the selected channel is actually traversed and the C6 incidence/passage counts are updated by declared relational events, what channel-label sequence results when BRC6 is recomputed from the new state at every aligned decision?

Do not reuse the old frozen profile as the state-update rule.

## 3. Foundation / geometry firewall

Native objects remain only packet/path/count/declared relational channel structures.

Forbidden as theorem-critical premises or BRC6 semantics:

- line / straightness;
- distance / geometric length;
- shortest path / geodesic;
- angle / turn / left / right / opposite;
- rotation angle;
- displacement vector;
- curvature;
- area / volume;
- Euclidean embedding or metric;
- force / energy / stress / strain / elasticity;
- physical direction calibration;
- physical probability;
- quantum amplitude.

`ALIGNED_SEGMENT_CELL_COUNT=L` is an exact packet/cell count only. All six candidates at each positive decision state must have equal L.

Any candidate-dependent L is `UNEQUAL_LENGTH_CONTAMINATION` and cannot be used as selector evidence.

## 4. BRC symbol and output semantics

Do not expand the acronym BRC.

Freeze:

`C6={0,1,2,3,4,5}`

as relational channel labels only.

A resolved deterministic BRC6 value is always exactly one element of C6.

`BRC6_UNRESOLVED_BY_CURRENT_SIGNATURE` is evaluator status only, never a seventh output.

## 5. Stage K-A — Selector robustness registry

Before scoring new witnesses, freeze a finite registry of exact label-blind selector relations/comparators.

Mandatory controls include at least:

1. `K-F1-LEX` — frozen Stage-J lexicographic maximum of `Z_d`.
2. `K-F1R-REVERSE-LEX` — same tuple, reverse horizon priority; control for horizon-order dependence.
3. `K-ENDPOINT-MAX/MIN` — unique max/min of the final frozen continuation count `C_K^d`; K is a declared counting horizon, not L.
4. `K-VISIT-MAX/MIN` — unique max/min of exact `sum_{n=0}^K C_n^d`.
5. `K-PARETO` — coefficient-free partial relation: d is a robust count-dominance winner iff it weakly dominates every competitor in every frozen spectrum coordinate and is strict in at least one coordinate against each competitor.
6. frozen F2 coefficient-box controls from Stage J or a strictly pre-frozen extension, always with one shared coefficient vector for all channels.

Every comparator must be cyclic-label equivariant and use identical arithmetic for all labels.

No comparator may read absolute label number, target identity, N/q-specific tables, or candidate segment length.

### Required selector classifications

For every tested state classify exactly:

- `SELECTOR_CONSENSUS_RESOLVED`: all predeclared comparators that are defined/resolved agree on one channel and no comparator gives a conflicting resolved channel.
- `SELECTOR_DEPENDENT`: at least two admissible exact label-blind comparators resolve to different channels.
- `SYMMETRY_UNRESOLVED`: exact stabilizer obstruction forbids a unique equivariant deterministic channel.
- `SIGNATURE_INSUFFICIENT_UNRESOLVED`: no exact stabilizer obstruction is proved, but the current finite signature family does not resolve.

Do not relabel `SELECTOR_DEPENDENT` as physical randomness or probability.

### Mandatory Stage-J base audit

Re-evaluate `W_ASYM_BASE` under the whole frozen Stage-K comparator registry.

The Driver expects this state may be `SELECTOR_DEPENDENT` because Stage J already showed F2 parameter sensitivity. Prove the exact classification; do not protect F1 from negative evidence.

## 6. Stage K-B — Horizon/signature extension audit

Separate two causes of unresolvedness.

Freeze a predeclared counting-horizon registry, for example:

`K in {1,2,3,4,6,8}`

or a similarly finite exact registry justified by compute limits.

For every horizon recompute exact CPBC continuation signatures without changing candidate segment L.

If deeper exact counts break a previous accidental tie, record:

`HORIZON_INSUFFICIENT_UNRESOLVED_RESOLVED`.

If a nontrivial channel automorphism preserves **all** controller-visible exact signatures at every tested/derived depth, preserve the stronger symmetry obstruction.

Do not claim a finite-horizon failure is a global impossibility unless an exact automorphism theorem proves it.

## 7. Stage K-C — True relational aligned-step carrier

Construct at least several finite pure-relational `C6_PORT_ALIGNED_CARRIERS` before trajectory scoring.

Each aligned decision node x must have six candidate channel labels d in C6.

For each `(x,d)` declare purely relationally:

- a target aligned node `y=T_NODE(x,d)`;
- an endpoint ingress port label `j=T_INGRESS(x,d)` at y;
- an aligned segment packet/cell count L.

Mandatory positive condition:

for every fixed x,

`L(x,0)=...=L(x,5)`.

`T_NODE` and `T_INGRESS` are declared port-pairing/transport relations only. Do not interpret `T_INGRESS` as opposite direction, angle, or vector.

Freeze carriers/transport maps before trajectory scoring.

Include multiple non-isomorphic or at least structurally distinct port-transport registries when computationally feasible.

Require carrier covariance: under any declared C6 relabeling automorphism tau, target/ingress port relations relabel consistently.

## 8. Stage K-D — Actual event-state update

At each aligned decision state at node x with current ingress i:

1. compute current exact local signatures `I_x[d], O_x[d], M_x[a,b]` from the accumulated event history;
2. construct every candidate `Z_d` by the frozen CPBC count recurrence/readout;
3. apply the selected BRC6 comparator being tested;
4. if unresolved, stop that deterministic trajectory and record the exact unresolved class;
5. if channel d is selected, record the real declared egress event:
   - `O_x[d] += 1`;
   - `M_x[i,d] += 1` when current ingress i is declared;
6. traverse exactly the frozen equal-cell-count segment `(x,d)`;
7. arrive at `y=T_NODE(x,d)` with ingress `j=T_INGRESS(x,d)` and record:
   - `I_y[j] += 1`;
8. recompute the next decision from the newly accumulated exact state.

Do not reuse the previous normalized signature profile unless it is independently proved to arise from this update.

Static PACKET_COUNT, segment L, and fixed event count are not selector evidence.

## 9. Stage K-E — True multi-step BRC6 dynamics

For each frozen carrier, initial state, and comparator class, compute the actual sequence

`d_0,d_1,d_2,...`

and relative channel-label sequence

`r_k=d_(k+1)-d_k mod6`

only after every state has been updated by Stage K-D.

Classify exact behavior as applicable:

- `TRUE_STATE_FIXED_CHANNEL_CLASS`;
- `TRUE_STATE_PERIODIC_CHANNEL_WORD`;
- `TRUE_STATE_EVENTUALLY_PERIODIC`;
- `TRUE_STATE_APERIODIC_WITHIN_PROVED_WINDOW`;
- `TRUE_STATE_UNRESOLVED_STOP`;
- `SELECTOR_DEPENDENT_TRAJECTORY`.

A cycle must be certified from repeated full relevant state/finite quotient, not merely repeated output labels.

Do not call channel-label cycles rotation, turning, curvature, orbit, or physical motion calibration.

## 10. Stage K-F — Selector-dependence versus trajectory-dependence

Mandatory cross-test:

For the same initial exact state/carrier, run every pre-frozen admissible comparator.

Determine whether:

- different selectors choose different first channels but later recoalesce to the same exact state class;
- different selectors produce genuinely different full-state trajectories;
- all selectors agree on a robust subset of states/trajectories.

Freeze one or more exact statements if supported:

- `BRC6_SELECTOR_ROBUST_STATE_DOMAIN`;
- `BRC6_SELECTOR_DEPENDENT_STATE_DOMAIN`;
- `BRC6_SELECTOR_DEPENDENT_DYNAMICS`;
- `BRC6_DYNAMIC_RECOALESCENCE_ACROSS_SELECTORS`.

Do not hide signed/label-specific differences in aggregate summaries.

## 11. Stage K-G — Perturbation under true state dynamics

Reuse legal perturbation types only after baseline trajectories are frozen:

- one exact count token;
- one current incidence event;
- one real tagged adjacency/launch-contribution perturbation.

Measure at every aligned decision:

- `DELTA_BRC6_k`;
- first divergence epoch;
- whether trajectories later exactly recoalesce in the full state quotient;
- unresolved transitions induced/removed by perturbation.

All candidate segment L values remain unchanged.

## 12. Stage K-H — Large-N system-scale test

Retain `N~10^36` only as system/tag/packet count scale.

Never use N as segment length.

For symbolic replicated state families, test whether comparator classification and true-state update laws remain exact under large common count backgrounds.

Prefer exact cancellation/factorization proofs. No O(N) huge enumeration.

If a large-N result changes because a count offset/residue changes, classify it exactly as count-state dependence; do not call it a physical macro/micro transition without a separate identifiability theorem.

## 13. Key interpretation gate

Stage K is **not** allowed to conclude that F1 is the native BRC law merely because it works.

Highest possible selector claims require evidence of comparator robustness or a theorem that removes the arbitrary comparator choice.

Possible dispositions include:

- `BRC6_SELECTOR_ROBUST_DYNAMIC_KERNEL_FOUND`;
- `BRC6_SELECTOR_DEPENDENT_BUT_TRUE_STATE_DYNAMICS_FOUND`;
- `BRC6_PARTIAL_SELECTOR_ONLY_NO_CANONICAL_DYNAMIC_LAW`;
- `BRC6_SIGNATURE_INSUFFICIENT_FOR_TRUE_DYNAMICS`.

Continue freezing:

`PHYSICAL_DIRECTION_CALIBRATION = NOT_ESTABLISHED`

`PHYSICAL_PROBABILITY_FROM_BRC_COUNTS = NOT_ESTABLISHED`

`PHYSICAL_RIGIDITY_INTERPRETATION = NOT_ESTABLISHED`

`PHYSICAL_ELASTICITY_INTERPRETATION = NOT_ESTABLISHED`

`QUANTUM_BRIDGE = NOT_ESTABLISHED`.

## 14. Required artifacts

At minimum produce:

1. `R059D_STAGE_K_SELECTOR_ROBUSTNESS_PROTOCOL.json`
2. `R059D_STAGE_K_SELECTOR_COMPARATOR_REGISTRY.json`
3. `R059D_STAGE_K_HORIZON_SIGNATURE_ATLAS.json`
4. `R059D_STAGE_K_C6_PORT_ALIGNED_CARRIER_REGISTRY.json`
5. `R059D_STAGE_K_TRUE_STATE_UPDATE_PROTOCOL.json`
6. `R059D_STAGE_K_TRUE_DYNAMICS_ATLAS.json`
7. `R059D_STAGE_K_SELECTOR_DEPENDENCE_LEDGER.json`
8. `R059D_STAGE_K_PERTURBATION_TRAJECTORY_RESPONSE.json`
9. `R059D_STAGE_K_LARGE_N_REGISTRY.json`
10. `R059D_STAGE_K_SYMMETRY_AND_UNRESOLVED_LEDGER.json`
11. `R059D_STAGE_K_TRIVIALITY_AND_LEAKAGE_LEDGER.json`
12. deterministic checker + output
13. report
14. artifact manifest
15. frozen checkpoint

All frozen Stage-J artifacts remain byte-immutable.

Then:

`STOP_FOR_DRIVER_REVIEW`.
