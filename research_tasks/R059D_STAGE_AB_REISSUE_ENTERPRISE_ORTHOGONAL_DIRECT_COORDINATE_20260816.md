# R059D Stage AB REISSUE — ENTERPRISE ORTHOGONAL / DIRECT SIX-DIRECTION COORDINATE GENERATION

Task-ID: `RS-R059D-STAGE-AB-REISSUE-ENTERPRISE-ORTHOGONAL-DIRECT-COORDINATE`
Generation: `R059D`
Stage: `AB REISSUE`
Status: `DRIVER_APPROVED_TASKBOOK`
Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`
Researcher-ID: `EM-R059D-4E8B71`
Date: `2026-08-16`

## 0. User correction / scientific pivot

The user supplied a direct main-coordinate construction by drawing the lattice:

- choose positive `u` as the `0°` direction;
- origin `O=(0,0,0)`;
- one `0°` step gives `(1,0,0)`;
- from `(1,0,0)`, one `+60°` step gives `(1,-1,1)`, hence the `+60°` step increment is `(0,-1,1)`;
- define the `+60°` relation from the primary axis as **进取垂直** (`ENTERPRISE_ORTHOGONAL`).

This retypes the old X/Y/Z/AA transverse staircase as a historical collapse/readout experiment, not the main lattice-address generator.

The superseded twin-world Stage AB must not be executed.

## 1. Enterprise Orthogonality

Freeze a project-local relation:

`ENTERPRISE_ORTHOGONAL(u,p)`

meaning that `p` is one positive C6/A2 sector turn from `u`.

In classical Euclidean calibration this sector is `+60°`.

Important wording:

- `ENTERPRISE_ORTHOGONAL = one primitive +60° lattice turn`;
- it is not a claim that classical Euclidean perpendicularity is 60°;
- when interoperability with Euclidean geometry is discussed, classical perpendicular remains 90° and Enterprise Orthogonal is a distinct project relation.

## 2. Two direct coordinate generators

Freeze

`e=(1,0,0)` for the `0°` unit step,

`p=(0,-1,1)` for the `+60°` Enterprise-Orthogonal unit step.

The independent coordinate group is the free abelian rank-2 group generated by `e,p`.

Every lattice cell has candidate address

`C(q,r)=q e + r p=(q,-r,r)`, with `q,r in Z`.

Do not introduce floating point.

## 3. Six C6/A2 unit directions

Using the frozen A2/C6 adjacency scaffold, derive and audit the six unit directions:

- `d0 = e = (1,0,0)`;
- `d60 = p = (0,-1,1)`;
- `d120 = p-e = (-1,-1,1)`;
- `d180 = -e = (-1,0,0)`;
- `d240 = -p = (0,1,-1)`;
- `d300 = e-p = (1,1,-1)`.

Required checks:

- opposite directions are negatives;
- the six directions occur in the correct C6 cyclic adjacency order;
- elementary triangular/hexagonal closure relations return to the expected cell;
- the user control `(1,0,0)+d60=(1,-1,1)` holds exactly.

If the A2/C6 scaffold does not force this minimal extension from `e,p`, report the exact missing relation rather than changing the user's two controls.

## 4. Main coordinate set

The direct triple-coordinate image is

`Lambda_E = {(x,y,z) in Z^3 : y+z=0}`

under the chosen frame.

Equivalently every cell has unique axial pair `(q,r)` and stored triple

`(q,-r,r)`.

Prove or refute exact bijection:

`Z^2 <-> Lambda_E`, `(q,r) -> (q,-r,r)`.

The triple is the project-facing coordinate. The pair `(q,r)` may be used as an implementation normal form.

## 5. Path-generated coordinates

For a finite path word in the six directions, define its coordinate by exact integer sum of the corresponding unit increments.

Prove on the A2/C6 scaffold:

- same CELL_ID reached by different legal paths produces the same final coordinate;
- step followed by inverse returns the previous coordinate;
- commuting/reordered path controls give the same endpoint where CELL_ID says they recoalesce;
- elementary closed cycles have zero total displacement;
- distinct CELL_IDs in the tested atlas have distinct coordinates.

The target is not a preferred path. The target is path-independent endpoint address.

## 6. Traceability

For every cell preserve:

`CELL_ID -> one or more path words -> summed direct coordinate -> exact endpoint coordinate`.

A deterministic canonical path may be stored for replay, but it is not the only native history.

Given `(x,y,z)` in `Lambda_E`, reverse exactly:

`q=x`, `r=z=-y`.

Then generate a canonical replay word using signed copies of `e` and `p`.

## 7. Frame / orientation semantics

The user chose `+u` as `0°` and the positive 60° sector as Enterprise Orthogonal. This is a coordinate-frame declaration.

Audit the reflected frame where the Enterprise-Orthogonal direction is `-60°` and prove that it is a mirror coordinate chart, not a different lattice ontology.

Do not claim the homogeneous infinite lattice intrinsically prefers the chosen origin or clockwise/counterclockwise orientation.

Absolute address may therefore retain a chart/frame identifier when multiple frames coexist.

## 8. Relation to older R059D results

Retype, do not delete:

- Stage W first-round homogeneous coordinate model remains a historical negative control;
- Stage W REISSUE2 remains a valid experiment on an alternative stored-coordinate semantics;
- Stage X binary staircase and Y/Z/AA frontier analyses remain mathematically valid only for that prior transverse-collapse readout line;
- they do not constrain the new direct main coordinate unless an explicit bridge theorem is later proved.

In particular the old pure-ray form

`Gamma_u(n)=(n,-a_n,-a_n)`

must not be used as the main coordinate after this reissue.

The direct main `+u` ray is

`(n,0,0)`.

## 9. What “collapse” means after this correction

Test the following retyping:

The coordinate-generating collapse is the algebraic reduction of six local C6 directions to two independent generators `e,p`, with all other directions represented by integer combinations.

This is a structural / relational collapse, not a root-rounding rule.

Do not force square-root, triangular, frontier, or binary-staircase machinery into the main coordinate generator.

## 10. Bounded atlas validation

Build at least radius 8 around the origin, or larger if cheap.

For every CELL_ID record:

- all six neighbors;
- direct coordinate;
- at least one canonical path;
- all shortest paths if cheap;
- cyclic and reflection images;
- collision audit;
- path-recoalescence audit.

Required deterministic results:

- zero coordinate collisions among distinct CELL_IDs;
- zero same-CELL_ID coordinate disagreements;
- every tested coordinate satisfies `y+z=0`;
- all six neighbor transitions match the frozen increment table.

## 11. Required artifacts

Freeze at least:

1. `R059D_STAGE_AB_ENTERPRISE_ORTHOGONAL_DEFINITION.json`
2. `R059D_STAGE_AB_DIRECT_GENERATOR_PROTOCOL.json`
3. `R059D_STAGE_AB_SIX_DIRECTION_TABLE.json`
4. `R059D_STAGE_AB_MAIN_COORDINATE_BIJECTION.json`
5. `R059D_STAGE_AB_PATH_INDEPENDENCE_PROOF.json`
6. `R059D_STAGE_AB_TRACEABLE_CELL_REGISTRY.json`
7. `R059D_STAGE_AB_REFLECTION_FRAME_AUDIT.json`
8. `R059D_STAGE_AB_OLD_COORDINATE_RETYPE_LEDGER.json`
9. deterministic checker source/output
10. report
11. manifest
12. frozen checkpoint.

## 12. Hard negatives

Checker must reject at minimum:

- `ENTERPRISE_ORTHOGONAL_IDENTIFIED_WITH_EUCLIDEAN_90_DEGREES`;
- `PLUS_U_MAIN_RAY_USES_OLD_TRANSVERSE_STAIRCASE`;
- `OLD_STAGE_AA_FRONTIER_RULE_REQUIRED_FOR_MAIN_COORDINATE`;
- `FLOATING_POINT_REQUIRED`;
- `DISTINCT_CELL_ID_COORDINATE_COLLISION_IGNORED`;
- `SAME_CELL_PATH_DISAGREEMENT_IGNORED`;
- `PREFERRED_PATH_USED_TO_DEFINE_CELL_IDENTITY`;
- `ORIGIN_OR_ORIENTATION_CALLED_INTRINSIC_WITHOUT_CERTIFICATE`.

## 13. Required disposition

Return one:

- `ENTERPRISE_ORTHOGONAL_DIRECT_COORDINATE_FOUNDATION_PROVED`;
- `DIRECT_COORDINATE_FOUNDATION_REQUIRES_ONE_ADDITIONAL_A2_RELATION`;
- `USER_TWO_STEP_CONTROL_INCONSISTENT_WITH_FROZEN_CELL_SCAFFOLD`;
- `SEMANTIC_HARD_STOP` with exact gate.

Then stop for Driver review. Do not resume root/frontier coordinate-generation claims before this direct-coordinate checkpoint is reviewed.
