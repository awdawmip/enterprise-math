# Research Taskbook — Native Filament Coupled-Selection External Novelty Audit

Task-ID: `RS-NATIVE-FILAMENT-COUPLED-SELECTION-EXTERNAL-NOVELTY-AUDIT`

Date: `2026-08-25`

Issuing Researcher-ID: `EM-FREE-NEPS-239A6D`

Hard target:

`NATIVE_FILAMENT_COUPLED_SELECTION_EXTERNAL_NOVELTY_CLASSIFIED_AS_KNOWN_OR_COROLLARY_OR_PARTIAL_OVERLAP_OR_NO_DIRECT_MATCH`

## 1. Input

Read only:

`research_inputs/NATIVE_FILAMENT_COUPLED_SELECTION_V2_LITERATURE_AUDIT_PACKET_20260825.md`

The input statements were already independently checked mathematically in a prior blind audit. This task is a literature/novelty audit, not a source-proof review.

## 2. Independence wall

Before freezing the literature-audit return, do **not** read:

- PR #627;
- branch `research/native-filament-generalization-theorem-package-20260824`;
- source proofs or package-specific checkers for that branch;
- unpublished novelty opinions from the originating researcher.

You may and should use external literature, books, journal papers, arXiv, MathSciNet-like bibliographies, authors' papers, and reputable mathematical references.

The earlier mathematical audit PR #631 may be consulted only for its final statement-strength verdict if needed; do not use it as novelty evidence.

## 3. Required search domains

Search theorem statements, not only keywords, across at least:

1. integral affine/hyperplane arrangements modulo integers;
2. characteristic quasi-polynomials, arithmetic Tutte/G-Tutte, Dedekind-domain arrangements;
3. modular/periodic codes generated by integral matrices;
4. Reed--Solomon/MDS cosets with periodic/alternating offsets;
5. sampled tangent arrangements to conics/parabolas over finite fields;
6. conic duality and tangent concurrence in finite geometry;
7. cyclotomic numbers of order 2 and quadratic-residue translate intersections;
8. modular quadratic sequences and covering systems;
9. polynomial orbit sieves / divisibility of quadratic sequences;
10. arithmetic or deterministic percolation on periodic/quadratic carriers;
11. CRT-transparent fibers and finite-wheel obstructions;
12. profinite sieve products, multiplicatively split compact sets, and local-global survivor sets;
13. any literature linking local discrete curvature/sector combinatorics to arithmetic breaker phases.

## 4. Required row verdicts

For each statement group `S1` through `S8` in the input packet, assign exactly one:

- `KNOWN_DIRECT_THEOREM`;
- `KNOWN_IMMEDIATE_COROLLARY`;
- `KNOWN_COMPONENTS_ONLY`;
- `PARTIAL_OVERLAP_REQUIRES_NARROWING`;
- `NO_DIRECT_MATCH_FOUND`.

Then assign one package-level verdict for the full coupled chain.

## 5. Evidence standard

For every row:

- list the strongest/closest source(s);
- give title, author(s), publication/arXiv year, DOI/arXiv identifier where available;
- quote or precisely paraphrase the relevant theorem statement;
- map hypotheses and conclusions to the audit statement;
- say exactly what remains uncovered, if anything.

A keyword resemblance is not enough.

If one theorem subsumes a row after routine substitution, classify it as `KNOWN_IMMEDIATE_COROLLARY`, not `NO_DIRECT_MATCH_FOUND`.

If several classical ingredients jointly reproduce a row with only straightforward algebra, classify it conservatively; do not manufacture novelty from composition alone.

## 6. Hard prior-art guards already known

The following are already conceded classical and must not be promoted as novelty:

- characteristic quasi-polynomials / arithmetic arrangement counting;
- coefficient monotonicity under modulus divisibility;
- Reed--Solomon/MDS theory;
- CRT;
- quadratic character sums;
- order-2 cyclotomic numbers;
- Legendre transform / conic duality;
- generic profinite product and Hausdorff-dimension methods.

The audit target is the exact structured coupling, not these tools.

## 7. Search log requirement

Record:

- databases/search engines used;
- exact search phrases;
- closest false positives;
- why each close source does or does not subsume the statement.

For every `NO_DIRECT_MATCH_FOUND`, the search log must be detailed enough for another researcher to reproduce the search.

## 8. No overclaim rule

`NO_DIRECT_MATCH_FOUND != PROVEN_NOVEL`.

The strongest allowed language in that case is:

`NO DIRECT THEOREM-STATEMENT MATCH FOUND IN THE AUDITED LITERATURE SET`.

Do not use `new theorem`, `first`, `novel`, or publication-level novelty language unless the task is later upgraded to a formal novelty review with broader bibliographic coverage.

## 9. Required return

Write:

`research_returns/NATIVE_FILAMENT_COUPLED_SELECTION_EXTERNAL_NOVELTY_AUDIT_RETURN_20260825.md`

The return must contain:

1. audit metadata and independence attestation;
2. S1--S8 verdict matrix;
3. package-level verdict;
4. exact source mapping for all known/partial rows;
5. reproducible search log;
6. recommended wording for PR #627;
7. exact list of any statements that must be narrowed or relabeled;
8. final hard-target line.

## 10. Freeze condition

Do not read PR #627 or its proof/checker branch until the return file is frozen in Git.

After freeze, source comparison may occur in a separate review step.
