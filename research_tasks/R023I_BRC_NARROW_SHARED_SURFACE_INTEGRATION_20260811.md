<!-- ENTERPRISE_MATH_TASK_V1
{
  "task_id": "RS-R023I-BRC-NARROW-SHARED-SURFACE-INTEGRATION",
  "title": "R023I BRC Narrow Shared-Surface Integration",
  "kind": "RESEARCH",
  "owner": "taskbook/unassigned",
  "base_state": "READY",
  "priority": "P0",
  "leverage": "FOUNDATIONAL_INTEGRATION",
  "frontier": "Replay the frozen R023 Lean-checked Branch-Recoalescence Collapse semantic core into the shared theorem/formal surface without changing theorem content, broadening the Boolean/result-support contract, or importing the R021 representation Pareto layer.",
  "next_action": "Consume the exact frozen R023 Lean module, register only the required root/shared-surface/lineage entries, verify status wording and semantic ownership, and return a bounded integration payload or a precise semantic correction if exact replay is impossible.",
  "dependencies": [
    {
      "target": "RS-R023-BRC-SEMANTIC-LEAN-CORE",
      "action": "CONSUME_FROZEN_OWNER_HEAD_0b72b9e549e1469567764fbe89f9f2baa8b55453",
      "satisfied": true
    },
    {
      "target": "R023 semantic Lean candidate",
      "action": "PRESERVE_EXACT_THEOREM_CONTENT_FROM_6eea57de1d30d6c2fe983121f6e209286a5c9895",
      "satisfied": true
    },
    {
      "target": "P023 deterministic future-safe theorem family",
      "action": "PRESERVE_DETERMINISTIC_BASELINE_AND_LAYER_BRC_AS_RELATIONAL_SUPPORT_SEMANTICS",
      "satisfied": true
    }
  ],
  "source_refs": [
    "R023 Draft artifact #498 / final owner head 0b72b9e549e1469567764fbe89f9f2baa8b55453",
    "R023 frozen semantic Lean candidate 6eea57de1d30d6c2fe983121f6e209286a5c9895",
    "standard warnings-fatal Lean layer run 31484436882 PASS on final owner head",
    "bilingual-sync PASS on final owner head",
    "quality/reference residual failure isolated to pre-existing shared repository-tool index drift for tools/research_identity.py and tools/research_taskbook.py"
  ],
  "evidence_status": "BRC_LEAN_CHECKED_NARROW_SHARED_SURFACE_GATE",
  "last_progress_ref": "R023 final owner head Lean gate accepted by Driver",
  "last_progress_at": "2026-08-11T19:15:00+08:00",
  "hard_block": null,
  "tags": [
    "R023I",
    "BRC",
    "branch-recoalescence-collapse",
    "Lean",
    "shared-surface",
    "integration",
    "no-new-mathematics"
  ],
  "claim_lease_minutes": 1440,
  "created_by_role": "RESEARCH_DRIVER",
  "task_authority": "DRIVER_APPROVED",
  "identity_policy": "AUTO_RESOLVE_OR_ALLOCATE",
  "identity_lane": "R023I",
  "policy_review": {
    "policy_set": "research_taskbook_policy.json",
    "policy_digest": "sha256:a03b06c1c6d29ca2776592fd12aa77406f45a21afb8fc1a8431b25cd41963c77",
    "review_state": "PASS",
    "temporary_overrides": []
  }
}
-->

# R023I — BRC Narrow Shared-Surface Integration

Status: `READY / P0 / FOUNDATIONAL_INTEGRATION / NO NEW MATHEMATICS / NOT CANONICAL`

## 1. Mother question

R023 has a frozen Boolean/result-support semantic core whose final owner head passed the repository's warnings-fatal Lean layer. The remaining question is purely one of exact exposure:

> can that frozen theorem package be registered in the shared formal/theorem surface without changing theorem statements, silently strengthening status, or confusing pointwise future-safe quotients with relational support semantics?

This task is a bounded integration slice. It does not reopen R021 discovery and does not search for stronger BRC mathematics.

## 2. Frozen input and accepted status

Use the exact R023 owner payload at:

- final owner head `0b72b9e549e1469567764fbe89f9f2baa8b55453`;
- semantic Lean candidate `6eea57de1d30d6c2fe983121f6e209286a5c9895`;
- module `EnterpriseMath/Relation/BranchRecoalescence.lean`;
- return audit `docs/R023_BRC_LEAN_RETURN.md`.

Accepted formal content consists of:

1. `NO_RESURRECTION` in factorization form;
2. `ONE_STEP_COARSEST` as a kernel/factorization universal property, including the coarse-successor-support specialization;
3. `SUPPORT_BRANCH_INVARIANT` preserving the exact reachable `Set X` under exact split, relational direct image, and literal-union recoalescence;
4. `FORGETFUL_RECOALESCENCE_IFF` with operational suffix safety defined independently of signature equality;
5. the three-state one-step-exact/two-step-inexact quotient-composition witness;
6. the middle-incidence correlation witness and the current-coarse-insufficient suffix-safety witness.

The semantic status is `LEAN_CHECKED_WIP / READY_FOR_NARROW_SHARED_SURFACE_INTEGRATION`, not yet `LEAN_CHECKED_MAIN`.

## 3. Exact integration delta

Start from the current shared surface and make only the smallest changes required to expose the frozen R023 theorem family.

Expected candidate files are:

- exact replay of `EnterpriseMath/Relation/BranchRecoalescence.lean` from the frozen owner payload;
- `EnterpriseMath.lean` root registration if required;
- `research_common_surface.json` entries needed for root-import and theorem-family routing;
- `docs/RESEARCH_COMMON_SURFACE.en.md` and `docs/RESEARCH_COMMON_SURFACE.zh-CN.md` entries describing the same scoped theorem family;
- existing theorem/status/lineage sidecars only when their current schema requires a record for a newly exposed Lean theorem family.

Do not create a duplicate theorem source or rewrite the owner module merely to fit prose naming.

## 4. Semantic wording that must survive unchanged

Shared-surface prose must preserve all of these boundaries:

- runtime branching does not resurrect a point distinction erased from the complete runtime encoding;
- the one-step coarsest result is uniqueness of the induced kernel/partition up to relabeling, not literal uniqueness of a classifier codomain;
- exact support branching preserves Boolean/set-valued reachable support through union-preserving relational execution;
- literal exact-union recoalescence and forgetful/hull recoalescence are different operations;
- forgetful replacement is safe exactly relative to the declared remaining language/observable signature;
- current coarse equality alone is not sufficient for forgetful safety;
- multiplicity, provenance, probability/weights, and signed or amplitude cancellation are outside this carrier and require a richer semantics.

Do not describe BRC as semantically stronger than the deterministic P023 future-safe quotient. The shared layering should read conceptually as:

`pointwise future-safe factorization baseline -> relational/result-support execution layer -> representation/resource choices`.

## 5. Negative-boundary registration

Expose the two counterexamples only at the scope actually established:

- three fine states are the minimum only within the R021 declared exhaustive one-generator/full-starting-fibre search class; the Lean witness proves failure, not the global enumeration minimality claim;
- the middle-incidence example proves that marginal/nonempty coarse middle information can create a spurious composition when middle identity is forgotten.

Do not generalize either witness into an unstated global impossibility theorem.

## 6. Exclusions

The following remain outside this integration slice:

- R021 branch-dictionary Pareto tables and branch-budget optimization;
- R022 md5collgen/HashClash tool-mining conclusions;
- a canonical minimal branching presentation;
- multiplicity/provenance/weighted/signed carriers;
- new arithmetic specializations;
- new Foundation primitives.

The known repository-tool index drift involving `tools/research_identity.py` and `tools/research_taskbook.py` is an independent shared-surface maintenance item. Do not absorb unrelated tool-routing edits into the BRC theorem payload merely to make this task look self-contained.

## 7. Required audit

Return an exact file/delta table stating for every changed file:

- why the change is required for the R023 theorem family;
- whether it is an exact replay, root registration, human router entry, machine router entry, or lineage/status entry;
- whether any theorem statement or assumption changed;
- the post-integration status label that is justified by the actual coverage.

Also provide a name map from the four R021/R023 claim IDs to the exact Lean declaration names exposed through the shared surface.

## 8. Correction gate

If exact shared exposure requires changing a theorem statement, adding a new semantic hypothesis, weakening a proved conclusion, or changing the carrier interpretation, do not repair that inside this integration slice.

Return:

`R023I_SEMANTIC_REPLAY_MISMATCH / RETURN_TO_R023`

with the smallest exact mismatch.

Mechanical namespace/import/router adjustments that do not alter theorem meaning are allowed.

## 9. PASS / return classification

Preferred return:

`R023I_EXACT_REPLAY_COMPLETE / BRC_SHARED_SURFACE_DELTA_READY / NO_NEW_MATHEMATICS / NOT_CANONICAL`

If the payload is exact but an unrelated repository maintenance defect prevents a clean final gate, return:

`R023I_EXACT_REPLAY_COMPLETE / EXTERNAL_MAINTENANCE_GATE_REMAINS / NOT_CANONICAL`

If semantic replay is impossible without changing R023 mathematics, use the correction return from Section 8.

The task succeeds by exposing the already-proved theorem family faithfully, not by adding more BRC theory.
