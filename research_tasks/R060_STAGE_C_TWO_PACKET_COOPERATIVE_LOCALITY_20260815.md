# RS-R060 — STAGE C TWO-PACKET COOPERATIVE LOCALITY / PLATEAU REPAIR TEST

Task-ID: `RS-R060-STAGE-C-TWO-PACKET-COOPERATIVE-LOCALITY`
Generation: `R060`
Stage: `C`
Status: `DRIVER_APPROVED_TASKBOOK`
Identity-policy: `AUTO_RESOLVE_OR_ALLOCATE`
Identity-lane: `R060`
Date: `2026-08-15`
Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`

## 0. Frozen parent

Stage B is accepted as the scientific parent of this task at:

- owner head: `a36083002fe5a676afb6b3972bf792086c782bce`
- Stage-B checkpoint SHA256: `4e0f8d719b6cc4737bbf5b790fb672a74003ce72b6592cedf322242ea7220505`
- disposition: `PLATEAU_QUOTIENT_STRONG_STABILITY_FAILS_EXACTLY`

Stage 0 / A / B artifacts and counterexamples are immutable.

Preserve exactly:

- carrier `C3_RELATIONAL_RANK3_CHANNEL_CARRIER`;
- `UNIT_PACKET(x)=1`;
- scalar readout `B(C)=CONTACT_CUT_COUNT`;
- full carrier-automorphism quotient;
- Stage-A raw S1/S2 failures;
- Stage-B zero-B one-packet plateau quotient and its PS1/PS2 failures;
- R059L remains parked and is not a parent.

Do not modify old results to make Stage C look successful.

## 1. Scientific question

Stage B established two facts simultaneously:

1. one-packet `ΔB=0` plateau completion is genuinely useful: it merges the minimal Stage-A S2 pair at `N=4` and pushes plateau-level PS1/PS2 failure to `N=6`;
2. it is insufficient: the two `N=6` terminal branches remain distinct terminal plateaus.

The next minimal question is therefore:

> Is the remaining instability caused by the **single-packet locality restriction**, rather than by the contact-cut scalar itself?

Stage C tests the smallest cooperative extension only: exactly two packet relocations acting as one relational event.

Do not add a new scalar, metric, geometry, tie-breaker, stochastic rule, or arbitrary preferred shape.

## 2. Cooperative-2 move semantics

Freeze a proof/experiment operation called:

`D3_L2_CONNECTED_TWO_PACKET_RELOCATION`.

For a connected fixed-N state `C`, choose:

- two distinct occupied packets `u1,u2 in C`;
- two distinct empty packets `v1,v2 notin C`;
- a bijection `pi:{u1,u2}->{v1,v2}` such that each `ui ADJ pi(ui)` in the frozen carrier.

Let:

`C'=(C\{u1,u2}) union {v1,v2}`.

The move is admissible only if:

1. `C'` is connected under frozen adjacency;
2. the changed support `{u1,u2,v1,v2}` is connected under the induced frozen adjacency relation;
3. no intermediate one-packet state is required to be admissible — the pair is one simultaneous cooperative event;
4. no metric distance, centroid, Euclidean geometry, graph-distance score, or external energy is consulted.

The changed-support connectedness is a relational locality condition, not a geometric diameter.

Reject duplicate descriptions of the same unordered cooperative event deterministically.

## 3. Exact delta bookkeeping

For every admissible L2 move, compute exactly:

`ΔB = B(C')-B(C)`.

Where possible derive and freeze a local contact-count identity for `ΔB` using only the contacts removed/added by the four changed packets, including corrections for contacts internal to the changed support.

Do not rely only on whole-state recomputation if an exact local identity can be proved.

Classify every admissible L2 event into:

- `L2_ZERO` if `ΔB=0`;
- `L2_STRICT` if `ΔB<0`;
- `L2_UPHILL` if `ΔB>0`.

`L2_UPHILL` is diagnostic only and is never an allowed collapse transition in Stage C.

## 4. Cooperative plateau completion

Stage C does not discard the Stage-B one-packet zero relation.

Define the new zero relation as the symmetric closure generated by:

- all frozen legal one-packet `L1_ZERO` relocations from Stage B;
- all admissible `L2_ZERO` cooperative relocations from Section 2.

Take exact connected components modulo the frozen full carrier automorphism group.

Call them:

`C2_PLATEAU`.

Every C2 plateau must have one constant `B` value.

Raw-state truth remains separate: merging in a C2 plateau does not make raw states equal.

## 5. Cooperative strict macro dynamics

Between C2 plateaus, allow a directed macro edge if any representative admits either:

- a frozen one-packet strict D3_L1 move with `ΔB<0`, or
- an admissible L2_STRICT cooperative move with `ΔB<0`

into a representative of the target C2 plateau.

Every macro edge must strictly lower B.

Therefore termination should remain exact. Prove it explicitly.

Do not permit uphill macro edges.

## 6. Primary diagnostics — N<=6 first and stop early

Reuse the complete frozen raw orbit registry `N=1..6`.

Do not expand N before answering all of the following exactly:

### C3-1 — Does cooperative zero closure merge the Stage-B N=6 split?

The Stage-B critical terminal plateaus were:

- `N06-P0003`, containing `N06-O0005`;
- `N06-P0007`, containing `N06-O0014` and `N06-O0045`.

Determine whether their raw members lie in one C2 plateau under `L1_ZERO + L2_ZERO`.

Return an explicit L2-zero witness/path if yes, or an exact component-separation certificate if no.

### C3-2 — Does an L2 strict escape destroy either Stage-B terminal plateau?

Determine whether any member of those plateaus has an admissible L2_STRICT exit.

If yes, give the exact move and `ΔB`.

### C3-3 — Recompute plateau confluence

Define:

`C2TP(C)` = terminal C2 plateaus reachable under the cooperative strict macro DAG.

Test:

- `C2PS1(C): |C2TP(C)|=1`;
- `C2PS2(C): C2PS1(C)` and every frozen P1 one-packet perturbation neighbor `C1` satisfies `C2TP(C1)=C2TP(C)`.

Important: P1 perturbation semantics remain the frozen Stage-0 definition. P1 may have positive, zero, or negative `ΔB`; do not weaken robustness after seeing a failure.

### C3-4 — Earliest failure

Find exact minimal:

`N_C2PS1_FAIL`

and

`N_C2PS2_FAIL`

inside `N<=6`, if they exist.

If both properties hold for all `N<=6`, return only a bounded PASS. Do not promote it to an all-N theorem.

## 7. Mandatory comparison table

Freeze a side-by-side progression for each N=1..6:

- raw connected orbit count;
- Stage-A raw terminal orbit count;
- Stage-B one-packet plateau count and terminal plateau count;
- Stage-C C2 plateau count and terminal C2 plateau count;
- earliest raw S1/S2 failure;
- earliest Stage-B PS1/PS2 failure;
- earliest Stage-C C2PS1/C2PS2 failure.

The goal is to measure exactly what cooperative locality repairs and what it does not.

## 8. Interpretation discipline

Allowed conclusions include:

- `SINGLE_PACKET_LOCALITY_CAUSES_STAGE_B_SPLIT` if exact cooperative-2 evidence establishes that the critical split disappears for the stated reason;
- `TWO_PACKET_COOPERATION_INSUFFICIENT` if the split/failure survives;
- `C2_BOUNDED_STRONG_STABILITY_N_LE_6` if C2PS1+C2PS2 holds throughout the frozen registry.

Forbidden conclusions:

- `WORLD_IS_3D`;
- `3D_STABLE` from bounded enumeration;
- `RANK3_IMPOSSIBLE` from a failure;
- physical energy barrier / gravity / curvature / surface tension interpretations;
- that Stage-A or Stage-B failures were wrong;
- that a cooperative quotient is a unique raw shape.

## 9. Semantic firewall

Strictly forbidden in Stage C:

- new scalar or fitted score;
- lexicographic/tie-break preference invented after the counterexample;
- Euclidean norm/distance/angle/area/volume;
- centroid/center/radius;
- graph distance as an optimization score;
- shortest path / geodesic;
- stochastic selection;
- uphill collapse transition;
- three-or-more-packet cooperative moves;
- changing the rank-3 carrier;
- changing frozen P1 perturbation semantics;
- using R059L as a required parent;
- treating `B` as geometric surface area.

## 10. Required artifacts

Freeze at least:

1. `R060_STAGE_C_L2_COOPERATIVE_MOVE_PROTOCOL.json`
2. `R060_STAGE_C_L2_DELTA_B_IDENTITY.json`
3. `R060_STAGE_C_L2_MOVE_REGISTRY.json`
4. `R060_STAGE_C_C2_PLATEAU_EQUIVALENCE_PROTOCOL.json`
5. `R060_STAGE_C_C2_PLATEAU_ATLAS.json`
6. `R060_STAGE_C_C2_MACRO_DAG.json`
7. `R060_STAGE_C_STAGE_B_SPLIT_REPAIR_CERTIFICATE.json`
8. `R060_STAGE_C_C2PS1_ATLAS.json`
9. `R060_STAGE_C_C2PS2_ATLAS.json`
10. `R060_STAGE_C_COMPARISON_LEDGER.json`
11. `R060_STAGE_C_SEMANTIC_CLAIM_LEDGER.json`
12. deterministic Stage-C checker output
13. `R060_STAGE_C_COOPERATIVE_LOCALITY_CHECKPOINT.json`

## 11. Checker hard negatives

The checker must reject at least:

- `THREE_PACKET_MOVE_USED`;
- `DISCONNECTED_CHANGED_SUPPORT_ALLOWED`;
- `UPHILL_MACRO_MOVE_ALLOWED`;
- `NEW_SCALAR_ADDED`;
- `TIE_BREAKER_ADDED`;
- `P1_PERTURBATION_REDEFINED`;
- `C2_PLATEAU_PROMOTED_TO_RAW_EQUALITY`;
- `STAGE_A_FAILURE_ERASED`;
- `STAGE_B_FAILURE_ERASED`;
- `BOUNDED_C2PS1_PROMOTED_TO_ALL_N`;
- `BOUNDED_C2PS2_PROMOTED_TO_ALL_N`;
- `RANK3_RESULT_PROMOTED_TO_WORLD_DIMENSION`;
- `GEOMETRY_USED_FOR_MOVE_SELECTION`;
- `R059L_USED_AS_PARENT`.

## 12. Required return

Return:

- Researcher-ID;
- exact L2 move definition;
- exact L2 delta-B identity;
- complete N<=6 cooperative move counts;
- exact fate of Stage-B `N06-P0003 / N06-P0007` split;
- exact C2PS1/C2PS2 result and minimal failures if any;
- comparison table;
- checker result;
- artifact hashes;
- checkpoint SHA256;
- owner head / Draft PR if published;
- one primary disposition:
  - `C2_BOUNDED_STRONG_STABILITY_N_LE_6`;
  - `SINGLE_PACKET_LOCALITY_CAUSES_STAGE_B_SPLIT_BUT_OTHER_FAILURE_REMAINS`;
  - `TWO_PACKET_COOPERATION_INSUFFICIENT`;
  - `SEMANTIC_HARD_STOP` with exact violated gate.

Then stop for Driver review.

Do not enter Stage D without authorization.
