# HODGE Stage H0B — Enterprise Operational Proof-Leverage Search

Date: `2026-08-17`
Status: `ACTIVE / DRIVER-ISSUED TASKBOOK`
Driver: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`
Task ID: `RS-HODGE-H0B-ENTERPRISE-OPERATIONAL-PROOF-LEVERAGE`
identity_policy: `AUTO_RESOLVE_OR_ALLOCATE`
identity_lane: `HODGE-H0B-OPERATIONAL-LEVERAGE`
owner branch: `research/hodge-h0b-operational-proof-leverage`
control branch: `research/hodge-special-control-plane`

## 0. Mission

H0A0 has repaired and frozen the realization qualification criterion.

Frozen H0A0 owner head:

`f4e6cf84eb191e0b7442913e018e1f6347e9584e`

Frozen disposition:

`H0A0_CRITERION_V2_FROZEN_NONVACUOUS`

The legacy information-nonfactorization gate is no longer the primary qualification rule.

The current V2 principle is:

> An Enterprise realization may be fully invertible / fully information-recoverable and still be mathematically nontrivial if it moves theorem-critical structure into an independently defined Enterprise operational form and supplies an exact, non-tautological proof-leverage certificate.

This stage is the first **carrier / operational-structure search under Criterion V2**.

The single hard target is:

`ENTERPRISE_OPERATIONAL_REALIZATION_REACHES_R2`

where R2 means:

`R2_NONTRIVIAL_OPERATIONAL_REALIZATION`.

The preferred stronger result is an R3 seed:

`ENTERPRISE_HODGE_RELEVANT_OPERATIONAL_REALIZATION_CANDIDATE`.

However:

- do **not** prove or claim the Hodge conjecture;
- do **not** start full Enterprise cohomology / H1;
- do **not** execute the suspended historical H0A presentation-groupoid task as a mainline search;
- do **not** require information non-factorization against a classical realization as a success condition.

A genuine R0/R1 negative result is valid and must be frozen.

---

## 1. Frozen authority and minimal startup packet

Read only the smallest sufficient packet, then work.

Mandatory:

1. `AGENTS.md`
2. `docs/GITHUB_INTERACTION_BUDGET.md`
3. `research_common_surface.json`
4. `driver_handoffs/HODGE_SPECIAL_DRIVER_HANDOFF_20260817.md`
5. `driver_handoffs/HODGE_SPECIAL_DRIVER_PI_GEOMETRY_ADDENDUM_20260817.md`
6. H0A0 frozen artifacts at head `f4e6cf84eb191e0b7442913e018e1f6347e9584e`:
   - `research_results/HODGE_H0A0_REALIZATION_CLASSIFIER_V2.json`
   - `research_results/HODGE_H0A0_PROOF_LEVERAGE_CERTIFICATE_SPEC.json`
   - `research_results/HODGE_H0A0_COMPARISON_STRENGTH_REGISTRY.json`
   - `research_results/HODGE_H0A0_HODGE_LEVERAGE_INTERFACE.json`
   - `research_results/HODGE_H0A0_H0A1_DECISION.json`
   - `research_results/HODGE_H0A0_SEMANTIC_CHECKPOINT.md`
7. `native_semantics_admissibility.json`
8. `FOUNDATIONAL_LOGIC.md` / `foundational_logic.json` only as needed for native-vs-derived typing.

Consult exact Enterprise theorem/spec/code files only when a candidate uses them. Do not recursively traverse the repository.

Useful already-frozen families may include, if actually consumed:

- Boolean A4 Branch Recoalescence support semantics;
- declared future-signature / future-safe quotient machinery;
- composition-safe collapse / quotient semantics;
- existing A3/A4 relation carriers only within their frozen semantic boundaries.

Do not silently enrich a frozen Boolean carrier with multiplicity, provenance, signed weight, probability, amplitude, or geometry.

---

## 2. Driver acceptance of H0A0 and identity provenance note

Driver review accepts:

`HODGE_RELEVANT_REALIZATION_CRITERION_IS_WELL_TYPED_AND_NONVACUOUS = PASS`.

Criterion V2 is the active Hodge-special qualification criterion for this stage.

The H0A0 published artifacts record Researcher-ID:

`EM-HODGE-H0-2F8C71`.

A prior manual relay had preallocated a different H0A0 ID. Preserve the published H0A0 artifacts without rewriting their mathematics. Record the discrepancy as provenance only; it is not a mathematical blocker and must not contaminate the new H0B identity.

H0B must resolve/use its own role identity under the repository identity protocol.

---

## 3. Frozen Criterion V2 — mandatory semantics

### 3.1 Comparison objects

No bare load-bearing `C(X)` is permitted.

Use the exact declared comparison level:

- `C_full^alg(X)` — full declared algebraic source realization;
- `C_full^an(X)` — full declared analytic realization;
- `C_top(X)` — topological/homotopy reduct;
- `C_H(X)` — exact cohomological/Hodge readout;
- `C_state(X;O)` — declared limited observation/readout.

Information tags are secondary only.

### 3.2 Operational ranks

Use exactly:

- `R0_INERT_REPARAMETRIZATION`
- `R1_DERIVED_REORGANIZATION`
- `R2_NONTRIVIAL_OPERATIONAL_REALIZATION`
- `R3_HODGE_RELEVANT_OPERATIONAL_REALIZATION`

### 3.3 R2 hard gate

Enterprise R2 requires all of:

1. exact typed comparison;
2. independently generated Enterprise operational structure;
3. exact comparison/correctness theorem;
4. complete `PROOF_LEVERAGE_CERTIFICATE`;
5. presentation/naturality/functoriality at the exact scope claimed;
6. target-leakage PASS;
7. non-tautology PASS;
8. explicit prior-art / novelty status.

Information non-factorization is **not** required.

Information enrichment is **not** sufficient.

### 3.4 R3 boundary

R3 requires R2 plus a typed interface:

`rational Hodge-class-side structure`

`-> independently defined Enterprise operational constraint`

`-> explicit algebraic-cycle / Chow lifting interface and unresolved exact obligations`.

H0B may identify an R3 candidate, but final algebraic lifting correctness is not required in this stage.

No H1 automatically starts even if H0B reports R2 or R3. Driver review is mandatory.

---

## 4. H0A1 is now a conditional presentation-naturality sub-gate

The historical H0A source:

`6776540d97f7b4c49d50ee21b93895fd0e15cc3c`

remains suspended and must not be executed unchanged.

H0A0 froze the recommendation:

`RETYPE_H0A1_AS_PRESENTATION_NATURALITY_SUBPROBLEM`.

Therefore H0B uses the following rule:

- if a candidate is source-intrinsic and does not depend on a chart/embedding/grid placement, presentation-naturality may be `N/A` with proof;
- if a candidate consumes chart, cover, embedding, coordinate, ordering, factor choice, finite presentation, or placement data, it must pass a bounded `H0A1_PRESENTATION_NATURALITY_SUBGATE`;
- do not build the full all-presentations groupoid unless the concrete candidate actually needs it.

Minimum sub-gate when triggered:

1. at least two algebraically equivalent presentations;
2. one nontrivial source automorphism / relabeling;
3. product factor-swap if product structure is claimed;
4. exact natural equivalence / equivariance statement at the theorem-critical scope;
5. failure must downgrade the candidate rather than be hidden.

Presentation naturality is a qualification condition, not proof leverage by itself.

---

## 5. Theorem-relevant benchmark: algebraic local-to-global / assembly structure

H0B must not search for arbitrary computational tricks. It must use a benchmark that is structurally relevant to the future Hodge lifting problem.

Primary benchmark family:

`ALGEBRAIC_LOCAL_TO_GLOBAL_ASSEMBLY`.

The benchmark concerns whether algebraically sourced local states / local candidate data / overlap constraints can be checked, propagated, recoalesced, quotiented, or assembled into a declared global algebraic object or consistency state.

This is Hodge-relevant because any future algebraic-cycle lifting mechanism will require exact control of local/global compatibility, descent, support, incidence, multiplicity, rational coefficient behavior, or choice-independence.

But H0B must keep a strict boundary:

- classical sheaf descent, Čech cocycles, Cartier divisor gluing, line-bundle transition functions, Chow groups, and known divisor theory are **prior art controls / checkers**;
- they are not Enterprise generator definitions;
- reproducing a standard descent theorem under new symbols receives no Enterprise R2 credit unless an independently generated Enterprise operational form yields a strict V2 leverage certificate beyond mere renaming.

The benchmark need not claim that successful assembly already gives an algebraic cycle representing a Hodge class.

---

## 6. Required candidate families

Study at least three structurally different Enterprise candidate families. They may fail.

### Candidate A — BRC / support recoalescence local-global route

Start from algebraically declared finite relations / overlaps / transition support.

Use only frozen Boolean A4/BRC semantics unless a richer carrier is independently justified from already frozen Enterprise primitives and algebraic source data.

Attempt to define an Enterprise operational consistency / assembly condition using:

- branch split;
- relational direct image;
- reachable-support propagation;
- literal-union recoalescence;
- suffix-safe forgetful replacement;
- declared future-support signatures if needed.

Seek strict leverage of one or more types:

- `LOCALIZATION`
- `COMPOSITIONAL_FACTORING`
- `DEPENDENCY_REDUCTION`
- `FINITE_OBSTRUCTION_BASIS`

Hard negative boundary:

If the construction merely restates ordinary relation composition, ordinary reachability, or a standard Čech/descent support condition without an independent Enterprise theorem-critical simplification, classify at most R1 / prior-art recovery.

### Candidate B — future-safe quotient / minimal operational language route

From a declared algebraic local/global state space plus a declared family of permitted algebraic operations or transition relations, construct the strongest correctly typed Enterprise future signature / safe quotient available from existing machinery.

Core question:

> Can the global theorem-critical consistency language be compressed to a smaller exact operation/obstruction language while preserving exactly the downstream property being audited?

Potential leverage classes:

- `DEPENDENCY_REDUCTION`
- `FINITE_OBSTRUCTION_BASIS`
- `COMPOSITIONAL_FACTORING`
- `NORMAL_FORM`

The candidate must state the future language explicitly. A quotient is useful only relative to a declared theorem-critical observation/operation language.

Do not claim a universal minimal quotient unless proved.

### Candidate C — algebraically sourced precision / collapse normal-form route

Revisit precision/collapse under Criterion V2, but **not** the failed arbitrary-grid claim from H0.

Precision/collapse input must be one of:

- algebraically generated filtration/partition data;
- explicitly declared operational precision whose dependence is typed and later proved irrelevant at the claim scope;
- another source whose admissibility is proved under `native_semantics_admissibility.json`.

No Euclidean metric/grid/circle/angle/radius may be imported.

Attempt to show that composition-safe collapse or quotienting turns a global/high-layer consistency predicate into:

- a normal form;
- bounded dependency;
- monotone/integer obstruction;
- exact local/compositional rule.

If the only substance is ideal-adic / valuation / Gröbner / tropical / formal-neighborhood prior art represented in Enterprise notation, record prior-art recovery and no Enterprise R2 credit.

### Optional Candidate D — richer relation carrier already frozen in Enterprise Math

A richer A3/A4 carrier may be consumed only if:

- its semantics is already frozen in the repository;
- its weights/multiplicities/labels arise from declared algebraic source data rather than ad hoc invention;
- the candidate records exactly which structure is prior art and which operational theorem is Enterprise-specific.

Do not create a new weighted/provenance carrier merely to force success.

---

## 7. Exact toy registry

Minimum exact test registry:

1. point / trivial source;
2. `P^1_C` with a finite algebraic cover/presentation stress test;
3. `P^1_C x P^1_C` with factor-swap stress if product structure is used;
4. one nontrivial algebraic morphism `P^1 -> P^1`;
5. one finite exact local-overlap / gluing surrogate where all relations can be exhausted;
6. at least one deliberately inconsistent local-data control;
7. at least one consistent local-data control.

Optional Hodge-adjacent controls may use symbolic line-bundle / Cartier / divisor gluing data, but:

- known Picard groups;
- known divisor bases;
- known Hodge numbers;
- Lefschetz `(1,1)` algebraicity;
- known algebraic cycle representatives

must not be Enterprise generator inputs.

Use exact symbolic/integer/rational/algebraic computation where possible.

Finite computation proves only the declared finite carrier unless a theorem is supplied.

---

## 8. Mandatory PROOF_LEVERAGE_CERTIFICATE for every R2/R3 claim

Every candidate claiming R2/R3 must instantiate the frozen H0A0 schema with at least:

1. `certificate_id`;
2. typed source object and exact `C_*` level;
3. typed Enterprise object and N/HX layer;
4. exact source theorem-critical predicate/operator;
5. independent Enterprise definition;
6. exact comparison theorem;
7. non-tautology audit;
8. strict leverage class;
9. exact strict leverage witness;
10. predeclared measure if a scalar/complexity witness is used;
11. presentation/naturality/functoriality scope;
12. target-leakage audit;
13. prior-art/novelty status;
14. exact downstream theorem obligation unlocked.

A phrase such as “simpler”, “more local”, “more discrete”, “more algebraic”, or “easier to prove” is not a certificate.

Accepted leverage classes remain those frozen in H0A0 unless an equally exact new class is justified.

---

## 9. Comparison theorem gate

A candidate does not reach R2 merely because an Enterprise computation matches examples.

At least one exact theorem or exact finite-domain exhaustive proposition must relate the source predicate and Enterprise constraint.

Possible strengths:

- equivalence on declared scope;
- source implies Enterprise;
- Enterprise implies source;
- sound-and-complete on a declared finite subdomain.

One-direction results must be labeled one-direction.

The exact theorem must be proved only after both source and Enterprise predicates are independently defined.

---

## 10. Enterprise-specific origin / prior-art firewall

For every candidate, record:

- exact frozen Enterprise primitives consumed;
- exact algebraic source data consumed;
- prior-art mathematical objects used only as controls;
- whether the resulting invariant/normal form/obstruction already exists before Enterprise realization;
- whether Enterprise merely computes/re-encodes a known invariant;
- strongest justified novelty status.

Classifications include at least:

- `ENTERPRISE_SPECIFIC_OPERATIONAL_CANDIDATE`
- `PRIOR_ART_RECOVERY_NO_ENTERPRISE_R2_CREDIT`
- `GENERIC_RELATIONAL_REPACKAGING_NO_ENTERPRISE_R2_CREDIT`
- `NOVELTY_UNRESOLVED`

A prior-art construction may operationally satisfy the abstract R2 criterion as a **control**, but it does not satisfy the hard H0B target unless an Enterprise-specific candidate independently reaches R2.

---

## 11. Hodge adjacency / R3 pre-interface

H0B's hard target is R2, not R3.

However each surviving R2 candidate must be tested against the H0A0 Hodge-leverage interface.

At minimum answer:

1. Could the operational constraint plausibly occupy `HBR-4` — strict Enterprise leverage?
2. Could its local/global assembly semantics contribute to `HBR-5` — algebraic-cycle lifting interface?
3. What exact missing object prevents a typed `C_H -> Enterprise constraint` theorem?
4. Does the candidate require full Enterprise cohomology before that bridge can even be stated?

If an exact non-tautological Hodge-side bridge is actually found, the stage may classify R3.

Do not manufacture R3 by inserting the Hodge condition into the definition of the Enterprise constraint.

---

## 12. Target-leakage firewall

Forbidden Enterprise generators:

- known Hodge decomposition answers;
- known Hodge numbers;
- known algebraic cycle representatives;
- Lefschetz `(1,1)` conclusion or classical proof copied into the Enterprise predicate;
- singular/cellular/Čech cohomology simply relabeled Enterprise;
- Kähler/harmonic metric structure as undeclared native data;
- classical Euclidean metric / equidistant circle / standard real pi;
- tropical / Gröbner / valuation / formal-neighborhood / Fourier / Smith normal form renamed Enterprise;
- prior-art descent equations copied verbatim and called new Enterprise operational structure.

Allowed as checker/control with explicit typing:

- classical algebraic geometry;
- sheaf/descent/Čech facts;
- divisor/line-bundle theory;
- known Hodge theory;
- prior-art normal forms and obstruction theorems.

Known answers may validate a result after construction, never select the Enterprise construction.

---

## 13. Negative controls

The checker must reject at least:

1. pure relabeling / coordinate change;
2. arbitrary hidden color/fiber;
3. generic relation composition restated as BRC without leverage;
4. standard Čech cocycle condition copied into Enterprise notation;
5. deliberately weak `C_state` producing easy non-factorization;
6. arbitrary re-quantization defect presented as obstruction;
7. prior-art normal form presented as Enterprise novelty;
8. presentation-dependent candidate with no naturality certificate.

---

## 14. Required artifacts

Produce at minimum:

1. `research_results/HODGE_H0B_BENCHMARK_REGISTRY.json`
2. `research_results/HODGE_H0B_ENTERPRISE_CANDIDATE_REGISTRY.json`
3. `research_results/HODGE_H0B_ENTERPRISE_PRIMITIVE_PROVENANCE.json`
4. `research_results/HODGE_H0B_COMPARISON_THEOREM_REGISTRY.json`
5. `research_results/HODGE_H0B_PROOF_LEVERAGE_CERTIFICATE_REGISTRY.json`
6. `research_results/HODGE_H0B_PRESENTATION_NATURALITY_SUBGATE.json`
7. `research_results/HODGE_H0B_PRIOR_ART_NOVELTY_LEDGER.json`
8. `research_results/HODGE_H0B_HODGE_R3_PREINTERFACE.json`
9. `research_results/HODGE_H0B_TARGET_LEAKAGE_LEDGER.json`
10. `research_results/HODGE_H0B_CLASSIFICATION.json`
11. `research_results/HODGE_H0B_SEMANTIC_CHECKPOINT.md`
12. one deterministic checker + checker output
13. manifest with SHA-256 digests.

Exact computation scripts may be added when useful.

---

## 15. Deterministic checker minimum

The checker must at minimum verify:

- Criterion V2 names and ranks are used;
- no bare load-bearing `C(X)`;
- information tag is separate from operational rank;
- no R2 candidate is accepted without complete leverage certificate;
- no prior-art control earns Enterprise R2 credit;
- presentation naturality is checked whenever presentation data is theorem-critical;
- H0A historical task remains suspended;
- H1 remains blocked;
- target-leakage ledger passes;
- finite computation is not promoted beyond its domain;
- final disposition matches the evidence.

Checker PASS means artifact/protocol consistency only.

---

## 16. Completion dispositions

Freeze exactly one strongest primary disposition:

### `H0B_R0_NO_OPERATIONAL_STRUCTURE`

No independently generated Enterprise operational structure relevant to the benchmark survives.

### `H0B_R1_REORGANIZATION_ONLY`

Enterprise-derived organization exists, but no complete strict V2 proof-leverage certificate is established.

### `H0B_R2_PRIOR_ART_CONTROLS_ONLY`

The criterion works and prior-art controls reach operational R2, but no Enterprise-specific candidate does.

This does **not** pass the H0B hard target.

### `H0B_R2_ENTERPRISE_OPERATIONAL_LEVERAGE_FOUND`

At least one Enterprise-specific candidate passes all R2 gates with an exact proof-leverage certificate.

This passes the hard target but does not automatically open H1.

### `H0B_R3_ENTERPRISE_HODGE_RELEVANT_CANDIDATE`

An R2 Enterprise-specific candidate additionally supplies an exact typed non-tautological Hodge bridge/lifting pre-interface satisfying the frozen R3 requirements.

This still does not prove Hodge. Driver review is required before H1.

No other primary disposition is permitted.

---

## 17. Advancement vector

Before H0B:

- Hodge conceptual route: `~23%`;
- realization qualification criterion: `~85% / V2 FROZEN`;
- Enterprise-specific operational R2 candidate: `0 confirmed`;
- R3 Hodge-relevant realization candidate: `0 confirmed`;
- Enterprise cohomology: `0%`;
- algebraic lifting theorem: `0%`;
- unknown Hodge attack readiness: `0%`.

If Enterprise R2 is found:

`operational-realization +35 / proof-leverage +35 / presentation-naturality +10 conditional / Hodge-bridge +10 / cohomology +0 / unknown-Hodge +0`.

If only R1/prior-art controls survive:

freeze the exact no-go boundary and route the next stage to a different Enterprise primitive family rather than relaxing V2.

---

## 18. Scientific meaning

H0B is successful only if Enterprise Math itself changes a theorem-critical operational form in a mathematically load-bearing way.

The target is **not**:

`Enterprise stores more information`.

The target is:

`Enterprise supplies an independently defined operational language in which a Hodge-relevant algebraic local/global proof obligation acquires an exact normal form / local rule / finite obstruction / dependency reduction / compositional factorization that is not merely transported notation.`

That is the first post-H0A0 proof-leverage qualification step toward Hodge.

---

Driver return target:

`EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`

No automatic H1 start.