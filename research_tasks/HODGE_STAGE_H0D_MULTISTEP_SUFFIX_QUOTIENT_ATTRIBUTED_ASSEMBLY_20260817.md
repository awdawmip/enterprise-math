# HODGE Stage H0D — Multi-Step Suffix-Quotient / Attributed Assembly Realization

Date: `2026-08-17`
Status: `ACTIVE / DRIVER-ISSUED TASKBOOK`
Driver: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`
Task ID: `RS-HODGE-H0D-MULTISTEP-SUFFIX-QUOTIENT-ATTRIBUTED-ASSEMBLY`
identity_policy: `AUTO_RESOLVE_OR_ALLOCATE`
identity_lane: `HODGE-H0D-ATTRIBUTED-ASSEMBLY`
owner branch: `research/hodge-h0d-multistep-suffix-quotient-assembly`
control branch: `research/hodge-special-control-plane`

## 0. Driver acceptance of H0D0

H0D0 is accepted.

Frozen H0D0 owner result must be read from:

- owner branch: `research/hodge-h0d0-proof-leverage-attribution`
- frozen disposition: `H0D0_ATTRIBUTION_CRITERION_FROZEN_NONVACUOUS`
- hard target: `ENTERPRISE_PROOF_LEVERAGE_ATTRIBUTION_CRITERION_IS_WELL_TYPED_AND_NONVACUOUS = PASS`

H0D0 freezes four independent axes:

1. information/recoverability;
2. abstract operational R0/R1/R2/R3;
3. leverage attribution relative to a predeclared source baseline;
4. novelty/prior-art status.

The active Hodge-special R2 rule is now:

`R2_V2 + R2_ATTRIBUTION_ADDENDUM_PASS`.

Prior art is not a negative R2 gate. Novelty is not required. Source-inherited leverage does not earn transform attribution.

H0D0 also froze:

`NO_EXISTING_ATTRIBUTED_R2_COMPONENT__SEARCH_NEW_OPERATIONAL_SOURCE`.

Historical H0B/H0C results remain immutable.

---

## 1. Mission

This is the first new operational-source search after the H0D0 attribution repair.

The single hard target is:

`MULTISTEP_ENTERPRISE_REALIZATION_REACHES_TRANSFORM_ATTRIBUTED_R2`.

A stronger optional result is:

`MULTISTEP_ENTERPRISE_HODGE_R3_SEED_FOUND`.

The search is deliberately **not** for external novelty.

The search is for an exact realization/transform whose theorem-critical leverage is absent from a predeclared source representation baseline and for which the realization map is load-bearing in producing the operational normal form / dependency reduction / finite interface / compositional factorization.

The preferred mechanism is a genuinely multi-step Enterprise suffix/future-safe quotient of an algebraic-style local-to-global assembly problem.

Do not:

- reopen legacy information-nonfactorization;
- require new mathematics merely to earn R2;
- search a new primitive family before testing the strongest attributed use of existing Enterprise quotient/BRC machinery on a genuinely new multi-step source scope;
- start full H1 cohomology;
- claim Hodge;
- use known algebraic-cycle answers as generator data.

---

## 2. Frozen authority / minimal startup packet

Read only the smallest sufficient packet, then work.

Mandatory:

1. `AGENTS.md`
2. `docs/GITHUB_INTERACTION_BUDGET.md`
3. `research_common_surface.json`
4. `driver_handoffs/HODGE_SPECIAL_DRIVER_HANDOFF_20260817.md`
5. `driver_handoffs/HODGE_SPECIAL_DRIVER_PI_GEOMETRY_ADDENDUM_20260817.md`
6. H0A0 Criterion V2 artifacts at `f4e6cf84eb191e0b7442913e018e1f6347e9584e`:
   - `research_results/HODGE_H0A0_REALIZATION_CLASSIFIER_V2.json`
   - `research_results/HODGE_H0A0_PROOF_LEVERAGE_CERTIFICATE_SPEC.json`
   - `research_results/HODGE_H0A0_HODGE_LEVERAGE_INTERFACE.json`
7. H0D0 frozen artifacts from `research/hodge-h0d0-proof-leverage-attribution`:
   - `research_results/HODGE_H0D0_FOUR_AXIS_MODEL.json`
   - `research_results/HODGE_H0D0_SOURCE_BASELINE_SPEC.json`
   - `research_results/HODGE_H0D0_LEVERAGE_ATTRIBUTION_CERTIFICATE_SPEC.json`
   - `research_results/HODGE_H0D0_H0B_H0C_ATTRIBUTION_RETYPE.json`
   - `research_results/HODGE_H0D0_ROUTE_DECISION.json`
   - `research_results/HODGE_H0D0_SEMANTIC_CHECKPOINT.md`
8. Exact frozen Enterprise modules only as consumed, especially:
   - `EnterpriseMath/Relation/BranchRecoalescence.lean`
   - future-signature / future-safe quotient surfaces from the canonical common surface;
   - partial-operation future-safety surfaces only if used;
   - composition-safe collapse only if used.
9. `native_semantics_admissibility.json`.

Do not recursively traverse the repository.

---

## 3. Mandatory anti-gaming baseline sandwich

H0D must use **two predeclared source baselines** before evaluating any Enterprise success.

### `B_raw` — raw algebraic/local-overlap source baseline

Freeze:

- exact local carriers;
- exact overlap/restriction/compatibility relations;
- source global-assembly predicate;
- source-native local evaluation;
- direct raw enumeration/execution semantics;
- exact source labels and algebraic data.

`B_raw` may not contain any suffix quotient, future-signature state, automaton minimization, Enterprise branch/recoalescence state, or target normal form.

### `B_std` — strengthened standard source baseline

Before seeing the Enterprise result, enlarge `B_raw` by all **obvious standard source-native manipulations** that a fair baseline should already permit at the declared scope, including as applicable:

- ordinary relation composition/direct image;
- equality tests;
- direct elimination of an explicitly quantified intermediate state;
- ordinary connected-component/path traversal when already present in the source graph;
- ordinary algebraic simplification available directly in the source representation.

Do not include the candidate Enterprise transform itself merely because prior art could define something mathematically equivalent.

The exact boundary between `B_std` and the Enterprise transform must be frozen before success evaluation.

### Attribution strength

Classify attribution as:

- `ROBUST_TRANSFORM_ATTRIBUTED`: credited leverage is absent from both `B_raw` and `B_std` and the transform is load-bearing;
- `BASELINE_SENSITIVE_ATTRIBUTION`: absent only from `B_raw` but already present in `B_std`;
- `SOURCE_INHERITED`: same credited leverage exists already in `B_raw` or `B_std`;
- `SHARED_OR_PARTIAL`;
- `UNRESOLVED`.

The H0D hard target requires at least one `ROBUST_TRANSFORM_ATTRIBUTED` R2 candidate on its declared scope.

Do not make universal impossibility claims about all conceivable source algorithms.

---

## 4. Required source family — genuinely multi-step assembly

Use a finite exact algebraic-style local-to-global source whose theorem-critical obligation is not a one-step output map already equal to the candidate quotient.

At minimum build a source with **three or more sequential/branching overlap stages after the initial state**, such as:

`U0 -> U1 -> U2 -> U3`

or a branching/diamond/tree cover with multiple suffixes.

The source must include:

- finite exact local state carriers `S_i`;
- exact algebraically sourced or algebraic-style overlap relations/restriction compatibility;
- at least one branch point or multiple admissible intermediate states;
- at least one consistent global assembly;
- at least one inconsistent assembly;
- at least two distinct suffix queries/continuations so that a reusable interface state has mathematical content;
- no target Hodge answer.

The preferred source problem is:

> decide/propagate exact global extendability or a declared family of final algebraic observations across a multi-step cover without retaining every fine intermediate state/history.

Finite-source results remain finite-source results unless a theorem is proved.

### Algebraic stress objects

Use at least one actual algebraic toy only as a sourcing/naturality stress test, not as a Hodge-answer generator. Acceptable examples include:

- a bounded finite family of local algebraic data on the standard affine cover of `P^1`;
- a finite bounded local-data family on a simple product/toric cover;
- another exact small algebraic cover.

Do not claim a general scheme/sheaf theorem from a finite surrogate.

---

## 5. Candidate D1 — recursive suffix/future-safe quotient

For each cut/stage `i`, define a raw suffix behavior from a local state `s in S_i` to the declared remaining source language.

Attempt to construct an Enterprise quotient:

`q_i : S_i -> Q_i`

where equality in `Q_i` means exact indistinguishability for the declared remaining suffix language, with all enabledness/domain information included when operations are partial.

The quotient may use:

- support/future signatures;
- declared future-safe equality;
- exact coarsest sufficient partition machinery;
- the strongest correctly typed relational analogue.

Required theorem obligations:

1. soundness: equal quotient states have identical declared suffix behavior;
2. completeness/coarsest claim on the exact finite carrier, if claimed;
3. recursive transition/composition on quotient states;
4. exact relationship between global assembly and the quotient computation;
5. source-baseline attribution under both `B_raw` and `B_std`.

A one-step restatement of H0B Candidate B is forbidden. The new source must require genuine multi-step reusable suffix structure.

---

## 6. Candidate D2 — suffix-safe branch/recoalescence

Attempt a multi-step branch/recoalescence execution where intermediate branches may be forgotten/recoalesced exactly when the declared remaining suffix signatures agree.

Required checks:

- exact fine reachable-support/observation preservation;
- exact suffix-safe forgetfulness condition;
- branch-history reduction measure;
- composition across at least two remaining stages;
- no resurrection/spurious path;
- partial-operation enabledness preserved if applicable.

A Boolean support-only result is permitted if the theorem-critical source language is itself Boolean support. Do not silently add multiplicity/provenance/sign.

The candidate earns R2 only if the branch/recoalescence transform creates an operational form absent from both source baselines.

---

## 7. Candidate D3 — multi-operation / partial future language

Use only if it creates a structurally distinct test.

Declare a finite family of deterministic or relational continuation operations with nontrivial enabledness/domain behavior.

Attempt to build the exact future-safe quotient preserving:

- action enabledness;
- reached observations;
- declared operation words/suffixes.

Do not totalize partial operations ontically by adding an `UNDEFINED` world state; any absorbing undefined sink is verification-only and must remain typed as such.

Target leverage classes may include:

- `DEPENDENCY_REDUCTION`;
- `FINITE_OBSTRUCTION_BASIS`;
- `COMPOSITIONAL_FACTORING`;
- `NORMAL_FORM`;
- `LOCALIZATION`.

---

## 8. Strict leverage / attribution certificate

Every R2 claim must include both:

1. the H0A0 `PROOF_LEVERAGE_CERTIFICATE`;
2. the H0D0 `LEVERAGE_ATTRIBUTION_CERTIFICATE`.

At minimum predeclare a strict measure before evaluation, such as:

- number of intermediate fine states that theorem-critical execution must distinguish;
- number of reusable suffix-interface classes;
- number of branch-history tokens retained after a cut;
- number of local obligations needed for all declared suffix queries;
- size of an exact obstruction/interface basis.

Pure runtime benchmarking is insufficient by itself. The measure must correspond to a theorem-critical structural obligation.

Required anti-gaming question:

> Can the same exact structural reduction be obtained directly inside `B_std` without using the Enterprise realization/transform?

If yes, attribution fails even if the target representation looks cleaner.

---

## 9. Novelty policy

Novelty is a separate axis.

Explicitly allow:

`R2 + ROBUST_TRANSFORM_ATTRIBUTED + CLASSICAL_PRIOR_ART`.

This counts as H0D hard-target success on the declared mathematical scope.

It does **not** count as an external novelty claim.

Record separately:

- `CLASSICAL_PRIOR_ART`;
- `PROJECT_EXISTING_REPACKAGING`;
- `PROJECT_NATIVE_DERIVATION_OF_PRIOR_ART_STRUCTURE`;
- `NOVELTY_UNRESOLVED`;
- `NEW_ENTERPRISE_THEOREM_CANDIDATE`.

Do not demote a transform-attributed R2 merely because the mechanism is known mathematics.

Do not promote a candidate merely because it is novel.

---

## 10. Presentation/naturality subgate

The historical all-presentations H0A remains suspended as a mainline task.

For any claimed H0D candidate, test only the naturality required by its scope:

- state relabeling;
- cover/patch relabeling;
- reversal/order change if the theorem claims order independence;
- algebraically equivalent presentation if an actual algebraic object is promoted beyond a fixed chosen cover.

If the candidate is inherently an ordered suffix machine, order dependence is allowed but must be explicit. Do not call an ordered finite theorem globally presentation-independent.

---

## 11. R3 preinterface

Only after a robust attributed R2 exists, attempt an R3 seed.

Do not start full cohomology.

The minimal R3 preinterface must state:

`C_H(X) rational Hodge-side source condition`

`-> typed local/cover data source for the attributed Enterprise realization`

`-> multi-step Enterprise quotient/assembly constraint`

`-> exact unresolved algebraic-cycle/Chow lifting obligations`.

Required unresolved objects include:

- an actual `C_H -> local algebraic source` comparison map;
- rational coefficient compatibility;
- cover/presentation independence at the claimed global scope;
- cycle-class compatibility;
- existence and correctness of algebraic lifting.

Known cycle representatives may not be generator inputs.

An R3 seed is not a Hodge proof.

---

## 12. Required controls

### Positive attribution control

Re-use the H0D0 DFT control without claiming novelty:

`CLASSICAL_PRIOR_ART + TRANSFORM_ATTRIBUTED_R2`.

### Negative source-inherited controls

Re-use, without rewriting history:

- H0C S1 spanning-forest reduction;
- H0C S2 path telescoping;
- H0C S3 equality-kernel quotient;
- H0B one-step future-safe quotient.

These must remain source-inherited on their frozen scopes.

### Baseline-gaming control

Construct at least one candidate that appears transform-attributed against `B_raw` but becomes source-inherited against `B_std`.

Expected classification:

`BASELINE_SENSITIVE_ATTRIBUTION / NO_H0D_HARD_TARGET_CREDIT`.

This control is mandatory.

---

## 13. Classification

Freeze exactly one strongest primary disposition:

### `H0D_ROBUST_TRANSFORM_ATTRIBUTED_R2_FOUND`

At least one candidate passes V2 R2 plus the attribution addendum against both `B_raw` and `B_std` on the declared scope. Novelty may be zero.

### `H0D_R3_ATTRIBUTED_HODGE_SEED_FOUND`

Robust attributed R2 plus a typed non-tautological R3 preinterface. No lifting theorem is claimed.

### `H0D_BASELINE_SENSITIVE_ONLY`

Apparent attribution disappears under the strengthened fair source baseline.

### `H0D_SOURCE_INHERITED_OR_R1_ONLY`

All candidate leverage is already available in the source baseline or no strict leverage exists.

### `H0D_ATTRIBUTION_UNRESOLVED`

Use only for a concrete missing baseline/comparison theorem that prevents classification.

No other disposition without Driver review.

---

## 14. H1 firewall

`H1` remains blocked.

Even `H0D_ROBUST_TRANSFORM_ATTRIBUTED_R2_FOUND` does not automatically open H1.

H1 requires an accepted R3-class Hodge/lifting interface or explicit Driver override.

No automatic next stage.

---

## 15. Required artifacts

At minimum produce:

1. `research_results/HODGE_H0D_SOURCE_BASELINE_SANDWICH.json`
2. `research_results/HODGE_H0D_MULTISTEP_SOURCE_REGISTRY.json`
3. `research_results/HODGE_H0D_SUFFIX_QUOTIENT_REGISTRY.json`
4. `research_results/HODGE_H0D_BRANCH_RECOALESCENCE_REGISTRY.json`
5. `research_results/HODGE_H0D_PARTIAL_FUTURE_LANGUAGE_REGISTRY.json` or explicit `NOT_USED`
6. `research_results/HODGE_H0D_COMPARISON_THEOREM_REGISTRY.json`
7. `research_results/HODGE_H0D_PROOF_LEVERAGE_CERTIFICATE_REGISTRY.json`
8. `research_results/HODGE_H0D_ATTRIBUTION_CERTIFICATE_REGISTRY.json`
9. `research_results/HODGE_H0D_BASELINE_GAMING_CONTROL.json`
10. `research_results/HODGE_H0D_PRESENTATION_NATURALITY_LEDGER.json`
11. `research_results/HODGE_H0D_PRIOR_ART_NOVELTY_LEDGER.json`
12. `research_results/HODGE_H0D_TARGET_LEAKAGE_LEDGER.json`
13. `research_results/HODGE_H0D_HODGE_R3_PREINTERFACE.json`
14. `research_results/HODGE_H0D_CLASSIFICATION.json`
15. `research_results/HODGE_H0D_SEMANTIC_CHECKPOINT.md`
16. one deterministic checker + checker output
17. manifest with SHA-256 digests.

Checker PASS means protocol/exact finite consistency only, not Hodge.

---

## 16. Mandatory checker gates

At minimum verify:

- H0A0 V2 remains unchanged;
- H0D0 attribution addendum is active;
- novelty does not determine R2;
- `B_raw` frozen before evaluation;
- `B_std` frozen before evaluation;
- no post-hoc baseline weakening;
- source problem is genuinely multi-step, not H0B one-step replay;
- exact soundness/completeness status for each quotient claim;
- strict leverage measure is predeclared;
- attribution checked against both baselines;
- DFT positive attribution control remains PASS;
- H0B/H0C source-inherited controls remain source-inherited on frozen scopes;
- mandatory baseline-gaming control is rejected;
- no bare load-bearing `C(X)`;
- no target leakage;
- no classical circle/pi generator;
- no known Hodge/cycle answer generator;
- no automatic H1;
- no Hodge proof claim.

---

## 17. Completion estimate / advancement vector

Before H0D:

- Hodge conceptual route: `~34%`;
- Criterion V2: `FROZEN`;
- attribution addendum: `FROZEN`;
- robust transform-attributed Enterprise R2: `NOT FOUND`;
- R3 Hodge bridge: `NOT FOUND`;
- Enterprise cohomology: `0%`;
- algebraic lifting theorem: `0%`;
- unknown-Hodge attack readiness: `0%`.

If H0D reaches robust attributed R2:

- realization qualification becomes materially positive on a declared multi-step algebraic-style scope;
- next mainline stage should attempt an actual algebraic/Hodge-side R3 bridge rather than search novelty for its own sake;
- H1 remains blocked pending Driver review.

Advancement vector:

`multi-step-source +25 / transform-attribution +35 / compositional-assembly +30 / baseline-robustness +25 / R3-preinterface +10 / novelty-search +0 / H1 +0 / unknown-Hodge +0`.

---

## 18. Scientific meaning

The point of H0D is not to prove that Enterprise Math invented automata minimization, dynamic programming, quotienting, or relational composition.

The point is to answer a narrower and more important question:

> On a predeclared Hodge-adjacent algebraic local-to-global source representation, does an Enterprise realization/quotient actually create a theorem-critical operational interface that is absent from the fair source baseline and is load-bearing for exact multi-step assembly?

If yes, that is the first robust Hodge-special operational R2, even if every abstract ingredient has prior-art analogues.

If no, freeze the negative result and change operational source rather than weakening the criterion.

Driver return target:

`EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`

Do not automatically start the next stage.