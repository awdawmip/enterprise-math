# P017 — c515 j=1 Low-Pair Canonical Aggregation and Final Finite Closure — Taskbook Proposal

Status: `TASKBOOK_PROPOSAL / PENDING_DRIVER_REVIEW / NOT_DISPATCHABLE`

Date: `2026-08-28`

Proposal author role: `RESEARCHER`

Current owner branch: `research/p017-p2-chen-carry-bridge-20260823`

Frozen mathematical source head:

`1df79532c1470e90d723aa48f5d660e406dca52a`

Proposed task id if Driver decides a new task id is required:

`RS-P017-C515-J1-LOWPAIR-FINAL-AGGREGATION`

Preferred lifecycle decision:

`CONTINUE_SAME_TASK`

Reason: the remaining work is the unique unresolved carrier of the existing P017 finite P2 mother question. This proposal does not itself create a scheduler-dispatchable task and does not assert `DRIVER_APPROVED`.

---

## 0. Control-plane gate

This packet is a researcher-authored continuation proposal. Under the current Enterprise Math role policy, only a `RESEARCH_DRIVER` may promote it to an official `READY / DRIVER_APPROVED` taskbook or dispatch it.

Driver review should preserve the frozen mathematical source head above unless a deliberate rebase audit shows that a later source commit materially changes the frontier.

The preferred review outcome is `CONTINUE_SAME_TASK`, not a new sibling task, because no new mother question has appeared.

---

## 1. Mother question

Close the finite analytic side of the P017 consecutive-square `P2` route at and above the Tier-A splice without reintroducing generic Rosser/Iwaniec factorization multiplicity.

The current finite-oriented packet is

\[
\boxed{
 a=6,\qquad b=\frac{93}{20},\qquad c=\frac{103}{20},\qquad d=\frac59.
}
\]

The Tier-A splice datum is

\[
K_0=116009280740973308,
\qquad W_0=K_0+1.
\]

The preferred exact anchor is

\[
Q_{23}=P(23)=223092870,
\]

with anchored interval length

\[
L_{23}=232018561402828200.
\]

The only remaining load-bearing residual valuation shell is

\[
\boxed{j=1.}
\]

---

## 2. Current frozen frontier — do not redo closed work

The following are inputs, not subtasks.

### 2.1 Base rough population is already finite

Seventh-order odd Bonferroni gives

\[
\boxed{
\frac{S_{\rm odd}(z)}{L_{23}}>\frac{3839}{50000}=0.07678.
}
\]

No fundamental lemma, Mertens approximation, or old lower-Rosser base remainder is needed to finite-normalize the base `S(A,z)` term.

### 2.2 T3 is not an independent remainder

The corrected source bookkeeping identifies `T3` with the least-prime/Buchstab shell. It must not be charged again as a separate upper-sieve error.

### 2.3 One pair credit is exact; two-pair absorption is false

For least-prime exponent `u`, the source pointwise credit absorbs:

1. the least-prime shell; and
2. exactly one maximal ordered-pair penalty uniformly.

The superseded claim that two ordered pairs are uniformly free is forbidden.

Every residual ordered-pair occurrence therefore has a canonical three-distinct-prime witness

\[
\boxed{r<p_1<p,\qquad rp_1p\mid n.}
\]

After conditioning on that witness,

\[
\boxed{Q_{\rm res}\le D^{1/2}=W^{5/9}.}
\]

### 2.4 All valuation shells j>=2 are closed

The complete corrected high-valuation residual satisfies

\[
\boxed{
\frac{|R_{j\ge2}|}{L_{23}}<\frac3{25000}=0.00012.
}
\]

No analytic estimate is to be spent on `j>=2`.

### 2.5 Worst corrected hard Rosser core is finite depth four

After exact `P(23)` prestrip, the hard prime alphabet at the worst residual level is

\[
29\le q\le1439,
\]

containing exactly `219` primes.

At

\[
Q_*=W^{5/9},
\]

every hard upper-Rosser state has

\[
\boxed{\omega(d)\le4}
\]

and the exact worst-level hard-state census is

\[
\boxed{74025}.
\]

### 2.6 Generic factorization multiplicity has already been removed from the hard split

The surviving hard modulus has a unique deterministic suffix factorization

\[
b=b_1b_2
\]

onto the c515 long/short scales. In the legal low-pair interface this gives the exact scale targets

\[
M<B=x^{31/72},
\qquad
N\le N_0=x^{1/8}.
\]

Do not replace this by an `O(1)` well-factorable decomposition.

### 2.7 P(23) anchor multiplicity is controlled by an exact activation automaton

For fixed P(23)-stripped hard modulus `b`, hard depth `s`, and

\[
T=Q/b,
\]

an anchor subset `e` is supported exactly when

\[
T_{\rm crit}^{(s)}(e)<T.
\]

On ratio-`6/5` geometric blocks:

- `i+j<=4` forces `e=1` for both hard-depth parities;
- `i+j<=16` forces `e=1` for even hard depth;
- on those same diagonals odd hard depth has at most nine anchor labels and no two-prime anchor.

Do not charge all `256` anchor states uniformly.

### 2.8 Terminal T4 is already finite

The terminal `T4` Rosser remainder is already bounded by an exact 68-state/Dusart-Abel certificate. It is not part of this task.

When combining final ledgers, convert its normalization from the full-basin denominator to `L23` exactly rather than copying a decimal.

---

## 3. Unresolved new information gap

The exact remaining gap is:

> construct a canonical one-sided aggregation of every corrected `j=1` residual ordered-pair occurrence, respecting the three-prime witness, finite depth-four hard Rosser support, unique hard suffix factorization, monotone external-prime cutoff, and exact P(23) anchor activation law; then prove an explicit bound strong enough to close the finite c515 weighted ledger, or produce an exact no-go certificate for this c515 route.

The task is **not** “make a generic bilinear constant explicit”.

The task is **not** “find another exponent pair”.

The task is **not** “optimize B-spline order”.

---

## 4. Proposed hard objective

`P017_C515_J1_LOWPAIR_RESIDUAL_FINITE_CLOSED_OR_EXACT_NO_GO`

A successful execution must end in exactly one of the following substantive verdicts.

### PASS — FINITE_CLOSURE

Prove a rigorous one-sided estimate for the complete `j=1` residual carrier and combine it, with no double counting, with all already-frozen finite terms to obtain a strictly positive weighted lower bound uniformly for every `W>=W0`, or prove a monotonicity reduction making the `W0` certificate sufficient for every `W>=W0`.

### NO_GO_CURRENT_C515

Prove, under the current exact c515 kernel and allowed reductions, an explicit obstruction showing that the remaining `j=1` carrier cannot fit inside the available finite positive ledger by this route. The no-go must identify the minimal dominant block/family or an unavoidable aggregation multiplicity and must quantify the deficit.

A smaller but still nonclosing numerical constant is **not** a terminal PASS.

---

## 5. Exact task decomposition

### Stage A — canonical three-prime residual-shell reindexing

Construct an injective indexing, or a rigorously bounded-multiplicity one-sided majorant, for residual occurrences using data of the form

\[
(r,p_1,p; e,b)
\]

with

\[
r<p_1<p,
\qquad e\mid P(23),
\qquad (b,P(23))=1.
\]

Requirements:

1. prove where the residual pair `(r,p)` sits after the one-pair credit;
2. prove how `p_1` enters the conditioned sieve level;
3. do not silently reuse the old two-prime level `D/(rp)` after conditioning on `p_1`;
4. establish exact uniqueness or an explicit multiplicity constant for the shell reindexing;
5. retain the monotone largest/second-largest-prime encoding where still legal.

The preferred output of Stage A is an exact theorem of the form

\[
R_{j=1}\le \sum_{\mathfrak s\in\mathcal S} w(\mathfrak s)\,\mathcal E(\mathfrak s)
\]

with every physical residual occurrence represented once, or with an explicitly proved bounded overcount.

### Stage B — integrate the finite depth-four Rosser core

For each conditioned shell, replace generic sieve-factorization language by the exact beta-2 activation threshold

\[
q_{\rm crit}(d)<Q.
\]

Exploit:

- hard primes `29..1439` at the worst residual scale;
- depth `<=4`;
- exact worst-level census `74025`;
- monotonicity of support as `Q` decreases from `W^(5/9)`.

Do not enumerate all possible integers up to `Q`; enumerate only the structurally legal finite Rosser states or certify their aggregate by an exact DP/threshold method.

### Stage C — transport the canonical suffix split through the corrected residual shell

Reuse the unique hard suffix rule only after proving that its hypotheses survive the three-prime conditioning.

Where legal, derive long/short variables satisfying c515 scale ceilings comparable to

\[
M<B=x^{31/72},
\qquad N\le x^{1/8}.
\]

If the conditioned shell changes the natural `M,N`, derive the corrected deterministic split rather than forcing the old one.

No generic well-factorable decomposition may be substituted for this stage.

### Stage D — exact P(23) anchor aggregation

Write every small-prime component through the exact anchor activation law

\[
T_{\rm crit}^{(s)}(e)<T.
\]

On ratio-`6/5` blocks, split by diagonal

\[
h=i+j
\]

and hard-depth parity.

At minimum the proof must separately exploit:

1. `h<=4`: anchor-free for both parities;
2. `5<=h<=16`: even-depth anchor-free, odd-depth at most nine labels;
3. deeper diagonals: exact activation thresholds rather than a uniform factor `256`.

The anchor family should be aggregated before triangle inequality whenever possible. A proof paying `256` on every block is rejected unless it simultaneously proves that the resulting bound still closes the final finite ledger.

### Stage E — close shallow blocks physically before invoking oscillation

Attempt exact support/cardinality/valuation closure first on every block where the active state family is already sparse enough.

Use, as applicable:

- one-incidence odd carry `|e(q)|<1`;
- finite hard-state thresholds;
- exact anchor count on that block;
- state divisibility forced by `rp_1p`;
- previously proved quadratic-energy/support compression.

Record the last diagonal that closes without analytic oscillation.

### Stage F — one final Cauchy/reciprocal analysis on the surviving deep blocks

Only after Stages A–E, apply Cauchy/duality to the blocks that remain load-bearing.

The existing ratio-`6/5` reciprocal lemma may be used:

\[
\left|\sum_{M<m\le(6/5)M}e(t/m)\right|
\le15\sqrt{t/M},
\]

but only on blocks where its frequency-window hypotheses are proved.

Required aggregation discipline:

1. group arithmetic frequencies before paying the constant `15`;
2. do not pay `15` independently for every `(n_1,n_2,h_1,h_2)` tuple;
3. use exact fixed-frequency multiplicity or an `L^2` square-sum bound;
4. exploit the anchor automaton before Cauchy so the anchor label count enters as a true active multiplicity, not `256`;
5. preserve monotone prime endpoints by Abel/partial summation or a nested-domain Cauchy lemma instead of rectangularizing with a large worst-case factor.

If the reciprocal lemma is illegal on a block, use a certified finite/support fallback. Do not silently extrapolate its window.

### Stage G — rebuild the finite ledger from native finite quantities

Do **not** use the old formal `0.00179 L` source-main budget as the authoritative final criterion.

Reconstruct the final one-sided weighted ledger from the current finite decomposition, including:

- Bonferroni-7 base rough lower bound;
- exact least-shell + one-pair pointwise credit;
- complete `j>=2` bound `<0.00012 L23`;
- the new `j=1` bound;
- terminal `T4`, converted exactly to `L23` normalization;
- exact P(23) discarded-tail loss;
- any remaining explicit prime-sum normalization actually used.

The source-decimal `G_c>0.076` packet may be used as a diagnostic/cross-check, but it must not be substituted for a missing finite term.

### Stage H — uniformity above the splice

A certificate only at the single integer `K0` is insufficient for analytic overlap.

Prove either:

1. every normalized upper-error term is nonincreasing and every required lower contribution is nondecreasing/safely bounded for all `W>=W0`; or
2. a direct explicit inequality valid uniformly for all `W>=W0`.

Every threshold that is checked only at `W0` must carry a proved monotonicity direction.

---

## 6. Input whitelist / authority order

The following source files are the preferred exact packet at source head `1df79532...`.

1. `docs/P017_P2_C515_T12_LEAST_SHELL_BUDGET_CORRECTION_20260827.md` — **authoritative correction** for pointwise pair budget and residual three-prime witness.
2. `docs/P017_P2_C515_T12_HIGH_VALUATION_DIRECT_COLLAPSE_20260827.md` — closes every `j>=2` shell.
3. `docs/P017_P2_C515_BASE_ODD_BONFERRONI7_20260827.md` — fully finite base rough count.
4. `docs/P017_P2_C515_T12_SUBROOT_ROSSER_CANONICAL_CARRIER_20260827.md` — use only the retained exact beta-2 activation / unique top-two / monotone-cutoff facts consistent with the correction note.
5. `docs/P017_P2_C515_T12_LOPAIR_CANONICAL_SUFFIX_FACTORIZATION_20260827.md` — unique hard long/short split.
6. `docs/P017_P2_C515_T12_P23_ANCHOR_AFTER_SUFFIX_FACTORIZATION_20260827.md` — preferred P(23) prestrip and exact anchored interval.
7. `docs/P017_P2_C515_P23_ANCHOR_ACTIVATION_AUTOMATON_20260827.md` — exact anchor activation thresholds.
8. `docs/P017_P2_FULL_BASIN_C515_TERMINAL_T4_CERTIFICATE_20260826.md` — closed terminal term.
9. `docs/P017_P2_EXPLICIT_RECIPROCAL_SUM_LEMMA_20260825.md` — existing explicit reciprocal constant/window.
10. `docs/P017_P2_RESIDUAL_ENERGY_COMPRESSION_20260826.md` — optional physical-space energy fallback.

Triggered dependencies may be opened only when an exact formula from the listed packet points to them. If a triggered dependency conflicts with item 1, item 1 controls.

---

## 7. Forbidden moves

The following are task-breaking errors.

1. **Do not resurrect superseded pair-absorption claims.** In particular, do not assume two ordered pairs are uniformly absorbed.
2. **Do not charge T3 separately.** It is already part of the corrected least-prime-shell bookkeeping.
3. **Do not re-open `j>=2`.** Its complete contribution is already finite-closed.
4. **Do not replace Bonferroni-7 base counting by a generic fundamental lemma** unless a strict quantitative advantage is proved and all new finite constants are charged.
5. **Do not reintroduce generic well-factorable multiplicity** where the canonical hard suffix theorem already provides a unique split.
6. **Do not charge all 256 P(23) anchors uniformly** on shallow blocks; use the activation automaton.
7. **Do not reuse the old `0.00179 L` budget as a theorem.** Rebuild the final finite ledger.
8. **Do not use the source-decimal `G_c` as a finite main term.** It is diagnostic unless the source normalization is independently made finite.
9. **Do not treat a floating-point scan as proof.** Final inequalities must be exact integer/rational certificates or rigorous interval arithmetic with explicit tails.
10. **Do not claim a finite P2 theorem, all-K theorem, or Legendre theorem merely from a `K0` computation.** Uniformity above the splice and the external finite-splice premise remain separate proof obligations.
11. **Do not return to exponent-pair/B-spline optimization before exhausting the current canonical residual shell.** A route change requires an exact no-go from this task.

---

## 8. Mandatory validation

Every PASS/NO_GO return must include machine-checkable evidence.

Minimum checker split:

### Checker A — structural reindexing

Verify on the finite legal prime alphabets / state ranges:

- uniqueness or certified multiplicity of the three-prime residual-shell index;
- depth-four beta-2 support law;
- compatibility of the hard suffix split after conditioning;
- P(23) anchor activation threshold semantics.

### Checker B — normalized finite bound

Using exact `Fraction`/integer arithmetic where feasible, verify:

- each block/diagonal contribution;
- exact normalization to `L23`;
- no double counting of least-shell, one-pair credit, `j>=2`, or T4;
- final strict positivity or exact no-go deficit;
- monotonicity/uniformity certificate for all `W>=W0`.

Any numerical quadrature or prime-count inequality must include its explicit one-sided error enclosure.

---

## 9. Required deliverables after Driver approval

Recommended paths:

1. `docs/P017_P2_C515_J1_CANONICAL_SHELL_REINDEXING_20260828.md`
2. `experiments/p017_p2_c515_j1_shell_reindexing_20260828.py`
3. `docs/P017_P2_C515_J1_FINAL_AGGREGATION_20260828.md`
4. `experiments/p017_p2_c515_j1_final_aggregation_20260828.py`
5. `research_returns/P017_P2_C515_J1_LOWPAIR_FINAL_AGGREGATION_RETURN_20260828.md`

If the route returns `NO_GO_CURRENT_C515`, replace item 3 by an exact obstruction/no-go note rather than a vague negative report.

No source-current-state synchronization file should be rewritten until the final verdict is machine-checked.

---

## 10. Return contract

The return must state, in this order:

1. `VERDICT`: `PASS_FINITE_CLOSURE` or `NO_GO_CURRENT_C515`;
2. frozen source head actually used;
3. exact canonical shell index and its multiplicity;
4. exact active hard/anchor state counts by geometric diagonal/parity class;
5. last diagonal closed by physical/support methods;
6. analytic blocks remaining after physical closure;
7. explicit Cauchy/reciprocal constant ledger, if used;
8. final `L23`-normalized finite ledger;
9. uniformity argument for every `W>=W0`;
10. checker paths and PASS output;
11. all corrections/supersessions discovered during execution;
12. theorem-strength boundary and non-claims.

A return that merely says “constants look small enough” is invalid.

---

## 11. Successor gate prepared for Driver review

### unresolved_new_information_gap

Whether the unique remaining `j=1` low-pair residual carrier admits a canonical one-sided aggregation, compatible with three-prime conditioning, the finite depth-four Rosser core, unique c515 suffix factorization, monotone external-prime endpoint, and P(23) activation automaton, that fits inside the fully reconstructed finite weighted ledger.

### candidate_outcomes

1. `FINITE_CLOSURE`: exact aggregation closes the weighted finite lower bound uniformly above the Tier-A splice.
2. `STRUCTURAL_CLOSURE_BUT_NUMERICAL_DEFICIT`: the aggregation theorem is proved but its sharp certified cost exceeds the finite reserve; return the exact deficit and dominant block family.
3. `AGGREGATION_OBSTRUCTION`: a required injective/controlled-multiplicity reindexing or monotone-domain factorization fails; return a minimal exact obstruction/counterexample.

### kill_condition

Kill the current c515 aggregation route, without further B-spline/exponent-pair tuning, if an exact certificate proves that even the best legal one-sided `j=1` aggregation under the frozen kernel exceeds the available reconstructed finite positive ledger, or if a minimal structural obstruction proves that every legal aggregation necessarily incurs a multiplicity large enough to force that deficit.

### why_not_close_parent_task

The P017 analytic side is not complete: one load-bearing `j=1` residual carrier remains and no uniform finite positive weighted lower bound has yet been proved.

### why_not_reassign_existing_owner

The unresolved carrier is a direct continuation of the same c515 owner branch and depends on its exact corrected bookkeeping, finite support censuses, and anchor machinery. Reassignment would add synchronization risk without introducing a genuinely separate mother question.

### why_not_return_to_free_exploration

The information gap is already sharply identified and falsifiable. There is no discovery ambiguity requiring a new FREE Phase-A search; the next action is a deterministic proof/obstruction computation on a finite structured carrier.

---

## 12. Non-claims

This proposal does not claim:

- an official Driver-approved task;
- a completed finite `P2` theorem;
- `P2` in every consecutive-square basin;
- Legendre's conjecture;
- a canonical project promotion;
- that the external finite-splice premise has been independently proved here.

The taskbook becomes dispatchable only after explicit Driver review/promotion under the current Enterprise Math control-plane rules.
