# R059D Stage R — Global Complement / Observable Quotient / Absolute Anchor Identifiability

Task-ID: `RS-R059D-STAGE-R-BRC-GLOBAL-COMPLEMENT-OBSERVABLE-ANCHOR-IDENTIFIABILITY`
Generation: `R059D`
Status: `DRIVER_APPROVED_TASKBOOK`
Date: `2026-08-16`
Driver: `EM-DVR-R0457K / CONTROL_PLANE`
Researcher-ID: `EM-R059D-9C6B2A`
Owner branch: `research/r059d-stage-r-brc-global-complement-observable-anchor`
Frozen parent: `6e3e8334ab773a1b5710652da5dadc790fcf583a`

## 0. Frozen input discipline

Stage Q is accepted and immutable.

Read only the current project-definition/foundation files and frozen R059D Stage N/O/P/Q results needed for semantic typing. Do not modify any prior-stage artifact.

Project mode remains:

`REFOUND, NOT REJECT`.

Current frozen facts include:

- completion-neighbor two-state legality;
- pure algebra collapse `C=L+(U-L)b`, `b^2=b`;
- branch pair as a free `Z2` torsor under `b -> 1-b`;
- stateless symmetric unique selector no-go;
- straight continuation `b_(k+1)=b_k` after initialization;
- pairwise relative synchronization `b_u xor b_v=c_uv`;
- connected consistent component normal form `b_v=a xor p_v`;
- one exact singleton anchor synchronizes a connected component;
- global complement ambiguity remains without anchor;
- `eta=2q-L-U` is endpoint-reflection odd but is not by itself a selector.

Do not reinterpret Stage R as path search, endpoint argmax, reward maximization, nearest rounding, or ML fitting.

## 1. Core scientific question

Stage Q leaves one global `Z2` freedom per unanchored connected component.

Determine which of the following is mathematically correct for the BRC substrate:

### Route G — quotient/gauge-type route
All native/macroscopically admissible observables of the frozen problem factor through relative torsor invariants. Then global complement is observationally redundant within that declared observable algebra, and an absolute branch origin is unnecessary for those observables.

### Route A — anchor-identification route
At least one independently typed admissible observable changes under global complement. Then the two globally complementary assignments are mathematically distinguishable, and an exact post-credit mechanism must produce or import an independent singleton anchor.

Do not assume either route.

## 2. Stage R0 — freeze observable registry before scoring

Before evaluating complement behavior, freeze a finite registry of observable/certificate families. At minimum include:

1. `RELATIVE_XOR_OBSERVABLES`
   - `b_u xor b_v`
   - rooted parity differences
   - cycle parity
   - component satisfiability

2. `ABSOLUTE_COMPLETION_ENDPOINT_OBSERVABLES`
   - realized completed value `COLL_C(q;b)`
   - signed collapse residue `rho=q-COLL_C(q;b)`

3. `STRUCTURAL_MACRO_CERTIFICATES`
   - rank-one straightness on already initialized continuation
   - exact affine conservation identities
   - completion-layer membership/idempotence

4. `CONTEXTUAL_ANCHOR_CERTIFICATES`
   - independently declared singleton upstream constraints
   - preexisting donor/orientation relation with exact transformation law

5. `SCALAR_ORDER/COVARIANCE_CONTROLS`
   - order monotonicity candidate
   - endpoint reflection covariance
   - translation covariance
   - positive scale covariance
   - completion idempotence

The registry must be frozen before results are inspected. No adding a favorable observable after seeing outcomes.

## 3. Exact observable quotient theorem

For a connected unanchored relative component with global complement action

`Gamma(b)_v = b_v xor 1`,

define a declared observable algebra `O`.

Prove or refute:

`GLOBAL_COMPLEMENT_IS_OBSERVATIONALLY_REDUNDANT_RELATIVE_TO_O`

iff every `O in O` satisfies

`O(Gamma(b)) = O(b)`

for all exact feasible assignments.

If true only for a subalgebra, characterize the maximal frozen complement-invariant subalgebra generated by the registry.

Do not call this physical gauge equivalence unless all observables relevant to that physical claim have independently been shown to factor through the quotient.

## 4. Distinguishing-observable audit

For every registry family, classify:

- `COMPLEMENT_INVARIANT`
- `COMPLEMENT_COVARIANT_BUT_NOT_INVARIANT`
- `COMPLEMENT_DISTINGUISHING`
- `UNRESOLVED_TYPING`

For any distinguishing observable, determine whether it is:

- genuinely independent of the branch being selected;
- merely a relabeling/convention;
- circular because it is computed only after substituting the candidate branch;
- externally/boundary supplied;
- a legitimate downstream macro calibration.

A branch-conditioned quantity cannot become an initialization anchor merely because its two candidate values differ.

## 5. Exact post-credit anchor conversion

Let a legitimate macro certificate be encoded as exact equations

`F_r(C_1,...,C_m, h)=0`

with independently typed context `h`.

Reduce through completion-neighbor Boolean forms to the exact feasible set

`A={b: all P_r(b,h)=0}`.

Positive anchor result requires:

`|A|=1`

before using the realized branch itself.

Freeze one of:

- `EXACT_MACRO_CERTIFICATE_GENERATES_SINGLETON_ANCHOR`
- `MACRO_CERTIFICATE_ONLY_GENERATES_RELATIVE_CONSTRAINT`
- `MACRO_CERTIFICATE_IS_SYMMETRIC_NO_INFORMATION`
- `MACRO_CERTIFICATE_ANCHOR_IS_CIRCULAR`

Retain signed and higher-order finite-difference/Mobius credit, but no arbitrary scalar weighting.

## 6. Scalar 5 control — can midpoint collapse be derived rather than assumed?

Mandatory scalar control:

`L=4, U=9, q=5`, completion carrier `C=4+5b`.

Freeze before scoring the following axiom candidates as separate toggles, not one bundled assumption:

- A0 completion-neighbor legality only;
- A1 joint translation covariance of generic interval selector under transformed completion layer;
- A2 positive affine scale covariance;
- A3 endpoint-reflection equivariance;
- A4 deterministic single-valuedness away from exact unresolved symmetry;
- A5 order monotonicity: increasing `q` inside a fixed gap may not switch `upper -> lower` after already switching `lower -> upper`;
- A6 local constancy away from selector transition points / finite number of transitions per bounded gap;
- A7 completion idempotence at `L,U`;
- A8 cross-gap/refinement compatibility, if an exact formulation can be declared without using metric distance.

Study the selector family under each cumulative/alternative subset.

Critical target:

Determine whether midpoint-threshold collapse

`q < (L+U)/2 -> L`
`q > (L+U)/2 -> U`

is:

- uniquely derived from a clearly stated minimal axiom package;
- only one member of a larger admissible selector family;
- dependent on an extra order/continuity axiom;
- or incompatible with some frozen BRC semantics.

If midpoint threshold emerges, explicitly state which axiom is doing the nontrivial work. Do not disguise it as a theorem of completion-neighbor legality alone.

Do not use Euclidean distance-to-endpoint language. Algebraic midpoint/order statements are allowed.

For `q=5`, report whether the derived package truly forces `4`, leaves `{4,9}`, or depends on a non-native calibration axiom.

## 7. Centered-gap carrier audit

Continue exact analysis of

`eta=2q-L-U`.

Prove/refute:

- whether `eta` plus a declared orientation of the branch torsor can define a covariant selector;
- whether choosing the torsor identification `eta-sign <-> branch` is extra structure;
- whether any exact macro certificate can independently fix that identification;
- whether `eta=0` necessarily remains multibranch under all symmetry-preserving exact rules.

No `sign(eta)` selector is allowed unless the identification/orientation is independently established.

## 8. Cross-scale anchor propagation

Construct exact finite examples where a single independently justified coarse/fine anchor propagates through relative XOR constraints.

Must include:

- one tree with fine anchor -> coarse synchronization;
- one tree with coarse anchor -> fine synchronization;
- one disconnected control where only anchored components become absolute;
- one contradictory multi-anchor control;
- one cyclic parity consistency control.

Distinguish:

`ANCHOR_SOURCE`
from
`ANCHOR_PROPAGATION`.

Stage Q already solved propagation. Stage R must not count propagation itself as evidence for where the anchor came from.

## 9. Straightness boundary

Replay straightness only as a relative certificate:

`b_(k+1) xor b_k=0`.

Do not use rank-one straightness to choose the global component origin.

If a separate anchor exists, show how straightness propagates it. Without an anchor, retain both globally complementary straight realizations.

## 10. Generalization / failure conditions

Test what breaks when:

- completion carriers have more than two legal endpoints;
- relative constraints are nonlinear Boolean relations rather than XOR;
- multiple independent torsor components interact;
- cyclic networks require holonomy/cycle summaries;
- observable registry includes absolute endpoint-valued quantities.

Pairwise-XOR results remain scoped; do not silently universalize them.

## 11. Large-background and covariance gate

Use exact symbolic/O(1) calculations for backgrounds near `10^36` and several completion scales.

Preserve:

- coordinate permutation covariance;
- global inversion covariance where typed;
- joint affine transformation of state and completion layer;
- branch torsor action.

Large scale is not length, probability, selector confidence, or metric magnitude.

## 12. Hard negatives

Do not use as positive proof:

- nearest rounding;
- Euclidean distance to endpoints;
- arbitrary threshold inserted post hoc;
- endpoint argmax;
- arbitrary reward weights;
- ML fitting;
- random tie-break;
- hidden coordinate/label ordering;
- branch-conditioned residue as its own anchor;
- physical probability from symmetry;
- physical gauge equivalence without an observable-factorization theorem.

## 13. Required artifacts

At minimum:

1. `R059D_STAGE_R_OBSERVABLE_REGISTRY.json`
2. `R059D_STAGE_R_GLOBAL_COMPLEMENT_OBSERVABLE_QUOTIENT.json`
3. `R059D_STAGE_R_DISTINGUISHING_OBSERVABLE_LEDGER.json`
4. `R059D_STAGE_R_EXACT_POST_CREDIT_ANCHOR_PROTOCOL.json`
5. `R059D_STAGE_R_SCALAR_SELECTOR_AXIOM_REGISTRY.json`
6. `R059D_STAGE_R_SCALAR_MIDPOINT_SELECTOR_THEOREM_OR_COUNTERCLASS.json`
7. `R059D_STAGE_R_ETA_ORIENTATION_IDENTIFICATION_AUDIT.json`
8. `R059D_STAGE_R_CROSS_SCALE_ANCHOR_PROPAGATION.json`
9. `R059D_STAGE_R_STRAIGHTNESS_RELATIVE_CONTROL.json`
10. `R059D_STAGE_R_GENERALIZATION_FAILURE_LEDGER.json`
11. `R059D_STAGE_R_COVARIANCE_LARGE_BACKGROUND.json`
12. deterministic checker source/output
13. report
14. artifact manifest
15. frozen checkpoint

## 14. Allowed final dispositions

Positive/negative freezes may include:

- `GLOBAL_COMPLEMENT_QUOTIENT_SUFFICIENT_FOR_DECLARED_OBSERVABLE_ALGEBRA`
- `GLOBAL_COMPLEMENT_DISTINGUISHABLE_BY_ADMISSIBLE_OBSERVABLE`
- `ABSOLUTE_ANCHOR_REQUIRED_FOR_ENDPOINT_VALUED_OBSERVABLES`
- `EXACT_MACRO_CERTIFICATE_GENERATES_SINGLETON_ANCHOR`
- `MIDPOINT_SELECTOR_DERIVED_FROM_EXPLICIT_AXIOM_PACKAGE`
- `MIDPOINT_SELECTOR_NOT_DERIVABLE_FROM_BARE_COMPLETION`
- `ETA_ORIENTATION_IDENTIFICATION_REQUIRES_EXTRA_STRUCTURE`
- `ABSOLUTE_SELECTOR_REMAINS_CONTEXTUAL`
- `GLOBAL_COMPLEMENT_PHYSICAL_GAUGE_NOT_ESTABLISHED`

Negative results are first-class.

After all artifacts/checker are frozen:

`STOP_FOR_DRIVER_REVIEW`.
