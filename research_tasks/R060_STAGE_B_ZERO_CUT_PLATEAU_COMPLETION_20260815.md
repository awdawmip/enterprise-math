# RS-R060 — STAGE B ZERO-CUT PLATEAU COMPLETION / QUOTIENT-STABILITY TEST

Task-ID: `RS-R060-STAGE-B-ZERO-CUT-PLATEAU-COMPLETION`
Generation: `R060`
Stage: `B`
Status: `DRIVER_APPROVED_TASKBOOK`
Identity-policy: `AUTO_RESOLVE_OR_ALLOCATE`
Identity-lane: `R060`
Date: `2026-08-15`
Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`

## 0. Frozen parent and scientific disposition

Stage A is accepted at owner head:

`2f5e7a63531a03fc7fecac0c32b35b6e96617343`

Stage-A checkpoint SHA256:

`702640598186e46579887f4b53b1abea304c8dc0fdca21364af6c18444915535`

Frozen Stage-A result:

`RANK3_D3_L1_STRONG_STABILITY_FAILS_EXACTLY`.

Exact first failures:

- S2 local perturbation robustness first fails at `N=4`;
- S1 single-initial confluence first fails at `N=6`;
- supported mechanisms include `BRANCH_ORDER_SENSITIVITY`, `MULTIPLE_STRICT_LOCAL_MINIMA`, `PERTURBATION_CHANGES_BASIN`, and `CONTACT_CUT_DEGENERACY`.

Stage 0 and Stage A artifacts are immutable. Do not alter or reinterpret the counterexamples away.

This failure is only for the frozen `C3 + D3_L1 + P1` model. It is not `RANK3_IMPOSSIBLE`, `3D_IMPOSSIBLE`, or a world-dimension conclusion.

## 1. Stage-B question

The minimal S2 counterexample is an equal-`B` adjacent relocation between two inequivalent D3_L1 terminal states, and the minimal S1 counterexample ends in two inequivalent terminal states with the same final `B`.

Therefore Stage B asks one narrow question:

> Is the Stage-A instability caused only by treating equal-`CONTACT_CUT_COUNT` relational rearrangements as frozen terminal distinctions?

Test the smallest symmetry-respecting completion that does not introduce a new scalar, metric, geometry, tie-breaker, or cooperative multi-packet move:

**close all legal one-packet `ΔB=0` relocations into finite B-level plateau components, then run strict `B` descent between plateau components.**

This is a quotient-stability test. It does not erase the already-proved raw-state instability.

## 2. Frozen objects — no repair by redefinition

Keep exactly frozen:

- carrier `C3_RELATIONAL_RANK3_CHANNEL_CARRIER`;
- `UNIT_PACKET(x)=1`;
- fixed finite connected `N`-packet states;
- full carrier automorphism group and canonicalization;
- `B(C)=CONTACT_CUT_COUNT`;
- one-packet adjacent relocation proposal;
- connectivity requirement;
- original P1 perturbation neighborhood from Stage 0;
- Stage-A S1/S2/S3 definitions as raw-state results.

Do not add a second energy/readout in this stage.

## 3. Zero-cut relocation relation

For admissible states `C,C'` of the same `N`, define a raw zero-cut relocation step:

`C ~0 C'`

iff `C'` is obtained from `C` by one occupied-to-adjacent-empty relocation, both states are connected, and

`B(C') = B(C)`.

Prove explicitly that `~0` is reversible on admissible states.

Then define `PLATEAU(C)` as the equivalence closure generated by:

- carrier automorphism equivalence; and
- finite sequences of `~0` steps.

The primary implementation may work on frozen automorphism orbits if it proves the quotient is exact and no raw zero-step connection is lost.

Every plateau has one exact integer `B` value.

## 4. Plateau macro-dynamics

Define a strict macro transition

`P -> P'`

between distinct plateau components iff some representative `C in P` has a legal frozen D3_L1 strict descent `C -> D` with `D in P'`.

No uphill move is allowed.

Because every macro edge strictly decreases integer `B`, prove the plateau macro-dynamics is acyclic and terminates.

A plateau is `PLATEAU_TERMINAL` iff no representative has any strict `B`-decreasing D3_L1 exit.

Do not call a non-singleton terminal plateau a unique raw shape.

## 5. Plateau terminal-set semantics

For any initial state `C`, define

`TP(C)`

as the set of terminal plateau components reachable from `PLATEAU(C)` through all strict macro branches.

Compute all branches exactly; no greedy path, hidden tie-breaker, or single representative is allowed.

Define:

### PS1 — PLATEAU SINGLE-INITIAL CONFLUENCE

`|TP(C)|=1`.

### PS2 — PLATEAU LOCAL-PERTURBATION ROBUSTNESS

`PS1(C)` holds and for every `C1 in P1_ADJACENT_RELOCATION(C)`,

`TP(C1)=TP(C)`.

P1 remains the frozen Stage-0 perturbation neighborhood and does **not** require `ΔB<0` or `ΔB=0`.

## 6. Raw-state vs quotient-state truth must remain separate

Freeze both levels simultaneously:

- raw Stage-A S1/S2 failures remain exact and immutable;
- Stage B may only ask whether a plateau quotient repairs stability at a weaker output semantics.

If a terminal plateau contains more than one raw automorphism orbit, label it explicitly as a degenerate plateau class.

Forbidden inference:

`PS1 PASS => raw S1 PASS`.

Forbidden inference:

`PS2 PASS => raw S2 PASS`.

A bounded plateau success may be reported only as a quotient-level candidate.

## 7. Mandatory exact tests before any range expansion

Use the complete frozen `N<=6` Stage-A registry first.

### 7.1 Minimal S2 pair

For Stage-A minimal S2 counterexample:

- `N04-O0002`, `B=18`;
- `N04-O0003`, `B=18`;
- one allowed equal-B P1 relocation connects them.

Prove whether they lie in the same exact plateau component.

Expected diagnostic, not premise:

`S2_MINIMAL_PAIR_MERGES_UNDER_PLATEAU`.

### 7.2 Minimal S1 endpoints

For Stage-A minimal S1 counterexample from `N06-O0006`, endpoints:

- `N06-O0005`, `B=24`;
- `N06-O0014`, `B=24`.

Determine exactly whether these endpoints lie in the same plateau component.

Do not assume they do merely because `B` agrees.

### 7.3 Complete N<=6 plateau atlas

For every frozen state orbit `N=1..6`:

- enumerate exact zero-cut plateau component;
- enumerate exact strict macro exits;
- compute exact `TP(C)`;
- classify PS1;
- classify PS2 under the frozen P1 neighborhood;
- record plateau sizes in raw automorphism-orbit count;
- record whether terminal plateaus are singleton or degenerate.

## 8. Stop rule / no blind expansion

Do not expand beyond `N=6` before the complete `N<=6` plateau result is frozen.

If PS1 or PS2 fails within `N<=6`, freeze the **minimal exact counterexample** and stop. Do not repair again in Stage B.

If both PS1 and PS2 pass for all frozen `N<=6`, return bounded success and stop for Driver review. Do not promote it to an all-N theorem.

No `N=7` expansion in this task unless a later Driver instruction explicitly authorizes it.

## 9. Interpretation discipline

Allowed outcomes:

- `PLATEAU_QUOTIENT_STRONG_STABILITY_FAILS_EXACTLY`;
- `PLATEAU_QUOTIENT_RESCUES_BOUNDED_N_LE_6_STRONG_STABILITY`;
- `PLATEAU_COMPLETION_SEMANTIC_HARD_STOP`.

None implies a world-dimension conclusion.

A plateau rescue would show only that equal-B relational mobility is sufficient to remove the known bounded failures at quotient level.

A plateau failure would show that Stage-A instability is deeper than equal-B freezing and should motivate a later, separately authorized cooperative-move or richer-relational-readout investigation.

## 10. Semantic firewall

Strictly forbidden:

- Euclidean distance/norm;
- center/centroid/radius;
- length/area/volume/surface-area interpretation;
- graph distance as score;
- shortest path/geodesic;
- curvature/angle;
- gravity/energy/physical metastability interpretation;
- arbitrary tie-breaker;
- deterministic choice inserted solely to force confluence;
- a new scalar potential/readout;
- multi-packet cooperative moves;
- uphill `B` moves;
- deleting or weakening Stage-A counterexamples;
- calling a non-singleton plateau a unique raw state;
- claiming rank-3 or world-3D from bounded success;
- claiming 3D impossibility from failure;
- modifying R059L.

## 11. Required artifacts

Freeze at least:

1. `R060_STAGE_B_ZERO_CUT_RELATION_PROTOCOL.json`
2. `R060_STAGE_B_PLATEAU_EQUIVALENCE_PROTOCOL.json`
3. `R060_STAGE_B_PLATEAU_COMPONENT_ATLAS.json`
4. `R060_STAGE_B_PLATEAU_MACRO_DAG.json`
5. `R060_STAGE_B_MINIMAL_S2_PAIR_PLATEAU_CERTIFICATE.json`
6. `R060_STAGE_B_MINIMAL_S1_ENDPOINT_PLATEAU_CERTIFICATE.json`
7. `R060_STAGE_B_PS1_ATLAS.json`
8. `R060_STAGE_B_PS2_ATLAS.json`
9. `R060_STAGE_B_TERMINAL_PLATEAU_LEDGER.json`
10. `R060_STAGE_B_SEMANTIC_CLAIM_LEDGER.json`
11. deterministic Stage-B checker output
12. `R060_STAGE_B_PLATEAU_COMPLETION_CHECKPOINT.json`

If failure exists, additionally freeze minimal PS1/PS2 counterexample artifacts as applicable.

## 12. Checker hard negatives

The checker must reject at least:

- `PLATEAU_EQUIVALENCE_ERASES_STAGE_A_RAW_FAILURE`;
- `PS1_PROMOTED_TO_RAW_S1`;
- `PS2_PROMOTED_TO_RAW_S2`;
- `NON_SINGLETON_PLATEAU_CALLED_UNIQUE_SHAPE`;
- `ZERO_MOVE_GREEDY_REPRESENTATIVE_ONLY`;
- `UNEXPLORED_ZERO_MOVE_BRANCH_DROPPED`;
- `UNEXPLORED_STRICT_MACRO_BRANCH_DROPPED`;
- `UPHILL_B_MOVE_ALLOWED`;
- `NEW_SCALAR_ADDED`;
- `TIE_BREAKER_ADDED_TO_FORCE_CONFLUENCE`;
- `COOPERATIVE_MULTI_PACKET_MOVE_ADDED`;
- `P1_PERTURBATION_REQUIRES_DESCENT`;
- `BOUNDED_PS1_PASS_PROMOTED_TO_ALL_N`;
- `BOUNDED_PS2_PASS_PROMOTED_TO_ALL_N`;
- `RANK3_RESULT_PROMOTED_TO_WORLD_DIMENSION`;
- inherited Stage-0/Stage-A geometry and R059L firewalls.

## 13. Required return

Return:

- Researcher-ID;
- frozen parent anchors;
- exact zero-cut relation and reversibility proof;
- plateau-equivalence definition;
- macro-dynamics and termination proof;
- exact N=4 minimal S2-pair plateau disposition;
- exact N=6 minimal S1-endpoint plateau disposition;
- complete `N<=6` PS1/PS2 atlas summary;
- terminal plateau singleton/degeneracy summary;
- checker result;
- artifact hashes;
- checkpoint SHA256;
- owner head / Draft PR if published;
- one permitted scientific disposition from Section 9.

Then stop for Driver review.
