# HODGE Stage H0C — Algebraically Sourced Signed-Incidence Assembly

Date: `2026-08-17`
Status: `ACTIVE / DRIVER-ISSUED TASKBOOK`
Driver: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`
Task ID: `RS-HODGE-H0C-ALGEBRAIC-SIGNED-INCIDENCE-ASSEMBLY`
identity_policy: `AUTO_RESOLVE_OR_ALLOCATE`
identity_lane: `HODGE-H0C-SIGNED-INCIDENCE`
owner branch: `research/hodge-h0c-algebraic-signed-incidence-assembly`
control branch: `research/hodge-special-control-plane`

## 0. Driver acceptance of H0B

H0B is accepted as a valid negative qualification result.

Frozen H0B owner head:

`d93d050542dac6074ec7343e534b3854811ff7fd`

Frozen disposition:

`H0B_R2_PRIOR_ART_CONTROLS_ONLY`

Hard target result:

`ENTERPRISE_OPERATIONAL_REALIZATION_REACHES_R2 = NOT_ESTABLISHED`

H0B showed:

1. Boolean BRC/recoalescence is exact but too weak for the selected algebraic local-to-global benchmark; it gives execution correctness without strict V2 proof leverage.
2. Future-safe quotienting gives a genuine exact `DEPENDENCY_REDUCTION` control, but the mechanism is prior-art behavioral/kernel/partition mathematics and receives zero Enterprise-specific R2 credit.
3. Composition-safe repair is exact but on the honest minimal surrogate performs `4 -> 4`; no strict simplification was established.
4. A3 weighted relation state supplies signed integer structure but loses overlap-incidence identity when generated only from capacities/local totals; identical weighted fields can coexist with different assembly truth values.

The common missing object exposed by H0B is:

`ALGEBRAICALLY_SOURCED_SIGNED_INCIDENCE_RELATION_CARRIER_WITH_EXACT_ASSEMBLY_COMPARISON`

H0C attacks exactly that missing object.

H0B published Researcher-ID `EM-HODGE-H0B-6A91D3`. Preserve that provenance. H0C must resolve/use a fresh H0C identity.

---

## 1. Mission

The single hard target is:

`ALGEBRAIC_SIGNED_INCIDENCE_ENTERPRISE_REALIZATION_REACHES_R2`

where R2 is Criterion V2:

`R2_NONTRIVIAL_OPERATIONAL_REALIZATION`.

The preferred stronger outcome is an R3 seed:

`ALGEBRAIC_SIGNED_INCIDENCE_HODGE_R3_SEED`.

This stage must determine whether algebraic overlap/incidence data can generate an Enterprise carrier that simultaneously preserves:

- overlap/support identity;
- signed/integer or rationally extendable relation information;
- exact composition/recoalescence/assembly semantics;
- enough structure to obtain a strict non-tautological `PROOF_LEVERAGE_CERTIFICATE`.

H0C must not merely combine an incidence matrix with integer weights and rename the result Enterprise.

H0C does **not** prove Hodge, does **not** start full H1 cohomology, and does **not** assume a classical Cech/simplicial boundary operator as an Enterprise generator.

---

## 2. Frozen authority / minimal startup packet

Read only the smallest sufficient packet, then work.

Mandatory:

1. `AGENTS.md`
2. `docs/GITHUB_INTERACTION_BUDGET.md`
3. `research_common_surface.json`
4. `driver_handoffs/HODGE_SPECIAL_DRIVER_HANDOFF_20260817.md`
5. `driver_handoffs/HODGE_SPECIAL_DRIVER_PI_GEOMETRY_ADDENDUM_20260817.md`
6. H0A0 frozen Criterion V2 artifacts at `f4e6cf84eb191e0b7442913e018e1f6347e9584e`:
   - `research_results/HODGE_H0A0_REALIZATION_CLASSIFIER_V2.json`
   - `research_results/HODGE_H0A0_PROOF_LEVERAGE_CERTIFICATE_SPEC.json`
   - `research_results/HODGE_H0A0_HODGE_LEVERAGE_INTERFACE.json`
7. H0B frozen artifacts at `d93d050542dac6074ec7343e534b3854811ff7fd`:
   - `research_results/HODGE_H0B_CLASSIFICATION.json`
   - `research_results/HODGE_H0B_ENTERPRISE_CANDIDATE_REGISTRY.json`
   - `research_results/HODGE_H0B_PROOF_LEVERAGE_CERTIFICATE_REGISTRY.json`
   - `research_results/HODGE_H0B_COMPARISON_THEOREM_REGISTRY.json`
   - `research_results/HODGE_H0B_HODGE_R3_PREINTERFACE.json`
   - `research_results/HODGE_H0B_SEMANTIC_CHECKPOINT.md`
8. Exact A3/A4 source files only as consumed, including:
   - `src/enterprise_math/weighted_relation_field.py`
   - `src/enterprise_math/a3_a4_support_bridge.py`
   - canonical A4/BRC source when used.
9. `native_semantics_admissibility.json`.

Do not recursively traverse the repository.

---

## 3. Criterion V2 remains frozen

Do not relax or replace Criterion V2.

Information non-factorization is neither necessary nor sufficient for R2.

Every claimed R2 candidate must have:

- exact typed source/comparison object;
- independently generated Enterprise operational object;
- exact comparison/correctness theorem;
- non-tautology audit;
- strict V2 leverage certificate;
- claim-scope naturality/functoriality;
- target-leakage PASS;
- prior-art/novelty status.

A carrier that simply stores all source data without simplifying any theorem-critical obligation remains R1 even if it is lossless and beautifully structured.

---

## 4. Required source problem

Use `ALGEBRAIC_LOCAL_TO_GLOBAL_ASSEMBLY` as the benchmark family, but strengthen H0B's finite overlap surrogate so that incidence identity is explicit.

At minimum declare:

- a finite family of algebraic-style local patches/objects `U_i`;
- local state data on each `U_i`;
- explicit overlap objects/relations `U_i ∩ U_j` or the strongest exact finite surrogate;
- compatibility/incidence identity for each overlap;
- at least one consistent assembly instance;
- at least one inconsistent instance;
- at least one pair of instances that agree on all local totals/capacities but differ in overlap incidence, reproducing the H0B D-counterexample boundary.

The finite surrogate is not a theorem about schemes, sheaves, Cech descent, or Hodge classes.

Use a small algebraic example such as the standard two-chart cover of `P^1` only as presentation/naturality stress if needed. Do not import its known Hodge answer.

---

## 5. Required carrier search: support identity + signed relation state

Attempt to construct an Enterprise carrier from two frozen ownership layers:

1. A4-style exact support/incidence identity;
2. A3-style signed integer relation state.

The carrier may be a relation-indexed signed state, typed correspondence, enriched relation, or another exact object, but it must preserve both dimensions separately enough to avoid H0B's information loss.

A candidate schematic form such as

`edge/support identity + signed relation entry`

is permitted as a starting point, but earns no result by itself.

The stage must specify:

- exact carrier set/type;
- exact source-to-carrier generation rule;
- exact role of support identity;
- exact role of sign/integer/rational coefficient;
- exact reversal law;
- exact composition/recoalescence law;
- exact collapse/coarsening law if claimed;
- what data is lost or retained.

### 5.1 Orientation/sign rule

Do not choose arbitrary signs and call them canonical.

If oriented pairs are used only as storage coordinates, prove the transformation law under reversal, e.g. antisymmetry or the correct typed analogue.

A theorem-critical result must be invariant/equivariant under the declared relabeling/orientation convention.

### 5.2 Rational coefficient boundary

Classical Hodge uses rational coefficients.

Enterprise integer-first structure may be useful, but H0C must not silently replace the rational Hodge conjecture by an integral one.

If the carrier is initially integral, provide a typed extension/interface to rational coefficients or state precisely why that question is deferred.

No integral-Hodge claim is allowed.

---

## 6. Algebraic sourcing gate

The hardest gate is not storage; it is source generation.

Produce an exact theorem or strongest admissible finite statement of the form:

`declared algebraic local/overlap data -> Enterprise signed-incidence carrier`.

The generation rule must not use:

- known algebraic cycle representatives;
- known Hodge decomposition answers;
- classical Cech cocycle truth values;
- a classical boundary matrix copied verbatim and renamed Enterprise;
- harmonic/Kahler data;
- Euclidean geometry/circle/pi.

If the only way to create the signed carrier is to first construct a classical Cech/simplicial chain complex and then translate it, classify:

`PRIOR_ART_TRANSPORT_ONLY / NO_ENTERPRISE_R2_CREDIT`.

If support identity can be sourced algebraically but signed weights cannot, freeze the exact missing signed-source object.

If signed weights can be sourced but support/incidence identity cannot, freeze the H0B information-loss boundary again rather than fabricating identity.

---

## 7. Exact assembly comparison gate

The candidate carrier must be tested against a theorem-critical assembly predicate.

At minimum seek an exact statement on the declared finite carrier:

`SOURCE_ASSEMBLY_PREDICATE <-> ENTERPRISE_OPERATIONAL_CONSTRAINT`

or the strongest correctly one-sided theorem.

The Enterprise constraint must be defined independently before comparison.

Required negative control:

Two source systems with the same local totals/capacities but different incidence must not collapse to the same carrier if their assembly truth differs, unless the candidate explicitly admits incompleteness and remains below R2.

Required soundness/completeness audit:

- false positive assembly;
- false negative assembly;
- incidence deletion;
- incidence relabeling;
- orientation reversal;
- duplicated/redundant local state.

Finite exhaustive checking proves only the declared finite carrier unless accompanied by theorem.

---

## 8. Strict proof-leverage gate

Exact representation/comparison is not enough.

A successful R2 must produce at least one strict V2 `PROOF_LEVERAGE_CERTIFICATE` from an accepted class, preferably:

- `FINITE_OBSTRUCTION_BASIS`;
- `LOCALIZATION`;
- `COMPOSITIONAL_FACTORING`;
- `DEPENDENCY_REDUCTION`;
- `INTEGRALITY_POSITIVITY_MONOTONICITY`;
- `NORMAL_FORM`.

The leverage witness must be declared before success evaluation.

Examples of admissible strict witnesses include:

- raw global assembly search reduced to a smaller exact obstruction set;
- exact branch/path family reduced to a signed obstruction basis;
- a local signed closure law plus a proved assembly theorem replacing global search;
- a strict carrier/dependency reduction while preserving the complete declared assembly language.

The following do **not** count:

- storing the source overlap relation unchanged;
- adding signed weights without reducing any theorem obligation;
- rewriting an incidence matrix in different notation;
- applying generic linear algebra with no Enterprise-specific operational role;
- selecting a success metric after seeing the result.

---

## 9. A3/A4 bridge-specific test

The frozen A3/A4 bridge already generates A4 supports from A3 weighted fields using radius thresholds and records a one-way coarse-support compatibility because signed cancellation can make the converse fail.

H0C must audit whether that existing bridge can contribute to the signed-incidence carrier without being misused.

At minimum test:

1. zero-relation classes;
2. generated support identity;
3. signed field antisymmetry and weighted three-block closure;
4. partition/coarsening behavior;
5. cancellation under aggregation;
6. whether preserving the support relation alongside signed field entries repairs H0B's assembly incompleteness;
7. whether any resulting repair is strictly simpler than the raw source problem.

If the resulting carrier is merely `(raw support relation, raw weighted field)`, classify it at most R1 unless a strict leverage theorem is proved.

---

## 10. Prior-art firewall

Build a compact prior-art matrix for any load-bearing candidate.

At minimum include as controls/neighbors:

- signed incidence matrices;
- graph boundary/coboundary operators;
- simplicial/cellular chain complexes;
- Cech cocycles/descent;
- weighted relations / weighted automata / semiring path sums as relevant;
- standard homological algebra if invoked.

These may validate mathematics or expose no-go boundaries, but receive zero Enterprise novelty credit.

Do not claim novelty merely from:

`support + integer weight`.

Enterprise-specific credit requires a project-native generation/comparison/leverage theorem not reducible to relabeling a standard construction.

---

## 11. Presentation/naturality subgate

The historical all-presentations H0A remains suspended as a mainline task.

Trigger presentation naturality only if the H0C candidate depends on choices such as:

- cover ordering;
- local chart;
- overlap orientation;
- patch labeling;
- chosen algebraic presentation.

At the claimed scope test:

- patch relabeling;
- orientation reversal;
- at least one algebraically equivalent presentation if a global algebraic claim is made;
- refinement/coarsening only if claimed.

A finite ordered-cover theorem may remain valid on that declared scope without proving full `PGL_2(C)` naturality.

Do not overclaim.

---

## 12. R3 seed interface

H0C may identify an R3 seed only if an Enterprise-specific R2 has already passed.

Then type, without solving Hodge:

`C_H(X) rational Hodge-class condition`

`-> candidate Enterprise signed-incidence state/constraint`

`-> local/integer/signed assembly obstruction`

`-> explicit algebraic-cycle/Chow lifting obligations`.

Required unresolved obligations must include as applicable:

- construction of the `C_H -> Enterprise` comparison;
- rational coefficient compatibility;
- cycle-class compatibility;
- descent/naturality;
- existence of algebraic cycle;
- lifting correctness.

Do not use known cycle answers as generator data.

R3 seed does not prove the lifting theorem.

---

## 13. Required candidate families

Study at least three structurally distinct variants:

### Candidate S1 — Support-indexed A3 signed field

Preserve explicit A4 overlap/support identity and attach A3 signed relation data to supported pairs.

Question: does this repair assembly completeness and create strict leverage, or merely store raw data?

### Candidate S2 — Signed recoalescence / cancellation carrier

Attempt an exact branch/recoalescence semantics where signed/integer contributions can cancel under a declared law.

Question: can exact global assembly/cancellation be computed from a smaller local obstruction basis?

Do not silently upgrade Boolean BRC; define the richer carrier explicitly.

### Candidate S3 — Quotient/coarsening-compatible signed incidence

Attempt a carrier compatible with Enterprise partition/collapse/coarsening laws.

Question: is there a strict assembly-preserving compression or normal form under coarsening?

If every safe repair reproduces the full raw incidence state, freeze an R1/no-reduction result.

Optional S4 may use another existing Enterprise primitive family only if it adds a genuinely distinct mechanism.

---

## 14. Required controls

Negative controls:

- raw incidence relation copied unchanged;
- arbitrary signed coloring of edges;
- incidence matrix relabeled Enterprise;
- support-only carrier that reproduces H0B's multiplicity/sign loss;
- signed-total-only carrier that reproduces H0B's incidence loss.

Positive prior-art controls:

- classical signed graph incidence/boundary theorem;
- one exact local-to-global finite obstruction theorem.

The classifier must award the controls zero Enterprise novelty credit.

---

## 15. Classification

Freeze exactly one strongest disposition:

### `H0C_R2_SIGNED_INCIDENCE_OPERATIONAL_REALIZATION_FOUND`

An Enterprise-specific algebraically sourced signed-incidence carrier passes exact assembly comparison and a strict V2 proof-leverage certificate.

### `H0C_R3_SIGNED_INCIDENCE_HODGE_SEED_FOUND`

R2 plus a typed non-tautological Hodge/lifting preinterface is explicit. No final lifting theorem is claimed.

### `H0C_R1_CARRIER_COMPLETE_BUT_NO_STRICT_LEVERAGE`

A correct signed-incidence carrier exists, but it merely retains/repackages the source or produces no strict theorem-critical simplification.

### `H0C_FAIL_ALGEBRAIC_SOURCE_GENERATION`

The desired carrier cannot be generated from the permitted algebraic/Enterprise inputs without importing forbidden classical structure.

### `H0C_PRIOR_ART_ONLY_NO_ENTERPRISE_CREDIT`

A mathematically successful carrier/leverage theorem is obtained, but it is fully prior-art transport/recovery with no Enterprise-specific operational result.

No other final disposition is permitted without Driver review.

---

## 16. H1 firewall

No automatic H1 start.

Even an R3 seed returns to Driver review.

H1 remains blocked unless Driver explicitly accepts a sufficiently typed R3 candidate as the basis for Enterprise chains/cochains.

Do not build full cohomology in H0C.

---

## 17. Required artifacts

At minimum produce:

1. `research_results/HODGE_H0C_ALGEBRAIC_OVERLAP_SOURCE_SPEC.json`
2. `research_results/HODGE_H0C_SIGNED_INCIDENCE_CARRIER_REGISTRY.json`
3. `research_results/HODGE_H0C_A3_A4_BRIDGE_AUDIT.json`
4. `research_results/HODGE_H0C_SOURCE_GENERATION_THEOREM_REGISTRY.json`
5. `research_results/HODGE_H0C_ASSEMBLY_COMPARISON_REGISTRY.json`
6. `research_results/HODGE_H0C_PROOF_LEVERAGE_CERTIFICATE_REGISTRY.json`
7. `research_results/HODGE_H0C_ORIENTATION_NATURALITY_LEDGER.json`
8. `research_results/HODGE_H0C_RATIONAL_COEFFICIENT_INTERFACE.json`
9. `research_results/HODGE_H0C_PRIOR_ART_NOVELTY_LEDGER.json`
10. `research_results/HODGE_H0C_TARGET_LEAKAGE_LEDGER.json`
11. `research_results/HODGE_H0C_HODGE_R3_PREINTERFACE.json`
12. `research_results/HODGE_H0C_CLASSIFICATION.json`
13. `research_results/HODGE_H0C_SEMANTIC_CHECKPOINT.md`
14. one deterministic checker + checker output
15. manifest with SHA-256 digests.

The checker must distinguish protocol PASS from mathematical R2/R3 success.

---

## 18. Mandatory checker gates

At minimum verify:

- Criterion V2 unchanged;
- no bare load-bearing `C(X)`;
- support identity and signed relation data are typed separately;
- source-to-carrier generation is explicit;
- arbitrary signed coloring fails;
- raw incidence copy earns no R2;
- H0B D-counterexample is repaired or explicitly remains a counterexample;
- exact assembly soundness/completeness status is recorded;
- any R2 has a strict leverage certificate;
- prior-art signed incidence/Cech/chain constructions receive zero Enterprise novelty credit;
- rational coefficient boundary is explicit;
- orientation/relabeling scope is explicit;
- target leakage passes;
- H1 remains blocked;
- no Hodge proof claim.

---

## 19. Completion estimates / advancement vector

Before H0C:

- Hodge conceptual route: `~28%`;
- Criterion V2: `FROZEN`;
- Enterprise-specific operational R2: `NOT FOUND`;
- signed/incidence carrier sourcing: `OPEN`;
- Enterprise cohomology: `0%`;
- algebraic lifting theorem: `0%`;
- unknown Hodge attack readiness: `0%`.

If H0C reaches Enterprise-specific R2:

- operational realization qualification: major gate passed on declared local-to-global scope;
- Hodge conceptual route: `~35%`;
- R3 search becomes the immediate next frontier.

If H0C reaches R3 seed:

- Driver may consider a narrowly scoped H1/Hodge-bridge stage, but no automatic start.

If H0C remains R1/fails source generation:

- preserve the exact no-go;
- do not weaken Criterion V2;
- next route must change primitive family or source theorem, not add arbitrary carrier decoration.

Advancement vector:

`signed-incidence-source +35 / A3-A4 bridge +30 / exact-assembly +35 / proof-leverage +30 / rational-interface +10 / presentation-naturality +10 / full-cohomology +0 / Hodge-proof +0`.

---

Driver return target:

`EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`

No automatic downstream stage.