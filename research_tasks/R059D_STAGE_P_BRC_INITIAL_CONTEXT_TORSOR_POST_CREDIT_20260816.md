# R059D Stage P — Initial Branch Context / Symmetry Torsor / Post-Credit Identification

Task-ID: `RS-R059D-STAGE-P-BRC-INITIAL-CONTEXT-TORSOR-POST-CREDIT`
Generation: `R059D`
Status: `DRIVER_APPROVED_TASKBOOK`
Date: `2026-08-16`
Driver: `EM-DVR-R0457K / CONTROL_PLANE`
Researcher-ID: `EM-R059D-9C6B2A`
Owner branch: `research/r059d-stage-p-brc-initial-context-torsor-post-credit`
Frozen parent: `c2ab05c7a101e1bebbe64306335731f7ecb35851`

## 0. Frozen Stage-O inputs

Stage O is immutable. Consume only the following established semantics/results:

- `ADJACENT_COMPLETED_STATE_GATE_IS_DEFINITIONAL_GIVEN_COMPLETION_LAYER`
- `COORDINATE_COMPLETION_Z_INHERITED_FROM_INTEGER_DISPLACEMENT_MODULE`
- `D6_EMERGES_FROM_AFFINE_PLUS_COMPLETION_NEIGHBOR_PRIMITIVE`
- `STATELESS_EXCHANGE_EQUIVARIANT_UNIQUE_BRC_SELECTOR_IMPOSSIBLE_AT_SYMMETRIC_STATE`
- `ONE_BIT_BRANCH_MEMORY_MINIMAL_FOR_EQUIVARIANT_STRAIGHT_CONTINUATION`
- `STRAIGHTNESS_ONE_BIT_MEMORY_CONTINUATION_LAW_ESTABLISHED_WITH_INITIAL_BRANCH_UNIDENTIFIED`
- `UNIQUE_CONTEXTUAL_SELECTOR_FAMILY_ON_EXPLICIT_CONTINUATION_DOMAIN`

Stage-O frozen head:

`c2ab05c7a101e1bebbe64306335731f7ecb35851`

Do not modify Stage J/K/L/M/N/O artifacts.

## 1. Governing project semantics

Read and obey:

- `PROJECT_DEFINITION.md`
- `PROJECT_DEFINITION.zh-CN.md`
- `project_definition.json`
- `FOUNDATIONAL_LOGIC.md`
- `foundational_logic.json`
- `native_semantics_admissibility.json`
- `GEOMETRIC_TOOL_REFOUNDATION_POLICY.md`
- `research_notes/BRC_PURE_ALGEBRA_COLLAPSE_CREDIT_COORDINATE_TRANSFER_ANCHOR_20260816.md`

Project mode remains:

`REFOUND, NOT REJECT`.

BRC is still a pure algebraic completion-neighbor collapse carrier first. Do not retype it as path merge, endpoint argmax, nearest rounding, reward maximization, probability sampling, or ML classifier.

## 2. Core scientific question

Stage O proved that at a local transverse-exchange-symmetric state `s=tau(s)` with branch action `b -> 1-b`, no deterministic stateless exchange-equivariant unique selector exists.

Therefore Stage P asks:

> What exact algebraic context can legitimately generate the **first** collapse bit `b_0`?

Do not assume a unique initial branch exists. The acceptable outcomes include:

- `UNIQUE_INITIAL_BRANCH_FROM_EXACT_CONTEXT`
- `INITIAL_MULTIBRANCH_IS_INTRINSIC_UNDER_SYMMETRY`
- `INITIAL_BRANCH_DEFINED_ONLY_RELATIVE_TO_ORIENTATION_CONTEXT`
- `INITIAL_SELECTOR_STILL_NONIDENTIFIED`

A negative theorem is a valid primary result.

## 3. Branch-pair as a Z2 torsor / free involutive pair

For the symmetric two-branch state, freeze only the algebra already proved:

`B={0,1}`

with exchange action

`tau_B(b)=1-b`.

Audit the mature algebraic description that this is a free transitive `Z2` action (a two-state torsor). If terminology is used, prove the exact finite statement and do not import geometric orientation semantics by name.

Required theorem/audit:

- `tau_B` has no fixed point;
- the action is transitive on `{0,1}`;
- choosing one branch canonically is equivalent to choosing a section/origin in this two-state torsor;
- no such choice can be exchange-equivariant at a tau-fixed base state without additional context.

Freeze only if proved:

`BRC_SYMMETRIC_BRANCH_PAIR_IS_FREE_Z2_TORSOR`

or an equivalent exact statement.

## 4. General contextual equivariance theorem

Introduce a context space `H` with involution `tau_H` and deterministic selector

`F : S x H -> {0,1}`.

At the local symmetric state `s=tau(s)`, require exact equivariance:

`F(s, tau_H(h)) = 1 - F(s,h)`.

Prove or refute the following:

### 4.1 Fixed-context no-go

If `h=tau_H(h)`, then no unique Boolean output can satisfy equivariance.

Freeze if proved:

`TAU_FIXED_CONTEXT_CANNOT_INITIALIZE_UNIQUE_BRANCH`

### 4.2 Minimal symmetry-breaking context

A context capable of unique branch selection must contain a tau-nonfixed distinction.

For finite contexts, determine the minimum cardinality required. Test whether a two-state free involutive context is minimum and sufficient.

Do not assume previous bit `h` exists at initialization.

## 5. Symmetry-invariant post-credit impossibility

This is a primary target.

Let a macro exact supervision/certificate `Phi` be invariant under transverse exchange:

`Phi(tau trajectory/state)=Phi(trajectory/state)`.

Suppose the two local initial branches are exchanged by `tau` and all other declared inputs are tau-symmetric.

Prove or refute:

> Any exact post-credit construction derived solely from tau-invariant supervision assigns exchange-related admissibility/credit structure to the two initial branches and therefore cannot uniquely identify one branch without an additional tau-odd context.

Do this in exact algebraic form, not probabilistically.

Possible formal carriers include:

- exact Boolean feasible sets;
- exact residual equations;
- exact discrete finite differences;
- exact multilinear/Mobius coefficients.

Do not use arbitrary magnitudes or weights.

If established, freeze:

`SYMMETRY_INVARIANT_POST_CREDIT_CANNOT_BREAK_INITIAL_BRANCH_SYMMETRY`

This theorem must explicitly explain why `STRAIGHT=rank-one` cannot choose the first branch: straightness is invariant under the exchange that swaps the two constant branch realizations.

## 6. Exact context-generated singleton criterion

Retain Stage-N/O set-valued legality:

`A(s,h) subseteq {0,1}`.

For exact higher constraints `F_r(C,h)=0`, substitute completion-neighbor binary forms and compute the exact Boolean feasible set.

Define:

- `A={0}` -> exact context forces branch 0;
- `A={1}` -> exact context forces branch 1;
- `A={0,1}` -> context does not initialize;
- `A=empty` -> inconsistent context.

Required theorem:

A unique initial branch is established only when the **full contextual exact solution set** is a singleton. No post-result tiebreaking is allowed.

Freeze if proved:

`INITIAL_BRC_SELECTION_EQUALS_EXACT_CONTEXTUAL_SINGLETON_WHEN_AVAILABLE`

## 7. Candidate tau-odd context audit

Audit candidate sources without promoting unsupported ones.

At minimum test:

1. **Previous collapse bit / previous donor relation**
   - valid for continuation;
   - unavailable for truly initial event unless a prior relation already exists.

2. **Exact upstream coupled-collapse constraint**
   - derive when it produces singleton `{0}` or `{1}`;
   - prove covariance under coordinate exchange/permutation.

3. **Signed collapse residue / finite-difference credit**
   - determine whether residue itself carries tau-odd information or whether symmetric paired residues merely exchange;
   - no absolute-value or nearest-endpoint ranking.

4. **Existing oriented algebraic relation**
   - e.g. a previously declared donor/recipient relation or non-symmetric exact boundary condition;
   - type it as context, not as hidden coordinate preference.

5. **Ingress/orientation state from earlier stages**
   - may be consumed only if an exact allowed transformation law is present in frozen inputs;
   - otherwise retain `NOT_PROMOTED`.

6. **External label order / coordinate naming**
   - must be rejected as non-equivariant unless explicitly declared as physical/mathematical context at a higher layer.

For each candidate record:

- context state space;
- tau action;
- whether tau-fixed states exist;
- whether the context can produce a unique branch;
- whether it merely re-encodes the branch bit;
- whether it is initial or continuation-only.

## 8. Relative-orientation hypothesis

Test the hypothesis that the initial branch is not an absolute scalar value but a branch **relative to an orientation/donor context**.

For a two-state free context `h in {0,1}` with `tau_H(h)=1-h`, classify all exact equivariant Boolean selectors `F(h)`.

Stage O found `{h,1-h}` under exchange equivariance. Stage P must interpret this structurally:

- Are these two functions merely the two choices of identification between context torsor and branch torsor?
- Is there any internal algebraic invariant that distinguishes `h` from `1-h` without importing a new origin?
- If not, freeze a non-canonicity theorem rather than choosing one.

Candidate freeze:

`INITIAL_BRANCH_IS_RELATIVE_TORSOR_IDENTIFICATION_NOT_ABSOLUTE_BIT`

only if rigorously established.

## 9. Post-credit from known mathematics: orientation-free vs oriented supervision

Construct exact tiny symbolic cases in two classes.

### 9.1 Orientation-free supervision

Examples must be invariant under transverse exchange, such as:

- straightness/rank-one continuation without externally declared orientation;
- symmetric closure identities;
- exchange-symmetric algebraic constraints.

Test that such supervision cannot initialize a unique branch.

### 9.2 Oriented/contextual supervision

Use exact non-symmetric algebraic context declared before scoring, for example:

- a fixed target donor relation;
- a fixed prior branch relation;
- an exact boundary equation whose Boolean reduction is singleton.

Show whether exact post-credit can then force a unique branch without arbitrary weights.

Important: do not use Euclidean distance-to-target, shortest path, angle, norm, visual line, or fitted coefficients.

## 10. Straight continuation replay

Replay Stage O exactly:

For fixed recipient with two Z-independent transfer vectors, rank-one straight continuation implies all branch bits equal.

Verify:

`b_{k+1}=b_k`.

Then place it in the new context theorem:

- after initialization, previous bit is a tau-nonfixed two-state context;
- the continuation selector is equivariant;
- it does not explain initialization.

Do not upgrade this to a universal scalar collapse selector such as the `5 -> 4/9` case.

## 11. Scalar `5 -> 4/9` control

Square completion layer:

`C_square={k^2:k>=0}`

for `q=5` gives legal endpoints `{4,9}`.

Stage P must explicitly test whether any context theorem from the vector two-branch problem transfers to this scalar case.

Required distinctions:

- completion-neighbor legality transfers;
- torsor structure of a two-element branch set transfers formally;
- transverse exchange action does **not** automatically exist in the scalar case;
- therefore vector symmetry no-go does not automatically prove scalar selector nonidentifiability.

Search for a genuine scalar involution/context if one is naturally present; otherwise freeze:

`SCALAR_5_INITIAL_SELECTOR_REMAINS_UNIDENTIFIED_BY_VECTOR_SYMMETRY_THEOREM`.

## 12. No hidden randomness / no probability

A randomized symmetric selector may be recorded as a mathematical control only.

Freeze:

`PHYSICAL_PROBABILITY_FROM_BRANCH_SYMMETRY = NOT_ESTABLISHED`.

Do not interpret 50/50 symmetry as physical probability.

## 13. Large-background / relabeling covariance

Test exact symbolic covariance for:

- affine sheet backgrounds near `10^36`;
- coordinate permutations;
- transverse exchange;
- global inversion;
- completion-layer affine transforms where the layer is transformed with the state.

Context laws must transform with context. A fixed hidden label preference is a failure.

## 14. Required artifacts

At minimum freeze:

1. `R059D_STAGE_P_SYMMETRIC_BRANCH_TORSOR_PROTOCOL.json`
2. `R059D_STAGE_P_CONTEXTUAL_EQUIVARIANCE_THEOREM.json`
3. `R059D_STAGE_P_TAU_FIXED_CONTEXT_NOGO.json`
4. `R059D_STAGE_P_SYMMETRY_INVARIANT_POST_CREDIT_NOGO.json`
5. `R059D_STAGE_P_EXACT_CONTEXTUAL_SINGLETON_PROTOCOL.json`
6. `R059D_STAGE_P_TAU_ODD_CONTEXT_CANDIDATE_LEDGER.json`
7. `R059D_STAGE_P_RELATIVE_ORIENTATION_SELECTOR_AUDIT.json`
8. `R059D_STAGE_P_ORIENTATION_FREE_VS_ORIENTED_SUPERVISION.json`
9. `R059D_STAGE_P_STRAIGHT_CONTINUATION_REPLAY.json`
10. `R059D_STAGE_P_SCALAR_5_CONTROL.json`
11. `R059D_STAGE_P_COVARIANCE_REGISTRY.json`
12. `R059D_STAGE_P_TRIVIALITY_LEAKAGE_LEDGER.json`
13. deterministic checker + output
14. report
15. artifact manifest
16. frozen checkpoint

## 15. Hard firewalls

Forbidden as positive proof:

- nearest rounding;
- endpoint argmax;
- arbitrary scalar reward;
- ML fitting/classifier;
- random tie-break;
- coordinate-name ordering;
- hidden donor preference;
- Euclidean distance/angle/norm/trigonometry as selector premise;
- path-language enumeration as BRC definition;
- physical probability from symmetry;
- using straightness to choose `b_0`.

Tiny enumeration may be an oracle only after symbolic rules are frozen.

## 16. Final dispositions

The final report must choose among and justify at least these statuses:

- `INITIAL_BRC_SELECTOR_ESTABLISHED_FROM_EXACT_CONTEXT`
- `INITIAL_BRC_SELECTOR_REQUIRES_TAU_ODD_CONTEXT`
- `INITIAL_BRANCH_IS_RELATIVE_CONTEXT_TORSOR`
- `SYMMETRY_INVARIANT_POST_CREDIT_INSUFFICIENT_FOR_INITIALIZATION`
- `INITIAL_MULTIBRANCH_IS_INTRINSIC_UNDER_FULL_SYMMETRY`
- `INITIAL_SELECTOR_STILL_NONIDENTIFIED`

Do not force a positive selector result.

After all artifacts/checker/manifest/checkpoint:

`STOP_FOR_DRIVER_REVIEW`
