# HODGE Stage H0I — Codimension-Two Class-First Lifting via Rank-2 Bundles / Zero Loci

Date: `2026-08-22`
Status: `ACTIVE / DRIVER-ISSUED TASKBOOK`
Driver: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`
Task ID: `RS-HODGE-H0I-CODIM2-CLASS-FIRST-VECTOR-BUNDLE-ZERO-LOCUS-LIFTING`
identity_policy: `AUTO_RESOLVE_OR_ALLOCATE`
identity_lane: `HODGE-H0I-CODIM2-LIFTING`
owner branch: `research/hodge-h0i-codim2-class-first-lifting`
control branch: `research/hodge-special-control-plane`

## 0. Driver acceptance of H0H-R1

H0H-R1 is accepted as a valid negative p=1 lifting result.

Frozen owner branch:

`research/hodge-h0h-r1-canonical-plane-picard-lifting`

Taskbook source:

`637ef5cd5dbe3ce1fbe8ef06844c6b28ae36947d`

Frozen semantic core:

`04a0abdad437e777bd2476e360fa17d30059580fd6fcbc4269bcb4844bfdb298`

Frozen disposition:

`H0H_R1_SOURCE_PICARD_DIVISOR_NORMAL_FORM_ALREADY_COMPLETE`

Hard target:

`LEFSCHETZ_11_LIFTING_SOURCE_INSTANTIATES_ROBUST_ATTRIBUTED_R2 = NOT_ESTABLISHED`.

Checker:

`3903/3903 PASS`.

The strongest abstract H1 future quotient reduced reusable prefix state from `1386 -> 203`, but fair source mathematics independently derived the same `(a,b)` Picard/gauge normal form. `dlog` and divisor/valuation candidates were likewise absorbed by standard kernel/image and Cartier/principal-divisor/SNF structure. The canonical Enterprise plane was correctly classified as non-load-bearing for H1/H2/H3; the rank-two native lattice bridge was only a compatible reparameterization.

Driver route decision:

`P1_RECOGNITION_AND_LIFTING_SOURCE_COMPLETE__PIVOT_TO_CODIMENSION_TWO_CLASS_FIRST_LIFTING`.

This statement is scoped: it does not claim all p=1 mathematics is exhausted; it says the audited Hodge-recognition and Picard/divisor-lifting obligations already possess exact source-native normal forms, so further Enterprise repackaging of those obligations is not the missing Hodge step.

---

## 1. Mission

Move beyond divisors and attack the first higher-codimension algebraicity interface.

Primary codimension:

`p = 2`.

Single hard target:

`CODIMENSION_TWO_CLASS_FIRST_LIFTING_SOURCE_INSTANTIATES_ROBUST_ATTRIBUTED_R2`.

Preferred stronger outcome:

`CODIMENSION_TWO_ENTERPRISE_R3_PRESEED`.

The central distinction of H0I is mandatory:

`BUNDLE_FIRST_GENERATION != CLASS_FIRST_LIFTING`.

A construction

`algebraic bundle/section -> c2 -> algebraic zero locus -> cohomology class`

is a useful positive control, but by itself is not a Hodge lifting mechanism. A genuine Hodge-relevant preseed must begin from a typed cohomological/de Rham class descriptor that is fixed independently of the candidate bundle/cycle and then operationally construct or certify algebraic codimension-two data realizing it.

H0I does not prove the Hodge conjecture and does not start H1.

---

## 2. Frozen authority / startup packet

Mandatory reads:

1. `AGENTS.md`
2. `docs/GITHUB_INTERACTION_BUDGET.md`
3. `research_common_surface.json`
4. `FOUNDATIONAL_LOGIC.md`
5. `foundational_logic.json`
6. `driver_handoffs/HODGE_SPECIAL_DRIVER_HANDOFF_20260817.md`
7. `driver_handoffs/HODGE_SPECIAL_DRIVER_PI_GEOMETRY_ADDENDUM_20260817.md`
8. H0A0 Criterion V2 at `f4e6cf84eb191e0b7442913e018e1f6347e9584e`
9. H0D0 attribution criterion at `96e79629b822a8cb3bc11be1cec8abe319e4cd20`
10. H0D abstract robust-R2 at `102f6c73a099a97a412e72c810f8e63d2c370234`
11. H0G filtered Čech–de Rham recognition result at `2335f1b91998943c055b9c02d144d0128e6cdc29`
12. H0H-R1 frozen artifacts on `research/hodge-h0h-r1-canonical-plane-picard-lifting`
13. current canonical native-plane authority `definitions/ENTERPRISE_THREE_POSITIVE_AXIS_OVERLAPPING_CIRCLE_CELL_PLANE_20260820.md` at `c4649b276a3d604822c586dddafd028e15d02976`.

Historical results remain read-only.

---

## 3. Primary benchmark: Gr(2,4)

Primary algebraic variety:

`X = Gr(2,4)_C`.

Reason for this benchmark:

- it is a smooth projective fourfold;
- codimension-two algebraic geometry is nontrivial;
- a rank-one Picard/divisor source does not by itself span all codimension-two structure;
- the tautological rank-two subbundle `S` and quotient bundle `Q` provide exact algebraic higher-rank transition data;
- standard Schubert calculus is available as an external checker but must not generate the Enterprise candidate.

Use a predeclared finite family of standard Plücker/big-cell affine charts. Represent points by row-reduced `2x4` matrices on each chart and derive overlap coordinate substitutions exactly.

Generate transition matrices for `S` and/or `Q` from the chart formulas. Do not import a precomputed Chern class or Schubert basis as the source generator.

Required exact source artifacts include:

- chart registry;
- overlap coordinate rings/localizations;
- transition matrices and cocycle checks;
- gauge/basis changes;
- bounded algebraic section family chosen before success evaluation;
- section-zero-locus ideals where applicable;
- exact codimension/regular-section checks at the finite declared scope;
- class descriptors used for comparison.

---

## 4. Required source distinction: bundle-first control vs class-first target

### 4.1 Bundle-first control

At least one positive control must start from an independently generated rank-two bundle cocycle and algebraic section, construct its codimension-two zero locus when regular, and verify the standard class relationship at the declared scope.

This control may use classical facts such as top-Chern/regular-zero-locus compatibility only as source mathematics / checker as explicitly typed.

It earns no Hodge lifting credit merely because the output cycle is algebraic.

### 4.2 Class-first lifting source

Freeze before bundle/cycle search a finite exact class carrier `V_target` in degree four.

Preferred route:

- derive `V_target` from an exact bounded Čech–de Rham / algebraic de Rham source on the chosen cover, or another source-independent cohomological carrier;
- include a declared Hodge-filtration/type tag only through typed classical comparison;
- choose positive/negative target descriptors before candidate bundle/cycle search.

Forbidden:

- defining target basis vectors by naming the known Schubert cycles that realize them;
- computing `c2(S)` or `c2(Q)` first and then declaring those resulting vectors to be the target basis;
- using known Chow generators to select only liftable targets.

If an exact class-first finite carrier cannot be produced without target leakage, freeze the missing object and do not fabricate one.

---

## 5. Classical comparison firewall

The following are source/prior mathematics and may be used as controls or fair-baseline operations:

- vector bundles as `GL_r(O)` Čech cocycles modulo gauge;
- determinant and exterior powers;
- Chern class formalism, Whitney sum, splitting-principle consequences used only at the exact declared scope;
- regular-section/top-Chern zero-locus theorem;
- determinantal/Fitting ideals;
- exact ideal membership, Gröbner reduction, saturation and elimination on the finite source when computationally available;
- rational equivalence / Chow-group relations at the declared source scope;
- standard Schubert calculus on `Gr(2,4)` as checker/control;
- known cohomology/Chow ranks as checker/control;
- cycle-class maps;
- exact integer/rational lattice reduction.

But:

`KNOWN_SCHUBERT_CYCLE_ANSWER_IS_CONTROL_ONLY`.

`KNOWN_HODGE_CLASS_ALGEBRAICITY_ON_GRASSMANNIAN_IS_CONTROL_ONLY`.

Do not use a known Schubert cycle, known Chow basis, or known `c2` answer to generate the candidate lift.

---

## 6. Fair source-baseline sandwich

Freeze both baselines before Enterprise success counts or obstruction ranks are evaluated.

### `B_raw^codim2`

May use:

- exact chart/transition execution;
- direct `GL2` cocycle and gauge checks;
- direct bounded section enumeration;
- direct zero-locus ideal construction;
- exact local codimension tests;
- direct comparison with the frozen class descriptor;
- brute-force/exact search on the declared finite carrier.

Do not preinstall future-signature or behavioral quotient machinery.

### `B_std^codim2`

Must additionally include every fair standard source operation, including as applicable:

- vector-bundle cocycle/gauge quotient normal forms;
- ordinary linear algebra on transition matrices;
- Chern-class source formulas;
- Whitney/splitting-principle reductions at declared scope;
- regular-section/top-Chern normal form;
- determinantal/Fitting ideal normal forms;
- Gröbner/saturation/elimination normal forms;
- source-derived Chow/rational-equivalence relations;
- Smith/Hermite or rational lattice normal forms;
- any independently derived source obstruction to realizing the frozen target class.

Anti-gaming:

A candidate winning only because `B_raw^codim2` withholds obvious vector-bundle/Chern/ideal/Chow structure must be classified `BASELINE_SENSITIVE_ATTRIBUTION`.

---

## 7. Required candidate families

Study at least three structurally distinct candidates.

### I1 — `GL2` cocycle future quotient

Use partial rank-two transition-matrix assignments / gauge states as a multi-step source.

Define complete future signatures for all remaining:

- cocycle constraints;
- gauge equivalence;
- target-class comparison;
- section/zero-locus obligations if coupled.

Ask whether the quotient yields strict reusable-state reduction beyond the fair source bundle quotient.

If it is merely standard nonabelian Čech cocycle/gauge reduction, classify source-inherited.

### I2 — zero-locus / ideal provenance carrier

For a bounded family of algebraic sections, retain exact provenance from section coefficients through local equations, gluing, ideal generation, codimension tests and cycle readout.

Seek a compositional carrier that permits branch recoalescence without losing multiplicity or component provenance.

Boolean support alone is insufficient when multiplicity matters.

Required stress:

- two locally similar support patterns with different multiplicity/provenance or codimension-two cycle readout;
- a cancellation/recombination case where naive support quotient fails.

### I3 — class-first Chern-to-cycle lifting interface

Starting from the frozen class descriptor `v in V_target`, search the bounded source for bundle/section/cycle data whose exact class comparison equals `v`.

This is the most Hodge-relevant candidate.

Required distinctions:

- class recognition vs algebraic construction;
- existence vs chosen representative;
- bundle nonuniqueness vs cycle nonuniqueness;
- equality of class after comparison vs equality of local equations.

A bundle-first `c2` computation is not accepted as I3 success.

### Optional I4 — intersection-of-divisors control

Include decomposable codimension-two classes obtained by intersecting p=1 divisor lifts as a negative/control lane.

If the new mechanism works only on divisor products, it does not establish a genuinely higher-codimension route component.

### Optional I5 — native-plane compatibility

Only if a load-bearing candidate genuinely uses Enterprise-native coordinates/cells, audit it against the current canonical plane.

Do not force vector-bundle, ideal or Chow lattices into native geometry.

A mere rank or lattice isomorphism is R0/R1 reparameterization.

---

## 8. Hard attribution gate

Every claimed Hodge-special R2 must carry:

- Criterion V2 `PROOF_LEVERAGE_CERTIFICATE`;
- H0D0 `LEVERAGE_ATTRIBUTION_CERTIFICATE`;
- predeclared source baseline;
- exact comparison theorem;
- strict leverage witness;
- target-leakage audit;
- novelty/prior-art ledger.

Hard-target credit requires:

`ROBUST_TRANSFORM_ATTRIBUTED`

against both `B_raw^codim2` and `B_std^codim2`.

Possible leverage classes include:

- `FINITE_OBSTRUCTION_BASIS`;
- `DEPENDENCY_REDUCTION`;
- `COMPOSITIONAL_FACTORING`;
- `NORMAL_FORM`;
- `LAYER_LOWERING`;
- `INTEGRALITY_POSITIVITY_MONOTONICITY`;
- provenance-preserving reduction if precisely defined.

Novelty is not a gate.

---

## 9. R3 preseed gate

Do not award `CODIMENSION_TWO_ENTERPRISE_R3_PRESEED` merely because an algebraic cycle was generated.

A preseed requires, at minimum:

1. a target degree-four cohomology/de Rham descriptor fixed independently of the output cycle;
2. a typed Hodge-side qualification for the declared target subspace, without importing algebraicity;
3. a robust transform-attributed Enterprise operational constraint/lifting component;
4. an algebraically generated codimension-two candidate cycle or cycle class;
5. exact comparison showing the output cycle class equals the input target descriptor;
6. rational scaling compatibility at the claim scope;
7. presentation/gauge independence;
8. no known cycle answer used as generator.

If any item is missing, freeze `NO_R3_PRESEED` and name the missing object.

---

## 10. Rational coefficients / multiplicity

Hodge remains a rational statement.

Track separately:

- integral cycle multiplicities;
- rational cycle coefficients;
- torsion if present;
- degree-four Betti/de Rham class coordinates;
- Chern-class integrality;
- rational scaling.

Do not silently replace rational Hodge with integral Hodge.

When zero loci are used, multiplicity and scheme structure must not be silently discarded if load-bearing.

---

## 11. Presentation / naturality / gauge

At the exact claim scope audit:

- chart relabeling;
- Plücker-coordinate presentation changes;
- local frame / `GL2` gauge changes;
- equivalent section presentations;
- ideal-generator changes;
- source-derived cycle equivalence;
- any symmetry of `Gr(2,4)` used in the finite benchmark.

Do not claim arbitrary-cover or full functoriality beyond what is proved.

---

## 12. Target leakage firewall

Forbidden as candidate generators:

- known Schubert cycle realizing the target class;
- known Chow basis chosen from the target answer;
- known Hodge number used to tailor candidate-state dimension;
- known Hodge-conjecture conclusion on the benchmark;
- a preselected bundle whose `c2` is exactly the desired class when that class was defined from the same bundle;
- known algebraic representative fed into future signatures.

Controls/checkers may use those facts only after candidate generation and must be logged separately.

---

## 13. Required artifacts

At minimum produce:

1. `HODGE_H0I_CLASSIFICATION.json`
2. `HODGE_H0I_ALGEBRAIC_SOURCE_SPEC.json`
3. `HODGE_H0I_TARGET_CLASS_CARRIER.json`
4. `HODGE_H0I_GL2_TRANSITION_REGISTRY.json`
5. `HODGE_H0I_BUNDLE_FIRST_CONTROL.json`
6. `HODGE_H0I_ZERO_LOCUS_PROVENANCE_REGISTRY.json`
7. `HODGE_H0I_CLASS_FIRST_LIFT_REGISTRY.json`
8. `HODGE_H0I_SOURCE_BASELINE_SANDWICH.json`
9. `HODGE_H0I_PROOF_LEVERAGE_CERTIFICATE_REGISTRY.json`
10. `HODGE_H0I_ATTRIBUTION_CERTIFICATE_REGISTRY.json`
11. `HODGE_H0I_MULTIPLICITY_RATIONAL_LEDGER.json`
12. `HODGE_H0I_PRESENTATION_NATURALITY_LEDGER.json`
13. `HODGE_H0I_TARGET_LEAKAGE_LEDGER.json`
14. `HODGE_H0I_PRIOR_ART_NOVELTY_LEDGER.json`
15. `HODGE_H0I_R3_PRESEED.json`
16. `HODGE_H0I_COMPARISON_THEOREM_REGISTRY.json`
17. `HODGE_H0I_SEMANTIC_CHECKPOINT.md`
18. deterministic checker + output
19. manifest with SHA-256 digests.

---

## 14. Required terminal classification

Freeze exactly one primary disposition among:

- `H0I_ROBUST_ATTRIBUTED_CODIM2_R2_FOUND`
- `H0I_R3_PRESEED_FOUND`
- `H0I_SOURCE_VECTOR_BUNDLE_CHOW_NORMAL_FORM_ALREADY_COMPLETE`
- `H0I_CLASS_FIRST_TARGET_CARRIER_MISSING`
- `H0I_ATTRIBUTION_UNRESOLVED`
- `H0I_HARD_BLOCK`

If blocked, name:

- `missing_object`;
- `owner`;
- `necessity`;
- `unblock_condition`.

H1 remains `NOT_ADMISSIBLE` unless separately opened by Driver after review.

---

## 15. Stop rule

Stop after freezing H0I artifacts and checker results.

Do not:

- start H1;
- claim the Hodge conjecture;
- generalize from `Gr(2,4)` to all smooth projective varieties;
- move to codimension three;
- import a known Schubert answer as the generator.

Return to:

`EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`.
