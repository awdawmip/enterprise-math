# R059D Stage M REISSUE — BRC6 Discrete Three-Axis Vector Carrier / Equal-L Endpoint Branch-Count Microgrammar Robustness

Task-ID: `RS-R059D-STAGE-M-BRC6-DISCRETE-THREE-AXIS-VECTOR-ENDPOINT-COUNT-MICROGRAMMAR-ROBUSTNESS`
Generation: `R059D`
Status: `DRIVER_APPROVED_REISSUE_TASKBOOK`
Date: `2026-08-15`
Driver: `EM-DVR-R0457K / CONTROL_PLANE`
Researcher-ID: `EM-R059D-9C6B2A`
Owner branch: `research/r059d-stage-m-brc6-three-axis-vector-endpoint-count`

## 0. Frozen parent and reissue disposition

Stage L is frozen at owner head:

`da350b7b1e2ae21491e6251fdf2ba9cf0d4557ca`

All BRC6 Stage J/K/L artifacts remain immutable.

The earlier Stage-M taskbook:

`research_tasks/R059D_STAGE_M_BRC6_EQUAL_L_ENDPOINT_BRANCH_COUNT_MICROGRAMMAR_ROBUSTNESS_20260815.md`

is superseded before execution.

This reissue changes the carrier typing, not the Stage-L endpoint-count conclusion.

Freeze Stage-L results:

- `BRC6_EQUAL_L_ENDPOINT_BRANCH_COUNT_COLLAPSE_ESTABLISHED`;
- `BRC6_ENDPOINT_COUNT_TRUE_STATE_DYNAMICS_ESTABLISHED`;
- `ALIGNED_SEGMENT_CELL_COUNT=L=4` for all six directed candidates;
- `L` is a common endpoint evaluation boundary only;
- endpoint branch count in the inherited affine quotient is `B_d=C_(L-1)^d=C_3^d=A_d+3I[d]`;
- positive collapse is unique strict endpoint-count argmax; exact ties unresolved;
- `BRC6_NATIVE_CANONICALITY = NOT_ESTABLISHED`.

## 1. Scientific correction: vectors are admitted

The project already uses a discrete number-axis / integer count foundation. Vector addition over those discrete coordinates is therefore admitted as an algebraic carrier operation.

Do not treat VECTOR as forbidden merely because continuum Euclidean geometry also uses vectors.

Stage M freezes a discrete plane carrier as the rank-two integer module

`V2_3AXIS = {(a,b,c) in Z^3 : a+b+c=0}`.

This is a two-degree-of-freedom discrete vector space/module represented with three coordinated axes.

Freeze three directed axis generators:

`u=(1,-1,0)`

`v=(0,1,-1)`

`w=(-1,0,1)`

with exact relation

`u+v+w=0`.

The six directed candidate steps are

`D6={+u,-u,+v,-v,+w,-w}`.

Thus:

`PLANE_DIMENSION = 2`

`RELATIONAL_AXIS_COUNT = 3`

`DIRECTED_CHANNEL_COUNT = 6`.

The six BRC6 outputs are not six independent axes. They are three axis pairs with opposite vector orientation.

A packet at coordinate `x in V2_3AXIS` may make a declared vector adjacency transition

`x -> x+d`

for `d in D6`.

Allowed native/algebraic vector operations for this stage:

- integer coordinate equality;
- vector equality;
- vector addition/subtraction;
- additive inverse `d -> -d`;
- integer scalar multiplication when needed for exact bookkeeping;
- finite vector sums of transition events;
- exact endpoint coordinate comparison.

Still forbidden unless separately established:

- Euclidean norm;
- dot product;
- angle;
- cosine/sine;
- metric length;
- shortest path;
- geometric straightness;
- slope/curvature;
- continuous scalar coordinates;
- physical displacement calibration.

`VECTOR` is algebraically allowed; `EUCLIDEAN METRIC GEOMETRY` is not silently inherited.

## 2. BRC6 vector typing

Replace structureless labels `0..5` by a frozen bijection with `D6`.

The concrete numeric encoding may be used for serialization only, but the semantic output is

`BRC6(sigma) in D6`.

Equivalently:

`BRC6(sigma) = NEXT_STEP_VECTOR`.

All selector/covariance theorems must now be restated as automorphism covariance of the three-axis vector carrier.

At minimum test the finite symmetry group generated by:

1. cyclic permutation of the three axes `u->v->w->u`;
2. global inversion `d->-d`.

Do not assume an absolute privileged axis or sign.

Any state invariant under an automorphism that moves every candidate vector cannot have a unique equivariant BRC6 output.

## 3. Equal-L boundary remains independent of vector sum

Freeze:

`ALIGNED_SEGMENT_CELL_COUNT=L=4`.

Every candidate segment has exactly four continuation packet cells, regardless of which `d in D6` labels the next directed axis.

`L` remains packet/cell count.

Do not replace L by vector norm.

Do not compute `||d||`.

Do not rank candidate vectors by any norm or metric.

Endpoint branch-count selection remains a count collapse, not vector-length optimization.

## 4. Stage-M0 vector carrier checker gate

Before any microgrammar scoring, construct and freeze:

1. `R059D_STAGE_M_DISCRETE_THREE_AXIS_VECTOR_PROTOCOL.json`
2. `R059D_STAGE_M_VECTOR_AXIS_PAIR_PROTOCOL.json`
3. `R059D_STAGE_M_VECTOR_ADJACENCY_PROTOCOL.json`
4. `R059D_STAGE_M_VECTOR_SYMMETRY_PROTOCOL.json`
5. `R059D_STAGE_M_VECTOR_GEOMETRY_FIREWALL.json`

Checker must reject:

- six independent axes;
- rank three interpretation of the plane;
- failure of `a+b+c=0` closure;
- candidate vector not in `D6`;
- use of norm/angle/dot product as selector premise;
- candidate-dependent L;
- absolute-axis tie break.

Stage M0 must prove exact closure:

if `x in V2_3AXIS` and `d in D6`, then `x+d in V2_3AXIS`.

Also prove:

`u+v+w=0`.

## 5. Endpoint branch-count observable

Retain Stage-L collapse observable:

`B_d = exact admissible raw-history multiplicity arriving at the candidate d next aligned endpoint after the common L=4 packet cells`.

Here `d in D6` is a directed vector candidate.

The endpoint may be represented by the coordinate

`x + S_d`

where `S_d` is the declared vector sum of the four packet transitions for candidate d, but Stage M must not assume `S_d=4d` unless explicitly declared and checked by the candidate segment grammar.

The collapse rule remains:

`BRC6_COUNT_MODE(sigma)=d`

iff

`B_d > B_e` for all `e != d`.

Exact maximal ties:

`BRC6_UNRESOLVED_BY_ENDPOINT_BRANCH_COUNT`.

Unresolved remains evaluator status, not a seventh vector.

## 6. Main Stage-M question: microgrammar robustness under fixed vector carrier

Keep fixed within each evaluation:

- same current vector coordinate x;
- same six candidate vectors D6;
- same four continuation packet cells per candidate;
- same aligned endpoint boundary convention;
- same endpoint-count collapse rule.

Vary only the internal count-preserving branching/recoalescence microgrammar.

For each pre-frozen microgrammar `g`, compute:

`B_d^(g)`

by raw-history enumeration and exact CPBC compression.

Prove:

`CPBC endpoint count = raw-history multiplicity`

on every tiny seed used for evidence.

No arbitrary weights.

No support-only collapse.

## 7. Microgrammar registry

Freeze before scoring at least:

### M-G0 AFFINE_UV_REPLAY

Exact Stage-L affine replay.

### M-G1 SINGLE_SPLIT_RECOALESCE

One exact internal branch pair recoalesces before endpoint.

### M-G2 DELAYED_SPLIT_RECOALESCE

Same packet count and endpoint, split occurs at a different internal packet index.

### M-G3 TWO_STAGE_SPLIT_RECOALESCE

Two successive count-preserving internal branching events.

### M-G4 LAUNCH_CLASS_SPLIT_CONTROL

Branching acts on launch-origin histories.

### M-G5 VECTOR_INTERNAL_REWRITE_ENDPOINT_EQUIVALENT

Different internal vector-labeled transition word/history organization but exact same endpoint branch-count functional as another grammar.

At a given evaluation all six directed vector candidates must use the same grammar g. Candidate-specific grammar is forbidden.

Every grammar must be covariant under the frozen axis-permutation/global-inversion symmetry group.

## 8. Vector path/history bookkeeping

Every raw history stores both:

- its transition-event sequence;
- the exact discrete vector step attached to each event.

For a history h:

`VECTOR_SUM(h)=sum_t d_t`.

This is an algebraic vector sum only.

Do not call it Euclidean displacement length.

Histories with different internal vector words may recoalesce at the same packet coordinate/state; multiplicities must add.

Required tiny cases:

- immediate reversal `d,-d`;
- repeated same vector step;
- loop with total vector sum zero;
- revisit;
- two different vector words ending at same coordinate;
- branch/recoalescence with multiplicity >1;
- same-axis opposite-orientation histories;
- different-axis histories that recoalesce algebraically.

## 9. Witness replay and robustness classes

Replay:

- `W_ASYM_BASE`;
- `W_CONSENSUS_DOMINANT2`;
- `W_FULLY_SYMMETRIC`;
- `W_S1_TIE_S2_RESOLVE`;
- `W_SIGNATURE_INSUFFICIENT_PAIRS`.

Retype their six entries onto D6 and store exact vector-keyed endpoint count tables.

Classify:

- `MICROGRAMMAR_STRONG_CONSENSUS_RESOLVED`;
- `MICROGRAMMAR_COMPATIBLE_WITH_TIES`;
- `MICROGRAMMAR_DEPENDENT`;
- `MICROGRAMMAR_ALL_UNRESOLVED`.

If two admissible grammars uniquely select different directed vectors on the same state, freeze microgrammar dependence and do not promote native canonicality.

## 10. Structural theorem targets

### M-T1 VECTOR-COVARIANT ENDPOINT FUNCTIONAL EQUIVALENCE

If two microgrammars induce identical vector-keyed endpoint-count functional `B_d`, prove identical BRC6 outputs for every state.

### M-T2 COMPONENTWISE NONNEGATIVE DOMINANCE

If one directed vector candidate dominates every other candidate in every nonnegative seed component used by all frozen grammars, prove grammar-independent unique winner or compatible tie.

### M-T3 VECTOR MICROGRAMMAR DEPENDENCE CERTIFICATE

Find an exact state and two legal vector-covariant grammars with conflicting strict endpoint-count winners if possible.

### M-T4 AXIS/ORIENTATION FACTORIZATION AUDIT

Determine whether the six-way endpoint count can be factored without loss into:

`AXIS_COUNT(A), AXIS_COUNT(B), AXIS_COUNT(C)`

plus an orientation comparison inside the winning axis pair.

Do not force such factorization. Either prove exact equivalence or reject it with a counterexample.

## 11. True vector-state dynamics

Use actual vector coordinate update.

After BRC6 selects `d in D6`, update the current coordinate by the declared equal-L segment vector sum:

`x_next = x + S_d`.

Also perform the exact Stage-K/L incidence bookkeeping:

`O_x[d] += 1`

`M_x[i,d] += 1`

and the target ingress update under the declared vector-port transport.

At every new aligned decision recompute all six endpoint branch counts from the accumulated exact state.

Do not reuse an old normalized signature profile.

Record:

- selected directed vector sequence `d_0,d_1,...`;
- cumulative vector sum;
- current discrete vector coordinate;
- first unresolved epoch;
- endpoint-count table;
- no full-state-cycle claim unless full state repeats.

Because counts may grow monotonically, repeated vector direction words need not imply full-state recurrence.

## 12. Equal-L six-output coverage

For any resolved vector witness, prove six-output coverage by symmetry orbit, not six hand-written cases.

Use the frozen three-axis symmetry group to transport one resolved witness across all six directed vectors.

Freeze a coverage theorem only if the orbit actually reaches all six directed candidates.

## 13. Perturbation tests

Repeat exact perturbations under vector typing:

- one launch/count token;
- one incidence event;
- one real tagged adjacency/vector-step perturbation.

For the real tagged perturbation, change exactly one declared adjacency transition vector to another allowed D6 step while preserving the taskbook's local typing constraints.

Record:

- first BRC6 vector change;
- endpoint-count delta vector;
- trajectory divergence;
- unresolved creation/removal;
- exact state recoalescence if any.

## 14. Large-N

Retain exact symbolic probes near `10^36`.

N is system/tag/packet scale only.

It is not vector magnitude, norm, length or coordinate precision.

Use exact integer arithmetic only.

## 15. Required artifacts

At minimum:

1. `R059D_STAGE_M_DISCRETE_THREE_AXIS_VECTOR_PROTOCOL.json`
2. `R059D_STAGE_M_VECTOR_AXIS_PAIR_PROTOCOL.json`
3. `R059D_STAGE_M_VECTOR_ADJACENCY_PROTOCOL.json`
4. `R059D_STAGE_M_VECTOR_SYMMETRY_PROTOCOL.json`
5. `R059D_STAGE_M_VECTOR_GEOMETRY_FIREWALL.json`
6. `R059D_STAGE_M_VECTOR_MICROGRAMMAR_REGISTRY.json`
7. `R059D_STAGE_M_RAW_HISTORY_CPBC_VECTOR_ORACLE.json`
8. `R059D_STAGE_M_VECTOR_ENDPOINT_FUNCTIONAL_ATLAS.json`
9. `R059D_STAGE_M_VECTOR_MICROGRAMMAR_ROBUSTNESS_LEDGER.json`
10. `R059D_STAGE_M_VECTOR_AXIS_ORIENTATION_FACTORIZATION_AUDIT.json`
11. `R059D_STAGE_M_VECTOR_TRUE_DYNAMICS_ATLAS.json`
12. `R059D_STAGE_M_VECTOR_PERTURBATION_RESPONSE.json`
13. `R059D_STAGE_M_VECTOR_LARGE_N_REGISTRY.json`
14. deterministic checker + output
15. report
16. artifact manifest
17. frozen checkpoint

Then:

`STOP_FOR_DRIVER_REVIEW`.

## 16. Interpretation firewall

Allowed freeze if proved:

- `DISCRETE_THREE_AXIS_VECTOR_CARRIER_ESTABLISHED`
- `BRC6_VECTOR_ENDPOINT_COUNT_MICROGRAMMAR_ROBUST_DOMAIN_ESTABLISHED`
- `BRC6_VECTOR_ENDPOINT_COUNT_MICROGRAMMAR_DEPENDENCE_ESTABLISHED`
- `BRC6_VECTOR_TRUE_STATE_DYNAMICS_ESTABLISHED`

Continue unless separately proved:

`BRC6_NATIVE_CANONICALITY = NOT_ESTABLISHED`

`EUCLIDEAN_METRIC_FROM_VECTOR_CARRIER = NOT_ESTABLISHED`

`PHYSICAL_DIRECTION_CALIBRATION = NOT_ESTABLISHED`

`PHYSICAL_PROBABILITY_FROM_BRC_COUNTS = NOT_ESTABLISHED`

`PHYSICAL_RIGIDITY_INTERPRETATION = NOT_ESTABLISHED`

`QUANTUM_BRIDGE = NOT_ESTABLISHED`.
