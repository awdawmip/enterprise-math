# Research Taskbook — Native Filament Final Narrow External Novelty Audit

Task-ID: `RS-NATIVE-FILAMENT-FINAL-NARROW-EXTERNAL-NOVELTY-AUDIT`

Date: `2026-08-25`

Hard target:

`NATIVE_FILAMENT_FINAL_NARROW_COUPLED_SELECTION_EXTERNAL_NOVELTY_INDEPENDENTLY_CLASSIFIED`

## 1. Allowed mathematical input

Read only:

`research_inputs/NATIVE_FILAMENT_FINAL_NARROW_NOVELTY_BLIND_PACKET_20260825.md`

The statement has already passed independent mathematical replication. This task is literature/theorem-statement overlap classification, not proof checking.

## 2. Independence wall

Before freezing the return, do not read:

- PR #627 or branch `research/native-filament-generalization-theorem-package-20260824`;
- PR #632 direct/nonblind audit returns;
- originating-researcher novelty opinions;
- package-specific proofs or checkers.

External literature is allowed and required.

## 3. Required literature domains

At minimum search:

1. classical/projective conic and parabola tangent geometry;
2. translated/homothetic parabola tangent concurrence;
3. split one-dimensional tori/hyperbolas and quotient by inversion/sign groups;
4. Dickson polynomials and Joukowski `x+c/x` maps;
5. Dickson/Joukowski value-set cardinality and preimage multiplicity;
6. minimal/small value-set rational functions;
7. low-degree many-to-one/2-to-1 rational functions over finite fields;
8. finite-field interval/arithmetic-progression target value sets;
9. the exact extremal constraints `q=2s-1`, `q=2s+1` combined with Dickson/Joukowski images;
10. arithmetic dynamics / covering systems / deterministic arithmetic percolation for boundary-capacity matching;
11. any literature coupling a geometry-selected scalar simultaneously to tangent incidence and rational-function value-set saturation.

## 4. Evidence standard

For each F1--F6 statement in the input packet:

- assign one required verdict label;
- give the strongest exact source(s);
- identify theorem/proposition/section where possible;
- map hypotheses and conclusions;
- separate direct subsumption from routine algebraic consequence;
- record what remains uncovered.

Keyword resemblance is insufficient.

## 5. Search log

Record databases/search engines and exact search phrases or reproducible semantic queries.

For `NO_DIRECT_MATCH_FOUND`, list at least the three closest sources and why none subsumes the statement.

## 6. Required return

Write:

`research_returns/NATIVE_FILAMENT_FINAL_NARROW_EXTERNAL_NOVELTY_AUDIT_RETURN_20260825.md`

The return must contain:

1. auditor identity and independence attestation;
2. F1--F6 verdict matrix;
3. package-level verdict;
4. exact source mapping;
5. search log;
6. recommended wording for PR #627;
7. exact list of any novelty claims that must be downgraded;
8. hard-target line.

## 7. Allowed conclusion strength

Even if every search returns no direct match, the maximum allowed wording is:

`NO DIRECT THEOREM-STATEMENT MATCH FOUND IN THE AUDITED LITERATURE SET`.

Do not write `new theorem`, `novel`, `first`, or equivalent publication novelty claims.

## 8. Freeze condition

Freeze the return in Git before reading any prohibited source branch or direct-audit opinion.
