# R059D Stage Q — BRC Cross-Scale Torsor Synchronization / Relative Collapse Constraints / Post-Credit Gauge Identification

Task-ID: `RS-R059D-STAGE-Q-BRC-CROSS-SCALE-TORSOR-SYNCHRONIZATION`
Generation: `R059D`
Status: `DRIVER_APPROVED_TASKBOOK`
Date: `2026-08-16`
Driver: `EM-DVR-R0457K / CONTROL_PLANE`
Researcher-ID: `EM-R059D-9C6B2A`
Owner branch: `research/r059d-stage-q-brc-cross-scale-torsor-synchronization`
Frozen parent: `a621f80d0294f5a5139eb4a2ed26e552e6368b18`

## 0. Frozen inputs

Stage P is immutable. Consume only its typed conclusions and frozen Stage N/O algebraic foundations.

Mandatory frozen inputs:

- `BRC_SYMMETRIC_BRANCH_PAIR_IS_FREE_Z2_TORSOR`
- `INITIAL_BRC_SELECTOR_REQUIRES_TAU_ODD_CONTEXT`
- `TAU_FIXED_CONTEXT_CANNOT_INITIALIZE_UNIQUE_BRANCH`
- `SYMMETRY_INVARIANT_POST_CREDIT_CANNOT_BREAK_INITIAL_BRANCH_SYMMETRY`
- `INITIAL_BRC_SELECTION_EQUALS_EXACT_CONTEXTUAL_SINGLETON_WHEN_AVAILABLE`
- `INITIAL_BRANCH_IS_RELATIVE_CONTEXT_TORSOR`
- `BARE_TORSOR_IDENTIFICATION_NONCANONICAL`
- `STRAIGHTNESS_ONE_BIT_MEMORY_CONTINUATION_LAW_ESTABLISHED_WITH_INITIAL_BRANCH_UNIDENTIFIED`
- completion-neighbor collapse `COLL_C(q;b)=PREV_C(q)+(NEXT_C(q)-PREV_C(q))b`

Project mode remains `REFOUND, NOT REJECT`.

Do not use nearest rounding, endpoint argmax, arbitrary reward weights, ML fitting, random tie-break, hidden coordinate ordering, Euclidean distance/angle/norm/trig as selector premises, or path-language BRC reinterpretation.

## 1. Core scientific question

Stop searching for an absolute pointwise law `b=f(q)` as the default BRC model.

Test whether BRC is more naturally a **network of relative Z2 collapse constraints** whose local bits are determined only up to a global torsor origin unless an independent tau-odd anchor exists.

For collapse variables `b_v in {0,1}`, study exact relative constraints of the form

`b_u xor b_v = c_uv`, `c_uv in {0,1}`

and exact Boolean generalizations derived from declared higher-level algebra/post-credit.

The central hypothesis to test is:

`UPWARD/DOWNWARD COLLAPSE + POST-CREDIT -> RELATIVE TORSOR SYNCHRONIZATION`

rather than independent local scoring.

## 2. Stage Q0 — Z2 torsor constraint graph

Freeze a finite graph/hypergraph registry before scoring.

Each node is one collapse torsor variable.

Each binary edge carries an exact relation:

- `c=0`: same branch relation;
- `c=1`: complementary branch relation.

Do not interpret 0/1 as physical directions or probability.

Prove exactly for each connected component:

1. If all cycle parities are consistent and there is no singleton anchor, the solution set has exactly two globally complementary assignments.
2. If one exact singleton/tau-odd anchor fixes one node, every node in that connected component is uniquely determined.
3. If cycle parity is inconsistent, the exact solution set is empty.
4. Disconnected unanchored components each contribute one independent Z2 torsor degree of freedom.

Required freezes if proved:

- `CONNECTED_CONSISTENT_RELATIVE_BRC_COMPONENT_HAS_GLOBAL_Z2_TORSOR_AMBIGUITY`
- `ONE_EXACT_ANCHOR_SYNCHRONIZES_CONNECTED_COMPONENT`
- `ODD_PARITY_CYCLE_INCONSISTENCY_DETECTED`

Enumeration may verify tiny graphs but proof must be symbolic/algebraic.

## 3. Straightness as one exact relative edge law

Replay straight continuation only as an already proved downstream relation:

`b_(k+1) xor b_k = 0`.

Do not use straightness to create an absolute initial branch.

Show how a straight chain is determined up to one global complementary assignment before anchoring.

Then add a genuine independent anchor and show exact propagation.

## 4. Cross-scale upward/downward collapse coupling

Construct tiny exact two-level and three-level collapse systems.

Examples must include:

- fine bits `b_i`;
- a coarser collapse torsor `B`;
- exact algebraic relations derived from declared completion/collapse equations, not chosen weights.

Test both directions:

### Upward

Fine relative constraints induce a coarse relation or coarse feasible set.

### Downward post-credit

A coarse exact singleton or relative relation restricts compatible fine assignments.

The required question is whether repeated upward/downward exact constraint propagation converges to the same Boolean solution set as solving the full joint system at once.

If yes, freeze a finite exact message-passing/fixed-point theorem for the declared class.

If not, preserve the counterexample and identify what information was lost by local propagation.

No probability semantics.

## 5. Intrinsic gap-asymmetry carrier audit

For a scalar completion gap `L<q<U`, define the centered algebraic carrier

`eta(q;L,U)=2q-L-U`.

Under endpoint reflection `J(q)=L+U-q`, prove

`eta(J(q))=-eta(q)`.

Audit whether `eta` is, up to scale, the unique affine-linear translation-covariant endpoint-swap-odd carrier.

Controls:

- vector symmetric half-state: `L=-1,U=0,q=-1/2` gives `eta=0`;
- scalar square control: `L=4,U=9,q=5` gives `eta=-3`.

Critical firewall:

`eta != 0` is only a candidate tau-odd context carrier.

Do **not** freeze `sign(eta)` as an up/down selector unless independently forced by exact post-credit constraints. In particular, do not smuggle nearest-rounding into the theory.

Determine whether bare algebra still leaves two equivariant identifications between the eta-orientation context and branch torsor.

## 6. Exact post-credit anchoring

Use Stage-P singleton criterion.

For independently declared macro relations/certificates, substitute completion-neighbor forms and reduce to exact Boolean constraints.

Classify each tested context:

- `ABSOLUTE_SINGLETON_ANCHOR`
- `RELATIVE_EDGE_ONLY`
- `SYMMETRIC_NO_INFORMATION`
- `INCONSISTENT`

A positive anchor must exist independently of the branch it selects. Reject circular candidates such as branch-conditioned residue sign used to select that same branch.

At least one oriented toy control may be used to prove sufficiency, but it must be labeled as supplied context rather than naturally generated context.

## 7. Gauge/torsor interpretation audit

Determine whether two globally complementary assignments in an unanchored connected component are:

A. merely two coordinate/orientation descriptions of the same relative collapse structure; or
B. genuinely different downstream states under the currently frozen algebra.

Do not declare physical equivalence unless proved.

Use the precise language:

- `GLOBAL_Z2_TORSOR_AMBIGUITY`
- `GAUGE_EQUIVALENCE_NOT_ESTABLISHED` unless exact observable invariance is proved.

Audit which quantities are invariant under global bit complement and which are not.

## 8. Scalar 5 bridge

Keep scalar control explicit:

`5` between square-completed states `4,9` has legal branch torsor `{4,9}`.

The vector symmetric no-go does not automatically apply because `eta=-3 != 0`.

Test only:

- completion-neighbor legality;
- intrinsic eta carrier;
- exact contextual constraints;
- torsor-identification noncanonicity.

Do not declare `5->4` or `5->9` without an exact independently justified anchor/constraint.

## 9. Covariance

Required audits:

- global branch complement;
- coordinate permutation for vector transfer components;
- global inversion;
- affine transformation of completion layers when layer and state transform together;
- positive integer scale;
- large exact backgrounds near `10^36` without huge enumeration.

## 10. Required artifacts

At minimum:

1. `R059D_STAGE_Q_Z2_TORSOR_CONSTRAINT_GRAPH_PROTOCOL.json`
2. `R059D_STAGE_Q_COMPONENT_SOLUTION_THEOREM.json`
3. `R059D_STAGE_Q_STRAIGHT_CHAIN_SYNCHRONIZATION.json`
4. `R059D_STAGE_Q_CROSS_SCALE_UP_DOWN_CONSTRAINT_PROPAGATION.json`
5. `R059D_STAGE_Q_GAP_CENTERED_TAU_ODD_CARRIER.json`
6. `R059D_STAGE_Q_POST_CREDIT_ANCHOR_LEDGER.json`
7. `R059D_STAGE_Q_GLOBAL_COMPLEMENT_INVARIANT_AUDIT.json`
8. `R059D_STAGE_Q_SCALAR_5_CONTEXT_CONTROL.json`
9. `R059D_STAGE_Q_COVARIANCE_REGISTRY.json`
10. `R059D_STAGE_Q_TRIVIALITY_LEAKAGE_LEDGER.json`
11. deterministic checker source/output
12. report
13. manifest
14. frozen checkpoint

## 11. Allowed conclusions

Positive or negative results are equally valid.

Possible freezes include:

- `BRC_RELATIVE_CONSTRAINT_SYNCHRONIZATION_ESTABLISHED`
- `GLOBAL_Z2_TORSOR_AMBIGUITY_ESTABLISHED`
- `ONE_ANCHOR_GLOBAL_COMPONENT_INITIALIZATION_ESTABLISHED`
- `CROSS_SCALE_POST_CREDIT_FIXED_POINT_ESTABLISHED`
- `CROSS_SCALE_LOCAL_PROPAGATION_INSUFFICIENT`
- `CENTERED_GAP_TAU_ODD_CARRIER_ESTABLISHED`
- `CENTERED_GAP_CONTEXT_DOES_NOT_BY_ITSELF_SELECT_BRANCH`
- `BRC_INTERNAL_LAW_IS_RELATIONAL_NOT_POINTWISE` only if justified by exact results
- `ABSOLUTE_INITIAL_SELECTOR_STILL_NONIDENTIFIED`

Do not promote any physical interpretation.

After all artifacts/checker/checkpoint are frozen:

`STOP_FOR_DRIVER_REVIEW`
