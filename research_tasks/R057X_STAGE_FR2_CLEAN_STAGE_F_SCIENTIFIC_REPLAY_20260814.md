# RS-R057X-STAGE-FR2-CLEAN-STAGE-F-SCIENTIFIC-REPLAY

Researcher-ID: `EM-R057X-5E8C41`

Generation: `R057X`

Stage: `FR2 — EXACT-INPUT CLEAN SCIENTIFIC REPLAY OF STAGE F`

Purpose: resolve the scientific replay question left open by accepted Stage FR, using the exact accepted A-H and G-I joint-surface bytes and the unchanged Stage-F science contract. This is reconciliation/replay only. It does not authorize new science, new motif semantics, or D4 generator work.

## Frozen identity anchors

Accepted Stage FR:

- `R057X_STAGE_FR_FREEZE_IDENTITY_CHECKPOINT_SHA256`
  `dde3a3edd0a2af71885c6e686747e81cd96d15f692b51494f7416fd6625192c6`

Immutable Stage-F V1:

- checkpoint `4cf6a1fd4d748e1175e77503247f41706aacb4946802a3da7bd03a52a4fdad54`
- disposition `INSUFFICIENT`
- verdict `14285c351e076996d040e41d31b044bb5b66af195398adc4caeb34af8bf30e8f`
- exact-check `8889a44f2801fab2015bbadf8a376514e986b500a4b51f8ee37c3193b75a5edf`
- manifest `9cb0fb2c75533ad5b0afe4641122d67215573cf1315582a389f76a213ec370bc`
- V1 tar bundle `516bc759ae47ba9fbb12f7e3c272267b9a2e16cf2d2a3df9184ed3d4577417cc`

Original Stage-F taskbook remains the sole science contract:

- `research_tasks/R057X_MATCHED_RESIDUAL_HOTSPOT_ISOLATION_COVERAGE_CONTROL_20260814.md`
- source `8abe99ad7c2ea54cb112c7a96a4c92166ce47495`

No rule in that taskbook may be changed in FR2.

## Exact recovered A inputs

Accepted A Stage-H checkpoint:

- `R057_A_STAGE_H_JOINT_SURFACE_CHECKPOINT_SHA256`
  `bf3c30df26f7a4095935bfce2682e7f8b4bb834ec2c74b838a5d73b26b7e41dc`

The original accepted Stage-H bundle was recovered and yielded the exact original target bytes:

1. `R057_A_FROZEN_SAMPLE_RESIDUAL_MOTIF_EXPOSURE.json`
   - bytes `2089833`
   - SHA256 `4baad1a7c3528d9b147a3ee38fb5436a3e607a2141178a284f540bfc1ce5eaf3`

2. `R057_A_FROZEN_TRANSITION_NUISANCE_SURFACE.json`
   - bytes `50118`
   - SHA256 `ac58760ca6f460961e41287452127060e3e3d2dfd9cf069a65fc6029f1b06e6f`

Original accepted A Stage-H bundle SHA256:

`7985a4af5c118554411716b941633670b6429fcf905a72b6c166af9c5b457fd0`

These are recovered frozen bytes, not regenerated semantics.

## Exact G inputs

Accepted G Stage-I checkpoint:

- `R057G_STAGE_I_JOINT_SURFACE_CHECKPOINT_SHA256`
  `a963b2fa951435655885b7eca4ec1d01561825bbb712396aab3516405560171f`

Exact G surfaces:

1. `R057G_FROZEN_SAMPLE_RESIDUAL_MOTIF_EXPOSURE.json`
   - bytes `1467133`
   - SHA256 `f50c9cdab6143e6d1e5339bfb3079e30b56e70991bca40ce9225cfdcc2415c22`

2. `R057G_FROZEN_TRANSITION_NUISANCE_SURFACE.json`
   - bytes `44983`
   - SHA256 `14b198f6d1b87cc40454453e99046a946b7f841a6b76469fbbf2f84009b1e723`

## FR2 hard firewall

No coefficient refit.
No optimizer.
No symbolic regression.
No new teacher.
No teacher expansion.
No K expansion.
No new feature/operator/surrogate.
No motif-catalog expansion.
No parser/context/segmentation/assembly/readout mutation.
No cross-arm fitted rescaling.
No interpolation.
No R057Y gravity evidence.
No D4 generator invention.
No post-hoc threshold changes.
No change to nuisance-light ranking/tie/intersection rule.
No change to scale gate.
No change to quartiles.
No change to primary denominator.
No promotion of matched-only denominator.

Stage-F V1 artifacts remain immutable and must not be overwritten.

---

# LANE FR2-0 — EXACT INPUT AVAILABILITY / BYTE GATE

Before any science computation, the FR2 runtime must possess the four exact large-surface files named above.

For every file verify both exact byte length and SHA256.

Do not substitute:

- snippets;
- summaries;
- parsed-equivalent JSON;
- semantically equivalent recomputation;
- regenerated files with different bytes.

If any exact input is unavailable or fails byte/hash verification, stop with:

`FR2_HARD_STOP_EXACT_INPUT_UNAVAILABLE`

and publish only the availability audit. Do not execute the Stage-F science code.

Also verify the original Stage-F taskbook bytes/hash and the Stage-E/FR anchors.

---

# LANE FR2-1 — LOCK THE ORIGINAL STAGE-F SCIENCE CONTRACT

Freeze an FR2 rule registry reproducing the original Stage-F rules exactly, including at minimum:

- primary exposure = `motif_count / ALL eligible cyclic k=2..6 windows`;
- matched-catalog-only exposure = sensitivity only;
- G unmatched fraction retained explicitly;
- Stage-E residual-blind nuisance-light rule unchanged;
- `S_A=sqrt(3) R_A`;
- `S_G=R_G`;
- `|log(S_A/S_G)| <= log(4/3)`;
- no interpolation;
- no fitted cross-arm rescaling;
- quartiles HIGH `>=0.75`, LOW `<=0.25`;
- frozen 62 Stage-B motifs only;
- frozen D2 residuals primary;
- same support / LOW_SUPPORT logic;
- same BH policy if p-values are used;
- same five allowed primary dispositions.

Compute a machine-readable rule digest and compare against the V1/FR rule audit. Any substantive rule mismatch is:

`FR2_HARD_STOP_RULE_IDENTITY_MISMATCH`.

---

# LANE FR2-2 — CLEAN COMPLETE STAGE-F REPLAY

Execute the original Stage-F lanes F1-F6 from the four exact frozen surfaces, with no outcome-driven edits.

Recompute at minimum:

- catalog coverage audit;
- G unmatched-fraction residual association diagnostics;
- symmetric nuisance-light strata on both arms;
- 62-motif A enrichment table;
- 62-motif G enrichment table;
- all-sample all-eligible-denominator diagnostics;
- nuisance-light sensitivity;
- matched dimensionless-scale sensitivity;
- matched-only sensitivity;
- G low/high unmatched-fraction sensitivity;
- full 62-row cross-arm hotspot crosswalk;
- final five-way disposition.

Every derived value must be deterministic from the exact input bytes and frozen rule registry.

---

# LANE FR2-3 — V1 REPLAY COMPARISON

Compare the clean replay result to immutable V1 without overwriting V1.

Record exactly:

- whether every V1-computable field reproduces;
- which fields V1 marked unavailable/insufficient because the decisive joint surfaces were not executable in that runtime;
- whether clean replay preserves `INSUFFICIENT`;
- if not, which newly executable pre-existing Stage-F lanes deterministically change the disposition;
- whether the difference is fully explained by input-byte availability rather than a science-rule change.

The distinction must be explicit:

`V1_BYTE_IDENTITY_VALID`

is independent of

`V1_SCIENTIFIC_DISPOSITION_REPRODUCED`.

---

# LANE FR2-4 — COVERAGE-LIMITED CLAIM THRESHOLD

If and only if the clean replay returns `COMMON_MATCHED_MOTIF_SIGNAL_COVERAGE_LIMITED`, freeze the concrete motif IDs supporting that result.

For each such motif record:

- A support count;
- G support count;
- A primary all-eligible association direction/value;
- G primary all-eligible association direction/value;
- A high-minus-low absolute-residual enrichment;
- G high-minus-low absolute-residual enrichment;
- nuisance-light persistence;
- matched-scale persistence;
- matched-only denominator sensitivity;
- G unmatched-fraction sensitivity;
- whether any qualitative direction reverses under coverage control.

Then explain exactly why evidence is:

- stronger than `INSUFFICIENT`;
- weaker than `COMMON_MATCHED_MOTIF_HOTSPOT_ISOLATED`;
- not enough for `CATALOG_DOMAIN_MISMATCH_DOMINATES`.

If concrete motif evidence is absent, coverage-limited cannot be the replay disposition.

---

# LANE FR2-5 — FINAL RECONCILIATION VERDICT

Return exactly one reconciliation verdict:

1. `V1_SCIENTIFIC_DISPOSITION_REPRODUCED_AUTHORITATIVE`
2. `V1_BYTE_IDENTITY_VALID_BUT_CLEAN_REPLAY_YIELDS_DIFFERENT_DISPOSITION`
3. `FR2_HARD_STOP_EXACT_INPUT_UNAVAILABLE`
4. `FR2_HARD_STOP_RULE_IDENTITY_MISMATCH`
5. `FR2_REPLAY_INTERNAL_INCONSISTENCY`

If verdict (1):

- Stage-F V1 remains the authoritative science checkpoint.
- Do not create V2.

If verdict (2):

- V1 remains immutable historical byte identity;
- freeze a new **Stage-F V2 repair checkpoint** from the clean replay;
- the V2 provenance must state `SAME_FROZEN_SCIENCE_RULES / EXACT_INPUT_AVAILABILITY_REPAIR`;
- record the new five-way Stage-F disposition and concrete evidence;
- this is a repair checkpoint, not retroactive mutation of V1.

No D4 is authorized by FR2 itself. FR2 must stop for Driver even if a common hotspot is isolated.

---

# Required FR2 artifacts

At minimum:

- `R057X_STAGE_FR2_EXACT_INPUT_REGISTRY.json`
- `R057X_STAGE_FR2_RULE_IDENTITY_REGISTRY.json`
- `R057X_STAGE_FR2_CATALOG_COVERAGE_AUDIT.json`
- `R057X_STAGE_FR2_SYMMETRIC_NUISANCE_LIGHT_STRATA.json`
- `R057X_STAGE_FR2_MOTIF_RESIDUAL_ENRICHMENT_A.json`
- `R057X_STAGE_FR2_MOTIF_RESIDUAL_ENRICHMENT_G.json`
- `R057X_STAGE_FR2_CROSS_ARM_HOTSPOT_CROSSWALK.json`
- `R057X_STAGE_FR2_V1_REPLAY_COMPARISON.json`
- `R057X_STAGE_FR2_FINAL_VERDICT.json`
- `R057X_STAGE_FR2_REPLAY_CHECKPOINT.json`
- deterministic checker / exact-check output
- delivery manifest

Freeze and return:

`R057X_STAGE_FR2_REPLAY_CHECKPOINT_SHA256`

If and only if final verdict is `V1_BYTE_IDENTITY_VALID_BUT_CLEAN_REPLAY_YIELDS_DIFFERENT_DISPOSITION`, also freeze and return:

`R057X_STAGE_F_V2_MATCHED_RESIDUAL_HOTSPOT_CHECKPOINT_SHA256`

Then stop for Driver review.

## Epistemic label

`FREEZE_RECONCILIATION / EXACT_INPUT_CLEAN_REPLAY / NO_NEW_SCIENCE / NOT_THEOREM / NOT_CANONICAL`
