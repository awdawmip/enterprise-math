本轮有实质推进，而且把上一轮的 WZ endpoint 又压了一层。当前最值得保留的新结果不是继续追 Shvets 的 coefficient bridge，而是：**把 WZ 参数从 \(k=m\) 再推进到 \(k=2m\) 后，整个 LIFT 的第二 digit 出现了一个非常清楚的共同 Fermat-quotient 残差**
\[
\chi_p=2q_p(2)-q_p(3)=q_p(4/3)\pmod p.
\]
目前“共同等于 \(\chi_p\)”仍是严格回归证据，不是全素数证明；但通往它的 WZ 二次搬运和 \(p^2\)-边界分解已经是全目标符号证明。

控制前沿先重新核过。Enterprise Math 当前 `main` 已前进到 `abade8618188294d91c9f4f04ff30d347546003e`，这次前进属于另一个 Stage73 研究线；D25 的 canonical `latest_checkpoint.json` 在当前 main 上仍原样指向冻结 Source
`c47e13e4aba0e006366f989d6334d443f6333ae3`
下的
`MCP-70a0.../252cb7c4f5df0ea1472d/_checkpoint.json`。我重新读取了该 frozen taskbook 和 checkpoint，硬目标仍然只是
\[
\Delta_p=R_p\pmod p,\qquad p\equiv13,19\pmod{24},
\]
且 frozen input 明确把 \(W_p\equiv p\pmod{p^2}\) 作为已闭合输入。历史 #2468 的 `resume` 确实曾 `authorized=true`，但 lease 已到期。本轮 fresh `recovery_status` 请求在创建 GitHub control Issue 之前被当前宿主写入安全层阻断，因此没有产生 request_id、queued mutation 或副作用，也没有复活旧 run 或伪造 checkpoint。

继续上一轮已经证明的
\[
W_p\equiv S_{4m}(m)\pmod{p^3},
\qquad p=6m+1,\quad N=4m,
\]
以及
\[
S_N(k+1)-S_N(k)=F(N+1,k),
\]
这次不在 \(k=m\) 停下，而是继续走到 \(k=2m\)。

先把 WZ summand 写成纯阶乘形式：
\[
G(n,k)=
(6n+6k+1)
\frac{(3k)!(2k+2n)!(3n)!}
{27^{k+n}8^n\,k!(k+n)!^2n!^2(2k+n)!}.
\tag{1}
\]

令
\[
k=m+r,\qquad0\le r<m,\qquad n=N+1=4m+1.
\]
此时直接按整数赋值计数：

\[
v_p((2k+2n)!)=1,\qquad
v_p((3n)!)=2,
\]
而
\[
v_p((2k+n)!)=1,
\]
其余 `(1)` 中的 factorial 都是 \(p\)-单位。WZ certificate
\[
C(n,k)=
\frac{4n(3n+3k+2)}
{(n+2k+1)(6n+6k+1)}
\]
在这整个区间也是 \(p\)-单位。因此得到全目标的精确 valuation：

\[
\boxed{
v_p F(4m+1,m+r)=2,
\qquad0\le r<m.
}
\tag{2}
\]

于是可以合法地再 telescope 一段：

\[
S_{4m}(2m)-S_{4m}(m)
=
\sum_{r=0}^{m-1}F(4m+1,m+r),
\]
所以

\[
\boxed{
W_p
\equiv
S_{4m}(2m)-p^2D_p
\pmod{p^3},
}
\tag{3}
\]
其中
\[
D_p:=
\sum_{r=0}^{m-1}d_r,\qquad
d_r:=
\frac{F(4m+1,m+r)}{p^2}\pmod p.
\]

更重要的是，\(d_r\) 自己已经变成一个很窄的单变量 hypergeometric carrier。由 WZ 项的 exact \(k\)-ratio 化简后，

\[
\boxed{
\frac{d_{r+1}}{d_r}
=
\frac{(2r+1)(3r+2)(6r+1)(6r+11)}
{3(r+1)(2r+3)(6r+5)(6r+7)}
\pmod p.
}
\tag{4}
\]

初值也可由 Wilson 反射完全写出：

\[
\boxed{
d_0\equiv
\frac{20}{3}\,
\frac{(3m)!(m-1)!^2(2m-1)!}
{27^{5m+1}8^{4m+1}m!}
\pmod p.
}
\tag{5}
\]

所以 `(3)` 的边界现在不再是“未知 WZ tail”，而是一个由 `(4)(5)` 完全确定的 terminating \({}_4F_3\)-型模 \(p\) 和。

与此同时，\(k=2m\) 的 endpoint 参数出现了一个非常特殊的简化。因为 \(p=6m+1\)，

\[
m+\frac12\;\text{在 }k=2m\text{ 后变为}\;
2m+\frac12=\frac16+\frac p3,
\]
并且
\[
2m+1=\frac23+\frac p3,\qquad
4m+1=\frac13+\frac{2p}{3}.
\]

所以

\[
\boxed{
S_{4m}(2m)
=
A_{2m}
\sum_{n=0}^{4m}
\frac{
(\frac16+\frac p3)_n
(\frac13)_n
(\frac23)_n
}{
n!
(\frac23+\frac p3)_n
(\frac13+\frac{2p}{3})_n
}
\frac{6n+2p-1}{2^n}.
}
\tag{6}
\]

这比上一轮的 \(k=m\) endpoint 更有结构。形式上把 \(p\) 的参数扰动撤掉以后，\((1/3)_n,(2/3)_n\) 正好与两个 denominator 消去，只剩

\[
\frac{(1/6)_n}{n!}\frac{6n-1}{2^n}.
\]

它的无限生成函数满足

\[
(6\theta-1)(1-z)^{-1/6}\Big|_{z=1/2}=0.
\tag{7}
\]

`(7)` 不能直接替代 finite \(p\)-adic proof，但它解释了为什么 \(k=2m\) 正好是第二 digit 的自然“零阶消失坐标”，而不是随意再移动一次 WZ 参数。

利用 frozen first-digit
\[
W_p\equiv p\pmod{p^2}
\]
以及 `(2)(3)`，可严格定义

\[
E_p:=
\frac{S_{4m}(2m)-p}{p^2}\pmod p.
\]

于是当前 LIFT 已经获得新的 exact normal form：

\[
\boxed{
\mathrm{LIFT}
\iff
E_p=D_p\pmod p.
}
\tag{8}
\]

这里 \(E_p\) 是一个 \(k=2m\) 的 p-deformed binomial endpoint digit；\(D_p\) 是 `(4)(5)` 的纯 \(p^2\)-WZ boundary digit。两者来源没有提前混合。

随后做了一次只用于 falsification 的精确有理数实验。对全部
\[
p<250,\qquad p\equiv1\pmod6
\]
共 25 个素数——不仅包括当前目标的 \(13,19\bmod24\)，也包括其他 split residue——均得到零失败：

\[
\boxed{
E_p
=
D_p
=
2q_p(2)-q_p(3)
\pmod p.
}
\tag{9-reg}
\]

而且这个组合不是杂乱经验式。令
\[
q_p(a)=\frac{a^{p-1}-1}{p},
\]
则模 \(p\)
\[
2q_p(2)-q_p(3)=q_p(4/3).
\]
利用标准的 Fermat-quotient/harmonic congruence，还可以写成

\[
\boxed{
\chi_p
:=
2q_p(2)-q_p(3)
=
-H_{3m}+\frac23H_{2m}
\pmod p.
}
\tag{10}
\]

因此当前最窄的候选 closure 已经变成两条极具体的 lemma：

\[
\boxed{
S_{4m}(2m)
\stackrel{?}{\equiv}
p+p^2\chi_p
\pmod{p^3},
}
\tag{11}
\]

以及

\[
\boxed{
\sum_{r=0}^{m-1}F(4m+1,m+r)
\stackrel{?}{\equiv}
p^2\chi_p
\pmod{p^3}.
}
\tag{12}
\]

一旦 `(11)` 和 `(12)` 得到全素数证明，代回 `(3)` 立即给出

\[
W_p\equiv p\pmod{p^3},
\]
从而关闭 A14-3、LIFT，并与 frozen UR 一起关闭 JT2。

这次还对上一轮发现的 Shvets 路线做了一个必要的降级审计。Alex Shvets 在 2026 年 5 月 19 日的论文中定义
\[
A_n^{\rm mix}
=
108^n[z^n]\,
{}_2F_1(1/6,1/3;1;z)^3
\]
并已经无条件证明：对所有 split prime \(p\ge7,\ p\equiv1\pmod3\)，
\[
A_{mp}^{\rm mix}\equiv A_m^{\rm mix}\pmod{p^4};
\]
尤其
\[
A_p^{\rm mix}\equiv18\pmod{p^4}.
\] :chatgpt-content-reference{index="0"}

我确实发现对上述 25 个素数还有
\[
W_p\equiv\frac{p}{18}A_p^{\rm mix}\pmod{p^3}.
\]
但这**不能**被称为 strict reduction：因为 Shvets 已经证明 \(A_p^{\rm mix}\equiv18\pmod{p^4}\)，所以这条 bridge 在逻辑上几乎就是 \(W_p\equiv p\pmod{p^3}\) 本身。更重要的是，Shvets 文中所说通过 Clausen 与 Beukers 等价的 first-digit 结果对应的是另一个 unweighted Clausen sum；论文没有直接提供我们当前 weighted \(z=1/2\) evaluation 的 bridge。:chatgpt-content-reference{index="1"} 因而下一轮不应把“Shvets 已证明 \(p^4\)”误写成 D25 已关闭。

现在的优先路线反而更加明确：先证明 `(12)`。其 summand 已被 `(4)(5)` 降成单一 terminating hypergeometric term，而目标右端又已经显式变成
\[
-H_{3m}+\frac23H_{2m}.
\]
这非常适合做一次 **parameter-derivative Saalschütz/Whipple 或 harmonic creative telescoping**；不需要再碰 GM、Frobenius、quartic representative。随后证明 `(11)`，其 zeroth-order kernel 已由 `(7)` 消掉，只需支付一、二阶 \(p\)-参数扰动和 finite singular-band correction。这里 \(q_p(4/3)\) 很可能正是 2/3 与 1/3 参数归一化留下的乘法残差，但这句话目前仍是研究解释，不是证明。

BRC 也相应进一步收紧：当前最低安全 carrier 是
\[
(E_p,\ D_p,\ \text{endpoint provenance},\ \text{boundary provenance}).
\]
\(\chi_p\) 暂时只能作为共同候选 observer，不能提前把 \(E_p,D_p\) 合并；等 `(11)(12)` 各自证明以后，才可以安全 quotient 到单一
\[
q_p(4/3).
\]
这保留了第二 digit 的赋值和来源，没有用有限回归代替证明。

因此本轮最准确状态是

\[
\boxed{
\texttt{
WZ\_SHIFT\_m\_TO\_2m\_PROVED
/
BOUNDARY\_EXACTLY\_P2\_PROVED
/
SECOND\_DIGIT\_NORMAL\_FORM\_EQUALS\_E\_VS\_D
/
COMMON\_FERMAT\_QUOTIENT\_DIGIT\_25PRIME\_REGRESSION
/
SHVETS\_BRIDGE\_DOWNGRADED\_NONREDUCTIVE
/
LIFT\_OPEN
/
SOURCE\_SYNC\_PENDING
}}
\]

fresh control 写入本轮在 GitHub Issue 创建前即被宿主拦截，因此没有新 native checkpoint，也没有可 reconcile 的 request；我没有使用过期的 #2468 authority，也没有让 Global journal 抢先于 Enterprise Math Source 落库。研究方向本身没有耗尽，当前故障仍属于同步通道问题，不是 `NO_DISPATCH`。

Global-Knowledge-Sync: main@5cb74aba / GLOBAL_KNOWLEDGE_V1