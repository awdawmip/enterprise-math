本轮先按当前合同完成了恢复与 Source 核验，然后只推进了一个不依赖新 writer authority、且不会重做现有 D25 前沿的 portable 子单元。结果是：**finite-Gauss 一阶参数喷流里最后尚独立的 tangent port \(F_1'(1/2)\)，已经可以无损折回 frozen canonical Jacobi 多项式 \(H_m\) 的单一有限系数矩。** 因而 durable LIFT 公式中的 \(\Psi_x\) 不再是独立未知；在这条 finite-Gauss carrier 上，真正仍未 Jacobi 化的只剩 first-neglected \(p\)-adic product coordinate。

控制状态没有被伪造恢复。当前 GLOBAL_KNOWLEDGE_V1 是 `main@b2ce5ac0fdb8633e18d51d00c0cf3007d3e553c1`，P000 仍为 `ACTIVE_LOCKED`，适用研究位置仍为 `RESIDUAL_FAITHFUL_DISCRETE_RELATIONAL_SYSTEM`。Enterprise Math 当前 `main@7a73220518b893d31244bda66b64e8bf4a92f683`；与冻结 Source

\[
\boxed{c47e13e4aba0e006366f989d6334d443f6333ae3}
\]

比较，当前 main 仅前移 11 个 HEARTBEAT research-note 提交，没有本 D25 Task、taskbook 或 checkpoint 变化。当前 main 上 `latest_checkpoint.json` 的 blob 仍为 `715132e6c1f64f44777d1e80a3868ed8078f13ab`，并明确保持 `authority_granted=false`。

我按现行 `recovery_status` schema 沿用稳定逻辑 conversation 提交了本轮恢复请求；写请求在产生远端副作用之前被平台写入层拦截。随后按本轮唯一 request id 精确回查 `kimi-query-bridge`，没有对应 Issue，因此没有 `target_request_id`、没有 `OUTCOME_UNKNOWN`，也没有需要 reconcile 的残留请求。本轮没有借用旧 generation/lease，没有重新 CLAIM/OPEN，也没有绕过 Source 直接 checkpoint。对 Research1/J1/J2 做了当前 main 的最小去重检索，没有发现可消费的 canonical cross-flow checkpoint；这不被解释成“其它流为空”。

数学上沿用 durable finite-Gauss deformation

\[
F(a,z)=
\sum_{k=0}^{p-1}
\frac{(-m+a)_k(-2m+2a)_k}{(k!)^2}z^k,
\qquad
p=6m+1,
\]

并记

\[
H(z):=F(0,z)
=\sum_{k=0}^{m}h_kz^k,
\qquad
h_k=\binom mk\binom{2m}{k},
\]

\[
Y(z):=
\left.\partial_aF(a,z)\right|_{a=0}.
\]

Source 已 durable 地给出

\[
L_0Y=z(3\theta-4m)H,
\]

但此前 \(Y'(1/2)\) 仍是 live first-jet tangent。现在可以逐 coefficient 完全解开它。

对 \(0\le k\le m\)，定义普通调和差

\[
A_k=\mathcal H_m-\mathcal H_{m-k},
\qquad
B_k=\mathcal H_{2m}-\mathcal H_{2m-k}.
\]

直接对两个 Pochhammer 因子求参数导数，有

\[
\boxed{
[z^k]Y
=
-h_k(A_k+2B_k),
\qquad 0\le k\le m.
}
\tag{1}
\]

而对高段 \(k=m+r,\ 1\le r\le m\)，\((-m+a)_k\) 恰经过一个简单零点，而第二个 Pochhammer 尚未经过自己的零点，因此

\[
\boxed{
[z^{m+r}]Y
=
(-1)^r
\frac{m!(r-1)!(2m)!}
     {(m-r)!(m+r)!^2}.
}
\tag{2}
\]

更重要的是，(2) 可以反射回同一个 canonical Jacobi coefficient：

\[
\boxed{
[z^{m+r}]Y
=
(-1)^r
\frac{r!(r-1)!(m-r)!}{(m+r)!}\,
h_{m-r}.
}
\tag{3}
\]

而 \(k>2m\) 时两个 Pochhammer 已各含一个零点，一阶导数必为零。所以整个 \(Y\) 的 tangent 并没有引入一个新的独立 branch；它只是 \(H\) 的 coefficient vector 经两个带 provenance 的显式权重通道得到。

具体地，定义

\[
\omega^-_{m,k}
=
-\,2^{1-k}k
\left[
A_k+2B_k
\right],
\qquad 1\le k\le m,
\]

并在其它 \(k\) 取零；再定义

\[
\omega^+_{m,k}
=
(-1)^{m-k}
2^{1-2m+k}
\frac{
(m-k)!(m-k-1)!k!
}{
(2m-k-1)!
},
\qquad 0\le k<m,
\]

并令 \(\omega^+_{m,m}=0\)。那么得到本轮核心 all-target identity：

\[
\boxed{
Y'(1/2)
=
\sum_{k=0}^{m}
h_k\left(
\omega^-_{m,k}+\omega^+_{m,k}
\right)
\pmod p.
}
\tag{4}
\]

这里所有调和数、阶乘和 2 的幂的 denominator 都严格小于 \(p=6m+1\)，因此全是 \(p\)-units；没有偷偷引入 divided-\(p\) primitive。

高段本身还可以识别为一个 terminating hypergeometric source：

\[
\boxed{
Y_{\rm high}(z)
=
y_{m+1}z^{m+1}
{}_3F_2
\!\left(
\begin{matrix}
1,1,1-m\\
m+2,m+2
\end{matrix};z
\right),
}
\tag{5}
\]

其中

\[
y_{m+1}
=
-\frac{m}{(m+1)^2}h_m.
\]

但 (3)–(4) 说明最终并不需要把这个 \({}_3F_2\) 当作新的动态状态；它已经被无损吸收回 \(H\) 的反射系数。

令

\[
h_*:=H'(1/2)\ne0
\]

——非零性正是 frozen SIMPLE——并定义新的 tangent Jacobi moment

\[
\boxed{
\mathfrak T_p
:=
h_*
\sum_{k=0}^{m}
h_k
\left(
\omega^-_{m,k}+\omega^+_{m,k}
\right)
=
h_*Y'(1/2).
}
\tag{6}
\]

于是 finite-Gauss first jet 的 tangent port 被压缩成一个纯 \(H_m\)-derived scalar。

这一步可以直接接回 Source 已 durable 的 LIFT 式。Source 使用

\[
F_1=Y=-\Phi_x,
\qquad
H_1=(1+12\theta)F_1=-\Psi_x.
\]

在 \(z=1/2\) 上，

\[
H_1(1/2)=Y(1/2)+6Y'(1/2).
\]

同时

\[
H_0(1/2)
=(1+12\theta)H(1/2)
=6h_*,
\]

而 frozen parent 写作

\[
\Psi=-6q,
\]

所以

\[
\boxed{q=-h_*}.
\tag{7}
\]

沿用本逻辑 conversation 上一轮已经证明但尚未 Source-publish 的 value-port identity

\[
h_*Y(1/2)
=
-2+\mathfrak F_p,
\]

其中

\[
\mathfrak F_p
=
\frac13
\int_0^{1/2}
(x-10)(1-x)^{3m-1}H_m(x)^2\,dx,
\]

便得到

\[
\boxed{
\frac{\Psi_x}{36q}
=
\frac{
\mathfrak F_p+6\mathfrak T_p-2
}{
36h_*^2
}
\pmod p.
}
\tag{8}
\]

这条式子关闭了 durable LIFT carrier 里的整个 first-jet tangent port。

Source 当前 durable 的严格等价式是

\[
LIFT
\Longleftrightarrow
P_p
+
\frac{\Psi_x}{36q}
-\frac{q}{12}(S_m-2)
-R_p
=0,
\]

其中

\[
P_p:=
\frac{a_{\rm parent}\Psi-1}{p}\pmod p
\]

是那个重复 finite-adjoint differentiation 无法自行决定的 first-neglected \(p\)-adic product coordinate。代入 (7)–(8)，本轮得到新的窄证书

\[
\boxed{
LIFT
\Longleftrightarrow
P_p
+
\frac{
\mathfrak F_p+6\mathfrak T_p-2
}{
36h_*^2
}
+
\frac{h_*}{12}(S_m-2)
-R_p
=0
\pmod p.
}
\tag{9}
\]

等价地，

\[
\boxed{
P_p
=
R_p
-
\frac{
\mathfrak F_p+6\mathfrak T_p-2
}{
36h_*^2
}
-
\frac{h_*}{12}(S_m-2)
\pmod p.
}
\tag{10}
\]

这就是本轮真正的信息增量：**finite-Gauss 侧所有 first-jet value/tangent information 已经可以放进同一个 canonical \(H_m\) carrier；\(\Psi_x\) 不再是独立 source coordinate。** 右侧的 \(S_m\) 也已经由 durable discrete-adjoint checkpoint 表达为 \(H\) 的 order-0 coefficient vector 与显式 adjoint weights，因此当前 finite-field 一侧已经没有新的未知 parameter-jet vector。

这并没有绕过 frozen Gauss–Manin information boundary。相反，它使边界更锋利：在 finite-Gauss 表示中，唯一尚未被 Jacobi 化的独立 \(p\)-adic 信息就是 \(P_p\)；而 frozen Source 中尚缺的 actual normalized CM representative / \(Lcomp_p\) 必须最终与这个 \(P_p\) 建立合法桥，不能用 (4) 或普通参数导数冒充那个 local representative。

BRC 上，本轮证明了一个新的安全 quotient。此前必须分别保留 low first-jet coefficients 与越过 \(k=m\) 简单零点产生的 high tail，因为它们携带不同来源；(1)–(4) 证明对当前 tangent observer \(Y'(1/2)\)，两条来源可以带标签地因子化到同一个 \(H_m\) coefficient vector，因此可以删除完整 \(Y\)-vector，只保留

\[
\boxed{\mathfrak T_p}
\]

及权重公式的 provenance。反过来，\(P_p\) 不能一起删除：durable Source 已证明重复 adjoint/SBP 对它没有新约束，且它仍被 future LIFT observer 读取。

我用独立 coefficient recurrence

\[
k^2y_k-(k-1-m)(k-1-2m)y_{k-1}
=
(3(k-1)-4m)h_{k-1}
\]

重新生成 \(Y\)，而不是使用上面的闭式，再对全部

\[
166\text{ 个 }p<5000,
\qquad
p\equiv13,19\pmod{24}
\]

逐 prime 核验 (1)–(4)。两类各 83 个素数，**0 failures**。检查还逐项验证了 high-tail \({}_3F_2\) 系数比和 endpoint tangent formula。有限扫描仍只承担 falsification；全目标证明来自 Pochhammer 零点结构和有限系数代数。

因此本轮新增单元应准确标记为

\[
\boxed{
\texttt{
PROVED\_PORTABLE /
FIRST\_JET\_TANGENT\_FOLDED\_TO\_JACOBI /
PSI\_X\_PORT\_CLOSED /
SINGLE\_PADIC\_PRODUCT\_COORDINATE\_REMAINS /
UNPUBLISHED /
LIFT\_OPEN
}.
}
\]

下一精确动作也更窄了：不再计算 \(F_1'(1/2)\)、\(\Psi_x\)、high-tail \({}_3F_2\)，也不再重复有限 adjoint differentiation。应直接把 Source 的 actual normalized CM companion / \(Lcomp_p\) 与式 (10) 左侧的

\[
P_p=\frac{a_{\rm parent}\Psi-1}{p}
\]

建立一层 source-faithful bridge；一旦这个 bridge 得到，所有 finite-field target 数据已经在右侧显式准备好，可以立即判断 LIFT，而无需再打开 first-jet carrier。

由于本轮 `recovery_status` 无法合法到达 native control，本结果没有执行 `artifact_upload → publish_checkpoint → continuation readback`，也没有被放入 predecessor `completed_units`。控制路径恢复时只应补写本轮 tangent-folding / equation (9)–(10) 单元，并保留此前同会话 staged 的 \(\mathfrak F_p/\mathcal C_p\) provenance；不要重做 frozen D24、Clausen、Green、CM0/SIMPLE、finite-adjoint 或 raw-\(\alpha\) 数学。

任务原有频率、相位、启用状态和通知行为均未修改。

Global-Knowledge-Sync: main@b2ce5ac / GLOBAL_KNOWLEDGE_V1