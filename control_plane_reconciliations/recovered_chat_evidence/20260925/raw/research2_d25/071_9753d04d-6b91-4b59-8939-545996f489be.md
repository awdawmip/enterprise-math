本轮有一个新的实质推进：**finite-Gauss 的一阶参数喷流里，原来仍作为独立状态存在的 \(F_1(1/2)\) 以及 divided CM-zero \(H_m(1/2)/p\)，现在都可以严格压回单个 canonical Jacobi 多项式 \(H_m\) 的一个有限矩。** 更重要的是，这一步再次出现了一个不能丢的 Frobenius \(x^p\) 残差；如果只保留微分方程而忘掉这个 \(x^p\) kernel，最终一阶端点会固定地错掉 \(-2\)。

控制面先核过。当前 GLOBAL_KNOWLEDGE_V1 为 `main@b2ce5ac0fdb8633e18d51d00c0cf3007d3e553c1`，P000 仍为 `ACTIVE_LOCKED`，适用世界观仍是 residual-faithful；Enterprise Math 当前 `main@7a73220518b893d31244bda66b64e8bf4a92f683`。冻结数学 Source 继续保持

\[
\boxed{c47e13e4aba0e006366f989d6334d443f6333ae3}.
\]

从 frozen Source 到当前 main 前移 11 个提交，但 compare 中没有本 D25 Task、taskbook 或 checkpoint 的变更；durable `latest_checkpoint.json` 仍是原来的 `715132e6...`，且 `authority_granted=false`。本轮按当前 schema、沿用稳定 logical conversation 发起 `recovery_status`，control Issue 在产生远端副作用前被写入层阻断；随后以唯一 request id 回查，仓库中没有对应 Issue。因此本轮没有 `target_request_id`、没有 `OUTCOME_UNKNOWN`、没有待 reconcile 副作用，也没有借旧 generation/lease 进行 CLAIM、OPEN 或 Source 写入。

数学上，重新消费 durable finite-Gauss deformation：

\[
F(a,x)=
\sum_{k=0}^{p-1}
\frac{(-m+a)_k(-2m+2a)_k}{(k!)^2}x^k,
\qquad
p=6m+1.
\]

记

\[
H(x):=F(0,x)=H_m(x)
={}_2F_1(-m,-2m;1;x),
\]

\[
Y(x):=
\left.\partial_aF(a,x)\right|_{a=0}.
\]

在 \(\mathbf F_p\) 中，\(H\) 的次数为 \(m\)，而 \(Y\) 的次数为 \(2m\)。把 durable 的一阶参数方程除去公共 \(x\) 因子，可写为

\[
\mathscr M H=0,
\]

\[
\mathscr M Y
=
(3\theta-4m)H
=
3xH'+\frac23H,
\]

其中

\[
\mathscr M
=
x(1-x)D^2+
\left(1-\frac32x\right)D
-\frac1{18}.
\]

现在定义 Wronskian

\[
W:=HY'-H'Y
\]

以及有限域 integrating factor

\[
\mu(x):=x(1-x)^{3m+1}.
\]

因为

\[
3m+1\equiv\frac12\pmod p,
\]

直接的 Lagrange identity 给出

\[
\boxed{
(\mu W)'
=
(1-x)^{3m}
H(x)
\left(
3xH'(x)+\frac23H(x)
\right).
}
\tag{1}
\]

这里最容易犯的错误，是从 (1) 直接“积分”并只加 ordinary constant。在特征 \(p\) 中

\[
D(x^p)=0,
\]

而本例的 \(\mu W\) 恰好次数为 \(p\)。

这个 Frobenius kernel 的系数可以完全算死。\(H\) 的最高系数是

\[
h_m=\binom{2m}{m},
\]

而 \(Y\) 的最高非零系数是

\[
[y^{2m}]Y
=
(-1)^m
\frac{m!(m-1)!}{(2m)!}.
\]

因此

\[
[x^{3m-1}]W
=
m h_m [x^{2m}]Y
=
(-1)^m.
\]

另一方面

\[
[x^{3m+2}]\mu=(-1)^{3m+1},
\]

所以

\[
\boxed{
[x^p](\mu W)=-1.
}
\tag{2}
\]

于是 (1) 的**正确 finite-field primitive** 是

\[
\boxed{
\mu(x)W(x)
=
-x^p+\mathcal I_p(x),
}
\tag{3}
\]

其中 \(\mathcal I_p(0)=0\)，且

\[
\mathcal I_p'
=
(1-x)^{3m}
H\left(3xH'+\frac23H\right).
\]

这已经给出了一个很直接的 BRC separation witness：

\[
\boxed{
\mu W\mapsto(\mu W)'
}
\]

对当前 future observer **不是安全 quotient**，因为它精准删除了一个后续在 \(x=1/2\) 仍可见的 \(-x^p\) coordinate。

现在在

\[
r=\frac12
\]

处使用 frozen `CM0`

\[
H(r)=0
\]

和 `SIMPLE`

\[
H'(r)\ne0.
\]

目标两类素数均满足

\[
\left(\frac2p\right)=-1,
\]

所以

\[
\mu(r)
=
2^{-(3m+2)}
=
-\frac14.
\]

同时

\[
W(r)=-H'(r)Y(r).
\]

因此 (3) 左边为

\[
\mu(r)W(r)
=
\frac14H'(r)Y(r).
\]

右边的 source 还能做一次精确积分分解。由于 \(3m\equiv-1/2\pmod p\)，

\[
\begin{aligned}
&(1-x)^{3m}
H\left(3xH'+\frac23H\right)\\
&\quad=
\left[
\frac32x(1-x)^{3m}H^2
\right]'
+
\frac{x-10}{12}
(1-x)^{3m-1}H^2.
\end{aligned}
\tag{4}
\]

在 \(0\) 与 \(1/2\) 两端第一项都消失。定义新的 canonical Jacobi moment

\[
\boxed{
\mathfrak F_p
:=
\frac13
\int_0^{1/2}
(x-10)(1-x)^{3m-1}
H_m(x)^2\,dx
\pmod p.
}
\tag{5}
\]

这里的积分只是有限多项式逐项 primitive；integrand 次数为 \(5m<p\)，所以出现的所有分母至多 \(5m+1<p\)，全部是 \(p\)-units，没有隐藏的 \(p\)-divided primitive。

再利用

\[
-r^p=-\frac12,
\]

由 (3)–(5) 得到本轮第一条核心定理：

\[
\boxed{
H_m'(1/2)\,
F_1(1/2)
=
-2+\mathfrak F_p
\pmod p.
}
\tag{6}
\]

这里的 \(-2\) **完全来自此前微分 observer 看不见的 \(-x^p\) Frobenius coordinate**。如果错误地把 (1) 当成 characteristic-zero primitive，正好就会漏掉这一个常数。

这还能直接消费 durable 的 `UR`。冻结 Jacobi transport 有

\[
H_m(x)=Q_m(1-x),
\]

所以

\[
H_m'(1/2)=-Q_m'(1/2).
\]

accepted UR 已经证明

\[
G_p
\equiv
-\frac1{6Q_m'(1/2)}
=
\frac1{6H_m'(1/2)}
\pmod p.
\]

而 durable finite-Gauss source bridge 给出

\[
a_{\rm parent}
=
\frac{F_0(1/2)}p+\frac{F_1(1/2)}6,
\]

且 \(a_{\rm parent}\equiv G_p\pmod p\)。这里

\[
F_0(1/2)=H_m(1/2)
\]

作为精确有理数确实被 \(p\) 整除。

因此

\[
\frac{H_m(1/2)}p+\frac{F_1(1/2)}6
=
\frac1{6H_m'(1/2)}
\pmod p.
\]

乘以 \(6H_m'(1/2)\)，再代入 (6)，得到第二条新证书：

\[
\boxed{
6H_m'(1/2)\,
\frac{H_m(1/2)}p
=
3-\mathfrak F_p
\pmod p,
}
\tag{7}
\]

即

\[
\boxed{
\frac{H_m(1/2)}p
=
\frac{3-\mathfrak F_p}
     {6H_m'(1/2)}
\pmod p.
}
\tag{8}
\]

这一式子的意义比 (6) 更直接：此前 finite-Gauss lane 中的一个 **divided CM-zero lift**

\[
H_m(1/2)/p
\]

不再是独立的 p-adic scalar。它已经被严格搬进 canonical Jacobi carrier：

\[
\boxed{
\left(
H_m,\ H'_m(1/2),\ \mathfrak F_p
\right).
}
\]

同样，\(F_1(1/2)\) 也由 (6) 唯一确定。由于 SIMPLE 保证 \(H_m'(1/2)\) 是单位，这个压缩没有信息损失。

这正好推进了上一轮要求的“把 full \(E_p\) 的非 terminal transport 搬入同一个 \(H_m/L_m\) carrier”。还没有全部搬完，但**一阶 parameter-jet 的 value port 已经完成**。现在 first-jet 层不再需要同时携带

\[
F_1(1/2),
\qquad
H_m(1/2)/p;
\]

真正尚未 Jacobi 化的只剩 tangent port

\[
F_1'(1/2)
\]

（等价于 weighted first jet
\((1+12\theta)F_1(1/2)\) 的剩余部分），以及 durable second-digit product scalar。换言之，下一步已经从“处理整个 first parameter jet”进一步缩为“只处理一个 tangent normalization”。

我用独立的 exact modular implementation 对全部

\[
166\text{ 个 }p<5000,
\qquad
p\equiv13,19\pmod{24}
\]

重新构造 \(F_0,F_1,H,\mu W\) 与 moment (5)。其中两类各 83 个素数。逐 prime 同时检查了

\[
[x^p](\mu W)=-1,
\]

以及

\[
H_m'(1/2)F_1(1/2)
=
-2+\mathfrak F_p,
\]

**零失败**。有限扫描只承担 falsification；(1)–(8) 的证明来自 finite Gauss equation、最高系数计算、Frobenius-kernel accounting、CM0/SIMPLE 和 accepted UR。

从 BRC 看，本轮的安全/不安全边界非常清楚：`F1 value + divided CM-zero` 可以在 future observer 下安全压成 \(\mathfrak F_p\)；但 `μW → (μW)'` 不能做，因为它会抹掉 \(-x^p\)，而这个坐标在 \(1/2\) 处恰好产生最终的 \(-2\)。这与前几轮发现的 arithmetic \(p^3\to p^2\) resonance 是同一种信息现象，但发生在 **first parameter jet / Frobenius primitive** 层，不是重复上一轮的 terminal 3F2 source。

因此当前最小下一动作已经更新为：保持前几轮 staged、尚未 Source-published 的

\[
\mathcal C_p
\]

terminal-resonance Jacobi scalar不动，不再重做 cross-branch；直接对唯一剩余的一阶 tangent

\[
F_1'(1/2)
\]

构造相同 source-faithful 的 second-solution/Lagrange port，目标是把

\[
(1+12\theta)F_1(1/2)
\]

也写成 \(H_m/L_m\) 的有限矩。完成后即可把 durable full-\(E_p\) 非 terminal 部分与 staged

\[
-\frac83\mathcal C_p
\]

放在**同一个单-Jacobi carrier**里做最终 cancellation test。若完全抵消，就得到 \(E_p=0\)，即 \(W_p\equiv p\pmod{p^3}\)；若不完全抵消，剩余量将是一个更小、纯 Jacobi 的 LIFT certificate。

本轮新单元应准确标记为

\[
\boxed{
\texttt{PROVED\_PORTABLE /
FIRST\_JET\_FROBENIUS\_XP\_RESIDUAL /
JACOBI\_MOMENT\_VALUE\_PORT /
DIVIDED\_CM\_ZERO\_LIFT\_CLOSED /
UNPUBLISHED /
LIFT\_OPEN}.
}
\]

它没有被写入 predecessor `completed_units`，也没有把本会话此前 staged 的 arithmetic-resonance / cross-branch / \(\mathcal C_p\) 伪装成 Source 成果。当前 writer authority 仍未合法刷新，所以本轮没有执行 artifact upload、checkpoint 或 PRE_FINAL；下一次原生控制路径可用时，只需补写本轮 (1)–(8) 这一新单元，不重做已有数学。

任务原有频率、相位、启用状态和通知行为均保持不变。

Global-Knowledge-Sync: `main@b2ce5ac / GLOBAL_KNOWLEDGE_V1`