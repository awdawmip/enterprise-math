本轮先完成了 control/source 的重新核验。GLOBAL_KNOWLEDGE_V1 当前 canonical `main` 为 `ff4e73fe45dc1b8811640d1882b3d48925ae1734`，P000 仍为 `ACTIVE_LOCKED`；Enterprise Math 当前 `main` 为 `e147ec201fe3fd84492e47d0ed3da8455137958f`。本任务冻结数学 Source 继续钉在

\[
\boxed{c47e13e4aba0e006366f989d6334d443f6333ae3}.
\]

从该 frozen Source 到当前 `main` 共前移 10 个提交，但 compare 结果没有任何本 Task、task record 或 taskbook 文件变化；更直接地，当前 `main` 与 frozen Source 上的 `latest_checkpoint.json` blob 完全相同，都是 `715132e6c1f64f44777d1e80a3868ed8078f13ab`，且仍明确写着 `authority_granted=false`。因此没有新的 canonical D25 frontier 需要抢先消费，也没有 source-pin 漂移。

我按当前 `recovery_status` schema、沿用稳定逻辑 conversation_id 发起了本轮恢复请求，但该 GitHub control Issue 在真正到达私有仓库前被平台安全层阻断。随后用本轮唯一 request id 回查 `awdawmip/kimi-query-bridge`，结果为空，因此本轮没有 Issue、没有 `target_request_id`、没有 `OUTCOME_UNKNOWN`，也没有需要 reconcile 的副作用。没有借用旧 lease、没有新建身份、CLAIM 或 OPEN。这个是当前 control 写路径的能力阻断，不是 `NO_DISPATCH`，也没有被永久化；研究任务继续保持启用。

数学上，本轮实际完成了上一轮
\[
\text{Jacobi reflection bridge}
\longrightarrow
\text{gauged reflected carrier}
\]
之后的下一步：**把后半段 \(2m\) 长度的 gauged reflected tail 精确消掉，只剩一个 canonical low-branch moment 和前 \(m\) 步 centered response。** 同时得到一个很符合 residual-faithful 位置的现象：在特征 \(p\) 下，微分算子看不见的 \(x^p\) Frobenius 常数，正好是连接两个局部 Gauss branch 时不能丢掉的结构残差。

仍令

\[
p=6m+1,\qquad
r=3m+1=\frac{p+1}{2},\qquad
q=3m-1,
\]

并沿用已经 staged 的两个局部 Gauss 对象

\[
A(x)
=
{}_2F_1(-m,-2m;3m+1;x)
=
\sum_{i=0}^{m}A_i x^i,
\]

\[
B(x)
=
\sum_{j=0}^{q}
\frac{(m+1)_j(2m+1)_j}
     {(3m+2)_j\,j!}x^j,
\qquad
Z(x):=x^rB(x).
\]

它们对应的二阶算子为

\[
\mathscr M
=
x(1-x)D^2
+
\left(\frac12-\frac32x\right)D
-\frac1{18}.
\]

前轮已经得到

\[
\mathscr M A=0,
\qquad
\mathscr M Z=\rho_p x^{p-1},
\]

并把 terminal amplitude 闭合为

\[
\boxed{
\rho_p=\frac14D_m^2,
\qquad
D_m:=\binom{3m}{m}.
}
\]

本轮定义 Wronskian

\[
W(x):=A(x)Z'(x)-A'(x)Z(x).
\]

把方程除以 \(x(1-x)\) 后，一阶系数是

\[
P(x)
=
\frac{1-3x}{2x(1-x)}
=
\frac1{2x}-\frac1{1-x}.
\]

通常 integrating factor 会写成 \(x^{1/2}(1-x)\)。在当前有限域中可以合法地取

\[
\boxed{
\mu(x)=x^r(1-x)
}
\]

因为

\[
r=\frac{p+1}{2}\equiv\frac12\pmod p.
\]

Lagrange identity 因而给出

\[
\boxed{
(\mu W)'
=
\rho_p x^{p+r-2}A(x).
}
\tag{1}
\]

这里若像普通特征零计算一样“积分后只加一个常数”，会丢掉关键数据。在 \(\mathbf F_p[x]\) 中

\[
d(x^p)=0.
\]

而 \(x\to0\) 时

\[
A(x)=1+O(x),\qquad
Z(x)=x^r+O(x^{r+1}),
\]

所以

\[
\mu W
=
r x^{2r-1}+O(x^{2r})
=
r x^p+O(x^{p+1}).
\]

因此 (1) 的正确有限域 primitive 不是零常数，而是

\[
\boxed{
\mu(x)W(x)
=
r x^p
+
\rho_p
\sum_{i=0}^{m}
A_i
\frac{x^{p+r-1+i}}
     {p+r-1+i}.
}
\tag{2}
\]

所有分母都是 \(p\)-units，因为模 \(p\) 后为

\[
p+r-1+i\equiv3m+i,
\qquad
0\le i\le m<p.
\]

这一项

\[
\boxed{r x^p}
\]

就是本轮新的 structural residual。它在 derivative observer 下完全不可见，但在 \(x=1/2\) 的后续 observer 下非零；所以 quotient

\[
\mu W\quad\mapsto\quad(\mu W)'
\]

对当前 declared future 并不安全。

现在使用上一轮已经证明的 reflection/CM0：

\[
D_mA(x)=H_m(1-x),
\]

\[
A(1/2)=0,
\qquad
D_mA'(1/2)=-H_m'(1/2)\ne0.
\]

对目标两类素数都有

\[
\left(\frac2p\right)=-1,
\qquad
2^{-3m}=-1.
\]

定义一个新的 canonical low-branch connection moment

\[
\boxed{
\mathfrak S_p
:=
D_m^2
\sum_{i=0}^{m}
\frac{A_i2^{-i}}{3m+i}
\pmod p.
}
\tag{3}
\]

把 (2) 代入 \(x=1/2\)，由于

\[
\mu(1/2)W(1/2)
=
-\frac18A'(1/2)B(1/2),
\]

而

\[
r2^{-p}=\frac14,
\]

并且源项恰为

\[
-\frac{\rho_p}{2}
\sum_{i=0}^{m}\frac{A_i2^{-i}}{3m+i},
\]

得到新的 exact connection formula

\[
\boxed{
A'(1/2)B(1/2)
=
-2+\mathfrak S_p.
}
\tag{4}
\]

再用 Jacobi reflection，

\[
A'(1/2)=-\frac{H_m'(1/2)}{D_m},
\]

于是第二局部 branch 的 endpoint 被完全压回 canonical low branch：

\[
\boxed{
B(1/2)
=
\frac{
D_m(2-\mathfrak S_p)
}{
H_m'(1/2)
}.
}
\tag{5}
\]

这里没有把 \(B\) “设为可忽略”，而是证明了它的 endpoint 不需要作为独立动态状态保存。

\(\mathfrak S_p\) 本身也没有引入新的第二支路 provenance。因为

\[
2^{-3m}=-1,
\]

它等价于一个纯 low-Jacobi finite antiderivative：

\[
\boxed{
\mathfrak S_p
=
-D_m
\int_{1/2}^{1}
(1-y)^{3m-1}H_m(y)\,dy
\pmod p,
}
\tag{6}
\]

这里的“积分”只是有限多项式逐项 primitive；所有出现的分母都已证明为 \(p\)-units，不涉及连续性假设。

接着把它与上一轮 gauged reflected kernel 真正接起来。记

\[
K_s:=C_s\chi_s,
\qquad
\chi_s=\frac{(3m+2)_{s-1}}{(s-1)!}.
\]

上一轮已有 coefficient bridge

\[
\frac{B_{q-s+1}}{b_q}
=
10\,2^{-(s-1)}K_s
\]

以及

\[
b_q=-\frac9{20}D_m^2.
\]

把 \(j=q-s+1\) 反代并在 \(x=1/2\) 求和，所有 \(2\)-幂再次塌成公共因子：

\[
B(1/2)
=
-20b_q
\sum_{s=1}^{3m}K_s
=
9D_m^2
\sum_{s=1}^{3m}K_s.
\]

与 (5) 比较，得到整个长度 \(3m\) gauged reflected branch 的**总质量闭式**：

\[
\boxed{
\sum_{s=1}^{3m}K_s
=
\frac{
2-\mathfrak S_p
}{
9D_mH_m'(1/2)
}
\pmod p.
}
\tag{7}
\]

这直接消费了上一轮尚存的后 \(2m\) tail。

回忆上一轮 staged residual：

\[
\Lambda_p
=
D_m^2
\left[
\sum_{s=1}^{m}
K_s\bigl((m-s)P_s+Q_s\bigr)
+
Q_\infty
\sum_{s=m+1}^{3m}K_s
\right],
\]

其中

\[
Q_\infty
=
-\frac{H_m'(1/2)}{2D_m}.
\]

把 tail 写成“总和减前 \(m\) 项”，再代入 (7)，定义 centered prefix response

\[
\boxed{
R_s
:=
(m-s)P_s+Q_s-Q_\infty,
}
\]

便得到本轮最重要的 strict reduction：

\[
\boxed{
\Lambda_p
=
\frac{\mathfrak S_p-2}{18}
+
D_m^2
\sum_{s=1}^{m}
K_sR_s
\pmod p.
}
\tag{8}
\]

因此上一轮还需要动态携带的

\[
K_{m+1},\ldots,K_{3m}
\]

现在已经全部安全删除；它们对 future observer 的作用被一个 canonical low-branch moment \(\mathfrak S_p\) 完整替代。

而且 \(R_s\) 还能用 CM0 后的 low coefficient tail直接写成

\[
\boxed{
R_s
=
-\sum_{i=s}^{m}
(m-s+i)\,u_i,
\qquad
u_i:=A_i2^{-i}.
}
\tag{9}
\]

所以当前 portable D25 residual 的实际状态已经从上一轮的

\[
\text{长度 }3m\text{ gauged reflected response}
\]

缩为

\[
\boxed{
\mathfrak S_p
\quad+\quad
\text{前 }m\text{ 步的 }(K_s,R_s)\text{ bilinear observer}.
}
\]

这也是一次明确的 BRC 信息判决。安全消去的是“后半段 reflected branch 的逐点状态”；不安全消去的是 (2) 中的 \(x^p\) Frobenius integration constant，以及前 \(m\) 步 \(K_sR_s\) 的关联。若只保存 differential equation (1)，而忘掉 \(r x^p\)，会得到错误的 connection formula，相差一个真正会被 D25 observer 读取的常数项。

上一轮已经有

\[
\mathcal J_p=27\Lambda_p,
\qquad
\delta_{\rm terminal}E_p=-24\Lambda_p.
\]

所以本轮 (8) 还能直接给出

\[
\boxed{
\mathcal J_p
=
\frac32(\mathfrak S_p-2)
+
27D_m^2
\sum_{s=1}^{m}K_sR_s
\pmod p,
}
\tag{10}
\]

以及

\[
\boxed{
\delta_{\rm terminal}E_p
=
-\frac43(\mathfrak S_p-2)
-
24D_m^2
\sum_{s=1}^{m}K_sR_s
\pmod p.
}
\tag{11}
\]

因此 arithmetic-resonance repair 已不再需要任何 \(s>m\) 的动态 reflected state。

我做了独立 exact modular regression。对全部

\[
166\text{ 个 }p<5000,
\qquad
p\equiv13,19\pmod{24},
\]

逐 prime 重建 \(A,B,\mathfrak S_p,D_m,H_m'(1/2),K_s,P_s,Q_s\)，同时核验

\[
A'(1/2)B(1/2)
=
-2+\mathfrak S_p,
\]

\[
\sum_{s=1}^{3m}K_s
=
\frac{2-\mathfrak S_p}{9D_mH_m'(1/2)},
\]

以及新式 (8) 与上一轮原始 \(\Lambda_p\) 的完全一致性；三组检查均为 **0 failures**。有限扫描仍只承担 falsification；证明是 (1)–(9) 的有限域 Lagrange identity、Frobenius-kernel accounting、Jacobi reflection 与已有 coefficient bridge。

因此当前下一步已经比上一轮更窄。不要再对 \(3m\)-长 branch 做 adjoint，也不需要研究 \(B(1/2)\)、\(b_q\)、\(\rho_p\) 或后 \(2m\) tail。只需针对

\[
\boxed{
D_m^2\sum_{s=1}^{m}K_sR_s
}
\]

构造 observer-specific finite adjoint；其中

\[
K_{s+1}/K_s
=
\frac{2s(s+3m+1)}
{(s+m+1)(s+2m+1)}
\]

是一阶 rational transport，而 \(R_s\) 已由 (9) 完全回到 low \(A\)-branch。最有价值的下一目标，是判断该 \(m\)-步 bilinear observer 是否进一步 telescope 到 \(\mathfrak S_p\)、\(H_m'(1/2)\) 或 frozen original-\(3F_2\) endpoint；若不能，剩余 adjoint defect 本身就是比 \(\Lambda_p\) 更小的 LIFT certificate。

本轮新增单元的准确状态是

\[
\boxed{
\texttt{PROVED\_PORTABLE /
FROBENIUS\_XP\_CONNECTION\_RESIDUAL /
LOCAL\_GAUSS\_ENDPOINT\_REDUCTION /
BACK\_2M\_GAUGED\_TAIL\_ELIMINATION /
UNPUBLISHED /
LIFT\_OPEN}.
}
\]

这里特别没有把前几轮 staged 的 arithmetic-resonance、cross-branch、Jacobi-reflection 单元冒充 Source predecessor `completed_units`；本轮是在同一会话 portable frontier 上继续组合。当前 native control Issue 创建在到达远端前被平台层阻断，所以不能合法执行 `artifact_upload → publish_checkpoint → continuation readback`，也不能声称 PRE_FINAL 已通过。下一轮控制通道恢复后，只应补写本轮的 Frobenius-\(x^p\) connection / equation (8) reduction，不重做既有 D24、Clausen、Green、CM0/SIMPLE、Gauss–Manin gauge 或前几轮 cross-branch 数学。

任务原有频率、相位、启用状态和通知行为均未修改。

Global-Knowledge-Sync: main@ff4e73f / GLOBAL_KNOWLEDGE_V1