# CBRC F5AR — Source and Target Leak Audit

Status: `TARGET_LEAK_AUDIT_PASS`
Date: `2026-08-25`
Researcher-ID: `EM-CBRCF5AR-7E8B04`
Task-ID: `RS-CBRC-F5AR-INDEPENDENT-BRANCH-ONTOLOGY-AXIOM-ADMISSION-REPLICATION`
Owner branch: `research/cbrc-f5ar-independent-branch-ontology-axiom-admission`

Audit verdict:

`TARGET_LEAK_AUDIT_PASS`.

## 1. Publication-liveness chronology

Fresh Researcher-ID:

`EM-CBRCF5AR-7E8B04`.

The precreated owner branch was verified remotely before mathematics.

Initial execution stamp commit:

`7426380728a9c3d9ede199b1867c86f3f8f683d4`.

After reading the taskbook, but before reading any mathematical source, the stamp was reconciled to the stricter Section 2 schema by adding all four exact mathematical source refs.

Reconciled pre-math stamp commit:

`680ff7d2eb164b3be33522c52561afaa30c48ce4`.

The owner branch was then verified `identical` to that commit before any mathematical source was read.

The stamp records:

- `phase = STARTED_BEFORE_MATH`;
- `admission_verdict = null`;
- `math_source_read_before_stamp = false`.

No mathematical source was read before the reconciled stamp was remotely verified.

## 2. Governing taskbook

Taskbook:

`research_tasks/COHERENT_BRC_F5AR_INDEPENDENT_BRANCH_ONTOLOGY_AXIOM_ADMISSION_REPLICATION_20260825.md@7fc3522b2e5b9273025c8d399206460e44b13f6b`.

The taskbook was used as governance/whitelist authority, not as an additional mathematical premise.

## 3. Exact mathematical whitelist actually used

### Source 1

`research_inputs/CBRC_F5A_BRANCH_ONTOLOGY_AXIOM_ADMISSION_PACKET_20260825.md@b904a86aa24ed35564956181a7c1309074a782ea`.

Used for:

- accepted A0–A4 candidate formulations;
- the F5R kernel-branch independence witness as frozen input;
- the non-negotiable distinction among enriched nonzero, old projection, witness support, signed aggregate and post-recoalescence support;
- the conditional F4 consequence boundary.

### Source 2

`definitions/ENTERPRISE_BRC_MULTIPATH_ENRICHMENT_BRIDGE_20260821.md@6ec0d73a19e28ec586c59a97d24f5798c9119771`.

The pinned SHA resolves as the exact Git blob for this frozen definition.

Used for:

- separation of concrete Path-formal witnesses from later enrichment slots;
- `PATH_FORMAL_BRC -> N_BRC -> BOOLEAN_BRC`;
- exact `(1,1)` commuting diamond witness count `2 -> 2 -> 1`;
- component typing and translation covariance;
- non-invertibility of the Boolean shadow.

### Source 3

`definitions/ENTERPRISE_NATIVE_LINE_TRACE_FORMULA_20260821.md@b631242db84c5bd3640e6dc554b19a1d04d464f3`.

The pinned SHA resolves as the exact Git blob for this frozen definition.

Used for:

- typed witness identity;
- the distinct words `XiXj`, `XjXi` with common typed terminal and trace;
- trace composition and realization-fiber semantics;
- distinction between path witness and carrier endpoint.

### Source 4

`driver_reviews/CBRC_F5R_INDEPENDENT_FORGETFUL_BRANCH_SEMANTICS_DRIVER_REVIEW_20260825.md@6e40f56745c405042ad2216d1f62b110312ffb83`.

Used only as the accepted semantic boundary, specifically:

- current semantics do not derive per-branch old-projection nondegeneracy;
- exact candidate-false kernel witness exists;
- total old-coordinate conservation forces only “at least one” at the elementary split;
- any rank lower bound is conditional on a new axiom;
- no F6/rank-two construction is authorized.

No source proof downstream of that accepted boundary was read.

## 4. Execution-history-only material

The F5A publication-liveness failure was consulted only as execution history through the account-level durable journal/bootstrap flow.

It supplied no mathematical premise and no missing F5A verdict.

The missing/nonmaterialized F5A mathematical result was neither reconstructed nor inferred.

## 5. Forbidden material audit

Before raw freeze, this execution did not read or use:

- downstream coherent-BRC/wave free research;
- R063 mathematics;
- R064 mathematics;
- R065 mathematics;
- FQ mathematics;
- external quantum mechanics;
- quantum walks;
- Hilbert-space constructions;
- Born rules;
- path integrals;
- gauge theory;
- wave equations;
- any preselected rank-two carrier;
- complex-number carrier constructions;
- quadratic-integer carrier constructions;
- finite phase groups;
- norms;
- inner products;
- positive quadratic forms;
- square laws;
- Hadamard/Fourier/splitter targets;
- any proposed F6 answer.

## 6. Selector audit

The admission verdict was selected from:

- exact logical implication/minimality;
- finite countermodels for failed converses;
- arbitrary-depth refinement proof;
- typed signed-cancellation compatibility;
- model-extension conservativity over canonical Path/N/Boolean BRC;
- ontology cost.

The conditional statement

`GLOBAL_ZERO_SEPARATION + ADMITTED_BRANCH_FAITHFULNESS => torsion_free_rank(C) >= 2`

was not used to choose A0.

It is stated only after the admission decision because the taskbook requires the conditional consequence if the admitted rule forces both elementary old projections nonzero.

No carrier of that rank was constructed or selected.

## 7. Target-leak result

The result

`F5AR_ADMIT_RESTRICTED_ELEMENTARY_RULE_ONLY`

was reached without downstream structural targets.

The strongest load-bearing facts were all visible inside the whitelist:

1. canonical BRC distinguishes path witnesses, counts, Boolean support and later enrichment slots;
2. current semantics admit a kernel-only active branch with zero old projection;
3. A0 exactly forbids that elementary zero-child pattern;
4. A0 does not propagate to arbitrary depth without hereditary refinement scope;
5. a global support-reflecting retraction is stronger than the branch-typed requirement;
6. pre-erasure branch faithfulness must not be conflated with post-erasure aggregate nonzeroness.

Freeze:

`TARGET_LEAK_AUDIT_PASS`.

Stop boundary remains active: no F6, no rank-two construction/classification, no downstream wave comparison, no Foundation promotion.

## 8. Exact pushed-checker execution evidence

Pushed checker commit:

`d8abcf11e2129b4ffececde4813c46c786f58064`.

Remote checker Git blob SHA:

`8c2008bf39a9ab0517e5f4c074f17fc0a59bf629`.

The bytes executed locally were hashed using Git's canonical blob hash

`sha1("blob " + len(bytes) + NUL + bytes)`,

and reproduced exactly:

`8c2008bf39a9ab0517e5f4c074f17fc0a59bf629`.

Therefore:

`EXECUTED_CHECKER_BYTES_IDENTICAL_TO_PUSHED_BLOB = true`.

Checker file SHA-256:

`315539c4cf0509c99d5d12a6ca38c65a3d528c0544328226653d695c4def9368`.

Exact execution result:

- `F5AR_CHECKER_RESULT=PASS`;
- `check_count=65`;
- `mismatch_count=0`;
- `finite_tree_depth=4`;
- `finite_tree_models=6561`;
- deterministic digest `eab541a2e3144852acd883f5b6152d61a39e560b5913fba2371d39273c4831b0`.

The checker is evidence only. The arbitrary-depth closure claim is proved in the theorem report by induction and one-step extension, not inferred from depth-4 enumeration.

Freeze:

`F5AR_PUSHED_CHECKER_PASS`.
