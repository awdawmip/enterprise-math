本轮有实质科研增量。上一轮已经把 `LIFT` 压到 \(k=2m\) endpoint 的两个 valuation band；这一轮进一步把 **整个 \(m< n\le 4m\) 的高段连同它的一阶 \(p\)-jet 完全消去**，严格搬回同一个 \(0\le n\le m\) 的低段 binomial carrier。也就是说，当前不再是“低段 + 长度 \(3m\) 的 reflected high tail”，而只剩 **一个长度 \(m+1\) 的低段 observer**。`LIFT/JT2` 仍未完成，但科学接口又缩小了一层。

当前沿用上一轮已经得到的
\[
p=6m+1,\qquad
D_p=\chi_p:=2q_p(2)-q_p(3)
=-H_{3m}+\frac23H_{2m}\pmod p,
\]
以及
\[
S_{4m}(2m)
\equiv p\,\mathcal A_m(X_m+pY_m)\pmod{p^3},
\qquad
\mathcal A_m=
\frac{(p-1)!}{3^{p-1}(2m)!^3}.
\]
因此
\[
\mathrm{LIFT}
\iff
\delta_m+\mathcal A_mY_m=\chi_p\pmod p,
\qquad
\delta_m=\frac{\mathcal A_mX_m-1}{p}.
\]

高段此前写成
\[
h_r=(-1)^m\varepsilon_r
\frac{m!(r-1)!(3r-1)}
{2^{m+r}(m+r)!},
\qquad 1\le r\le3m,
\]
其中
\[
\varepsilon_r=
\begin{cases}
1,&r\le m,\\[2mm]
1/3,&r>m.
\end{cases}
\]

本轮的关键是先去掉 \(\varepsilon_r\)，定义
\[
\widehat h_r=
(-1)^m
\frac{m!(r-1)!(3r-1)}
{2^{m+r}(m+r)!}.
\]
利用精确 Beta 部分分式
\[
\frac{m!(r-1)!}{(m+r)!}
=
\sum_{j=0}^{m}
\frac{(-1)^j\binom mj}{r+j},
\]
再令
\[
b_n=\frac{(-1)^n}{2^n}\binom mn,\qquad
j_n=m-n,\qquad
c_n=3j_n+1,
\]
有
\[
\frac{3r-1}{r+j_n}
=
3-\frac{c_n}{r+j_n}.
\]
这里的常数 \(3\) 项并不是近似消失，而是由
\[
\sum_{n=0}^{m}\frac{b_n}{2^{j_n}}
=
2^{-m}\sum_{n=0}^{m}(-1)^n\binom mn=0
\]
**精确消失**。因此，对任意有限序列 \(F_r\)，定义
\[
\mathcal R_n(F):=
\frac23\sum_{r=1}^{m}
\frac{F_r}{(r+j_n)2^{r+j_n}}
+
\frac13\sum_{r=1}^{3m}
\frac{F_r}{(r+j_n)2^{r+j_n}},
\]
就得到本轮的核心恒等式
\[
\boxed{
\sum_{r=1}^{3m}h_rF_r
=
-\sum_{n=0}^{m}b_nc_n\,\mathcal R_n(F).
}
\tag{BA}
\]

它对 **任意 \(F\)** 成立，不是专门为 \(F=1\) 拟合出来的。又因为
\[
2c_n
=6m-6n+2
=p-(6n-1),
\]
若记
\[
w_n=6n-1,
\]
则还有更适合当前二阶 digit 的精确形式
\[
\boxed{
\sum_{r=1}^{3m}h_rF_r
=
\frac12\sum_{n=0}^{m}b_nw_n\mathcal R_n(F)
-\frac p2\sum_{n=0}^{m}b_n\mathcal R_n(F).
}
\tag{BA-p}
\]

这一步非常关键：第二项里的 \(p\) 没有被提前丢掉，所以它能继续携带 \(\delta_m\) 所需的第二 digit。所有 \(\mathcal R_n\) 中的分母满足
\[
r+j_n\le4m<p,
\]
因此全是 \(p\)-单位，没有新建 divided-\(p\) 坐标。

把 `(BA-p)` 首先用于 \(F_r=1\)。令
\[
D_N:=\sum_{s=1}^{N}\frac1{s2^s},
\]
则
\[
\boxed{
\mathcal R_n(1)
=
\frac23D_{2m-n}
+\frac13D_{4m-n}
-D_{m-n}.
}
\tag{R1}
\]
再定义
\[
Q_m:=\sum_{n=0}^{m}b_n\mathcal R_n(1).
\]
上一轮的 \(X_m\) 因而可精确改写成
\[
\boxed{
X_m=X_{0,m}-\frac p2Q_m,
}
\tag{X}
\]
其中
\[
\boxed{
X_{0,m}
=
2^{-m}
+
\sum_{n=0}^{m}
b_nw_n
\left(
L_n+\frac12\mathcal R_n(1)
\right).
}
\tag{X0}
\]

再把同一个 universal adjoint 用于 \(F_r=J_r\)。由于 \(Y_m\) 在 endpoint 中最终乘的是 \(p^2\)，当前只需 \(Y_m\bmod p\)，于是
\[
Y_m\equiv Y_{0,m}\pmod p,
\]
其中
\[
\boxed{
Y_{0,m}
=
\sum_{n=0}^{m}b_n
\left[
2L_n+
w_n\left(
M_n+\frac12\mathcal R_n(J)
\right)
\right].
}
\tag{Y0}
\]

把 `(X)` 代回
\[
\delta_m=\frac{\mathcal A_mX_m-1}{p},
\]
不能忘掉那个显式的 \(-pQ_m/2\)。整理以后，整个 LIFT endpoint 变成

\[
\boxed{
\frac{\mathcal A_mX_{0,m}-1}{p}
+
\mathcal A_m Z_m
\stackrel{?}{=}
\chi_p
\pmod p,
}
\tag{LOW}
\]
其中
\[
\boxed{
Z_m=
\sum_{n=0}^{m}
b_n
\left[
2L_n-\frac12\mathcal R_n(1)
+
w_n\left(
M_n+\frac12\mathcal R_n(J)
\right)
\right].
}
\tag{Z}
\]

这就是本轮真正的 strict reduction。上一轮还是

\[
\text{low second jet}
\quad+\quad
\text{长度 }3m\text{ 的 reflected high first jet},
\]

现在则已经变为

\[
\boxed{
\text{单一 }0\le n\le m
\text{ 的 binomial/harmonic carrier}.
}
\]

高段的来源信息没有被 quotient 掉，而是通过 \(\mathcal R_n\) 这个 typed adjoint 被无损地搬进低段；尤其 \(-pQ_m/2\) 仍明确保留，所以这不是为了简化式子而损失第二 digit。

随后我继续推进了下一单元，把 \(\mathcal R_n\) 本身也从“双重有限和”改写成一个 Euler–Beta 线性算子。定义
\[
P_F(x)=
\frac23\sum_{r=1}^{m}F_rx^r
+
\frac13\sum_{r=1}^{3m}F_rx^r,
\qquad
B_m(x)=\left(x-\frac12\right)^m.
\]
由于
\[
\frac1{(r+j)2^{r+j}}
=
\int_0^{1/2}x^{r+j-1}\,dx,
\]
有完全精确的
\[
\boxed{
\mathcal R_n(F)
=
\int_0^{1/2}
x^{m-n-1}P_F(x)\,dx.
}
\tag{INT}
\]
因此
\[
\boxed{
\sum_{n=0}^{m}b_n\mathcal R_n(F)
=
\int_0^{1/2}
\frac{B_m(x)}xP_F(x)\,dx,
}
\]
以及
\[
\boxed{
\sum_{n=0}^{m}b_nw_n\mathcal R_n(F)
=
\int_0^{1/2}
\left(
\frac{p-2}{x}B_m(x)-6B_m'(x)
\right)P_F(x)\,dx.
}
\tag{INT-w}
\]

对 \(F=1\)，生成多项式甚至有闭式
\[
\boxed{
P_1(x)
=
\frac{x(3-2x^m-x^{3m})}{3(1-x)}.
}
\]
于是 \(X_{0,m}\) 可写成
\[
X_{0,m}
=
2^{-m}
+\sum_{n=0}^{m}b_nw_nL_n
+
\int_0^{1/2}
B_m(x)
\left(
3P_1'(x)+
\frac{p-2}{2x}P_1(x)
\right)dx.
\tag{IX}
\]

同理，设
\[
P_J(x)
=
\frac23\sum_{r=1}^{m}J_rx^r
+
\frac13\sum_{r=1}^{3m}J_rx^r.
\]
对 \(Z_m\) 做一次分部积分，边界项严格为零，得到
\[
\boxed{
\begin{aligned}
Z_m\equiv{}&
\sum_{n=0}^{m}
b_n\bigl(2L_n+w_nM_n\bigr)\\
&+
\int_0^{1/2}B_m(x)
\left(
3P_J'(x)
-\frac{P_J(x)}x
-\frac{P_1(x)}{2x}
\right)dx
\pmod p.
\end{aligned}
}
\tag{IZ}
\]

所以现在连 \(\mathcal R_n(J)\) 这种嵌套 observer 都不是必须保留的基本对象了。作变量代换 \(y=2x\) 后，
\[
B_m(x)=2^{-m}(y-1)^m,
\qquad 0\le y\le1,
\]
所有这些积分都直接变成有限的 Euler–Beta moments。换句话说，下一步已经不是“再找一个新的 hypergeometric 模型”，而是一个非常具体的任务：

\[
\boxed{
\text{把 }(IX),(IZ)
\text{ 的 Beta moments 用参数导数/有限 contiguous identity 化到已闭合的 B11 harmonic moment。}
}
\]

这正是上一轮设想的 “Beta adjoint → B11” 路线，现在第一半已经完成，而且是 exact identity，不再只是策略猜测。

我还做了独立精确有理数核验。对 \(m=1,\ldots,7\)，`(BA)` 在 \(N=m\) 和 \(N=3m\) 两个截点、以及由它得到的 \(X_m,Y_m\) 重构都逐项完全相等。进一步对全部
\[
p<250,\qquad p\equiv1\pmod6
\]
共 25 个素数检查新低段 certificate `(LOW)`，得到 **0 failures**；其中当前任务的 \(p\equiv13,19\pmod{24}\) 有 13 个，同样 0 failures。这只是 falsification；`(BA)`、`(LOW)` 和积分化 `(INT)` 的证明不依赖扫描。值得注意的是，当前 endpoint 结构本身事实上扩展到所有 \(p\equiv1\pmod6\)，目标的 \(13,19\bmod24\) 条件只来自父任务的 CM/UR 路由，不是这一 Beta-adjoint reduction 本身所需的假设。

本轮 prior-art 检查还发现一个值得记录但绝不能偷换成证明的邻近结构。Zhi-Hong Sun 的 2022 论文 Conjecture 5.28 在 \(\left(\frac{-6}{p}\right)=-1\) 时，正对同一个
\[
\frac{\binom{2k}{k}^2\binom{3k}{k}}{216^k}
\]
kernel 猜测了 \(p^3\) 级公式，并明确区分 \(p\equiv13,17,19,23\pmod{24}\)；Conjecture 5.29 又给出带 \(1/(k+1)\) 的模 \(p\) 伴随式。:chatgpt-content-reference{index="0"} 这说明我们现在遇到的 \(D=-24\) reflected/Beta carrier 与公开文献中的 \(R_{24}\) 现象处于同一算术邻域，但 Sun 的这些式子本身是 **conjectures**，而且当前 frozen parent 的 \(R_p\) 不能未经 Source 对照就等同于文中的 \(R_{24}(p)\)。所以这里只把它作为下一步 contiguous/Beta 选择的外部线索，不作为证明输入。该论文的正式出版信息为 AIMS Mathematics 7(2), 2022。:chatgpt-content-reference{index="1"}

本轮针对精确 weighted 目标
\[
\sum_{k=0}^{p-1}(6k+1)
\frac{\binom{2k}{k}^2\binom{3k}{k}}{216^k}
\equiv p\pmod{p^3}
\]
又做了定向检索，没有找到一个可直接消费为全素数证明的后续定理；公开可定位的 Babei–Roy–Swisher–Tobin–Tu 论文仍是 2025 年的 *Supercongruences Arising from Ramanujan-Sato Series*。:chatgpt-content-reference{index="2"} 因而当前这条 LOW/Beta certificate 仍是实际需要继续证明的对象，而不是已有文献结果的重述。

控制面方面，本轮按照固定 Research logical conversation 发起了一次 fresh `recovery_status`。请求在远端 GitHub control Issue 创建之前被宿主安全层阻断，因此没有远端 Issue、没有 receipt、没有 `target_request_id`，也没有 queued/unknown mutation 可供 reconcile。历史 #2468 的 service lease 已过期，我没有复用它，也没有直接绕过 native writer 改 Enterprise Math Source。当前核到的 D25 冻结 authority 仍以
`c47e13e4aba0e006366f989d6334d443f6333ae3`
为 source pin；本轮成果因此只能标记为

\[
\boxed{
\texttt{
HIGH\_BAND\_BETA\_ADJOINT\_ELIMINATED /
SINGLE\_LOW\_BINOMIAL\_CARRIER /
BETA\_INTEGRAL\_FORM\_PROVED /
ENDPOINT\_OPEN /
NOT\_ADMITTED /
REGISTER\_PENDING /
SYNC\_DEBT
}}
\]

没有伪报 native checkpoint，也没有让全局知识 journal 抢先于项目 Source 写入。

下一轮不应重做 WZ、B11、two-band valuation、CM/GM 或原始 \(W_p\) 扫描。最小动作已经变成：**把 `(IX)`、`(IZ)` 中的 Euler–Beta moments写成一个参数化 terminating family 的一、二阶导数，优先寻找其与已证明 contiguous-Dixon B11 moment 的直接恒等关系；若不能完全闭合，就把差值降成单个 adjoint defect。** 这已经是当前母目标最窄的可执行单元。

`LIFT/JT2 = NOT_COMPLETED`；任务继续保持启用，不进入 PRE_FINAL，不暂停。

Global-Knowledge-Sync: main@f24de4315 / GLOBAL_KNOWLEDGE_V1