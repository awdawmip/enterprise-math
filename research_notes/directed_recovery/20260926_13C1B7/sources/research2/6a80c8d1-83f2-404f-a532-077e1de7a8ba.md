本轮有一个实质性的纠偏与推进：**此前把 \(p\,\xi^{p-1}d\xi\) 的 exact-form shift 当成一个任意的局部 \(\mathbf F_p\)-自由度，只在 formal completion 里成立；一旦恢复 d=3 椭圆曲线上的全局代数极点约束，这个自由度并不自动存在。** 这直接缩小了 D25 当前的 companion bottleneck。

控制面先说明到必要程度：GLOBAL_KNOWLEDGE_V1 仍核在 `978895ef2dc303c385fae14d8a54e482cdb0a3b0`，Enterprise Math 当前 main 为 `06495d21fb1cb93eb6e46ccccf148a9beeaf8f84`；P000 仍 `ACTIVE_LOCKED`，适用研究位置仍为 residual-faithful。当前工具面没有原生 `em_status/recovery_status`，因此按现行规则读取 Source，没有为了查询状态创建 Issue。冻结 D25 Source 仍是

\[
c47e13e4aba0e006366f989d6334d443f6333ae3,
\]

current main 上的 `latest_checkpoint.json` 仍是同一 blob `715132e6...`，且 `authority_granted=false`。因此下面仍是本线程自己的 `UNREVIEWED / NOT_ADMITTED / REGISTER_PENDING` portable evidence；没有借旧 E267A2/session/claim。

论文的 d=3 family 正是

\[
E_t:\qquad
y^2+xy+\frac t{27}y=x^3,
\]

而正式证明在局部参数 \(\xi=-x/y\) 下使用 \(\nu=c\,\partial_\lambda\omega+\omega\)，并把 \(\omega,\nu\) 的 leading coefficient 都规范为 1。:chatgpt-content-reference{index="0"} 论文同时明确区分 global algebraic de Rham 与 formal/divided-power completion；后者确实允许以 formal primitives 模 \(pA[[\xi]]\) 来描述。:chatgpt-content-reference{index="1"}

真正的新推进从全局 Weierstrass 模型开始。令

\[
a:=\frac t{27},\qquad
F(x,y,t)=y^2+xy+ay-x^3,
\]

并记

\[
D:=F_y=2y+x+a.
\]

标准 invariant differential 为

\[
\omega=\frac{dx}{D}.
\]

如果先取“固定 affine \(x\)”的 parameter lift，则由 \(F=0\)

\[
y_t=-\frac{y}{27D},
\]

从而

\[
D_t
=
2y_t+\frac1{27}
=
\frac{x+a}{27D}.
\]

因此得到一个**具体的全局 rational Gauss–Manin representative**

\[
\boxed{
\left.\partial_t\omega\right|_x
=
-\frac{x+a}{27D^3}\,dx.
}
\tag{1}
\]

这里 \(D=0\) 正是非零 2-torsion divisor：广义 Weierstrass 的取负为

\[
(x,y)\longmapsto(x,-y-x-a),
\]

所以固定点满足 \(2y+x+a=0\)。

若记

\[
T:=E[2]\setminus\{O\},
\qquad \deg T=3,
\]

则在每个 \(T_i\) 附近，\(D\) 是一阶 local parameter，而 \(x-x(T_i)\) 是二阶，因此

\[
dx\sim D\,dD.
\]

于是 (1) 最坏只有二阶极点：

\[
\boxed{
\left.\partial_t\omega\right|_x
\in H^0(E,\Omega^1(2T)).
}
\tag{2}
\]

在 \(O\) 处用 \(x\sim\xi^{-2},\,y\sim-\xi^{-3}\) 可见它反而正则并消失。

这立刻给出一个此前 local-gauge argument 没有保留的全局刚性命题。

若两个 representative \(\nu_1,\nu_2\in H^0(E,\Omega^1(2T))\) 的差在采用的 rational de Rham complex 中为

\[
\nu_1-\nu_2=df,
\]

那么 \(df\) 的极点至多二阶意味着 \(f\) 的 pole divisor 至多为 \(T\)，故

\[
\deg (f)_\infty\le3.
\]

如果它们在 \(O\) 的 local expansion 只从目标 \(p\)-层开始不同，即

\[
f-f(O)
=
\eta\,\xi^p+O(\xi^{p+1}),
\]

则

\[
\operatorname{ord}_O(f-f(O))\ge p.
\]

但非零 rational function 的零点总次数必须等于极点总次数，所以只要 \(p>3\)，

\[
p\le\deg(f)_0=\deg(f)_\infty\le3
\]

不可能。因此

\[
\boxed{
f=\text{constant},\qquad \eta=0.
}
\tag{3}
\]

这给出本轮第一个有限命题：

> **Global bounded-pole rigidity.**  
> 在固定 pole budget \(2T\) 内，一个保持全局代数 provenance 的 exact perturbation，不能任意加入
> \[
> p\eta\,\xi^{p-1}d\xi
> \]
> 而保持所有更低 local jet 不变。目标素数 \(p\equiv13,19\pmod{24}\) 均有 \(p\ge13\)，所以这个局部 \(\mathbf F_p\)-fiber 在该全局 carrier 中实际上坍缩。

这并不是说 frozen checkpoint 的 local formal calculation 错了；更准确地说，它证明了一个 **formal-completion information boundary**，但尚未证明其中每一个 local exact shift 都能全球化并保持 source differential 的 pole provenance。

本轮还能把 fixed-\(x\) 与 frozen 使用的 fixed-\(\xi\) representative 之间的 bridge 完全写出来，而且比引入任意 \(\eta\) 更具体。

由

\[
\xi=-\frac{x}{y}
\]

直接得到，在 fixed \(x\) 下

\[
\left.\partial_t\xi\right|_x
=
\frac{\xi}{27D}.
\]

设 \(V\) 是 fiber 上的 vertical vector field，使得

\[
D_\xi=D_x+V
\]

保持 \(\xi\) 不变。由 \(V(\xi)=-(\partial_t\xi)_x\) 和 \(V(F)=0\) 解得

\[
\boxed{
V(x)=\frac{x}{27(y+x+2a)}.
}
\tag{4}
\]

于是 Cartan 公式给出

\[
D_\xi\omega-D_x\omega
=
d_{\rm rel}(\iota_V\omega),
\]

即

\[
\boxed{
\left.\partial_t\omega\right|_\xi
-
\left.\partial_t\omega\right|_x
=
d_{\rm rel}g,
\qquad
g=
\frac{x}
{27(2y+x+a)(y+x+2a)}.
}
\tag{5}
\]

所以 frozen 的所谓 exact-form gauge，在 fixed-\(x\) 与 fixed-\(\xi\) 之间至少有一个**明确的 rational primitive**；并不是一个凭空的 prime-dependent \(\eta\)。

还可以进一步把 fixed-\(\xi\) representative 本身化成非常简单的全局公式。由 \(y=-x/\xi\) 代回曲线，

\[
\xi^2x^2-(1-\xi)x+a\xi=0.
\tag{6}
\]

定义

\[
R:=2\xi^2x-(1-\xi).
\]

由 (6)

\[
\boxed{
R^2=(1-\xi)^2-4a\xi^3.
}
\tag{7}
\]

而对 (6) 关于 \(\xi\) 微分，可得

\[
\frac{dx}{d\xi}=\frac{D}{R}.
\]

因此 invariant differential 直接变成

\[
\boxed{
\omega=\frac{d\xi}{R}.
}
\tag{8}
\]

这与论文在 \(\xi\) 下展开 formal differential 的做法完全相容。:chatgpt-content-reference{index="2"}

现在保持 \(\xi\) 不动求 \(t\)-导数。因为 \(a=t/27\)，

\[
\partial_tR
=
-\frac{2\xi^3}{27R}.
\]

所以

\[
\boxed{
\left.\partial_t\omega\right|_\xi
=
\frac{2\xi^3}{27R^3}\,d\xi.
}
\tag{9}
\]

在目标

\[
\lambda=4t(1-t),\qquad
s^2=2,\qquad
\frac{dt}{d\lambda}=\frac s4,
\]

故

\[
\boxed{
\left.\partial_\lambda\omega\right|_\xi
=
\frac{s\,\xi^3}{54R^3}\,d\xi.
}
\tag{10}
\]

这给出了 frozen \(J_p^{(\xi)}\) 的一个真正 global-algebraic realization：

\[
\boxed{
J_p^{(\xi)}
=
[\xi^{p-1}]
\frac{s\,\xi^3}{54R^3}.
}
\tag{11}
\]

不再只是“选择一个 formal representative”这一抽象描述。

再看极点。方程 (7) 把 \(E\) 直接写成双覆盖

\[
R^2=(1-\xi)^2-4a\xi^3.
\]

设

\[
S:=(R)_0.
\]

右侧是三次多项式，因此

\[
\deg S=3.
\]

在 \(S\) 的分支点附近，\(R\) 是 local parameter 且 \(d\xi\sim R\,dR\)，故

\[
R^{-3}d\xi\sim R^{-2}dR.
\]

因此

\[
\boxed{
\left.\partial_\lambda\omega\right|_\xi
\in H^0(E,\Omega^1(2S)),
\qquad
\deg S=3.
}
\tag{12}
\]

于是与 (3) 完全相同的 rigidity argument 可以直接应用到**我们实际关心的 fixed-\(\xi\) gauge**：

\[
\boxed{
\text{在 }H^0(E,\Omega^1(2S))
\text{ 内，若 exact perturbation 首次出现在 }\xi^p,
\ p>3,
\text{则它必为零。}
}
\tag{13}
\]

这就是本轮最重要的结论。

此前 portable/frozen 路线把

\[
\partial_\lambda\omega
\mapsto
\partial_\lambda\omega+d(\eta\xi^p+\cdots)
\]

视为一个完整的 \(\eta\in\mathbf F_p\) fiber。现在必须加上限定：

\[
\boxed{
\text{该 fiber 在 local formal completion 中存在，
但在固定 global pole divisor 的代数 representative 类中未必存在；
对 }2S\text{ pole budget，它实际上不存在。}
}
\]

这正是一个 residual-faithful/BRC 的信息损失实例。当前 population 是 \(p\equiv13,19\pmod{24}\)，future observer 是 D25 second product digit；此前 carrier 只保留 local formal class 与 jet，丢掉了 `GLOBAL_POLE_DIVISOR / ALGEBRAIC_REPRESENTATIVE_PROVENANCE`。一旦把这个 provenance 恢复，原先看似一整个 \(\mathbf F_p\) 的 fiber 不再是合法 future state。

因此本轮 BRC resolution 应更准确写成

\[
\boxed{
\texttt{
REUSE\_APPLIED
+
COMPOSE\_APPLIED
+
INFORMATION\_LOSS\_WITNESS
+
CARRIER\_REFINEMENT\_REQUIRED
}.
}
\]

这也意味着上一轮提出“需要另找一个 canonical primitive splitting \(r_p\)”的路线不能直接作为最小 bottleneck 继续。**在先确认 actual source companion 是否已经被全局 bounded-pole algebraic gauge 唯一固定之前，引入一个自由 \(r_p\) 可能是在重复制造一个因忘记全局 pole provenance 才出现的残差。**

现在真正的下一精确动作已经改变为：

1. 证明论文中用于 \(b(p-1)\) 的 actual normalized companion，确实可以在同一个 fixed-\(\xi\) algebraic gauge
   \[
   \nu_c^\xi
   =
   \omega
   +
   c\,\left.\partial_\lambda\omega\right|_\xi
   \]
   中实现，而不仅是抽象 de Rham eigenclass。论文正式证明明确采用 \(\xi=-x/y\)、\(\nu=c\partial_\lambda\omega+\omega\) 并据此计算 \(b(p-1)\)，这是支持这一方向的原始依据，但还不能把我们的 global pole-rigidity 自动升级为其唯一解释。:chatgpt-content-reference{index="3"}

2. 精确审核 (10) 的 CM eigenline coefficient \(c\)，而不是额外添加任意 local exact term。

3. 若仍需比较另一 gauge，则使用已经得到的 rational bridge
   \[
   g=
   \frac{x}{27D(y+x+2a)}
   \]
   并计算
   \[
   [\xi^p]\,g,
   \]
   而不是把这一位记作自由的 \(\eta_p\)。由 (5)，其 differential contribution恰好是
   \[
   p[\xi^p]g\,\xi^{p-1}d\xi.
   \]

4. 然后再把这一**确定的** coefficient 与 durable
   \[
   \widehat\alpha_p,\quad
   Lcomp_p,\quad
   K_p=\tau_p-\alpha_p^2-Lcomp_p
   \]
   合流；LIFT 仍保持开放。

所以本轮不是关闭 LIFT，而是把当前 bottleneck 从

\[
\text{“一个任意 local } \mathbf F_p\text{ gauge digit”}
\]

收紧为

\[
\boxed{
\text{“验证 actual CM companion 的全局 pole provenance，
并计算一个明确 rational bridge 的 }\xi^p\text{ coefficient。”}
}
\]

本轮 portable 状态应记录为

\[
\boxed{
\texttt{
UNREVIEWED /
NOT\_ADMITTED /
PORTABLE\_EVIDENCE /
GLOBAL\_BOUNDED\_POLE\_GAUGE\_REPAIR /
FIXED\_XI\_GM\_REPRESENTATIVE\_EXPLICIT /
LOCAL\_FP\_GAUGE\_NOT\_GLOBAL\_UNDER\_FIXED\_POLE\_BUDGET /
FIXED\_X\_TO\_FIXED\_XI\_RATIONAL\_BRIDGE /
REGISTER\_PENDING /
SYNC\_DEBT /
LIFT\_OPEN
}.
}
\]

由于 Source authority 仍为 false，本轮没有把这个纠偏写成 canonical checkpoint、Result 或 `completed_units`；也没有覆盖 frozen checkpoint 中原来的 local information-boundary 声明。它现在应视为一个**待独立审阅的严格限定/反例**：如果后续审核确认 actual source companion 落在上述 bounded-pole algebraic gauge 中，那么原来的 arbitrary-\(\eta\) fiber 必须收缩；如果不能确认，则明确缺口就是“为什么 actual companion 允许脱离该 pole budget”，而不是继续默认它可以。

原错峰每小时频率、相位、启用状态和通知行为均未改变。