本轮有实质新推进。先把控制面压到必要范围：当前 `GLOBAL_KNOWLEDGE_V1` 已核到 `main@77fa066c646e42f7c96290e47b89599cd0c14321`，Enterprise Math 当前 `main@6fe3b9166c1b5ee1dc8c8987d6d7f5f82e0469c1`。从冻结 D25 Source

\[
\boxed{c47e13e4aba0e006366f989d6334d443f6333ae3}
\]

到当前 main 共前移 24 个提交；实际 compare 中没有改动 frozen D25 checkpoint 或其正式数学产物，新增的是控制恢复、未审阅 chat evidence、另一路 D25-B11 portable 证明及 Heartbeat 研究。另一路真实线程 `6ab33b2f-...` 的 B11 已读作去重证据，没有接管或重复其 WZ/contiguous-Dixon 路线。本轮工具面仍没有原生 `em_status/recovery_status` 可读入口，因此严格依当前 `AUTONOMOUS_RESEARCH_OPERATIONS` 的读写分离规则直接读取 Source，没有为了查状态创建 Issue，也没有重放旧 E267A2/shared-session 的任何写动作。

P000 仍保持锁定，当前世界观仍是 `RESIDUAL_FAITHFUL_DISCRETE_RELATIONAL_SYSTEM`。本真实对话继续使用自己的稳定逻辑 ID `chatgpt-thread-6ab0a2a3-1880-83ee-a0c0-f2016b6074ac`；本地研究角色仍是 `REGISTER_PENDING`，这不阻止本轮 portable mathematics，也不冒充正式 CLAIM。

这轮我没有继续追求“把 \(c_p\) 和 exact-form gauge 分别求出来”。重新从 frozen checkpoint 的

\[
Lcomp_p=
\frac{c_p\,\alpha_p J_p-1}{p}\pmod p
\]

以及 durable 的 representative-gauge theorem 出发，发现可以做一个更严格的 BRC reduction：

\[
\boxed{\text{D25 product observer 实际只需要一个组合残差，而不需要分别知道 }c_p\text{ 的 lift 和 exact-form gauge。}}
\]

这把当前的未知量从“两类来源”重新压成了**一个 affine scalar**。

先把模 \(p^2\) 的 lift 写清。Source 已经 durable 地给出 raw coefficient 的一位更深 lift：

\[
a_p=p\,\widehat\alpha_p\pmod{p^3},
\qquad
\widehat\alpha_p\in A/p^2A,
\]

并且

\[
\widehat\alpha_p
=
\widehat G_{T,p}-D_p-E_p+pM_p
\pmod{p^2}.
\tag{1}
\]

记

\[
\alpha_0:=\widehat\alpha_p\bmod p.
\]

在 frozen `fixed-\xi` trivialization 下，取 canonical reference Gauss–Manin coefficient

\[
J_p^{\xi}\in A/p^2A,
\qquad
j_0:=J_p^{\xi}\bmod p .
\]

真正的 local representative 可以与它差一个 Source 已经证明不可由 de Rham class 看见的 exact-form digit：

\[
\boxed{
J_p^{\rm act}
=
J_p^\xi+p\eta_p
\pmod{p^2},
\qquad
\eta_p\in A/pA .
}
\tag{2}
\]

另一方面，actual normalized companion coefficient 写成

\[
c_p.
\]

为了使 frozen 定义中的 numerator 可被 \(p\) 整除，其第一位不是自由的，而必须满足

\[
c_0\alpha_0j_0=1,
\]

因此

\[
\boxed{
c_0=(\alpha_0j_0)^{-1}.
}
\tag{3}
\]

这里已经能消费 frozen checkpoint 的 all-target first-digit bridge：

\[
6\alpha_0j_0
=
1-6C_pJ_p^F.
\]

定义

\[
U_p:=1-6C_pJ_p^F,
\]

则

\[
\boxed{
c_0=6\,U_p^{-1}.
}
\tag{4}
\]

这给出了一个此前没有写清的严格结论：**period lane 中的系数 \(6\) 本身不是 source companion 的第一位；source 第一位是 \(6\) 乘上显式 transfer factor \(U_p^{-1}\)。** 这里完全没有使用此前 166-prime 的经验式。

现在进入真正的一位 lift。工作环是 Source 使用的未分歧 Witt 环，因此可以使用 Teichmüller lift。记

\[
[c_0]\in A
\]

为 \(c_0\) 的 Teichmüller lift。于是 actual normalization 唯一可以写成

\[
\boxed{
c_p=[c_0](1+p\gamma_p)
\pmod{p^2},
}
\tag{5}
\]

其中

\[
\gamma_p\in A/pA
\]

是 normalization-lift residual。

接着固定一个完全 computable 的 reference quantity：

\[
\boxed{
L_p^\xi
:=
\frac{[c_0]\widehat\alpha_pJ_p^\xi-1}{p}
\pmod p.
}
\tag{6}
\]

它不是新的未知 CM 数据；它只使用 frozen raw-\(\alpha\) lift、fixed-\(\xi\) reference derivative 和第一位 normalization。

把 (2)、(5) 代回 actual `Lcomp`：

\[
\begin{aligned}
Lcomp_p
&=
\frac{
[c_0](1+p\gamma_p)
\widehat\alpha_p
(J_p^\xi+p\eta_p)-1
}{p}
\\
&\equiv
L_p^\xi
+
\gamma_p(c_0\alpha_0j_0)
+
c_0\alpha_0\eta_p
\pmod p.
\end{aligned}
\]

利用 \(c_0\alpha_0j_0=1\)，立即得到本轮的核心式：

\[
\boxed{
Lcomp_p
=
L_p^\xi+\Xi_p
\pmod p,
}
\tag{7}
\]

其中

\[
\boxed{
\Xi_p
:=
\gamma_p+\frac{\eta_p}{j_0}.
}
\tag{8}
\]

这就是新的最小 residual coordinate。

它非常重要，因为 \(\gamma_p\) 与 \(\eta_p\) 单独都依赖规范：前者描述 normalization lift，后者描述 exact-form representative shift；但对当前 D25 product observer，它们只能通过

\[
\gamma_p+\eta_p/j_0
\]

出现。

换句话说，BRC 的精确 fiber constancy 已经证明：

\[
(\gamma_p,\eta_p)
\longmapsto
\Xi_p
\]

是对当前 future observer 的安全 quotient。

这个 reduction 还能做一个很好的自洽检验。若我们改变 reference representative

\[
J_p^\xi
\longmapsto
J_p^\xi+p\delta,
\]

则

\[
L_p^\xi\longmapsto L_p^\xi+\frac{\delta}{j_0},
\]

与此同时 actual representative 不变意味着

\[
\eta_p\longmapsto\eta_p-\delta,
\]

因此

\[
\Xi_p\longmapsto\Xi_p-\frac{\delta}{j_0}.
\]

于是

\[
\boxed{
L_p^\xi+\Xi_p
}
\]

严格不变。

同样，如果不用 Teichmüller lift而把第一位 \(c_0\) 换成另一个 lift

\[
[c_0]'=[c_0](1+p\varepsilon),
\]

那么 reference part 增加 \(\varepsilon\)，而 normalization residual \(\gamma_p\) 减少 \(\varepsilon\)，总和仍不变。

所以这里不是把规范依赖“忽略”了，而是把它们明确组织成一个 affine torsor，并证明了 current observer 只读取一个 quotient coordinate。

还可以把 \(\Xi_p\) 改写成一个更接近 frozen `next_action` 所说的“invariant divided normalization scalar”。

定义 reference companion 的第 \(p\) coefficient：

\[
b_p^\xi
:=
a_p+[c_0]J_p^\xi,
\]

actual normalized CM companion 为

\[
b_p^{\rm CM}
:=
a_p+c_pJ_p^{\rm act}.
\]

两者模 \(p\) 相同，所以差值可被 \(p\) 整除。直接由 (2)、(5)：

\[
\frac{b_p^{\rm CM}-b_p^\xi}{p}
\equiv
c_0(\gamma_pj_0+\eta_p)
\pmod p.
\]

乘以 \(\alpha_0\)，再用 \(c_0\alpha_0j_0=1\)，得到：

\[
\boxed{
\Xi_p
=
\alpha_0
\frac{
b_p^{\rm CM}-b_p^\xi
}{p}
\pmod p.
}
\tag{9}
\]

这是本轮最有用的新 certificate。

它说明 frozen checkpoint 中所要求的

> “one pinned exact representative or one invariant divided normalization scalar”

现在可以非常具体地选择为：

\[
\boxed{
\Xi_p
=
\alpha_0\,
\frac{\text{actual CM companion coefficient}
-\text{canonical fixed-}\xi\text{ reference coefficient}}
{p}.
}
\]

以后不需要分别求出“\(c_p\) 的第二位”和“exact-form gauge \(\eta_p\)”再组合；只需从 actual CM endomorphism/local differential relation 中直接读这个**一个 divided displacement**。

而且 reference baseline 本身其实已经可以全部落到现有 finite source carrier。Source durable 的 raw coefficient identity是

\[
H_{3,p-1}(t)
=
F_{2r}(t)-pD_p(t)+p^2M_p(t)
\pmod{p^3},
\qquad
p=6r+1.
\]

fixed-\(\xi\) representative 定义为

\[
J_p^\xi
=
\frac{s}{4}\partial_t H_{3,p-1}(t),
\qquad s^2=2.
\]

因为现在只需要 \(J_p^\xi\bmod p^2\)，对上一式求导后 \(p^2M_p'\) 自动消失，于是得到新的完全有限公式：

\[
\boxed{
J_p^\xi
\equiv
\frac{s}{4}
\left(
F_{2r}'(t)-pD_p'(t)
\right)
\pmod{p^2}.
}
\tag{10}
\]

因此 (1)、(3)、(6)、(10) 组合起来说明：

\[
\boxed{L_p^\xi\ \text{已经是现有 finite-Gauss/raw-source carrier 的显式可计算量。}}
\]

实际 CM 几何现在只剩一个任务：

\[
\boxed{\text{确定 }\Xi_p.}
\]

这比 frozen checkpoint 原来的“同时 pin \(c_p\) lift、quartic normalization、exact-form gauge、CM representative”更窄。

把它重新接回 D25 product：

\[
TRAW_p
=
\alpha_0^2+Lcomp_p,
\]

故

\[
TRAW_p
=
\alpha_0^2+L_p^\xi+\Xi_p,
\]

而

\[
K_p=\tau_p-TRAW_p.
\]

所以得到最终的新 normal form：

\[
\boxed{
K_p
=
\tau_p
-\alpha_0^2
-L_p^\xi
-\Xi_p
\pmod p.
}
\tag{11}
\]

其中前三项已经全部处于 durable/finite source carrier；**D25 companion lane 唯一尚未 native 化的 scalar 就是 \(\Xi_p\)**。

我另外做了一个独立的有限代数回归，不调用任何 CM 猜测：对 \(p=13,19,37,43,61,67,73,97\)，随机选择数百组满足 \(c_0\alpha_0j_0=1\) 的 lifts、normalization residual 和 exact-form residual，逐项验证了

\[
Lcomp-L^\xi
=
\gamma+\eta/j_0
=
\alpha_0(b^{CM}-b^\xi)/p
\]

，所有检查均通过。这个回归只验证展开实现没有符号/除法错误；证明本身就是上面的有限 \(p\)-adic 代数，不依赖抽样。

BRC 在本轮的实际使用可以准确写成：

\[
\boxed{
\texttt{
REUSE\_APPLIED
+
COMPOSE\_APPLIED
+
STRICT\_REDUCTION\_APPLIED
}.
}
\]

population 仍是目标素数 \(p\equiv13,19\pmod{24}\)；observer 是 divided D25 product digit；允许的 future operation 是继续乘入 raw coefficient、quartic factor 与 endpoint carrier。对这个 observer，分别保留 \((\gamma_p,\eta_p)\) 是冗余的，但直接删除两者又是不保真的；最小充分残差正是 \(\Xi_p\)。如果未来研究的是 actual differential 本身而不是 product observer，则这个 quotient 不再自动合法，必须恢复来源标签。

本轮还专门避免了另一条 D25 流的重复：当前 main 中线程 `6ab33b2f-...` 已经保全 B11 contiguous-Dixon closure，状态仍是 `UNREVIEWED / NOT_ADMITTED / REGISTER_PENDING`。我没有把它计入自己的 completed units，也没有进入其 WZ endpoint 路线。

当前下一精确动作已经变为一件非常小的事：不再重算 \(\alpha_p\)、不再重新处理 quartic chain correction，也不再试图分别确定 \(c_1\) 与 \(\eta\)。应使用实际判别式 \(-24\) CM endomorphism，只计算 actual normalized companion 的

\[
b_p^{CM}\pmod{p^2},
\]

然后由

\[
\Xi_p
=
\alpha_0
\frac{b_p^{CM}-b_p^\xi}{p}
\]

直接取得唯一缺失 scalar。随后式 (11) 即可与已有 finite-Gauss/WZ endpoint carrier 合流。若显式 CM map太重，也可以寻找任何 Source-faithful observable，只要严格证明它等于 (9)，无需恢复整条 local differential。

因此本轮新增 portable 单元的准确状态是

\[
\boxed{
\texttt{
UNREVIEWED /
NOT\_ADMITTED /
PORTABLE\_EVIDENCE /
COMPANION\_GAUGE\_AND\_NORMALIZATION\_AFFINE\_QUOTIENT /
SINGLE\_DIVIDED\_DISPLACEMENT\_XI /
FIXED\_XI\_BASELINE\_FINITE /
REGISTER\_PENDING /
LIFT\_OPEN
}.
}
\]

没有写正式 checkpoint、Result 或 CLAIM，也没有把旧 shared session 复活或归属到本线程。原定时频率、相位、启用状态和通知行为保持不变。