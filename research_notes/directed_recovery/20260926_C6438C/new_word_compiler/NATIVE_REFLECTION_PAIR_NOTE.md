# Exact native reflection pair on the complete 61-mode carrier

Status: AUTHOR_EXECUTED_FINITE_WORD_BINDING / SHARED_CONTEXT / UNREVIEWED / NOT_ADMITTED.

Researcher: `EM-DIRECT-C6438C`.  
Activity: `RA-CAAAC604CB513AEA8BBC1DFC`.  
Research session: `MCP-9e0873ae3aae418192f81173029434e7`.  
Registration Source: `f0e5fb6f478a5a380ab5a7533d2585f7f43ee3bf`.

This certificate binds two reflections and their product to the existing native finite-word executor. Every complete basis column was generated by the actual frozen `stage80.fixed_phase.apply_word`, using the actual `primitives()` receipt. Exact integer/Fraction calculations only verify identities among those executed columns. The existing target32/vector64 bank remains unchanged; no target-precision construction was invoked.

## 1. Native words and their temporal order

Let `A_w` be the native H4 on coordinates `(0,1,2,3)`, let `A_v` be H4 on `(0,1,2,4)`, and let `D_0` negate coordinate 0 while fixing every other coordinate. Define

`H_w=A_w D_0 A_w`, `H_v=A_v D_0 A_v`, `G=H_w H_v`.

The script uses the frozen word tuples `('h4',...)`, `('neg',0)`, and `('swap',3,4)`. `apply_word` lists operations in temporal order. Thus each palindromic reflection runs H4, negation, H4; **G executes H_v first and H_w second**. The explicit inverse executes H_w first and H_v second. Each primitive is an involution, and every inverse check also executes the frozen reverse-word API.

The eight completely checked words are `A_w`, `A_v`, `D_0`, swap(3,4), `H_w`, `H_v`, `G`, and `G^-1`. In addition, all 61 columns verify that the second H4 support is obtained by routing conjugacy:

`A_v = swap(3,4) A_w swap(3,4)`.

The primitive receipt retains the actual H4 sign matrix, the negative-matching observation, the swap columns, matching provenance, and complete controlled primitive checks returned by `primitives()`. The selected H4 supports and the displayed sign/swap indices were executed in the full 61-mode carrier. Other legal index choices follow by the same source-defined coordinate relabeling; they were not separately enumerated in this bounded check.

## 2. Reflection identities from actual complete columns

The normals are first obtained as actual H4 images of `e0`; the certificate then checks their simple expressions:

`w=(e0+e1+e2+e3)/2`,

`v=(e0+e1+e2+e4)/2`.

Exact observers give

`<w,w>=<v,v>=1`, `<w,v>=3/4`.

For every one of the 61 input basis vectors, the executed reflection columns satisfy

`H_w e_j = e_j - 2w w_j`,

`H_v e_j = e_j - 2v v_j`.

Hence these are the full-carrier Householder reflections `I-2ww^T` and `I-2vv^T`, each of determinant -1. These formulas are certified expressions of actual columns; they were not substituted for `apply_word` when generating the evidence.

Each word's entire 61-by-61 Gram matrix was computed from its executed columns and equals the identity. Every column also returned exactly to its input after the actual inverse word. This checks arbitrary retained-mode input by linearity, rather than only clean first-coordinate states.

## 3. The exact two-dimensional rotation

Put `P=span(w,v)`. Its Gram determinant is

`1-(3/4)^2=7/16>0`,

so the normals are independent. The script actually executes G on w and v, retaining both numerator vectors and denominators, and verifies

`G w = (5/4)w - (3/2)v`,

`G v = (3/2)w - v`.

Thus the matrix on the rational, nonorthogonal basis `(w,v)` is

```text
      [  5/4   3/2 ]
M  =  [ -3/2    -1 ] .
```

It has trace `1/4` and determinant 1. Together with the already checked positive Gram form and orthogonality, this proves that the action on P is a plane rotation. Its exact rotation cosine is half the trace:

`cosine = 1/8`, `sine_squared = 1-(1/8)^2=63/64`.

These are exact rational identities; the executor does not evaluate an angle, cosine, sine, pi, or a floating-point approximation. The plane characteristic polynomial is

`X^2 - (1/4)X + 1`.

The full trace check gives `trace(G)=59+1/4`, as required by this plane action and the fixed complement.

## 4. Complete fixed complement and inverse

A rational basis of `P^perp` is

`e0-e1`, `e0-e2`, `e0-e3-e4`, and `e5,...,e60`.

The first three vectors are independent by inspecting coordinates 1, 2, and 3, and the remaining vectors have separate coordinates at least 5. All are orthogonal to both w and v. Each of these 59 vectors was passed through actual `apply_word(G)` and returned unchanged, with denominator 1. In particular every standard coordinate from 5 through 60 is untouched. There is no projection, reset, deletion, or reinitialization of complement modes.

The explicit six-primitive inverse word `H_v H_w` agrees on all 61 columns with reverse execution of G. Consequently G is a genuine full-carrier determinant-one word, with a native finite inverse.

## 5. Symbolic consequence and compiler boundary

The two nontrivial eigenvalues cannot be roots of unity. If one were a root of unity, its inverse would be too; their sum would be an algebraic integer. Their sum here is the rational number `1/4`, which is not an integer, a contradiction. Thus G has infinite order. This is an exact consequence of the rational characteristic polynomial, without an eigenvalue solver or an angle computation.

This result supplies a certified finite-word ingredient for further group-generation or approximation arguments. It is not itself a complete arbitrary-target compiler, a convergence-rate estimate, an efficient word search, or an achieved higher-precision approximation. Any density and effective-compilation theorem must state its additional argument separately. No large word enumeration or new phase-bank compilation was performed in this run.

## 6. Execution counts, source, and artifacts

The successful bounded run executed:

- Eight words, each on all 61 basis columns: **488 forward columns** and **488 inverse recoveries**.
- Their eight full Gram matrices: **29768 exact entries** checked.
- **61 routing-conjugacy columns** and **61 explicit-inverse comparison columns**.
- Two actual plane-vector runs and 59 actual fixed-complement runs.
- **Six actual native BRC core calls** establishing the reused primitive catalog and associated controls. Core calls are not the count of individual word-gate applications.

Every forward column, denominator, recovered inverse column, complete Gram matrix, word, primitive receipt, and additional vector check is stored in `NATIVE_REFLECTION_PAIR_CERTIFICATE.json.gz`.

The native kernel is pinned to commit `bc7babbb9e890f6d5a7094430a5fbdccf66c77ad`, path `src/enterprise_math/brc_weighted_recurrent.py`, git blob `4e6b3132580e3cd70a20a0d8bd4d28792b961afb`, SHA256 `7520822074b8f29e1c54f57b9543c043202db8bd7c6e27f3f1d6176028ac4a26`.

The frozen Stage87 source is `0852cad130c1d877174d235687cf60c19f318c58`. The certificate records the actual source paths and SHA256 values of `stage80/fixed_phase.py` and the executed check script.

Artifact size: 12247 compressed bytes, 319819 uncompressed JSON bytes. Uncompressed payload SHA256:

`05424867b64a27146e1073c658682771c7a733e46fc10e4fd9a85042720c347d`.

The completed run exited successfully and checked the compressed payload round trip. Reproduction:

```powershell
& 'D:/kimi-query-bridge/.venv/Scripts/python.exe' 'D:/em/TEMP/sep26-shor-general/new_word_compiler/check_native_reflection_pair.py' --activity RA-CAAAC604CB513AEA8BBC1DFC
```

Fresh timing and native-call receipt timestamps can change on rerun; the exact mathematical columns and identities must remain the same.

Global-Knowledge-Sync: main@441ebef / GLOBAL_KNOWLEDGE_V1
