# Heartbeat66 — partial record access, two-excitation return, and operational collapse

Progress-Event-ID: HEARTBEAT66-PARTIAL-ACCESS-20260924-48FD
Status: SAME_AUTHOR_CONDITIONAL_MODEL / ACTUAL_TYPED_BRC / NOT_ADMITTED
Researcher-ID: none newly assigned; no closed identity borrowed.
Research-Activity-ID: unavailable. Earlier session registration was tool-safety blocked; this turn does not retry or bypass that operation. Evidence preservation is not registration or admission.

## Scope and recovered frontier

Continue Stage65 cumulative52432236046eb8d6ada27f812a97c73f5f55d70c from the mounted bundle, actual SHA2563a588887df400c675d52a2b18fcf1dad156cb0aae4f463260c189cef55819e6e. Global immutable read7740e0d225bece921a1668ef9c8ec166213d3d09; source read18324538ab54c263131a158725201c34990a1c52. Unchanged bootstrap/manual/P000/worldview/BRC-only blobs reused. Six native spatial axes, material identity and residual fidelity are unchanged; internal labels and operation counts are not new space axes or calibrated physical heartbeat time. Complex-linear, Born and instrument rules remain declared bridges, not derived physics.

Live L retains the full native material/port/Gauss link-field/bank state S, generator G and ALL live returning energy receivers. New recording pointers are different subsystems. Partial trace is allowed only for pointers declared inaccessible, uncoupled and non-returning throughout the subsequent protocol. It is not physical deletion or reset. Classical log access is not the quantum access required to change a physical recording basis.

## 1. Real records and exact partial recovery

For local occupation h_j(z) in {0,1} of a complete live basis z, a fresh independent pointer starts0. Controlled F_j^{h_j(z)}, F_j=[[c_j,s_j],[s_j,-c_j]], c_j^2+s_j^2=1, creates its actual record. F_j is rational, self-adjoint, unitary. Pointer levels are degenerate only in the declared ledger; initialization, control and reset costs are not asserted zero.

Tracing these pointers gives the exact channel
D(rho)_{z,z'}=rho_{z,z'} product_{j:h_j(z)!=h_j(z')} c_j.
The factors form one actual record Gram matrix, hence a positive completely positive trace-preserving Schur channel; they are not independently fitted pairwise rates. Live source/energy occupations are unchanged.

For a quantum-accessible pointer use Q_lambda=(I+lambda F)/2, implemented by the ACTUAL rational dilation W=Q_+ tensor I+Q_- tensor X on a ready report. The identity Q_lambda F^h|0>=lambda^h Q_lambda|0> gives outcome weight(1+lambda c)/2 independent of live input. Feed back phase(-1)^h for the negative outcome. Keeping all outcomes cancels that accessible record exactly, including unknown live/reference entanglement. With accessible set A and inaccessible I, the resulting channel retains only the factors from I. No successful subensemble is selected.

In the two-code family a|x>|A_x>|I_x>+b|y>|A_y>|I_y>, let Gamma=<I_y|I_x> be the product of remaining independent overlaps. For ANY measurement E_r on A followed by phase-only code feedback, positivity and Cauchy-Schwarz imply
sum_r |<A_y|E_r|A_x>| <= sum_r sqrt(<A_x|E_r|A_x><A_y|E_r|A_y>) <=1.
Therefore recovered coherence2|rho_xy|<=2|ab|Gamma. The actual erasers saturate this bound for the present product-record family. All samples count; arbitrary amplitude re-preparation/filtering is excluded. Correlated/mixed records need their full cross operator, not a count-only c^m substitution. The general trace-norm recovery bound is prior art, not claimed new here.

## 2. Two locally prepared excitations and an actual echo

Two adjacent Cells each have two internal receiver rails. Start source s,G0 with independent receiver word1010. An onsite one-excitation rational reflection followed by an edge-local controlled exchange generates
psi=(3/5)|1010>+(4/5)i^phi|0101>, phi in C4.
All terms have TWO source-free energy quanta, exactly one per Cell; no material identity is copied. Each Cell's actual total-rail-count instrument returns1 and leaves the complete live state unchanged, whereas individual-rail records distinguish the configurations. Equal energy alone does not protect against other measurements.

Mark L0 and L1 with independent c3/5,s4/5 pointers. With source/G isolated, four actual native rail shifts return the old bits. Quantum-erase neither, one, or both physical pointers, then undo the preparation and read the original full live configuration. Exact BRC values:
I size2:Gamma9/25,P_echo11017/15625;
I size1:Gamma3/5,P_echo2549/3125;
I size0:Gamma1,P_echo1.
The formula is P_echo337/625+(288/625)Gamma. It includes all instrument outcomes. Full recovery restores the live state, not all pointer registers to ready0. For sharp records c0, one still-inaccessible distinguishing pointer keeps P_echo337/625 even after recovering the other; recovering both gives1.

An actual orthogonal copy of a sharp physical pointer into an unaccessed log register also leaves337/625 after attempting erasure on the original pointer alone. Thus having a recorded symbol, or even one apparatus, is not control of every remaining physical record.

Without the recombiner, continue the ORIGINAL source/generator/live-rail contacts. First three material-status distributions are phase independent. At the fourth contact, c acquires a phase contrast:
Delta4(all recovered)=834316443648/3814697265625;
Delta4(partial)=Gamma*Delta4(all recovered).
Explicit pointer propagation and reduced live densities agree at each of all four contacts. This is an actual downstream material readout, not a target probability table or a third-contact signal.

## 3. Feedback follows the physical token

Original marked slot1 moves to slot0 after the inherited one-shift permutation. Correct feedback on the moved token restores the density. Stale-slot feedback fails on two independently split dual-rail pairs, still at sharp excitation2. A first draft used the correlated1010/0101 code, where the two controls coincide on its support and the supposed failure was absent. The test preparation was corrected, not the BRC gate; the episode is retained in IMPLEMENTATION_NOTES.md. When intervening dynamics mixes rather than permutes occupations, the required feedback is the conjugated original observable and need not remain local. No instantaneous remote feedback is inferred.

## 4. A scoped effective-dephasing certificate

On the two-configuration code, remaining-record channel D_Gamma=(1+Gamma)/2 id+(1-Gamma)/2 Z(.)Z. For any input including an arbitrary reference, trace distance between D_Gamma and full nonselective dephasing D_0 is at mostGamma/2. This follows from Gamma/2*(rho-ZrhoZ) and trace-distance normalization, and is attained by a balanced code superposition. Any common future instrument/feedback history using only allowed accessible systems preserves this total-variation bound.

For actual independent unread overlaps c3/5, canonical positive BRC overlap words give
c^17/2=129140163/1525878906250 <1/10000, while c^16/2>1/10000.
Seventeen is sufficient for that worst-case two-code tolerance. It counts physical distinguishing-record interactions, not HTTP retries, elapsed time or duplicate symbols. It is not a general many-code bound. Conditional rare historyp can amplify a Delta certificate to min(1,2Delta/p), requiring nonzero approximate likelihood. Later quantum access to a hidden record changes the allowed experiment and invalidates the earlier no-access equivalence.

This justifies an observer/resolution-scoped approximation to nonselective dephasing, NOT the derivation of one objective outcome or an assertion that the entire world lost the relation. A tiny independently specified rational marker mismatch was also retained: c=(10000^2-1)/(10000^2+1),s=20000/(10000^2+1), one missing record gives exact echo deficit576/62500000625>0. Not rounding or measured physics.

## 5. Actual multi-excitation and native-field validation

The Stage65 conserved sector/intertwining theorem is REUSED, not counted as a new theorem. Start s,G2 and four ground rails, generator capacity2; twelve actual contacts include repeated returns/refill, no refreshed inputs. Explicit complete BRC states equal compact live densities each step; the q2 sector has21 internal states and reaches441 density pairs. The complete twelve-step inverse restores initialization.

An actual unread pointer plus three subsequent status instruments with ideal/noisy report channels gives54 visible histories, each matching as ENTIRE live densities and raw likelihoods. PartialRecordReader retains returning bits, applies an actual-kernel-derived unread-record channel, maintains conditional state, and rejects later resurrection of a discarded pointer as0. Passive snapshots, idempotent events and atomic conflict handling are computational safeguards, not additional physics.

The real twelve-neighbour stored native fibre of raw weight4/625 receives the two-excitation preparation and an unread marker. Three inherited contacts/rail shifts leave372 full endpoints,41616 live density pairs. A common extra native motion yields2328 endpoints. Every explicit endpoint preserves Gauss D ell=delta_xplus-delta_xminus, one source identity, separation<=1 and total declared energy5 including the older receiver. Entire density equality was checked before the extra common native motion; preservation afterwards follows the common-channel theorem, not an unperformed second compact-motion test. Native-motion and full contact inverses were actually checked.

## 6. Delivery and status

247 named checks;49 actual unchanged canonical BRC core calls in the final fresh process. Counts include54 record histories and many full-density/basis tests, not247 independent experiments. Corebc7babbb9e890f6d5a7094430a5fbdccf66c77ad:src/enterprise_math/brc_weighted_recurrent.py, blob4e6b3132580e3cd70a20a0d8bd4d28792b961afb, SHA2567520822074b8f29e1c54f57b9543c043202db8bd7c6e27f3f1d6176028ac4a26. Existing C4 gates and pair observer are reused; no trigonometry, ordinary propagator or eigensolver.

Cumulative2b85d54e3f2016acb071620da1b3d7645c7a677f, entrySTART_HERE_STAGE66.md. New stage66/access.py,run.py,PROOF.md,RESULTS.json,BRC_CONTRACT.json,PROVENANCE.json,IMPLEMENTATION_NOTES.md,MANIFEST.json and package initializer. Fresh LOCAL clone replay matches all scientific fields after excluding only elapsed_ns recursively; original8718006042ns,replay11518112907ns retained separately.1258 inherited files unchanged,9 manifest entries verified,git fsckPASS.
BundleBRC_Heartbeat_partial_access_stage66_20260924.bundle:38080394bytes,SHA256ad0ff506ab5e8b48c8e9c9a3e1af026c67a02dff9ad2ef50ba4c166ee21bd6b1. Drive1qrewHA34IA8NS1UypIK86RQQ4Cy-4r05 uploaded and fetched; actual returned bytes cmpIDENTICAL.
ProofSHA25649c73862c2d0fcffc2ae37bb0d8c7f6c934fec95b5390472c1dcf4f5b7dd1640;codeSHA256a8a1241db4bd11b56b7d39e8de20fa68d7f5bcd188389d74ef6c135795d74c27;resultsSHA2564c62edd842d4045e27481ccdbee8ab80ad829bd22f4aee486622e98eb4b6ffd9.

Primary sources: Miatto etal arXiv1410.2313v1 Eq11 (parsed full PDF; screenshot attempt failed, no diagram-analysis claim), Gregoratti-Werner quant-ph/0209025, Pichler-Zoller1510.04646 (latter two abstracts/metadata). Partial-environment erasure, environment-assisted correction and delayed feedback are prior art. No exhaustive novelty audit or new dedicated-provider retrieval. Same author; no independent review, Lean, physical calibration, experimental fit or remote reader deployment. Previous safety-blocked session registration was not reattempted; no new ID/RA/pre_final receipt exists and no closed activity is modified.

NEXT: mixed/correlated physically prepared records with explicit access/transport/feedback costs and a many-code recovery certificate. Native phases/Born origin, autonomous timing, geometry-derived spatial fringes and actual lab data remain open. Do not repeat this partial-record product-law or two-code threshold as new work. The parent physics objective is not complete.
