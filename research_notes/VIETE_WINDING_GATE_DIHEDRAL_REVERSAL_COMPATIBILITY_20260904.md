# Viète winding-memory gate dynamics: exact dihedral sweep reversal and the central H/S V4 subgroup

Status: `FREE_RESEARCH / EXACT FINITE DYNAMICAL-SYMMETRY COMPATIBILITY / NOT_FOUNDATION`
Date: `2026-09-04`
Researcher: `EM-FREE-7D3C9A / FREE_AXIOM_DISCOVERY`
Issue: `#1158`

## 1. Question

The winding-memory construction realizes the finite precision state at level `m` as

\[
G_m=C_{3\cdot2^m}.
\]

Current discrete Cell rotation semantics separately requires that reversing local sweep orientation invert a deterministic transition:

`T_{-epsilon}=T_epsilon^{-1}`.

Does the winding-memory gate architecture reproduce this reversal law exactly while keeping the direction/deck involution `H` distinct from sweep chirality `S`?

Yes. The full finite symmetry is dihedral, with the previously found `V4` as a canonical subgroup.

## 2. Positive gate transition

Let

\[
N=3\cdot2^m
\]

and write the gate state additively as

\[
G=C_N=\mathbf Z/N\mathbf Z.
\]

Define one positive orientation/sweep step by

\[
\boxed{R(k)=k+1.}
\]

Then `R` has order `N` and generates the full finite gate cycle.

## 3. Sweep reversal as state inversion

Define

\[
\boxed{S(k)=-k.}
\]

Then

\[
S^2=\mathrm{id}.
\]

Moreover

\[
\begin{aligned}
SRS(k)
&=SR(-k)\\
&=S(-k+1)\\
&=k-1\\
&=R^{-1}(k).
\end{aligned}
\]

Therefore

\[
\boxed{SRS=R^{-1}.}
\]

This is exactly the group-theoretic form of the current rotation-reversal requirement.

Hence the finite dynamics generated by `R` and `S` is the dihedral group

\[
\boxed{D_N=C_N\rtimes C_2^S.}
\]

No continuous angle or Euclidean reflection is required for this algebraic reversal law.

## 4. Interpretation of native sweep epsilon

The current candidate native state

\[
(\rho,C,\epsilon)
\]

uses `epsilon` to distinguish the two local sweep directions.

The winding-gate process realizes the same operational distinction by choosing which transition acts:

\[
\epsilon=+\quad\leadsto\quad R,
\]

\[
\epsilon=-\quad\leadsto\quad R^{-1}.
\]

The involution `S` exchanges those two descriptions by conjugation.

Thus the winding-memory architecture has an exact candidate intertwiner for the **operation** carried by native sweep orientation:

\[
\boxed{
\text{SWEEP SIGN}
\longleftrightarrow
\{R,R^{-1}\}.
}
\]

This does not identify sweep `epsilon` with the direction/deck sheet itself.

## 5. Direction/deck half-turn is a different operation

Because `N` is even for `m>=1`, define

\[
\boxed{H=R^{N/2}.}
\]

On states,

\[
H(k)=k+N/2.
\]

This is the central half-period/deck transformation.

It is not sweep reversal. Instead:

\[
H^2=\mathrm{id},
\]

and since `H` is a central power of `R`,

\[
SH=HS.
\]

Therefore the two binary operations generate

\[
\boxed{
\langle H,S\rangle\cong C_2^H\times C_2^S=V_4.
}
\]

This recovers the independent finite `V4` theorem from a dynamical presentation.

## 6. H is the kernel/deck action of one binary precision cover

The coarse-graining map

\[
p_m:C_{3\cdot2^m}\to C_{3\cdot2^{m-1}}
\]

is reduction modulo

\[
3\cdot2^{m-1}.
\]

Its kernel is

\[
\{0,3\cdot2^{m-1}\}
=
\{0,N/2\}.
\]

Thus the nontrivial deck element is exactly

\[
\boxed{H=R^{N/2}.}
\]

So the binary cover sheet and the half-turn operation are the same `H` object in the finite winding-state dynamics.

## 7. Sweep reversal descends through every precision quotient

For the reduction map `p_m`,

\[
p_m(Sk)=p_m(-k)=-p_m(k)=S(p_m(k)).
\]

Hence

\[
\boxed{p_m\circ S=S\circ p_m.}
\]

Similarly,

\[
p_m\circ R=R\circ p_m
\]

for the coarse generator understood at the quotient level.

Therefore sweep reversal and positive/negative transition semantics are compatible with every binary precision coarse-graining.

The process does not need to redefine chirality at each resolution.

## 8. Winding-coordinate form

The Chinese remainder decomposition gives

\[
C_{3\cdot2^m}
\cong
C_3\times\mathbf Z/2^m\mathbf Z.
\]

The second coordinate is the finite winding-resolution memory.

Under an appropriate compatible CRT gauge:

- one complete coarse `C3` winding increments the 2-primary coordinate by `1`;
- the deck involution adds `2^{m-1}`;
- sweep reversal negates the 2-primary coordinate.

Thus the two operations have the process-memory meanings

\[
\boxed{H:w\mapsto w+2^{m-1}}
\]

and

\[
\boxed{S:w\mapsto-w}
\]

on the binary winding coordinate, up to the ordinary choice of local cut/gauge for intermediate coarse positions.

These commute because

\[
-(w+2^{m-1})\equiv -w+2^{m-1}\pmod{2^m}.
\]

## 9. Quarter-turn degeneration inside the dihedral action

At the first quarter-turn layer `C12`, the principal root state is

\[
k=3=N/4.
\]

Then

\[
H(k)=k+N/2=9
\]

and

\[
S(k)=-k=9.
\]

Thus `H` and `S` act identically on that specific two-state orbit even though they are different group operations.

At `C24`, the principal state `k=3` gives

\[
H(3)=15,
\qquad
S(3)=21,
\]

so the four-state `V4` orbit appears.

The dihedral model therefore explains the seed degeneracy and its immediate resolution without conflating the two `C2` generators.

## 10. Character action

Under a compatible one-dimensional character,

\[
R\mapsto u
\]

for a primitive finite phase.

Then

\[
H=R^{N/2}\mapsto-1,
\]

while state inversion/sweep reversal sends

\[
u\mapsto u^{-1}.
\]

Therefore the character readout reproduces exactly

\[
H:u\mapsto-u
\]

on a state shifted by the half-period and

\[
S:u\mapsto u^{-1}.
\]

The two binary semantics are already present in the finite dihedral process before any continuous circle is completed.

## 11. Why this is stronger than cardinality matching

A six-state set by itself says almost nothing.

The winding-memory architecture now satisfies simultaneously:

1. cyclic positive transition;
2. exact negative-sweep inverse transition;
3. a central half-period deck involution;
4. commuting but distinct `H` and `S` binary operations;
5. compatible binary coarse-graining;
6. the quarter-turn `H=S` orbit degeneration;
7. the deep `V4` split;
8. the correct finite character actions used by the Viète root algebra.

Thus its agreement with current rotation semantics is algebraic/dynamical, not merely numerical or state-count based.

## 12. Native status

This theorem proves **compatibility and sufficiency** of the winding-memory gate architecture with the frozen reversal law.

It still does not prove that actual Enterprise Cell dynamics must retain winding modulo powers of two or must have the cyclic gate quotient assumed here.

Therefore:

`WINDING_GATE_DIHEDRAL_MODEL = EXACT_FINITE_COMPATIBILITY_MODEL`.

`CURRENT_P000_SELECTS_WINDING_GATE_DIHEDRAL_MODEL = NOT_PROVED`.

The remaining native question is selection/effectivity, not internal consistency.
