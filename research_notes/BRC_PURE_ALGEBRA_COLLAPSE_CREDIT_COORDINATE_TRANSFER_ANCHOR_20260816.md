# BRC Pure-Algebra Collapse / Post-Credit / Three-Axis Transfer Anchor

Status: `DRIVER_RESEARCH_ANCHOR / PRE-TASKBOOK`
Date: `2026-08-16`

## Route correction

BRC collapse is currently to be understood first as a **pure algebraic collapse choice**, e.g.

`5 -> 4` versus `5 -> 9`,

not as path merging, endpoint argmax, or residual path-language selection.

For `k^p < n < (k+1)^p`, write

`L=k^p`, `U=(k+1)^p`, `G=U-L`, and a collapse bit `b in {0,1}`:

`C_p(n)=L+G b`.

Thus `b=0` is lower/down collapse and `b=1` is upper/up collapse.

The current core hypothesis is that BRC internal structure is discovered by **post-credit assignment from independently known correct macro mathematics/experience back onto these algebraic collapse bits and their higher-order interactions**. Do not replace this with arbitrary reward weights or path enumeration.

## Signed collapse residue

For `n=L+d`,

`rho(n,b)=n-C_p(n)=d-Gb`.

Down/up choices therefore carry exact signed algebraic residuals. Post-credit should study how these residuals and their interactions cancel, reinforce, or remain compatible with known higher-level identities.

## Three-axis plane transfer hypothesis

For a two-dimensional three-axis coordinate carrier, freeze an affine gauge/constraint

`x+y+z = K`.

An elementary `+y` increment cannot be `(0,+1,0)` while preserving the constraint. The compensating change in the other coordinates must sum to `-1`.

Under minimal integer support, exactly two local states exist:

`(-1,+1,0)` and `(0,+1,-1)`.

From `(100,0,0)` these are

`(99,1,0)` and `(100,1,-1)`.

The unresolved symmetric pre-collapse carrier is naturally

`(-1/2,+1,-1/2)`

relative to the starting state. At integer precision, each `-1/2` has two adjacent collapses `{0,-1}`; the affine constraint forces complementary choices: exactly one transverse coordinate collapses to `-1` and the other to `0`.

Introduce one bit `b in {0,1}`:

`Delta_y^+(b)=(-b, +1, -(1-b))`.

So

- `b=1 -> (-1,+1,0)`;
- `b=0 -> (0,+1,-1)`.

More generally, for target coordinate `i`, sign `s in {+1,-1}`, and the other two coordinates `j,k`:

`Delta_i^s(b): Delta_i=s, Delta_j=-s b, Delta_k=-s(1-b)`.

This preserves `Delta_x+Delta_y+Delta_z=0` exactly.

The union of these minimal transfer vectors is

`D6={e_i-e_j : i != j}`,

equivalent to the current six vectors `+/-u,+/-v,+/-w` with

`u=(1,-1,0)`, `v=(0,1,-1)`, `w=(-1,0,1)`.

## Key interpretation

This suggests a concrete internal BRC decomposition:

1. a macro coordinate increment creates an unresolved symmetric fractional compensation across the other two axes;
2. pure algebraic up/down collapse converts the fractional compensation into complementary integer collapse bits;
3. the affine conservation constraint removes two of the four naive bit combinations;
4. two exact local branches remain;
5. post-credit from independently known macro laws determines whether one branch is selected, both remain admissible, or later structure is required.

Thus the six local directed vectors may arise as **integer transfer states generated by coupled complementary algebraic collapse**, not as six arbitrary direction labels.

## Critical firewall

This is a research hypothesis, not yet a theorem of the project. The `1/2` symmetric carrier may be derived from transverse exchange symmetry plus the affine sum constraint, but its native status and relation to later refounded angle/metric semantics remain to be proved.

Do not resume Stage-N path-language work until this pure-algebra BRC route is resolved or explicitly released by Driver.
