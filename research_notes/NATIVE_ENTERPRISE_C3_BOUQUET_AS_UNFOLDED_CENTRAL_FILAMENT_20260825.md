# Native Enterprise C3 bouquet as the unfolded even-shell central filament

Status: `FREE_RESEARCH_EXACT_CROSS_ROUTE_IDENTIFICATION / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-25`

Researcher-ID: `EM-FREE-NEPS-239A6D`

## 1. Two previously separate routes

The native-prime program produced two apparently different objects:

1. the equal-coordinate C3 bouquet

`6m^2-2m+1`,

`6m^2+1`,

`6m^2+2m+1`;

2. the long odd-curvature filament

`F_3(H,r)=H+(3r^2+eps(r))/2`.

They are in fact the same carrier viewed in two directions.

## 2. Central filament coordinate

In the tri-sector shell-fiber presentation,

`N(r,t,sigma)=B_r+t+sigma*r`,

with

`B_r=1+3r(r-1)/2`.

The long sigma-1 filament uses

`t=h+ceil(r/2)`.

Take the central transverse coordinate

`h=0`.

The corresponding abstract odd-curvature intercept is

`H=h+1=1`.

Hence the central filament is

`F_3(1,r)=1+(3r^2+eps(r))/2`.

## 3. Even shell and equal-coordinate point

Let

`r=2m`.

Then

`t=ceil(r/2)=m`.

This is exactly the equal-coordinate midpoint of a sector side.

The sigma-1 central value is

`c_m=F_3(1,2m)=6m^2+1`.

## 4. C3 unfolding

At fixed shell `r` and side position `t`, the three C3 slot values differ by exactly `r`:

`N_0=c-r`,

`N_1=c`,

`N_2=c+r`.

For `r=2m`, `c=6m^2+1`, this gives

`c-r=6m^2-2m+1`,

`c=6m^2+1`,

`c+r=6m^2+2m+1`.

Therefore

`C3 EQUAL-COORDINATE BOUQUET`

`=`

`C3 UNFOLDING OF THE h=0 EVEN-SHELL CENTRAL FILAMENT`.

This is an exact identity, not a statistical resemblance.

## 5. Product form

The bouquet product becomes

`P_m=(c-r)*c*(c+r)`

`=c*(c^2-r^2)`.

With `c=6m^2+1`, `r=2m`,

`P_m=216m^6+84m^4+14m^2+1`.

Thus the previously frozen bouquet product is the cubic norm of one unfolded C3 fiber.

## 6. 105 gate restated on the filament shell

The exact simultaneous-prime necessary gate was

`105|m`.

Since `r=2m`, this is equivalently

`210|r`.

Therefore the bouquet statement can be read directly on the central filament:

`THE C3 UNFOLDING OF h=0 CAN BE THREE-PRIME ONLY ON SHELLS r DIVISIBLE BY 210`

outside the already isolated tiny exceptions.

So the 105/210 modular recoalescence belongs to the same central filament carrier used by the later breaker theory.

## 7. Longitudinal versus transverse arithmetic

The unified carrier has two distinct arithmetic readouts.

### Longitudinal readout

Move in shell index `r` along fixed `h`.

This gives:

- quadratic-plus-parity dynamics;
- split-hyperbola tangent arrangement;
- universal breaker classification;
- breaker-coprime capacities.

### C3 transverse readout

At fixed even shell `r`, unfold the central point through the three sector slots.

This gives:

- the AP triple `c-r,c,c+r`;
- the equal-coordinate bouquet;
- the `105|m` simultaneous-prime gate.

Thus the early C3 bouquet and later filament phase theory are orthogonal readouts of one native shell-fiber object rather than unrelated constructions.

## 8. A 105 coherence identity

For the native specialization, three independently frozen quantities now meet numerically and structurally.

### Bouquet gate

The maximal small-prime saturation gate is

`3*5*7=105`.

### Filament breaker capacity

The first breaker is `5`, so its breaker-coprime capacity is

`k_*=2*5-1=9`.

### k=9 tangent sample extremum

For an odd length-k window, the maximal mixed-parity distance product is

`M_k=(k-2)(k-4)`.

At `k=9`,

`M_9=7*5=35`.

Multiplying by the native curvature coefficient gives

`B*M_9=3*7*5=105`.

Hence

`BOUQUET GATE 105 = NATIVE CURVATURE * SHARP9 TANGENT-SAMPLE EXTREMUM`.

Equivalently,

`105 = 3*(9-2)*(9-4)`.

This explains why the same integer reappears in the early C3 sieve and the terminal tangent-discriminant calculation.

## 9. Consequence for 53

The terminal k=9 local exceptional characteristic comes from

`B*M_9+1=105+1=106=2*53`.

Thus

`53=(105+1)/2`.

The chain can therefore be written coherently as

`native sector/curvature 3`

`-> global breaker 5`

`-> breaker capacity 9`

`-> tangent distance factors 7 and5`

`-> native extremal obstruction 105`

`-> terminal sampled-tangent characteristic 53`.

This is an exact arithmetic coherence chain.

## 10. Scope guard

The equality of the two occurrences of `105` is exact, and the C3 bouquet/central-filament identification is exact.

However, this note does **not** claim that the bouquet's modular-saturation proof and the tangent-discriminant proof are the same proof. They are two distinct arithmetic operations on the same native carrier that happen to meet at the same native extremal scalar.

Current classification:

`EXACT CROSS-ROUTE COHERENCE / CAUSAL NOVELTY UNRESOLVED`.

## 11. Research significance

The native-prime program now has one carrier rather than two disconnected stories:

`CENTRAL NATIVE FILAMENT`

`-> LONGITUDINAL HYPERBOLA/BREAKER GEOMETRY`

and

`-> TRANSVERSE C3 BOUQUET/105 GEOMETRY`.

This substantially strengthens the case that the constants `3,5,7,9,105,53` are organized by the native shell-fiber allocation rather than arising from unrelated post-hoc fits.