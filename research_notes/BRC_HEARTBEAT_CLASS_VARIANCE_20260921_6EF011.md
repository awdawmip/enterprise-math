# Heartbeat: multiple coherent classes and variance-resolved imperfections

Event-ID: brc-heartbeat-class-variance-20260921-6EF011
Research-Activity-ID: RA-6EF011C2E75C4A799606AFEA
Status: RESEARCH_CANDIDATE; same-author proofs and tests, no independent review or Lean.
Parent standalone: 2fde6b21cdfd99c71aca4a0eb9a60cfe1510f2c8.
Read snapshots: global7fbc736d5c013dccb3c4114a6c4c127616ee2fcc; projectcc98d1f33af1508c3e1388697fa421a0afe6d4ea. P000 and residual-first constraints unchanged.

Stage29 replaces the small-number-of-marked-sites representation with several entire interaction classes. Only permutations within each class are averaged. The exact subgroup theorem preserves central-only instruments and complete joint histories whenever the Hamiltonian is invariant within classes. Heterogeneous complex coherent full-rank PRODUCT preparations use Stage27.compile_bath unchanged in each class. All irrep weights and complex matrices are retained; classes propagate together, not as independent reduced states. Arbitrary correlated initial preparation is covered by the abstract quotient but not by this compiler.

The fixed effective model has J X_S, class-dependent transverse fields, u N^2/K, the previous collective longitudinal term, and class-dependent centered terms Z_S e_a(N_a-M_a N/K)/2. It is nonquadratic, collective, and not a derived geometrically local native realization. No resets, fresh baths or new spatial axes are introduced.

In irrep tuple n_a, occupation is k_a=(M_a-n_a)/2+q_a. The unnormalized basis has Gram product_a binomial(n_a,q_a); rational flip coefficients are q_a and n_a-q_a. An adapter executes the unchanged Stage25 integer Taylor propagator. Product matrix contractions retain every phase and all multiplicities. Raw probabilities are never artificially normalized. Total joint density capacity is 4 product_a binomial(M_a+3,3), polynomial only for a fixed number of classes and exponential when every site is its own class. Dense contraction, bit growth and Taylor time steps are separately charged.

Frozen main plan: five cases, three repeats, 15 saved rows. Two classes4+4/6+6/8+8 with coefficients+1/2,-1/2 at model time1 give central populations0.3528835122638171/0.35253220550794695/0.3521889129680449. Median total computation0.566383270/4.097061268/19.817903448 seconds. Joint density slots4900/28224/108900; propagator columns162/512/1250. The8+8 case's largest individual block has dimension162. Representing one whole eight-site class as eight independent marked sites would instead have43253760 slots; that larger calculation was NOT run. Two3-site classes at time16 give0.5140547098184698 in median1.834716091s. Three2-site classes with distinct transverse fields at time4 give0.5201427467673928 in1.386006244s. Main numerical probability errors are below1.03e-20. These are computational, not physical times; no CPU affinity or optimal-classical-method comparison.

A symmetry-variance lemma is proved for finite groups. If T(V)=0, H0 and the observed subsystem respect the group, the ACTUAL initial state is invariant, and kappa=||T(V^2)||, then reduced trace error is at most min(1,||V||t/hbar,kappa t^2/hbar^2). Canonical purification makes the mixed invariant state an invariant vector. On that enlarged space PVP=0 and ||QVP||^2<=kappa. Leakage norm is <=sqrt(kappa)t/hbar; its return changes the retained amplitude by <=kappa t^2/(2hbar^2). Cross P/Q terms vanish only at the invariant observer. This controls center observations, not full joint trace error. Standard projection and Duhamel methods are used; global novelty is not established.

For arbitrary initial preparation with a=D(Omega,T Omega), the certified bound becomes min(1,x,2a x+kappa t^2/hbar^2), x=||V||t/hbar. One must not set a=0 merely because H0's compiler averages the input. A time-dependent extension replaces sqrt(kappa)t/hbar by the integral of sqrt(||T(V(s)^2)||)/hbar; central instruments can be included without reinitializing bath histories.

For actual local longitudinal coefficients e_i, retain each class mean and let delta_i be the centered residual. V=Z_S sum_i delta_i n_i/2, v=sum_i|delta_i|/4 exactly. Direct permutation averaging proves T(V^2)=sum_a S2_a N_a(M_a-N_a)/(4M_a(M_a-1)), S2_a=sum_class delta_i^2. Thus kappa=sum_a floor(M_a^2/4)S2_a/(4M_a(M_a-1)); singleton contribution is zero. Residuals need not be independent noise or exactly vanish.

Two six-site classes with coefficients+1/2 +/-1/100 and-1/2 +/-1/100 have v3/100 and kappa9/100000. At time4 the invariant-preparation bound is0.00144, versus the generic0.12 and the worst-norm quadratic0.0144. An exact rational dynamic program selects the minimum-kappa partition among a declared family of sorted contiguous segments, retaining original site IDs. It does not claim optimum over arbitrary partitions or minimum actual runtime.

A separately fixed globally iid coherent full-rank12-site input validates that preparation condition for every candidate partition. One/two/three/four classes give residual bounds1/0.00144/0.00072/0 and matrix capacities1820/28224/134400/640000. Only one and two classes were propagated; three/four are certificate-only results. Two classes take14.808638435s in one diagnostic run and give center population0.46016009174974787, with the true imperfect model in approximately[0.4587200917,0.4616000918], plus explicitly included numerical error. Four classes give zero MODEL residual because four distinct coefficients are represented exactly, not because physical imperfection ceases to exist.

A four-site invariant-within-class preparation with nonzero residuals+/-10^-8 gives actual-minus-class-mean probability-2.1415116148696355e-17 at time4. Unchanged Stage28 all-explicit propagation and the class solver at120bits enclose the difference with radius2.281e-40 or less; a separate100-digit full-Hilbert calculation agrees. A heterogeneous two-site preparation instead has a first-order response about4.96045e-12, exceeding the invalid invariant-only4e-16 bound. Its asymmetry-aware bound holds. Cross-class reassignment of the same four input factors also changes the center by about1.92486063e-5; within-class reassignment is exactly invisible. No discarded labels are reconstructed from a quotient.

Validation:7789 finite assertions, including15 full-space central states,5 two-time histories,3 unchanged Stage28 singleton comparisons,3 unchanged Stage27 fully symmetric comparisons,147 exact twirled-square identities,18 exact residual norms,24 dynamical variance tests and25 exhaustive contiguous-partition optima. A separate84-check diagnostic uses arbitrary correlated invariant states and general zero-twirl Hermitian residuals. Three100-digit full-Hilbert cases fit directed candidate intervals; two further100-digit phase/asymmetry diagnostics are retained. All15 raw rows and five saved medians are replayed without replacing timing data. Candidate and references are separately coded but same-author.

Marking or grouping changes representation, not purity supply. Finite-device full-rank floors can still decrease with resource size. No universal zero-temperature nonexistence, generic quantum simulation, new BRC family or Shor speedup is claimed. Established context includes Bastin-Martin2307.06141 and Jakab-Zimboras2001.08310. Complete derivations, code, frozen plans and records are in the cumulative standalone bundle, not the entire enterprise-math repository.

Next: a geometrically local neighbourhood with an outside-memory bound, or sparse contractions avoiding the growing product of class dimensions. Preserve the exact class compiler, preparation-asymmetry condition, variance residual and all measured costs. Existing activity identity is retained; aggregate checkpoint intake must be verified separately from source and Drive publication.
