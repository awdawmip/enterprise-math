# Viète / X6 rotation research — state-machine handoff

Status: `FREE_RESEARCH / DURABLE HANDOFF / TASK-PUBLICATION SOURCE / NOT_FOUNDATION`
Date: `2026-09-09`
Researcher: `EM-FREE-7D3C9A / FREE_AXIOM_DISCOVERY`
Research activity: `RA-7D3C9A1255A6E9B4F21C7D`
Parent lineage: completed `#1158` -> open/retyped `#1255`

## 1. Executive state

The original #1158 Viète mother question is complete: the nested radical / normalized-bisector recursion, target-free finite precision readout, convergence/completion, classical compatibility, and precision-pro-state interpretation were closed earlier.

The stronger #1255 question exposed a type error in the original global formulation. The derived precision carrier

\[
\mathcal P_{\rm rot}=\varprojlim_m C_{6\cdot 2^m}\cong C_3\times\mathbf Z_2
\]

is **not** the physical trajectory-time phase space. Physical C6/C12 refinement uses two fine physical microsteps per one coarse step, whereas the inverse-limit precision projection is a same-address resolution relation. The synchronous trajectory-to-odometer interpretation is therefore a no-go.

The surviving positive structure is a bundle/section picture:

- native trajectory/path dynamics lives in the X6 Cell/path base;
- an oriented half-turn/root operation selects a static precision/root lineage in `P_rot`;
- forward/reverse sweep chirality selects the two principal addresses `+3/-3`;
- precision depth is an observer/refinement coordinate, not native time.

The current unresolved work is no longer the Viète recursion. It is the **operation-safe native rotation/path law and its minimal composable provenance state**.

## 2. Mandatory current typing

Current P000 / X6 authority must be consumed before extending this line:

- `p000_reality_foundation.json` — signed six-axis discrete Cell space, time as trace/order of relational change;
- `definitions/P000_DISCRETE_DIRECTION_TRIADIC_BALANCE_20260905.md` — off-axis objects are composite native paths; ordered steps/branch/multiplicity/provenance cannot be erased without a safe observer lease;
- `definitions/ENTERPRISE_X6_NATIVE_SPATIAL_CELL_TORSOR_20260905.md`;
- `definitions/00_CURRENT_NATIVE_FOUNDATION.md`;
- current joint observer-preservation contract;
- current BRC first-line observer/provenance discipline.

Hard boundary: `TRIADIC_CLOSURE_E` is a primitive **force-event** stability predicate. It may not be silently replaced by an ordinary coordinate vector-sum condition and therefore has not been used to select the OUTER rotation path.

## 3. Do not redo — durable Viète/X6 frontier

### A. Centered-X6 fixed-radius and native C12 microtrace

`research_notes/VIETE_X6_FIXED_RADIUS_MICROTRACE_C12_20260905.md`
commit `e91bdc1ebfb3cbe4004c5920d0b80ff733d79eaa`
checker commit `3435b1a4e59b029037ee6bdb026091afc1be8d27`

Closed:

- no primitive signed-axis Cell step preserves exact native radius;
- every adjacent signed-C6 macrostep has exactly two shortest native BRC lifts, INNER and OUTER;
- the OUTER intermediate Cell is the exact first half-angle/root direction on the FCC STAR readout;
- the all-OUTER choice gives a true 12-Cell primitive native cycle;
- previous/current macro phase recovers local STAR frame/sweep, but not generic INNER/OUTER provenance.

### B. Physical-time clock versus precision clock

`research_notes/VIETE_PHASE_REFINEMENT_VS_PRECISION_PROSTATE_CLOCK_20260905.md`
commit `4bdb7159eebfa90808096a84d1f912a8107868ea`

Closed:

- physical phase refinement satisfies a two-fine-step / one-coarse-step relation;
- standard precision inverse-limit projection is not the physical phase-collapse map;
- synchronous `trajectory successor -> +1 on P_rot` is the wrong type.

Related finite-period/memory obstruction:
commit `47c765c54e7ae48d0ca32d49ac24642062bd13c5`.

### C. Native half-turn -> principal root bundle section

`research_notes/VIETE_NATIVE_HALF_TURN_PRINCIPAL_ROOT_BUNDLE_20260905.md`
commit `a72226e2430ffa4786321ac86e7f0c599d5f0389`
checker commit `317e6cd986f9422f714ee03a8464573164c91721`

Closed on the declared OUTER half-turn scope:

\[
\Sigma(A(k,\varepsilon))=3\varepsilon\in\mathcal P_{\rm rot}.
\]

At C6 the two signs coincide as the same half-turn; at C12+ they split into inverse quarter/root towers. Endpoint half-turn preserves the relative root lineage; sweep reversal negates it.

### D. All 20 native triads have intrinsic signed C6/C12 path structure

Parallel X6 upper result:

- `research_notes/X6_TRIADIC_ROTATION_GENERATORS_V1_20260906.md`;
- `research_notes/X6_ROTATION_PATH_GROUPOID_V2_20260906.md` (consume the path carrier, but see the dagger correction below);
- merge commit `40996d9239309bc703435ffc3d814932638b0e94`.

For every selected triad `S=(i,j,k)`, the signed frame generator

\[
Q_S(e_i)=-e_j,\quad Q_S(e_j)=-e_k,\quad Q_S(e_k)=-e_i
\]

has order six, and the all-OUTER lift gives a native 12-Cell microcycle. The generated frame group is

\[
(C_2)^6\rtimes A_6.
\]

The full concrete path/provenance layer remains richer than the frame action.

### E. All-20 Viète C12/C24 root anchor

`research_notes/VIETE_X6_ALL20_TRIAD_ROOT_C12_C24_20260906.md`
commit `57440ab85f4b95b6085d8f00fe630c7a8f941ad1`
checker commit `bb15238647f0842905512644a89e32e6a8205725`

For every oriented triad, the actual quarter-root Cell on the OUTER half-turn is a balanced support-two Cell such as

\[
-e_j+e_k,
\qquad L_E^2=2.
\]

The same native Cell supplies:

- the C12 quarter character `J` at projective-character strength;
- after normalization, the C24 distinguished balanced spinor `S[1:1]=(1+J)/sqrt(2)`.

Thus all 20 native triads have the intrinsic X6 C6/C12/C24 root bridge. Only the four established FCC STAR triads additionally possess the current planar FCC circle/gate/bisector interpretation.

### F. Exact 20-triad FCC carrier census

`research_notes/VIETE_X6_20_TRIAD_FCC_CARRIER_CENSUS_20260906.md`
commit `1a40b539f3da05aab9dec333e3fd2695cf0c8a6e`
checker commit `73127e208605cf6423adbd31b6fa1742c3cd1d85`

Under the fixed FCC six-line carrier:

\[
20=4\text{ STAR}+4\text{ triangle}+12\text{ path}.
\]

The four STAR triples are rank-2 planar C6 carriers. The other sixteen triples are rank-3 determinant-2 bases of the full FCC parity lattice and are not additional triangular carrier planes. Do not use this carrier fact to deny their native X6 triadic C6/C12 cycles.

### G. K4/FCC atlas root-sign transport

`research_notes/VIETE_K4_ROOT_ATLAS_C24_SPINOR_20260906.md`
commit `435b3f36d0192997634c1ee2db4ce7c2e072a295`
checker commit `51f46265710a3aa86e01eaa41487a439674cd184`

`research_notes/VIETE_K4_OVERLAP_REMOTE_PORT_CONNECTION_20260906.md`
commit `b18177ddc5dfba8be60e8e674aa3713a73017830`
checker commit `952993c99340ef7cce7542b5573ab2182a3e9328`

On the four FCC STAR atlas, the root-sign line is twisted exactly like the local Euler chirality line. For one shared K4 edge `ij`, the S4 axis-permutation skeleton admits exactly two natural overlap transports fixing that shared line:

\[
T^-_{ij}=(ij),\qquad T^+_{ij}=(ij)(k\ell).
\]

They differ by whether the two remote atlas ports are preserved or swapped and therefore by chirality sign. The six edge choices realize the full 64 edge-cochain population; face holonomy is the orientation parity of the actual S4 loop permutation. Selecting the physical connection remains open.

### H. Unique deterministic root-compatible shortest branch law

checker commit `e6f018e0f4648b35fe60ae187357d79fdf778cb5`.

For one `Q_S` cycle the six macro edges give 64 shortest branch words. Requiring `Q_S`-equivariance reduces deterministic branch laws to exactly

`IIIIII` and `OOOOOO`.

Requiring a faithful nonzero C12 microphase/root cycle kills the all-INNER law and uniquely selects all-OUTER.

This is a **root-compatible deterministic-law uniqueness theorem**, not a proof that all physical native rotation dynamics must choose OUTER under every observer.

## 4. Latest type correction and path-jet threshold — do not lose

`research_notes/X6_ROTATION_PATH_DAGGER_AND_BRANCH_JET_V3_20260909.md`
commit `9323a0a39199d780439b4e64cbe2506ec824fe60`
checker commit `1d782f657f6f9f2dfacd28d73a4f9cc9c7562eea`

### Concrete path category correction

If concrete path history is retained and composition is literal concatenation, reversal is a dagger/involution, not a true categorical inverse. A forward path followed by its reversal is a nonempty backtracking loop, not the empty identity. Therefore the provenance-preserving object must be treated as a dagger/path category unless an explicit cancellation/homotopy quotient is introduced and proved observer-safe.

### Exact branch-jet census

For primitive steps `s_r`, define

\[
S_k(\gamma)=\sum_{r_1<\cdots<r_k}s_{r_1}\otimes\cdots\otimes s_{r_k}.
\]

Concatenation obeys

\[
S_n(\gamma\delta)=\sum_{p+q=n}S_p(\gamma)\otimes S_q(\delta).
\]

On the 64 concatenated-shortest branch words of one complete `Q_S` cycle:

\[
\#J_{\le1}=1,\qquad
\#J_{\le2}=27,\qquad
\#J_{\le3}=64.
\]

Degree two records only the three antipodal branch-pair counts `0/1/2`; degree three is the first lossless truncation among degrees one through three on this finite population. No global arbitrary-length sufficiency claim has been made.

## 5. Exact remaining research units

The next state-machine tasks are intentionally separated so a new researcher can claim one without reopening closed Viète work.

1. `RS-EM7D3C9A-1255-PATH-DAGGER-CATEGORY`
   - formalize/audit the concrete-path dagger-category correction;
   - identify any safe quotient that is genuinely a groupoid.

2. `RS-EM7D3C9A-1255-PATH-JET-ORDER3`
   - prove/formalize the exact `1/27/64` branch-jet threshold and covariance over all 20 triads;
   - freeze its exact observer lease.

3. `RS-EM7D3C9A-1255-GLOBAL-JET-COMPOSITION`
   - continuation after the first two tasks;
   - determine whether any fixed finite jet order gives an operation-safe quotient for longer/arbitrary rotation paths, or prove a growing-order/no-fixed-order obstruction.

4. `RS-EM7D3C9A-1255-ROTATION-LAW-ATLAS`
   - integrate root-compatible OUTER uniqueness with the full path/BRC carrier and the K4 remote-port overlap bit;
   - derive or refute physical/native selection of branch law and atlas connection without misusing `TRIADIC_CLOSURE_E`.

## 6. Dependency picture

```text
completed #1158
   |
   v
#1255 clock separation + native principal-root bundle
   |
   +--> all-20 Q_S / C6-C12-C24 root bridge
   |
   +--> K4 overlap transport classification
   |
   +--> concrete-path dagger correction
   |       |
   |       +--> TASK 1
   |
   +--> degree-3 finite branch-jet threshold
           |
           +--> TASK 2

TASK 1 + TASK 2
       |
       v
     TASK 3  (global composition / finite-order sufficiency or no-go)

all-20 root bridge + K4 transport + BRC path carrier
       |
       v
     TASK 4  (actual law / atlas selection)
```

## 7. Handoff safety rules

A claimant must not:

- reopen #1158 nested-radical derivation as if it were unresolved;
- identify `P_rot` precision depth with physical time;
- call a concrete-history path reversal a categorical inverse without an explicit cancellation quotient;
- erase INNER/OUTER provenance before proving the declared future operations factor through the chosen observer;
- use force-level `TRIADIC_CLOSURE_E` as a coordinate/path branch-selection theorem;
- call the sixteen non-STAR native triads additional planar FCC STAR charts;
- promote any FREE result here to Foundation or Working Truth merely because it is durable or state-machine published.

The state-machine tasks are research handoffs, not mathematical acceptance.
