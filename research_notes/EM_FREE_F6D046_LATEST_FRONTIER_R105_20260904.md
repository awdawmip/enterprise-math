# EM-FREE-F6D046 latest frontier through R105

Status: `FREE_RESEARCH / CORRECTION-AWARE DURABLE HANDOFF / DERIVED / NOT_AXIOM / NOT_FOUNDATION`

Date: `2026-09-04`

Researcher: `EM-FREE-F6D046 / FREE_AXIOM_DISCOVERY`

Parent candidate: `EM-FREE-F6D046-C1-ROTATIONAL-PERIOD-WRONSKIAN` — remains `REJECT_AS_NEW_AXIOM`.

## Verified closed chain

The continuation after the Ramanujan period/Wronskian and common-cover work now closes the following stages.

### 1. Characteristic-zero mixed Prym

For the dimension-four mixed Prym `P46`:

- `P46/Qbar` is geometrically simple;
- `End^0_Qbar(P46)=Q(i)` and `End_Qbar(P46)=Z[i]`;
- `Aut_Qbar(C46)=C4`;
- the Mumford--Tate group is the full unitary similitude group of signature `(3,1)`;
- the connected Sato--Tate group is `U(4)` and the full component group is `C2`;
- Hodge and Tate classes on all self-powers are generated by divisor classes.

### 2. The p=7 Honda--Tate and explicit Jacobian model

With

`f7(X)=X^8+5X^6+245X^2+2401=h(X^2)` and
`h(T)=T^4+5T^3+245T+2401`, one has

- `P46,F49 ~ B^2`, where `B/F49` is absolutely simple;
- an explicit genus-two curve `C7/F49` satisfies `B ~ Jac(C7)`;
- `P46,F7 ~ Res_F49/F7 Jac(C7)`;
- `End^0_QbarF7(P46,7)=M2(F)` for a quartic primitive CM field `F`;
- the square splitting is not induced by any automorphism of the special genus-five curve, whose full geometric automorphism group remains `C4`.

### 3. Quartic CM and reflex

The CM field satisfies

- `F+=Q(sqrt(417))`;
- its normal closure is `D4` and equals `F(sqrt(6))`;
- the primitive reflex field has real subfield `Q(sqrt(6))`;
- double reflex returns `Q(sqrt(417))` and the original primitive CM type;
- the Frobenius--Verschiebung order `R=Z[alpha,49/alpha]` has index `14` in `O_F`.

### 4. Reduction and p-divisible classification

At inert primes `p=3 mod 4`, `P_p(X)=H_p(X^2)` and after quadratic base change the characteristic polynomial is `H_p(T)^2`. At `p=7`:

- Newton slopes are `0^2,(1/2)^4,1^2`;
- the reduction is the inert signature-`(3,1)` mu-ordinary stratum;
- the explicit Prym Cartier matrix has rank and stable rank `2`;
- `p-rank(P46,7)=2`, `a(P46,7)=2`;
- Ekedahl--Oort final type is `(1,2,2,2)`;
- `P46,7[7] = L^2 direct_sum I_11^2`;
- the full p-divisible group is `(Q7/Z7)^2 direct_sum G_(1/2)^2 direct_sum mu_(7^infty)^2`.

### 5. Polarization descent and arithmetic holonomy

The Prym polarization has kernel `E[2]`. Frobenius acts on it with irreducible polynomial `T^2+T+1`, so:

- its three degree-two principalizations are first defined over `F_(7^3)`;
- the geometric endomorphism character is killed over `F_(7^2)`;
- simultaneous strictification occurs first over `F_(7^6)`;
- the combined finite-etale holonomy is `C2 x C3 = C6`.

### 6. Two-adic integral mechanism

At two:

`F tensor Q2 = Q2(sqrt(-2)) x Q2(sqrt(5))`.

The second factor is unramified with residue field `F4`, and

- `ker(lambda_P) = O_unramified/2 = F4` integrally;
- the three principalization lines are `F4^x`;
- choosing a line lowers the maximal unramified local order to `Z2+2 O_unramified`, index `2`;
- the shared guaranteed global scalar order has index `28` in `O_F`;
- the CM Hasse unit index is `1`, global units reduce trivially in `F4^x`;
- the ring-class kernel and scalar-unitary local-global defect are both `C3`.

### 7. Separation from the canonical Weil restriction

The canonical Weil-restriction fourfold has maximal scalar center `O_unramified` at the unramified two-adic place. Every Prym principalization has scalar center `Z2+2 O_unramified`. Hence no Prym principalization is geometrically isomorphic, even unpolarized, to the canonical Weil-restriction abelian fourfold. Any smooth genus-four Jacobian realization must therefore be a genuinely different principally polarized object, not a relabeling of the compact-type `Delta_2` boundary polarization.

## Exact current hard frontier

The remaining unresolved object is the full rank-two integral Hermitian lattice at the unramified two-adic place, including its non-scalar unitary automorphism group. It must decide:

1. whether the three `C3` ring-class principalizations are pairwise nonisomorphic as unlabelled ppav;
2. whether any of them has an indecomposable theta divisor and lies in the smooth genus-four Jacobian locus;
3. the explicit low-bidegree correspondence realizing a rank-one idempotent of `M2(F)`.

Frobenius polynomials, rational endomorphism algebras, Newton/EO data, scalar CM units and scalar center orders are now exhausted: they cannot decide the non-scalar Hermitian-lattice orbit.

## Scope freeze

- `P000` is unchanged and was not challenged.
- No new axiom candidate is opened.
- All theorems are derived and source-scoped.
- The recovery placeholder `EM_FREE_F6D046_R40_R44_CM_REFLEX_REDUCTION_STRATIFICATION_20260904.md` has no theorem authority; the authoritative R40--R44 note is the path beginning `EM_FREE_F6D046_P46_CM_REFLEX...`.

Classification: `DERIVED_CHAIN_R105 / EXACT_DURABLE_FRONTIER / NOT_NEW_AXIOM / NOT_FOUNDATION / P000_UNCHANGED`.
