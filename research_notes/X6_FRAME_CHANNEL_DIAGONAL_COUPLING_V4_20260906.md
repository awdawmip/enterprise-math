# X6 frame/channel V4: minimal triadic diagonal coupling and exact 64-fold channel kernel

Status: `FREE_RESEARCH / EXACT COUPLING THEOREM / NOT_FOUNDATION`
Date: `2026-09-06`
Tasks: `RS-X6-CELL-CHANNEL-INTERNAL-STATE`, `RS-X6-UPPER-STRUCTURE-INTEGRATION`
Depends on:
- triadic frame group `R_triad=(C2)^6 semidirect A6`;
- triadic-state-induced `C3` channel circulation V3;
- one chosen global axis-channel gauge frame from V2.

## 1. Two motions that should not be duplicated

For an oriented selected triad `S=(i,j,k)`, the native frame generator is

`Q_S=J_S rho_S`,

where

- `rho_S=(i j k)` is the positive-axis 3-cycle;
- `J_S` flips the signs of the three selected axes.

V3 independently derived the unique nontrivial cyclic-origin-invariant unsigned channel circulation on the same ordered active triad:

`U_S=(i j k)`

for one chirality, with inverse for the reversed triad.

After one global axis-channel gauge frame is chosen, these are exactly the same unsigned label permutation.

Therefore

`U_S = pi(Q_S)=rho_S`,

where

`pi:R_triad -> A6`

forgets the six sign-flip coordinates and retains the positive-axis permutation.

## 2. Coupled triadic event

Define the minimal frame-channel update for one triadic macro event by

`Q_S on signed spatial/frame labels`

and

`pi(Q_S) on unsigned channel labels`.

The coupled update is therefore the pair

`(Q_S,pi(Q_S))`.

It lies in the graph subgroup

`Gamma_pi = {(g,pi(g)) : g in R_triad} subset R_triad x A6`.

Projection onto the first factor is an isomorphism

`Gamma_pi ~= R_triad`.

Thus the unsigned channel circulation does not introduce an independent group degree of freedom in this minimal coupled law. It is a typed readout of the positive-axis permutation part of the triadic frame update.

## 3. Exact sequence and 64-fold channel kernel

The projection has exact sequence

`1 -> (C2)^6 -> R_triad --pi--> A6 -> 1`.

Therefore the full triadic frame update determines the unsigned channel update uniquely.

Conversely one unsigned channel permutation `u in A6` has exactly

`2^6=64`

signed frame elements above it, differing by independent axis sign flips.

So:

`FRAME_STATE -> CHANNEL_PERMUTATION`

is deterministic, while

`CHANNEL_PERMUTATION -> FRAME_STATE`

has a 64-element fiber.

Dropping signed frame information and retaining only unsigned channel transport is not operation-safe for observers that later inspect half-turn/sign state, local signed phase, or full X6 frame action.

## 4. Event-sequence theorem

For a finite sequence of triadic frame events

`g_1,...,g_m in R_triad`,

the cumulative frame update is

`G=g_m ... g_1`.

The corresponding minimal coupled unsigned channel update is

`U=pi(g_m)...pi(g_1)`.

Because `pi` is a homomorphism,

`U=pi(G)`.

Therefore channel holonomy generated by this minimal triadic coupling is exactly the positive-axis permutation projection of frame holonomy.

It is not an additional independent holonomy variable.

If a future channel law produces a different cumulative permutation for the same frame history, then the model contains additional internal/channel dynamics beyond this minimal coupling.

## 5. Local C6 versus channel C3

For one selected triad generator `Q=Q_S`:

`Q^6=1`,

but

`pi(Q)=rho`

has order three.

Hence the cumulative unsigned channel transport along the six frame phases is

`1, rho, rho^2, 1, rho, rho^2`.

The triadic half-turn

`Q^3=J_S`

lies in the sign kernel and is therefore invisible to unsigned channel transport:

`pi(Q^3)=1`.

Thus the frame C6 naturally projects to a channel C3.

This is a different quotient from the internal shell/closure `C12` and from local sign-sheet readouts; the types must remain separate.

## 6. Relation to instantaneous PF-10 passage

V3 showed that the instantaneous unsigned directed passage relation on one ordered triad is the same cycle `rho_S` at every internal R phase.

The present theorem gives the cumulative transport interpretation:

- event-local passage generator: `rho_S`;
- cumulative channel-frame state after r macro events: `rho_S^r`.

So the passage relation can remain locally constant while the transported channel frame advances through its `C3` phase.

This resolves a possible ambiguity between “active passage law” and “accumulated channel state”.

## 7. Reversal and chirality

Reversing the oriented triad replaces

`Q_S -> Q_S^{-1}`

and therefore

`U_S -> U_S^{-1}`.

So channel-circulation chirality is the unsigned projection of the selected triadic frame chirality in the minimal coupled law.

This local coupling still does not identify the channel chirality with the global `Ori_6` parity charge: every 3-cycle is even and preserves `Ori_6`.

## 8. Operation-safe quotient consequences

### Safe direction under the minimal coupling

If the full triadic frame event `g` and the global axis-channel gauge are retained, storing `pi(g)` separately is redundant for future operations that use only this minimal coupled channel law. It can be reconstructed exactly.

### Unsafe reverse direction

If only `pi(g)` is retained, the 64 sign-kernel states are lost. A future signed-X6/frame observer can distinguish them.

### Path provenance remains richer

Neither `g` nor `pi(g)` determines the concrete Cell path witness. INNER/OUTER or general Path-formal/BRC provenance remains in the event layer until a law-specific section or safe observer quotient is supplied.

Thus the safe hierarchy is

`CONCRETE EVENT PATH -> SIGNED FRAME g -> UNSIGNED CHANNEL pi(g)`

for this coupling, with strictly increasing information loss.

## 9. Changing active triads generates A6 channel dynamics

The nontrivial channel circulations associated with all oriented 3-subsets are precisely the 3-cycles on six channel labels.

All 3-cycles generate `A6`.

Therefore the unsigned channel transformation group generated by all triadic internal circulations is exactly

`A6`.

This is not a second independent copy in the minimal law; it is the quotient image of `R_triad`.

## 10. Current integration consequence

One significant redundancy in the upper typed architecture can now be removed **conditionally on the minimal triadic coupling law**:

`SIGNED TRIADIC FRAME UPDATE` already determines `UNSIGNED CHANNEL PERMUTATION UPDATE`.

The still-independent factors are:

- concrete path/BRC provenance;
- signed/token internal triad phase beyond the unsigned passage quotient;
- shell/closure mode;
- event dependency/time history;
- observer/chart and precision context.

Any future evidence of channel dynamics not factoring through `pi` is evidence for an additional internal field/coupling rather than a failure of the present theorem.

No Foundation promotion is made.
