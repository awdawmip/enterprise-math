# Recovery M08

Verified browser-certifier code commit: `f33be2c0c51d696c7cd81b73d5269c5b0683dce4`.

Completed: `certified_hex_browser.py` now supports the older multiplicative
observer's exact axial-lexicographic tie through `populationLexicographic`, while
preserving every base certificate. 85 combined numerical/HTML/cross-language
checks plus 69 M04 DOM checks pass; finite tie audit remains 1,080 / 504.

Do not rebuild the numeric kernel. Do not replace local lexicographic ties with
the base q/r/s priority. Do not infer exact scale from the old float slider. Do
not call the A2 display observer native X6.

Next executable unit: migrate `multiplicative_lab.html` itself. Embed/reuse the
existing angular/certified browser scripts, add exact textual scale and precision
state, make rebuild asynchronous with latest-generation-wins, clear stale cells
while computing, preserve unresolved/null cells, and version sessions/reports
without laundering V1 float state.
