# Viète V4 observer quotient -> AGM squared-shape dynamics

Status: `FREE_RESEARCH / EXACT CROSS-FAMILY ORBIT-QUOTIENT THEOREM / NOT_FOUNDATION`
Date: `2026-09-04`
Researcher: `EM-FREE-7D3C9A / FREE_AXIOM_DISCOVERY`
Issue: `#1158`
Cross-family consumer: `#1161`
Depends on:
- `research_notes/VIETE_DIRECTION_SHEET_SWEEP_CHIRALITY_V4_CORRECTION_20260904.md`
- `research_notes/FREE_RESEARCH_1161_VIETE_BISECTOR_SQUARE_FACTORIZATION_20260903.md`

## 1. Question

The #1158 refinement line has separated two commuting binary operations on an oriented finite bisector state:

- `H` — half-turn/root-deck sign, acting as simultaneous sign reversal;
- `S` — sweep/chirality reversal, acting as transverse sign reversal.

Together they generate

\[
V_4=C_2^H\times C_2^S.
\]

Independently, #1161 proves that one Gauss–Legendre AGM step factors through the same finite Viète bisector state and then performs componentwise squaring.

This note proves that the squaring stage is exactly the complete orbit quotient by the #1158 `V4` action. Thus the AGM shape variables are not merely sign-insensitive by accident: they are coordinates on the `V4` observer quotient of the oriented Viète state.

## 2. Oriented Viète bisector shell

Work with a real/algebraic ordered coefficient extension of the Enterprise sector-local component algebra.

Let

\[
\mathcal U
=
\{(C,S):C^2+S^2=1\}.
\]

For the positive AGM regime used below one has additionally

\[
C>0,
\qquad
|S|<C,
\]

but the orbit classification itself does not need these inequalities.

Define the two involutions

\[
\mathsf H(C,S)=(-C,-S)
\]

and

\[
\mathsf S(C,S)=(C,-S).
\]

Then

\[
\mathsf H^2=\mathsf S^2=\mathrm{id},
\qquad
\mathsf H\mathsf S=\mathsf S\mathsf H.
\]

Their product is

\[
\mathsf H\mathsf S(C,S)=(-C,S).
\]

Hence the orbit of a generic state is

\[
\boxed{
\{(C,S),(C,-S),(-C,-S),(-C,S)\}.
}
\]

This is exactly independent sign reversal of the two component coordinates.

## 3. Squared coordinates are V4-invariant

Define

\[
Q:\mathcal U\to\mathcal Q,
\qquad
Q(C,S)=(x,y):=(C^2,S^2),
\]

where

\[
\mathcal Q
=
\{(x,y):x\ge0,\ y\ge0,\ x+y=1\}.
\]

For every `g in V4`,

\[
\boxed{Q(g\cdot(C,S))=Q(C,S).}
\]

Thus `Q` descends to the orbit set

\[
\bar Q:\mathcal U/V_4\to\mathcal Q.
\]

## 4. Squared coordinates are a complete orbit invariant

Suppose

\[
Q(C,S)=Q(C',S').
\]

Then

\[
C'^2=C^2,
\qquad
S'^2=S^2.
\]

Over the ordered real/algebraic coefficient field this implies

\[
C'=\eta_C C,
\qquad
S'=\eta_S S,
\qquad
\eta_C,\eta_S\in\{\pm1\},
\]

with the obvious harmless stabilizer degeneracy when one component is zero.

The four sign pairs

\[
(\eta_C,\eta_S)
\]

are exactly the four actions generated by `H` and `S`.

Therefore

\[
\boxed{
Q(C,S)=Q(C',S')
\iff
(C',S')\in V_4\cdot(C,S).
}
\]

Hence

\[
\boxed{
\mathcal U/V_4\cong\mathcal Q
}
\]

via the squared-coordinate map.

This remains true on the axis boundary; only orbit cardinality drops because of stabilizers.

## 5. Observer meaning

Any observer depending only on

\[
C^2,\quad S^2
\]

is exactly a `V4`-invariant observer and therefore factors through `Q`.

Conversely, every function on the quotient shell `Q` pulls back to a `V4`-invariant observer on the oriented state.

Thus the quotient is not merely a convenient compression. It is the complete state retained by observers that forget both:

1. root/deck sign `H`;
2. sweep/chirality sign `S`.

## 6. AGM factorization uses only the V4 quotient

Now use the exact #1161 notation. For a positive AGM pair

\[
a>b>0,
\]

define

\[
\mathcal H:=a+b,
\qquad
U:=a-b,
\qquad
V:=2\sqrt{ab},
\]

and normalized cone direction

\[
r=V/\mathcal H,
\qquad
s=U/\mathcal H.
\]

The shared Viète normalized equal-resultant root is

\[
(C,S)
=
\frac{(1+r,s)}{\sqrt{2(1+r)}}.
\]

The #1161 factorization proves

\[
C^2=\frac{1+r}{2},
\qquad
S^2=\frac{1-r}{2}.
\]

Set

\[
x=C^2,
\qquad
y=S^2.
\]

Then the updated AGM pair is

\[
\boxed{
a^+=\frac{\mathcal H}{2}(x+y),
\qquad
b^+=\frac{\mathcal H}{2}(x-y).
}
\]

Since `x+y=1`, this is simply

\[
a^+=\mathcal H/2,
\qquad
b^+=\mathcal H(x-y)/2.
\]

No sign of `C` or `S` occurs.

## 7. The whole next AGM cone state factors through Q

The same #1161 result gives

\[
\boxed{
\mathcal H^+=\mathcal H x,
\qquad
U^+=\mathcal H y.
}
\]

The next normalized shape defect is

\[
\boxed{
\delta^+:=\frac{U^+}{\mathcal H^+}=\frac{y}{x}.
}
\]

The chord-loss coordinate is exactly

\[
\boxed{\ell=y.}
\]

And the remaining cone leg is

\[
\boxed{
V^+
=
\mathcal H\sqrt{x^2-y^2}.
}
\]

In the positive AGM regime `r=x-y>0`, so the positive root is fixed by the positive scalar state.

Therefore the entire next positive AGM state

\[
(a^+,b^+,\mathcal H^+,U^+,V^+,\delta^+,\ell)
\]

is a function only of

\[
\boxed{(\mathcal H,Q(C,S)).}
\]

Equivalently, the oriented signs of the Viète bisector state have been completely quotiented out before the next AGM shape is formed.

## 8. Exact quotient factorization diagram

At derived finite-algebraic strength the local update factors as

```text
positive AGM cone state
    -> normalized Viète bisector state (C,S)
    -> V4 orbit quotient Q(C,S)=(C^2,S^2)
    -> AGM quadratic sum/difference channels
    -> next positive AGM cone state
```

Thus

\[
\boxed{
\text{AGM LOCAL SHAPE UPDATE}
=
\text{VIETE BISECTOR DYNAMICS OBSERVED THROUGH THE }V_4\text{ QUOTIENT}.
}
\]

This statement is exact at the already-proved G1/G2 layer.

## 9. Why Viète and AGM have different sign requirements

The scalar Viète longitudinal factor is

\[
C.
\]

It is:

- invariant under sweep inversion `S`;
- odd under deck/root sign `H`.

Therefore scalar Viète can quotient chirality but still needs the principal positive-longitudinal `H` section.

By contrast the AGM shape variables use

\[
C^2,\quad S^2.
\]

They are invariant under **both** `H` and `S`.

Hence:

\[
\boxed{
\text{VIETE SCALAR STATE} = \text{S-quotient + principal H section},
}
\]

while

\[
\boxed{
\text{AGM SHAPE STATE} = \text{full V4 quotient}.
}
\]

This cleanly explains why AGM does not need a root-sign or chirality label once the common bisector shell has been formed.

## 10. Quadratic acceleration as quotient dynamics

Near the identity-oriented fixed state one has small transverse coordinate `S` and `C` close to `1`.

The AGM next shape defect is

\[
\delta^+=\left(\frac{S}{C}\right)^2.
\]

Thus the first nontrivial signed transverse quantity has been passed through the `V4` quotient and becomes second order.

The quadratic/superattracting contraction is therefore structurally tied to the sign-erasing quotient:

\[
\boxed{
\text{first-order oriented defect}
\xrightarrow{V_4\text{ quotient}}
\text{second-order unsigned shape defect}.
}
\]

This does not mean every group quotient produces acceleration. Here the exact squared-coordinate orbit map is what causes the order doubling.

## 11. Stabilizers and special states

At `C=0` or `S=0`, the `V4` orbit has fewer than four distinct points because one of the sign flips acts trivially on the zero coordinate.

The quotient coordinates remain well defined and complete.

The quarter-turn seed is an important example: `C=0`, so the distinction between `H` and `S` collapses on that orbit. This is the same seed degeneracy already isolated in #1158.

For the positive AGM regime used in #1161, the bisector state has

\[
C>S>0,
\]

so the orbit is generically four-element and the quotient is regular.

## 12. Cross-family consequence

The two precision algorithms now share more than one completion constant and one local bisector formula.

They are related by an explicit observer hierarchy:

```text
oriented finite bisector state
    |-- S-even + principal H section --> scalar Viète factor/product
    |
    `-- full V4 quotient -----------> AGM squared shape/cone update
```

The later internal identity

\[
\Pi_{\rm rot}^{\rm Viete}
=
\Theta_{\rm AGM}
=
\Pi_*^{\rm AGM}
=
\tau
\]

therefore sits on top of a common local state with two different observer quotients, rather than connecting unrelated recursions only at their limit.

## 13. Native boundary

This theorem does not promote the `V4`-acted algebraic orientation shell to a native G0 Cell state.

The #1158 native frontier still concerns whether actual Cell/process dynamics realizes the winding/deck precision architecture and the required cross-layer orientation bridge.

The #1161 AGM closure additionally uses derived positive-root/cone and analytic completion structure.

Therefore the correct strength is:

`VIETE_TO_AGM_V4_QUOTIENT = EXACT_DERIVED_G1_G2_CROSS_FAMILY_THEOREM`.

`VIETE_TO_AGM_V4_QUOTIENT != NATIVE_CELL_IDENTITY`.
