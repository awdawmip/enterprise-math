# Heartbeat Stage37: full-packet handoff and clock-only recycling

Event-ID: brc-heartbeat-packet-handoff-20260922-6EF011
Research-Activity-ID: RA-6EF011C2E75C4A799606AFEA
Status: RESEARCH_CANDIDATE; same-author derivations and tests, no independent review or Lean.
Parent standalone:092751e28e2d79c84cfe9e0c7377560abd358e7a.
Global read:a43e6f3b48e91895a5f660ef1317442f9f0ca20a; project read:50a96ccf2fde23797d1b47b14159427fa6107441. P000/residual-first unchanged.

Stage37 exports the complete processed packet, including side outputs, restores arbitrary dirty buffer rows, and rewinds only the scalar clock rather than undoing the data. It is an externally controlled finite transducer, NOT a completed autonomous machine. No intermediate ideal reading, postselection, pure scratch or refreshed-clock assumption is introduced.

The inherited dirty-row identity is followed by a final-row/port SWAP and inverse row permutation: (S,B1,...,BG,N)->(N,B1,...,BG,U S). It is a complete operator identity, valid with arbitrary initial coherences and correlations. Original S-buffer correlations are transferred to the output; marginal return is not called decorrelation. A new input packet is consumed; the old packet is transferred, not cloned. For the14-gate/four-wire source, actual lists contain60 internal data cells,4 port cells and60 range-four two-site swaps, or396 adjacent swaps with168 additional dirty relay cells. All relays/buffers retain their states. Exact64-dimensional matrices test four source-gate types and correlated full-rank inputs.

Ordinary target-only handoff followed by U inverse is a retained failure witness: the exported target is5/32, but the full engine is distance17/96 from its initial state, even though its target marginal returns to1/3. The missing side-output correlations matter. This is not a no-go theorem for every other recovery architecture.

Let F=D(u tensor I)D-dagger be the inherited history evolution, with ready framesI and every finished frameU. Set B=||Q_unfinished u P_ready||^2. Switching to identity links and the negative scalar-clock Hamiltonian gives E=(u-dagger tensor I)F. On the entire ready subspace, ||(E-I tensor U)P_ready||<=2sqrt(B), including arbitrary references. The data marginal still has the stronger Stage36 bound B. At an exact mirror B=0, the clock is returned jointly, not only marginally, while U remains on the data. The switch, matching reverse duration, source inputs and outgoing registers are explicit resources. Uncontrolled bare drift is not covered for free.

A full-rank one-edge example at q=1-1e-8 and clock mixture xi0.001 gives output population error3.33333336667e-17 but clock trace distance9.99000004995e-9. Exact formulas are output error=B/3 and clock-distance-squared=(1-xi)^2 B(1-B). Directed rational square-root bounds and an independently coded110-digit clock exponential agree. Thus accurate output alone does not justify reusing a clock as if unchanged.

For n complete transactions, a hybrid over ideal prefixes gives entire-stream-plus-engine trace error<=sum_j e_j, with e_j=2sqrt(B_j) plus declared implementation errors. The actual clock is never projected/reinitialized between uses. A supplied initial convex bad componentxi is charged once: xi+(1-xi)min(1,sum e_j). Ready/bad cross-coherence cannot be substituted by a population tail. Reusing Stage36's337-site window with xi0.001, the one-million-cycle scalar bound is0.00100934674739341 under otherwise exact mode controls. This is a finite proof/certificate calculation, NOT a million-cycle physical run or full million-output simulation; three million fresh memory qubits would be needed for the declared cooling packets.

Forty-bit dyadic coefficients are actually generated by the unchanged Stage36 compiler. Separate calibrated stream certificates include all70 forward data operations and396 adjacent handoff swaps at per-gate half-angle error<=1e-8, plus reverse-duration mismatch<=1e-8 hbar/g. Finite1/100/1000/1000000-cycle results are saved; the last bound reaches1 and is retained as uninformative. The one-head norm and number-preserving calibration scope are not extended to the whole physical unary rail without its preparation-tail contract.

At exact mirror a diagonal15-state clock returns its marginal while a common hidden start label persists across all outputs. Exact target mean0.156409722222222 and pair covariance2.77377989969136e-5 are retained. For32 packets, parity is0.5000030994951042, versus0.5000030565928656 after incorrectly refreshing the clock each time. No2^32 table is built; this is a fixed-frame terminal-observer contraction. A separate three-state clock with noncommuting frames(I,X,ZX),q4/5 and full-rank coherent input is propagated for16 packets with no replacement. Its all-zero probability3.03296553217e-5 differs from the refreshed-clock3.13692897592e-8. The largest exact denominator is693bits. Two/three complete-output density calculations and24 independent110-digit full-history values verify the retained-memory contraction.

The source compressor's reused exact bare-gap ledger is raw packet energy13/12, exported packet119/96, pulse work5/32, target change-17/96 and side-memory increase1/3. Equal-gap transport swaps preserve bare energy. This does not imply zero cost for switching couplings or feeding packets, nor a unified autonomous generator-plus-battery model. If only the cooled target is retained, old memory side outputs require separately charged resetting. Clock/buffer recycling is not unlimited cooling with a finite resource stock.

Frozen plan: six cases times three repeats,18 immutable rows. Median core times: actual layout0.003904349s; mirror8/32 contractions0.022519638/0.021097391s; four/sixteen nonideal quantum-memory cycles0.007403368/0.038137965s; million-cycle scalar certificate0.000767016s. These are heterogeneous computational tasks, not physical runtimes or speed comparisons with the best classical solver.407 finite assertions pass, including weighted-clock unitarity, arbitrary correlated ready states, complete transport identities and whole-record tests. Four110-digit reference groups and full12/24-dimensional output-stream states pass. All18timing rows and6candidate results are replayed without replacement. Source Stage19/30/34/35/36 modules execute unchanged.

One real configured arxiv request used private Issue306,batchbe9aed5c-1a52-46fa-b46a-4c52f000e0e4,matched comment5774389658. It returned COMPLETED/job NO_HIT,1provider call,0records,0bridge-model calls for the restrictive query catalytic coherence repeated use correlations. This is not evidence of literature absence. Public primary context:Bennett1973 original text hosted at Princeton; arXiv1804.05154 and2106.12592 abstracts, with their different correlation assumptions retained. Swap transfer, reversibility, history dressing and telescoping bounds are established; no global novelty is claimed.

Next: finite autonomous mode switching and packet supply, or more economical transport retaining the whole-stream return guarantee. No free scheduler, fresh used registers, permanent storage, generic quantum simulation, Shor speedup or universal absolute-zero nonexistence theorem is asserted. Full proof,code,plans and results are in the cumulative standalone bundle. Source/Drive publication and aggregate activity intake are separate.
