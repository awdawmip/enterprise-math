# Navier–Stokes A3/FCC cubic defect symbol audit and first leakage laws

Status: `RESEARCH_NOTE / DURABLE_FRONTIER / EXACT_SYMBOL_AUDIT + EXACT_PAIR_LAWS + OPEN_GLOBAL_ESTIMATE / NOT_PROMOTED / NOT_MILLENNIUM_PROOF`
Researcher-ID: `EM-FREE-7N3K2A`
At: `2026-09-07T15:40:00+08:00`
Parent: `research_notes/ns_a3_cubic_defect_multiplier_frontier_20260907.md@6e3c2ef7fa80f2dd96e22412b08e079487fc1bd0`
Authority immediately before write: `enterprise-math main@6e3c2ef7fa80f2dd96e22412b08e079487fc1bd0`.

## 0. Defect frame

For `xi=(x,y,z)` retain the four signed cubics

`g0=xyz`,
`gx=x(x^2-y^2-z^2)`,
`gy=y(y^2-z^2-x^2)`,
`gz=z(z^2-x^2-y^2)`.

Write `G=(g0,gx,gy,gz)` and `mu_j(xi)=g_j(xi)/|xi|^3` for `xi!=0`. The parent note proves that the common nonzero zero set is exactly the six A3/FCC root lines and that these four cubics span the full degree-3 vanishing space.

BRC resolution remains: keep the signed four-channel vector and the root-line/radial/helicity labels until cancellation is resolved; positive norms are post-cancellation observers only.

## 1. Exact commutator branch formula

Let `Lambda=(-Delta)^(1/2)`. For smooth divergence-free periodic `u`,

`F(u):=int omega·S omega dx = - <Lambda u, [Lambda,u·grad]u>`.

With Fourier output `r=p+q`,

`([Lambda,u·grad]u)^hat(r)`
` = i sum_{p+q=r} (|r|-|q|) (uhat(p)·q) uhat(q)`.

Therefore an ordered resonant triad branch `a+b+c=0` contributes a factor of the form

`i |c| (|c|-|b|) (uhat(a)·b) (uhat(b)·uhat(c))`

up to the Fourier normalization/conjugation convention.

Two exact null mechanisms are branchwise, not merely aggregate:

1. on an equal-shell STAR/A2 triad, `|a|=|b|=|c|`, so the shell-difference factor is zero for every ordering;
2. on a collinear triad, `b` is parallel to `a`, so `uhat(a)·b=0` by incompressibility for every ordering.

This strengthens the earlier energy-transfer statement: the commutator symbol itself vanishes termwise on the exact A3 root-ray resonance skeleton.

## 2. Resonance transversality at a STAR branch

Use the normalized oriented STAR triple

`a=(1,1,0)/sqrt(2)`,
`b=(-1,0,-1)/sqrt(2)`,
`c=(0,-1,1)/sqrt(2)`,

so `a+b+c=0`.

On the six-dimensional resonance chart `(k,p)` with `q=-k-p`, stack the differentials of all twelve defect components

`(G(k),G(p),G(q))`.

At `(a,b,c)` the resulting `12 x 6` Jacobian has exact rank `5`. Its one-dimensional kernel is precisely common radial scaling of the triad. A concrete nonzero `5 x 5` minor is obtained by deleting the first coordinate column and taking rows `(0,1,4,5,8)`; its determinant is `-1/8`.

Hence inside the resonance manifold the twelve signed cubic defect channels generate all five transverse directions to the one-dimensional exact STAR branch. There is no first-order hidden resonance direction invisible to the defect frame.

## 3. Resonance transversality at a collinear branch

Take a representative collinear resonance

`k=a`, `p=2a`, `q=-3a`,

with the same root direction `a=(1,1,0)/sqrt(2)`.

The stacked `12 x 6` defect Jacobian now has exact rank `4`. Its two-dimensional kernel is exactly independent radial motion of `k` and `p` along the common root line (with `q=-k-p`). A concrete `4 x 4` minor, columns `(0,2,3,5)` and rows `(0,1,4,5)`, has determinant `4`.

Thus the defect channels also generate the full normal bundle to the two-dimensional collinear root-ray resonance branch.

### Consequence for the candidate D3 estimate

For comparable-frequency charts the local Hadamard-division step in the parent candidate is not missing a first-order normal variable: every smooth symmetrized trilinear symbol that vanishes on the exact STAR/collinear root-ray branch is locally generated by the signed defect components with smooth coefficients, after choosing a nonvanishing Jacobian minor. What remains is a uniform chart/paraproduct summation and high-high-low boundary audit, not local algebraic transversality.

## 4. Exact pair-output defect laws

Use unnormalized roots of squared length `2` and define the normalized scalar defect

`delta(k)=|G(k)|/|k|^3`.

By A3 symmetry a pair of distinct signed roots is classified by its dot product `-1,0,+1` (opposite roots have dot `-2`). Let `m,n>0` be radial multipliers.

### 4.1 STAR / 120-degree pair, dot = -1

Take

`alpha=(1,1,0)`, `beta=(-1,0,-1)`.

Then

`k=m alpha+n beta=(m-n,m,-n)`

and exactly

`G(k)=mn(m-n)*(-1,-2,2,2)`.

Also

`|k|^2=2(m^2+n^2-mn)`.

Therefore

`delta_STAR(m,n)`
` = sqrt(13)|mn(m-n)| / [2(m^2+n^2-mn)]^(3/2)`.

The shell mismatch `(m-n)` is an exact factor. Equal-radius 120-degree closure is defect-free.

### 4.2 Orthogonal root pair, dot = 0

Take

`alpha=(1,1,0)`, `beta=(1,-1,0)`.

Then

`k=(m+n,m-n,0)`

and

`G(k)=(0,4mn(m+n),-4mn(m-n),0)`.

Hence

`delta_0(m,n)=2|mn|/(m^2+n^2)`.

At equal radii `m=n`, the output is a coordinate-axis direction and `delta_0=1`. If `n/m -> 0`, then `delta_0 ~ 2n/m`.

### 4.3 Acute root pair, dot = +1

Take

`alpha=(1,1,0)`, `beta=(1,0,1)`.

Then

`k=(m+n,m,n)`

and

`G(k)=mn(m+n)*(1,2,-2,-2)`.

Also

`|k|^2=2(m^2+mn+n^2)`.

Therefore

`delta_+(m,n)`
` = sqrt(13)|mn(m+n)|/[2(m^2+mn+n^2)]^(3/2)`.

At equal radii,

`delta_+(1,1)=sqrt(13/54)`;

for `n/m -> 0`,

`delta_+(m,n) ~ [sqrt(13)/(2sqrt(2))] n/m`.

### 4.4 Same/opposite root line

Same-line sums stay on the same A3 root line and have zero defect; opposite equal vectors produce zero output. Thus they create no angular A3 defect.

### Structural conclusion

Every root-root interaction with separated radial scales creates only `O(low/high)` normalized A3 defect. Order-one first-generation defect is confined to comparable-scale incompatible root pairs:

- dot `0` -> axis-defect channel;
- dot `+1` -> `211` composite-defect channel;
- dot `-1` -> only if the STAR radii are mismatched by order one.

This is a quantitative refinement of the earlier finite leakage alphabet.

## 5. A positive scalar defect energy exists, but must be used after signed factorization

The exact sum of squares is

`|G(xi)|^2`
` = x^6+y^6+z^6 - x^4y^2-x^4z^2-y^4x^2-y^4z^2-z^4x^2-z^4y^2 + 7x^2y^2z^2`.

Let

`p=x^2+y^2+z^2`,
`q=x^2y^2+y^2z^2+z^2x^2`,
`r=x^2y^2z^2`.

Then exactly

`|G|^2 = p^3 - 4 p q + 13 r`.

With the tetrahedral quartic

`H=x^4+y^4+z^4-2x^2y^2-2y^2z^2-2z^2x^2 = p^2-4q`,

this is

`|G|^2 = p H + 13 r`.

Thus on the sphere the nonnegative scalar defect symbol is

`delta(xi)^2 = 1 - 4 q/p^2 + 13 r/p^3`.

It vanishes exactly on the six root lines and has quadratic transverse vanishing there. This gives an exact positive defect-energy multiplier after branchwise cancellation has already been preserved. BRC warning: `delta^2` alone does not retain the signed four-channel provenance needed for nonlinear ideal division.

## 6. Exact defect transport equation

Let `T_j` be the order-zero Fourier multiplier with symbol `i mu_j` (the factor `i` makes the odd real symbol compatible with real-valued fields), and define

`v_j=T_j u`.

Because `T_j` commutes with constant-coefficient derivatives, Laplacian and Leray projection,

`partial_t v_j - nu Delta v_j + P(u·grad v_j)`
` = - P [T_j,u·grad]u`.

Since `div u=div v_j=0`, the transport term is L2-skew and

`(1/2)d/dt ||v_j||_2^2 + nu||grad v_j||_2^2`
` = - <v_j,[T_j,u·grad]u>`.

Summing over the four signed channels gives the exact defect energy

`E_D=(1/2)sum_j ||T_j u||_2^2`.

The commutator has bilinear symbol

`([T_j,u·grad]u)^hat(k)`
` = - sum_{p+q=k} [mu_j(k)-mu_j(q)] (q·uhat(p)) uhat(q)`

up to the harmless Fourier `i` convention.

Because `mu_j` is homogeneous of degree zero, a low-high interaction satisfies the standard mean-value gain

`mu_j(q+p)-mu_j(q)=O(|p|/|q|)`

when `|p| << |q|`. If both inputs lie exactly on A3 root rays, `mu_j(q)=0`, and Sections 4.1–4.3 give the exact output leakage size rather than merely a generic smooth-symbol bound.

The crude commutator energy estimate still involves uncontrolled critical quantities such as a Lipschitz norm of `u`; therefore this identity is not yet an unconditional defect bootstrap.

## 7. New bottleneck after the audit

The local symbol geometry is no longer the main uncertainty. The unresolved source is now sharply localized:

`COMPARABLE-SCALE + INCOMPATIBLE A3 ROOT PAIR`

particularly dot-0 axis outputs and dot-+1 `211` outputs.

The next finite test is to retain the signed four-channel source vectors and divergence-free/helical polarization, sum over complete A3 symmetry orbits, and check whether the comparable-scale defect injection contains an additional cancellation before taking positive norms. If no such cancellation exists, any unconditional route must control this source dynamically rather than geometrically.

The global candidate estimate D3 from the parent note remains `UNPROMOTED`: the STAR/collinear local normal-generation audit passed, but a uniform high-high-low/paraproduct proof has not yet been completed.
