# Viète gate precision: exact halving of normalized Cayley distance and the six-gate transverse-residual pi readout

Status: `FREE_RESEARCH / EXACT FINITE PRECISION LAW + TARGET-FREE GATE READOUT / NOT_FOUNDATION`
Date: `2026-09-03`
Researcher: `EM-FREE-7D3C9A / FREE_AXIOM_DISCOVERY`
Issue: `#1158`
Parents:
- `research_notes/VIETE_FINITE_CYCLIC_SHORTEST_ROOT_REFINEMENT_20260903.md`
- `research_notes/VIETE_BINARY_CYCLE_COVERS_2ADIC_VS_ANALYTIC_COMPLETION_20260903.md`
- `research_notes/VIETE_TARGET_FREE_ROTATION_COMPLETION_CONSTANT_20260903.md`

## 1. A half-angle definition with no angle

Let

\[
C_N=\mathbb Z/N\mathbb Z
\]

with the standard cycle/Cayley graph generated by `+1` and `-1`.

For a state `x`, define its finite graph distance to identity

\[
\delta_N(x,e)
\]

and the normalized rotation-distance coordinate

\[
\boxed{
\theta_N(x):=\frac{\delta_N(x,e)}{N}
\in[0,1/2].
}
\]

This is purely finite and rational. It is not a real angle and does not use `pi`.

Refine by the connected two-fold cover

\[
\pi_N:C_{2N}\to C_N.
\]

For a coarse state `x`, let `y` be a lift of minimum refined Cayley distance to identity, retaining both lifts when tied.

The claim is that this finite rule exactly halves `theta`.

## 2. Exact distance-halving theorem

Choose the representative

\[
x=k\pmod N,
\qquad0\le k<N.
\]

Its two lifts are

\[
y_0=k\pmod{2N},
\qquad
y_1=k+N\pmod{2N}.
\]

The coarse distance is

\[
\delta_N(x,e)=\min(k,N-k).
\]

The refined distances are

\[
\delta_{2N}(y_0,e)=k,
\]

\[
\delta_{2N}(y_1,e)=N-k.
\]

Therefore the minimum refined distance is exactly the old coarse distance:

\[
\boxed{
\min_{y\in\pi_N^{-1}(x)}\delta_{2N}(y,e)
=
\delta_N(x,e).
}
\]

But the refined cycle has twice as many states. Hence every shortest lift satisfies

\[
\boxed{
\theta_{2N}(y)
=
\frac{\delta_N(x,e)}{2N}
=
\frac12\theta_N(x).
}
\]

Thus one binary orientation-resolution cover is an **exact half-turn-distance refinement**.

This gives a discrete definition of “half-angle” that contains no angle variable.

## 3. Unique root versus antipodal tie

If `x` is not the half-turn, exactly one lift realizes the minimum distance.

If `N` is even and

\[
x=N/2,
\]

then

\[
\delta_N(x,e)=N/2
\]

and both lifts have refined distance

\[
N/2.
\]

Therefore both satisfy

\[
\theta_{2N}=1/4.
\]

Hence:

\[
\boxed{
\text{the half-turn is exactly the unique state whose finite half-refinement has two equally precise lifts.}
}
\]

This reproduces the two quarter-turn branches without a selector.

## 4. The other lift has the complementary normalized distance

Let

\[
\theta=\theta_N(x)\in[0,1/2].
\]

For a non-half-turn state, the shortest lift has normalized distance

\[
\theta/2.
\]

The discarded antipodal lift has normalized distance

\[
\boxed{
\frac12-\frac\theta2.
}
\]

Thus the shortest-root rule is uniquely characterized by **exact halving of the normalized discrete rotation distance**.

Equivalently:

\[
\boxed{
\text{finite half-refinement} = \text{binary cover lift with }\theta\mapsto\theta/2.
}
\]

No Euclidean angular-bisector notion is involved.

## 5. Six-gate tower and exact dyadic distance

Use the connected binary cover tower

\[
G_m=C_{3\cdot2^m},
\qquad m\ge0.
\]

At `m=1`, `G_1=C_6`. Its half-turn is the state

\[
h_1=3\pmod6,
\]

with

\[
\theta_6(h_1)=1/2.
\]

At `m=2`, the two retained lifts are

\[
q_2^+=3\pmod{12},
\qquad
q_2^-=-3\pmod{12},
\]

and

\[
\theta_{12}(q_2^\pm)=1/4.
\]

Thereafter shortest refinement keeps the representatives `+3` and `-3` at every finer cycle:

\[
q_m^\pm=\pm3\pmod{3\cdot2^m},
\qquad m\ge2.
\]

Therefore

\[
\boxed{
\theta_{3\cdot2^m}(q_m^\pm)=2^{-m}.
}
\]

So the Viète oriented sheets have an exact rational finite precision coordinate at every depth.

## 6. Algebraic character readout

Attach an algebraic unit-character realization to `G_m`. Let `z_m^+` be the character state corresponding to `q_m^+` and `z_m^-=(z_m^+)^{-1}`.

Define the Enterprise two-component readouts

\[
c_m=\frac{z_m^++(z_m^+)^{-1}}2,
\]

and transverse magnitude

\[
s_m=\left|\frac{z_m^+-(z_m^+)^{-1}}{2J}\right|,
\]

where `J^2=-1` is the already-typed Enterprise algebraic component marker.

Then

\[
c_m^2+s_m^2=1.
\]

At `m=2`, the quarter-turn state gives

\[
c_2=0,
\qquad s_2=1.
\]

The covering/shortest-root relation gives the exact recurrence

\[
\boxed{
c_{m+1}=\sqrt{\frac{1+c_m}{2}}}
\]

and the corresponding transverse half-root relation.

Thus the finite gate-distance refinement generates the Viète nested radicals as an algebraic observer.

## 7. Six-gate transverse-residual pi readout

The size of the finite orientation cycle is

\[
|G_m|=3\cdot2^m.
\]

For `m>=2`, define the finite gate readout

\[
\boxed{
\Pi_m^{\rm gate}
:=
\frac{|G_m|}{6}\,s_m
=
2^{m-1}s_m.
}
\]

At the first quarter-turn resolution:

\[
\Pi_2^{\rm gate}=2.
\]

At the next level:

\[
s_3=\frac{\sqrt2}{2},
\]

so

\[
\Pi_3^{\rm gate}=4\frac{\sqrt2}{2}=2\sqrt2.
\]

The subsequent values are exactly the previously defined target-free Viète precision sequence, with index shift

\[
\boxed{
\Pi_m^{\rm gate}=\Pi_{m-2}.
}
\]

Therefore

\[
\boxed{
\Pi_{\rm rot}
=
\lim_{m\to\infty}
\frac{|C_{3\cdot2^m}|}{6}\,s_m.
}
\]

This is a finite six-gate interpretation of the intrinsic completion constant.

## 8. Meaning of the factor 1/6

The denominator `6` is not inserted to match classical `pi` numerically.

It is fixed by the first binary refinement of the current `C3` positive-ray quotient:

\[
C_3\leftarrow C_6.
\]

`C6` is the first shell carrying the emergent half-turn/deck phase. Measuring later cycle size in units of this six-state coarse oriented shell gives

\[
\frac{|G_m|}{6}=2^{m-1}.
\]

Hence the gate readout is:

\[
\boxed{
\text{number of six-gate resolution units}
\times
\text{current transverse unit-state residual}.
}
\]

No target classical constant is used to set the scale.

## 9. Exact telescoping in gate variables

Let the longitudinal scaled radical state be

\[
r_m=2c_m,
\]

and transverse scaled residual

\[
h_m=2s_m.
\]

Then

\[
r_{m+1}=\sqrt{2+r_m},
\]

\[
h_{m+1}=\sqrt{2-r_m},
\]

and

\[
r_{m+1}h_{m+1}=h_m.
\]

The gate readout is

\[
\boxed{
\Pi_m^{\rm gate}=2^{m-2}h_m.
}
\]

Thus doubling the number of six-gate resolution units is compensated by the shrinking transverse residual, and their product tends to the finite constant `Pi_rot`.

## 10. Intrinsic error quartering in cover depth

The target-free asymptotic theorem for the previous indexing says

\[
\Pi_{\rm rot}-\Pi_n
\sim
\frac{\Pi_{\rm rot}^3}{6\,4^{n+1}}.
\]

With `n=m-2`,

\[
\boxed{
\Pi_{\rm rot}-\Pi_m^{\rm gate}
\sim
\frac{\Pi_{\rm rot}^3}{6\,4^{m-1}}.
}
\]

Therefore one additional binary gate-cover bit gives

\[
\boxed{
\frac{\Pi_{\rm rot}-\Pi_{m+1}^{\rm gate}}
{\Pi_{\rm rot}-\Pi_m^{\rm gate}}
\to\frac14.
}
\]

The precision multiplier is intrinsic to the gate-refinement tower.

## 11. Classical compatibility is still last

Only after the finite state tower, algebraic readout, completion constant, and intrinsic precision law are defined does the classical character completion identify

\[
\Pi_{\rm rot}=\pi.
\]

Thus the classical formula becomes

\[
\boxed{
\pi
=
\lim_{m\to\infty}
\frac{|C_{3\cdot2^m}|}{6}\,s_m.
}
\]

But the right-hand finite sequence was defined without the numerical target `pi`.

## 12. Precision ontology

The #1158 mechanism can now be read as:

```text
finite orientation cycle G_m
    -> exact rational precision theta_m=2^-m
    -> algebraic unit-state transverse residual s_m
    -> six-gate renormalized scalar Pi_m^gate=(|G_m|/6)s_m
    -> intrinsic completion Pi_rot
    -> classical compatibility Pi_rot=pi
```

This makes finite resolution endogenous to the state itself:

- `m` records binary cover depth;
- `theta_m` is exact finite unresolved rotation distance;
- `s_m` is the algebraic transverse residual;
- `Pi_m^gate` is the scalar completion readout;
- no decimal precision parameter is attached externally.

## 13. Native boundary

This theorem is exact for the G1 finite orientation-cover model generated from the current `C3` ray quotient plus the connected binary-refinement principle.

It does not prove that physical G0 Cell rotation realizes the connected cover tower. That remains the final native lift problem, and Q29 forbids assuming uniqueness from current P000 alone.

What is now independent of that residual is the entire finite precision mechanism once the orientation quotient/refinement is admitted.
