# Nollm Hecke–Radix Multiplicative Field — synchronized research frontier

Status: `RESEARCH_NOTE / NONCANONICAL / NOT_FOUNDATION / NOT_WORKING_TRUTH`
At: `2026-09-08T21:42+08:00`
Scope: `cross-project research: enterprise-math <-> nollm`
EM source snapshot before write: `awdawmip/enterprise-math@9a12bd06643f58948d61b78a42f7426b98ea3e47`
Nollm source snapshot: `awdawmip/Nollm@91bd14ab394e87931b45baaaa87671f30fcfd706`
Global source snapshot: `awdawmip/chatgpt-global-knowledge@d72eeeb7034b7cf5dd04e7cefc3e2dd580babe9e`

## Purpose

Synchronize the current Nollm multiplicative-memory-field research into Enterprise Math as a durable research frontier. This note records proved finite identities, exact constructions, negative results and explicit conjectural interpretation. Persistence does not promote any claim into Foundation, Working Truth, or Nollm architecture.

## Core candidate model

For positive integers retain the exact arithmetic carrier

`n = product_p p^(v_p(n))`.

The ideal 2D similarity coordinate uses radius `r(n)=C*sqrt(n)`, so planar average density is asymptotically constant and `det(T_n)=n`. Multiplication composes scale and angle. The exact arithmetic carrier must not be replaced by the physical Cell address.

The research model has four typed surfaces:

1. **Arithmetic carrier** — prime valuations `v_p(n)` and exact residual/sheet state.
2. **Hecke shape field** — index-p sublattice geometry / shape correspondence.
3. **Radix Coverage** — p residue classes after one index-p sublattice is selected.
4. **Nollm physical observer** — layer/hex Cell/Q16 Coverage/vacancy resolution.

Hecke branch multiplicity `p+1` and radix-digit multiplicity `p` are distinct carriers and must not be collapsed.

## Exact and finite results already obtained

### A. Positive memoryless Coverage cannot diffuse and later exactly refocus

For an unbiased normalized positive kernel with expanding mean law `E[X_(k+1)|X_k=x]=alpha*x`, `|alpha|^2=p>1`, the centered variance satisfies

`V_(k+1)=p V_k + E|eta_(k+1)|^2`.

Therefore `V_k/p^k` is nondecreasing. If first-step variance is nonzero, a later exact point-mass refocus is impossible under the stated positive memoryless assumptions. Exact refocus requires retained hidden state/provenance, a reset/nonlinearity, or signed/phase cancellation. This is a proved derivation under the stated assumptions.

### B. Exact lifted recurrence for inert primes

For inert prime `p == 2 (mod 3)`, choose even recurrence r and k with

`alpha_p = sqrt(p) * exp(i*pi*k/(3r))`,

so

`alpha_p^r = p^(r/2) * omega^k in Z[omega]`.

The module

`Z[omega][X] / (X^r - p^(r/2) omega^k)`

provides an exact finite-sheet lift. The projected 2D hex position may be off-lattice while the lifted arithmetic state remains exact.

A finite prime-period choice `r_p=2p` for odd inert primes, with `k_p` chosen near a low-discrepancy target, gave good finite spatial statistics through 1,000,000 integers, but this remains numerical evidence and not an asymptotic theorem.

### C. Determinant = exact Coverage count

For an expanding integer lattice map A, `|det A|` is the index of `A Z^2` and equals the number of residue/digit classes. Thus an index-p lattice step naturally has p radix digits. This gives multiplication-as-Coverage an exact index interpretation rather than a probability-cloud interpretation.

For the exact Eisenstein Nollm matrix generated by `(a,b)`,

`M=[[a,-b],[b,a+b]]`,

`det M = a^2 + ab + b^2`.

For example `7=1^2+1*2+2^2`, so an exact determinant-7 hex similarity supports seven residue digits.

### D. Exact p=5 eight-step lattice loop

For the target phase `theta=52.5 deg = 7*pi/24`, an exact sequence of determinant-5 integer axial matrices was found:

1. `[[-1,-4],[2,3]]`
2. `[[-1,-4],[2,3]]`
3. `[[1,-1],[2,3]]`
4. `[[1,-2],[1,3]]`
5. `[[1,-1],[3,2]]`
6. `[[1,-3],[1,2]]`
7. `[[-1,-3],[3,4]]`
8. `[[1,-1],[3,2]]`

with exact product

`M8 ... M1 = [[0,-625],[625,625]] = 625*omega`

in the axial convention. Each step has determinant 5 and the full endpoint has determinant `5^8`.

Using the centered digit set `D_i={-2e,-e,0,e,2e}` at every step gives exact support sizes

`5,25,125,625,3125,15625,78125,390625`

with no collisions. At step 8 the digit words biject to all of `(Z/625Z)^2`. Hence the arithmetic point trajectory returns to an exact conformal lattice map while Coverage expands to a complete residue torus rather than refocusing to one Cell.

### E. Arithmetic anti-conformal defect

For an orientation-preserving axial integer matrix `M=[[a,b],[c,d]]` acting on `z=q+r*omega`, write

`T_M(z)=u z + v conjugate(z)`.

Define the exact Eisenstein-valued anti-conformal numerator

`B(M)=-(b+c)+(a+c-d) omega`.

Then

`Delta(M)=N(B(M))=3|v|^2 in Z_(>=0)`.

`Delta=0` iff M is conformal/similarity-preserving. For `det M=n`, the singular-value dilatation is

`K(M)=(sqrt(3n+Delta)+sqrt(Delta))/(sqrt(3n+Delta)-sqrt(Delta))`.

For inert prime p, one-step `Delta=0` is impossible; because Delta is an integer norm, `Delta>=1`. For p=5 this lower bound is attained and implies one-step `K>=5/3`.

Crucially `Delta` is only an observer. The complex defect amplitude carries a phase and composes before norming; local nonzero defects can cancel exactly over a cycle. Therefore B/u/v must be retained when future composition matters. Premature quotient to Delta loses the repair phase.

### F. Hecke correspondence repairs cross-prime commutativity

Selecting one nonconformal determinant-p matrix per prime generally breaks `pq=qp` because arbitrary integer matrices need not commute. The full lattice Hecke correspondence restores the correct multiplicative structure.

Let `T(n)[Lambda]` be the formal sum of index-n sublattices and `R(d)[Lambda]=[d Lambda]`. Classical relations give

- if `gcd(m,n)=1`, `T(m)T(n)=T(mn)`;
- `T(p^r)T(p)=T(p^(r+1))+p R(p) T(p^(r-1))`;
- in general `T(m)T(n)=sum_(d|gcd(m,n)) d R(d) T(mn/d^2)`.

Therefore the non-strict multiplicative defect is supported exactly on shared factors. For p=5, exact enumeration gives 6 index-5 sublattices, 36 two-step paths and 31 distinct index-25 endpoints; the homothetic return lattice `5 Z^2` occurs six times. Thus

`T(5)^2=T(25)+5R(5)`.

This makes the repeated-prime backflow/recoalescence multiplicity arithmetic information rather than a snap error.

## BRC carrier audit

Resolution: `REUSE_APPLIED`.

Preserve separately before compression:

- prime valuation identity;
- Hecke shape branch provenance and multiplicity;
- selected determinant-p shape step when a bounded path is materialized;
- radix digit provenance/count;
- anti-conformal complex defect amplitude B (or u,v);
- exact determinant/index;
- lifted inert-prime residue/sheet state.

Observers only:

- `Delta=N(B)` and dilatation K;
- angular-sector discrepancy;
- nearest-neighbor statistics;
- physical Cell snap/vacancy shift;
- normalized positive Q16 Coverage weights.

Positive branch mass is not signed/complex defect cancellation. Boolean support is insufficient for Hecke recoalescence because it loses repeated-path multiplicity.

## Current research hypothesis

The candidate `Hecke–Radix Multiplicative Field` may give Nollm a geometry in which:

- radius `sqrt(n)` supplies the exact 2D area-density law;
- Hecke shape correspondences supply commuting multiplicative geometry for coprime factors;
- repeated factors produce controlled gcd-indexed backflow;
- radix digits supply exact area Coverage;
- local inert-prime shear is deterministic repairable geometry rather than stochastic uncertainty;
- the existing Nollm layer/Cell/Q16 machinery remains a physical observer rather than the arithmetic truth carrier.

This interpretation is a research hypothesis. It is not yet a proved Nollm memory architecture or a proof of physical equidistribution.

## Source provenance

Synchronized from GLOBAL_KNOWLEDGE events:

- `journal/nollm/2026-09-08/20260908T201800+0800-multiplicative-hex-field-experiment.md`
- `journal/nollm/2026-09-08/20260908T204300+0800-positive-coverage-no-go-and-prime-period-lift.md`
- `journal/nollm/2026-09-08/20260908T205800+0800-determinant-coverage-radix-tiling-link.md`
- `journal/nollm/2026-09-08/20260908T211600+0800-p5-exact-hecke-radix-loop-and-beltrami-defect.md`
- `journal/nollm/2026-09-08/20260908T213400+0800-hecke-coverage-gcd-defect.md`

## Next frontier

Lay out several arithmetic/shape/radix/physical layers and inspect the same construction under changed observers: valuation depth, shape-space recurrence, anti-conformal phase, residue-torus geometry, and shared-factor backflow. Search specifically for invariants that are invisible in the ordinary polar-angle view, and for a bounded selector that preserves useful Hecke multiplicity without materializing the full correspondence.