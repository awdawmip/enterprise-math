# Heartbeat Stage38: finite autonomous tape sweep and retained loop operation

Event-ID: brc-heartbeat-autonomous-tape-holonomy-20260922-6EF011
Research-Activity-ID: RA-6EF011C2E75C4A799606AFEA
Status: RESEARCH_CANDIDATE; same-author proofs/tests, no independent review or Lean.
Parent standalone18b1d2f9da75eef15dc8ae7a5e46804b421bb48d. Global readabaf3eabd2817104bdd10900799054c9a7f7f574; project read6e9f0150600c81ae2289391bf3b83a442c463ed3. P000/residual-first unchanged.

Stage38 removes runtime mode switching for a FINITE PRELOADED BATCH by changing architecture: a moving computation front applies spatial copies of the inherited14-gate packet circuit to successive four-qubit tape blocks. No per-packet clock rewind, handoff, measurement, refreshed register or zero workspace is supplied. This is not an indefinitely reusable stationary controller. Output packets remain at their original tape locations; fixed-port retrieval/reset is not implemented.

The unchanged Stage34 compiler and Stage35 local rail are actually executed. The complete data operator is U tensor n, valid for arbitrary correlated/coherent packet inputs and references. For this fixed program, active cursor edges=32n-5, data gates=14n, data sites=4n, maximum rail alphabet16, term support<=3cells and native-path diameter2. The alphabet bound is uniform in packet COUNT, not arbitrary program complexity. This trades larger local dimension than Stage36 for no data buffer rows and lower interaction arity. Program copies and preloaded inputs are explicitly counted.

Appending ready idle positions and a finished suffix gives threshold T=32n-5+r and L=2T+r edges. The inherited whole-data read-window bound holds for arbitrary ready-supported clock/data correlations. A scalable exact extension uses B<=min(1,sum_(k<=r)C(L,k)*3^T/2^(L-r)), proved from E[3^-X] for X binomial with p>=3/4. The certificate is uniform over [pi L/3,2pi L/3]hbar/g. Ninety-nine cases compare it with the unchanged exact Stage36 binomial interface. The new bound is conservative, not a replacement claim of equality.

For n1/8/32/256/1024, actual emitted clock states are55/503/2039/16375/65527; data sites4/32/128/1024/4096. Max local rail dimension remains16. The respective leakage certificates are0.0004233056952,4.36895037e-32,4.86886122e-128,1.33833054e-1023,4.28718774e-4094. A known convex bad preparation massxi0.001 is retained; it is not replaced by an off-ready population that could carry coherence. All32-bit dyadic clock coefficients are actually generated. With all14n data-gate half-angle deviations bounded by1e-8, combined stream error bounds are approximately0.00142318843,0.00100248241,0.00100994425,0.00107958815,0.00131836723. Tiny tail alone is not total apparatus accuracy.

Large cases compute complete layout/hash, every dyadic coefficient, and scalar mathematical certificates, NOT full multi-packet density evolution. n1/8/32/256 use3repeats with median0.000741487/0.004854497/0.020567863/0.158374790seconds; n1024 is ONE run0.887599649seconds, not a median. n1024 rail tensor dimension has43827bits. Time units in the read-window formula are physical-model units and grow with n at fixed one-cursor normg. The full multihead tensor norm is notg; a bound isg(L+1)/2. The full-rank physical preparation tail and invalid cursor sectors are not discarded.

For independent packets with initial target1/3 and memories1/4, each ideal output target is5/32; allzero target probability(27/32)^n and even parity[1+(11/16)^n]/2 are conditional on that product input, not generic correlated-stream algorithms. A separate two-packet coherent CV model retains all9clock starts/all81 transitions. Target probabilities0.3498836814904364,0.3162260280072754 and joint11=0.1108246351537711 are compared against complete144-dimensional static evolutions. Six correlated-ready diagnostics also test the whole-stream theorem.

A second completed unit identifies what is lost if the track is simply closed. For a cycle with unitary linksG0,...,G(C-1), tree dressing removes gates from open edges but leaves M=G(C-1)...G0 on the closing edge. Only M=I allows an unfluxed scalar cycle; a scalar phase still requires retaining flux. With M eigenphasesphi_a, uniform-coupling spectra are2gamma*cos[(2pi m+phi_a)/C]. Ordered winding factors M^w are complex operators, not positive branch counts. Adding a closing inverse to force identity also cancels the intended round operation. This is not a universal impossibility theorem for autonomous cyclic machines.

The three-state INTERNAL-clock example links(X,I,I) has exact spectrum{-2,-1,1,2}; incorrectly dropping its matrix seam gives{-1,2}. At t=2atan(3/5), a full-rank product preparation gives true target640331/1252815=0.511113771785938 versus false45619/73695=0.619024357147703, exact difference-45064/417605. This internal loop is not a triangle of primitive native spatial steps.

For a near-identity seamM(q)=cI-isX with c=(1-q^2)/(1+q^2),s=2q/(1+q^2), the exact invariant Tr[H(q)^3-H(0)^3]=-24q^2/(1+q^2). Atq1e-8 the invariant is-24/10000000000000001. In an explicit mixed6-dimensional model att1, the target probability changes by+6.2985966271946734e-17. The72-order exact rational Taylor candidate has deterministic radius4.2257221528426465e-84; it is not silently trace-normalized. An independently assembled110-digit exponential is inside the interval. Small loop residues are retained, not called experimental signals or heat.

The sweep conserves its specified static history generator. The inherited external-pulse bare-energy ledger would scale as n*5/32 work, target decrease n*17/96, side-memory increase n/3; this is NOT a free energy supply for the static implementation. Actual bare drift/battery/preparation must be modeled separately. At twice the ideal mirror time the complete finite state recurs and undoes processing. Neither permanent output storage nor cyclic feed/reset is completed.

Validation:25543 assertions, including24840 individual cursor-edge checks (not independent physical experiments);12 complete tensor-operator comparisons,3 complete physical-rail embeddings,6 full144-dimensional dynamics,99 exact binomial comparisons, cyclic spectrum/seam tests and a nonzero tiny-observable interval. Eighteen additional numerical cases cover12 cycle spectra and6 correlated-ready states. Nine110-digit reference records cover4 ring exponentials,1 tiny seam and4 scalar-clock probability matrices; scalar references are not claimed as110-digit full144-dimensional calculations. Frozen8cases yield22immutable rows; all8candidate results and timing summaries replay without replacement. One1024-packet case is a single run. Candidate/reference code has one author.

One configured academic request actually used private Issue448,batchbc12a1c8-329a-44f8-bbfd-60082e1d0441,matched intake5776711978/result5776713776 and requestSHA70866b18744a646a03433c1ae59a2bcb79ff0051b6187012f92bb52fb7fb8f93. OuterFAILED/nestedPARTIAL,3metadata/abstract previews,1provider call,0bridge-model calls; no supplied row was used as proof evidence. Primary official abstracts consulted separately:0802.0886,1712.07395 and PhysRevA75.062332. Autonomous Hamiltonian computation, gauge dressing and exponential-Markov bounds are prior art; global novelty is not claimed.

Next: a finite cyclic controller/input conveyor that retains its matrix-valued loop operation and charges direction, readiness and reset resources. The completed finite tape must not be renamed perpetual controller reuse. No Shor speedup, generic efficient quantum simulator, universal absolute-zero theorem or new worldview axiom. Full proofs/code/raw data are in the cumulative standalone Git bundle; Source publication, Drive delivery and activity aggregate intake are separate.
