# Free Research — Stationary Weighted Triple-Branch Realization

Status: `FREE_RESEARCH_FRONTIER / PRE_RECOALESCENCE_WEIGHTED_REALIZATION / PRODUCT_MEASURE_STATIONARY / SUPPORT_PRESERVED / BOOLEAN_EXTENSION_REQUIRED / NOT_WORKING_TRUTH / NOT_FOUNDATION`
Date: `2026-09-04`
Parent: `FREE_RESEARCH_WEIGHTED_S3_LIFT_PROJECT_MIXER_20260904.md`

## 1. Executive advance

The weighted `S_3` lift–transpose–project mixer is not only an abstract affine update.  It is generated by a finite pre-recoalescence branch process with a canonical stationary measure.

Given a finite weighted action family `(S,u)`, form ordered triples with product branch mass

\[
\boxed{
w(a,b,c)=u_au_bu_c.}
\]

Each of the three position transpositions preserves this mass pointwise.  Their uniform Markov average therefore preserves both the full product distribution and its positive support.

Lifting an action value to the first coordinate, applying the transposition average, and pushing back the two partner coordinates produces exactly

\[
\boxed{x_a\mapsto\frac{x_a+2\bar x}{3}.}
\]

Thus the strict `1/3` relation contraction is generated before support recoalescence by a finite measure-preserving history operation.

---

## 2. Ordered triple branch space

A branch label is

\[
t=(a,b,c)\in S^3.
\]

Define the three involutions

\[
s_{12}(a,b,c)=(b,a,c),
\]

\[
s_{13}(a,b,c)=(c,b,a),
\]

\[
s_{23}(a,b,c)=(a,c,b).
\]

They preserve product mass exactly:

\[
\boxed{w(s_{ij}t)=w(t).}
\tag{2.1}
\]

Hence they preserve the positive support of the weighted branch ensemble.

The uniform Markov operator is

\[
\boxed{
(\mathsf M_3F)(t)
=\frac13\bigl(F(s_{12}t)+F(s_{13}t)+F(s_{23}t)\bigr).
}
\tag{2.2}
\]

For nonnegative branch readouts, `M_3F` remains nonnegative.

The product density is pointwise stationary:

\[
\boxed{\mathsf M_3w=w.}
\tag{2.3}
\]

This is stronger than global mass preservation.

---

## WTB-T01 — Local first-coordinate action

Lift a scalar action channel `x` by

\[
F_x(a,b,c)=x_a.
\]

Then

\[
\boxed{
(\mathsf M_3F_x)(a,b,c)
=\frac{x_a+x_b+x_c}{3}.
}
\tag{3.1}
\]

This is the fixed-triple local standard projection.

---

## WTB-T02 — Exact weighted pushback

Define the unnormalized first-coordinate pushback

\[
\mathcal N_a(F)
:=\sum_{b,c\in S}u_bu_cF(a,b,c).
\]

For the mixed lifted channel,

\[
\boxed{
\mathcal N_a(\mathsf M_3F_x)
=\frac{U^2x_a+2U\sum_bu_bx_b}{3}.
}
\tag{4.1}
\]

If `U!=0`, normalize by `U^2`:

\[
\boxed{
\frac{\mathcal N_a(\mathsf M_3F_x)}{U^2}
=\frac{x_a+2\bar x}{3}.
}
\tag{4.2}
\]

This proves that the affine weighted state update is exactly the first marginal of the stationary triple-branch Markov process.

---

## 5. Relation-field consequence

For weighted totals `c_a=u_ax_a`, the pushed-back totals are

\[
c'_a=\frac13c_a+\frac23u_a\bar x.
\]

The common-mean term carries no internal relation field, so

\[
\boxed{Z'_{ab}=Z_{ab}/3}
\]

and

\[
\boxed{\mathcal E'_Z=\mathcal E_Z/9.}
\]

The product branch process therefore realizes the previously derived state-level contraction exactly.

---

## 6. Relation to exact Boolean support

The operation preserves weighted support, but current exact BRC recoalescence retains only the unweighted union support.  That Boolean token cannot recover the product masses or the first marginal after the branches have been merged.

Therefore the correct execution order is

\[
\boxed{
\text{weighted triple lift}
\to
\text{transposition Markov step}
\to
\text{weighted marginal/readout}
\to
\text{optional exact support recoalescence}.
}
\]

Recoalescing before the weighted marginal invokes the previously formalized no-resurrection obstruction.

This is a conservative extension: forgetting all masses and totals returns the same exact reachable support semantics.  The extension adds quantitative branch provenance, not new fine endpoints.

---

## 7. Formal status

Lean file:

- `EnterpriseMath/Relation/WeightedTripleBranchMixer.lean`.

It formalizes:

1. the three involutive position swaps;
2. pointwise product-mass invariance;
3. positive-support invariance;
4. stationarity of product mass under the Markov average;
5. positivity preservation;
6. the local first-coordinate formula;
7. exact unnormalized pushback;
8. exact normalized identification with the weighted `S_3` mixer.

The branch is not declared Lean-green until workflow completion.

---

## 8. Updated boundary

Closed at finite weighted-extension strength:

- a canonical stationary branch measure;
- exact generation of the `S_3` mixer before recoalescence;
- support preservation;
- weighted marginal realization;
- strict relation contraction.

Still open:

- promotion of this weighted branch extension into the primitive Enterprise state language;
- an arithmetic theorem showing that the prime-winding dynamics supplies the required product triple ensemble at each moving cutoff;
- summability of the half-scale tail forcing;
- a native quantitative PNT remainder.

---

## 9. Next theorem

The next target is no longer the existence of the mixer.  It is an arithmetic compatibility theorem:

> Show that the degree-three prime-winding collision packet has exactly the stationary product weights required by the triple-branch mixer, including the moving-cutoff tail decomposition, and identify the surviving half-scale marginal after the `1/9` same-scale energy contraction.

The fully split coefficient

\[
\frac{(\Lambda_Y*\Lambda_Y*\Lambda_Y)(m)}m
\]

already supplies the grouped product mass.  What remains is to retain its ordered triple refinement through the cutoff cascade.
