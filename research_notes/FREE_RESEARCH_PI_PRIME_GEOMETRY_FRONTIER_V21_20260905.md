# Free Research — Pi-to-Prime Geometry Frontier V21

Status: `FREE_RESEARCH_CURRENT_FRONTIER / PRIME BIRTH-WINDING GEOMETRY STABLE / PNT CLOSED BY REAL SMOOTHING / VARIABLE-DEPTH VOLTERRA FRAME CLOSED / CENTERED FOURTH-ORDER ZERO-PHASE EQUIVALENCE CLOSED / EIGHT-CHAMBER CARRIER AUDITED / NATIVE FOURTH-ORDER DECAY OPEN / NOT WORKING TRUTH / NOT FOUNDATION`
Date: `2026-09-05`
Project: `Enterprise Math / 进取数论`
Supersedes as current frontier: `FREE_RESEARCH_PI_PRIME_GEOMETRY_FRONTIER_V20_20260905.md`
Researcher-ID: `EM-FREE-PI-PRIME-20260905`

## 1. Stable interpretation

The arithmetic extension of the Enterprise pi geometry remains

\[
\boxed{
\begin{aligned}
\text{prime }p
&=\text{birth of an irreducible multiplicative-holonomy direction},\\
p^a
&=\text{the }a\text{th winding layer in that direction},\\
\det\mathcal W_N
&=\operatorname{lcm}(1,\ldots,N),\\
\psi(N)&=\log\det\mathcal W_N.
\end{aligned}}
\]

The degree-three ordered provenance carrier supplies:

- the `3!` history multiplicity behind the quadratic prime completion;
- the exact `S_3` standard-sector energy factor `1/9`;
- the odd-simplex direct-composite chord
  \[
  4U_N^2|f(N)|^2
  \le3(U_NE_1+E_{\rm dir}+E_{\rm tr}),
  \]
  which anchors the parity-constant mode.

The ordinary prime number theorem has already been recovered in the project by real Selberg smoothing.  V21 concerns the still-open native finite-history decay mechanism and any quantitative geometric remainder.

---

## 2. Dynamic variable: the first Volterra defect

Let

\[
(Lf)(n)=\sum_{a\le n}u_af(q_a n),
\qquad
A(n)=\sum_{a\le n}u_a,
\qquad
M_Af=A f,
\]

with `u_a=Lambda(a)/a`.  The correct dynamic defect is

\[
\boxed{D=[M_A,L]-L^2.}
\]

For the full signless residual

\[
G_f=(M_A+L)f,
\]

the exact parametrix is

\[
\boxed{
A(n)^2f(n)=(M_A-L)G_f(n)-Df(n).
}
\]

For `f=r=psi/n-1`, the established arithmetic layer gives `G_r=O(1)`.  Thus a native proof is equivalent to forcing

\[
Df(n)/A(n)^2\to0.
\]

---

## 3. Growing-depth Volterra geometry

If the logarithmic action measure has cumulative mass `t+O(1)`, then its `k`-fold simplex mass satisfies, uniformly for

\[
k=o(\sqrt{\log n}),
\]

\[
\boxed{
\mathcal C_k(n)
=
\frac{(\log n)^k}{k!}
\exp\!\left(O\!\left(\frac{k^2}{\log n}\right)\right).
}
\]

This includes the natural depth

\[
k\asymp\log\log n.
\]

For

\[
\Delta_k=[M_A,L^k]-kL^{k+1},
\]

the two positive sides remain mass-balanced with relative error

\[
O((k^2+1)/\log n).
\]

The derivation identity is exact:

\[
\boxed{
\Delta_k=\sum_{j=0}^{k-1}L^jDL^{k-1-j}.
}
\]

---

## 4. Reverse frame and automatic commutator closure

Path ANOVA over the possible placements of `D` in a `k`-history word gives a positive reverse frame for `Df(n)`.  Its three components are:

1. a high-order scalar defect;
2. placement curvature generated by `[L,D]`;
3. an endpoint history relation of `Df`.

The first component is `O(k^-4)` after normalization by `A(n)^4`.

The double commutator has the exact recoalesced form

\[
([L,D]f)(n)
=
\sum_{ab\le n}u_au_b
\kappa_A(n;a,b)f(q_{ab}n),
\]

where

\[
\kappa_A(n;a,b)
=A(q_an)+A(q_bn)-A(n)-A(q_{ab}n).
\]

The logarithmic rectangle cancels, leaving

\[
|\kappa_A|\le4C+2\log2.
\]

Therefore the normalized placement term is uniformly

\[
O(A(n)^{-2}).
\]

For even `k`, the last relation is parity-correct:

\[
|Df(n)+Df(q_{\mathbf a}n)|^2
\]

over odd history length `k-1`.

---

## 5. Signless lift and centered fourth-order packet

For a deterministic quotient `Q_c` and `delta_c=I+Q_c`, on the absorbing-zero carrier `f(0)=0`,

\[
\boxed{
\delta_cDf=D\delta_cf+[Q_c,D]f.
}
\]

The commutator `[Q_c,D]` is again the bounded logarithmic rectangle coefficient.  Hence the final history relation lifts to a stopped pair field

\[
F_c(a,b)=\delta_cf(\Phi_n(a,b)).
\]

After centering the valid/stopped parity sign before squaring, define

\[
\boxed{
\mathfrak D_4(f;n)
=
\frac1{A(n)}
\sum_{c\le n}u_c\mathcal D_n^{(2)}(F_c),
}
\]

where

\[
\begin{aligned}
\mathcal D_n^{(2)}(F)
=\frac16\mathbb E_{a,b,d}
\Big(&|F(a,b)-F(b,a)|^2\\
&+|F(a,b)-F(d,b)|^2\\
&+|F(a,b)-F(a,d)|^2\Big).
\end{aligned}
\]

The pair-valued `S_3` Poincare theorem gives

\[
\operatorname{Var}(F)\le3\mathcal D_n^{(2)}(F).
\]

All parity imbalance and deterministic-quotient commutator errors are `O(A(n)^-2)` for bounded `f`.

---

## 6. Exact zero-phase characterization

For the actual prime error,

\[
\boxed{
r(n)\to0
\quad\Longleftrightarrow\quad
\mathfrak D_4(r;n)\to0.
}
\]

### Forward direction

For fixed `Z`, all folded and suffix endpoints below `Z` have total normalized mass

\[
O((1+\log Z)/A(n)).
\]

On the complement, `|r|` is bounded by the tail supremum `epsilon_Z`.  Hence

\[
\mathfrak D_4(r;n)
\le8\epsilon_Z^2
+O\!\left(B^2\frac{1+\log Z}{A(n)}\right).
\]

### Reverse direction

Choose even

\[
k(n)\asymp\log\log n.
\]

The growing-depth history endpoint lies below any fixed `Z` with probability

\[
O\!\left(
\frac{k(n)(1+\log Z)+k(n)^2}{\log n}
\right)
\to0.
\]

Thus pointwise decay of `mathfrak D_4` implies decay of its history average.  The reverse frame and parametrix then force `r(n)->0`.

This identifies the final gate exactly but does not prove its decay independently of PNT.

---

## 7. Eight-chamber carrier audit

The three pair-transposition differences split into exactly eight nonzero positive chambers:

1. stopped swap relation;
2. first-slot valid/valid common-suffix curvature;
3. first-slot stopped/stopped relation;
4. two first-slot mixed valid/stopped boundary relations;
5. second-slot valid/valid common-suffix curvature;
6. two second-slot mixed valid/stopped boundary relations.

The valid swap chamber vanishes, and the second-slot both-stopped chamber vanishes.

For every endpoint pair `x,y`,

\[
|\delta_cf(x)-\delta_cf(y)|^2
\le2|f(x)-f(y)|^2
+2|f(q_cx)-f(q_cy)|^2.
\]

Hence `mathfrak D_4` is exactly a coefficient-safe assembly of:

- retained ordered relation fields;
- common-suffix transports;
- moving-cutoff boundary channels already isolated in V14/V15.

No new observable or state component remains undiscovered.

---

## 8. Corrected verification boundary

Two synthetic checker corrections are part of the V21 frontier.

1. The growing-depth fixture must impose
   \[
   M_A1=L1.
   \]
   The corrected file is
   `scripts/check_free_research_growing_depth_commutator_frame_v19_corrected.py`.

2. `Q_cL=LQ_c` holds on the absorbing-zero subspace, not as an unrestricted full-matrix identity.  The corrected file is
   `scripts/check_free_research_defect_signless_lift_v20_absorbing_corrected.py`.

Additional exact checkers:

- `scripts/check_free_research_log_rectangle_commutator_v19.py`;
- `scripts/check_free_research_centered_fourth_order_gate_v20.py`.

The first-draft versions are explicitly superseded by errata.  A targeted corrected workflow has been added, but no green status is asserted until a completed run is independently observed.

---

## 9. Unique remaining mathematical task

Everything now reduces to one statement:

\[
\boxed{
\mathfrak D_4(r;n)\longrightarrow0
}
\]

proved from the finite prime-winding relation dynamics rather than imported from the already known PNT.

The precise coefficient-level route is:

1. use the eight-chamber audit;
2. absorb mixed chambers into the V14 tail potentials;
3. use the `1/9` standard relation mixer on the valid and stopped pure chambers;
4. route all common-suffix copies to lower scales without per-level recanonicalization loss;
5. prove a strict or summably defective recurrence for `mathfrak D_4`.

Alternative valid routes are a native slow-oscillation theorem or an increasing-depth stopped-history transposition estimate.

No quantitative native prime remainder, Working Truth, Foundation status, or RH-scale conclusion is claimed.
