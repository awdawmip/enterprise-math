# Enterprise native shell primorial resonance ladder

Status: `FREE_RESEARCH_EXACT_LOCAL_FACTOR / FINITE_CENSUS / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-23`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Parent: `NATIVE_ENTERPRISE_C3_SHELL_AP_RECOALESCENCE_RESONANCE_20260823.md`

For every admissible native shell `r` with `6|r`, each C3 fiber is a three-term arithmetic progression with common difference `r`.

For an extra prime `p>3`, the local triple-prime root count is 1 when `p|r` and 3 when `p does not divide r`. Therefore adding `p` as a distinct shell factor multiplies the relative local resonance weight by

`rho_p=(p-1)/(p-3)>1`.

The multiplier strictly decreases as `p` increases. Hence, under any upper bound on `r`, the maximum relative resonance skeleton is obtained by taking the smallest available distinct primes first. This produces the native primorial ladder

`6 -> 30 -> 210 -> 2310 -> 30030 -> 510510 -> ...`.

The first factors are

- `6=2*3`: universal full-C3 admissibility gate;
- `30=2*3*5`: first additional resonance shell skeleton;
- `210=2*3*5*7`: also the exact equal-coordinate midpoint recoalescence shell gate;
- `2310=2*3*5*7*11`: strongest distinct-prime resonance skeleton available below 5000;
- `30030=2*3*5*7*11*13`: next theoretical rung.

The relative multiplier (ignoring the fixed 2,3 baseline) is

`R(r)=product_{p|r,p>3}(p-1)/(p-3)`.

Thus

- `R(30)=2`;
- `R(210)=3`;
- `R(2310)=3.75`;
- `R(30030)=4.5`.

For `r<=5000`, 2310 and 4620 share the maximum distinct-prime resonance skeleton. In the finite full-shell census:

- `r=2310` has 18 fully-prime C3 fibers;
- `r=4620` has 27, the largest observed count through 5000.

The raw count also grows with the number of available side offsets, so equal resonance skeletons need not have equal counts. The exact arithmetic content is the multiplicative local-factor ladder; the finite counts are only scale checks.

Interpretation:

`NATIVE SHELL FACTORIZATION -> C3 PRIME RESONANCE`.

This gives a radial prime-organization law in the Enterprise shell coordinate without introducing a Euclidean radius. The arithmetic prime-tuple factor itself is classical; the research content here is its direct realization as a native-shell resonance hierarchy.
