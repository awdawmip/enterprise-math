# Recovery M17

Parent objective: progressively migrate actual Enterprise Math / Nollm research
code from floating decision semantics to integer plus typed residuals.

Verified local frontier: M16 remote baseline `86111435b173464c91c000275077b74411fe93db`;
M17 modifies the actual Phase32 Python caller only. Explicit `cell_pitch=(n,d)`
uses the M16 uint32 certifier and the already-tested axial lexicographic tie adapter.
Legacy no-option objects remain unchanged.

Validation: M16 154 retained tests plus Phase32 17 original + 12 new = 183 recorded
tests, zero failures/errors and exactly two source-slice skips for missing unchanged
`phase32_lab.html`. Nine 4096 actual-schedule cases and one 65536 golden/pitch-one
case are fully certified and match legacy aggregate/identity cells. Clean replay
produces byte-identical deterministic evidence.

Do not repeat M01–M16, do not infer exact pitch from float `config.pitch`, do not
replace this caller's axial lexicographic ties with the base q/r/s tie rule, and do
not call this A2 observer native X6/P000.

Next executable unit after publication: M18 real CLI/machine-output exact pitch and
precision contract for Phase32. Browser `phase32_lab.html` is a later separate unit;
M15 production-workbench/full-root-package admission remains independently pending.
