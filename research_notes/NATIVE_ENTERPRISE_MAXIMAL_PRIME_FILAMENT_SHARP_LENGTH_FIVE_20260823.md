# Native Enterprise maximal-prime filament：sharp length-five cap

Status: `FREE_RESEARCH_EXACT_EXISTENCE_PLUS_OBSTRUCTION / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-23`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Parent: `NATIVE_ENTERPRISE_MAXIMAL_PRIME_FILAMENT_TRANSVERSE_CLASS_20260823.md`

## 1. Rolling filament

A length-`L` filament consists of `L` consecutive maximal five-prime flowers on one constant transverse coordinate

`h=t-ceil(r/2)`.

It uses exactly `L+4` distinct primes because consecutive five-prime windows overlap in four values.

If the first shell is `r`, the consecutive prime gaps are

`d(s)=3s-4` for even `s`,

`d(s)=3s-5` for odd `s`,

for `s=r,...,r+L+2`.

## 2. Exact mod-5 capacity calculation

Because parity affects the gap word, classify the start by

`r mod 10`

and the transverse coordinate by

`h mod 5`.

There are exactly `10*5=50` local residue pairs to test.

For each pair, generate the rolling `L+4` values and require every value to be nonzero mod 5.

The exact survivor counts are:

- `L=1`: 15 of 50 pairs;
- `L=2`: 10 of 50;
- `L=3`: 7 of 50;
- `L=4`: 4 of 50;
- `L=5`: 2 of 50;
- `L=6`: 0 of 50.

Therefore, once all values exceed 5,

`MAXIMAL PRIME FILAMENT LENGTH <= 5`.

This is an exact modular obstruction, not a finite-search observation.

## 3. The two extremal length-five channels

For `L=5`, the only mod-5/parity survivors are

- `(r mod 10, h mod 5)=(3,4)`;
- `(r mod 10, h mod 5)=(8,1)`.

Combining with the universal maximal-flower condition

`h=4 mod 6`

gives the two exact transverse channels modulo 30:

- `(r mod 10, h mod 30)=(3,4)`;
- `(r mod 10, h mod 30)=(8,16)`.

If a sixth consecutive flower were added, the tenth rolling value is divisible by 5 in both surviving classes. Thus the length-five cap is sharp at the local-sieve level.

## 4. Explicit length-five filament

A deterministic search found a genuine length-five filament at

`r0=10688`,

`h=-2474`.

It lies in the second allowed extremal channel:

`10688 mod 10 = 8`,

`-2474 mod 30 = 16`.

The five consecutive flower centers occur at

- `(10688,2870)`;
- `(10689,2871)`;
- `(10690,2871)`;
- `(10691,2872)`;
- `(10692,2872)`.

The union consists of exactly nine primes:

`171283421`,

`171315481`,

`171347543`,

`171379609`,

`171411677`,

`171443749`,

`171475823`,

`171507901`,

`171539981`.

The consecutive gaps are

`32060, 32062, 32066, 32068, 32072, 32074, 32078, 32080`,

which exactly match

`d(10688),...,d(10695)`.

Each of the five consecutive length-five windows is a maximal five-prime flower.

All nine values are below `2^64` and were independently rechecked by direct trial division through their square roots.

## 5. Sharp result

Combining existence and obstruction gives the exact finite-geometry statement

`MAXIMAL CONSECUTIVE PRIME-FILAMENT LENGTH = 5`

for non-exceptional values greater than 5.

This means the native incidence geometry admits a longest possible rolling cluster:

- five overlapping maximal flowers;
- nine distinct prime Cells;
- a sixth flower is prohibited by the mod-5 channel.

## 6. Interpretation

The sharp cap is generated by the interaction

`TRIPLE-CELL INCIDENCE`

`-> MAXIMAL FIVE-PRIME FLOWER`

`-> CONSTANT-h FILAMENT`

`-> ROLLING PRIME GAP LAW`

`-> MOD-5 CAPACITY EXHAUSTION AT L=6`.

No claim is made that the underlying prime k-tuple phenomenon is new in classical number theory. The research-specific statement is the exact length cap for this native Enterprise rolling-incidence filament selected before the prime search.
