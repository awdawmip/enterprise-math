# X6 triadic rotation V4: the index-two frame coset is not the FCC/Viète chirality sheet

Status: `FREE_RESEARCH / EXACT GROUP-THEORETIC SEPARATION / NOT_FOUNDATION`
Date: `2026-09-06`
Task: `RS-X6-NATIVE-ROTATION-DYNAMICS`
Consumes:
- `R_triad=(C2)^6 semidirect A6` from V1;
- full signed frame skeleton `B6=(C2)^6 semidirect S6`;
- current FCC/K4 `S4<S6` edge action;
- current Viète/Euler STAR chirality local system.

## 1. The remaining algebraic frame coset

Let

`B6=(C2)^6 semidirect S6`

be the full signed coordinate-frame symmetry of the X6 component norm/adjacency skeleton.

Let

`R_triad=(C2)^6 semidirect A6`

be the frame group generated by all intrinsic triadic signed-C6 generators.

Define

`par:B6 -> C2`

by forgetting sign flips and taking the parity of the underlying positive-axis permutation.

Then

`ker(par)=R_triad`.

Therefore

`B6/R_triad ~= C2`

and `R_triad` is normal of index two.

No Euclidean determinant or reflection interpretation is required. `par` is only positive-axis permutation parity.

## 2. One odd axis permutation is sufficient to generate the entire missing coset

Let `t in B6` have odd underlying axis permutation.

Because `R_triad` is an index-two normal subgroup, `t` lies in the unique other coset and

`<R_triad,t>=B6`.

Thus the question “does physical native frame rotation exceed the triadic-generated group?” is algebraically reduced to one binary admissibility question:

- if no odd-permutation frame generator is physically admitted, the reachable frame group remains `R_triad`;
- if any one odd-permutation frame generator is admitted, the generated signed frame group becomes all of `B6`.

This does not decide the physical answer.

## 3. The FCC/K4 subgroup lies entirely inside A6

The current FCC atlas identifies the six native/carrier line labels with the six edges of `K4`. A vertex permutation `g in S4` induces a permutation `iota(g)` of the six edges.

Take a vertex transposition `(ab)`. On the six K4 edges:

- the edge `ab` is fixed;
- the edge joining the two untouched vertices is fixed;
- the four remaining incident edges are exchanged in two disjoint pairs.

Hence the induced six-edge permutation is a product of two transpositions and is even.

Vertex transpositions generate `S4`, so

`iota(S4) subset A6`.

Therefore the entire current FCC atlas-preserving subgroup already lies in the positive-axis permutation projection of `R_triad`.

In particular, current FCC chart transport does not supply the missing odd `S6/A6` coset.

## 4. Local STAR chirality reversal can occur under an even global six-axis permutation

The current Viète/Euler atlas theorem uses K4 vertex transpositions `T_ij` to move between STAR charts. Those transitions reverse the local cyclic STAR orientation.

But by Section 3 the induced permutation on the six global axis labels is even.

Therefore:

`LOCAL_STAR_CHIRALITY_FLIP != GLOBAL_S6_AXIS_PARITY_FLIP`.

A local three-slot orientation reversal is a chart-frame statement. It can be produced by an even permutation of the six full axis labels.

Hence the Viète/Euler chirality sheet cannot be identified with the quotient bit `B6/R_triad`.

## 5. The two C2 objects have different bases and different future observers

### Axis-parity quotient

The bit

`par(g) in C2`

is attached to a full six-axis frame transformation. It asks whether the underlying positive-axis permutation is even or odd.

### STAR chirality sheet

The Viète/Euler bit is attached to a local STAR chart/sweep orientation. It is required because K4 overlap transport can reverse the local cyclic ordering and signed root coordinate.

They answer different questions and have different projection maps.

A calculation may carry both bits, one bit, or neither depending on the future-operation horizon. No automatic identification is operation-safe.

## 6. Non-FCC chart holonomy also does not determine global axis parity

The all-20 Johnson-chart connection has `S3` slot holonomy. Around a four-axis chart triangle the returned local slot frame contains a transposition.

This transposition acts only on the three slots of the starting local chart. It does not specify how the complementary three global axes are transformed.

A local slot transposition has both even and odd extensions to `S6`: multiply a chosen odd extension by a transposition on two complementary axes to obtain an even extension with the same local action.

Therefore the `S3` chart holonomy does not by itself determine the `S6/A6` parity bit either.

## 7. Exact binary frontier for complete frame dynamics

At the static signed-frame level the remaining enlargement problem is now exact:

`R_triad --index 2--> B6`.

All current intrinsic triadic generators, current FCC/K4 atlas permutations and their local chirality phenomena are compatible with staying inside the even positive-axis permutation sector.

To prove the full `B6` is physically generated one needs an additional native law/event whose frame action has odd positive-axis parity.

To prove `R_triad` is the complete physical frame group one needs a native invariant or generation principle forbidding every odd-parity frame event.

Current P000 supplies neither theorem.

Thus this part of the rotation problem is no longer an unstructured group search; it is one precise law/admissibility gate.

## 8. Relation to physical path dynamics

Even if the static frame group were enlarged from `R_triad` to `B6`, frame membership alone would still not specify native rotation dynamics.

Every admitted frame event must still be lifted through the concrete `ROT_PATHCAT_X6` event/path layer, with branch/path provenance retained until a law or observer-safe quotient is proved.

So the index-two frame question and the path-law question remain distinct.

## 9. Current frontier

Closed here:

- exact parity character with kernel `R_triad`;
- any one odd frame element generates full `B6` together with triadic rotations;
- FCC/K4 `S4` image lies inside `A6` on the six edge axes;
- local STAR chirality reversal is explicitly separated from global six-axis permutation parity;
- all-20 local slot holonomy also does not determine global axis parity.

Open:

1. physical admissibility or prohibition of odd positive-axis frame events;
2. path-level realization if such an event exists;
3. possible coupling of the parity bit to internal/channel/field state;
4. external physical meaning, if any, of the algebraic coset.

No Foundation promotion and no identification with classical reflection/spin is made.
