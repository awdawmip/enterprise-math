# Heartbeat65 — exact returning memory and measurement-selective interference

Progress-Event-ID: HEARTBEAT65-RETURN-MEMORY-20260924-B6A9
Status: SAME_AUTHOR_CONDITIONAL_MODEL / ACTUAL_TYPED_BRC / NOT_ADMITTED
Researcher-ID: no new ID; current session registration was tool-safety blocked before an Issue receipt.
Research-Activity-ID: unavailable for this increment; do not borrow a closed activity.

## Scope and recovered frontier

Continue Stage64 standalone1a3bb74641e405820904da841666b485498ebd6e, recovered from its mounted bundle whose SHA256 was verified as e6e784d9b144554c070b75824b12cf6f27d8d4c897bc3f1fb9ab716af1f8c970. Global read62471e61bf1efa4c92cbd470ba81bce9e4d5a620; current Source/control read8fe4a6dbdbd3d7b3acd9458aafca675884605522. Unchanged bootstrap/manual/P000/worldview and BRC-only blobs were reused. P000, six native axes and residual fidelity are unchanged. Internal source/generator/rail labels are not additional spatial axes; operation rounds are not calibrated physical time.

Own status2417/hb65-status-20260924-b6a9 succeeded and reported no active service session. The standard new session_start hb65-session-20260924-b6a9 with the prior contribution history was blocked by the tool safety check before an Issue or registration receipt. No alternate-form retry, borrowed identity, fabricated activity, formal Task/CLAIM or mathematical authority. Evidence preservation is not registration completion.

## 1. Replace the no-return channel with actual live returning memory

S retains complete material/source/native port/link-field/nonnegative bank/accessible-record state. Generator G has levels0..K and energyg; L=2M source-free energy bits sit on two rails at M Cells A+j e2. Source statuses f,c,s have energies2,2,1. The actual generator V includes excited returning input; inherited contact C and cooling R are unchanged. F=RCV; a round applies F at L0, optional actual source-status instrument/report, then the inherited two-layer rail transport T. Onsite swaps and neighboring-Cell swaps with reflecting ends implement the L-contact return. No non-native wrap jump and no fresh bit after initialization.

Full raw outcome update Omega'_y=T I_y(F Omega F*) T*, normalized by its total trace. The new DelayReader encodes receiver occupation by sorted actual occupied-position sets, separately in bra and ket. It retains source/receiver cross terms and all live slots; it is a basis relabeling, not independent marginal replacement. Quantum columns are obtained from actual canonical-BRC compiled gates for every(s,g,e), including e1, not from a guessed probability matrix.

## 2. Exact excitation sectors bound memory through arbitrary returns

Define n_S=1 on f,c and0 on s; Q=n_S+g+sum e_j. C preserves n_S; V preservesg+e; R preservesn_S+e; shifts and phase/occupation/status records preserveQ. Each actual instrument outcome therefore preserves the promised sector, as does the common native-field movement with its separate bank ledger.

For Q=q and capacityK, the internal source/generator/rail sector has
D_q(L,K)=sum_(g=0..min(K,q)) choose(L,q-g)+2 sum_(g=0..min(K,q-1)) choose(L,q-1-g), with out-of-range binomial terms zero.
For q1,K>=1, D_1=L+3: storedG1vacuum, storedG0 with the single excitation at one of L rail positions, or free/entranceG0vacuum. Density entries are bounded by(L+3)^2 independently of elapsed contacts. AtL100 there are103 basis states; q2,K2 gives5253. Counts use the existing positive BRC Pascal recurrence. No square roots or numerical eigensolver.

If J embeds these coordinates into the full basis, UJ=JU_q and M_yJ=JM_(y,q). Induction proves all finite permitted adaptive histories and entire conditioned post-states agree. This is exact, not an energy-tail cutoff or finite-run extrapolation. Fixed q gives polynomial coordinate count in L; growing excitation budget, native-field size, arithmetic bit length, transcript storage, physical apparatus preparation and clock/control costs are NOT eliminated. A finite working density need not store a fresh measuring apparatus reversibly forever.

## 3. A returning-phase witness, not the previous two-fresh-bit example

Start source s,G1,ground rails. After one contactF, the full state is alpha|s,1,0>+beta*w|c,0,0>-beta*t|s,0,1>. The source and generator are then isolated between contacts. In the M2 protocol, two native rail shifts carry the used bit to R1 at neighboring Cell A+e2. Apply local phasezeta=i^phi on its excited state, then two further shifts return it to L0. The second contact is the SAME F, not an inverse or a newly prepared receiver.

For all fourzeta, just before return both the ENTIRE S,G density and the ENTIRE rail density are identical; total excitation is sharply1. Only their joint correlation differs. There is exactly no source-only phase dependence before recontact. A global readout of all subsystems together was not claimed identical.

The complete endpointc,G0,all-ground receives amplitude beta*w[alpha-u*t+alpha*t*zeta]. Thus
P_c(zeta)=beta²w²[(alpha-ut)²+alpha²t²+2alpha*t*(alpha-ut)Re(zeta)].
Atalpha3/5,beta4/5,u4/5,v3/5,t3/5,w4/5, actual BRC givesP_c(+1)=36864/390625,P_c(-1)=9216/390625,P_c(+/-i)=4608/78125. All outcomes retain their original normalization, no postselected empty generator or successful capture.

Same pre-return S,G density but different next source readouts proves that no S,G-only update can cover this allowed return family. More precision in the same reduced state cannot recreate the missing joint distinction. The effect appears after actual local return, not instantaneous propagation.

## 4. Real observation is selective, not a universal deletion command

At the remote midpoint, a real reversible receiver-occupation copy into a ready degenerate pointer, with pointer ignored only in final statistics, kills the returning-bit cross terms. Every phase then givesP_c=beta²w²[(alpha-ut)²+alpha²t²]=4608/78125. Receiver energy and its state space are not reset.

Instead, a real complete SOURCE-status record leaves coherence between |s,G1,B0> and |s,G0,B1>, since both have source status s. Its formula is
P_c^source-record(zeta)=beta²w²[alpha²+u²t²+alpha²t²+2alpha²t Re(zeta)].
Actual values are36864/78125 for+1,9216/78125 for-1,and4608/15625 for+/-i. Thus an actual source measurement can change the return response without erasing all receiver-phase interference. The two devices measure different observables; compression may not silently imitate either. Full unrecorded echo inverse and full recorded-round inverses were actually checked with the required records retained.

## 5. Empty generator does not certify negligible future coupling with returns

Stage64's decaying generator-tail bound requires fresh ground inputs and no refill. Prepare s,G0,one excited returning head bit. FullF givesP_c=alpha²w²=144/625; omittingV merely becauseG is empty givesP_c=w²=16/25. Difference256/625=beta²w², despite exactly zero initial generator excitation. The returning energy can refillG.

For a proposed generator-gate omission k, letdelta_k be the probability ofg+e_head>0 in the complete hybrid input, including weighted report histories. V equalsI ong=e=0 and||V-I||<=2, hence purification/contractivity gives output trace distance<=min(1,2sqrt(delta_k)). Hybrids with earlier omissions and exact later gates yield full future historyTV<=min(1,2sum_k sqrt(delta_k)). Uniform admissible-input bounds also suffice. Conditional historyp_y can amplify the bound to min(1,2Delta/p_y), provided the approximate history has nonzero probability. This symbolic certificate includes incoming excitation; the marginalG population alone is invalid here. Omission is a declared approximation, not removal of physical energy or residual state.

A separate exact finite-horizon theorem traces only slots whose token trajectories lie outside the complete future contact/observation cone. The retained joint marginal, not independent bit marginals, is sufficient even for initial entanglement. Extending the horizon beyond an omitted bit's return invalidates the certificate; one cannot reattach it as zero. General interslot couplings require a full circuit cone. The implemented DelayReader keeps all live slots and uses the exact sector instead, so makes no such horizon omission.

## 6. Executed comparisons and actual native field

Explicit full states and retained-sector densities match at every tested step for M1,2,3 through multiple returns. The fresh-ground Stage64 map agrees only throughL contacts; the complete S,G density first differs atL+1. A40-contactM5 closed receiver run, with real recurring old bits, stays within13 internal states/169 density entries and keeps norm1. This is not40 newly supplied receivers. Long-run exact fractions grow in bit length.

54 three-report histories (27 ideal plus27 noisy) match full true-record branches as ENTIRE live source/generator/rail densities; recorded inverses restore ready registers. 40-contact DelayReader also checks passive snapshots, idempotent same-event retries, and rejection of conflicting or impossible reports without state mutation. These safeguards are computation, not physics.

The inherited twelve-port stored fibre has raw weight4/625. AttachG1 plus two ground rail bits and execute three contacts including first return. Each step's full native-field/generator/rail density equals the retained representation. Third step has60 explicit endpoints and3600 density pairs; additional shared native motion has384 endpoints. Every explicit endpoint preservesGaussD ell=delta_xplus-delta_xminus, one identified source, separation<=1 and full declared energy4 including inherited receiver. Complete contact/generator/rail inverse and native-motion inverse pass. The actual density equality is checked before extra common native motion; equality after follows the common-channel theorem, not an invented second compressed-motion test.

## 7. Artifacts, execution provenance and limits

New cumulative52432236046eb8d6ada27f812a97c73f5f55d70c, entrySTART_HERE_STAGE65.md. Filesstage65/delay.py,run.py,PROOF.md,RESULTS.json,BRC_CONTRACT.json,PROVENANCE.json,MANIFEST.json.303 grouped named checks,20 actual unchanged canonical BRC core calls in the final process. Counts include basis/column checks and54 histories, not303 independent experiments. Corebc7babbb9e890f6d5a7094430a5fbdccf66c77ad:src/enterprise_math/brc_weighted_recurrent.py,blob4e6b3132580e3cd70a20a0d8bd4d28792b961afb,SHA2567520822074b8f29e1c54f57b9543c043202db8bd7c6e27f3f1d6176028ac4a26. Positive C4 weights, complete pair observations and multiplicity graphs remain separately typed. No classical trigonometry, pi expansion, ordinary numerical propagator or signed material mass.

Local bundle clone replay matched every scientific field, excluding only elapsed_ns recursively. Original1392296254ns and replay1415972040ns are retained.1249 inherited files unchanged,8 manifest entries checked,git fsckPASS,clean working tree. Same-author proof/implementation/replay, no independent review/Lean/empirical fit.

BundleBRC_Heartbeat_return_memory_stage65_20260924.bundle,38057562bytes,SHA2563a588887df400c675d52a2b18fcf1dad156cb0aae4f463260c189cef55819e6e. Drive1pZK4I1ynfNp34-v9D3YfCW9WZCMAOH2N uploaded and fetched; actual local bytecmp identical. ProofSHA256e498d564070ed1489bbc94aa59e988001547491de60085f100ad7972896e5d5b,codeSHA256d337f1b3757b6321ff265581092c675385cba37d0d00ab6e9f9cfbbb91994b29,resultsSHA2569ee0185dafa690b473c15f7a739ee3dec45541d4336a011fa6b71cc4012755b5. Replay is local, not cloud computation; no remote reader or new GUI/HTTP service deployed.

Primary public abstracts/metadata consulted: Grimsmo1502.06959/PRL115,060402(2015); Pichler-Zoller1510.04646/PRL116,093601(2016). Delayed coherent feedback and expanded-memory/tensor-network descriptions are prior art. No exhaustive novelty or full-text audit claimed. Current professional source catalog has no matched delay-feedback cache; request search found no exact pending topic. Current KQB CAPABILITIES blobc0737f0940fdb6bc814cb8cb35cdbf55dbfa1dfc contains no scholarly operation although global routing describes newer academic operations. No unlisted operation was invented or submitted; no dedicated-provider result, current quota test or successful raw cache is claimed.

NEXT: analyze many-excitation and partially accessible return channels, with a specific physical detector observable and an independently calibrated protocol; distinguish long-time recurrence/retention from irreversible commitment. Native phase/Born origin, autonomous timing/control, realistic recording/preparation/recycling costs, geometry-derived spatial fringes and laboratory data fitting remain open. This increment is not parent-physics completion. Activity registration remains incomplete because of the actual tool block.
