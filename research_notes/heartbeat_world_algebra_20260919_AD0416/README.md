# 心跳世界：命名约束与代数验证

本包是独立可复现研究快照，不是整个 Enterprise Math 克隆。

`definitions/` 是用户直接命名与原生六轴/时间约束；`p000_reality_foundation.json` 是保持全部旧字段的附加链接版本；`research/` 是待独立审定的代数研究，不能据此提升为 Foundation。

运行：`python tests/verify_heartbeat_world.py`，仅需 Python 标准库。包内三个完整复用模块保留已核验字节；prime-valuation 依赖为此前已标注的最小摘录，并非完整模块。

在源仓库的固定提交上可运行：`PYTHONPATH=src python research_notes/heartbeat_world_algebra_20260919_AD0416/tests/verify_heartbeat_world.py`。

不修改生产 Nollm，不触发 GitHub Actions，不宣称物理时间律、完整原生乘法唯一性、语义召回收益、独立评审或全项目测试通过。
