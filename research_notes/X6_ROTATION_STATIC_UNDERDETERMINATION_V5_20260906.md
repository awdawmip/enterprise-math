# X6 rotation V5: static underdetermination and symmetry-versus-generated-move theorem

Status: `FREE_RESEARCH / EXACT NO-GO + MODEL-SEPARATION / NOT_FOUNDATION`
Date: `2026-09-06`
Task: `RS-X6-NATIVE-ROTATION-DYNAMICS`
Depends on:
- `X6_TRIADIC_ROTATION_GENERATORS_V1_20260906.md`;
- `X6_TRIADIC_ROTATION_INDEX2_AND_CHIRALITY_SEPARATION_V4_20260906.md`;
- current P000 V5 / signed-X6 spatial Foundation.

## 1. Question

V4 reduced the remaining static frame enlargement to

`R_triad=(C2)^6 semidirect A6 < B6=(C2)^6 semidirect S6`

with index two. The unresolved question is whether an odd positive-axis permutation can itself be a physical native rotation event.

This note proves that the current **static** Foundation and the current triadic generator grammar cannot decide that question by themselves.

## 2. Full signed-frame automorphisms preserve the current static spatial substrate

Every `g in B6` acts on signed X6 coordinates by signed coordinate permutation.

It preserves exactly:

- native Cell adjacency `x -> x +/- e_i`;
- the component norm `sum_i x_i^2`;
- the six-axis pairwise `PERP_E` relation;
- primitive straight support-size-one status;
- composite/off-native support status;
- the family of all centered three-axis subtorsors;
- path length and the relabeled Path-formal step grammar.

None of these predicates contains positive-axis permutation parity.

Hence odd and even axis permutations are equally valid automorphisms of the current static signed-X6 geometry.

## 3. Static triadic balance predicates are also S6-invariant

The restricted equal-unit triadic realization asks for three primitive force occurrences on three distinct underlying native axes meeting one action Cell.

Any `g in S6` sends a distinct-axis triple to a distinct-axis triple and preserves the common-Cell incidence type.

Likewise the larger-population degree criterion

`sum_i d_i=3m` and `max_i d_i<=m`

is invariant under every permutation of the six axis labels.

The integer completion defect

`DEFECT_3(d)=3*max(max_i d_i,ceil(sum_i d_i/3))-sum_i d_i`

is also permutation invariant.

Therefore the current static triadic balance layer supplies no parity discriminator either.

## 4. The elementary triadic generator grammar is S6-covariant

For an **ordered** selected triad `S=(i,j,k)`, recall

`Q_S=J_S rho_S`,

where `rho_S=(i j k)` and `J_S` flips the three selected signs.

For every positive-axis relabeling `g in S6`,

`g J_S g^{-1}=J_{gS}`

and

`g rho_(i,j,k) g^{-1}=rho_(g i,g j,g k)`.

Hence

`g Q_(i,j,k) g^{-1}=Q_(g i,g j,g k)`.

So even an odd global axis relabeling maps every elementary triadic rotation rule to another rule of the same type.

This is stronger than normality of the generated group: the **generator family itself** is covariant under all `S6` relabelings when the orientation tuple is relabeled with it.

## 5. Symmetry of a dynamics is not the same as an executable move

The triadic-generated reachable frame group contains only even positive-axis permutations, but the grammar of allowed triadic moves has the larger `S6` relabeling symmetry.

Thus an odd axis permutation can act as an automorphism of the dynamics without being expressible as a finite product of the dynamics' elementary moves.

Formally:

`ODD_AXIS_RELABELING in Aut(TRIADIC_MOVE_GRAMMAR)`

but

`ODD_AXIS_RELABELING notin <Q_S>=R_triad`.

This separates two notions that must not be conflated:

- **symmetry of the law**;
- **move generated by the law**.

## 6. Two compatible dynamical completions give an underdetermination certificate

Define two candidate frame-event theories above the same current static P000 substrate.

### Even/triadic completion

Admit only frame events generated by the `Q_S` family (with their declared concrete path/BRC lifts). Reachable frame group is `R_triad`.

### Full-frame completion

Admit the same triadic events plus one entire `S6`-covariant conjugacy class of odd axis-permutation frame events, for example the axis transpositions. Then the generated frame group is all `B6`.

Both completions preserve all static P000 spatial and triadic predicates listed above. Both can be given concrete X6 path fibers because the signed coordinate graph is connected between any spatial source and its transformed target.

Therefore the current static Foundation is compatible with both answers to the odd-frame admissibility question.

This is an exact underdetermination/no-go result:

`CURRENT_STATIC_P000 + CURRENT_TRIADIC_STATIC_BALANCE`

cannot by itself prove either

`ODD_FRAME_EVENT_ALLOWED`

or

`ODD_FRAME_EVENT_FORBIDDEN`.

A new **dynamical** principle, orientation invariant, field/internal-state coupling, or direct user definition is required to settle the physical move set.

## 7. Explicit odd transposition path fiber

Let `t` swap two positive axes `i,j` and fix the other four. For a coordinate state `x`, put

`d=x_j-x_i`.

Then

`t x-x = d e_i-d e_j`.

The shortest primitive Cell-path length from `x` to `t x` is

`2|d|`,

and its N-BRC shortest-path multiplicity is

`binom(2|d|,|d|)`.

Thus no spatial connectivity obstruction prevents a path-level lift of an odd frame transformation.

If `x_i=x_j`, the spatial Cell is fixed by `t`, even though the frame transformation is nontrivial. This gives another exact witness that frame state cannot be reconstructed from spatial Cell displacement alone.

This path existence does **not** prove that `t` is a physically admitted rotation event; it only removes a possible spatial-path obstruction.

## 8. What could settle the parity gate

To close the physical frame group at `R_triad`, one needs an additional invariant preserved by every admitted native rotation event but changed by every odd positive-axis permutation.

To close it at full `B6`, one needs one native event/law with odd positive-axis parity, plus its operation-safe path/internal/time realization.

Current local STAR chirality and all-20 chart-slot holonomy do not supply such an invariant, by V4.

## 9. Current rotation frontier

Closed:

- static P000 spatial structure is fully `B6`-symmetric;
- static triadic balance is fully `S6`-symmetric;
- the elementary `Q_S` grammar is fully `S6`-covariant;
- odd relabeling can be an external symmetry without being a generated move;
- two explicit compatible dynamical completions certify underdetermination;
- odd transposition has a finite concrete X6 shortest-path fiber with exact length/multiplicity.

Remaining hard gate:

`PHYSICAL_MOVE_SET` must be decided by additional dynamics/interaction/calibration, not by more static group enumeration.

No Foundation promotion or physical odd-frame verdict is made.
