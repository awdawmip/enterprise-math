# Viète on universal X6 triadic rotation: all-20 native C12 root Cells, C24 balanced-spinor lift, and the branch-law boundary

Status: `FREE_RESEARCH / EXACT RESEARCH-COMPOSITION THEOREM / NOT_FOUNDATION`
Date: `2026-09-06`
Researcher: `EM-FREE-7D3C9A / FREE_AXIOM_DISCOVERY`
Parent issue: `#1255`
Parent Viète line: `#1158`
Consumes parallel research:
- `research_notes/X6_TRIADIC_ROTATION_GENERATORS_V1_20260906.md` (`EM-FREE-PI-PRIME-20260904`)
- `research_notes/X6_ROTATION_PATH_GROUPOID_V2_20260906.md` (`EM-FREE-PI-PRIME-20260904`)
Consumes prior Viète results:
- `research_notes/VIETE_NATIVE_HALF_TURN_PRINCIPAL_ROOT_BUNDLE_20260905.md`
- `research_notes/VIETE_NATIVE_SPINOR_C24_SATURATION_AND_C48_OBSTRUCTION_20260905.md`
- `research_notes/EULER_NATIVE_SEGMENT_SPINOR_BISECTOR_IDENTITY_20260903.md`
- `research_notes/X6_FCC_20SLICE_CARRIER_RANK_CLASSIFICATION_20260906.md`
Checker: `experiments/viete_x6_all20_triadic_root_c24_20260906/check_viete_x6_all20_triadic_root_c24.py`
Checker source commit: `bb15238647f0842905512644a89e32e6a8205725`
Current composed frontier: `main@bb15238647f0842905512644a89e32e6a8205725`

## 1. Rebase: the sixteen non-STAR selections are no longer root-path-open

The fixed FCC carrier census proved that among the 20 selected 3-of-6 axis triples:

- 4 STAR triples are rank-two triangular carrier planes with connected signed-ray `C6`;
- 4 TRIANGLE triples are full-rank FCC bases with carrier graph `C3 disjoint union C3`;
- 12 PATH triples are full-rank FCC bases with carrier graph `P3 disjoint union P3`.

Therefore the existing **planar FCC** C6/C12 mechanism is available only on the four STARs.

A parallel same-day result then proved something strictly different and stronger at native X6 strength: every oriented selected triad carries an intrinsic signed-C6 frame generator and an actual OUTER 12-Cell primitive microcycle, without using FCC planarity.

These results are compatible and must be kept distinct:

\[
\boxed{
\text{ALL 20}:\text{ native signed-X6 C6/C12 frame/path realization};
}
\]

\[
\boxed{
\text{ONLY 4 STAR}:\text{ additional planar FCC circle/gate/bisector interpretation}.
}
\]

This note composes the universal native triadic result with the Viète principal-root and component-spinor machinery.

## 2. Universal intrinsic triadic C6 generator

Choose an oriented three-axis selection

\[
S=(i,j,k).
\]

Let `Q_S` be the parallel research generator

\[
Q_S(e_i)=-e_j,
\qquad
Q_S(e_j)=-e_k,
\qquad
Q_S(e_k)=-e_i,
\]

fixing the complementary axes.

Then

\[
Q_S^6=1,
\qquad
Q_S^3|_S=-1.
\]

Starting at

\[
a_0=e_i,
\]

the six signed unit phases are

\[
\boxed{
a_0=e_i,
\ a_1=-e_j,
\ a_2=e_k,
\ a_3=-e_i,
\ a_4=e_j,
\ a_5=-e_k.
}
\]

The opposite sweep is generated by `Q_S^{-1}`.

Thus every selected triad has an intrinsic half-turn orbit

\[
a_0\xrightarrow{Q_S^3}-a_0.
\]

No FCC plane is required for this statement.

## 3. Universal OUTER C12 microcycle

For each adjacent macro phase define

\[
m_r=a_r+a_{r+1}.
\]

Because consecutive unit phases use distinct native axes,

\[
a_r\to m_r\to a_{r+1}
\]

consists of two primitive signed-X6 Cell steps.

The twelve states

\[
a_0,m_0,a_1,m_1,\ldots,a_5,m_5
\]

are pairwise distinct and form a closed primitive 12-Cell cycle.

This is the all-OUTER branch word in the universal rotation-path groupoid.

Give the cycle the phase indexing

\[
a_r\leftrightarrow[2r]_{12},
\qquad
m_r\leftrightarrow[2r+1]_{12}.
\]

Then one primitive Cell microstep is one `C12` phase step on this declared cycle.

## 4. The oriented half-turn root Cell is explicit for every triad

The half-turn from `a_0` to `a_3=-a_0` takes six C12 microsteps.

Its half-way microstate occurs after three microsteps:

\[
\boxed{
R_S^+=m_1=a_1+a_2=-e_j+e_k.
}
\]

Relative phase is

\[
+3\pmod{12}.
\]

Under the reverse sweep `Q_S^{-1}`, the corresponding half-way Cell is

\[
\boxed{
R_S^- = e_j-e_k=-R_S^+,
}
\]

with relative phase

\[
-3\pmod{12}.
\]

Therefore the same native construction that previously existed only on one FCC STAR now exists on every one of the 20 selected triads:

\[
\boxed{
\text{ORIENTED NATIVE TRIADIC HALF-TURN}
\longrightarrow
R_S^{\pm}\text{ at C12 phase }\pm3.
}
\]

This is an actual Cell-state/path statement, not only a character calculation.

## 5. The root Cell is automatically a balanced two-component native segment

For either sweep,

\[
R_S^{\pm}
\]

has exactly two nonzero native components, each of absolute value one.  Hence

\[
\boxed{L_E(R_S^{\pm})^2=2.}
\]

The signed-X6 BRC kernel gives

\[
\boxed{
N_{\min}(R_S^{\pm})=2,
\qquad
B_{\min}(R_S^{\pm})=2.
}
\]

For the forward sweep use the local signed component basis

\[
(f_1,f_2)=(a_1,a_2)=(-e_j,e_k).
\]

Then

\[
R_S^+=f_1+f_2
\]

has local component coordinates

\[
\boxed{[1:1].}
\]

For the reverse sweep use its own oriented local component frame.  It is again a positive balanced `[1:1]` segment in that reversed frame.  When both frames are expressed in one fixed reference chirality algebra, the two local complex markers are inverses/conjugates.

This frame-typed statement avoids the incorrect identification of reversal with simply negating both projective component coordinates in one fixed `J` chart.

## 6. C12 and C24 are two typed readouts of the same balanced root Cell

The projective segment-spinor theorem is sector-local and explicitly states that its component marker

\[
J^2=-1
\]

is **not** a primitive native spatial axis and is **not** identified with the classical carrier plane.

For the balanced local component pair `[1:1]`, it proves

\[
\mathcal C[1:1]=J
\]

and

\[
S[1:1]
=\frac{1+J}{\sqrt2},
\qquad
S[1:1]^2=J.
\]

Apply this theorem in the local oriented component frame carried by `R_S^+`.

Then the actual native Cell displacement `R_S^+` has two simultaneous, differently typed readouts:

### C12 character readout

Its projective character is the quarter-turn character

\[
\boxed{
\mathcal C(R_S^+)=J_S,
}
\]

corresponding to residue

\[
[3]_{12}.
\]

### C24 spinor readout

Its normalized component segment is

\[
\boxed{
\frac{R_S^+}{\sqrt2}
=S_S[1:1]
=\frac{1+J_S}{\sqrt2}
=U_{2,S},
}
\]

which is the distinguished order-eight C24 root.

Thus the C24 anchor does not need a second spatial Cell:

\[
\boxed{
\text{THE SAME BALANCED OUTER HALF-TURN MIDPOINT CELL}
\text{ ANCHORS C12 BY ITS SQUARED CHARACTER AND C24 BY ITS NORMALIZED SPINOR STATE}.
}
\]

This is now proved for all 20 triadic selections at native component/path strength.

## 7. Sweep reversal

Let one fixed reference local character marker be `J_S`.  Encode the two sweep sheets by

\[
\varepsilon=\pm1.
\]

Then the quarter character is

\[
K_{S,\varepsilon}=\varepsilon J_S
\]

and the C24 spinor is

\[
U_{2,S,\varepsilon}
=\frac{1+\varepsilon J_S}{\sqrt2}.
\]

Sweep reversal gives

\[
\varepsilon\mapsto-\varepsilon,
\]

hence

\[
K\mapsto-K
\]

and

\[
\boxed{
U_{2,S,-\varepsilon}
=U_{2,S,\varepsilon}^{-1}.
}
\]

The endpoint half-turn and sweep reversal remain distinct operations: the former moves `a_0 -> -a_0` inside one oriented arc, while the latter changes which arc/root sheet is used.

## 8. Universal principal precision lineage

The oriented root Cell supplies the same principal pro-state coordinate as before:

\[
\Sigma_{S,\varepsilon}=3\varepsilon.
\]

At precision level `m`,

\[
\boxed{
R_m(S,\varepsilon)
=[3\varepsilon]_{6\cdot2^m}.
}
\]

It has exact order

\[
2^{m+1}.
\]

Thus for every oriented native triad:

```text
C6  : half-turn; +/-3 coincide
C12 : actual OUTER midpoint Cell gives +/- quarter character
C24 : normalized same balanced Cell gives distinguished +/- spinor root
C48+: principal precision observer continues, but no rational two-component spinor exists
```

The existing quadratic-field obstruction is local-character algebraic and therefore applies uniformly once a local `J_S` is supplied.  It does not depend on FCC planarity.

Hence

\[
\boxed{
\text{C48+ REMAINS THE FIRST LEVEL WITHOUT A RATIONAL/INTEGER TWO-COMPONENT SPINOR REPRESENTATIVE FOR EVERY TRIADIC LOCAL CHARACTER.}
}
\]

## 9. Full S6 orientation/root bundle

There are 20 underlying three-axis selections and two cyclic orientations each.  Let

\[
\Omega_{20}
\]

be the already-derived 40-state orientation cover.

The triadic generator is equivariant on this oriented carrier: transporting the oriented ordered triad transports `Q_S`; reversing the orientation replaces it by `Q_S^{-1}`.

Therefore the principal root sheet, the C12 root Cell and the C24 normalized spinor are naturally defined over the **oriented** 40-state carrier.

There is no S6-equivariant section choosing one orientation on every 20-slice base, because a visible transposition fixes the underlying slice and reverses its orientation.

Thus the all-20 root structure is a bundle/cover object, not an untwisted scalar assignment on the 20 base slices.

This generalizes the earlier four-STAR cube root bundle.  On the four FCC STARs and the carrier-preserving `S4` subgroup, the 40-state orientation architecture restricts to the existing 8-state cube cover.

## 10. FCC interpretation is now an extra decoration, not the source of the native root path

The fixed-carrier determinant/adjacency census remains important:

- 4 STAR selections project to rank-two triangular FCC planes and recover the known circle/gate/bisector picture;
- 16 non-STAR selections project injectively as full-rank bases of the FCC parity lattice `D3` and do not support the same planar C6 carrier graph.

But the universal triadic theorem shows that the native C6/C12 **Cell/path** cycle exists before this carrier readout.

Therefore the correct type hierarchy is

\[
\boxed{
\text{NATIVE TRIADIC C6/C12 PATH ROOT}
\quad\text{is more general than}\quad
\text{PLANAR FCC STAR C6/C12 DECODER}.
}
\]

The four STARs do not create the root mechanism; they provide a particularly geometric classical carrier decoder for it.

This is an important retyping of the previous frontier.

## 11. BRC branch law: the root observer is not defined on the whole rotation-path groupoid without provenance

For each triadic macro edge there are two shortest native branches:

- INNER through zero;
- OUTER through `a_r+a_(r+1)`.

A full six-macrostep frame cycle therefore has

\[
2^6=64
\]

shortest branch words.

The universal rotation-path groupoid correctly retains these histories.

### Endpoint-only horizon

If the observer asks only for the macro frame endpoint `Q_S^6=1`, all 64 branch words have the same endpoint/frame result.  The branch word can be discarded for that weak horizon.

### One specified quarter-root horizon

For the start convention above, the half-turn root at microstep three lies on macro edge `r=1`.  It is nonzero/phase-refining iff that one branch is OUTER.

Exactly

\[
\boxed{32}
\]

of the 64 branch words satisfy this one-root condition.

### Full C12 nonzero midpoint horizon

If every macro edge is required to carry a nonzero intermediate root/phase state, every one of the six branch bits must be OUTER.

Hence exactly one branch word remains:

\[
\boxed{
(\mathrm O,\mathrm O,\mathrm O,\mathrm O,\mathrm O,\mathrm O).
}
\]

Thus the all-OUTER cycle is uniquely selected by the declared **full nonzero C12 phase-refinement horizon**, not by endpoint/frame data alone.

This is a precise BRC observer-dependent selection theorem.

## 12. Critical operation-safety boundary

The all-OUTER root-refining arrows are not promoted to a subgroupoid or a complete native dynamics law.

The parallel `ROT_PATH_X6` theorem already proves that shortest path fibers are not closed under rotation composition: a shortest `Q` path followed by a shortest `Q^-1` path yields a nonempty length-four loop although the shortest identity fiber has length zero.

Therefore

\[
\boxed{
\text{PRINCIPAL ROOT SECTION}
\neq
\text{OPERATION-SAFE QUOTIENT OF THE FULL ROTATION-PATH GROUPOID}.
}
\]

Actual dynamics must retain path history, a sufficient proved quotient, or an independently selected law.

The present theorem supplies a root/precision observer on the all-OUTER phase-refining subpopulation.

## 13. What is now closed for #1255 at relevant-scope strength

The original synchronous interpretation

\[
\text{physical trajectory time}\to\mathcal P_{\rm rot}
\]

remains refuted.

But the correctly retyped root-bundle problem is now globalized from four STARs to all twenty oriented triadic selections:

\[
\boxed{
\text{ORIENTED X6 TRIAD}
+\text{ ALL-OUTER ROOT-REFINING PATH LEASE}
\longrightarrow
\text{ACTUAL C12 ROOT CELL}
\longrightarrow
\text{C24 BALANCED SPINOR}
\longrightarrow
\text{PRINCIPAL }+/-3\text{ PRECISION LINEAGE}.
}
\]

This construction is:

- native signed-X6 at the C6/C12 path level;
- independent of FCC planarity;
- S6-covariant only on the oriented 40-state cover;
- compatible with the four-STAR cube/root-bundle restriction;
- explicitly branch/provenance typed;
- compatible with the C48+ rational-spinor obstruction.

This is a `SUCCESS` for the all-20 **triadic all-OUTER root observer/bundle**.

It is not a claim that every P000 native rotation trajectory follows the all-OUTER law, nor that all native rotations are generated by the triadic group.

## 14. New smallest frontier

After composing the parallel triadic result, the remaining Viète/rotation problem is no longer spatial existence of the first root path.

The hard problem is now **law selection and composition**:

1. derive or refute a native principle selecting the OUTER branch when the future observer is not stipulated in advance;
2. determine which quotient of `ROT_PATH_X6` retains enough loop/branch history to compose actual rotations while still supporting the principal root observer;
3. connect the triadic frame/path dynamics to P000 time without confusing time evolution with precision refinement;
4. determine whether the index-two complement of the triadic frame group contains additional admissible native rotation generators;
5. connect the same path law to the new triadic-force closure principle.

The spatial/component provenance chain itself is now sharp through C24 on all twenty oriented triads.
