# Heartbeat45: actual BRC-only recheck of the same finite loop input

Event-ID: brc-heartbeat-brc-only-loop-recheck-20260923-6EF011
Research-Activity-ID: RA-6EF011C2E75C4A799606AFEA
Researcher-ID: EM-DIRECT-6EF011
Status: SCOPED_RESEARCH_CANDIDATE / NOT_ADMITTED
Global read: 779e949dd90e7d85fd22f466afc02712fcc68dbb.
Source read: bc7babbb9e890f6d5a7094430a5fbdccf66c77ad.
Parent standalone: 77920ff71fbd8c3fbd50b6f181e592e4992735bf.

## Request and exact scope

The user requested one recalculation after prohibiting pi/trigonometric and non-BRC arithmetic routes. This unit actually recalculates the original finite two-plaquette static source/field and its signed relation-word response through the canonical positive BRC kernel. It does NOT recalculate the old physical-time t=1/4 field or the 144/284-state material trajectories. Relation depth and the recurrent discount are not physical time or heartbeat count. All old files and model-local evidence remain intact; native attribution of unvalidated old trajectories remains pending.

Executed unchanged source: src/enterprise_math/brc_weighted_recurrent.py at bc7babbb9e890f6d5a7094430a5fbdccf66c77ad, observed Git blob 4e6b3132580e3cd70a20a0d8bd4d28792b961afb. A local byte-for-byte copy has the identical Git blob. Calls execute finite_recurrent_mass_analysis, recurrent_mass_power and the canonical integer stability verifier. Foundation scope is definitions/ENTERPRISE_BRC_FINITE_RECURRENT_FOUNDATION_20260902.md: finite nonnegative rational total-mass recurrence, not signed/complex amplitudes.

The new native-input compiler and phase-character observer are separately declared scoped extensions, proved below. They are not claimed to be pre-existing canonical signed BRC or a general physical phase law. No pi, trigonometric, exponential, ordinary Taylor/Padé/Cayley evaluator or high-precision classical reference was used. Exact arithmetic is inside actual BRC kernels and their declared input/readout maps, not a renamed ordinary propagator.

## Identical graph and static source

The six native labels are v0=(0,0,0,0,0,0), v1=(0,1,0,0,0,0), v2=(1,0,0,0,0,0), v3=(1,1,0,0,0,0), v4=(2,0,0,0,0,0), v5=(2,1,0,0,0,0). Oriented edges, in the original order, are (0,2),(0,1),(1,3),(2,4),(2,3),(3,5),(4,5). D is outgoing-minus-incoming incidence, and C consists of the original two four-edge face circulations. Off-strip edges are absent under the same finite boundary convention, not an infinite-lattice approximation.

Input k=q*e0; source s=Dk=q(delta_v0-delta_v2). The remote observer is edge e3=(v2,v4). No angle or material trajectory is prescribed. The inherited quadratic field law remains an explicit constitutive candidate, not derived native force physics.

Fix any gauge phi_g=0. On the other vertices K put W_ij=1/degree(i) for adjacent retained vertices and zero otherwise, b_i=s_i/degree(i). Then phi=W phi+b is exactly the original node constraint. Split b into labelled positive/negative source channels. Every finite vertex reaches the grounded gauge vertex, so the positive BRC kernel is stable. The actual routine returns and verifies an integer certificate; its exact star computes both source channels. Reconstruct phi=W* b_positive-W* b_negative and j*=D^T phi. The omitted source equation is checked using sum s=0: the gauge is not a newly added physical sink.

For any compatible j, D(j-j*)=0 and <D^T phi,j-j*>=0. Hence j* is the unique quadratic minimizer and C(k)=C(j*)+C(k-j*). This is the precise finite-incidence-to-BRC correspondence used here.

## Actual same-input result

Gauge g=v2 gives retained order (v0,v1,v3,v4,v5). The BRC matrix has rows
(0,1/2,0,0,0), (1/2,0,1/2,0,0), (0,1/3,0,0,1/3), (0,0,0,0,1/2), (0,0,1/2,1/2,0).
The returned primitive integer stability certificate is (17,24,21,17,24).
For q=1, phi=(11/15,7/15,0,1/5,1/15,2/15),
j*=(11/15,4/15,4/15,-1/15,-1/5,1/15,-1/15),
h=k-j*=(4/15,-4/15,-4/15,1/15,1/5,-1/15,1/15).

At the SAME remote edge: minimum component -1/15, history component +1/15, complete initial field exactly0. Costs are11/30 and2/15, summing to1/2. Gauss defects, circulation defects and orthogonality are exactly zero. All six gauge choices agree.

For q=1/100000000 the minimum remote component is exactly -1/1500000000 and the history is its opposite; the complete local-update field remains zero. The four-loop same-boundary check returns minimum -1/209 and opposite history.

Thus the nonzero decomposition components survive actual BRC re-evaluation without classical-angle numerical error. They do not imply a nonzero complete remote change. Deleting the history is a change of state/update law, not an accuracy improvement.

## Phase-labelled branch extension and executed relation response

An explicit branch retains source, target, positive rational magnitude, phase in Z2 or Z4, and original provenance. Serial composition multiplies magnitudes, adds phase labels and concatenates words. Alternative recoalescence is performed only within endpoint/phase/depth fibers. Lift each state to (state,phase), then invoke the existing positive BRC module without changing it.

The declared final character is chi(M0,M1)=M0-M1 for Z2; for Z4 it is (M0-M2)+i(M1-M3), with i^2=-1 as an algebraic symbol, not an angle evaluation. Character preserves addition and phase convolution, proving correct signed readout for every finite word length. Endpoint/phase/depth is future-safe only for the frozen kernel and these observers; path-sensitive future operations require the original labelled graph/words, which are retained. This is an explicit new scoped observer extension, not a claim that positive mass itself supports interference.

The original edge/face graph has nine states and sixteen signed branches: edge->face coefficient C, face->edge coefficient -C. Its sign cover has eighteen positive BRC states. Actual BRC powers give remote (positive mass,negative mass,signed output): depths0..3=(0,0,0); depth4=(0,1,-1); depth5=(0,0,0); depth6=(8,0,8); depth7=(0,0,0); depth8=(0,49,-49). The unique depth-four word is edge0->face0->edge4->face1->edge3 with signs(+,-,-,-). No physical time function is evaluated from these coefficients.

For the separate all-depth recurrence with dimensionless per-branch discount z=1/4, the canonical integer stability test passes. Positive and negative remote masses are128/57057 and271/57057, yielding signed -1/399. Forgetting phase gives the different positive total +1/143. This is NOT the old physical-time t=1/4 readout.

Two equal positive-magnitude branch populations carrying i/3+i/3 versus i/3-i/3 have the same total mass2/3 but different exact characters2i/3 and0. All16 two-step Z4 phase composition cases are executed through the canonical BRC power routine. Arbitrary transcendental phases and a native time law are not thereby supplied.

## Verification and remaining work

Accepted invocation: python -S -m stage45.brc_loop_recheck. Sixty-two finite checks passed, with41 recorded direct canonical BRC power/star calls plus certificate-verifier calls. Tests include exact Gauss closure, complete history cancellation, all six gauges, tiny source scaling, sign reversal, four-loop static response, bounded branch-word reconstruction, all16 phase serial products, rejection of floating scientific inputs, and rejection of an unstable positive recurrent star. No symmetry/eigenvalue/high-precision library was used. The first environment guard observed automatically preloaded NumPy, which the scientific code did not call; -S removed that startup import for the accepted run. It is recorded as an environment issue, not a physical residual.

Full local proof, code, canonical vendor, exact outputs and execution receipt are in stage45. No independent researcher or Lean review, no physical instantaneous signal, no new macroscopic-size law, and no Shor performance claim. Current activity identity was revalidated via unchanged blob5955f25724c697adef07d2b5571e7bc2a8fdad37; no new formal task/run or acceptance is fabricated. Activity aggregate checkpoint integration is distinct from source publication.

Next: define and verify a BRC-native heartbeat transition from the same preparation and allowed controls, retaining phase and history. Until then the prior time-specific tiny differences remain NATIVE_ATTRIBUTION_PENDING_BRC_REVALIDATION. Do not fill this gap with a conventional propagator or label a branch discount as elapsed time.
