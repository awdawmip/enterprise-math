# Native Enterprise C3 CRT collapse tower: dimensions 2 through 19

Status: `FREE_RESEARCH_HIGH_DIMENSIONAL_COLLAPSE_CANDIDATE / EXACT_STRUCTURE + FINITE_PROBABLE_PRIME_EXPERIMENT / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-23`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Parent: `NATIVE_ENTERPRISE_C3_SHELL_RESIDUE_COLLAPSE_INVARIANT_20260823.md`

## 1. Why this is a different high-dimensional construction

The earlier orthogonal control used `N_0^d` with a sum-of-squares readout. This file does not introduce d Euclidean/orthogonal axes.

Instead, dimensions are generated by independent shell-residue collapse channels.

Let

`p_1=2, p_2=3, p_3=5, ...`

be the increasing primes and define the primorial shell modulus

`P_d = product_{i=1}^d p_i`.

Then the shell-residue collapse ring has the exact CRT decomposition

`Z/P_d Z ~= product_{i=1}^d F_{p_i}`.

Call `d` the **collapse-channel dimension**.

This is not claimed to be a canonical spatial dimension of Enterprise geometry. It is a native arithmetic high-dimensional coordinate generated by the shell-collapse invariant.

## 2. Primitive basin

A fully-prime C3 fiber must have shell residue coprime to the shell index.

At primorial shell `P_d`, the primitive basin is therefore

`U_d = (Z/P_d Z)^*`

with CRT form

`U_d ~= product_{i=1}^d F_{p_i}^*`.

Hence

`|U_d| = phi(P_d) = product_{i=1}^d (p_i-1)`.

The identity fiber is

`1_d = (1,1,...,1)`.

It is the high-dimensional image of the shell-unit identity and, for even shells, of the equal-coordinate midpoint fiber.

## 3. Downward collapse is canonical

Since

`P_{d-1} | P_d`,

there is a canonical reduction map

`pi_d : Z/P_d Z -> Z/P_{d-1} Z`.

Under CRT this simply forgets the last coordinate:

`(rho_1,...,rho_{d-1},rho_d) -> (rho_1,...,rho_{d-1})`.

The unit basin maps onto the lower unit basin, and every lower primitive state has exactly

`p_d-1`

primitive lifts.

The identity path is stable:

`pi_d(1_d)=1_{d-1}`.

Thus dimensions 2 through 19 form an exact downward-collapse tower rather than 18 unrelated coordinate systems.

## 4. Why the primorial tower is selected

Among squarefree shell moduli with d distinct prime channels, `P_d` is the unique smallest positive modulus because it uses the d smallest primes.

For a new shell prime `q>3`, absorbing q into the shell changes the local three-slot survivor fraction from

`(q-3)/q`

to

`(q-1)/q`,

with enhancement

`g(q)=(q-1)/(q-3)=1+2/(q-3)`.

This enhancement is strictly decreasing with q.

Therefore, after the mandatory 2 and 3 channels, choosing the smallest available primes simultaneously:

1. minimizes shell modulus at fixed collapse dimension;
2. maximizes the product of local resonance enhancements.

So the same primorial tower is selected by both minimum complexity and maximum local resonance.

## 5. Exact 2D-19D basin table

The frozen CSV records for every `d=2,...,19`:

- new prime channel `p_d`;
- primorial shell `P_d`;
- primitive basin size `phi(P_d)`;
- unit density `phi(P_d)/P_d`;
- cumulative local resonance gain from the added q>3 channels.

These structural columns are exact integer/rational consequences of the CRT tower.

Endpoint examples:

- d=2: `P_2=6`, `phi=2`, unit density `1/3`;
- d=4: `P_4=210`, `phi=48`, cumulative gain `3`;
- d=7: `P_7=510510`, `phi=92160`, cumulative gain about `5.142857`;
- d=12: `P_12=7420738134810`, `phi=1103619686400`, gain about `7.775372`;
- d=19: `P_19=7858321551080267055879090`, `phi=1018622055388670853120000`, unit density about `0.12962336`, gain about `10.324108`.

So increasing collapse dimension makes the primitive basin occupy a smaller fraction of the full residue space while increasing the local C3 resonance advantage.

## 6. Identity-fiber probable-prime experiment along the tower

For even shell r, the shell-unit identity fiber is

`M_-(r)=3r^2/2-r+1`,

`M_0(r)=3r^2/2+1`,

`M_+(r)=3r^2/2+r+1`.

At each exact primorial shell `r=P_d`, a three-bit mask records the result of the primality/probable-primality test on the three values. None of `d=2,...,19` has all three bits equal to 1 at the exact base primorial shell.

A separate finite search asks for the smallest multiplier

`1 <= k <= 20000`

such that all three identity-fiber values pass the research environment's primality test on

`r=P_d k`.

For values below `2^64`, the test used is deterministic. For larger values, the current SymPy `isprime` path uses strong BPSW and therefore has a small unproved pseudoprime possibility. Accordingly, the large-number hits in this section are classified as **probable-prime hits**, not certified primality theorems.

A hit was found inside this bound for every `d=2,...,19`.

First examples:

- d=2,3,4 all first hit the same shell `r=630` with multipliers `105,21,3` respectively;
- d=5 and d=6 first hit the same shell `r=90090` with multipliers `39,3`;
- d=7: first hit `k=306`, `r=156216060`;
- d=12: first probable-prime hit `k=85`, `r=630762741458850`;
- d=19: first probable-prime hit `k=12253`, `r=96288013965386512235686489770`.

These are bounded computational observations, not infinitude claims and not certified least-prime theorems for the large-number rows.

## 7. Dimensional persistence

If a shell `r` is divisible by `P_d`, it is automatically divisible by every `P_j` with `j<d`.

Therefore a bright identity fiber on a d-channel shell projects to the same identity fiber through every lower collapse dimension.

This explains repeated first-hit shells at low dimensions:

`630` simultaneously supports the d=2,3,4 primorial channels;

`90090` simultaneously supports the d=2,...,6 channels.

The high-dimensional object is therefore nested rather than re-sampled independently at each d.

## 8. Interpretation

The current native high-dimensional seed is

`2D tri-sector coordinate`

`-> C3 fold`

`-> shell residue rho mod r`

`-> shell unit basin`

`-> CRT decomposition into independent prime channels`

`-> primorial collapse tower X_19 -> ... -> X_2`.

This achieves a 2-to-19-dimensional exploration without importing a Euclidean orthogonal metric.

## 9. Boundary

CRT, primorials, unit groups and prime arithmetic progressions are classical. Prime arithmetic progressions with primorial-like common differences are also classical prior art.

The research-specific candidate is the derivation of this inverse CRT tower from the native Enterprise C3 shell-residue collapse, and its use as the high-dimensional coordinate rather than an externally imposed Cartesian space.

Current verdict:

`NATIVE_HIGH_DIMENSIONAL_SEED = CRT_COLLAPSE_CHANNEL_TOWER`.

Next hard test:

compare actual prime-fiber distributions under downward projections across dimensions, and determine whether any statistic survives projection that is not already determined by classical local sieve data.
