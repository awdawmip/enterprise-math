# X6 rotation V14: local phase-chirality D12 dynamics and exact predictive state

Status: `FREE_RESEARCH / EXACT LOCAL DYNAMICS / NOT_FOUNDATION`
Date: `2026-09-06`
Researcher: `EM-FREE-PI-PRIME-20260904 / FREE_AXIOM_DISCOVERY`
Tasks: `RS-X6-NATIVE-ROTATION-DYNAMICS`, `RS-X6-NATIVE-TIME-DYNAMICS`, `RS-X6-UPPER-STRUCTURE-INTEGRATION`
Depends on:
- `X6_ORI6_TRIADIC_PHASE_REVERSAL_V13_20260906.md`;
- `X6_ROTATION_PATH_GROUPOID_V2_20260906.md`;
- `X6_BRANCH_SELECTOR_CONTEXT_NOGO_V3_20260906.md`.
Checker: `experiments/x6_phase_chirality_d12_v14_20260906/check_phase_chirality_d12.py`.

## 1. Event semantics supplied by V13

Fix one local four-twist signed-C6 generator `Q` and its phase-reversal involutions. Let the six shell phases be indexed by

`r in Z/6Z`.

A local sweep chirality is

`epsilon in {+1,-1}`.

The chirality tells whether ordinary continuation uses `Q` or `Q^-1`.

The minimal local predictive state considered here is

`P=(r,epsilon)`.

This is an internal/relational phase state, not a spatial coordinate and not full Path-formal history.

## 2. Two event operations

Define two deterministic event operations.

### ADVANCE

Continue the current chirality by one shell macrostep:

`A(r,epsilon)=(r+epsilon,epsilon)`.

### PHASE_REVERSAL

Use the unique V13 pointed-edge odd event on the currently forward edge. It reaches the same next shell phase but reverses the subsequent sweep chirality:

`B(r,epsilon)=(r+epsilon,-epsilon)`.

The second formula follows because the V13 odd event is chosen to interchange the two shell phases on the active edge and conjugates the local generator to its inverse.

## 3. Same immediate phase, different future

For a fixed input `(r,epsilon)`, both event kinds have the same phase-only immediate output:

`phase(A(r,epsilon))=phase(B(r,epsilon))=r+epsilon`.

But their hidden chirality outputs are opposite.

One further ordinary advance separates them:

`A(A(r,epsilon))=(r+2epsilon,epsilon)`,

`A(B(r,epsilon))=(r,-epsilon)`.

Thus the event kind can be invisible in the immediate shell-phase observer while changing the deterministic future one event later.

This is an exact upper-layer witness that

`SAME CURRENT SPATIAL/PHASE TARGET != SAME RELATIONAL FUTURE`.

## 4. D12 relations

Direct calculation gives

`A^6=1`,

`B^2=1`,

`B A B = A^-1`.

Therefore the operations generate

`<A,B> ~= D_12`,

the dihedral group of order 12.

This is the same abstract local group obtained in V13 from one signed C6 generator and one compatible odd phase-reversal involution.

The present result is its exact action on the phase-chirality state.

## 5. The 12-state phase-chirality carrier is a D12 torsor

There are exactly

`6*2=12`

states `(r,epsilon)`.

The D12 action generated by A and B is transitive. Since the group also has order 12, the stabilizer of every state is trivial.

Hence

`PHASE_CHIRALITY_12`

is a regular D12 torsor.

This must not be confused with other 12-state objects already present in the project:

- the OUTER spatial 12-Cell microcycle;
- the SHELL/CLOSURE internal C12 event cycle;
- a precision/root C12 observer.

The present object has 12 states because it is `C6 phase x C2 chirality`; its operation group is dihedral, not cyclic C12.

## 6. Exact predictive minimality for {ADVANCE, PHASE_REVERSAL}

Consider the declared future language generated by the two operations A and B and an observer asking the exact future shell phase.

The full pair `(r,epsilon)` is sufficient.

It is also necessary:

- if two states have different r, the current phase observer already distinguishes them;
- if they have the same r but opposite epsilon, one ADVANCE sends them to different phases `r+1` and `r-1`.

Therefore every pair of distinct states is separated by the declared current/future language.

So the coarsest exact predictive quotient is the full 12-state set itself.

In particular:

`PHASE_ONLY_6 = NOT_OPERATION_SAFE`,

`CHIRALITY_ONLY_2 = NOT_OPERATION_SAFE`,

`PHASE_X_CHIRALITY_12 = EXACT_MINIMAL_PREDICTIVE_STATE`

for this restricted local event language.

This minimality is scope typed. Generic rotation Path/BRC provenance remains richer.

## 7. Local Ori6 charge is the D12/C6 quotient

The ordinary continuation subgroup is

`<A> ~= C6`.

It contains the charge-zero local phase rotations. The phase-reversal event B has `Ori6=1` by V13.

Hence the local event group has the exact quotient

`1 -> C6 -> D12 -> C2 -> 1`,

where the quotient bit records whether a group element belongs to the ordinary phase-rotation subgroup or the odd phase-reversal coset.

For an event word in A and B:

`local charge = (# occurrences of B) mod 2`.

Under the V13 frame realization this agrees with the restriction of the global V8 Ori6 event-charge ledger.

So the phase-reversal count is a one-bit operation-safe observer for the final orientation sector, while remaining insufficient for path or full frame reconstruction.

## 8. A second exact sequence

Projection of the signed local frame group to positive-axis labels gives the V13 sequence

`1 -> <Q^3> ~= C2 -> D12 -> S3 -> 1`.

Together the two quotients expose two distinct pieces of the same local D12 structure:

- `D12/C6 ~= C2`: odd-event / phase-reversal charge;
- `D12/C2 ~= S3`: unsigned three-axis permutation action.

Neither quotient alone recovers the full signed local dynamics.

## 9. Path realization remains above the 12-state Markov carrier

For a normal ADVANCE macro edge, the signed-X6 path fiber has INNER/OUTER shortest realizations and the chosen operation context determines which section/weight is valid.

For the V13 PHASE_REVERSAL event in triadic common-node context, the three-token joint shortest law is the unique common-pivot branch.

Therefore the 12-state phase-chirality carrier is a deterministic relation-state model after event laws are declared. It does not erase the distinction among concrete Path/BRC witnesses when future operations inspect them.

A frame/state cycle returning to the same `(r,epsilon)` may still have nonempty elapsed event/path history.

## 10. Current consequence

V13 and V14 together reduce the local Ori6-changing dynamics to a very small exact interface:

- state: `(phase r, chirality epsilon)`;
- charge-zero event: ADVANCE;
- charge-one event: PHASE_REVERSAL;
- event algebra: D12;
- exact local predictive state count: 12;
- concrete common-node path law for the charge-one event: supplied by V13;
- event-count/history and generic path provenance: remain separately typed.

The remaining physical question is not how to represent a reversal once declared. It is what higher-level condition, if any, triggers the PHASE_REVERSAL event rather than ADVANCE.

No Foundation admission, event rate or external novelty is claimed.
