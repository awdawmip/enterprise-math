# Two native owner tools: mathematical L4 integration candidate

This package prepares **mathematical-tool L4 integration** on observed main
`69d932107a696ce7d12bbb6d8fd0b55ddf1aac83` (tree
`245f563c4b2d42e118ade73885070d001e0e2278`). It is not a
`NO_NEW_MATHEMATICS` control-maintenance change. Copying an already reviewed
mathematical tool into main still requires the mathematical L4 channel.
The payload transports unchanged frozen mathematics: NO NEW MATHEMATICS is
introduced during L4 replay. This transport statement does not classify the
payload as control maintenance. Candidate validation does not itself perform
main admission, formal Result/review, Working Truth or Foundation transition.

## Exact source and selected scope

The immutable research publication is
[`136682aed87e8ce709d3d0ddb8c2041f1468b8ea`](https://github.com/awdawmip/enterprise-math/tree/136682aed87e8ce709d3d0ddb8c2041f1468b8ea),
tree `4abbd2c0be827e29e764cb46349d6166a542de98`. Its equivalent local source
commit is `8c546c2770977ccb26e7ac9ff7d7d70a1d135a99`. The preceding shell
publication is `514cd10eba2e29034052295d4c3fece50c491041`.

The same-named dated inventory shard contains exactly these two original
method objects, copied without field changes:

- `candidate.x6.quadratic_shell_max_shortest_length` — six existing APIs.
- `candidate.x6.one_positive_raw_stability_certificate` — existing `audit_case`.

The source shard has nineteen objects. Its other seventeen objects are not
available through this integration. Only the selected shard's top-level
provenance and availability were rebuilt. Later source integration must align
objects by `method_id`, preserving the loader's duplicate-ID rejection and
without replacing this two-item validation with a whole-source availability
claim. Catalog presence does not confer formal acceptance.

Twenty source, paper, consumer and independent-review files are copied byte
for byte from the public source. Their exact path/hash manifest lives in the
selected shard's `provenance.frozen_source_files_sha256`. All eighteen selected
source/validation references are included. The two extra closure files are
the historical shell selected-case JSON and the earlier raw X6 stability
contract. The twelve one-positive source pins are checked; nine dependencies
already exist in the observed main and the three papers are included here.
Existing native coordinates, canonical arithmetic facade, policy and package
initializers are not changed.

## Replaying this exact candidate

From the repository root, with Python 3.12 and normal assertions:

```text
python -X utf8 -B experiments/owner_native_tools_l4_20260908/validate_integration.py
```

The validator requires the public source commit and public historical shell
baseline `5eddb6ea23761d928197b3d8227a5b42a7975a34` in the local Git object
store. That baseline's tree is `8c0a6c8d0383b3063ae97d00be069db1613c726e`.
The original shell independent reviewer reads that exact historical helper
with `git show`; it compares only its existing N=60 and N=61 endpoint cases.
A shallow checkout must first obtain these exact published objects through
the repository's authorized Git transport. The validator does not fetch.
Missing historical Git objects are an explicit replay prerequisite, not a
claim that the published mathematical resource is absent or the product is
broken.

Older paper commands may also reference historical files deliberately left
outside the minimal closure, including `signed_N25.json`, the old standalone
shell audit and the broad stability frontier. Read those at the immutable
source URL above. Historical paper hashes refer to their stated earlier
inputs; they are retained as history, not rewritten to denote current code.
The source branch retains the full directory and history.

The integration validator reads the current two-entry shard, checks its exact
objects against the public source, all twenty copied hashes, eighteen refs,
twelve one-positive dependencies and the shell's native dependencies. It then
makes an isolated TEMP copy using the same canonical Python package. It
actually loads both entries exactly once through the existing toolbox,
checks their query hits and seven callable APIs, and runs:

- The selected static V2 gate on the two consumers, both independent reviewers,
  the shell runtime validator and this integration validator.
- The original shell consumer regression with its sixty-one selected cases
  and trace checks; its regenerated JSON must equal the original stored bytes.
- The existing bounded shell independent review.
- The original one-positive consumer's default recompute-and-compare entry.
- Carver's existing bounded independent one-positive review.

The original source JSON files in this checkout are never overwritten.
Independent reviewers' timestamped output is collected only in TEMP. The
separate `experiments/owner_native_tools_l4_20260908/receipt.json` records actual
commands, exit codes, input hashes, TEMP artifact/log hashes, and the unchanged
input check. It is the execution result; this note does not substitute old
nineteen- or eighteen-entry catalog results for the selected two-entry replay.

## Arithmetic and mathematical boundaries

The original `validate_selected_cases.py` imports `Fraction` and constructs
`Fraction(1)` and `1.0` exclusively as invalid-input rejection probes. It is
retained unchanged as a typed historical compatibility test, outside the
selected native static-gate claim. Its integer positive cases still execute
the migrated shell implementation. The current shell consumer materializes
division, remainder and roots through the existing BRC facade with traces;
the old direct-division multiplicity routines remain excluded from that
governed certificate.

One-positive masses are positive integer numerator populations with a shared
unevaluated canonical `DIV(1,s)`. The signed difference is analysis between
those positive BRC populations, never a negative native branch. Raw all20
addresses, or their lossless joint can3 and common offset, are retained.
The source consumer does not request quotient or root readout and therefore
does not fabricate collapse traces. Historical canonical package initialization
is preserved and is not claimed migrated. Static success plus finite call
observations does not prove transitive arithmetic compliance.

The original proofs, APIs, output contracts, limited mathematical scopes and
equality conditions are unchanged. Finite replay does not prove all-input
theorems, general p<=7 optimality or global novelty. No new mathematical or
arithmetic implementation is added by this integration validator.

## Admission remains separate

This candidate starts from the actual main merge of control PR #1380,
identified above. The earlier local preparation remains at
`3ba312c6320142459d33c66415e894943483388c`. Its receipt is retained there;
the receipt in this candidate records a new actual replay on the stated
control/main combination. All twenty original source files and the two
selected method objects remain exact copies of the public owner source.

`--base-commit <exact-current-main>` checks ancestry and records the
actual checkout head; it does not establish remote-main freshness or
promotion authority. The owner must complete applicable final CI and
one fresh current-main/conflict/expected-head admission pass. The
validator itself performs no repository-wide suite, commit, push or
merge. A later moving main requires an explicit combination assessment.

Global-Knowledge-Sync: main@982a440 / GLOBAL_KNOWLEDGE_V1
