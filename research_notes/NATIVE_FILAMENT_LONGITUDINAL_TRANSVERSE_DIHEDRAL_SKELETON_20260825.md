# Native filament longitudinal/transverse dihedral skeleton

Status: `FREE_RESEARCH_EXACT_GROUP_ACTION_SYNTHESIS / CLASSICAL_DIHEDRAL_COMPONENT / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-25`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Depends on the intrinsic hyperbola quotient and lane-label Joukowski theorems.

## 1. One carrier parameter

On the normalized odd-sector split hyperbola

`s*a*b=-1`,

use `a in F_q^*` as the carrier parameter, so

`b=-1/(s*a)`.

Assume `q` odd and `q` does not divide `s`.

Two natural quotient involutions arise from the two arithmetic readouts.

## 2. Longitudinal reflection

Exchanging the two tangent-difference coordinates

`(a,b)->(b,a)`

acts on the parameter by

`L_s(a)=-1/(s*a)`.

It is an involution:

`L_s^2=id`.

The longitudinal common-dual-value quotient is invariant under this exchange, together with the separate sign reversal `a->-a`.

## 3. Transverse Joukowski reflection

The transverse lane-label map is

`Lambda_s(a)=-s*(a+1/(2s*a))`.

Its two generic preimages are exchanged by

`T_s(a)=1/(2s*a)`.

Again

`T_s^2=id`.

Thus the transverse lane quotient supplies a second reflection on the same carrier.

## 4. Universal product

The dependence on `s` disappears when the two reflections are composed:

`L_s(T_s(a))=-2a`,

`T_s(L_s(a))=-a/2`.

Therefore the rotation generated by the two reflection quotients is

`rho(a)=-2a`.

Freeze:

`LONGITUDINAL REFLECTION * TRANSVERSE REFLECTION = UNIVERSAL SCALING BY -2`.

This relation is independent of sector count.

## 5. Dihedral group

Let

`d_q=ord_q(-2)`

be the multiplicative order of `-2` in `F_q^*`.

The group generated by `L_s,T_s` is a dihedral action with rotation subgroup

`<rho> = < -2 >`

of order `d_q`.

Abstractly, after quotienting any action kernel, the generated group has the standard presentation

`<L,T | L^2=T^2=1, (LT)^d_q=1>`.

Thus the two arithmetic quotients are two reflection quotients inside one dihedral skeleton.

The group-theoretic fact that two involutions generate a dihedral group is classical; the research-specific point is the geometry-selected formulas for these two involutions and the universal product `-2`.

## 6. Native q=5 and q=7

For q=5:

`ord_5(-2)=4=q-1`.

For q=7:

`ord_7(-2)=6=q-1`.

Therefore the rotation subgroup alone is transitive on `F_q^*` in both native extra saturation characteristics.

This is compatible with the fact that all nonzero transverse parameters belong to one connected orbit under alternating use of the two quotient reflections.

However, transitivity is not sufficient for transverse saturation in larger characteristics; for example `-2` can also be primitive for primes where the lane-label image is larger than the three native lanes.

Thus the dihedral skeleton explains carrier connectivity between the two quotient structures, while the Joukowski image-size theorem supplies the decisive saturation bound.

## 7. Native q=13 counter-control

At q=13,

`ord_13(-2)=12=q-1`,

so the same dihedral rotation is transitive on all nonzero parameters.

Nevertheless for s=3 the transverse Joukowski image has size

`(13-1)/2=6>3`,

so C3 saturation fails.

This control proves:

`DIHEDRAL TRANSITIVITY != TRANSVERSE SATURATION`.

The correct native 5/7 mechanism requires both:

1. connected/transitive carrier dynamics under the quotient reflections;
2. extremal collapse of the transverse Joukowski image into exactly the three native lane labels.

## 8. Relation to orientation reversal

The native presentation reversal acts as

`a->-a`.

It conjugates the transverse lane label

`Lambda_s(a)->-Lambda_s(a)`,

swapping the two outer lanes and preserving the central unordered lane.

Because `-1` commutes with the rotation `-2`, the orientation-reversal symmetry is compatible with the dihedral carrier action.

## 9. Prior-art boundary

Dihedral groups generated by two involutions, multiplicative orders and Joukowski inversion symmetries are classical.

No novelty is claimed for that group theory.

The retained research structure is the exact emergence of the two reflections from independently derived longitudinal and transverse native readouts, with sector-independent product `-2`.