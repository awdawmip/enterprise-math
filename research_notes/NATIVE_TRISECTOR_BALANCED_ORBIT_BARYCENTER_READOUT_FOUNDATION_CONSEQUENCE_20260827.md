# Native Tri-sector Balanced-Orbit Barycenter Readout — Foundation Consequence Node

Status: `FOUNDATION_CONSEQUENCE_ADMITTED / N2_READOUT_ONLY / P0_P1_PRIMITIVES_UNCHANGED / WHOLE_COMPARATOR_THEOREM_UNCHANGED`

Date: `2026-08-27`

Admission authority:

`driver_reviews/NATIVE_TRISECTOR_INVARIANT_READOUT_FOUNDATION_DERIVATION_DRIVER_REVIEW_20260827.md`

Source task:

`RS-NATIVE-TRISECTOR-INVARIANT-READOUT-FOUNDATION-DERIVATION-INTEGRATION-AUDIT`

Source result:

`RR-DDF27FE0D58FF6F9B8E4`

## 1. Semantic status

This node records a theorem **of an explicit N2 readout constructed from current P0/P1 and the accepted allocation torsor**.

It does not add or modify any P0/P1 primitive. In particular, it does not create:

- a distinguished physical axis, lane, rail, start ray, or frame;
- an N0 breaker object;
- an arbitrary odd-`s` native geometry;
- a primitive capacity law.

The scalarization below is choice-independent and fully defined from the accepted torsor, but it is not claimed to be the unique possible scalarization. The ceiling operation is part of the declared N2 readout definition, not an ontology claim.

## 2. Balanced-orbit barycenter readout

For the native shell

\[
A_r=\{(a,b,c)\in\mathbb N_0^3:\min(a,b,c)=0,\ a+b+c=r\},
\qquad |A_r|=3r,
\]

let the accepted six serializations be the `S_3`-torsor `lambda_f`, with shell start

\[
C_r=1+\frac{3r(r-1)}2.
\]

For `r>=2`, define the invariant set of distinct most-balanced states

\[
O_r=S_3\cdot(\lceil r/2\rceil,\lfloor r/2\rfloor,0).
\]

The multiset

\[
L_r=\{\lambda_f(x):x\in O_r\}
\]

is independent of `f`. Define the explicit N2 readout

\[
Z(r)=\left\lceil \operatorname{mean}(L_r)\right\rceil .
\]

Then

\[
\boxed{Z(r)=1+\frac{3r^2+\epsilon(r)}2},
\qquad
\epsilon(r)=r\bmod 2.
\]

For `r=2m`, the orbit positions are `m,3m,5m`; for `r=2m+1`, they are

`m,m+1,3m+1,3m+2,5m+2,5m+3`.

Both have mean position `3r/2`. The boundary `r=1` is excluded exactly.

## 3. Rebuilt breaker and capacity readouts

For odd prime `q` with `q∤6`, define the two translated branch images

\[
I_0(q)=\{-3x^2/2:x\in\mathbb F_q\},
\qquad
I_1(q)=\{-3x^2/2-1/2:x\in\mathbb F_q\}.
\]

Define the N2 predicate

\[
\operatorname{Break}(q)
\iff I_0(q)\cup I_1(q)=\mathbb F_q.
\]

The accepted split-hyperbola/Klein-four quotient theorem applies to this same two-branch covering predicate. A breaker requires one `K_4` orbit on a hyperbola of `q-1` points, hence `q-1<=4`. Under `q` odd and `q∤6`, only `q=5` remains, and direct evaluation gives

\[
I_0(5)=\{0,1,4\},
\qquad
I_1(5)=\{1,2,3\}.
\]

Thus the unique nonsingular odd breaker characteristic of this readout is

\[
\boxed{q_b=5}.
\]

For the translated family

\[
Z_H(r)=H+\frac{3r^2+\epsilon(r)}2,
\]

define `k_*(q)` as the sharp maximum consecutive `q`-coprime run over the breaker phase. The accepted quadratic-ramification theorem gives

\[
k_*(q)=2q-1.
\]

Therefore

\[
\boxed{k_*=9}
\]

at `q_b=5`. This remains distinct from the separate typed-Cell prime-incidence island cap `9`.

## 4. Two native unordered `{5,7}` readouts

For even shell `r=2m`, the accepted invariant balance packet is

\[
P_m=\{6m^2-2m+1,\ 6m^2+1,\ 6m^2+2m+1\}.
\]

For odd prime `q` with `q∤6`, let `Sat(q)` mean that the union of the roots of the three unordered packet members is all of `F_q^*`. Three nonzero quadratics contribute at most six roots, so `q-1<=6`. Direct root partitions give

- modulo `5`: `{1}|{2,3}|{4}`;
- modulo `7`: `{2,3}|{1,6}|{4,5}`.

Hence

\[
\boxed{\{q:Sat(q)\}=\{5,7\}}.
\]

Independently, for odd `k>=5`, on `W_k={0,...,k-1}` choose distinct `u,v,w` with `u≡v (mod 2)` and `w` of the opposite parity. Then

\[
\max |w-u||w-v|=(k-2)(k-4),
\]

and every maximizing unordered distance pair is

\[
\{k-4,k-2\}.
\]

At `k=9`, this second N2 set readout is also `{5,7}`.

## 5. Native scalar consequence

Using the current Foundation input `s=3`, never reverse-deriving it, the admitted N2 consequence is

\[
3\longrightarrow(5,7)\longrightarrow9
\longrightarrow35\longrightarrow105\longrightarrow53,
\]

where

\[
35=(9-4)(9-2),\qquad
105=3\cdot35,\qquad
106=2\cdot53.
\]

Typed guards remain binding:

- `5` is the readout breaker characteristic;
- `7` is the second saturation/boundary characteristic, not a breaker;
- `9` is breaker-coprime capacity, not the independent island cap;
- `105` is an exact equality, not a genealogy claim;
- `53` is a local terminal factor, not a global breaker.

## 6. What remains research-layer

The following are not Foundation-generated by this node:

- the controlled arbitrary odd-`s` comparator family;
- uniqueness of `s=3` within that full comparator family;
- named physical longitudinal/transverse/rail ontology;
- wholesale reclassification of `NATIVE_TRISECTOR_COUPLED_CLOSURE_THEOREM`.

The existing full theorem remains:

`AUDITED_RESEARCH_THEOREM / MODEL_SPECIFIC_SELECTION_THEOREM / DRIVER_ADMITTED`.

Foundation primitives and the current Foundation router remain unchanged.
