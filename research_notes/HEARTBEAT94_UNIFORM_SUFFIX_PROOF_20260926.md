# Heartbeat94 — 无碰撞后缀、精确均匀读数与观察范围内的误差预算

Progress-Event-ID: HEARTBEAT94-UNIFORM-SUFFIX-PROOF-20260926
Status: AUTHOR_SYMBOLIC_DERIVATION / SHARED_CONTEXT / UNREVIEWED / NOT_ADMITTED
Global read: 61e00d2a2a3ab41f3ec43990385f46e8a7806d41
Source read: e9abf794ac04c62518a38dea6640c4472cb4490a
Publication preflight: c0bc2ac62dad44f3285147e88b9a400ab237dc79；相对读快照仅增加别人的活动回执，不改动它们。
Parent cumulative, as recorded by Stage93: b8b53fb56128337b9b200b3f0971615794483720
Parent Source: 2f57aef7a3e381572becad8fc84cd4c39f975223:research_notes/HEARTBEAT93_COLLISION_FIRST_ROW_REUSE_20260926.md
Local conversation provenance retained: HB90-0de6b47ad07848a1b6e8347fd5156ca6；这不是服务或平台认证身份。
Research-Activity-ID: 未取得；REGISTER_PENDING。没有借用他人的session/CLAIM，没有重放历史报告的登记拒绝。本文是授权母题内的中立数学保全，不是正式Result或独立审阅。

## 0. 本轮确实做了什么，没做什么

完整读取本对话上传的Stage93证明及交付记录开头，复用既有真实BRC整轮恒等式、按需模乘、完整标签观察和终端表达。当前全局入口/项目路由/P000/BRC-only/残差保真规范的未变内容通过blob及差异核对复用。P000六原生空间轴与独立时间不变；内部维数、工作标签、程序步数不是新增空间轴或物理心跳。

本轮container.exec与python.exec均返回ClientError，未取得本地运行回执。没有调用新的科学BRC计算，没有重放12个Shor实例，没有新建可验证的累计bundle，也没有重新核验父bundle字节。本页是基于母线BRC接口的代数证明增量及待执行证书规范；LOCAL_EXECUTION_UNAVAILABLE / NEW_REGRESSION_NOT_RUN。读/写GitHub不等于科学执行。

母线每轮K_r=(I+(-1)^r U)/2、工作置换的来源标签与双射、内部T的完整正交性作为已证明接口真正复用。本文不改门、精度、标准QFT参照、二次概率、CF-only、随机源或失败规则；不把正BRC质量当振幅，不作新的三角/π/数值参考运算。

## 1. 已有一轮引理与本次问题

合法边界场v=sum_y |y> v_y，m=||v||²>0；所有内部分量及必要参考标签保留。完整内部字T不改变工作标签，M_sigma为已认证双射。实际孩子：

    v_r = (v + (-1)^r (M_sigma tensor T)v)/2.

如果S=supp_work(v)与sigma(S)不交，两个输入臂正交，所以两个孩子的质量都是m/2，支持都精确为S union sigma(S)。没有用不同内部方向相消来判断零；不同工作标签本来就是不同完整端点。

这是Stage92/93已有的一步事实。本页研究更强的问题：如何在不求出全部未来内部相位行的情况下，证明整段终端读取都均匀；是否能把这种不可见性用于收紧终端误差，而不删除完整后态。

## 2. 定理A：全报告支撑包络不交，则整段读数条件独立且公平

固定已有历史h，后续还有ell轮。每轮j的工作双射sigma_j预先由输入及原调度确定，不依赖尚未出现的报告。内部T_(j,h beta_<j)允许依赖真实旧报告，可以彼此不交换；要求是完整保内积、且不改变工作标签。对正混合或附带参考系统，按同一接口线性扩展。

给定真实当前非零支持E_0=S，定义不需要内部相位数值的包络：

    E_(j+1) = E_j union sigma_j(E_j),  0<=j<ell.

假设对所有j都有

    E_j intersect sigma_j(E_j) = empty.                 (A1)

则对每个长度j的报告词beta，真实状态支持恰为E_j，质量为m/2^j。因此每个长度ell的报告词都有

    Pr(beta | h) = 2^(-ell).                            (A2)

证明：j=0显然。归纳中，报告不同可以改变内部行，但每行非零性由正交T保持，工作两臂由(A1)不交，故不会在同一工作端点相消。一轮引理给支持E_(j+1)及质量减半。归纳同时覆盖全部报告，不仅覆盖一条样本。条件概率连乘得到(A2)。零质量父历史不作条件化，作为零项保留。

(A2)是分布结论，不是某个确定输出串。公平位仍来自原明确借用的随机接口。也没有断言读后场相同：实际内部字、符号和报告历史可以继续使后态不同。

## 3. 循环模乘版本与必要性范围

对于Shor最后ell轮，sigma_j=M_(a^(2^(ell-1-j)))。记M_a为原带padding恒等的全域工作置换。二进制路径x的总工作指数是

    e(x)=sum_(j=0..ell-1) x_j 2^(ell-1-j),  0<=e<2^ell.

如果2^ell个集合

    M_a^e S,   0<=e<2^ell                               (C1)

两两不交，则(A1)成立；反之，逐层(A1)也保证(C1)。可按二进制奇偶划分归纳：第j层的指数集合是{q*2^(ell-j):0<=q<2^j}，下一次模乘加入与之交错的指数。

还可直接展开实际完整后态。对固定报告beta：

    v_beta = 2^(-ell) sum_(x,y)
                 (-1)^(sum_j beta_j x_j)
                 |a^e(x) y> W_(x,beta) v_y,

其中W_(x,beta)=T_(ell-1,h beta_<ell-1)^(x_(ell-1)) ... T_(0,h)^(x_0)，严格保留原时间顺序，不交换因子。由(C1)及M_a双射，所有不同(x,y)对应不同工作端点；每个W正交。因此平方总和为2^ell*m/4^ell=m/2^ell。

在“允许任意正交内部T（含全部恒等），要求对所有支持于S的合法输入都成立”的普适范围内，(C1)还是必要的。若存在碰撞，取所有内部T=I及未归一化输入sum_(y in S)|y>e0。全零报告的展开全为同号；至少一个重合端点产生严格正交叉项，故其概率严格大于2^(-ell)。这不是说(C1)失败就证明当前固定门的实际读数不均匀；特殊相位相消仍可能使实际分布均匀。未取得证书时只能返回UNCERTIFIED，不能判模型错误。

## 4. 定理B：阶的二进赋值给出标准初态的均匀终端块

令真实模阶r=ord_N(a)=2^s d，d奇；r和s仅是证明变量，不能作为未说明的执行器输入。总控制位数t；已运行p=t-ell轮，其中0<=ell<=min(s,t)。从标准工作初态|1>出发，已使用的模乘指数均为2^ell的倍数，故每份正权前缀的工作支持包含于

    H_ell = <a^(2^ell)> subset <a>.

因为2^ell整除r，H_ell在循环群<a>中的指数恰为2^ell。集合a^e H_ell（0<=e<2^ell）两两不交。具体地，若a^e h=a^f h'，则e-f同余于2^ell*q模r，从而2^ell整除e-f；|e-f|<2^ell迫使e=f。

所以每个当前S subset H_ell满足(C1)，定理A适用。以母线低位先读取的整数编码：x为已经取得的p个低位，q为尚余ell位的整数值，k=x+2^p q。准确完整控制律满足

    P(k=x+2^p q) = P_prefix(x)/2^ell,
    0<=x<2^p, 0<=q<2^ell.                              (B1)

这证明最后ell个报告位联合均匀，且与此前报告独立。它们是整数k的高位；不是把高低位含义交换了。

该结论无需内部相位接近理想值，只需保留真实完整正交作用及既定工作标签语义。在本线实际even t<=10和冻结bank之外，必须另有合格bank/接口，不能凭此自动升级程序位宽覆盖。

### 4.1 纯二幂阶的加强结论

若r=2^s、t>=s，前t-s轮的M_i在轨道上为I。由历史初始为空且原反馈仅由旧报告1激活，归纳可得这些报告全为0，T均为I，状态保持|1>e0。定理B给最后s位全部公平独立，因此

    P(k)=1/2^s  if 2^(t-s) divides k,
         0      otherwise.                            (B2)

即完整控制峰严格等权，而不只是某一决定性位或成功事件的总质量正确。具体相位仍可改变工作—内部联合后态；(B2)不宣称完整态与理想QFT一致。

并行来源next_cf_only/POWER_OF_TWO_ANALYSIS.md（本次读取e9abf794...，blob5f3a09c60bd531da023a49fdc5aaee2552cb1a14）已证明同一宏式下的强制零前缀及好底数CF成功概率1/2，本文不重复认领。其第5节关于近似相位可能改变纯二幂阶的高位图样概率的表述，在本文明确的同一工作保持/历史控制假设下应收窄为“可能改变联合后态”；(B2)给出更强终端控制律。该源文件保持原样，本文作为可核查的追加澄清，不代作独立审阅。

### 4.2 只依靠此群支撑保证的最大范围

若ell>s，H_ell=<a^(2^s)>，其指数仅为2^s。a^0 H_ell与a^(2^s) H_ell相同，(C1)不成立。取H_ell上的同号均匀工作向量及全部T=I，前ell-s个报告被迫为0，其后s位才公平。全零报告概率为2^(-s)，不是2^(-ell)。因此不能仅从群支撑结构统一承诺更长的均匀后缀。某个特殊实际状态或相位仍可能额外公平，不在这个最大性主张内。

## 5. 不把未知阶交给编译器：有限支撑证书

可以从Stage93已有的真实当前支持S开始，仅调用原DemandPermutation的带来源单列规则，依次构造E_j和sigma_j(E_j)，核对(A1)。不读取r、因数、目标概率或后态相位数值。这是一份尚未实现并回归的新证书接口，不是已执行功能。

伪代码合同：

    E := exact nonzero work support of current parent
    for j in 0..ell-1:
        image := certified sigma_(p+j)(E), preserving each source label
        verify cardinality(image)=cardinality(E) from existing bijection contract
        if E intersects image:
            return UNCERTIFIED with first collision witness
        store replayable label proof
        E := E union image
    return CERTIFIED_UNIFORM_TERMINAL_SUFFIX

首次输入支持必须真实/精确；不能把尚未缓存或尚未求值的行当零。缓存政策不是数学支撑。返回UNCERTIFIED时继续原Stage93流程，不改变读数或后处理。

证书最直接的展开在通过时访问|S|(2^ell-1)个层源标签，最后支持大小2^ell|S|；可能有指数或约N的标签成本。它避免的是内部相位行求值，不是免费消除全部工作支持。若要更紧凑的陪集/着色证书，须另行构造和实际验证，不能把事后已知的s作为免费捷径。

代数说明而非新实验：N15,a2,t4的已知00前缀支持{1}；剩余乘数4、2给E0={1}，image0={4}；E1={1,4}，image1={2,8}。两步不交，推出四个控制输出0、4、8、12各1/4。保持原CF时4、12给非平凡因数，0仍是重试，不能因为后缀均匀就把这些高位固定为0或从k中删除。这里没有新运行或源查阶。

## 6. 完整后态必须继续可恢复；同随机带不等于同抽样API

通过证书且仅请求完整控制串及原CF/gcd时，可以不求剩余内部T的数值，逐位取得公平结果并保存它们。完整后态以本次父场、真实历史、每步原门序、每个已选结果所定义的嵌套K表达保留。需要工作/内部观察或继续相干操作时，再恢复相应实际关系，不把T设为I冒充原后态。

要逐事件复现原外部随机带，仍按原每位choose(1/2)的语义请求随机量。改成一次uniform(0,2^ell)虽有相同分布，却可能改变固定PRNG种子的路径，必须另说明随机接口；本文不声称二者逐seed相同。中途随机源耗尽，保留已取得的后缀报告及剩余表达，绝不重抽已发生结果。

全树只保存前缀权重及均匀块规则可紧凑描述终端分布，但显式列出全部2^t个概率仍需相应输出体积。CF必须使用原完整t位k；均匀并不表示这些随机位对因数提取无用。

## 7. 定理C：终端误差可以只计算不透明的前缀

比较两套具有相同N,a,t、标准初始工作支持和工作置换语义的完整正交内部实现。取同一个ell<=nu_2(r)，p=t-ell。两套实现都满足(B1)，故

    TV(P_terminal,Q_terminal)
      = (1/2) sum_x sum_q |P_prefix(x)/2^ell-Q_prefix(x)/2^ell|
      = TV(P_prefix,Q_prefix).                         (C2)

此式允许两套内部实现的末ell轮相位任意不同，甚至不接近：末段差异不进入这个控制终端观察。但工作/内部联合态、额外读取、相干回访、稀有事件条件化都不受“零差异”保护。

若实际相位m相对相同载体上的目标扩展有完整算子误差epsilon_m，则前p轮最多出现p-m+1次该相位。沿既有门级望远镜/仪器收缩接口：

    TV(P_terminal,P_ideal)
      <= sum_(m=2..p) (p-m+1) epsilon_m,               (C3)

空和为0，精确相位2的epsilon_2=0。前缀p可以为奇数；这只是偶宽已认证流式电路的前缀，不是新建一个奇宽原Shor准备。共同根相位3必须仍使用其真实替代误差，不能偷换成原phase3常数。

这比对全部t轮机械累加更细，但依赖本例的合法支撑/阶条件；本轮没有计算新的数值epsilon表，未用未验证常数替换父证书3699/68719476736。纯二幂阶时更强的(B2)直接给控制分布精确一致，仍不证明完整相位实现正确。

同一个确定性CF/gcd后处理不能增加未条件化总变差。不得删除均匀后缀或改动CF来利用(C3)。此结论也不解决奇部增大时的全部位宽分布逼近问题，不推翻其他固定截断模型的分布边界。

## 8. 对准确性验收的具体影响

N15等纯二幂阶控制直方图只能检验对应的工作/准备/读取结构，无法单独认证这些内部反馈相位。包含奇部的N21等输入可检验其前缀；但同样有终端后缀对内部相位不敏感。相位门本身或联合后态的认证仍需既有完整列/后态测试，不能由终端直方图替代。

本轮加强的是母线真实BRC接口的代数保证，不是从残差导出新的Born定律，也不是一般量子速度优势。一般半经典QFT已有Griffiths/Niu quant-ph/9511007；Shor模乘/阶的背景参照IBM Quantum Learning的Shor课程。只读取官方摘要和HTML，不宣称全文新颖性审查。

## 9. 待执行支持单元与不重做项

本轮新增科学核心调用0、新Shor回归0；不把继承Stage93的515项检查计成本轮成果。container.exec和python.exec当前均ClientError；未取得可验证的新sandbox产物。远端本文若写入并回读，只证明文本保全，不证明实验通过。

下一最小执行单元：在不改Stage93原运行器的独立适配层实现第5节证书与多轮SelectedRecipe；先按来源核验母包，再对12个冻结输入逐前缀判定，检验每个已认证末块全部词权重m/2^ell；强制恢复全部后态与原完整振幅/分母/尾量比较。记录标签证书成本、确实免于终端前展开的根字行、恢复追加成本；保留失败、随机耗尽与同seed边界。另在相同支持下换用已认证的不同正交内部字，核验纯二幂终端盲区及非二幂对照，不能加入新理想数值传播器。

不重做：Stage93碰撞优先、整行共享、按需置换、原CF后处理的已验证本体。不得为保持“每轮新计数”而重复宣称发现一步gamma公式。当前末块证明已完成；执行适配及独立审查尚未完成，父研究目标未整体闭合。
