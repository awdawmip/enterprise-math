# Heartbeat: rotating fixed-length segments and a nonprivileged starting endpoint

Event-ID: brc-heartbeat-endpoint-residual-20260922-6EF011
Research-Activity-ID: RA-6EF011C2E75C4A799606AFEA
Status: CONJECTURE_EXPLORATION_WITH_EXACT_TOY_IDENTITIES; NOT ADMITTED
Global read:90325d8187730b5a4a3345e518750f84d7b871ac. Project read:8c06cf22e54e777f140c0da1d75fee1822179e35.

## User direction

The user asks to follow conjectures and seek contradictions rather than require their conclusion in advance. Specifically: who carries the mismatch when a fixed-length segment is placed in native Cell coordinates; and can a nominally stationary starting endpoint move when it shares the residual generated during rotation? This strengthens the existing residual-first method, not a new P000 physical axiom. Do not pivot back to clock hardware or a universal-temperature claim.

## Scoped constructive hypothesis

Treat endpoint allocation, allowed composite paths and surrounding fields as jointly determined. Fix the segment's chosen length observable, not an unannounced pivot. Distinguish a chart origin, a native Cell identity, and a material endpoint/occupancy-field readout. Continuously valued effective endpoint corrections do not introduce fundamental sub-Cell coordinates into the native Z6 torsor.

The existing exact substrate gives L_E(z)^2=sum z_i^2, minimal path steps=sum|z_i|, and shortest ordered-path multiplicity N!/prod|z_i|!. Thus displacement(3,4,0,0,0,0) has endpoint length5, seven minimal primitive steps and35 ordered shortest realizations. Path excess is structural, not numerical error or demonstrated material extension. Fixing primitive path length instead of endpoint norm is a different constraint. Exact axis-label permutations need no endpoint-length error.

A scalar effective allocation model has endpoint corrections a,b and mismatch r, constrained by b-a=r, with cost(ka*a^2+kb*b^2)/2 for ka,kb>0. Its unique solution is a=-kb*r/(ka+kb),b=ka*r/(ka+kb),minimum cost=ka*kb*r^2/[2(ka+kb)]. Hence finite endpoint compliance can assign nonzero correction to the starting endpoint; hard pinning is the ka->infinity limit. Equal weights preserve the midpoint while moving both ends oppositely, so starting-end movement is not automatically center-of-mass transport. These are coarse compatibility costs, not an assertion of two-force primitive equilibrium or calibrated physical dynamics.

A precise pre-existing blind-observer witness is D=(1,1,1,1,1,1). Translating BOTH endpoints byD preserves every endpoint difference and length, and all20 positive-min-zero three-axis observations, while native squared displacement is6. The identity is reused from definitions/ENTERPRISE_X6_NATIVE_SPATIAL_CELL_TORSOR_20260905.md, not claimed as newly discovered. It proves observation does not certify immobility; it does not prove a given rotation generatesD.

An algebraic candidate update F(z)=Pz+e1,whereP cycles the six positive-axis labels, gives F^6(z)=z+D and preserves all segment lengths. The e1 term is an ASSUMED residual-allocation rule, not a derived force. This is an affine rotation-plus-translation example, not a pure rotation about a fixed pivot. Its six-step period follows from the chosenP, not world-dimensional numerology.

## Same-time test and falsifying restrictions

After reversible elimination of interior variables, retain E_eff(A,theta;L,environment,branch), not a frozenA. On a nondegenerate stable branch of an effective coordinate chart, dA*/dtheta=-(E_AA)^-1 E_Atheta. On native Cells compare neighbor costs and retain branch changes. A nonzero mixed response can make the starting endpoint change as orientation changes. Numerical solver iteration is not physical time; migration dynamics still requires an explicit rule.

Translation by an integer native vector does NOT change the endpoint difference, so it cannot by itself repair a direction/length incompatibility of a perfect Bravais coordinate lattice. A perfectly homogeneous cost depending only onB-A has no preference among translated copies. Preferred drift therefore needs actual coupling to a nonuniform reference/field, registry/branch constraints, or specified history; rounding alone is not a force. Under the same periodic orientation/environment and a unique global minimizer for every orientation, a closed angle cycle returns that minimizer. Persistent net motion requires an additional branch/memory/winding/degeneracy or dynamical mechanism, to be tested rather than presumed.

## Exact checks and next step

Local SymPy verifies the allocation solution and cost. Native integer checks verify the5/7/35 example, all20 observer identities for both endpoints, and six repeated affine updates preserving squared length25 while addingD. An initial Python-tool call failed; the actual checks were then executed successfully with container Python. No physical experiment, large benchmark, new temperature theorem, or rotation-causes-translation theorem is claimed. No independent researcher/Lean review.

Next bounded experiment: the SAME segment and environmental data with hard-pinned, finite-penalty, and free endpoint rules; solve all endpoints/path residuals jointly at each orientation; retain full native addresses, common depth and branch provenance. Check whether endpoint motion is relative to a fixed native reference, whether the midpoint moves, whether a closed cycle returns, and whether a zero-residual exact symmetry case is correctly retained.
