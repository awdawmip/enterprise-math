# Native Enterprise filament codes: integer arithmetic glue and universal two-probe decoder

Status: `FREE_RESEARCH_EXACT_LOCAL_TO_GLOBAL_GLUE / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-24`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Depends on:

- `NATIVE_ENTERPRISE_FILAMENT_CHIRAL_DOUBLE_COVER_ACCESS_STRUCTURE_20260824.md`;
- `NATIVE_ENTERPRISE_FILAMENT_CURVATURE_FLATTENED_AFFINE_MDS_CODE_20260824.md`;
- `NATIVE_ENTERPRISE_GLOBAL_PRIME_ISLAND_ENDPOINT_HOLOGRAPHY_3_TO_9_20260823.md`.

## 1. Odd-channel double cover versus the integer lift

Over every sufficiently large odd prime field, the two curvature modes are two affine MDS sheets and any two coordinate residues admit one compatible word on each sheet.

The actual native integer filament is smaller.  A window beginning at native shell R has

`V_j=c+3*R*j+(3*j^2+chi*epsilon_j)/2`,

with the exact arithmetic locks

`chi=(-1)^R`,

`affine slope b=3R`.

Thus chirality and affine slope are not independent integer parameters.  They become independent only after reduction to an odd field, where parity of R is forgotten and every parity class of R covers every residue modulo q.

## 2. Candidate decoder from any two indexed values

Take any two distinct known positions

`i<j`,

set `d=j-i`, and observe their integer values `V_i,V_j`.

For each sign `s in {+1,-1}`, define

`Q_s=2*(V_j-V_i)-3*(j^2-i^2)-s*(epsilon_j-epsilon_i)`.

A sign s is a valid native candidate only when

`R_s=Q_s/(6*d)`

is an integer and

`s=(-1)^(R_s)`.

The true native packet always supplies one valid candidate.

## 3. Exact uniqueness

There are two cases.

### Same-parity probes

If `epsilon_i=epsilon_j`, then

`Q_+=Q_-`.

Hence both signs propose the same shell candidate R.  Exactly one of the opposite signs can satisfy

`s=(-1)^R`.

So the mod-2 shell parity lock selects the unique chirality.

### Opposite-parity probes

If `epsilon_i!=epsilon_j`, then for opposite signs s and -s,

`R_(-s)-R_s = s*(epsilon_j-epsilon_i)/(3*d)`.

The right side is a nonzero proper fraction of absolute value `1/(3d)`.  Therefore the two candidates cannot both be integers.

At the pre-slope level the two numerators differ by exactly2.

- If `d>1`, then `2d` cannot divide that difference, so the false pre-slope candidate is automatically nonintegral.
- If `d=1`, both pre-slopes are integral and differ by one; exactly one is divisible by3, so channel3 selects the native slope.

At the uniform channel level, modulo3 exactly one sign passes the multiple-of-3 slope condition for every separation.

Thus every pair of distinct indexed integer values has exactly one valid `(chi,R)` candidate.

Freeze:

`ANY TWO INDEXED INTEGER VALUES ON A VALID FILAMENT`

`-> UNIQUE CHIRALITY + UNIQUE NATIVE SHELL SCALE`.

## 4. Complete reconstruction

After recovering `(chi,R)`, compute

`c=V_i-3*R*i-(3*i^2+chi*epsilon_i)/2`.

Then every value is regenerated by

`V_m=c+3*R*m+(3*m^2+chi*epsilon_m)/2`.

The transverse coordinate and full typed-Cell location follow from the existing filament potential / shell-address formulas.

Therefore the endpoint-holography theorem is a special case of a stronger statement:

`ANY TWO KNOWN POSITIONS ARE A GLOBAL INTEGER INFORMATION SET`.

The positions need not be endpoints and the window size need not be supplied to recover the infinite native filament sequence.

## 5. Small-channel complementarity

The two odd-field code sheets are glued by the smallest arithmetic channels together with exact index divisibility.

### Same-parity pair

All odd-modulus projections leave the two signs indistinguishable because the parity offset vanishes on both probes.

The missing bit is exactly the q=2 relation

`chi=(-1)^R`.

### Opposite-parity pair

The q=3 condition that the affine slope be a multiple of3 selects exactly one sheet for every separation.

For every nonadjacent pair, exact index-distance divisibility already selects the same sheet; for an adjacent pair it cannot, and q=3 is genuinely necessary.

Thus

`q=2` is the universal same-parity glue,

`index distance` glues nonadjacent opposite-parity observations,

`q=3` glues adjacent opposite-parity observations and supplies a uniform rule for the whole opposite-parity class.

This gives a new role for the mandatory 2 and3 channels: besides selecting long-incidence eligibility, together they guarantee reconstruction of the unique global integer packet hidden behind every good-odd-channel double cover.

## 6. Finite-modulus persistence of ambiguity

Let M be odd and coprime to `6*d`.

Then `6*d` is invertible modulo M, so both signs define valid residue candidates `R_s mod M` from the same two observations.  Hence no finite collection of such good odd channels can by itself remove the two-sheet ambiguity.

The unique native lift is selected only when the excluded small-channel/integrality data are restored.

This is the exact local-to-global chain

`GOOD ODD PRIME CHANNELS -> TWO-SHEET AFFINE CODE`,

`2/3 + INDEX INTEGRALITY GLUE -> ONE INTEGER FILAMENT`.

## 7. Prime-valued specialization

If the observed values happen to be primes in a realized island, the same decoder uses only those two prime values and their positions.

It does not use prime density, a global primality table, or any composite neighbors.  The primality event selects a valid packet; the arithmetic glue self-localizes the selected packet.

The result is richer than Boolean incidence brightness, but it remains an exact consequence of the frozen native allocation rather than a new prime-frequency theorem.

## 8. Boundary

The coding and congruence ingredients are classical.  The research-specific result is the exact coupling

`ALTERNATING CURVATURE CHIRALITY = SHELL PARITY`

and

`AFFINE SLOPE = 3 * SHELL`,

which turns the local odd-channel double cover into a unique globally decodable Enterprise integer filament.
