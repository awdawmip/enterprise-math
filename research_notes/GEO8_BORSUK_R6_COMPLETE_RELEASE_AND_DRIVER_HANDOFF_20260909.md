# GEO8/GEO9 Borsuk R6 line — complete release and persistent Driver handoff

Status: `ACTIVE LINE DOSSIER / ROUTING ONLY / NOT THEOREM EVIDENCE`  
Date: `2026-09-09`  
Parent Objective: `OBJ-EXTERNAL-GEOMETRY-BORSUK-R6-UPPER-BOUND-PRESSURE-20260902`  
Objective generation: `OG-1302C0D2A19AF22098E8 / OPEN`  
Issuing Driver: `EM-DVR-G6K2P9`  
Prepared source cut: `awdawmip/enterprise-math@55c265420e60fd1dbe371c25295bd900408df70c`

## 1. Current charter

This line owns the external Euclidean six-dimensional Borsuk constructive upper-bound pressure only. It does not modify P000 and does not infer a native metric from six named axes.

The retained external status remains `7 <= b(6) <= 33`, with exact `b(6)=7` open unless a later accepted universal Euclidean theorem changes the interval.

Current stable execution task:

`RS-GEO8-BORSUK-R6-LASSAK-33-COMPRESSION-PRESSURE / TP2-75A6C3F81E2D094B67CF / generation 2 / READY`.

As of this dossier cut there is no fresh Gen2 Result. The first unfinished mathematical unit is therefore the current-policy zero-math-drift revalidation already published under that task.

## 2. Verified frontier

The superseded Generation-1 execution produced immutable Result `RR-68BA014D54542DA7221C`. Its bytes are materialized on `main` and may be consumed as mathematical evidence, but it is not current-generation Driver-review authority.

Frozen exact boundary:

- Lassak R6 whole-atom template: one cap plus 32 sign-sector atoms;
- cap-sector exact witness squared distance: `1`;
- sector-sector strict-interior witness squared-distance lower bound: `15/13 > 1`;
- all `C(33,2)=528` atom pairs are incompatible, so the incompatibility graph is `K_33`;
- unions of whole Lassak atoms therefore cannot reduce 33 parts to 32;
- every orientation of the center-fixed untruncated 12 hypercube-facet cone port has a same-piece witness with squared distance `10/7 > 1`.

Scope guards: `K_33` is not `b(6)>=33`; the universal status remains `7<=b(6)<=33`; atom splitting, wall redrawing, truncation, movable/multiple apices, non-hypercubic directions, multiple UCS representatives, and new five-dimensional projection/orbit geometry remain outside the obstruction; no Euclidean-to-P000 theorem is granted.

Consume without replay: GEO7 accepted status/native bridge; Gen1 source reconstruction, checker, K33 obstruction, R4-to-R6 transfer audit, dependency graph, source manifest and adversarial audit; and the Gen2 publication-envelope repair.

## 3. Published state-machine package

### READY current mathematical execution

`RS-GEO8-BORSUK-R6-LASSAK-33-COMPRESSION-PRESSURE / TP2-75A6C3F81E2D094B67CF / generation 2`.

First action: claim/adopt the Gen2 revalidation, replay the exact checker and source boundary, and return a fresh current-generation Result. Preserve K33/15/13/10/7 byte-for-byte when no real mathematical drift exists.

### READY persistent line Driver governance

`GV-GEO8-BORSUK-R6-PERSISTENT-LINE-DRIVER / TP2-554B1F91AAE6D2399881 / GOVERNANCE`.

One explicitly activated Research Driver owns review/routing continuity, preserves valid owners, reviews Gen2, releases blocked successor generations only after their gates are satisfied, and keeps this dossier replacement-ready. Publication does not fabricate a continuously running Driver.

### BLOCKED mathematical successor

`RS-GEO9-BORSUK-R6-ATOM-SPLITTING-5D-UCS-CONSTRUCTION / TP2-E60B91F0492D6AACE1AA`.

Block: a Driver-accepted current-generation Result for the stable GEO8 task.

After that gate, the line Driver republishes this same Task-ID as a READY superseding generation. Do not create a duplicate task. It then attacks atom splitting/redrawing, truncation, movable/multiple apices, non-hypercubic directions, multiple UCS representatives, and five-dimensional normal/orbit geometry. A `b(6)<=32` claim requires universal continuous coverage plus strict `<1` diameter for every part.

### BLOCKED independent audit

`RS-GEO9-BORSUK-R6-CONTINUOUS-CERTIFICATE-INDEPENDENT-AUDIT / TP2-E775DD52A6818401B4F3`.

Block: a Driver-accepted GEO9 mathematical Result plus an actually independent verifier.

Role: independently verify global coverage, equality/near-equality diameter cases, exact subcertificates, source assumptions, and any BRC/provenance compression before final integration.

### BLOCKED final integration

`GV-GEO9-BORSUK-R6-UPPER-BOUND-FINAL-INTEGRATION / TP2-DF7C9B4B05DC45A0CA5E / GOVERNANCE`.

Block: accepted current-generation GEO8 boundary, accepted GEO9 atom-splitting/UCS Result, and accepted independent audit.

Role: integrate compatible claims, change the retained `b(6)` interval only if a universal theorem really changes it, then issue exactly one Objective disposition: `CLOSED`, `PARKED/NO_EXECUTABLE_ROUTE`, or `OPEN_WITH_EXPLICIT_NEW_INFORMATION_GAP`.

## 4. Persistent line Driver obligations

The active line Driver must preserve valid owner/claim scopes before fresh dispatch; use `research_control_dispatch.py` for owner recovery/fresh routing and `tools/research_driver_queue.py` for returned Result review routing; review exact current bytes; keep Gen1 evidence distinct from current-generation authority; use same-Task-ID superseding generations for repair or gate release; keep the GEO9 successor BLOCKED until Gen2 ACCEPTED; require an independent verifier before audit release; release final integration only after all accepted gates; apply BRC only at typed discrete branch/recoalescence bottlenecks and never treat it as a continuous-diameter proof; and update this dossier after every accepted semantic change or before responsibility transfer.

## 5. Smallest next executable actions

Exactly two immediate READY control surfaces exist:

1. `GV-GEO8-BORSUK-R6-PERSISTENT-LINE-DRIVER / TP2-554B1F91AAE6D2399881` — an explicitly activated Driver may take persistent line responsibility;
2. `RS-GEO8-BORSUK-R6-LASSAK-33-COMPRESSION-PRESSURE / TP2-75A6C3F81E2D094B67CF` — a task researcher may claim/adopt Gen2 revalidation.

The three GEO9 downstream objects remain BLOCKED until their exact taskbook gates are satisfied.

## 6. Recovery entry for a replacement conversation

A replacement Driver or researcher must not assume any predecessor is still running. First refresh Enterprise Math `main`, Issue #240 events for the exact task/claim, current publication head for the stable GEO8 task, exact Result/review/follow-up state, the persistent governance publication, and this dossier.

Then route as follows:

- fresh Gen2 Result exists and is unreviewed -> Driver consumes it through the derived Driver queue;
- Gen2 has a valid live owner -> preserve it;
- valid owner with stale session -> adopt the same claim;
- no valid owner -> fresh claim of current Gen2;
- Gen2 ACCEPTED -> release the blocked GEO9 mathematical task by same-Task-ID READY superseding generation;
- GEO9 successor ACCEPTED -> release independent audit only with an actually independent verifier;
- audit ACCEPTED -> release final integration.

Never replay verified-complete Gen1 mathematics without an integrity defect.

## 7. Authority boundary

This dossier is routing/recovery state only. Mathematical authority remains exact Results plus immutable Driver reviews; task existence remains immutable V2 publication records; runtime owner/claim state remains authenticated Issue #240 events plus the canonical reducer; Driver queue state is derived from operational publications plus Result/review/follow-up records; Objective status remains exact Objective generation records. This dossier creates none of those authorities by itself.
