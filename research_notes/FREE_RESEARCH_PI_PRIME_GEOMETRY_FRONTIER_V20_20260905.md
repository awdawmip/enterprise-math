# Free Research — Pi-to-Prime Geometry Frontier V20

Status: `FREE_RESEARCH_CURRENT_FRONTIER / PRIME-BIRTH AND WINDING GEOMETRY STABLE / PNT CLOSED BY REAL SMOOTHING / GROWING-DEPTH VOLTERRA MASS CLOSED / REVERSE FRAME CLOSED / PLACEMENT CURVATURE AUTOMATIC / FINAL DEFECT GRADIENT LIFTED TO FOURTH-ORDER POSITIVE PACKET / NATIVE PACKET DECAY OPEN / NOT WORKING TRUTH / NOT FOUNDATION`
Date: `2026-09-05`
Project: `Enterprise Math / 进取数论`
Supersedes as current frontier: V18 delayed-recanonicalization / arbitrary-order Volterra-defect frontier
Researcher-ID: `EM-FREE-PI-PRIME-20260905`

## 1. Stable geometric chain

The stable prime geometry remains

\[
\boxed{
\begin{aligned}
\text{prime }p
&=\text{irreducible multiplicative-holonomy birth direction},\\
p^a
&=\text{the }a\text{th winding layer},\\
\det\mathcal W_N
&=\operatorname{lcm}(1,\ldots,N),\\
\psi(N)&=\log\det\mathcal W_N.
\end{aligned}}
\]

The degree-three carrier retains complete ordered provenance, its `S_3` standard energy contracts by `1/9`, and the odd-simplex direct-composite chord anchors the constant parity mode:

\[
4U_N^2|f(N)|^2
\le3\bigl(U_NE_1+E_{\rm dir}+E_{\rm tr}\bigr).
\]

The classical PNT normalization is already closed in this project by real Selberg smoothing.  The open objective here is a genuinely native finite-history decay mechanism and a quantitative geometric remainder.

---

## 2. Why the V17 scalar positive recurrence was replaced

The complete odd-simplex expression is a coercive graph norm, not automatically a dissipated state.  Directly seeking

\[
\mathfrak E(N)
\le q\,\mathfrak E(N^{\theta})+\cdots
\]

confuses terminal coercivity with evolution.

The correct discrete analogue of the ideal Volterra identity

\[
[M_t,V]=V^2
\]

is the first defect

\[
\boxed{D=[M_A,L]-L^2.}
\]

The exact parametrix is

\[
\boxed{
A(N)^2f(N)=(M_A-L)G_f(N)-Df(N),
\qquad G_f=(M_A+L)f.
}
\]

For the prime error `r`, the V14 arithmetic layer gives `G_r=O(1)`.  Hence a native proof reduces to showing

\[
Df(N)/A(N)^2\to0.
\]

---

## 3. Uniform growing-depth history law

Let

\[
\mathcal C_k(N)=L^k1(N)
=
\sum_{q_1\cdots q_k\le N}
\prod_i\frac{\Lambda(q_i)}{q_i}.
\]

From the bounded discrepancy

\[
|A(x)-\log x|\le C
\]

one obtains, uniformly for

\[
k=o(\sqrt{\log N}),
\]

\[
\boxed{
\mathcal C_k(N)
=
\frac{(\log N)^k}{k!}
\exp\!\left(O\!\left(\frac{k^2}{\log N}\right)\right).
}
\]

Thus the natural variable depth

\[
k\asymp\log\log N
\]

is fully inside the factorial provenance regime.

For

\[
\Delta_k=[M_A,L^k]-kL^{k+1},
\]

the two positive sides have asymptotically equal mass

\[
\frac{k}{(k+1)!}(\log N)^{k+1}
\exp\!\left(O(k^2/\log N)\right).
\]

---

## 4. Exact commutator-placement reverse frame

The defect hierarchy satisfies

\[
\boxed{
\Delta_k
=
\sum_{j=0}^{k-1}L^jDL^{k-1-j}.
}
\]

For

\[
z_j=(L^jDL^{k-1-j}f)(N),
\]

the placement mean is `Delta_k/k`, while adjacent placement differences are

\[
z_{j+1}-z_j
=(L^j[L,D]L^{k-2-j}f)(N).
\]

Path ANOVA and the positive history mean give

\[
\begin{aligned}
|Df(N)|^2\le{}&
\frac{4}{k^2\mathcal C_{k-1}(N)^2}|\Delta_kf(N)|^2\\
&+
\frac{2(k-1)(2k-1)}{3k\mathcal C_{k-1}(N)^2}
\sum_{j=0}^{k-2}|L^j[L,D]L^{k-2-j}f(N)|^2\\
&+
\frac2{\mathcal C_{k-1}(N)}
\sum_{|\mathbf a|=k-1}w_{\mathbf a}
|Df(N)-Df(q_{\mathbf a}(N))|^2.
\end{aligned}
\]

For even `k`, the parity-correct version replaces the final difference by

\[
|Df(N)+Df(q_{\mathbf a}(N))|^2.
\]

---

## 5. The placement channel is automatically lower order

The double commutator has the exact recoalesced kernel

\[
\boxed{
([L,D]f)(N)
=
\sum_{ab\le N}u_au_b
\kappa_A(N;a,b)f(q_{ab}(N)),
}
\]

where

\[
\kappa_A(N;a,b)
=A(q_aN)+A(q_bN)-A(N)-A(q_{ab}N).
\]

The logarithmic main terms cancel and the floor errors are bounded, so

\[
\boxed{|\kappa_A(N;a,b)|\le4C+2\log2.}
\]

Consequently, uniformly in the growing-depth regime,

\[
\frac{	ext{placement term}}{A(N)^4}
=O\!\left(A(N)^{-2}\right)\|f\|_\infty^2.
\]

The high-order scalar term is likewise

\[
O(k^{-4})\|f\|_\infty^2.
\]

Thus only the parity-history gradient remains.

---

## 6. Deterministic-quotient signless lift

For a fixed quotient action `Q_c`, one has

\[
\boxed{
\delta_cDf=D\delta_cf+[Q_c,D]f,
\qquad\delta_c=I+Q_c.
}
\]

The deterministic commutator is again the same bounded rectangle curvature:

\[
\boxed{
([Q_c,D]f)(N)
=
\sum_{ac\le N}u_a
\kappa_A(N;a,c)f(q_{ac}(N)).
}
\]

Therefore the final history-gradient term is lifted to the positive fourth-order stopped packet

\[
\boxed{
\mathscr O_4(f;N)
=
\sum_{c\le N}u_c
\left[
\sum_{ab\le N}u_au_b|\delta_cf(q_{ab}N)|^2
+
\sum_{ab>N}u_au_b|\delta_cf(q_aN)|^2
\right].
}
\]

All coefficient/commutator errors are lower order.  For an odd history length `h=k-1`, alternating telescoping gives

\[
Df(x_0)+Df(x_h)
=
\sum_{r=0}^{h-1}(-1)^r\delta_{a_{r+1}}Df(x_r).
\]

Hence the final reverse frame is controlled by transported copies of `mathscr O_4` plus `o(A(N)^4)`.

---

## 7. Power-conjugation shortcut is ruled out

Conjugating by

\[
D/A^{2-\alpha}
\]

creates two competing effects.  At depth `k`, the high-order term gains

\[
k^{-2\alpha},
\]

while the endpoint parity coefficient becomes `s^alpha`, where the conjugated endpoint ratio has a Beta law concentrated at `s asymp 1/k`.

Thus:

- high-order decay requires `alpha log k -> infinity`;
- parity-coefficient matching requires `alpha log k -> 0`.

No power-conjugation sequence can make both errors vanish.  The final channel is intrinsically relation-valued.

---

## 8. Current unique gate

The former vague target “prove one global positive recurrence” has been reduced to:

\[
\boxed{
\operatorname{HistAvg}_{k(N)}
\left[
\frac{\mathscr O_4(r;\cdot)}{A(\cdot)^2}
\right]
\longrightarrow0,
\qquad
k(N)\asymp\log\log N.
}
\]

A valid closure may take either form:

1. identify `mathscr O_4` as a coefficient-safe subpacket of the existing V14/V15 multichannel relation energy and prove its scale recurrence;
2. center the stopped pair measure, use its pair-valued `S_3` Dirichlet form, and prove decay of the resulting fourth-order transposition energy;
3. import a native slow-oscillation theorem for the actual prime-error field.

No further mass, normalization, placement, or scalar-readout theorem is missing.

---

## 9. Verification artifacts

New exact checkers:

- `scripts/check_free_research_growing_depth_commutator_frame_v19_corrected.py`;
- `scripts/check_free_research_log_rectangle_commutator_v19.py`;
- `scripts/check_free_research_defect_signless_lift_v20.py`.

The corrected V19 checker explicitly imposes the carrier identity

\[
M_A1=L1.
\]

The first draft checker is superseded because its arbitrary diagonal fixture did not impose this arithmetic normalization.

Workflow success is not asserted until an independent CI run is observed.

---

## 10. Boundary

Closed at exact finite or research-note theorem strength:

1. growing-depth factorial history control;
2. variable-depth stopped/valid mass balance;
3. exact placement reverse frame;
4. automatic log-rectangle control of placement curvature;
5. parity-correct one-channel reduction;
6. exact signless lift to a positive fourth-order stopped packet;
7. no-go for diagonal power-conjugation closure.

Open:

1. native decay of the transported fourth-order stopped packet;
2. full coefficient-safe integration with the V14/V15 relation-state recurrence;
3. any unconditional native quantitative prime remainder;
4. Lean formalization and complete CI closure;
5. Working Truth, Foundation, or RH-scale promotion.
