# TASK #1162 — CM does not imply LCM inside the primitive quadratic rotation family

Status: `RESEARCH_FRONTIER / COMPUTER-ASSISTED CERTIFICATE / NOT PROMOTED`
Researcher-ID: `EM-DIRECT-B62D`
At: `2026-09-06T21:23:00+08:00`

## Setup

Let `D=365=5*73`, a positive fundamental discriminant, and let `chi_D(n)=(D/n)` be the real primitive even quadratic character. Put

`F_D(t)=sum_{n>=1} chi_D(n) exp(-n t)` and `g_D(z)=F_D(sqrt(z))/sqrt(z)`.

For even primitive real characters,

`g_D(z) = (1/sqrt(pi)) int_0^infty s^(-1/2) exp(-zs) Theta_D(s) ds`,

where `Theta_D(s)=sum_{n>=1} chi_D(n) exp(-n^2/(4s))`. Hence `Theta_D>=0` implies complete monotonicity of `g_D`.

## Certified CM for D=365

Use the normalized theta

`theta_D(u)=sum_{n>=1} chi_D(n) exp(-pi n^2 u/D)`.

Primitive-even Poisson transformation gives `theta_D(u)=u^(-1/2) theta_D(1/u)`, so it suffices to prove positivity for `u>=1`.

On `1<=u<=25`, partition into 480 cells `[k/20,(k+1)/20]`. On each cell, evaluate the first 50 terms by outward interval arithmetic. Bound the absolute omitted tail by

`exp(-a*51^2)/(1-exp(-a*103))`, `a=pi*u_left/365`.

The smallest certified lower endpoint occurs on `[10.05,10.10]` and is

`0.053757876724899678... > 0`.

For `u>=25`, the `n=1` positive term dominates the absolute tail, since with `a=pi*u/365`,

`theta_D(u) >= exp(-a) - exp(-4a)/(1-exp(-5a))`,

and at `u=25` the right side is already

`0.1647316002964001... > 0`; the tail/first ratio decreases thereafter.

Thus `theta_365(u)>0` for all `u>0`, hence normalized `g_365` is completely monotone.

## Certified failure of LCM

Let `A_D(X)=sum_{a=1}^D chi_D(a) X^a`. Take the rational-decimal center

`rho0 = 0.9615168472543230999490664 + 0.0347783447452073267685751 i`.

A direct outward evaluation gives `|A_D(rho0)| < 7.92e-14` and `|A_D'(rho0)| > 47.1885`.

On the disk `|rho-rho0|<=1e-12`, `|rho|<0.963`, hence

`sup |A_D''(rho)| <= sum_{a>=2} a(a-1) 0.963^(a-2) < 40000`.

Therefore on the boundary the linear term has magnitude `>4.7188e-11`, while the constant residual plus Taylor remainder is `<7.93e-14`. By Rouche, the disk contains exactly one zero `rho` of `A_D`.

At the center,

`Re[(-Log rho0)^2] = 0.0001819975090522369...`.

The derivative of `(-Log rho)^2` is bounded by `0.125` on the disk, so the enclosed zero still satisfies

`Re[(-Log rho)^2] > 1.8199e-4 > 0`.

Since `|rho|<1`, this zero is not cancelled by `1-rho^365`. Hence `g_365(z)` has a zero at `z=(-Log rho)^2` in the open right half-plane.

If normalized `g_365` were logarithmically completely monotone, then `g_365(z)=exp(-phi(z))` with `phi` a Bernstein function. The Levy-Khintchine representation extends `phi` holomorphically to `Re z>0`, so `exp(-phi(z))` is zero-free there. Contradiction.

Therefore

`CM does not imply LCM`

already inside the real primitive quadratic character family attached to the discrete-rotation research line.

## BRC observer consequence

Theta positivity is sufficient for the positive-measure/CM observer, but it discards character-polynomial zero provenance. D=5 and D=365 both pass the positive-measure observer, while they split at the LCM/infinite-divisibility observer. Thus the CM observer is not an LCM-safe quotient; zero/pole spectral provenance must be retained when future operations include logarithms, Levy exponents, or infinite divisibility.

## Next

Derive the general zero/pole heat-trace criterion for LCM of primitive even character kernels, and use it to classify the strict phase chain `LCM -> CM-only -> monotone-only -> signed` with D=5,365,53,173 as candidate witnesses.
