# X6 upper V17: active-triad transport, emergent reversal control and holonomy-state minimality

Status: `FREE_RESEARCH / EXACT CONDITIONAL MULTI-EVENT DYNAMICS + QUOTIENT THEOREM / NOT_FOUNDATION`
Date: `2026-09-06`
Researcher: `EM-FREE-PI-PRIME-20260904 / FREE_AXIOM_DISCOVERY`
Tasks: `RS-X6-TRIADIC-CLOSURE-DYNAMICS`, `RS-X6-NATIVE-TIME-DYNAMICS`, `RS-X6-UPPER-STRUCTURE-INTEGRATION`
Depends on:
- `X6_20_SLICE_FRAME_CONNECTION_HOLONOMY_V2_20260906.md`;
- `X6_CHART_HOLONOMY_ORI6_CONTROL_BRIDGE_V16_20260906.md`;
- `X6_ORI6_CONTROL_LEDGER_V15_20260906.md`;
- BRC / Operation-Safe Quotient discipline.
Checker: `experiments/x6_active_triad_holonomy_v17_20260906/check_active_triad_holonomy.py`.

## 1. From observer charts to an active relation process

V16 intentionally kept the Johnson-chart holonomy on the observer/context side. A stronger but still conditional model is now available.

Assume a subsystem has, at each relation-event layer, one **active triad**

`S_t subset {1,...,6}`, `|S_t|=3`.

When consecutive active triads share exactly two native axes,

`|S_t intersect S_{t+1}|=2`,

transport the active triad's internal three-slot frame by the canonical replacement law:

- the two shared native axis identities retain their internal slots;
- the entering axis inherits the slot vacated by the leaving axis.

This is exactly the `J(6,3)` connection, but now used as an explicitly declared **internal active-frame transport law** rather than merely as an observer bookkeeping convention.

This law is conditional research semantics, not P000/Foundation admission.

## 2. Active-triad event histories carry S3 holonomy

For an event history

`S_0 -> S_1 -> ... -> S_m`,

compose the canonical replacement transports.

If `S_m=S_0`, the active internal frame returns to the same three native axes with holonomy

`h in S3`.

The holonomy depends on the relation-event path, not only on its endpoint triad.

Thus one may define two typed history observers:

- full active-frame holonomy `h in S3`;
- binary charge `u=sgn(h) in C2`.

The latter is V16's unique nontrivial compositional binary Ori6-control bridge.

## 3. Same endpoint and same event count can have different reversal charge

Fix base triad

`S0={1,2,3}`.

Two three-switch loops are:

### Flat loop

`{1,2,3}->{1,2,4}->{1,2,5}->{1,2,3}`.

Its holonomy is identity, so `u=0`.

### Curved loop

`{1,2,3}->{1,2,4}->{1,3,4}->{1,2,3}`.

Its holonomy exchanges axes `2,3`, so `u=1`.

The two histories have:

- the same initial active triad;
- the same final active triad;
- the same number of chart/event switches `3`;
- different frame holonomy and different control charge.

Therefore

`ACTIVE_TRIAD_ENDPOINT + EVENT_COUNT`

is not sufficient for a future operation sensitive to reversal charge.

This is a direct BRC/provenance witness: the active-triad path cannot be collapsed to endpoint plus length before the holonomy-sensitive future is resolved.

## 4. The V15 control bit becomes an emergent history observable

Under the V16 conditional bridge, for a closed active-triad relation loop define

`u(loop)=sgn(h(loop))`.

Then the V15 recurrence becomes

`epsilon_after = (-1)^u epsilon_before`

for the local chirality state attached to the coupled event.

For a sequence of closed relation loops, concatenation gives

`u(total)=sum u(loop_r) mod2`.

Thus, in this conditional multi-event model, the previously external V15 binary control is generated by active-triad transport history itself.

The mechanism is not local to one isolated Cell state: it lives in the ordered relation path through changing active triads, exactly as anticipated by the native-time program.

## 5. Odd holonomy also supplies the pair context

When `h` is a transposition of the returned base triad, it identifies exactly which pair of local axes was exchanged.

Therefore an odd simple loop supplies both pieces V9/V13 required:

- charge bit `u=1`;
- pointed pair `{i,j}` inside the returned triad, with the remaining axis `k`.

Given the current four-twist local C6 generator, V13 then selects the unique common-node triadic signed lift of that transposition.

Hence on this declared active-frame transport interface:

`CURVED ACTIVE-TRIAD LOOP`

`-> TRANSPOSTION HOLONOMY`

`-> ORI6 CONTROL 1 + POINTED PAIR`

`-> UNIQUE V13 TRIADIC PHASE-REVERSAL EVENT`.

No external unordered-pair selector is needed at that stage.

## 6. Charge-only compression: C2 is sufficient and optimal

If the only future question is the final Ori6/reversal charge, the exact group quotient

`S3 -> S3/A3 ~= C2`

is sufficient.

It is the unique nontrivial binary group quotient by V16.

Thus the full active-triad history can be safely compressed to one bit **for this exact future language**.

This does not retain which pair is exchanged and cannot execute the exact V13 odd lift.

## 7. Exact compositional odd-lift future requires the full S3 holonomy

Suppose the future operation must support arbitrary further loop composition and, whenever the final holonomy is odd, must know the exact transposition in order to choose the pointed V13 lift.

Then a composition-respecting quotient of S3 must keep the three transpositions distinct.

Every group congruence is the coset relation of a normal subgroup. The only normal subgroups of S3 are

`{1}`, `A3`, `S3`.

- quotient by `S3` forgets everything;
- quotient by `A3` is C2 and merges all three transpositions into one odd class;
- only the trivial kernel keeps the three transpositions distinct.

Therefore **no proper group quotient of S3** is sufficient for exact compositional odd-lift prediction.

So the minimal compositional transport state is the full six-element holonomy:

`HOLONOMY_STATE = S3`.

This is a sharp operation-safe minimality theorem.

For one final non-compositional lookup restricted to simple triangular loops, a smaller four-way signature `{flat, swap12, swap13, swap23}` can suffice. It is not closed as a group state under arbitrary future loop composition.

## 8. Relation to native time

The active-triad path is an ordered relation-event history. Certified-independent events elsewhere may remain partially ordered as in the event-time program, but the transitions inside one holonomy-carrying active-triad loop are causally ordered because each frame transport consumes the previous active frame.

Thus this mechanism fits the current time architecture:

`dependency event path -> active-frame holonomy -> scoped control/observer`.

Scalar elapsed time or total event count alone cannot reconstruct the holonomy.

## 9. Relation to pure spatial channel flatness

The current pure spatial channel-frame theorem proves that full-S6-covariant translation-invariant deterministic channel permutation transport is flat/trivial.

The holonomy here does not contradict that theorem because it is not generated by bare coordinate translation. It is generated by **changing active triadic relation context**.

So any nonzero control obtained here is correctly typed as internal/relation history, not as hidden curvature of pure X6 translation.

## 10. Current consequence and boundary

This stage supplies a concrete answer to V15's question “can `u_t` come from a larger relation history?”:

**yes, conditionally**, if active triad frames are transported by the canonical shared-axis connection.

Then the control bit is not arbitrary:

`u=sgn(active-triad holonomy)`.

And when exact odd lift selection is needed, the full S3 holonomy is the minimal compositional repair state.

Still not proved:

- that physical systems must use this active-frame transport law;
- that every active-triad change corresponds to a Cell interaction of this type;
- a rate or trigger for entering a particular active-triad loop;
- that chart/internal frame holonomy should be interpreted as physical curvature.

No Foundation promotion or external novelty claim is made.
