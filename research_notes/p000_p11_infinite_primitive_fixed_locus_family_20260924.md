# P000 P11 infinite primitive fixed-locus family from the eta_2 section

Status: `PORTABLE_RESEARCH_NOTE / NO_CURRENT_CLAIM / NOT_SOURCE_CHECKPOINT / NOT_RESULT / UNREVIEWED`

Task: `RS-P000-P11-DIAGONAL-ELLIPTIC-FIBER-PRIMITIVE-ARITHMETIC`
Publication: `TP2-FB7F5A1D6B6C6BCCD62D`
Contributor lineage: `EM-DIRECT-C4D02C`, stable conversation `em-auto-staggered-20260923-r11`, session `MCP-5ff2a3263c5c4ca2b556ebe8e47e5624`.
Source pin consumed: `ff2eae807fa4f6b72da42698ea52ab3c7fb0e50c`.

Control boundary at persistence time: the latest exact continuation has `execution_authorized=false`, `continuation_seed=null`, `persisted_checkpoint=NOT_FOUND`, and no live claim. This file is project-repository durability only. It does not publish a Source checkpoint, grant mathematical acceptance, or enter predecessor `completed_units`.

## 1. Consumed exact interfaces

Consume without promotion or re-proof:

- the Driver-accepted parent fixed-locus reduction and primitive recovered-root quotient;
- the same-session portable Weierstrass carrier
  `E: W^2=X(X-P^2)(X-Q^2)`
  with inverse
  `T=(P+Q)(X-PQ)/(X+PQ)`,
  `Yq=4(P+Q)PQW/(X+PQ)^2`,
  `D=Yq/(2T)`,
  `U=(T+(P^2-Q^2)/T)/2`,
  `V=(T-(P^2-Q^2)/T)/2`;
- the same-session TYPE-5+ `eta_2=(1,2,2)` cover interface.

The parent Euclidean core is

`A=r^2-s^2`, `B=2rs`, `C=r^2+s^2`, `P=A+B`, `Q=|A-B|`.

## 2. Fixed auxiliary elliptic curve

Consider

`G: y^2=x^3-2x^2+2x=x((x-1)^2+1)`

with `R=(1,1)`.

The discriminant is `-2^8`, so 3 and 5 are primes of good reduction. Direct enumeration gives

`#G(F_3)=6`, `#G(F_5)=8`.

Good-reduction torsion injection implies that if `R` were torsion, every odd primary part of its order would be trivial and its 2-primary part would have order at most 2. Since `y(R)=1`, `R` is not 2-torsion. Therefore

`R` has infinite order in `G(Q)`.

For a secant or tangent `y=mx+c`, the three intersection x-coordinates satisfy

`x1*x2*x3=c^2`.

Hence x-squareclass is multiplicative under the group law. Since `x(R)=1` is a square, every nonzero multiple satisfies

`x(nR)=(a_n/b_n)^2`

for positive coprime integers `a_n,b_n`. The numerator `a_n` is always odd: if it were even then `v2(x)>0` would be even and at least 2, while `v2(x^2-2x+2)=1`, contradicting that `x(x^2-2x+2)` is a square.

The real cubic has only one real root, so `G(R)` is connected. The cyclic subgroup generated by the non-torsion point `R` is dense in the real circle group.

The exact point

`4R=(961/400,21359/8000)`

satisfies

`2 < 961/400 < 2+sqrt(2)`.

Therefore infinitely many positive multiples `nR` lie in the open interval

`2 < x(nR) < 2+sqrt(2)`.

## 3. Primitive TYPE-5+ core from one selected multiple

Fix such a multiple and write

`x(nR)=(a/b)^2=:u`,
`2<u<2+sqrt(2)`.

Define

`r=a^2-b^2`,
`s=b^2`.

Then `r>s>0`, `gcd(r,s)=1`, and `r,s` have opposite parity. Since `a` is odd:

- if `b` is even, `s=b^2` is divisible by 4;
- if `b` is odd, `r=a^2-b^2` is divisible by 8.

Thus every selected core is TYPE-5+.

The equation of `G` gives

`C=r^2+s^2=h^2`

for an integer `h`, and

`r+s=a^2`.

The core quantities simplify to

`A=a^4-2a^2b^2`,
`B=2a^2b^2-2b^4`,
`P=A+B=a^4-2b^4`,
`A-B=b^4(u^2-4u+2)`.

Since `2<u<2+sqrt(2)`, one has `u^2-4u+2<0`, hence

`B>A>0`,
`Q=B-A=-a^4+4a^2b^2-2b^4>0`.

This signed branch is a required provenance coordinate for strict P11 reconstruction.

## 4. Integral eta_2 cover lift and target point

Use the cover parameter

`q=b^2/a^2`.

The identity

`1-2q^2=(a^4-2b^4)/a^4=P/a^4`

cancels the parameterization denominators. The cover variables are exactly

`Uc=a^4+2b^4`,
`Vc=2a^2b^2`,
`Zc=2abh`,

and satisfy

`Uc^2-2Vc^2=P^2`,
`Uc^2-2Zc^2=Q^2`.

The corresponding target Weierstrass point is therefore integral:

`X=(a^4+2b^4)^2`,
`W=8a^3b^3h(a^4+2b^4)`.

Directly,

`X-P^2=8a^4b^4`,
`X-Q^2=8a^2b^2h^2`,

so

`W^2=X(X-P^2)(X-Q^2)`.

## 5. Exact strict positivity on the original fiber

Apply the exact inverse Weierstrass map. On the branch `Q=B-A`, direct simplification gives

`T = 2a^2(a^2-b^2)(a^6-2a^4b^2+2a^2b^4+4b^6) / (a^6+a^4b^2-2a^2b^4+2b^6)`.

Every factor is positive for `u>2`, so `T>0`. Choose `W>0`; then `Yq>0` and hence `D>0`. Also

`U=(T+(P^2-Q^2)/T)/2>0`.

The remaining coordinate is

`V = Q^2(a^8+a^6b^2+2a^2b^6+4b^8) / [(a^6-2a^4b^2+2a^2b^4+4b^6)(a^6+a^4b^2-2a^2b^4+2b^6)]`,

which is positive. Since

`V^2+D^2=Q^2`,

one gets

`0<D<Q`.

Thus every selected auxiliary multiple produces a rational point on the strict parent fiber:

`U>0`, `V>0`, `0<D<Q`.

The retained sign is essential. For `x(nR)>2+sqrt(2)`, one has `A>B`; the same target construction can reconstruct the opposite V-branch. Therefore replacing `A-B` by `Q=|A-B|` before the positivity observer is not BRC-safe.

## 6. Rational strict fiber point to primitive integral P11 datum

Let `L` be a common positive denominator of `D,U,V`, and put

`k=2L`.

Then

`d=kD`, `mu=kU`, `nu=kV`

are integers, `d congruent k (mod 2)`, and `mu,nu` are even. Define the integer base Pythagorean data

`{x0,y0}={kA,kB}`,
`b0=kC`,

with `x0>y0`. Then

`p=x0+y0=kP`,
`q0=x0-y0=kQ`,

and

`x0^2+y0^2=b0^2`,
`d^2+mu^2=p^2`,
`d^2+nu^2=q0^2`.

Hence the accepted parent reconstruction produces an integral strict `h=0` P11 datum.

Let `m` be the exact common gcd of the sixteen recovered outer roots. The parent theorem proves that division by `m` scales `(x0,y0,b0,d,mu,nu)` compatibly with the same homogeneous equations. Since `m` divides both `kA` and `kB` and `gcd(A,B)=1`, one has `m|k`. Thus the primitive quotient preserves the same Euclidean core `(r,s)`. Dividing by the full common root gcd gives recovered-root gcd exactly one.

Therefore every selected rational strict point yields a primitive integral P11 fixed-locus point.

## 7. Infinitely many distinct primitive cores

Density supplies infinitely many positive indices with

`2<x(nR)<2+sqrt(2)`.

If two positive indices had equal x-coordinate, then `nR=+/-mR`. Since `R` has infinite order, this forces `n=m`. Thus the selected x-values are infinite and distinct.

The core ratio is

`r/s=x(nR)-1`,

so distinct selected x-values give distinct primitive Euclidean cores. Primitive root-gcd quotienting changes only the external common scale and cannot identify different cores.

Hence:

`INFINITE_PRIMITIVE_FIXED_LOCUS_FAMILY_CANDIDATE`:

Infinitely many distinct primitive Euclidean cores admit primitive integral strict `h=0` P11 points.

At mathematical-candidate strength this meets the task's positive terminal alternative

`DIAGONAL_ELLIPTIC_FIBER_INFINITE_PRIMITIVE_FAMILY_PROVED`,

subject to lawful Source checkpoint, independent review, and freeze.

## 8. Exact regression certificate

For `n=4`:

`4R=(961/400,21359/8000)`,
`(a,b)=(31,20)`,
`(r,s)=(561,400)`,
`(A,B,C)=(154721,448800,474721)`,
`(P,Q)=(603521,294079)`,
`h=689`.

The common denominator of the inverse-map `D,U,V` is

`L=767326053623109361`,

and the parity-safe scale

`k=1534652107246218722`

gives

`(x0,y0,b0)=(688751865732102962433600, 237442908685242206886562, 728531583004032197926562)`,

`(d,mu,nu)=(377120747320472741536080, 845941311261274825126562, 247910702978256584099362)`.

Exact integer arithmetic verifies all three Pythagorean equations, fixed-locus parity, strict positivity, and recovered-root gcd one.

A deterministic exact-Fraction replay for `1<=n<=40` finds strict-interval indices exactly

`[4,13,23,31,40]`

and verifies the symbolic identities for all five. This finite replay is falsification/regression only; infinitude is the real-group density theorem.

Portable control uploads for this unit:

- `p000_p11_type5_infinite_mw_cover_family_20260924.md`, SHA-256 `21c6ab667af2da97461cec41f039a2e43d6f88669ab5a83cd092d8f6d5b45cc9`;
- `p000_p11_infinite_primitive_fixed_locus_family_20260924.md`, SHA-256 `4420d99c737a7dd2568c2b4aed21eda4d5c9586fce97e28e0a5f2037b1a62fe1`;
- `p000_p11_infinite_family_check_20260924.py`, SHA-256 `adf6d17baf9adfe6b3bed2887df512bf618ec81ef16fb6386de5136d2bdda04c`.

## 9. BRC resolution and next action

BRC resolution:

`COMPOSE_APPLIED / AUXILIARY_SQUARECLASS_PROVENANCE / SIGNED_AB_BRANCH_RETAINED / EXACT_RECONSTRUCTION_CLOSED / INFINITE_PRIMITIVE_P11_FAMILY`.

The critical repair coordinate is `sgn(B-A)` until strict positivity is certified. Denominator, scale/parity, and recovered-root-gcd ports are retained through the exact reconstruction and only then quotiented by the accepted primitive-root theorem.

Do not redo the parent genus-one reduction, Weierstrass interface, eta_2 cover derivation, auxiliary-curve construction, or the strict interval/reconstruction argument unless review identifies a concrete flaw.

The exact continuation at Source `ff2eae807fa4f6b72da42698ea52ab3c7fb0e50c` still reports `execution_authorized=false`, `continuation_seed=null`, `persisted_checkpoint=NOT_FOUND`, `completed_units=[]`, `NEEDS_DISPATCH`, `HANDOFF_READY`, and no live claim. Therefore this note is durable frontier only. The next lawful action is to obtain the exact authorized claim/open or same-session resume route, checkpoint and read back the consumed dependency chain plus this candidate and checker, then prepare the Result for independent Driver review/freeze.
