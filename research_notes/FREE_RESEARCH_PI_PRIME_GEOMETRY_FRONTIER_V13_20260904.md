# Free Research — Pi-to-Prime Geometry Frontier V13

Status: `FREE_RESEARCH_CURRENT_FRONTIER / PNT_CLOSED_BY_REAL_SMOOTHING / FULL_INTERMEDIATE_KERNEL_CLOSED / TWO_CHANNEL_ANOVA_CLOSED / STANDARD_ONE_NINTH / MEAN_HIGH_LOW_HALF_CONTRACTION / PUSHFORWARD_DENSITY_MATCHING_OPEN / NOT_WORKING_TRUTH / NOT_FOUNDATION`
Date: `2026-09-04`
Project: `Enterprise Math / 进取数论`
Supersedes as current frontier: `FREE_RESEARCH_PI_PRIME_GEOMETRY_FRONTIER_V12_20260904.md`

## 1. Stable pi-to-prime chain

The stable geometry remains

\[
\boxed{
\begin{aligned}
\tau^2
&=3!\lim_{M\to\infty}\det(I-B_M^{-2})^{-1},\\
\text{prime }p
&=\text{irreducible Krawtchouk birth direction},\\
p^a
&=\text{winding-layer birth},\\
\det\mathcal W_M
&=\operatorname{lcm}(1,\ldots,M),\\
\psi(M)&=\log\det\mathcal W_M,\\
\Psi_2(M)&=2M\log M+O(M),\\
\psi(M)&\sim M,\\
\pi(M)&\sim M/\log M.
\end{aligned}}
\]

The PNT closure uses classical real Selberg smoothing after the finite Hamming/prime-winding carrier supplies the positive degree-two energy.  No external novelty is claimed for the PNT or its classical proof.

---

## 2. Complete deepest provenance carrier

At scale `n=Y^3`, a deepest three-history packet has exactly one action at most `Y` and two actions above `Y`.  The scalar lower endpoint, the three-valued color, and even color plus the uncut intermediate vertex are successively insufficient for arbitrary transported readouts.

The complete finite carrier is

\[
\boxed{
(j,v_1,v_2,v_3,m),
}
\]

where `j` is the unique uncut position,

\[
v_i=\left\lfloor\frac{Y^3}{a_i}\right\rfloor,
\qquad
m=\left\lfloor\frac{Y^3}{a_1a_2a_3}\right\rfloor.
\]

Exact no-resurrection counterexamples prove the strictness of the recoverability hierarchy.  The full key recovers every declared readout factoring through these data.

---

## 3. Conditional variance is an ordered relation field

For a field `f`, put

\[
X_h=(f(v_1(h)),f(v_2(h)),f(v_3(h))).
\]

The internal standard energy of one history is exactly the cyclic ordered-curvature packet

\[
\boxed{
\sum_{\rm cyclic}|X_{h,i}-X_{h,j}|^2
=
|\Omega_{a,b\mid c}|^2
+|\Omega_{b,c\mid a}|^2
+|\Omega_{c,a\mid b}|^2,
}
\]

where

\[
\Omega_{a,b\mid c}
=\delta_{bc}f(q_a)-\delta_{ac}f(q_b)
=f(q_a)-f(q_b).
\]

For every coarse color/endpoint fiber and every retained branch position, its weighted conditional variance is exactly a complete-graph relation energy whose edge observable is another common-suffix curvature.  Hence the projection loss is not an unidentified boundary term; it is a positive ordered provenance field on the complete history bundle.

---

## 4. Exact representation-theoretic ANOVA

Write

\[
\mu_h=\frac13\sum_iX_{h,i},
\qquad
X_h^0=X_h-\mu_h(1,1,1).
\]

Then

\[
\boxed{
\|X_h-X_{h'}\|^2
=3|\mu_h-\mu_{h'}|^2
+\|X_h^0-X_{h'}^0\|^2.
}
\]

After finite weighted summation,

\[
\boxed{
\mathcal V_{\rm full}
=
\mathcal V_{\rm mean}
+
\mathcal V_{\rm std}.
}
\]

This is the decomposition

\[
\mathbb R^3=\mathbf1\oplus\mathrm{Std}.
\]

The weighted `S_3` lift--transpose--project mixer acts by amplitude `1` on the trivial line and `1/3` on the standard plane.  Thus the standard energy contracts by exactly `1/9`.

---

## 5. V12 correction: the history mean is scale-contracting

The history mean is fixed by slot permutations, but it is not fixed by the deepest scale geometry.

For one aligned deepest history pair, let `d_h` be the difference on the unique high branch and `d_1,d_2` the differences on the two low branches.  Then

\[
\boxed{
\frac12d_h^2+2(d_1^2+d_2^2)-
3\left(\frac{d_h+d_1+d_2}{3}\right)^2
=
\frac{(d_h-2(d_1+d_2))^2+6(d_1-d_2)^2}{6}.
}
\]

Consequently,

\[
\boxed{
3|\Delta\mu|^2
\le
\frac12|d_h|^2+2(|d_1|^2+|d_2|^2).
}
\]

The unique uncut action produces

\[
Y^2\le v_h\le Y^3,
\]

while both overcut actions produce

\[
v_{\ell_1},v_{\ell_2}<Y^2,
\]

and the common endpoint satisfies `m<Y`.  These are exact natural-number inequalities.

Thus the mean channel has coefficient `1/2` on its sole high-scale branch; all other forcing lies strictly below the square scale.

---

## 6. Deepest chamber normalization

The first-mass law

\[
A(X)=\sum_{q\le X}\frac{\Lambda(q)}q
=\log X+O(1)
\]

turns the degree-three prime-winding packet into logarithmic simplex measure.  The full product packet has mass

\[
\frac92(\log Y)^3+O((\log Y)^2),
\]

whereas the total deepest chamber has mass

\[
\frac12(\log Y)^3+O((\log Y)^2).
\]

Therefore its normalized mass is

\[
\boxed{
\frac19+O\left(\frac1{\log Y}\right).
}
\]

At exact mass `1/9`, if the high and two low conditional branch energies lie below a common recursive envelope `E`, the history-mean contribution obeys

\[
\boxed{
\frac19\mathcal E_{\rm mean}
\le
\frac12E.
}
\]

With mass slack `epsilon`, the coefficient is

\[
\boxed{
\frac12+\frac92\varepsilon.
}
\]

It remains below one whenever `epsilon<1/9`; the arithmetic chamber law gives `epsilon=O(1/log Y)`.

---

## 7. Correct two-channel cube-root RG

The deepest descended relation state has two orthogonal channels:

\[
\boxed{
\mathcal E^{\rm deep}
=
\mathcal E_{\rm mean}
\oplus
\mathcal E_{\rm std}.
}
\]

Their natural asymptotic transition coefficients are

\[
\boxed{
\mathcal E_{\rm std}'
\le
\left(\frac19+o(1)\right)
\mathcal E_{\rm std}
+	ext{lower-scale forcing},
}
\]

and

\[
\boxed{
\mathcal E_{\rm mean}'
\le
\left(\frac12+O(1/\log Y)\right)
\mathcal E_{\rm envelope}
+	ext{density-matching defect}.
}
\]

V12's diagonal block `(1,1/9)` was therefore too pessimistic.  The trivial slot representation has eigenvalue `1` under the local mixer, but the combination of deepest chamber selection and high/low scale separation gives a strict macroscopic coefficient.

---

## 8. Unique remaining finite-energy issue

Conditioning on color and final endpoint induces nonuniform branch weights.  For the high branch,

\[
\mu_{Y,m}(a)
=
\frac{\Lambda(a)}a
\sum_{\substack{b,c>Y\\abc\le Y^3\\
\lfloor Y^3/(abc)\rfloor=m}}
\frac{\Lambda(b)}b\frac{\Lambda(c)}c.
\]

The two low branches have analogous pushforwards.  The exact coercivity theorem applies to these measures, but the canonical recursive relation energy is expressed in the original prime-power action measure.

The remaining theorem must compare the induced measures with a common recursive envelope:

\[
\boxed{
E_H+E_{L_1}+E_{L_2}
\le
(1+O(1/\log Y))E_{\rm canonical}
+E_{\rm density\ defect},
}
\]

with a positive, lower-scale, or summable density defect.

This is now the sole missing bridge to a complete native quantitative cascade.  It is narrower than the earlier requests for a new spectral gap, a new curvature, or an unspecified boundary estimate: all those structures are already present.

---

## 9. Formal packet

Current new Lean modules include:

- `DeepChamberIntermediateNoGo.lean`;
- `DeepChamberFullIntermediateVariance.lean`;
- `DeepChamberHistoryMean.lean`;
- `DeepChamberVectorANOVA.lean`;
- `DeepChamberHighLowCoercivity.lean`;
- `DeepChamberScaleSeparation.lean`;
- `DeepChamberNormalizedCascade.lean`.

Current exact checkers include:

- `check_free_research_deep_full_intermediate_variance.py`;
- `check_free_research_deep_history_mean.py`;
- `check_free_research_deep_high_low_coercivity.py`.

They use integers and `Fraction` only for the exact finite checks.  Workflow status is tracked independently and is not presumed by this frontier record.

---

## 10. Updated boundary

Closed:

1. prime birth and winding determinant geometry;
2. PNT at classical real-smoothing strength;
3. complete deepest provenance carrier;
4. exact conditional variance as ordered curvature;
5. exact trivial/standard ANOVA;
6. standard energy coefficient `1/9`;
7. history-mean high/low coefficient `1/2`;
8. exact cube/square/cutoff scale separation;
9. chamber-normalized coefficient `1/2+O(1/log Y)`;
10. corrected two-channel RG typing.

Open:

1. induced pushforward-density comparison;
2. a summable estimate for its relation-energy defect;
3. closure of the full native two-channel cascade;
4. a native quantitative remainder for `psi(x)-x`;
5. any RH-scale conclusion;
6. Working Truth or Foundation promotion.

---

## 11. Next mother question

Can the high and low pushforward measures generated by the deepest kernel be disintegrated into the canonical logarithmic prime-power measure plus a bounded-discrepancy transport whose carré du champ is `O(1/log Y)` after normalization?

A positive answer would combine the strict coefficients `1/2` and `1/9` into a complete summable cube-root RG and produce the first genuinely native quantitative prime remainder in this program.
