# Native Enterprise long-filament local sieve：prime 5 is the unique single-channel breaker

Status: `FREE_RESEARCH_EXACT_LEGENDRE_COUNT / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-23`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Parent: `NATIVE_ENTERPRISE_PRIME_SIEVE_CONNECTIVITY_COLLAPSE_2D_1D_FINITE_20260823.md`

## 1. Long native filament

After the exact mod-6 incidence selection, every unbounded eligible component has

`h=t-ceil(r/2)==4 mod6`

and Cell sequence

`C_r=N(r,h+ceil(r/2),1)`.

Writing `r=2m` or `2m+1` gives

- even shells: `C_{2m}=6m^2+h+1`;
- odd shells: `C_{2m+1}=6m(m+1)+h+3`.

Fix an odd prime `q>3` and reduce h modulo q.

Call h **q-transparent** if no Cell anywhere on the infinite parity-complete filament is divisible by q.

## 2. Two root conditions

An even-shell q-hit exists iff

`6m^2+h+1=0 mod q`,

or equivalently

`m^2=-(h+1)/6`.

An odd-shell q-hit exists iff

`6m(m+1)+h+3=0 mod q`.

Completing the square gives

`(2m+1)^2=-(2h+3)/3`.

Therefore, away from the two zero-discriminant values, h is q-transparent exactly when both quantities

`f(h)=-(h+1)/6`,

`g(h)=-(2h+3)/3`

are quadratic nonresidues modulo q.

## 3. Exact transparent-class count

Let `chi` be the Legendre character modulo q and remove the two distinct roots

`a=-1`, `b=-3/2`.

Then

`T(q)=1/4 * sum_{h != a,b} (1-chi(f(h)))(1-chi(g(h)))`.

The required character sums are elementary:

- `sum chi(f) = -chi(f(b)) = -chi(3)` because `f(b)=1/12`;
- `sum chi(g) = -chi(g(a)) = -chi(-3)` because `g(a)=-1/3`;
- `sum chi(fg) = -1`, since `fg` is a quadratic with two distinct roots and square leading coefficient `1/9`.

Hence

`T(q) = (q-3 + Legendre(3/q) + Legendre(-3/q))/4`.

This is an exact integer for every prime `q>3`.

## 4. Unique breaker

For q=5:

`Legendre(3/5)=-1`,

`Legendre(-3/5)=-1`,

so

`T(5)=0`.

Thus every transverse class h mod5 is hit somewhere by a 5-divisible Cell, and every long mod-6 filament is cut.

For every prime `q>=7`, both Legendre symbols are at least `-1`, so

`T(q) >= (q-5)/4 > 0`.

Therefore every prime q>=7 has at least one q-transparent transverse class. No such single q-channel can cut all infinite mod-6 filaments.

Thus:

`q=5 IS THE UNIQUE PRIME >3 WITH ZERO TRANSPARENT FILAMENT CLASSES`.

Equivalently:

`PRIME 5 IS THE UNIQUE SINGLE-CHANNEL LONG-CONNECTIVITY BREAKER`.

## 5. First values

The exact transparent counts begin

- `T(5)=0`;
- `T(7)=1`;
- `T(11)=2`;
- `T(13)=3`;
- `T(17)=3`;
- `T(19)=4`;
- `T(23)=5`;
- `T(29)=6`;
- `T(31)=7`;
- `T(37)=9`;
- `T(41)=9`;
- `T(43)=10`;
- `T(47)=11`;
- `T(53)=12`;
- `T(59)=14`;
- `T(61)=15`;
- `T(67)=16`.

So later prime channels increasingly leave many completely transparent transverse classes.

## 6. Relation to the d=2..19 tower

The d=2 graph uses channels 2 and 3 and retains infinite one-dimensional filaments.

The d=3 channel is q=5, the unique channel that necessarily intersects every such filament. This forces finite components.

Every later individual channel q>=7 lacks this universal cutting property. Thus the observed d=3 connectivity transition is not an accident of the first 19 primes; it is selected by an exact all-q uniqueness statement.

## 7. Boundary

Legendre characters and quadratic character sums are classical. The research-specific content is the exact coupling of that count to the native incidence filament generated by the typed Enterprise allocation.
