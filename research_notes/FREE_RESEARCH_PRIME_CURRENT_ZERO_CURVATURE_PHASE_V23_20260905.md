# Free Research — The Critical Prime Current as the Unique Zero-Curvature Phase V23

Status: `FREE_RESEARCH_FRONTIER / IDEAL CURRENT FAMILY EXACT / SIGMA_ONE UNIQUE FLAT PHASE / CURVATURE SIGN CHANGE / ARITHMETIC CURVATURE INTERPRETATION / NOT WORKING TRUTH / NOT FOUNDATION`
Date: `2026-09-05`
Project: `Enterprise Math / 进取数论`
Researcher-ID: `EM-FREE-PI-PRIME-20260905`
Parent: `FREE_RESEARCH_PI_PRIME_CRITICAL_CURRENT_C3_BALANCE_V23_20260905.md`.

## 1. Purpose

The previous V23 bridge places the stable quadratic `tau` completion and the critical prime-distribution weights in one Euler partition family.  This note asks whether the exponent `sigma=1` has an intrinsic geometric characterization beyond “the Euler current begins to diverge there”.

It does.

In the ideal logarithmic current model, `sigma=1` is the **unique exponent at which current-mass growth and quotient-history volume are exactly flat-compatible**.  The corresponding commutator curvature vanishes identically only there and changes sign across the critical exponent.

---

## 2. Ideal logarithmic current family

Write

\[
\sigma=1+\kappa.
\]

The critical prime-winding measure `Lambda(q)/q` is asymptotically flat in logarithmic action coordinate `x=log q`.  Moving to exponent `1+kappa` multiplies the current by

\[
e^{-\kappa x}.
\]

Thus define the ideal action density

\[
\boxed{w_\kappa(x)=e^{-\kappa x}.}
\tag{2.1}
\]

Its cumulative current mass is

\[
\boxed{
A_\kappa(t)=\int_0^t e^{-\kappa x}\,dx
=\begin{cases}
(1-e^{-\kappa t})/\kappa,&\kappa\ne0,\\
t,&\kappa=0.
\end{cases}}
\tag{2.2}
\]

Define the quotient-history Volterra transport

\[
\boxed{
(J_\kappa f)(t)
:=\int_0^t e^{-\kappa x}f(t-x)\,dx
=\int_0^t e^{-\kappa(t-u)}f(u)\,du.}
\tag{2.3}
\]

The current mass and quotient transport are therefore generated by the same radial weighting.

---

## 3. Exact two-history kernel

The convolution of the exponential action kernel with itself is

\[
(w_\kappa*w_\kappa)(d)
=\int_0^d e^{-\kappa x}e^{-\kappa(d-x)}dx
=d e^{-\kappa d}.
\]

Hence

\[
\boxed{
(J_\kappa^2f)(t)
=\int_0^t(t-u)e^{-\kappa(t-u)}f(u)du.}
\tag{3.1}
\]

Thus the two-history continuation volume at displacement `d=t-u` is always the geometric path length `d` multiplied by the endpoint damping `e^{-kappa d}`.

---

## 4. Current-mass / history commutator curvature

Let `M_(A_kappa)` denote multiplication by `A_kappa(t)` and define

\[
\boxed{
D_\kappa
:=[M_{A_\kappa},J_\kappa]-J_\kappa^2.}
\tag{4.1}
\]

A direct calculation gives

\[
([M_{A_\kappa},J_\kappa]f)(t)
=\int_0^t e^{-\kappa(t-u)}
\bigl(A_\kappa(t)-A_\kappa(u)\bigr)f(u)du.
\]

Subtracting (3.1),

\[
\boxed{
(D_\kappa f)(t)
=\int_0^t e^{-\kappa(t-u)}
\left[
A_\kappa(t)-A_\kappa(u)-(t-u)
\right]f(u)du.}
\tag{4.2}
\]

Using the defining integral of `A_kappa`, this is equivalently

\[
\boxed{
(D_\kappa f)(t)
=\int_0^t e^{-\kappa(t-u)}
\left[
\int_u^t(e^{-\kappa x}-1)dx
\right]f(u)du.}
\tag{4.3}
\]

This is an exact curvature kernel, not an asymptotic expansion.

---

## 5. Unique flatness theorem

For `0<u<t`:

- if `kappa>0`, then `e^{-kappa x}-1<0` for `x>0`;
- if `kappa=0`, the integrand is identically zero;
- if `kappa<0`, then `e^{-kappa x}-1>0` for `x>0`.

Therefore the kernel in (4.3) has a strict sign off the diagonal:

\[
\boxed{
\begin{array}{ccl}
\kappa>0&:&D_\kappa\text{ has negative kernel};\\
\kappa=0&:&D_0=0;\\
\kappa<0&:&D_\kappa\text{ has positive kernel}.
\end{array}}
\tag{5.1}
\]

In particular,

\[
\boxed{
D_\kappa\equiv0
\iff\kappa=0
\iff\sigma=1.}
\tag{5.2}
\]

So the critical exponent is the unique zero-curvature phase of the ideal prime-current quotient geometry.

---

## 6. Meaning of the sign change

The bracket in (4.2) compares two quantities joining the same logarithmic endpoints `u<t`:

1. `A_kappa(t)-A_kappa(u)` — current mass contained between the endpoints;
2. `t-u` — geometric logarithmic displacement, equivalently the ideal two-history placement volume after the common exponential endpoint factor is removed.

For `sigma>1` (`kappa>0`), radial damping makes current mass smaller than geometric path length, hence negative curvature.

At `sigma=1`, current mass equals logarithmic displacement exactly:

\[
A_0(t)-A_0(u)=t-u,
\]

so one-step current transport and two-step history volume satisfy the flat Volterra identity

\[
\boxed{[M_T,J_0]=J_0^2.}
\tag{6.1}
\]

For formal `sigma<1`, radial weighting overemphasizes distant actions and the inequality reverses.

Thus `sigma=1` is not selected merely by convergence bookkeeping.  It is where the mass coordinate and logarithmic scale coordinate coincide.

---

## 7. Relation to the stable tau phase

The internal full-turn completion occurs at

\[
\sigma=2,
\qquad\kappa=1,
\]

through

\[
\mathcal Z(2)=\tau^2/6.
\]

This lies strictly in the negatively curved stable region of the current family.  Lowering the radial exponent from `2` to `1` removes the exponential damping until the current becomes flat in logarithmic scale and the partition current reaches its critical boundary.

The same family therefore has the phase picture

\[
\boxed{
\begin{array}{ccl}
\sigma=2&:&\text{stable quadratic completion carrying }\tau;\\
1<\sigma<2&:&\text{damped negative-curvature prime current};\\
\sigma=1&:&\text{unique flat, scale-invariant critical current}.
\end{array}}
\tag{7.1}
\]

No physical thermodynamic ontology is asserted; “phase/current/curvature” are typed mathematical descriptions of this one-parameter finite-history family.

---

## 8. Arithmetic finite curvature

At the arithmetic critical point define

\[
(Lf)(n)=\sum_{q\le n}\frac{\Lambda(q)}q f(\lfloor n/q\rfloor),
\qquad
A(n)=L1(n),
\]

and

\[
D=[M_A,L]-L^2.
\]

The ideal critical curvature is zero.  The arithmetic `D` is therefore the finite discrepancy from ideal flatness, generated by:

- atomic prime-power current rather than continuous flat current;
- floor quotient geometry;
- the bounded mass-gauge discrepancy `A(n)-log n`.

The exact stopped-fold formula from V18 makes this discrepancy concrete:

\[
Df(n)
=\sum_{ab>n}u_au_b f(q_a n)
-\sum_{ab\le n}u_au_b f(q_{ab}n),
\qquad u_a=\Lambda(a)/a.
\]

Thus the parity-fold carrier is literally the signed curvature of the critical prime current.

---

## 9. PNT zero phase revisited

V21 identifies a centered fourth-order positive energy `D4(r;n)` for the critical curvature, and V22 proves

\[
\mathfrak D_4(r;n)\to0
\]

through a quantitative real-variable completion.

The zero-curvature theorem above gives this convergence a sharper geometric meaning:

> prime-counting normalization is relaxation of the arithmetic critical current toward the unique flat logarithmic phase of the same prime-winding family whose stable quadratic phase reconstructs `tau`.

This is the strongest current structural statement connecting the pi-like completion to prime-distribution dynamics.

---

## 10. Correctness boundary

Proved exactly in the ideal derived logarithmic coordinate:

- exponential current family `w_kappa`;
- cumulative mass `A_kappa`;
- two-history kernel `J_kappa^2`;
- curvature kernel `(4.2)--(4.3)`;
- unique flatness and curvature sign change.

Reused exact finite arithmetic facts:

- critical weights `Lambda(q)/q`;
- `A(n)=log n+O(1)` pre-PNT;
- stopped-fold representation of arithmetic `D`;
- V21/V22 fourth-order zero-phase carrier and closure.

Not claimed:

- that the parameter `sigma` is a new physical spatial dimension;
- that `sigma=2 -> 1` is a literal temporal evolution of P000 space;
- a full six-dimensional native lift of this one-parameter derived coordinate;
- RH-scale decay or external novelty.
