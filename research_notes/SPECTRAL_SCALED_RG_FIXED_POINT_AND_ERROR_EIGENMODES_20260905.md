# Scaled spectral RG: exact completion fixed point and the Richardson error eigenmodes

Status: `FREE_RESEARCH / EXACT FORMAL-RG THEOREM-CANDIDATE / NOT_FOUNDATION`
Date: `2026-09-05`
Researcher: `EM-FREE-W59A / FREE_AXIOM_DISCOVERY`
Issue: `#1159`
Depends on:
- integer decimation semigroup `R_m`;
- common formal phase `ell` and inverse `U`;
- Jacobian identity `R_m'(u)=m^2 H_m(u)`.

## 1. Physical-scale decimation polynomials

Define

\[
\boxed{
P_M(t):=R_M\!\left(\frac{t}{M^2}\right).
}
\tag{RG-1}
\]

The common formal phase gives the exact formula

\[
\boxed{
P_M(t)
=U\!\left(M^2\ell\!\left(\frac{t}{M^2}\right)\right).
}
\tag{RG-2}
\]

Since

\[
\ell(s)=s+O(s^2),
\]

one immediately has, coefficientwise/formally and analytically on the identity-near branch,

\[
P_M(t)\longrightarrow U(t).
\]

Thus `U` is the scaled completion shape toward which every finite decimation polynomial converges.

---

## 2. Exact renormalization operators

For every integer `m>=1`, define an operator on formal maps `f(t)` by

\[
\boxed{
(\mathscr R_m f)(t)
:=R_m\!\left(f\!\left(\frac{t}{m^2}\right)\right).
}
\tag{RG-3}
\]

The semigroup law of `R_m` gives

\[
\boxed{
\mathscr R_m\mathscr R_n
=\mathscr R_{mn}
=\mathscr R_n\mathscr R_m.
}
\tag{RG-4}
\]

The finite scaled maps form an exact orbit:

\[
\boxed{
P_{mn}=\mathscr R_m P_n.
}
\tag{RG-5}
\]

Most importantly, the completion map is a common fixed point:

\[
\begin{aligned}
(\mathscr R_mU)(t)
&=R_m(U(t/m^2))\\
&=U(t).
\end{aligned}
\]

Hence

\[
\boxed{
\mathscr R_mU=U
\qquad(m\ge1).
}
\tag{RG-6}
\]

This is an exact fixed-point theorem, not an asymptotic analogy.

---

## 3. Phase-coordinate error is explicit

Because

\[
\ell(u)=\sum_{r\ge1}c_ru^r,
\qquad c_1=1,
\]

we have

\[
\boxed{
M^2\ell(t/M^2)
=t+
\sum_{r\ge2}
\frac{c_rt^r}{M^{2r-2}}.
}
\tag{RG-7}
\]

Therefore the entire finite-scale error is generated by applying the *same* nonlinear completion map `U` to an explicit positive phase perturbation.

The first perturbation term is

\[
\frac{t^2}{12M^2}.
\]

Expanding (RG-2) around `t` therefore gives

\[
\boxed{
P_M(t)
=U(t)
+
\frac{t^2U'(t)}{12M^2}
+O(M^{-4}).
}
\tag{RG-8}

Differentiating and using `P_M'(t)=H_M(t/M^2)` recovers the previously derived first determinant correction

\[
\boxed{
H_M(t/M^2)
=U'(t)
+
\frac1{M^2}
\left(
\frac{tU'(t)}6+
\frac{t^2U''(t)}{12}
\right)
+O(M^{-4}).
}
\tag{RG-9}

Thus the central-factorial modified equation is the derivative of the scaled-map RG expansion.

---

## 4. Exact linearization of the RG at the fixed point

Let `h(t)` be a formal perturbation.  The Fréchet/first-order linearization of (RG-3) at `U` is

\[
(D\mathscr R_m)_U h(t)
=
R_m'(U(t/m^2))\,h(t/m^2).
\tag{RG-10}
\]

Differentiate the exact fixed-point identity

\[
R_m(U(s))=U(m^2s):
\]

\[
R_m'(U(s))U'(s)=m^2U'(m^2s).
\]

Set `s=t/m^2`.  Then

\[
\boxed{
R_m'(U(t/m^2))
=
 m^2\frac{U'(t)}{U'(t/m^2)}.
}
\tag{RG-11}

Now define, for `r>=1`,

\[
\boxed{
h_r(t):=t^rU'(t).}
\tag{RG-12}

Substitution into (RG-10)-(RG-11) gives the exact eigen-relation

\[
\begin{aligned}
(D\mathscr R_m)_Uh_r(t)
&=
 m^2\frac{U'(t)}{U'(t/m^2)}
 \left(\frac{t}{m^2}\right)^rU'(t/m^2)\\
&=m^{2-2r}t^rU'(t).
\end{aligned}
\]

Therefore

\[
\boxed{
(D\mathscr R_m)_Uh_r
=m^{-2(r-1)}h_r.
}
\tag{RG-13}

The linearized spectrum is thus

\[
\boxed{
1,\ m^{-2},\ m^{-4},\ m^{-6},\ldots
}
\tag{RG-14}

with explicit common eigenfunctions `t^r U'(t)` for *every* integer-scale RG operator.

Classification: `EXACT_FORMAL_RG_LINEARIZATION`.

---

## 5. Why only even inverse powers appear

The fixed-point direction `r=1` has eigenvalue one.  It corresponds to changing the macroscopic phase normalization and is fixed by the normalization `U'(0)=1`.

All genuine irrelevant perturbations begin at `r=2`, with eigenvalues

\[
m^{-2},m^{-4},m^{-6},\ldots.
\]

Therefore any normalized finite approximation generated by the spectral RG has corrections only in even inverse powers of scale.

This is the structural origin of the `M^{-2s}` hierarchy already seen in:

- the central-factorial coefficient defects;
- WSR-T02;
- the modified-equation expansion;
- dyadic Richardson extrapolation.

It is not an accident of the first few coefficients.

---

## 6. Richardson annihilation is an RG spectral projector

For dyadic scale `m=2`, the irrelevant RG eigenvalues are

\[
4^{-1},4^{-2},4^{-3},\ldots.
\]

The previously derived annihilation operator

\[
\mathcal A_s
=
\prod_{r=1}^{s}
\frac{4^rE-I}{4^r-1}
\]

acts on scale sequences by comparing `q` and `2q`.

Equation (RG-13) shows that its factors are precisely the spectral projectors that vanish on the first `s` irrelevant RG eigencharacters

\[
q^{-2},q^{-4},\ldots,q^{-2s}.
\]

Hence:

\[
\boxed{
\text{Richardson annihilation}
=
\text{finite polynomial filtering of the linearized spectral-RG spectrum}.
}
\tag{RG-15}

This gives an invariant explanation of the extrapolation coefficients.  They are not chosen merely by matching an asymptotic table.

---

## 7. Single-scale phase truncation is the complementary nonlinear correction

The single-scale certificate hierarchy instead uses the exact phase perturbation (RG-7):

\[
M^2\ell(t/M^2)-t
=\sum_{r\ge2}c_rt^rM^{-2r+2}.
\]

Because all `c_r>0`, this perturbation admits explicit positive tails and gives algebraic one-scale brackets.

Thus the two precision mechanisms have a clean RG interpretation:

```text
single-scale formal-phase truncation:
  approximate the exact nonlinear phase perturbation before applying U

multiscale Richardson annihilation:
  project away successive irrelevant eigenmodes after scale evolution
```

They are complementary views of the same exact RG fixed-point geometry.

---

## 8. Finite determinant as the tangent RG observable

Since

\[
P_M'(t)=H_M(t/M^2),
\]

the finite determinant is the tangent/Jacobian observable of the finite RG map.

The completion determinant

\[
F(\sqrt t)=U'(t)
\]

is the tangent observable of the fixed point.

So the hierarchy can be summarized as

```text
finite decimation map P_M
  -> P_M -> U under scale refinement
  -> derivative P_M' = finite Dirichlet determinant
  -> derivative U' = completed sine-product function
  -> linearized RG eigenmodes t^r U'
  -> even inverse-scale corrections
  -> Richardson filters as spectral projectors
```

Freeze at free-research strength:

`U = COMMON_EXACT_SCALED_RG_FIXED_POINT`.

`M^-2s_CORRECTIONS = IRRELEVANT_SPECTRAL_RG_EIGENMODES`.

`RICHARDSON_FILTER = LINEARIZED_RG_SPECTRAL_PROJECTOR`.

`DIRICHLET_DETERMINANT = TANGENT_OBSERVABLE_OF_THE_FINITE_RG_MAP`.
