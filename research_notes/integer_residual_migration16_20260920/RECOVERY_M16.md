# Recovery M16

Parent objective: progressively migrate actual Enterprise Math / Nollm research code from floating decision semantics to integer plus typed residuals.

Verified local M16: existing `certified_hex.py` supports explicit power-of-two phase moduli through `2**32`; default 65536 behavior remains unchanged. 148 retained tests + 6 new tests pass, exact policy passes, and six finite uint32 synthetic populations certify all identities at 64 bits.

Do not redo M01-M14 arithmetic. M15 production-workbench/full-root-package admission is still blocked by connector-to-local byte materialization and is NOT accepted. Do not use the abandoned/manual workbench bridge or a test fixture as production evidence.

Next numerical unit: actual `phase32_lab.py` caller integration. Preserve its uint32 source and its declared axial lexicographic nearest-cell tie semantics; do not substitute the earlier q/r/s tie rule, do not infer exact pitch from float, and keep A2 observer distinct from native X6/P000. Start opt-in; default switch remains separate.
