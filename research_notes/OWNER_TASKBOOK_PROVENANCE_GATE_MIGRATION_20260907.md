# Taskbook provenance gate after physical V1 retirement

Auxiliary implementation package `/root/stability_research`; no formal task, claim, Result, publication, mathematical adoption or remote action. Base `16cc5663c191a73e997e3a973f589197d0694c89`, worktree `D:/em/integration-owner-control-20260907`, with the separately frozen owner integration delta. The original 22 source paths, seven fork-maintenance paths and independent fork audit retain their frozen bytes.

## Observed failure and authorized boundary

The actual `reference-integrity.yml` Python chain, using Python 3.12.14 on Windows, passed steps 1 through 18. Step 19, `python control_plane/check_v1_task_registry_fault_isolated.py`, failed at its import of the physically removed `tools.research_task_registry`. No later step was represented as executed. Initial logs and per-command exit codes are in `TEMP/owner-control-reference-chain-20260907/`; the failed chain is retained separately from the continuation after repair.

This was not a missing-bootstrap alias. The old wrapper also called the removed `audit_registry` and read the removed registry JSON. Current physical-isolation policy expressly bans those paths. Root authorized retirement of the V1 mirror field audit while preserving exact immutable publication provenance and the current strict error boundary. No legacy module, registry, writer, or runtime fallback has been restored.

Bounded caller search of workflow, control code, tools, tests and current docs found the old wrapper only in workflow step 19. It is removed; historical bytes remain at baseline Git blob `b031766fa55194285c3b1a883d647c12756ef5cb` (SHA256 `0269a7d6d5a7aae3f40ec0369442109cdc4fad9ae2f3ecac9e3120fb01134cad`). Two old tests called the previously removed predicate module rather than the wrapper. Their eight tests retain every assertion and fixture condition; only the import and Git-blob helper access move to the new current module. No compatibility implementation is duplicated.

## Retired and retained checks

The archived original implementation was read directly from `ce629e24e5af59128e25af87075c6622413684e0:tools/research_task_registry.py`, functions `retained_parallel_publication_ids` (59–83), `has_exact_v2_publication_authority` (86–133), and `audit_registry` (234–339).

Retired under the approved physical migration: loading the V1 mirror, its legacy schema/status/required fields, mirror-specific role/priority and V1 metadata checks. These are not replaced by fabricated empty data. The existing legacy migration manifest and its checker remain unchanged.

Preserved: the parsed taskbook domain `task_authority == PUBLISHED_REGISTERED` with `base_state` outside `DRAFT/BACKLOG`; exact task ID, repository-relative POSIX path and actual Git-blob identity; V2 record schema; ACTIVE/terminal state and standard V2 transaction, or explicitly validated historical provenance as described below; mandatory unified template existence/schema; strict record/body errors and exact existing isolation. Unparsed non-publication documents remain outside this inherited scan predicate; a referenced malformed taskbook still fails the strict record audit.

`has_exact_v2_publication_authority` preserves the former exact predicate. A caller-supplied retained-ID set is only a predicate input, not a public certificate. The public `audit(root)` derives retained IDs from validated canonical explicit selections, and performs the real strict record-error audit before accepting provenance. It never substitutes task-ID existence for path/blob identity.

The current `research_task_records_impl.audit` only detects one orphan class by task-ID existence. The new gate additionally catches a second book with the same task ID but no exact publication for its own path/blob. It does not weaken the existing record audit to implement this stronger consumer check.

## Three exact historical handovers

First execution of the new strict predicate exposed three actual historical nonstandard transactions. Their task IDs, paths and book blobs matched; their transaction qualification needed an explicit control interpretation. Root authorized these historical-source handovers, with no operational authority:

- `TP-0DE08FED8E3F3C9B` and `TP-FECD22B637D89CFD` must match the two exact existing `ALREADY_CURRENT_V2` rows in `control_plane/legacy_control_migration_manifest.json`, its approved schema/status/archive source, and fixed original record/book Git blobs. The source records are respectively `sha1:ebf870c747b4bd74a7211e39d099e53109118a0b` and `sha1:3a7d38967a6d07b111b905c8c7f9a04395754f1d`; their book pins are `sha1:10497cb4c43187ac1fc76bf22c3667407c2a9782` and `sha1:2d5fe1867191d05e905be5b719cc225a6fe312cd`. The complete path/identity tuples are `LEGACY_HANDOVERS` in the new checker. This is a two-object binding, not acceptance of arbitrary records sharing a migration transaction string.
- R043 publication `TP2-9D0A43C4F217B6E8C531` is historical evidence through membership in its currently validated exact `UNRESOLVED_PUBLICATION_FORK` head set. The checker still matches task ID, path, actual book blob, V2 schema and standard state. A lineage-forward row, arbitrary ID list, changed head set or enabled authority flag cannot enter this branch. The general existing exact-fork validator supplies the authority boundary; no new quarantine rule is created.

An existing audit-only row is usable only after both `validated_rows` and `audit_against` succeed against actual strict errors. It supplies an exact `(task_id, taskbook_path, actual Git blob)` source. Changed pins, lost nonoperational basis, stale declared errors and extra strict errors are still failures. No new audit-only suppression is registered here.

These handovers do not restore a resolution, claim, READY state, current publication, Foundation/Working Truth authority, successor trigger, or adoption of a mathematical result. The five fork tasks remain BLOCKED under their unchanged existing registrations.

## Validation and reproducibility

The new CLI ran with the existing bundled interpreter `C:/Users/Administrator/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe` (Python 3.12.14) and returned exit 0. This is Windows local validation of the workflow's Python commands, not a claim that GitHub's Ubuntu job has already run.

`python -m unittest tests.test_taskbook_publication_provenance tests.test_research_task_registry_v2_compat tests.test_v1_terminal_publication_provenance_unittest -v` with that interpreter: 21/21 PASS (13 new, eight preserved).

The new tests use real temporary taskbook/record/registry bytes and real existing record/fork/audit-only validators. Minimal fixtures stub only the global bootstrap installation, since unrelated semantic/review policy manifests are absent in those temporary roots; the actual repository CLI exercises full bootstrap. Tests cover same-ID/different-path books, task-ID/blob drift, orphans, unlisted nonstandard transactions, strict body/template failures, validated audit-only provenance, pin/basis/stale-error failures, extra strict errors, exact blocked forks, removed/changed/invalid fork entries, unsupported state, and the two fixed migration sources with manifest/record/book drift.

The workflow retains 34 Python steps in the original order; only step 19's description and command change to `python control_plane/check_taskbook_publication_provenance.py`. The remaining 15 entrypoints were statically checked for the same deleted-registry/scheduler import cause and did not contain it; that static check was not called a runtime pass. Per-step continuation logs after this repair are maintained outside the repository in `TEMP/owner-control-reference-chain-20260907/`, preserving the initial stopped chain.

## Owned paths and frozen non-note hashes

| Path | Change / SHA256 |
|---|---|
| `.github/workflows/reference-integrity.yml` | `40db1865f198680ffbca817a5229e126872c8cc4ad8a1135c086fe1c0f839458` |
| `control_plane/check_taskbook_publication_provenance.py` | `5f86de1e6b0a04ffb318cebe338dba4d2cd48c70216a5be02dbd9bc970c9af85` |
| `control_plane/check_v1_task_registry_fault_isolated.py` | deleted; baseline blob pinned above |
| `tests/test_taskbook_publication_provenance.py` | `7a26243d103f3ee0a130d56a9033552fb4dbfae84b9cb965d34653e152f64d1c` |
| `tests/test_research_task_registry_v2_compat.py` | `585a283c68f5e4d33ca2a6bb4ec6945e27a0bd5e908a0d7ee7b6323df46a524a` |
| `tests/test_v1_terminal_publication_provenance_unittest.py` | `cf1c7d6f8658b51dceb582ca20a58fefb19caaa8ff43150b63888434f3a6ae7f` |
| `research_notes/OWNER_TASKBOOK_PROVENANCE_GATE_MIGRATION_20260907.md` | own hash supplied separately |

Global-Knowledge-Sync: main@4fa7d7d / GLOBAL_KNOWLEDGE_V1
