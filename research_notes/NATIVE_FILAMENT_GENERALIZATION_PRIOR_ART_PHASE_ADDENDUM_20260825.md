# Native filament generalization: prior-art phase addendum (cyclotomy, sifted compacts, profinite dimension)

Status: `FREE_RESEARCH_PRIOR_ART_BOUNDARY / NO_EXTERNAL_NOVELTY_CLAIM`

Date: `2026-08-25`

Researcher-ID: `EM-FREE-NEPS-239A6D`

This addendum further narrows the novelty boundary after the odd-curvature, dual-parabola and transparent-basin generalizations.

## 1. Quadratic-residue translate intersections are classical cyclotomic numbers

The global breaker calculation can be rewritten as the intersection size of two affine images of the square set in `F_q`.

Cyclotomic numbers of order2 classically count intersections of the form

`|(1+C_i) intersect C_j|`

for the quadratic residue / nonresidue classes. Their exact values are well known:

- for `q=1 mod4`, the four order-2 cyclotomic numbers are combinations of `(q-5)/4` and `(q-1)/4`;
- for `q=3 mod4`, they are combinations of `(q-3)/4` and `(q+1)/4`.

Therefore the exact formula

`|I_0 intersect I_1|=[q+1+(B/q)+(-B/q)]/4`

must NOT be claimed as a new finite-field intersection theorem. It is an elementary/cyclotomic specialization.

The retained research-specific content is that the native/odd-sector shell geometry automatically produces the particular two quadratic images whose minimum-overlap condition is exactly the long-filament breaker condition.

## 2. Conic/parabola duality is classical

The Legendre/projective duality between a nondegenerate quadratic and its tangent-line conic is classical. Over finite fields, tangent/secant/internal/external point structure of conics is also classical.

Therefore the statements

- a quadratic is dual to a quadratic;
- tangent lines are parameterized by the Legendre dual;
- finite-field tangent counts are controlled by quadratic characters

are not novelty candidates.

The retained candidate is the exact identification of BOTH

`local sampled-tangent concurrence`

and

`global divisibility hit-set covering`

as the two sides of the same geometry-generated quadratic pair.

## 3. Multiplicatively split compact sieve sets are classical

Sieve literature explicitly considers choices of local subsets

`K_p subset Z/pZ`

and squarefree CRT products

`K_d ~= product_(p|d) K_p`,

viewing the coherent family as a multiplicatively split compact set / subset of a profinite product.

For example Olivier Ramaré's geometric-sieve exposition uses precisely this local-set/CRT compact language for sifted integer sets.

Therefore the exact product identity

`Theta_B(M)=product_(q|M) tau_B(q)`

and the existence of a compatible product set when every local factor is nonempty are classical CRT/product-set mechanisms.

The present local sets are unusually sparse (`tau_B(q)~q/4`) compared with ordinary finite-dimensional sieve sets, but the product formalism itself is not new.

## 4. Profinite compactness and Hausdorff-dimension tools are classical

Density of the integers in their profinite completion, inverse limits of finite quotients, Haar product measure, ultrametric Cantor constructions, Frostman/mass-distribution arguments and Hausdorff dimension in profinite groups are established theory.

Therefore

`Haar-null but full Hausdorff dimension`

for a specifically constructed product set is not by itself an external novelty claim.

The retained research-specific role is as a sharp infinite-dimensional phase readout of the sector/curvature breaker classification.

## 5. Analytic product asymptotic is classical once local factors are known

For no-break B, the local density has the form

`tau_B(q)/q = 1/4 * [1+(-3+chi_B(q)+chi_-B(q))/q]`

outside finitely many primes.

Mertens' theorem, convergence of prime sums for nonprincipal Dirichlet characters / nonvanishing of `L(1,chi)`, and the prime number theorem imply

`Theta_B(P_d)/P_d ~ C_B 4^(-d)/(log p_d)^3`.

This analytic step uses standard Euler-product technology and is not itself a novelty candidate.

## 6. Updated novelty candidate

After these downgrades, the strongest unresolved external-novelty candidate is NOT any isolated classical tool or counting formula.

It is the coupled selection theorem:

`cyclic shell sector count`

`-> central parity-curvature coefficient`

`-> locally measurable Poisson/curvature scalar`

`-> locked quotient-code family`

`-> one Legendre-dual quadratic pair`

`-> global breaker phase + local tangent-exception spectrum`

`-> sharp connectivity capacity`

`-> high-dimensional extinction/survival phase`.

For the actual tri-sector specialization:

`3 sectors`

`= local mean curvature3`

`= normalized Poisson source3`

`-> channels2,3 transparent`

`-> quadratic nonresidue at5`

`-> first breaker5`

`-> sharp prime-incidence cap9`,

while the same tangent family has its finite-window exceptional support ending at53.

The B=15 twelve-prime control witness independently confirms that the native 9-cap is breaker-phase specific rather than a universal artifact of the quadratic/parity code.

## 7. Promotion rule after this audit

Do not externally claim novelty for:

- order-2 cyclotomic numbers;
- quadratic residue intersection formulas;
- conic or Legendre duality;
- local CRT product basins;
- profinite compactness/full-dimension product-set constructions;
- Mertens/Dirichlet Euler-product asymptotics.

Any future theorem claim must be stated around the exact *coupling and selection* imposed by the native shell/incidence geometry, and must still undergo independent literature review.