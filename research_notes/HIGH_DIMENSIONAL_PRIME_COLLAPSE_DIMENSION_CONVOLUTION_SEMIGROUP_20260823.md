# 高维坍缩素数实验 E4 — 维数相加的卷积半群与非局部回声

Status: `FREE_RESEARCH / EXACT_EXPERIMENTAL_CARRIER_STRUCTURE / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-23`

Researcher-ID: `EM-FREE-HDPB-2308A9`

## 1. Exact dimension-convolution law

沿用

`W_{d,lambda}(n)=sum_s binom(d,s) A_s(n) lambda^s`

以及 generating series

`Theta_lambda(q)=1+lambda*sum_{a>=1} q^(a^2)`.

则

`W_{d,lambda}(n)=[q^n] Theta_lambda(q)^d`.

因此对任意 nonnegative dimensions `d,e`：

`Theta_lambda^(d+e)=Theta_lambda^d * Theta_lambda^e`,

从而精确得到

`W_{d+e,lambda}(n)=sum_{m=0}^n W_{d,lambda}(m) W_{e,lambda}(n-m)`.

Freeze:

`DIMENSION_ADDITION = ADDITIVE_SHELL_CONVOLUTION`.

在固定 lambda 下，dimension tower 是一个 convolution semigroup：

`W_{0,lambda}=delta_0`,

`W_{d+e,lambda}=W_{d,lambda} * W_{e,lambda}`.

## 2. 与 E3 lambda-semigroup 的双结构

E3 已有 support recoloring：

`T_lambda o T_mu = T_(lambda mu)`.

E4 新增：

`dimension addition -> sequence convolution`.

故当前 experimental carrier 有两个相容方向：

1. support-weight direction: `lambda * mu`;
2. dimension direction: `d + e` with convolution in shell readout `n`.

这比单独的 2D→19D table 更接近一个可计算的高维坍缩代数。

## 3. lambda=2 channel

令

`R_d(n)=W_{d,2}(n)`.

则精确有

`R_{d+e}=R_d * R_e`.

特别：

`R_10 = R_2 * R_8`,

`R_12 = R_4 * R_8`,

也有

`R_12 = R_6 * R_6`.

P5000 direct enumeration 对全部 `0<=n<=5000` 核验

`R_12(n)=sum_{m=0}^n R_4(m)R_8(n-m)`

零失败。

## 4. 10D angular echo 的卷积解释

E1 得到：

- local prime shell `R_2(p)` 对所有 `p==1 mod4` 只给相同 multiplicity 8，不能单独读出 `{a,b}`；
- local prime shell `R_8(p)=16(p^3+1)` 对所有 odd primes 也是 universal divisor wall；
- 但 `R_10(p)` 对 `p==1 mod4` 精确恢复 `p=a^2+b^2` 的 angular quantity `H4`，进而恢复 `{a,b}`.

由

`R_10(p)=sum_{m=0}^p R_2(m)R_8(p-m)`

可见该 extra information 不是由 `R_2(p)` 或 `R_8(p)` 的 **same-p local values** 携带，而是由整条 lower-dimensional sequences 在所有 decompositions

`p=m+(p-m)`

上的 additive correlation 聚合产生。

Freeze distinction:

`GLOBAL_SEQUENCE_GENERATED_BY_LOWER_DIMENSIONS != SAME_P_LOCAL_READOUT_DETERMINED_BY_LOWER_DIMENSIONS`.

这就是“低维信息在高维再次显影”的一个精确机制。

## 5. 12D semicircle residual 的卷积解释

E2 得到

`t_p=(R_12(p)-8(p^5+1))/(32p^(5/2))`

在 primes 上进入 semicircle/Sato-Tate distribution。

而 E4 给出

`R_12=R_4*R_8`.

因此 12D residual 可以看成：

- 4D divisor-wall sequence;
- 8D divisor-power-wall sequence;
- 在 additive split `n=m+(n-m)` 上的全历史 convolution；
- 减掉 smooth prime main term后留下的 normalized correlation residual.

这给项目内的无 modular-vocabulary 解释：

`4D/8D EXACT WALL LANDSCAPES -> ADDITIVE CONVOLUTION -> 12D DISTRIBUTED PRIME RESIDUAL`.

Phase-B classical theory可进一步把该 residual 识别为 modular/Sato-Tate object，但该识别不是建立 convolution mechanism 所必需的 premise。

## 6. Consequence for high-dimensional search

高维不应被理解为“每升一维产生一份独立的新信息”。

更准确：

- global dimension sequence is algebraically generated by lower-dimensional convolution;
- nevertheless, a **local shell at fixed n** can expose new effective invariants because it compresses the entire lower-dimensional history `m<n` into one coefficient;
- prime shells are therefore natural places to研究这些 additive-correlation residuals，而不是只看 raw multiplicity。

## 7. Current unified picture

`support spectrum A_s`

`-> lambda-weight/recolor semigroup`

`-> dimension-addition convolution semigroup`

`-> lambda=2 resonance`

`-> 4D sigma_1 wall`

`-> 8D sigma_3 wall`

`-> 10D = 2D * 8D nonlocal angular echo`

`-> 12D = 4D * 8D nonlocal semicircle residual`.

## 8. Verdict

Freeze:

`HDPB_E4_DIMENSION_ADDITION_CONVOLUTION_LAW_PROVED_ON_EXPERIMENTAL_CARRIER`.

`HDPB_E4_HIGH_DIMENSION_LOCAL_SHELL_IS_NONLOCAL_LOWER_DIMENSION_CORRELATION_READOUT`.

Next question:

> Can one define a canonical subtraction/quotient of lower-dimensional convolution-generated wall components so that the d=10 angular and d=12 distributed residuals become intrinsic “primitive residual coordinates” of the dimension tower?
