# Heartbeat76 — reciprocal matching, equilibrium obstruction and local resource delivery

Progress-Event-ID: HEARTBEAT76-RECIPROCITY-20260925
Status: SAME_AUTHOR_CONDITIONAL_MODEL / ACTUAL_TYPED_BRC / NOT_ADMITTED
Research-Activity-ID: none newly assigned; prior safety-blocked registration was not retried, bypassed or replaced by a closed identity. Evidence persistence does not confer role authority or admission.

## Scope and parent

Continue Stage75 cumulative3172593ebc7a32bbe5d549a699015b8bac3dae7a. Mounted parent bundle SHA256ecd7462e781871043d4621093b244167b7355c5310282f7125cc3a36e4dd7aa5 was verified before LOCAL cloning. Global read31553e131be119f2a02a45f60b5feab156bf49e4; Source reada27433478cfdc89ea350ec66a195d5507e998b44. Unchanged bootstrap/manual/project/P000/worldview/BRC-only blobs verified and complete loaded rules reused. Six native spatial axes, separate time, residual fidelity and material identity preserved.

This remains the Stage73 REAL rational residual-first branch. No rho/Kraus/Born sampler/old quantum propagator. Stage73 C1/C2 and Stage75 finite resource-slot identity lift remain conditional premises. Q is not calibrated energy; the identity is not a derived physical particle. Newly specified identity-controlled matching, prepared companion slots and independent finite auxiliaries are not consequences of C1/C2.

## 1. A real reciprocal load

Reuse the actual two-port matched closure(a,d)->(d,a). A local identity in incoming or bound state enables this exchange and swaps incoming/bound ownership; identity elsewhere leaves both residuals unchanged. XOR the new bound label into a report bit. All six ownership/report columns and the inverse are executed. Q=a²+d² and directed sum are preserved. No identity means no matched loading: this deliberately changes the previous passive-receiver constitutive rule.

The inherited actual twelve-port input(-1/2,-1/2,-1/2,+1/2,...,+1/2) emits(1,1,1,0,...). Three genuine native-neighbour receivers, initially empty, give exactly one identity record, exactly one stored resource1, and remaining field resource2 in each concrete microstate. All three identity positions occur with probability1/3 under the inherited uniform preparation. Full field/slot/ownership/report reversal restores the input. This is NOT exclusive absorption of totalQ3; it is selective reception of the LOCALQ1. Used slots and impossible zero-resource identity arrivals are not silently accepted.

## 2. Fixed-capacity fibre theorem: conditional uniformity is not automatic under backreaction

Let every exact field f have N finite allowed identity microstates Omega_f. Let T be a deterministic injective complete update with no new auxiliary state and the same output capacityN. Start from one SHARP field f with uniform identity. Suppose its N images occupy k_g states in output field fibre g. Then sum k_g=N, p_g=k_g/N, and the actual conditional identity law on g is uniform on that k_g-subset only.

Its exact total variation from replacing each output fibre by the FULLN-uniform law while retaining the same field marginal is
D_joint=sum_g p_g(1-k_g/N)=1-sum_g p_g².

Hence preserving the full conditional uniform law for EVERY sharp input field is equivalent to mapping each entire input fibre bijectively into one entire output fibre. Its output field cannot then depend on the identity. This is an elementary exact finite-fibre obstruction, NOT a theorem against all coarse Born-shaped position marginals, all joint equilibria, unequal capacities, open systems or other probability laws. FixedN corresponds here to fixed guidingQ and resource tick; apparatus modes are separately typed.

## 3. Matched companion slots generate an actual native-output witness

Take the actual generated active field(1,1,1,0,...), Qactive3,tick1/36,N108. Prepare THREE local internal companion slots with residuals(-1,-1,-1), Qapparatus3. Total active-plus-companionQ6 and total directed discrepancy0. Identity at portj enables only its existing matched swap(1,-1)->(-1,1). Both components are retained; the active sign reversal has a real apparatus ledger, not an injected quantum phase or erased source.

The three whole field-plus-companion states F_j occur with weight1/3. Conditional on F_j the identity is still atj, uniformly over36 ranks. Thus D_joint2/3. Continue the original full twelve-port closure and ordered local identity routing, then one actual native edge. For F_j the outgoing residuals are7/6 atj,-5/6 at the other two active ports, and1/6 at the remaining nine. Actual complete microstate propagation gives
Ptrue=(1/3,1/3,1/4,1/108,...,1/108).
Incorrect conditional re-uniformization on the same actually propagated fields gives
Pwrong=(11/36,11/36,11/36,1/108,...,1/108).
Third-port1/4 differs from11/36; full outputTV1/18. This is an exact model contrast, not data or a universal rotationally invariant prediction. Original ordered port convention remains explicit. Every actual field/identity/native inverse is checked. An unequal active field(1,2,2), tick1, has p=(1,4,4)/9 and D_joint16/27.

## 4. A minimal finite auxiliary repairs this example; reuse reintroduces history

Let maskm encode the three active signs and retain companions by their EXACT relationd=-a; no energy is erased. Let p denote the identity port, z a prepared independent uniform THREE-state auxiliary, and r the retained0..35 subrank. The actual local permutation is
(m,p,z,r)->(m xor2^p,z,p,r).
It consists of real matched feedback and same-Cell exchange of the identity port microstate with a nonmaterial auxiliary label. All72 finite columns, the matched physical implementation and full inverses are executed via canonical BRC. Output identity is uniform conditional on the feedback field, and actual later native outputs equal Pwrong above: here the change is a NEW physical operation, not probability editing. The used auxiliary stores oldp and is correlated with the field.

If g has k_g old labels and a later field-preserving deterministic repair uses an independent auxiliary with R possible values, identity support is at mostk_gR. Exact fullN-support needsR>=ceil(N/k_g); for equal-probability auxiliary states alsoN dividesk_gR. Herek_g36,N108, soR>=3 and the construction attains the bound. This is an information-support cost, not a thermodynamic energy bound.

Two local feedback contacts with the SAME auxiliary give m2=2^p0 xor2^z0,p2=p0. Two contacts using separately prepared independent auxiliaries have the same FIELD marginal, but different identity correlations. Given mask3=(-1,-1,+1), an event of probability2/9 in both, P(port2)=0 for reuse and1/3 for fresh. JointTV2/9. Following the SAME actual native closure/routing, third-output probabilities are1/4 versus25/108 and complete outputTV1/54. Every used auxiliary is retained. The complete two-auxiliary inverse restores initial values.

The theorem does not forbid a special closed equilibrium: uniform input on all8sign masks and3identity ports is preserved by the24-state feedback permutation. That preparation is not the original fixed positive field; it cannot be substituted without changing the source.

## 5. Finite causal resource delivery bound

In the separate local-load protocol there are no companion actuators: total incoming guidingQ3 is split as1 at each of three native neighbours. One stored unit is at the identity-selected detector; remainingQ2 is retained in the field. For source-free local resource-conserving gates plus at most one native transport edge per layer, nonnegative additive resource obeys
Q_D(t)<=sum_(x in N_t(D))Q_x(0).
All internal stores and external supplies must be included. Proof is the stepwise backward causal neighbourhood and local conservation, not an independent simulator or a general axiom forbidding other global models.

The three neighbour cells have pair distance2. Therefore the selected Cell has total availableQ<=1 for layers0,1, and only from layer2 isQ3 even causally possible. Actual leftover field propagation gives no incoming additionalQ at layer1; at layer2 its passing resource at the selected cell is101/1296,101/1296 or17/216 depending on position. This is passing field, not new absorbed energy or a new click. Total leftoverQ2 and its exact native inverse are verified. No instantaneous collection of the entire distributed pulse is claimed. A powered amplifier or identity-carried extra energy would be a new ledger, not remote withdrawal of the original field.

## Execution, artifacts and limits

Final fresh process321 named checks,18 actual unchanged canonical BRC core calls.216 checks are paired inverses/ledgers for108 related identity microstates,24 check the actual companion gate against its mask representation; counts are not independent experiments. Complete proofs supply all-fibre/support/causal claims. Canonical corebc7babbb9e890f6d5a7094430a5fbdccf66c77ad:src/enterprise_math/brc_weighted_recurrent.py,blob4e6b3132580e3cd70a20a0d8bd4d28792b961afb,SHA2567520822074b8f29e1c54f57b9543c043202db8bd7c6e27f3f1d6176028ac4a26. No trigonometry,eigensolver,numerical optimizer or substitute quantum propagator.

An early sign-only prototype omitted the explicit directed-discrepancy apparatus ledger. Final implementation replaced it by actual prepared companion matching and verified both signed and quadratic balances. This constitutive refinement is documented; no inherited result was edited. Only the final complete process is reported.

Cumulativea1621311040f438d848d6766e5e5ec2b67b5ca1f; entrySTART_HERE_STAGE76.md. Filesstage76/PROOF.md,reciprocal.py,run.py,RESULTS.json,BRC_CONTRACT.json,PROVENANCE.json,IMPLEMENTATION_NOTES.md,MANIFEST.json. Fresh LOCAL bundle-clone replay matches all science fields, excluding ONLYelapsed_ns recursively. Original2628358733ns and replay2507509215ns retained.1364 inherited files byte-identical,8manifest entries verified,git fsckPASS,cleanworktree.

BundleBRC_Heartbeat_reciprocal_reading_stage76_20260925.bundle,38326608bytes,SHA256742ad53afcd824078527e11ac3ff04fcdc2c179eee0fece7e75b4fe5a463488b. Drive1sRsce5e-88sN2tLG2zrPqA19cOVoU8SG uploaded,metadata and raw bytes fetched,local cmp/hashIDENTICAL. ProofSHA256b8eef0d8516ee8927035787c4aecaf37c9c5e0189e5d29af5f36ac6c48b509ad;code6d0db701b4a38becd97d9fe1e72328d27f5bdb5829fe8cde1010da49b281252f;resultsa8a8a3734340c00de9c2d18f85571241f6b4eb4c70938696b6a595bdcc360417. Local delivery/mnt/data/heartbeat76/DELIVERY.json.

Primary official abstracts/metadata read: Duerr/Goldstein/Zanghi quant-ph/9512031v1,Goldstein/Struyve0704.3070v1,Bennettphysics/0210005v2. Backreaction/equilibrium/reversible-record questions have prior literature; no first-discovery or exhaustive novelty claim. No dedicated-provider result, independent review,Lean,physical fit or remote deployment.

NEXT: specify a physically preparable source/actuator/identity joint equilibrium or a controlled departure law for fixed coherent inputs, and reconcile single-site record with actual local energy delivery. Do not rename an auxiliary uniform prior as derived randomness, or this complete-microstate theorem as a no-go for all Born marginals. Native phase/tensor/Born origin, autonomous timing, preparation/recycling and empirical calibration remain unresolved. Broad parent physics and activity registration are not complete.
