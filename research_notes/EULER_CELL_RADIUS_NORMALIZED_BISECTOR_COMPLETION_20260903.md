# Euler rotation from the C3 right turn, Cell-radius bisectors, and unit-speed completion

Status: `FREE_RESEARCH / FINITE CORE CLOSED / ANALYTIC COMPLETION TYPED / NOT FOUNDATION`  
Date: `2026-09-03`  
Researcher: `EM-FREE-F6D046 / FREE_AXIOM_DISCOVERY`  
Author/program signature: `YUAN X / Enterprise Math`

## 0. Result

The previously separate ingredients

- three positive-ray right-turn transport,
- the six oriented line directions,
- the six Cell transition gates,
- the quarter-turn symbol `i`,
- the Viète square-root tower,
- and the continuous Euler flow

can be organized by one exact bisector construction.

Let `R` be the positive cyclic right-turn on the nontrivial two-dimensional representation of the three ray labels. Then

\[
R^3=I,
\qquad
I+R+R^2=0.
\]

Define

\[
G=I+R.
\]

Then

\[
G^2=R,
\qquad
G^3=-I,
\qquad
G^6=I.
\]

Thus the six oriented direction states and endpoint reversal are generated internally by the three-ray right turn; an unrelated external reversal bit is not required at the representation level.

Let the current Cell radius be

\[
r=\frac1{\sqrt3}.
\]

Define the adjacent-state bisector

\[
H=r(I+G)=r(2I+R).
\]

Then

\[
H^2=G,
\qquad
H^6=-I,
\qquad
H^{12}=I.
\]

Its even powers are the six oriented direction states and its odd powers are their six intervening gate phases. The quarter-turn is

\[
J=H^3,
\]

and satisfies the exact identities

\[
\boxed{J=r(R-R^{-1})},
\qquad
\boxed{J^2=-I}.
\]

Therefore the complex orientation generator is not a primitive spatial axis. In this carrier/readout theorem it is the Cell-radius-normalized chiral difference between forward and backward native right-turn transport.

## 1. Typed setting

This note uses only the following current inputs.

1. The established three-axis research slice has three positive ray families cyclically permuted by a native right turn.
2. The pivot Cell has six oriented nearest-neighbor direction classes and six triple-intersection gates in alternating cyclic incidence.
3. The Cell radius is `1/sqrt(3)` at nearest-center spacing one.
4. A direction is a property of an ordered segment or transition germ, not of one isolated homogeneous Cell.
5. The matrix/character plane below is a derived representation. It is not the primitive Enterprise metric plane and is not identified with the P000 six native axes.

Freeze:

`C3_RIGHT_TURN_REPRESENTATION != PRIMITIVE_NATIVE_VECTOR_IDENTITY`.

`COMPLEX_STRUCTURE_OPERATOR != NATIVE_SPATIAL_AXIS`.

`CELL_RADIUS_NORMALIZATION_IS_A_CARRIER_CHARACTER_COMPATIBILITY`.

## 2. Integral model of the three-ray right turn

Use the exact integral matrix

\[
R=
\begin{pmatrix}
0&-1\\
1&-1
\end{pmatrix}.
\]

Then

\[
R^2=
\begin{pmatrix}
-1&1\\
-1&0
\end{pmatrix},
\qquad
R^3=I,
\qquad
I+R+R^2=0.
\]

The positive definite Gram form

\[
Q=
\begin{pmatrix}
2&-1\\
-1&2
\end{pmatrix}
\]

is invariant:

\[
R^TQR=Q.
\]

This is the standard two-dimensional augmentation representation of the cyclic permutation of three labels. No Euclidean angle and no numerical value of `pi` is used.

## 3. The six-state rotor is already inside C3

Set

\[
G=I+R
=
\begin{pmatrix}
1&-1\\
1&0
\end{pmatrix}.
\]

Using `I+R+R^2=0`,

\[
G^2=(I+R)^2=I+2R+R^2=R,
\]

and hence

\[
G^3=RG=R+R^2=-I.
\]

Consequently

\[
G^6=I.
\]

Moreover,

\[
G^TQG=Q.
\]

Therefore `G` is an exact order-six isometry whose square is the native three-ray right turn and whose cube is endpoint reversal.

This replaces the earlier provisional wording

\[
C_3\times C_2\cong C_6
\]

by a stronger representation statement:

\[
\boxed{
\text{the reversal element }-I\text{ and the six-direction rotor }G
\text{ are generated by }R\text{ itself}.
}
\]

The abstract group factorization remains true, but the `C2` endpoint is not an independent ad hoc input in this representation.

## 4. Why the Cell radius produces the gate rotor

The sum of two adjacent six-direction states is represented by

\[
I+G=2I+R.
\]

Its Gram dilation is exactly three:

\[
(I+G)^TQ(I+G)=3Q.
\]

Hence a positive scalar `a` makes `a(I+G)` a `Q`-isometry exactly when

\[
3a^2=1.
\]

The unique positive solution is

\[
\boxed{a=\frac1{\sqrt3}=r_{\rm Cell}}.
\]

Thus the current Cell radius is exactly the unique positive normalizer of the first gate bisector.

Define

\[
H=r(I+G).
\]

Since

\[
(I+G)^2=3G,
\]

one obtains

\[
H^2=r^2(I+G)^2=\frac13\,3G=G.
\]

Therefore `H` is the short square root of the six-state rotor. It has order twelve:

\[
H^6=G^3=-I,
\qquad
H^{12}=I.
\]

The twelve distinct powers interleave as

\[
H^{2j}=G^j,
\qquad
H^{2j+1}=HG^j,
\qquad 0\le j<6.
\]

The even powers are the six oriented direction phases; the odd powers are their intervening gate phases. This matches the actual alternating Cell/gate incidence cycle.

## 5. The quarter-turn operator

Define

\[
J=H^3.
\]

Because `H^2=G`,

\[
J=HG=r(I+G)G=r(G+G^2).
\]

Using `G=I+R` and `G^2=R`,

\[
G+G^2=I+2R.
\]

The relation `I+R+R^2=0` gives

\[
I+2R=R-R^2=R-R^{-1}.
\]

Therefore

\[
\boxed{J=r(R-R^{-1})}.
\]

Also

\[
(R-R^{-1})^2=-3I,
\]

so

\[
\boxed{J^2=-I}.
\]

This is the exact geometric role of the orientation symbol `i` in the present carrier theorem:

\[
\boxed{
i
\quad\longleftrightarrow\quad
\frac1{\sqrt3}
(\text{forward right turn}-\text{backward right turn}).
}
\]

Reflection exchanges `R` with `R^{-1}` and therefore sends `J` to `-J`. The endpoint `-I` is reflection invariant. Hence chirality is required to choose `i` rather than `-i`, while Euler's half-turn endpoint does not depend on that choice.

## 6. Operator `i` and state `i`

Two assertions must be distinguished.

1. On the coarse three-ray representation, `J` already exists as the normalized chiral operator satisfying `J^2=-I`.
2. In the finite orientation-state group `C6`, no element has order four. A quarter-turn *state* first appears in the gate refinement `C12`.

The gate rotor realizes the operator as a state:

\[
J=H^3.
\]

Thus

\[
\boxed{
\text{operator }i\text{ is latent in the C3 right turn, while state }i
\text{ first appears at the Cell-gate layer}.
}
\]

## 7. General normalized-bisector theorem

Let `A` be a two-dimensional real commutative rotation algebra with involution `*`. Let `U` be a unit element,

\[
U^*U=1,
\]

and suppose `U\ne-1`. Since `U+U^{-1}` is scalar, define

\[
\beta(U)=
\frac{1+U}{\sqrt{2+U+U^{-1}}},
\]

using the positive scalar square root whenever the denominator is positive.

Then

\[
\beta(U)^*\beta(U)=1
\]

and

\[
\boxed{\beta(U)^2=U}.
\]

Indeed,

\[
(1+U)^2
=U(2+U+U^{-1}).
\]

In a complex-like two-dimensional field, the two square roots are

\[
\pm\beta(U).
\]

The one with positive scalar component is unique. Therefore, after a chirality choice resolves the singular first root of `-1`, every subsequent forward/short square root is canonical.

For the current tower:

\[
G=\beta(R),
\qquad
H=\beta(G).
\]

The first normalization is one because `2+R+R^{-1}=1`; the second is the Cell radius because `2+G+G^{-1}=3`.

This closes the principal finite bridge that had remained open:

\[
\boxed{
\text{dyadic refinement is repeated normalized adjacency bisecting, not an imported angle-halving rule}.
}
\]

## 8. Unique invariant norm after `J` is constructed

Let the real character algebra be

\[
A_J=\{aI+bJ:a,b\in\mathbf R\},
\qquad J^2=-I.
\]

Let `q` be a positive quadratic form with

\[
q(I)=1,
\qquad
q(Jx)=q(x).
\]

Then `q(J)=1`. If `B` is the polar bilinear form, `J`-invariance gives

\[
B(I,J)=B(J,-I)=-B(J,I).
\]

By symmetry of `B`,

\[
B(I,J)=0.
\]

Hence

\[
\boxed{q(aI+bJ)=a^2+b^2}.
\]

Therefore the unit character conic is not an arbitrary ellipse once the normalized chiral operator and the unit identity are fixed. The standard norm is forced.

This is a representation theorem, not an identification of the character plane with the native Enterprise metric plane.

## 9. The dyadic root tower

Choose the chirality for which the first root of reversal is

\[
U_1=J.
\]

Define recursively

\[
U_{n+1}=\beta(U_n).
\]

Then

\[
U_{n+1}^2=U_n,
\qquad
U_n^{2^n}=-I,
\qquad
U_n^{2^{n+1}}=I.
\]

Write

\[
U_n=c_nI+s_nJ,
\qquad
c_n^2+s_n^2=1,
\]

with the forward branch `c_n,s_n>0` for `n>=2`. The bisector theorem gives

\[
\boxed{c_{n+1}=\sqrt{\frac{1+c_n}{2}}},
\]

and

\[
\boxed{s_n=2c_{n+1}s_{n+1}}.
\]

Thus the Viète nested radicals are the scalar traces of repeated normalized adjacency bisectors.

## 10. An internally scaled completion constant

Define

\[
\Pi_n=2^ns_n
=\frac{2}{\prod_{j=2}^n c_j}.
\]

The existing finite theorem gives

\[
\Pi_n<\Pi_{n+1}
\]

and an algebraic upper bound, so

\[
L=\lim_{n\to\infty}\Pi_n
\]

exists as a finite positive constant without importing classical `pi`.

The value `L` is not an arbitrary later calibration. It is the unique scale for which the dyadic root tower has unit infinitesimal speed.

Indeed,

\[
\frac{U_n-I}{L/2^n}
=
\frac{2^n(c_n-1)}L I
+
\frac{2^ns_n}L J.
\]

Since

\[
2^ns_n=\Pi_n\to L
\]

and

\[
1-c_n=\frac{s_n^2}{1+c_n}=O(4^{-n}),
\]

we obtain

\[
\boxed{
\lim_{n\to\infty}
\frac{U_n-I}{L/2^n}=J.
}
\]

Thus the same finite tower determines both its half-period scale `L` and its unit-speed generator `J`.

## 11. Dyadic character and continuous extension

Let

\[
D_L=\left\{\frac{kL}{2^n}:k\in\mathbf Z,n\ge1\right\}.
\]

Define

\[
E_0\left(\frac{kL}{2^n}\right)=U_n^k.
\]

The root compatibility makes this well defined. It is a group homomorphism on the dyadic subgroup:

\[
E_0(x+y)=E_0(x)E_0(y).
\]

Moreover `U_n->I`, and the exact norm together with the bound above gives continuity at zero. Hence `E_0` extends uniquely to a continuous homomorphism

\[
E:\mathbf R\longrightarrow\{aI+bJ:a^2+b^2=1\}.
\]

The preceding difference quotient fixes its generator:

\[
E'(0)=J.
\]

By the standard uniqueness theorem for a continuous one-parameter subgroup in a finite-dimensional algebra,

\[
\boxed{E(t)=\exp(tJ)}.
\]

Also

\[
E(L)=-I,
\qquad
E(2L)=I.
\]

The compatible *entire* root tower removes higher winding aliases: for every `n`,

\[
E(L/2^n)=U_n
\]

is the forward short root. Under the standard identification `J=i`, the unique unit-speed half-period is therefore

\[
\boxed{L=\pi}.
\]

This analytic identification is downstream of the finite construction; the finite construction itself defines `L` without using the target value of `pi`.

## 12. Euler formula

In the basis `I,J`, write

\[
E(t)=C(t)I+S(t)J.
\]

Since `E(t)=exp(tJ)` and `J^2=-I`,

\[
E(t)=\cos t\,I+\sin t\,J.
\]

Under `J=i`,

\[
\boxed{e^{it}=\cos t+i\sin t}.
\]

At the internally determined half-period `L=pi`,

\[
\boxed{e^{i\pi}=-1}.
\]

The geometric meaning is now:

\[
\boxed{
\text{unit-speed completion of Cell-radius-normalized chiral right-turn transport}
\text{ through one half-period equals endpoint reversal}.
}
\]

The constants play distinct typed roles:

- `1`: identity orientation;
- `-1`: endpoint reversal;
- `i`: normalized chirality operator / first gate-level quarter-turn;
- `e`: continuous multiplicative composition law;
- `pi`: the internally scaled first half-period of that unit-speed completion.

## 13. Sharp no-go statements

### 13.1 No chirality, no canonical sign of `i`

Reflection fixes `-I` and exchanges `J` with `-J`. A static unoriented carrier cannot choose between them.

### 13.2 Finite cyclic data alone do not choose a real numerical period

Without the unit-speed normalization, any positive rescaling of the real parameter gives the same abstract finite roots. The limit `L=lim 2^n s_n` supplies the missing scale.

### 13.3 The local Cell/gate geometry stops at twelve one-step states

The first refinement `C6 -> C12` is physically realized by actual gate incidence. Deeper exact roots require transition-history states or another proved physical carrier.

### 13.4 No current proof identifies all native trajectories with this phase tower

The ordered primitive neighbor-pair quotient supplies a restricted operation-safe six-direction source. A global factorization theorem for every admissible Cell trajectory remains open.

### 13.5 No six-gate explanation of the scalar exponent six in the N=58 defect product

The factor `(sqrt(2)-1)^6` is an ordinary scalar power, not automatically sixfold Cayley composition. No identification with the six gates is asserted without an additional theorem.

## 14. Status

Finite algebraic core:

`C3_RIGHT_TURN -> C6_ROTOR -> CELL_RADIUS_NORMALIZED_C12_GATE_ROTOR -> J^2=-I`

is exact and executable.

The normalized-bisector recursion and target-free completion constant are exact. The extension to a continuous one-parameter subgroup uses standard real analysis/topological-group completion. Identification of the internally defined unit-speed half-period with classical `pi` uses the standard characterization of `pi` in the real complex exponential.

Freeze candidate:

`AC-EM-FREE-F6D046-EULER-CELL-RADIUS-BISECTOR-COMPLETION-V1`:

> The three-ray right-turn representation internally generates the six oriented directions by `G=I+R`. The unique positive normalization of the adjacent six-direction sum is the current Cell radius `1/sqrt(3)`, producing the twelve Cell/gate rotor `H=(I+G)/sqrt(3)`. Its cube is the chiral operator `J=(R-R^{-1})/sqrt(3)` with `J^2=-I`. Repeated forward normalized bisectors define the Viète root tower and an internal half-period constant. Its unit-speed continuous completion is the Euler flow, and the half-period is classical `pi`.

Status: `FINITE_CORE_EXACT / PYTHON_EXACT_CHECKED_PENDING_CI / ANALYTIC_COMPLETION_STANDARD / GLOBAL_NATIVE_TRAJECTORY_LIFT_OPEN`.
