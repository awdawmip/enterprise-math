# X6 native axis-permutation rotation skeleton：S6 与 20 个三轴 slice

Status: `DERIVED / EXACT AXIS-PERMUTATION SYMMETRY / NOT CLAIM COMPLETE ROTATION GROUP`
Date: `2026-09-05`
Depends on: `X6_NATIVE_COORDINATE_COMPLETION_V7_20260905.md`

## 1. Full native coordinate candidate has 20 three-axis selections

P000 states that the full space has six native spatial axes and the familiar three-axis model is a selected 3-of-6 research slice. After the direct user correction, every distinct pair of native axes is Enterprise-orthogonal and the native right angle is 120 degrees.

Therefore at the native coordinate-label level there are

`binom(6,3)=20`

three-axis selections.

The current FCC carrier atlas realizes four close-packed triangular three-line slice types particularly directly. Those four are a **carrier-selected subatlas**, not the complete set of native 3-axis coordinate selections.

## 2. Exact S6 action on the six-coordinate completion

On

`G6_D=Z^6/Z(1,...,1)`

any permutation `sigma in S6` acts by

`[z_1,...,z_6] -> [z_{sigma^{-1}(1)},...,z_{sigma^{-1}(6)}]`.

This is well-defined because the all-ones diagonal is invariant.

On the min-zero section `A6_D`, simply permute the six entries; minimum zero is preserved, so no recanonicalization is required.

Every such permutation:

- permutes the six distinguished positive-axis generators;
- preserves the canonical adjacency graph generated by axis increments/inverses;
- preserves the P000 quadratic component readout `sum_i n_i^2`;
- permutes the 20 three-axis slice projections by `S -> sigma(S)`;
- transports hidden kernels `ker(pi_S)` to `ker(pi_sigma(S)`;
- transports triangle holonomy `H_S` to `H_sigma(S)`.

Thus

`S6 <= Aut_axis(G6_D,A6_D,component_norm)`.

Conversely an automorphism required to permute the distinguished six positive-axis generator set is uniquely one of these six-axis permutations. Hence the **positive-axis permutation automorphism group** is exactly

`Aut_positive_axis = S6`, order 720.

This does not claim the complete native rotation group has only 720 elements. It identifies the exact finite skeleton that permutes native axis labels while preserving the coordinate completion.

## 3. Local C3 is naturally contained

Fix any selected 3-axis slice S. The setwise stabilizer of S in S6 is

`S3_visible x S3_hidden`, order 36.

The visible `S3` permutes the three native axes of the selected slice. Its alternating subgroup

`A3 ~= C3`

is precisely the native 3-cycle used by the existing three-axis orientation observer.

So the previously successful local C3 is not an isolated accident: it sits canonically inside the full six-axis permutation skeleton for every one of the 20 slices.

## 4. Carrier FCC S4 sits inside native S6

Use the current K4 edge labeling of the six axes:

`AB,AC,AD,BC,BD,CD`.

Every K4 vertex permutation induces a permutation of its six edges, giving a faithful embedding

`S4 -> S6`.

This 24-element subgroup is exactly the previously derived FCC six-line atlas permutation group.

Inside the 20 native three-axis selections, the carrier S4 splits the triples into three orbits:

1. 4 K4 stars — the currently selected close-packed FCC slice types;
2. 4 K4 triangles/faces — complements of the stars;
3. 12 K4 three-edge paths — the remaining native 3-axis selections.

Orbit sizes are `4+4+12=20`.

Hence the old fact “FCC exposes four overlapping 120-degree slices” is compatible with, but much smaller than, the full native 20-slice coordinate atlas.

## 5. Interpretation of rotations beyond the FCC subatlas

An `S6` permutation outside the carrier `S4` generally sends a currently close-packed FCC star slice to a different 3-axis selection not represented as one of the four fixed FCC close-packed charts.

This is not a contradiction. P000 explicitly allows rotation to change which native spatial coordinates are visible in a chosen slice. The fixed FCC atlas is a coordinate/readout carrier, not the full native identity.

Therefore a useful typed hierarchy is

`FULL_NATIVE_AXIS_PERMUTATION_SKELETON = S6`

`FCC_ATLAS_PRESERVING_SUBGROUP = S4`

`SELECTED_SLICE_VISIBLE_CYCLIC_ROTATION = C3`.

No classical carrier rank is used in this derivation.

## 6. Rotation and the local hidden fibre

For a slice S,

`ker(pi_S) ~= Z^3`

is generated by the omitted three axes.

The stabilizer `S3_visible x S3_hidden` acts separately on visible and hidden triples. A rotation moving S to S' conjugates the exact sequence

`0 -> Z^3_hidden(S) -> G6_D -> G_S -> 0`

into the corresponding exact sequence for S'.

Thus “rotate -> select slice -> observe” has an exact discrete algebraic implementation at the coordinate-candidate level.

## 7. Self-check targets

A finite exact checker should verify:

- all 720 permutations preserve `can6`, axis adjacency and norm;
- action composition is exact;
- all 20 slices form one S6 orbit;
- the K4 edge-action subgroup contains exactly 24 elements;
- under that S4, slice orbit sizes are exactly `4,4,12`;
- every visible C3 generator embeds in the corresponding slice stabilizer;
- hidden kernel rank/count basis transports correctly.

These checks test the implementation; the group-theoretic statements above are direct finite proofs.
