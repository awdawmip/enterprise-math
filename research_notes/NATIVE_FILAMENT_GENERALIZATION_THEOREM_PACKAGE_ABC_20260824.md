# Native filament generalization theorem package A/B/C — pre-audit record

Status: `SUPERSEDED_BY_POST_AUDIT_V2_STATEMENT_FREEZE / PROVENANCE_ONLY`

Date: `2026-08-25`

Researcher-ID: `EM-FREE-NEPS-239A6D`

This file was the original A/B/C parameterized package written before blind independent audit PR #631 completed.

The authoritative current statement layer is now:

`research_notes/NATIVE_FILAMENT_COUPLED_SELECTION_POST_AUDIT_V2_STATEMENT_FREEZE_20260825.md`.

Independent audit return:

`research_returns/NATIVE_FILAMENT_COUPLED_SELECTION_INDEPENDENT_AUDIT_RETURN_20260825.md`

Verdict:

`PACKAGE_VERIFIED_WITH_NARROWING`.

## Surviving package roles

### A. Integral affine-arrangement specialization

Exact and useful, but the general characteristic-quasi-polynomial / coefficient-monotonicity framework is classical. Retain determinant-depth formulas as explicit specializations, not novelty claims.

### B. Locked finite-quotient / affine-MDS bridge

The family-specific quotient/cardinality formulas survive. For `M>2`, the exact effective shell period is

`lcm(2,M/gcd(B,M))`.

At `M=2`, after fixing the intercept, the exact/minimal effective shell period is `1`; the total code still has exactly two words.

Fixed-chirality good-prime sheets flatten to affine `[k,2,k-1]` Reed--Solomon/MDS cosets.

### C. Quadratic transparency / breaker theorem

The character-sum formula and CRT principle survive exactly. The character-sum machinery is classical; the model-specific connectivity coupling remains the research content.

## Audit narrowings that supersede the original text

1. `M=2` effective period is `1`, not the unqualified `lcm(2,M/gcd(B,M))` reading.
2. Dual-parabola geometry must use the chirality-dependent family
   `Q_e^(chi)(x)=x^2/(2B)-chi e/2`.
3. Mixed-parity concurrence iff requires the same-parity slope difference to be a q-adic unit; the finite-window condition `q>k-1` is sufficient.
4. Sharp values `1,5,9` are breaker-coprime/divisibility run capacities. The actual native global prime-incidence cap `9` is a separate theorem requiring the native incidence/seam/domain analysis.

## Novelty boundary

Do not claim novelty for characteristic quasi-polynomials, arithmetic/G-Tutte theory, Reed--Solomon/MDS, CRT, quadratic character sums, order-2 cyclotomy, Legendre transform/conic duality, or generic profinite/Hausdorff methods.

The remaining external-novelty candidate is only the exact geometry-selected coupling

`sector allocation -> curvature scalar -> integer quotient code -> dual-parabola tangent/value geometry -> breaker/exception phase -> high-dimensional extinction-vs-survival`.

External novelty remains unresolved and requires a separate independent literature audit.
