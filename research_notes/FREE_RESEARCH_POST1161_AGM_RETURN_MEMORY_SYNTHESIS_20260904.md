# Post-#1161 free research synthesis — branch memory, S4 transport, graded finite RG, and certified resource frontier

Status: `FREE_RESEARCH_SUCCESSOR_SYNTHESIS / CURRENT TERMINAL PACKET / NOT WORKING_TRUTH / NOT FOUNDATION`
Date: `2026-09-04`
Researcher: `EM-FREE-G61R8`
Parent terminal result: `research_notes/FREE_RESEARCH_1161_AGM_ROTATION_CHORD_SYNTHESIS_20260903.md`

## 0. Scope and terminal verdict

This packet does **not** reopen #1161. The #1161 mother question was already terminal on 2026-09-03, including the internal completion identity `Pi_*=Theta_AGM=tau`.

The present successor line attacked only the explicit residue left by that terminal packet:

> Can the finite branch-return reconstruction of the AGM chord/geometric channel be compressed into a canonical finite native/derived dynamics, transported across the current FCC/S4 geometry, and certified at finite precision without restoring a hidden square-root selector?

The strongest current answer is:

1. `FIRST_RETURN MASS = EXACT AGM CHORD LOSS`: **YES**;
2. `GEOMETRIC MEAN FROM POSITIVE RETURN MASS WITHOUT ROOT SELECTOR`: **YES**;
3. `FULL HISTORY COMPRESSES EXACTLY`: **YES**, to one integer predictive counter for scalar AGM, and to a rank-one signed memory lattice for branch-resolved dynamics;
4. `FIXED FINITE ALL-HORIZON MEMORY`: **NO**;
5. `FINITE STATE AT EVERY FINITE FUTURE PRECISION`: **YES**, with exactly `h+2` unlabeled counter classes;
6. `S4-EQUIVARIANT DIAMOND TRANSPORT`: **YES** at the accepted framed derived Full-Cell geometry;
7. `CANONICAL GLOBAL SIGNED WITNESS ORIENTATION`: **NO**;
8. `SCALAR AGM COUNTER/CHORD OBSERVER NEEDS SUCH A SECTION`: **NO** — it is section-independent;
9. `EXACT FINITE GRADED S4 RG`: **YES**;
10. `PURE-INTEGER WHOLE-TRAJECTORY COMPLETION CERTIFICATE`: **YES**;
11. `256-BIT FINITE RESOURCE PARETO AT MINIMUM OUTER STEP COUNT`: **SOLVED** for the declared compiler/cost model;
12. `PROMOTE HISTORY COUNTER TO BARE P000 INSTANTANEOUS G0/N0 CELL STATE`: **NO CURRENT DERIVATION**; the direct current-cell collision remains a semantic obstruction.

Thus the post-#1161 derived architecture is now complete:

\[
\boxed{
\text{native commuting-diamond skeleton}
\to
\text{N1 branch-memory lattice / scalar predictive counter}
\to
\text{finite S4-equivariant predictive tower}
\to
\text{N2 first-return/chord/AGM mean RG}
\to
\text{pure-integer certified completion}.}
\]

No further same-line AGM algebra is required. A genuine N0/G0 promotion is a separate P000/Foundation question and intersects the already active native/FCC program rather than this completed successor.

## 1. First-return reconstruction is exactly the chord-loss reconstruction

Let `D` be the actual two-witness commuting-diamond fiber and call a word balanced when the two witnesses occur equally often.

The number of words that return to balance for the first time at length `2n` is

\[
R_n=2C_{n-1},
\]

so the normalized positive mass is

\[
\boxed{
f_n=\frac{C_{n-1}}{2^{2n-1}}.}
\]

Define

\[
F(s)=\sum_{n\ge1}f_ns^{2n}.
\]

Catalan renewal gives the formal identity

\[
\boxed{F(2-F)=s^2.}
\]

For the #1161 cone state

\[
r=V/H,\qquad s=U/H,\qquad r^2+s^2=1,
\]

the positive solution is

\[
\boxed{r=1-F(s).}
\]

Hence the predecessor chord loss

\[
\ell=\frac{1-r}{2}
\]

is exactly

\[
\boxed{F(s)=2\ell.}
\]

The AGM update becomes

\[
\boxed{
H^+=H\left(1-\frac F2\right),
\qquad
U^+=H\frac F2,
\qquad
s^+=\frac{F}{2-F}.}
\]

The next endpoints are

\[
a^+=H/2,
\]

\[
\boxed{b^+=\frac H2(1-F).}
\]

Only after this positive branch-return construction is complete does the identity

\[
(b^+)^2=ab
\]

show

\[
b^+=\sqrt{ab}.
\]

So the exact geometric mean is reconstructed as arithmetic half-scale minus first-return defect mass, not selected by an imported real square-root primitive.

## 2. Minimal memory: rank-one signed lattice and its positive orbit

For branch-resolved dynamics define

\[
\boxed{
\mathcal M_D=\mathbb Z^D/\mathbb Z\mathbf1.}
\]

A branch history maps to the class of its multiplicity function. For `|D|=2`, this is rank one. Choosing temporary witness names gives a coordinate

\[
z=\#A-\#B,
\]

but swapping witnesses sends

\[
z\mapsto-z.
\]

Thus there is no canonical positive generator.

The scalar first-return observer retains only

\[
\boxed{d=|z|\in\mathbb N_0,}
\]

which is the orbit quotient

\[
\boxed{
\mathcal M_D/S_2\cong\mathbb N_0.}
\]

This is the coarsest all-horizon predictive state for unlabeled return mass. Different `d` have different future signatures because the earliest possible return time is exactly `d`.

Consequences:

- branch-resolved all-horizon exact memory: countably infinite rank-one signed lattice;
- scalar all-horizon exact memory: countably infinite nonnegative integer counter;
- no fixed finite automaton can preserve the exact all-horizon return language.

For finite future horizon `h`, the exact predictive quotient is

\[
\boxed{Q_h=\{0,1,\ldots,h,\infty_h\},}
\]

with

\[
\boxed{|Q_h|=h+2.}
\]

The branch-resolved horizon quotient has `2h+2` classes.

## 3. Direct current-G0 collision and hidden-fiber capacity

Repeated local diamonds recoalesce to the same terminal cell. After four blocks, the still-alive histories

\[
AAAA,\qquad AAAB
\]

have

- the same current recoalesced terminal cell;
- the same time/block index `4`;
- the same current local incidence type;

but imbalances

\[
4\quad\text{and}\quad2.
\]

Their earliest future return times differ. Therefore

\[
\boxed{
(\text{current cell},\text{time},\text{current local incidence})
\not\Rightarrow
\text{first-return future signature}.}
\]

The memory counter is not a renamed time coordinate.

More generally, at fixed block time `m`, the explicit alive histories

\[
A^{(m+d)/2}B^{(m-d)/2}
\]

realize every positive parity-compatible `d`, all at the same carrier terminal. Hence any hidden lift over that same readout needs at least

\[
\boxed{\left\lceil\frac m2\right\rceil}
\]

unlabeled predictive states, or

\[
\boxed{2\left\lceil\frac m2\right\rceil}
\]

branch-resolved states.

A uniformly bounded hidden fiber is therefore impossible for exact all-horizon AGM return memory.

## 4. FCC-visible lift no-go

The six visible FCC line families are the six edges of `K4`; the four three-line slices are its vertex stars. `S4` acts transitively on the six line slots, and the four slices jointly cover every slot.

Because `d` is a rotation scalar, any visible six-slot equivariant scalar encoding must be constant:

\[
(c,c,c,c,c,c).
\]

Such a field is visible in every slice. Therefore no nonzero scalar counter can be both

- rotation invariant;
- encoded in the six visible FCC slots;
- invisible to all existing slice observations.

Freeze:

\[
\boxed{
\text{S4-SCALAR + SLICE-INVISIBLE FCC-SIX-SLOT COUNTER LIFT}=\text{NO}.}
\]

The only possible spatial hiding place would be information discarded by a richer native-to-FCC readout, and any such fiber must have unbounded predictive capacity. Carrier equivariance alone does not determine such a fiber.

## 5. Recovery of the existing P000/FCC native-lift chain

Durable recovery showed that the native/FCC frontier was much more advanced than a bare “unknown bridge”. Relevant accepted generations are:

- **Gen6**: passive hidden fibers cannot create a missing base-space native rotation;
- **Gen7**: native cross-block `b` is not expressible in the then-current primitive language;
- **Gen8**: derived `BMix_b`/AxisHandledCell mechanism exists, but is not root ontology;
- **Gen9**: `S6` presentation symmetry obstructs canonical axis-to-channel identification; an `AXIS_CHANNEL_FRAME` is required;
- **Gen10**: frame/torsor/connection and gauge-invariant passage are constructed, but passage does not force base Cell `R_b`;
- **Gen11**: framed base-Cell `R_b` existence is classified and nontrivial witnesses exist;
- **Gen12**: one common framed Full-Cell model carries a faithful `S4` lift with `R_a,R_b`, group order `24`, trivial Cell/axis kernels, and transported four-star geometry;
- **Gen13**: structural lifts may be faithful split, hidden-kernel split, nonsplit, or absent; hidden operational kernels cannot be silently quotiented;
- **Gen18**: full lift fibers over the generating quotient motions, not one chosen section, must preserve any retained background/observer for global transparency;
- **Gen19**: the current active P0 successor studies nonconstant PF10 + nonflat connection moduli. No Gen19 result/return was found at this synthesis checkpoint.

The AGM successor consumes these results but does not duplicate active Gen19.

## 6. S4 diamond-memory bundle

Inside one K4/FCC star, a commuting diamond is an unordered pair of incident line families. There are

\[
\boxed{12}
\]

such diamond positions. `S4` acts transitively, with stabilizer `C2`.

Ordering the two active axes chooses one of the two concrete commuting path witnesses. There are

\[
\boxed{24}
\]

ordered witness states, and the `S4` action on them is regular.

Thus

\[
\boxed{D_{12}\cong S_4/C_2,
\qquad
D_{24}\cong S_4.}
\]

The nontrivial diamond stabilizer swaps the two concrete witnesses. Therefore there is no `S4`-equivariant global orientation section

\[
D_{12}\to D_{24}.
\]

Exhaustive checking of all `2^12=4096` sections finds zero equivariant sections.

The signed branch-memory bundle is the associated sign local system

\[
\boxed{
\mathcal L=S_4\times_{C_2}\mathbb Z_{\rm sgn}
\to S_4/C_2.}
\]

It is not canonically trivialized.

But the scalar orbit counter has trivial `C2` action:

\[
\boxed{
S_4\times_{C_2}\mathbb N_0
\cong
(S_4/C_2)\times\mathbb N_0.}
\]

Hence signed orientation is noncanonical while `|z|`, first-return mass, chord loss, and scalar AGM RG are canonical under witness-fiber transport.

This also satisfies the Gen18 full-lift-fiber transparency demand at the declared local observer strength: identity or swap of a two-witness fiber both preserve `|z|`.

## 7. Exact graded finite S4 RG

The finite horizon quotients form an exact precision tower. For `h>=1`, one branch step consumes one unit of future horizon, so the operation is graded:

\[
\boxed{
T_h:Q_h\to\mathbb Q_{\ge0}[Q_{h-1}].}
\]

For `1<=d<=h`,

\[
T_h(d)
=\frac12\delta_{q_{h-1}(d-1)}
+\frac12\delta_{q_{h-1}(d+1)}.
\]

Zero is absorbing for first-hit verification and `infinity_h` maps to `infinity_{h-1}`.

The quotient is operation-safe:

\[
\boxed{(q_{h-1})_*T=T_hq_h.}
\]

Precision projection and dynamics commute exactly:

\[
\boxed{
(\pi_{h-1,k-1})_*T_h
=T_k\pi_{h,k}.}
\]

Tensoring with the twelve diamonds gives

\[
\boxed{X_h=D_{12}\times Q_h,
\qquad |X_h|=12(h+2),}
\]

and the graded kernel is exactly `S4`-equivariant.

The committed checker verified

- `1872` counter-factorization cases;
- `12496` inter-horizon naturality cases;
- `170170` precision-projection composition cases;
- `14976` `S4` equivariance cases;
- exact recovery of the first `32` Catalan return coefficients.

Thus the scalar AGM return mechanism has a finite exact Markov realization at every finite future precision.

## 8. One-shell quadratic universality

For finite return depth

\[
F_N(s)=\sum_{k=1}^Nf_ks^{2k},
\qquad
T_N(s)=\frac{F_N(s)}{2-F_N(s)},
\]

every `N>=1` satisfies on `0<s<=1/4`

\[
\boxed{
\frac{s^2}{4}
\le T_N(s)
<\frac{256}{961}s^2
<\frac4{15}s^2.}
\]

Therefore

\[
\boxed{
\lim_{s\to0}\frac{T_N(s)}{s^2}=\frac14}
\]

for every nonzero finite depth.

The unique minimal nontrivial shell is

\[
N=1,
\]

with

\[
\boxed{T_1(s)=\frac{s^2}{4-s^2}.}
\]

Its scalar `S4` predictive state count is only

\[
\boxed{36}.}
\]

The exact shape error obeys

\[
\boxed{
0\le T-T_N<\frac{512}{961}s^{2N+2}.}
\]

Thus deeper shells improve precision but do not create the quadratic RG class; the first shell already fixes the universal leading coefficient.

## 9. Pure-integer whole-trajectory certificate

A fixed denominator `2^B` integer interval carrier combines outer AGM steps and inner return depth without treating finite approximants as exact states.

At current shape interval `[s_-,s_+]`, use

\[
F_N(s_-)
\le F(s)
\le F_N(s_+)+s_+^{2N+2}
\]

to propagate outward intervals for `a,b,A`. The only initial irrational `1/sqrt2` is enclosed by integer `isqrt`.

The completion readout needs no runtime root:

\[
\boxed{
\frac{4a_-b_-}{A_+}
\le\Pi_*
\le
\frac{H_+^2}{A_- -2P U_+^2}.}
\]

With `B=640`, six outer steps, and conservative return depths

\[
175,43,17,7,3,1,
\]

the exact interval compiler produced bracket-width exponents

\[
0,8,25,61,132,276,546.
\]

The final interval has width `<2^-546`, a common `544`-bit dyadic cell, and a common `162`-decimal-place cell.

The decimal expansion is output only; no target constant is used as input.

## 10. Certified 256-bit resource optimum at minimum outer count

The exact finite completion-bracket width satisfies

\[
U_n-L_n>s_n^2.
\]

Using

\[
s_0>1/6,
\qquad
s_{n+1}\ge s_n^2/4,
\]

gives

\[
U_4-L_4>s_4^2>rac{16}{24^{32}}>2^{-156}.
\]

Therefore four outer AGM steps cannot possibly produce a 256-bit completion bracket, irrespective of inner return precision. The minimum outer step count is five.

At exactly five steps and `B=640`, the interval oracle gives the following global inner-depth optima for the declared state-cost model.

### Width target

For

\[
\operatorname{width}<2^{-256},
\]

the minimum total return depth is

\[
\boxed{80}
\]

with

\[
\boxed{(50,18,8,3,1)}.
\]

Total scalar `S4` state cost:

\[
\boxed{1980}.}
\]

### Common-cell target

For a common 256-bit dyadic cell, the minimum total depth is

\[
\boxed{81}
\]

with

\[
\boxed{(51,18,8,3,1)}.
\]

Total state cost:

\[
\boxed{2004}.}
\]

The lower-bound argument is global within the fixed five-step compiler: coordinatewise necessary depths under any smaller total budget exhaust the boundary budget, and the unique width-budget boundary tuple fails the target.

## 11. Current G0/N0 verdict

The successor has now removed every avoidable algebraic, orientation, finite-state, and precision ambiguity.

What remains is semantic, not computational.

The exact scalar return counter is history-derived. The explicit same-cell/time collision proves it is not reconstructible from current instantaneous slice Cell state plus time. The visible FCC slots cannot hide it. A bounded hidden native/FCC fiber cannot carry it. Passive memory cannot substitute for missing native geometric relations, as already frozen by the P000 Gen6 obstruction.

At the same time, the accepted framed Full-Cell program shows that rich derived `S4` geometry and nontrivial hidden kernels are mathematically consistent downstream. The scalar counter is natural over that derived geometry and transparent to local two-witness lift ambiguity.

Therefore the strongest current classification is

\[
\boxed{
\text{derived equivariant Markov architecture: COMPLETE};
}
\]

\[
\boxed{
\text{bare P000 instantaneous G0/N0 promotion: NOT DERIVED}.}
\]

Promoting the counter further would require an independent native-state relation/axiom or a Foundation theorem showing that the required history information is already intrinsic to the primitive Full Cell state. That question is outside the completed AGM successor and overlaps the active P000/FCC foundation line.

## 12. Durable artifacts

Major notes created in this successor:

- `FREE_RESEARCH_POST1161_FIRST_BALANCE_RETURN_G0_BOUNDARY_20260904.md`;
- `FREE_RESEARCH_POST1161_FIRST_RETURN_ONE_COUNTER_NO_GO_20260904.md`;
- `FREE_RESEARCH_POST1161_FCC_VISIBLE_COUNTER_LIFT_NO_GO_20260904.md`;
- `FREE_RESEARCH_POST1161_BRANCH_MEMORY_LATTICE_20260904.md`;
- `FREE_RESEARCH_POST1161_HIDDEN_FIBER_CAPACITY_AND_INTERFACE_INDEPENDENCE_20260904.md`;
- `FREE_RESEARCH_POST1161_S4_DIAMOND_MEMORY_BUNDLE_20260904.md`;
- `FREE_RESEARCH_POST1161_S4_GRADED_FINITE_RETURN_RG_20260904.md`;
- `FREE_RESEARCH_POST1161_ADAPTIVE_RETURN_DEPTH_RESOURCE_LAW_20260904.md`;
- `FREE_RESEARCH_POST1161_TWO_INDEX_WHOLE_TRAJECTORY_CERTIFICATE_20260904.md`;
- `FREE_RESEARCH_POST1161_FIVE_STEP_LEXICOGRAPHIC_PARETO_20260904.md`.

Corresponding task-local checkers cover Catalan return counts, one-counter predictive minimality, FCC lift obstruction, branch-memory quotienting, hidden-fiber capacity, S4 associated-bundle naturality, graded finite RG, adaptive depth, whole-trajectory integer intervals, and finite Pareto optimality.

No new global tool family is claimed; existing predictive-quotient, finite-symmetry, precision, and typed branch machinery was reused where applicable.

## 13. Terminal boundary

The post-#1161 successor objective is complete at the strongest current non-Foundation level.

No same-line executable mathematical step remains that can promote the N1 memory to bare P000 G0 **without adding new native structure or entering the active P000/FCC foundation program**.

Future work may use this packet as a test suite for any proposed native/FCC lift:

- it must transport the 12-diamond geometry;
- preserve the scalar `|z|` observer across full lift fibers;
- provide unbounded predictive fiber capacity if the memory is to be spatially internalized;
- and explain how history-derived `M_D`/`N_0` information becomes primitive-cell definable without contradicting the current same-cell/time collision.

Until such a Foundation bridge exists, the exact, finite, equivariant, certified AGM reconstruction above is the terminal result of this free-research successor.
