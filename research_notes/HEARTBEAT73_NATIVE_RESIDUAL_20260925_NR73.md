# Heartbeat73 — 从原生相容残差出发，而不是从量子读出倒推

Progress-Event-ID: HEARTBEAT73-NATIVE-RESIDUAL-20260925
Status: SAME_AUTHOR_CONDITIONAL_MODEL / ACTUAL_TYPED_BRC / NOT_ADMITTED
Current user direction: 从原生残差出发试试
Researcher-ID / Research-Activity-ID: none newly assigned; previous safety-blocked registration is not retried, bypassed or replaced with a closed identity.

## 0. Recovered evidence and change of question

Continue the mounted Stage72 cumulative b8c2aea42deb5c50d4f9863a8d79b9c192adc33c, parent bundle SHA256 c761c4ab5a71a29beb57efcf7e6f5d255170aa28f2c4500141607101765ec6fe verified before LOCAL clone. Global immutable read1dfcdce36edee5c4e80fa78a1147d0b90fdc216b; Source read e307855cdc5168538d3df13865327c9b90501273. Bootstrap/manual/project/P000/worldview/position/coordinate/BRC-only unchanged blobs are verified and their loaded complete rules reused. Six native spatial axes and the independent time type remain unchanged.

The user explicitly moves the research upstream. This increment does NOT assume rho, Kraus rules, Born sampling, complex phases or the prior quantum walk as its starting physics. It constructs a real rational compatibility-message field on the native neighbour graph. It does not identify this new field with the earlier material-bearing integer Gauss field, and does not rerun the two-material capture model. How actual joint material occupation supplies these residual messages remains an open bridge. Previously known scattering coefficients informed this construction: this is not blind discovery or an independent prior-art audit.

## 1. Two explicit local constitutive assumptions

Incoming port residuals a_i are signed compatibility discrepancies, with source, direction, unit and finite-resolution metadata. A positive/negative discrepancy is not positive/negative material or probability. Define outgoing reconciliation messages b_i and local newly unmet defect s. Positive port weights are g_i, G=sum_i g_i, A=sum_i g_i a_i.

Assume C1: a_i+b_i=phi for every connection incident on this Cell. Assume C2: sum_i g_i(b_i-a_i)=s, so every net local deficit/sink is accounted for. These are NEW candidate closure laws, not automatic consequences of P000 or discreteness.

They have the unique solution
phi=(2A+s)/G,
b_i=(2A+s)/G-a_i.
For twelve equivalent native ports and s=0, b_i=(sum_j a_j)/6-a_i, hence C=J12/6-I. Thus the previously supplied coefficient form has a lower-level real compatibility realization without a Born assumption. Conservation alone would not force it: C1 is substantive, and other conserving constitutive choices remain possible.

Output at (x,i) moves to (x+e_i,opposite i); only primitive native edges are used. All twelve spatial directions are active. The update count is not calibrated physical heartbeat time.

## 2. Nontrivial sign and a quadratic resource follow conditionally

For s=0 write a=mu*1+delta with mu=A/G and sum g_i delta_i=0. The law gives b=mu*1-delta, so C^2=I. It retains and reverses the disagreement, rather than replacing all inputs by a mean.

Q(a)=sum_i g_i a_i^2 obeys Q(b)=Q(a). With source s, the exact identity is Q(b)-Q(a)=(2sA+s^2)/G=s*phi. Q is a quadratic relational invariant, NOT yet calibrated energy or detector probability.

For incoming (6,0,...,0), actual output is (-5,1,...,1). Signed total6 and Q36 are preserved. Absolute-value sum changes6->16; fourth-power sum1296->636. No opposite phase or pi was fed in; the sign comes from the closure constraint. The global finite-support field conserves Q under source-free scatter/transport.

## 3. A finite-difference uniqueness theorem, not a Born derivation

Let f:Q->R obey f(0)=0, f(-x)=f(x), f(x)>=0. Assume an equivalent-port additive resource E(a)=sum_i f(a_i) is conserved by C=J12/6-I for EVERY rational twelve-port input. Then f(x)=f(1)x^2 on Q. This uses no smoothness, continuum evolution or numerical fit. Universal rational inputs are a cross-resolution model law, not an assertion of infinite physical precision; a fixed finite test set does not justify the universal conclusion.

Proof: take mixed finite differences along distinct input coordinates i,j in E(Ca)=E(a). The right side vanishes. Writing x=Ca, the left side is sum_k Delta_(Cki h)Delta_(Ckj t)f(x_k)=0. C is invertible, so its x coordinates are independently variable. Choose k different from i,j; Cki=Ckj=1/6. Vary only x_k. Thus every Delta_u Delta_v f(x) is independent of x. Its value B(u,v) is separately additive, hence rational-bilinear: B(u,v)=beta uv. Subtract(beta/2)x^2 from f; the remainder is additive on Q, hence lambda x. Evenness and f(0)=0 remove linear/constant terms, and nonnegativity fixes the sign. This establishes a unique additive even conserved resource, not a unique click law. Nonadditive functions of Q remain conserved when additivity is dropped.

## 4. Actual positive native sources generate a dark outlet

Begin with a zero residual field. At native Cells -e1 and +e1 inject unmet residuals s72 and s360. The closure emits respectively6 and30 on all twelve native connections. After one real neighbour transport the common Cell0 receives(6,30,0,...,0).

Run each source separately and then together. At output port0, the first source contributes-5, the second+5, and the combined field gives0. The retained BRC contribution lanes are excess5 and deficit5, not an erased pair of physical sources. Individual squared responses25+25 do not equal the combined0. Other outgoing values(-24 and ten6s) conserve local Q936. Full source pulse Q11232 and signed sum432 stay constant through three additional native scatter/transport steps; complete port-fibre supports are276,1608,6420. No finite-box boundary or non-native hop is used. The actual net-field inverse restores the initial field.

This is a response/interference construction of two explicit classical-compatible discrepancy sources, not a single particle experiment. Raw BRC lane sums are formal positive contribution weights, not conserved matter or actual energy. Fixed local linear future rules factor through their net value; a later source-selective physical operation would require expanded provenance and invalidates that quotient. Initial source programs and edge provenance remain available.

## 5. The same closure also determines local readout backaction

Attach an INTERNAL receiver with positive weight eta and initially zero incoming residual; spatial directions remain12. The same C1/C2 gives
phi_eta=2A/(12+eta), b_i=phi_eta-a_i, b_D=phi_eta.
Thus Q_D=eta*b_D^2=4eta A^2/(12+eta)^2 and sum_spatial b_i^2+Q_D=sum_spatial a_i^2. Field backaction and receiver response are not independently chosen collapse parameters.

For uniform input a_i=mu, receiver fraction is48eta/(12+eta)^2<=1, with complementary fraction(12-eta)^2/(12+eta)^2. At eta12 all common-mode resource enters the receiver; the zero-sum residual field remains as -delta. This receiver cannot read that zero-sum mode. Actual uniform inputs1 give receiver Q values192/25,12,192/25 for eta3,12,48. Stronger coupling is not monotonic stronger reception.

The prior dark input(6,30,0,...,0), now loaded by eta12, yields spatial(-3,-27,3,...,3) and receiver residual3. Receiver Q108 plus spatial Q828 equals936. A zero outlet becomes-3 because a real new relation changed local closure, not because a state was projected or a record deleted. Keeping the receiver value and applying the full closure again restores net input. Replacing its output by0 does not. The receiver is not yet a dissipative detector, permanent absorber or objective outcome selector.

## 6. The remaining quantum bridge is explicit

No step has used P(y)=Q_y/sum Q or sampled a branch by a quadratic norm. Two positive local response laws can map output resources1,4 to normalized weights1/5 and1/17 (linear versus squared resource). Actual BRC-derived outputs feed the two declared observers; neither is claimed a completed instrument model. The invariant alone does not select one.

A signed real residual field and nonlinear response can show interference, yet do not give arbitrary quantum phase, tensor composition, indivisible single-particle detection or unique outcomes. The physical source/receiver model, exclusive click mechanism, resource-accounted record writing and empirical comparison must still be supplied. Do not return to preassuming Born and call this increment its derivation.

## 7. Execution and delivery

229 named finite check groups,6 actual calls of the unchanged canonical positive BRC core in the final fresh process. Most checks are complete12/13-port columns and weighted pair identities, not independent experiments. Source pin bc7babbb9e890f6d5a7094430a5fbdccf66c77ad:src/enterprise_math/brc_weighted_recurrent.py; blob4e6b3132580e3cd70a20a0d8bd4d28792b961afb; SHA2567520822074b8f29e1c54f57b9543c043202db8bd7c6e27f3f1d6176028ac4a26.

The new compiler generates positive excess/deficit covers directly from the two closure equations; evolution uses actual BRC output columns and typed net/pair observers. No imported old quantum propagator, complex phase gate, matrix exponential, trigonometry, eigensolver or numeric optimizer. An initial source-work test mistakenly used4sA instead of2sA; the BRC model was unchanged, the incorrect assertion was corrected and documented. Failed execution is not counted as validation.

Cumulative commit e21c8d650c246e724ecfc8c69d92c96e4fe091f1; START_HERE_STAGE73.md. New stage73/native_residual.py,run.py,PROOF.md,RESULTS.json,BRC_CONTRACT.json,PROVENANCE.json,IMPLEMENTATION_NOTES.md,MANIFEST.json. Fresh LOCAL bundle clone replay matches all scientific fields, excluding elapsed_ns only. Original3844726817ns and replay4025103152ns retained.1334 inherited tracked files unchanged,8 manifest entries verified,git fsckPASS.

Bundle BRC_Heartbeat_native_residual_stage73_20260925.bundle,38248969bytes,SHA2562a31c4eb1cf60340537d388e630e3be462feb40728714a44e6ad268a99ee5796. Drive upload returned file ID1K5BOYBA5o8SwhYL4HEOFUhIYASPF93fV; raw-return verification is not yet claimed in this note. Local delivery/mnt/data/heartbeat73/DELIVERY.json.
ProofSHA256c467f0157eb01cc56c87f5c8ef792049f613fc5158e24a0508d5291fe7d243c2;codeSHA256e57ae95598351ed6e07d0bde1246bcc54ed0916c888802771b8b1b9b94949eea;resultsSHA2567ffc074b490f8320eff4ddc3b18d31e5b8938b3689025eaaccb1f8df8d9a5efb.

Primary sources actually read: Julius O. Smith III, Physical Audio Signal Processing, Lossless Scattering section and author front page at dsprelated.com/freebooks/pasp; Kirchhoff-style equal-potential/flow-conserving weighted scattering is prior art, not a new physical discovery here. Kostrykin/Schrader arXiv math-ph/9806013v2 official abstract read; Feynman Lectures III.1 online text distinguishes wave intensity interference from discrete quantum detections. No new classical reference simulator was run, no full novelty audit or dedicated-provider result is claimed. Same-author proof/code/replay, no independent review/Lean/experimental fitting/physical time calibration or remote server.

LAST_DURABLE_FRONTIER: residual-only source/closure/transport, all-positive-source dark outlet, finite-difference quadratic-resource theorem, locally coupled receiver and actual BRC evidence.
NEXT: derive or falsify a resource-accounted exclusive detector-event law on this residual carrier, including required material source and record degrees of freedom. Preserve all conditional assumptions; no automatic promotion of signed classical field interference to quantum physics or P000. Prior capture/survival results remain intact but do not answer the current upstream request.
