# Native filament hyperbola quotient: Dickson/Joukowski factorization and novelty narrowing

Status: `FREE_RESEARCH_EXACT_CLASSICAL_FACTOR_IDENTIFICATION / NOVELTY_NARROWING / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-25`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Parents:

- `NATIVE_FILAMENT_SPLIT_HYPERBOLA_TANGENT_COVER_BRIDGE_20260825.md`;
- `NATIVE_FILAMENT_INTRINSIC_HYPERBOLA_QUOTIENT_AND_BREAKER_SYMMETRY_20260825.md`.

## 1. Hyperbola parameterization

Fix

`H_(B,C): B*a*b=C`,

with `B,C !=0`, and put

`kappa=C/B`.

Parameterize by

`a=t`,

`b=kappa/t`,

for `t in F_q^*`.

The intrinsic tangent-side symmetry group becomes

`S: t -> kappa/t`,

`R: t -> -t`.

Thus the Klein-four quotient is generated by scaled inversion and negation.

## 2. Two-step quotient

First quotient by scaled inversion using

`z=t+kappa/t`.

This is the classical Joukowski/Dickson coordinate: `t` and `kappa/t` have the same `z`.

Then negation sends

`z -> -z`.

Therefore the full `K_4` quotient is controlled by

`z^2`.

Equivalently, the quotient map is a degree-two Dickson object followed by the sign quotient.

## 3. Exact Dickson formula

The degree-two Dickson polynomial is

`D_2(z,kappa)=z^2-2*kappa`.

Its functional identity gives

`D_2(t+kappa/t,kappa)=t^2+kappa^2/t^2`.

Hence

`(t-kappa/t)^2`

`=D_2(t+kappa/t,kappa)-2*kappa`.

The intrinsic common-dual-value map was

`pi_i(a,b)=-B*(b-a)^2/8-d_i`.

Under `a=t`, `b=kappa/t`, this becomes

`pi_i(t)`

`=-B/8 * D_2(z,kappa) + C/4 - d_i`.

Thus the algebraic quotient itself is an affine transform of a degree-two Dickson value map.

## 4. Novelty downgrade

The following therefore belong squarely to classical finite-field function theory:

- the scaled inversion quotient `t <-> kappa/t`;
- the coordinate `t+kappa/t`;
- the degree-two Dickson functional equation;
- value-set/orbit analysis of the resulting quotient map.

In particular, the existence of a closed formula for the quotient value-set size should not be promoted as a new theorem.

Relevant classical literature includes work on value sets of Dickson polynomials over finite fields, where the cardinality of `D_n(F_q,a)` is determined explicitly.

## 5. What remains research-specific

The retained coupling is now narrower and cleaner:

1. native/sector curvature generates a parity-sampled pair of translated parabolas;
2. mixed tangent concurrence reduces to a finite difference sample on the split hyperbola;
3. the same hyperbola, quotiented by **geometric exchange/reflection symmetries of the tangent triple**, is exactly the common dual-value set;
4. the quotient happens to be a classical Dickson/Joukowski quotient;
5. local exceptional characteristics and global breaker characteristics apply two different operations to the same geometry-selected torsor:
   - finite sample incidence;
   - full symmetry quotient.

Freeze the novelty-safe formulation:

`GEOMETRY SELECTS THE CLASSICAL DICKSON QUOTIENT AND COUPLES IT TO A FINITE TANGENT-SAMPLE DISCRIMINANT`.

Do not claim:

`WE DISCOVERED A NEW DICKSON/JOUKOWSKI VALUE-SET THEORY`.

## 6. Native 5/53 interpretation survives the downgrade

The Dickson identification does not collapse the main local/global distinction.

For native `B=3`:

- `q=5`: the full `K_4`/Dickson quotient has one orbit/value and therefore breaks globally;
- `q=53`: the `k=9` finite tangent-difference sample hits the same split hyperbola, but the full quotient has thirteen classes and therefore does not break.

So the strongest surviving coupled statement is still:

`SAME GEOMETRY-SELECTED TORSOR, TWO DIFFERENT READOUTS`.

The algebraic quotient readout is classical Dickson theory; the finite tangent-sample / quotient coupling remains the theorem-family candidate.

## 7. External novelty status

Targeted literature searching finds the Dickson/Joukowski quotient as a known component and therefore narrows the novelty frontier further.

Current verdict for the **quotient formula alone**:

`KNOWN_COMPONENT / DO NOT PROMOTE`.

Current verdict for the **tangent-sample versus Dickson-quotient coupling selected by the filament geometry**:

`NO_DIRECT_MATCH_FOUND / NOT PROOF OF NOVELTY`.