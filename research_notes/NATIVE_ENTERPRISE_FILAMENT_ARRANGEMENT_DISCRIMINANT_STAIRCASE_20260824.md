# Native Enterprise filament windows: arrangement-discriminant staircase

Status: `FREE_RESEARCH_EXACT_EXCEPTIONAL_SUPPORT_CLASSIFICATION / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-24`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Depends on:

- `NATIVE_ENTERPRISE_FILAMENT_DUAL_TANGENT_EXCEPTION_CUTOFF_STAIRCASE_20260823.md`;
- `NATIVE_ENTERPRISE_FILAMENT_CURVATURE_FLATTENED_AFFINE_MDS_CODE_20260824.md`;
- `NATIVE_ENTERPRISE_FILAMENT_P_ADIC_EXCEPTION_DESINGULARIZATION_20260824.md`.

## 1. Discriminant product

For a length-k filament window and chirality `chi`, consider every mixed-parity triple of indices.

Let `u,v` be the two same-parity indices and `w` the opposite-parity index. Define the concurrence obstruction

- `D=3*(w-u)*(w-v)+chi` when `u,v` are even;
- `D=3*(w-u)*(w-v)-chi` when `u,v` are odd.

Define the arrangement discriminant product

`Delta_k^chi = product |D|`

over all mixed-parity index triples.

No factor is zero for the realized windows `k=5,...,9`.

For a prime q larger than the slope-collision range, the dual-tangent arrangement is non-generic modulo q exactly when q divides `Delta_k^chi`.

## 2. Exact factorizations

The frozen factorizations are:

### k=5

`Delta_5^+ = 2^18 * 5^2`

`Delta_5^- = 2^15 * 5^4`

### k=6

Both chiralities:

`Delta_6 = 2^34 * 5^5 * 7 * 11 * 23`

### k=7

`Delta_7^+ = 2^56 * 5^6 * 7^4 * 11^2 * 13 * 23^2`

`Delta_7^- = 2^60 * 5^8 * 7^3 * 11^2 * 23^2`

Thus q=13 is a one-chirality exceptional channel at k=7.

### k=8

Both chiralities:

`Delta_8 = 2^96 * 5^10 * 7^6 * 11^4 * 13^2 * 23^3 * 31 * 53`

### k=9

`Delta_9^+ = 2^140 * 5^14 * 7^9 * 11^8 * 13^4 * 23^4 * 31^2 * 53^2`

`Delta_9^- = 2^140 * 5^14 * 7^8 * 11^8 * 13^3 * 23^6 * 31^2 * 53^2`

The different q=13 and q=23 valuations are the finite discriminant shadow of the sharp-nine chirality asymmetry.

## 3. Radical staircase

Taking the union of both chirality supports gives:

- k=5:
  `rad=2*5=10`;
- k=6:
  `rad=2*5*7*11*23=17710`;
- k=7:
  `rad=2*5*7*11*13*23=230230`;
- k=8:
  `rad=2*5*7*11*13*23*31*53=378267890`;
- k=9:
  the same radical `378267890`.

Therefore the exceptional-prime support stabilizes one step before the maximal island size:

`EXCEPTIONAL SUPPORT SATURATES AT k=8`.

Adding the ninth Cell increases concurrence multiplicities and reveals chirality valuation imbalance, but introduces no new exceptional prime channel.

## 4. Relation to the 5 -> 53 cascade

The smallest structural prime, 5, is already the unique global connectivity breaker.

The discriminant staircase then adds the finite local channels

`7,11,13,23,31,53`,

and stops.

After the sharp global cap fixes `k<=9`, the arrangement discriminant proves that no prime above 53 can alter the local incidence multiplicity type.

Thus the same native geometry selects two endpoints of distinct roles:

`5 = global connectivity transition`,

`53 = final local arrangement singularity`.

## 5. Boundary

Discriminants of line arrangements are classical. The research-specific object is the exact discriminant staircase generated by the native parity-curved filament and the sharp prime-island size spectrum.
