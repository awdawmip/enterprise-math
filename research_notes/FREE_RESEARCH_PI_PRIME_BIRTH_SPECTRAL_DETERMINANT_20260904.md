# Free Research — Pi-to-Prime Birth Spectral Determinant Bridge

Status: `FREE_RESEARCH_FRONTIER / ANCHOR_EXPOSED / NOT_WORKING_TRUTH / NOT_FOUNDATION / NOVELTY_UNVERIFIED`
Date: `2026-09-04`
Base: `main@5d96189179793ac44e55fc99e9b1cf6cebd2e604`
Project: `Enterprise Math / 进取数论`
P000 interpretation: six-dimensional discrete cell space; rotation primary; any Hamming/Krawtchouk carrier below is a typed finite spectral carrier, not a claim that its shell dimension is spatial dimension.

## 0. Question and information status

User-selected mother direction:

> Extend the current geometric meaning of the endogenous full-turn constant `tau = Pi_*` to arithmetic primes.

This pass is deliberately `ANCHOR_EXPOSED`: it consumes current #1161 AGM/diamond completion work and current #1159 Wallis/sine/Krawtchouk work. It is therefore not a blind Phase-A novelty claim.

The main result of this pass is an exact finite bridge:

\[
\boxed{
\text{Krawtchouk integer spectrum}
\to
\text{multiplicative birth block}
\to
\text{prime Euler determinant}
\to
\tau^2
}
\]

with a target-free finite rational error certificate.

No claim is made that the general ideas “primes are multiplicative generators”, “zeta is an Euler product/partition function”, or “primitive dynamical orbits give Euler products” are new. They are prior mathematics. The project-specific content is the typed reuse of the same finite Krawtchouk rotation-mode carrier currently used by #1159, together with the internal `tau` completion and the #1161 finite diamond/Wallis state.

---

## 1. Source dependencies and deduplication

### Current project inputs

1. `#1161` / `research_notes/FREE_RESEARCH_1161_AGM_ROTATION_CHORD_SYNTHESIS_20260903.md` on main:
   \[
   \Pi_*=\Theta_{\rm AGM}=\tau,
   \]
   repeated native diamond multiplicity, central provenance shells, and pure-integer monotone `tau` squeeze.

2. `#1159` current theorem packet on PR `#1172`, head inspected in this pass at
   `93402acfba5b5b92dc06944e218a8f717fc3c79a`:
   - finite Hamming/Krawtchouk shell operator `K_M` with eigenvalues `0,1,...,M` on a complete eigenbasis;
   - literal even/odd restricted determinant bridge to Wallis;
   - internal sine-type power series `S` with first positive zero `tau`;
   - research-level infinite product
     \[
     \frac{S(x)}x
     =\prod_{k\ge1}\left(1-\frac{x^2}{k^2\tau^2}\right).
     \]
   The literal finite determinant portion is Lean-green on the PR; the full infinite-product formalization remains a downstream Lean dependency.

3. Weighted-BRC global substrate: rational multiplicative gauge/holonomy is allowed to retain finite prime-valuation coordinates before optional logarithmic readout. Logarithms are derived, not native state.

### Internal prime dedup

Historical `R005-C` harvest explicitly reports that its stable canonical prime layer consists of bounded primality/enumeration, factor witnesses, square-basin identities, centered-prime/slack coordinates and power-free action bases; that harvest itself developed no new prime theorem.

Draft `#333` does contain a prime primitive-generator / `Omega`-word compiler architecture. Its arithmetic core overlaps the elementary fact used below: primes are the forced primitive multiplicative generator set. However #333 studies quotient-action compilation/storage/depth, not the Krawtchouk spectral birth block, its determinant, or the `tau` completion below.

Relation to #333: `COMPOSABLE_INDEPENDENT / SHARED_ARITHMETIC_CORE`, not a replacement theorem.

### External prior-art boundary

- Bost-Connes/primon-gas constructions already realize zeta-type partition functions using prime-labelled modes; no novelty is claimed for a prime-mode partition interpretation.
- Inoue, arXiv:2602.20211 (2026), treats finite-cutoff Euler products through the multiplicative formal group and logarithmic linearization; no novelty is claimed for finite Euler cutoff or multiplicative-to-additive log coordinates.
- Classical dynamical zeta functions use Euler products over primitive periodic orbits. Current Enterprise BRC Foundation explicitly does **not** identify its primitive periodic edge-word orbits with arithmetic primes. No such identification is asserted here.

---

# PBH-T01 — Multiplicative birth equals arithmetic primality

For integer `k >= 2`, let

\[
\mathcal H_{<k}
:=\left\{\prod_{j=2}^{k-1}j^{e_j}:e_j\in\mathbb N_0\right\}
\]

be the multiplicative monoid generated by earlier positive integer modes. Define

\[
\operatorname{Birth}(k)
\iff
k\notin\mathcal H_{<k}.
\]

Then

\[
\boxed{
\operatorname{Birth}(k)
\iff
k\text{ is prime}.
}
\]

Proof. If `k=ab` is composite with `2<=a,b<k`, then `k` is generated by earlier modes. Conversely, if `k` is a product of earlier integers with a nontrivial factor, then it is composite. `□`

This theorem does not redefine primality by a numerical oracle. It says that, as the full integer spectrum grows by resolution cutoff, prime indices are exactly the cutoffs where the multiplicative generator rank acquires a new direction.

Typed interpretation:

`ARITHMETIC_HOLONOMY_FIBER_RANK_JUMP != NEW_SPATIAL_DIMENSION`.

P000 spatial dimension remains six.

---

# PBH-T02 — Arithmetic birth block of the Krawtchouk shell

Use the current #1159 Hamming/Krawtchouk operator `K_M`. In its proved eigenbasis write

\[
K_M g_k=k g_k,
\qquad 0\le k\le M.
\]

Define the arithmetic birth subspace

\[
V_M^{\rm birth}
:=\operatorname{span}\{g_p:p\le M,\ p\text{ prime}\},
\]

and the restricted operator

\[
B_M:=K_M|_{V_M^{\rm birth}}.
\]

In the selected basis,

\[
\boxed{B_M=\operatorname{diag}(p:p\le M,\ p\text{ prime}).}
\]

Therefore the same finite spectral carrier has exact arithmetic readouts

\[
\boxed{\dim V_M^{\rm birth}=\pi_{\mathbb P}(M),}
\]

\[
\boxed{\det B_M=\prod_{p\le M}p.}
\]

The determinant is the finite primorial in exact integer form. Only after an optional logarithmic readout,

\[
\log\det B_M=\sum_{p\le M}\log p=\vartheta(M),
\]

so Chebyshev `theta` is derived, not primitive state.

Terminology warning: the existing #1159 symbol/file phrase `even_prime` refers to the positive-even/pseudodeterminant block after removing the zero eigenvalue; it does **not** mean arithmetic primes. The block `B_M` above is the arithmetic-prime birth block.

---

# PBH-T03 — Finite prime-holonomy spectral determinant

For integer `r>=1`, define

\[
Z_M(r)
:=\det(I-B_M^{-r})^{-1}.
\]

Since the eigenvalues of `B_M` are the primes `p<=M`,

\[
\boxed{
Z_M(r)
=\prod_{p\le M}(1-p^{-r})^{-1}
=\frac{\det(B_M)^r}{\det(B_M^r-I)}.
}
\]

For every fixed finite `M` this is a finite rational number. No log, floating spectrum, analytic continuation or pre-known `pi` is needed.

At `r=2`,

\[
\boxed{
Z_M(2)
=\frac{\det(B_M)^2}{\det(B_M^2-I)}.
}
\]

This is the first bridge quantity used below.

---

# PBH-T04 — Internal Basel bridge: `tau^2/6` is the prime completion

The #1159 internal product gives

\[
\frac{S(x)}x
=\prod_{k\ge1}\left(1-\frac{x^2}{k^2\tau^2}\right),
\]

while the internally defined sine-type series has

\[
\frac{S(x)}x
=1-\frac{x^2}{3!}+O(x^4).
\]

An elementary comparison `\sum_{k>N}k^{-2}<=1/N` gives convergence of the quadratic coefficient independently of any derivative/interchange assumption. Comparing the `x^2` term therefore yields

\[
\boxed{
\sum_{n\ge1}\frac1{n^2}=\frac{\tau^2}{6}.
}
\]

Unique factorization then gives the absolutely convergent Euler product

\[
\boxed{
\frac{\tau^2}{6}
=\prod_p(1-p^{-2})^{-1}
=\lim_{M\to\infty}Z_M(2).
}
\]

Equivalently,

\[
\boxed{
\tau^2
=6\lim_{M\to\infty}
\det(I-B_M^{-2})^{-1}.
}
\]

This is an **internal** `tau -> arithmetic primes` completion: it does not need the classical name or numerical value of `pi` as input.

Status boundary: the finite spectral pieces are exact; this theorem inherits the current research-level #1159 infinite-product dependency, whose full Lean formalization is still pending on PR #1172.

---

# PBH-T05 — Target-free finite rational certificate

The tail after the arithmetic cutoff satisfies

\[
1<\frac{\tau^2/6}{Z_M(2)}
=\prod_{p>M}(1-p^{-2})^{-1}
\le
\prod_{n=M+1}^{\infty}(1-n^{-2})^{-1}.
\]

The all-integer product telescopes exactly:

\[
\prod_{n=M+1}^{N}(1-n^{-2})
=\frac{M(N+1)}{(M+1)N},
\]

so its inverse tends to `(M+1)/M`. Hence

\[
\boxed{
6Z_M(2)<\tau^2
\le
6Z_M(2)\frac{M+1}{M}.
}
\]

The finite interval uses only the arithmetic-prime birth block and rational arithmetic. Its exact relative width, measured from the lower endpoint, is

\[
\boxed{\frac1M.}
\]

If `q(M)` is the least prime strictly above `M`, the same proof may start at `q(M)` and gives the slightly sharper certified factor

\[
\boxed{
\frac{\tau^2/6}{Z_M(2)}\le\frac{q(M)}{q(M)-1}.
}
\]

This sharpened version can reuse the status-preserving R005 prime toolkit for the finite next-prime witness; it does not require a new primality implementation.

---

# PBH-T06 — Quadratic order is the first stable positive-integer completion

For `r=1`, the finite Euler product diverges as the cutoff grows. Indeed, for `M>=1`, expansion of the finite prime factors contains every reciprocal `1/n` with `n<=M`, hence

\[
Z_M(1)\ge\sum_{n=1}^{M}\frac1n\to\infty.
\]

For every integer `r>=2`,

\[
Z_M(r)\le\sum_{n\ge1}n^{-r}<\infty,
\]

so the cutoff limit exists.

Therefore

\[
\boxed{
2=\min\{r\in\mathbb N_{>0}:\lim_{M\to\infty}Z_M(r)<\infty\}.
}
\]

This makes the quadratic observer structurally distinguished: it is the **first stable integer holonomy completion order**.

Project interpretation is deliberately weaker than a literal geometric identification. Enterprise sector length and the #1161 defect budget are quadratic (`L_E^2`, `U_n^2`), and the prime completion that closes to `tau` also first becomes stable at integer order two. This is an exact alignment of observer order. It is **not yet** a theorem that the Krawtchouk prime eigenvalue `p` is a native spatial length.

---

# PBH-T07 — Wallis/diamond state has an exact prime-valuation flux

Let

\[
P_{2n}=4^n,
\qquad
C_n=\binom{2n}{n},
\]

and use the #1161/#1159 exact Wallis state

\[
W_n
=\frac{P_{2n}^2}{(2n+1)C_n^2}
=\frac{16^n}{(2n+1)\binom{2n}{n}^2}.
\]

For each prime `p`, define

\[
c_p(n):=v_p\!\binom{2n}{n}.
\]

Then

\[
\boxed{
v_p(W_n)
=4n\,\mathbf1_{p=2}
-2c_p(n)
-v_p(2n+1).
}
\]

By Kummer, `c_p(n)` is exactly the number of carries in the addition `n+n` in base `p`. Thus every finite Wallis/diamond state has an exact finite prime-holonomy coordinate vector.

The one-step ratio is

\[
\frac{W_{n+1}}{W_n}
=\frac{4(n+1)^2}{(2n+1)(2n+3)},
\]

so the local valuation flux is

\[
\boxed{
\Delta v_p
=2\,\mathbf1_{p=2}
+2v_p(n+1)
-v_p(2n+1)
-v_p(2n+3).
}
\]

This is an exact multiplicative decomposition of the finite rotation-mode correction into prime coordinates; optional `log p` weights may convert it to an additive real readout, but the valuation vector itself is the exact integer carrier.

---

# PBH-T08 — `tau` is an Archimedean completion boundary, not a coordinatewise valuation limit

For `p=2`, Kummer/Legendre gives

\[
v_2\!\binom{2n}{n}=s_2(n),
\]

where `s_2(n)` is the number of `1` bits in `n`. Hence

\[
\boxed{
v_2(W_n)=4n-2s_2(n)\to\infty.}
\]

Therefore

\[
|W_n|_2\to0.
\]

But #1159 gives the real/Archimedean completion

\[
W_n\to\tau/2.
\]

So the same exact rational finite states satisfy

\[
\boxed{
W_n\to\tau/2\text{ in }\mathbb R,
\qquad
W_n\to0\text{ in }\mathbb Q_2.
}
\]

Consequently the full-turn constant should not be represented as a naive coordinatewise limit of the finite prime-valuation vector. The finite valuation fiber keeps reorganizing while the Archimedean readout converges.

This is a completion statement, not an adelic theorem; no adelic product formula is claimed here.

---

# PBH-T09 — Two independent finite carriers squeeze the same completion

The #1161 pure-integer diamond squeeze uses

\[
L_n
=\frac{2P_{2n}^2}{(2n+1)C_n^2},
\qquad
U_n
=\frac{4P_{2n}^2}{(4n+1)C_n^2},
\]

with

\[
L_n<\tau\le U_n,
\qquad
\frac{U_n-L_n}{L_n}=\frac1{4n+1}.
\]

Combine it with PBH-T05. For every `n,M>=1`,

\[
\boxed{
\max\{L_n^2,\,6Z_M(2)\}
<\tau^2
\le
\min\left\{U_n^2,\,6Z_M(2)\frac{M+1}{M}\right\}.
}
\]

Every displayed endpoint is finite rational data.

Thus the native-diamond central-provenance carrier and the arithmetic-prime birth determinant give independent, target-free finite certificates for the same internal completion.

This is the strongest current finite `pi <-> primes` bridge in this pass.

---

## 10. General zeta-weight tower

For every integer `r>=2`,

\[
\boxed{
Z_M(r)
=\det(I-B_M^{-r})^{-1}
\longrightarrow
\zeta(r).
}
\]

The #1159 product gives more than the quadratic coefficient. For example, comparing the `x^4` coefficient yields internally

\[
\boxed{
\zeta(4)=\frac{\tau^4}{90}.
}
\]

Hence

\[
\boxed{
\lim_{M\to\infty}\det(I-B_M^{-4})^{-1}
=\frac{\tau^4}{90}.
}
\]

Higher even weights can in principle be recursively extracted from the same product coefficients. This pass does not claim a completed general Bernoulli-number theorem inside the project.

---

## 11. Geometric meaning now supported

At current theorem strength:

1. **Prime `p`** = a new irreducible multiplicative direction born when the finite integer rotation spectrum first reaches mode `p`.
2. **Prime power `p^a`** = repeated winding/occupation of one birth direction in the arithmetic holonomy fiber.
3. **Composite integer** = finite superposition/product of earlier birth directions.
4. **Prime counting `pi_P(M)`** = rank of the arithmetic birth block.
5. **Primorial** = exact determinant of the birth block.
6. **Chebyshev theta** = derived logarithmic determinant readout.
7. **`zeta(r)`** = completion of the finite birth-block determinant observer of order `r`.
8. **`tau^2`** = six times the minimal stable integer (`r=2`) prime-birth determinant completion.
9. **Finite Wallis/diamond states** = exact rational trajectories carrying prime-valuation/carry flux at every resolution.

The word `direction` above means an arithmetic multiplicative/holonomy fiber direction. It does not add spatial axes to P000.

---

## 12. Strong boundary: the factor six is not yet six-dimensional geometry

We now have the exact internal identity

\[
\tau^2=6\,Z_{\mathbb P}(2),
\qquad
Z_{\mathbb P}(2):=\lim_M Z_M(2).
\]

P000 independently fixes `ENTERPRISE_SPACE_DIMENSION=6`. The numerical equality of these two sixes is striking but **not currently causal**.

In the proof above, the coefficient `6` enters as `3!` from

\[
S(x)/x=1-x^2/3!+\cdots.
\]

Therefore the following is a new falsifiable bridge question, not a theorem:

> Can the quadratic prime-birth determinant `Z_P(2)` be realized as one native isotropic quadratic response of the six-dimensional Enterprise rotation-cell state such that the multiplicity `6` is forced by the six spatial degrees of freedom, rather than inserted as a direct sum or recognized after the fact from the power-series coefficient?

A trivial six-copy direct sum is not an acceptable solution. The factor must be forced by native six-dimensional incidence/rotation symmetry or an equivalent invariant.

---

## 13. Next formalization frontier

Suggested exact theorem order, without introducing a new prime oracle:

1. `PBH-T01` in Lean using `Nat.Prime` and finite multiplicative generation.
2. Reuse #1172's Krawtchouk eigenbasis and build the actual arithmetic birth index submatrix/restriction.
3. Prove `det B_M = primorial(M)` and `dim birth = Nat.primeCounting M`.
4. Prove the finite rational identity
   \[
   Z_M(2)=\det(B_M)^2/\det(B_M^2-I).
   \]
5. Prove the telescoping rational tail certificate in PBH-T05.
6. Reuse #1161 integer Wallis definitions to formalize PBH-T07 valuation flux.
7. Attach PBH-T04 only after the #1159 infinite product / coefficient interface has the required formal theorem surface.
8. Only then open the six-dimensional factor-six bridge.

No Riemann-hypothesis claim follows from this packet. No identification of current BRC primitive periodic orbits with arithmetic primes is made.

---

## Current classification

- `MULTIPLICATIVE_BIRTH <-> PRIME`: `PROVED / ELEMENTARY`.
- `KRAWTCHOUK_ARITHMETIC_BIRTH_BLOCK`: `DERIVED_FROM_CURRENT_#1159_FINITE_SPECTRUM / LEAN_BRIDGE_NOT_YET_WRITTEN`.
- `FINITE_BIRTH_DETERMINANT_EULER_FACTOR`: `PROVED / FINITE_EXACT`.
- `TAU^2/6 = ZETA(2)`: `RESEARCH-PROVED_FROM_#1159_PRODUCT / INFINITE_LEAN_DEPENDENCY_PENDING`.
- `TAU^2 = 6 * PRIME_BIRTH_DETERMINANT_COMPLETION`: `RESEARCH-PROVED_WITH_SAME_DEPENDENCY`.
- `FINITE_RATIONAL_PRIME_TAIL_CERTIFICATE`: `PROVED / ELEMENTARY`.
- `QUADRATIC_MINIMAL_STABLE_INTEGER_ORDER`: `PROVED / ELEMENTARY`.
- `WALLIS_PRIME_VALUATION_FLUX`: `PROVED / ELEMENTARY + KUMMER`.
- `ARCHIMEDEAN_VS_2ADIC_COMPLETION_SPLIT`: `PROVED GIVEN #1159 REAL LIMIT`.
- `FACTOR_6 = P000 SIX_SPATIAL_DIMENSIONS`: `OPEN / NOT_DERIVED`.
- `ARITHMETIC_PRIMES = CURRENT_BRC_PRIMITIVE_PERIODIC_ORBITS`: `NOT_CLAIMED`.
- `EXTERNAL_NOVELTY`: `UNVERIFIED / NO CLAIM`.
