# X6 full Cell cover classification：唯一剩余自由度是六轴全对角环的闭合阶 `m`

Status: `DERIVED / EXACT CLASSIFICATION / STRONGEST CURRENT X6 FRONTIER`
Date: `2026-09-05`
Depends on: V7–V12 coordinate completion.

## 1. Why a cover classification is needed

The coordinate observer is closed as

`G6_D=Z^6/Z*1`.

But Packet/Path logic permits the possibility that two full Cell states have the same six-axis coordinate observation while differing by a genuine non-coordinate Cell-identity fibre.

To avoid an arbitrary “hidden state” placeholder, classify every such possibility in the natural homogeneous commuting-translation model class.

## 2. Model class

Assume on one connected homogeneous Cell component:

1. there are six total commuting native-axis translation actions, generated by `e_1,...,e_6`;
2. the action is transitive from one Cell anchor, so the endpoint translation group is `Z^6/H` for some subgroup `H<=Z^6`;
3. the already-closed coordinate observer is the quotient
   `Z^6/H -> G6_D=Z^6/Z*1`;
4. full Cell equality never identifies two different six-axis coordinate classes.

Condition 3–4 are equivalent to

`H <= Delta`,

where

`Delta=Z*(1,...,1)`

is the coordinate-diagonal return subgroup.

Thus any extra full-state distinction can only refine the coordinate-diagonal loop; it cannot modify relative six-axis coordinates.

## 3. Classification theorem

Every subgroup of the infinite cyclic group `Delta ~= Z` is

`m Delta`

for a unique integer `m>=0`, with convention `0 Delta={0}`.

Therefore every connected commuting coordinate-faithful Cell cover is uniquely

`G6^(m)=Z^6 / m Z*(1,...,1)`, `m=0,1,2,...`.

The map to coordinate state is

`G6^(m) -> G6_D=G6^(1)`.

Its fibre is

- `Z` if `m=0`;
- `Z/mZ` if `m>=1`.

Hence no arbitrary finite/infinite hidden group survives this model-class audit: the only possible full Cell identity fibre is the cyclic memory of how many complete six-axis diagonal loops have occurred.

## 4. Meaning of m

Let

`D=e_1+e_2+e_3+e_4+e_5+e_6`.

Coordinate observation kills D because it is a global diagonal shift.

In the full cover `G6^(m)`:

- `m=1`: D already returns to the same full Cell;
- `m=2`: one D-loop moves to the second sheet and two D-loops return;
- general finite m: exactly m full diagonal loops return;
- `m=0`: D has infinite order and no positive number of complete diagonal loops returns the full Cell.

Thus `m` is the exact **full Cell diagonal-closure order**.

Do not interpret it as an additional spatial dimension. It is a possible fibre over the six-axis coordinate center identity.

## 5. Why this cover does not repeat the PR1257 type error

The superseded PR1257 obtained torsion by first identifying the two chart-local Cell trajectories of each shared K4 line axis. That was an illicit transfer from line identity to Cell-transition identity.

The present cover classification does **not** identify any chart-local shared-axis endpoints. It begins only after the six-axis coordinate observer has been reconstructed from component/slice data. The fibre is attached to the one global diagonal coordinate loop and is independent of the K4 shared-axis chart presentations.

In particular for `m=2` the group is

`Z^6 / 2Z*1 ~= Z^5 x Z/2`,

not the superseded `Z^2 x Z/2` model.

All six positive axis translations remain distinct.

## 6. BRC can distinguish m exactly

Under equal positive six-axis branching, a coordinate return at length `6r` requires each axis to occur exactly r times.

In the full m-cover that path returns to the **same full Cell** iff the diagonal class r is zero in the fibre:

- finite `m>=1`: `r == 0 mod m`;
- `m=0`: only `r=0`.

Therefore for finite m the nonzero full return lengths are exactly

`6m,12m,18m,...`.

At length `6r` satisfying the return condition, exact N-BRC return multiplicity is

`(6r)!/(r!)^6`.

Examples:

- `m=1`: first full return at length 6, multiplicity 720;
- `m=2`: no full return at length 6; first at length 12, multiplicity 7,484,400;
- `m=3`: first at length 18, multiplicity `18!/(3!)^6`;
- `m=0`: no nontrivial balanced positive coordinate word ever returns the same full Cell.

Hence m is experimentally/computationally observable at the full Cell endpoint/BRC layer even though every selected 3-axis coordinate observer forgets it.

## 7. Relation to 3-axis local loops

For a selected three-axis slice S, its positive triangle displacement

`H_S=sum_{i in S}e_i`

is not the full diagonal D. It remains a nontrivial full coordinate displacement whose selected-slice observation vanishes.

Therefore current local 3-axis Cell-return data do not by themselves fix m. They constrain slice observations, not the full six-axis diagonal closure.

This is exactly why the previous attempt to derive full returns by combining local endpoint identities was over-strong.

## 8. S6 symmetry does not fix m

The diagonal subgroup and every `mDelta` are invariant under all six-axis permutations. Thus all models `G6^(m)` preserve the exact S6 axis-permutation skeleton, P000 pairwise orthogonality and the six-axis component norm observer.

So rotation symmetry cannot choose m.

## 9. The one true Foundation decision

The coordinate-complete native Cell model is exactly

`m=1`.

Equivalently any one of the following freezes the same choice:

`CELL_IDENTITY_BY_FULL_SIX_AXIS_CENTER_ADDRESS`;

`GLOBAL_SIX_AXIS_DIAGONAL_LOOP_RETURNS_ONE_CELL`;

`FULL_CELL_COORDINATE_MAP_HAS_TRIVIAL_FIBRE`.

If this is selected, then

`X6_native = affine torsor over G6_D`

with primitive min-zero address `A6_D`.

If it is not selected, no further mathematics based only on current coordinate/symmetry/slice information can decide m, because the complete countermodel family above satisfies all those observers.

This is now a genuine logical boundary rather than an unfinished algebra calculation.
