# R059D — Triaxial Integer Cell / Root-Collapse Correction

Status: `DRIVER_CONCEPTUAL_CORRECTION_ANCHOR`
Date: `2026-08-16`
Scope: R059D BRC lane

## Core correction

Do not identify carrier-transfer tuples such as `(1,-1,0)` with the raw triaxial coordinate law.

The working hypothesis to test is instead:

1. Crystal-cell coordinates are integer-only completed addresses.
2. Moving from the origin one cell in the positive `u` direction gives integer coordinate `(1,-1,-1)`: +1 on `u`, and -1 on each of the other two triaxial directions.
3. Repeating the same positive-`u` cell transition can produce a noninteger intermediate algebraic magnitude on the two transverse axes before collapse. Example candidate after two steps: `(2,-sqrt(2),-sqrt(2))`, which may collapse to integer address `(2,-1,-1)`.
4. The radical value is not itself a stored crystal-cell coordinate. It is a pre-completion algebraic value generated by integer cell counting and must collapse back to integer coordinate values.
5. The scientific task is therefore to label/count nearby cells first and infer the root order and collapse rule from exact self-consistency, rather than starting from an abstract branch selector.

## Key bridge to scalar BRC example

If the transverse magnitude is square-root typed in a 2D triaxial plane, then for integer count `n`:

`sqrt(n)` lies between consecutive integers `k` and `k+1` whenever `k^2 < n < (k+1)^2`.

Completion of the root coordinate to `k` or `k+1` corresponds, on the squared-count layer, to

`n -> k^2` or `n -> (k+1)^2`.

Thus the previously isolated scalar question

`5 -> 4 or 9`

is exactly equivalent to

`sqrt(5) -> 2 or 3`

under a square-root completion hypothesis.

This is a hypothesis bridge to be tested from the cell atlas; it is not yet a frozen theorem.

## Immediate research method

Before any further selector/stabilizer/post-credit abstraction:

- enumerate origin-near crystal cells;
- assign only integer completed triaxial addresses;
- for repeated moves along each axis direction, derive the pre-completion algebraic values from cell counts;
- test candidate root orders (`sqrt`, cube-root, etc.) only by exact consistency;
- record where the radical crosses integer completion layers;
- infer whether one collapse law simultaneously reproduces all neighboring-cell addresses and symmetries.

The intended level is elementary exact algebra and integer counting wherever possible.

## Route correction

The unexecuted Stage V post-credit-pullback route is superseded before execution by this coordinate-first investigation.

Do not claim:
- root order already proven;
- floor/ceil rule already proven;
- `sqrt(n)` law universal;
- physical dimensionality derived;
- prior abstract selector results invalid as mathematics.

They are simply not the current black-box entry point.
