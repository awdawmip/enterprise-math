# Native tri-sector unique longitudinal/transverse boundary-closure theorem

Status: `FREE_RESEARCH_EXACT_CROSS_ROUTE_SELECTION / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-25`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Depends on:

- odd-sector extremal Joukowski saturation theorem;
- breaker-capacity theorem;
- finite-window tangent-product cutoff theorem.

Only `s=3` is native Enterprise geometry. General odd `s` is a controlled comparator family.

## 1. Three boundary constructions

For a nontrivial odd-sector model `s>=3`, three a priori different boundary numbers arise.

### Transverse Joukowski boundaries

The lane-label image-size theorem has two extremal characteristics:

`q_-(s)=2s-1`,

`q_+(s)=2s+1`.

Only when these are prime and the image equals the native lane set can they become extremal transverse saturation channels.

### Longitudinal breaker capacity

Let `q_b` be an odd universal breaker.

The exact breaker-coprime capacity is

`k_*=2q_b-1`.

For the odd-curvature family, the only possible odd breakers are `3` and `5`.

### Tangent-window boundary factors

For an odd window length `k`, the maximal mixed-parity distance product is

`M_k=(k-2)(k-4)`.

Thus the two factors generated by the sharp breaker window are

`k_*-4`,

`k_*-2`.

## 2. Boundary-closure equation

Ask when the longitudinal sharp-window factors coincide exactly with the two transverse Joukowski boundaries:

`k_*-4=2s-1`,

`k_*-2=2s+1`.

The equations are equivalent and give

`k_*=2s+3`.

Since

`k_*=2q_b-1`,

we obtain

`q_b=s+2`.

## 3. Unique nontrivial solution

For `s>=3`, the relation `q_b=s+2` implies

`q_b>=5`.

But the global orbit-capacity theorem proves that no odd universal breaker exceeds `5`.

Therefore

`q_b=5`,

`s=3`.

Then

`k_*=2*5-1=9`.

Hence:

`THE ONLY NONTRIVIAL ODD-SECTOR MODEL IN WHICH`

`SHARP LONGITUDINAL TANGENT FACTORS`

`MATCH BOTH TRANSVERSE JOUKOWSKI BOUNDARIES`

`IS`

`(s,q_b,k_*)=(3,5,9)`.

## 4. Native numerical closure

At the unique solution:

`q_-=2s-1=5`,

`q_+=2s+1=7`,

`k_*-4=5`,

`k_*-2=7`.

Therefore

`M_9=(9-4)(9-2)=5*7=35`.

Multiplying by the native curvature coefficient:

`s*M_9=3*5*7=105`.

This is exactly the transverse C3 bouquet gate.

Thus the earlier identity

`BOUQUET GATE = B * TANGENT-SAMPLE EXTREMUM`

is forced by a unique boundary-matching solution inside the odd-sector family.

## 5. Terminal local characteristic

The terminal sampled-tangent obstruction is

`s*M_9+1=105+1=106`.

Its largest odd prime divisor is

`53`.

Equivalently

`53=(105+1)/2`.

So the native chain becomes

`sector s=3`

`-> breaker q_b=5`

`-> capacity k_*=9`

`-> tangent factors 5,7`

`= transverse extremal boundaries 5,7`

`-> product with curvature =105`

`-> terminal local characteristic53`.

## 6. Why other first-breaker-5 sectors do not close

There are infinitely many controlled sector counts in the residue classes with first breaker5, for example `s=27 mod60`.

They still have longitudinal capacity

`k_*=9`

and tangent factors

`5,7`.

But their transverse Joukowski boundaries are

`2s-1,2s+1`,

which are far larger and do not match `5,7`.

Thus first-breaker5 alone does not produce the native 105 coherence. The closure additionally selects the smallest sector coefficient `s=3`.

## 7. Independence from the extremal-saturation proof

The boundary-closure theorem uses:

- the universal breaker bound `q_b<=5`;
- the exact capacity formula `k_*=2q_b-1`;
- the exact tangent-factor formula `(k-4,k-2)`;
- the Joukowski boundary values `(2s-1,2s+1)`.

It does not use the second-moment proof that only s=3 actually saturates both Joukowski boundaries.

Therefore the two results are logically distinct and mutually reinforcing:

1. second moments prove that `s=3` uniquely realizes both extremal transverse saturation channels;
2. boundary closure proves that `s=3` uniquely aligns those channels with the longitudinal sharp-window geometry.

## 8. Selection consequence

Within the controlled odd-sector family, the native value `s=3` is now characterized by multiple exact properties:

- minimum nontrivial central-fiber gate;
- latest possible finite breaker within that minimum-gate class;
- unique dual-extremal Joukowski saturation;
- unique longitudinal/transverse boundary closure.

This does not establish external novelty, but it strongly reduces the risk that the native constants `3,5,7,9,105,53` are unrelated fitted coincidences.

## 9. Prior-art boundary

The individual formulas are elementary once the carrier is defined.

The research-specific candidate is the exact simultaneous closure of independently derived longitudinal and transverse boundary constructions selected by the native tri-sector allocation.