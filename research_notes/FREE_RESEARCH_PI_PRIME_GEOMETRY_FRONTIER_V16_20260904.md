# Free Research — Pi-to-Prime Geometry Frontier V16

Status: `FREE_RESEARCH_CURRENT_FRONTIER / V15_PROFILE_DISCRETIZED / SINGLE_CHANNEL_NOGO / PERSISTENT_STANDARD_INTERTWINER / PARITY_BLOCK_COUPLING / ALL_DEPTH_GAMMA_OVERLAP / RADIAL_SHELL_GAP / CONSTANT_MODE_ANCHOR_OPEN / NOT_WORKING_TRUTH / NOT_FOUNDATION`
Date: `2026-09-04`
Project: `Enterprise Math / 进取数论`
Supersedes as current frontier: `FREE_RESEARCH_PI_PRIME_GEOMETRY_FRONTIER_V15_20260904.md`
Researcher-ID: `EM-FREE-PI-PRIME-20260904`
Research-Mode: `FREE_AXIOM_DISCOVERY`

## 1. Stable geometric chain

The stable pi-to-prime interpretation remains

\[
\boxed{
\begin{aligned}
\tau^2
&=3!\lim_{M\to\infty}\det(I-B_M^{-2})^{-1},\\
\text{prime }p
&=\text{irreducible multiplicative birth direction},\\
p^a
&=\text{the }a\text{-th winding layer},\\
\det\mathcal W_M
&=\operatorname{lcm}(1,\ldots,M),\\
\psi(M)&=\log\det\mathcal W_M.
\end{aligned}}
\]

The new V16 advance concerns the error field

\[
r(N)=\psi(N)/N-1.
\]

After parity twist, the signless prime-power return equation becomes an ordinary gradient equation on the Hamming history carrier.  The apparent analytic `-1` mode is therefore a constant mode in parity-twisted history geometry.

---

## 2. Exact profile kernel and its arithmetic transfer

The V15 stopped/core scattering law gives

\[
(1-2s)^2+4s(1-s)=1.
\]

After the `S_3` standard energy is multiplied by `1/9`, the retained profile is

\[
\boxed{k(s)=1-\frac{32}{9}s(1-s).}
\]

V16 proves that the actual finite prime-power child-scale distribution inherits the Mellin multiplier

\[
\boxed{
M(\beta)=
\frac1{1-\beta}
-\frac{32}{9(2-\beta)}
+\frac{32}{9(3-\beta)}.}
\]

The unique critical exponent is the root in `(0,1)` of

\[
\boxed{9\beta^3-45\beta^2+86\beta-32=0,}
\]

namely

\[
\beta_*=0.481892\ldots.
\]

If the retained two-channel state satisfies the exact finite profile recurrence, then

\[
E(N)=O((\log N)^{-\beta})
\qquad(\beta<\beta_*),
\]

and a one-time quadratic scalar readout yields

\[
|r(N)|=O((\log N)^{-\beta/2}).
\]

This remains conditional on the complete two-channel recurrence.

---

## 3. Scalar recanonicalization is impossible

The row mean and stopped/core contrast satisfy

\[
m^2+c=x^2.
\]

After mixing,

\[
m^2+c/9=k(s)x^2.
\]

Since

\[
\min k=1/9,
\]

any uniform map reconstructing `x^2` from the mixed scalar energy must cost at least `9`.  It cancels the entire `1/9` gain.

Therefore the following architecture is ruled out:

\[
\text{mix standard}
\to\text{forget channel type}
\to\text{scalarize}
\to\text{repeat}.
\]

A quantitative proof must retain the mean/parity and standard relation channels until the final readout.

---

## 4. Persistent standard channel is exact

For a first action `a`, let

\[
m=\lfloor N/a\rfloor,
\qquad
\alpha=A(m)/A(N).
\]

In the second-action stopped/core row, the parent row mixer

\[
K_N^{\rm row}=\frac13I+\frac23\mathbb E[\cdot\mid a]
\]

restricts to the valid core as

\[
\boxed{
(K_N^{\rm row}F_N)|_{\rm core}
=K_mg_m+\text{constant}.}
\]

Hence the child mixed relation energy reappears without any inverse norm factor.  The exact row formula is

\[
\boxed{
\begin{aligned}
\mathbb E[(K_N^{\rm row}F)^2]
={}&((1-2\alpha)x+\alpha\eta)^2
+\alpha V(m)\\
&+\frac19\alpha(1-\alpha)(2x-\eta)^2,
\end{aligned}}
\]

where

\[
\eta=x+P_mr=O((1+\log m)^{-1}).
\]

The averaged residual forcing is

\[
O\left(\frac{\log\log N}{(\log N)^2}\right).
\]

Thus the standard channel is no longer the unresolved state component.  Only the global parity/root left inverse remains.

---

## 5. Exact one/two-history parity coupling

The return equation

\[
r+Pr=e
\]

implies

\[
\boxed{2r=(P^2-P)r+2e-Pe.}
\]

On normalized logarithmic scale:

- one adaptive history has limiting density `1`;
- two adaptive histories have limiting density `-log s`.

Their exact continuum overlap is

\[
\boxed{1-e^{-1}.}
\]

For `K` logarithmic blocks, the exact finite coupling inequality is

\[
\boxed{
\frac12|\nu(f)-\mu(f)|
\le(1-L)\|f\|_\infty
+\sqrt{L\mathcal V_{\rm block}(f)}.}
\]

Consequently

\[
|r(N)|
\le(1-L_{N,K})S(N)
+\sqrt{L_{N,K}\mathcal V_{N,K}(r)}
+O\left(\frac{\log\log N}{\log N}\right).
\]

If the matched block variance receives the existing `1/9` standard factor, the limiting homogeneous coefficient is

\[
\boxed{
e^{-1}+\frac13\sqrt{1-e^{-1}}
=0.632895\ldots<1.}
\]

This coefficient is conditional on an equivariant value-energy intertwiner, not on the already completed distributional coupling.

---

## 6. All-depth factorial/Gamma overlap

After `k` adaptive histories, the ideal remaining scale has density

\[
f_k(s)=\frac{(-\log s)^{k-1}}{(k-1)!}.
\]

Consecutive depths have exact overlap

\[
\boxed{
L_k=1-e^{-k}\frac{k^k}{k!}.}
\]

The unmatched mass

\[
e^{-k}k^k/k!
\]

is the central Poisson probability and is asymptotic to

\[
(2\pi k)^{-1/2}.
\]

This connects factorial provenance with the unique parity-law crossing defect at every history depth.

---

## 7. Uniform arithmetic approximation at growing depth

At an intermediate state `m`, the normalized logarithmic child-ratio law has

\[
W_1(K_m,\operatorname{Uniform}(0,1))
\ll(1+\log m)^{-1}.
\]

If `D_k` is the coupling error between the actual depth-`k` remaining scale and the ideal product-uniform scale, scale covariance gives

\[
D_{k+1}\le C/\log N+D_k/2.
\]

Therefore

\[
\boxed{D_k=O(1/\log N)}
\]

uniformly in `k`.

For `K` equal log blocks,

\[
\boxed{
\operatorname{TV}(\text{actual block law},\text{Gamma block law})
=O(K/\sqrt{\log N})}
\]

uniformly in history depth.  Hence

\[
\boxed{
L_{N,k,K}
\ge1-e^{-k}\frac{k^k}{k!}
-K^{-1}-O(K/\sqrt{\log N}).}
\]

Choosing

\[
K=(\log N)^{1/4},
\qquad
k\asymp c\log\log N,
\]

gives the explicit distributional defect

\[
1-L_{N,k,K}
=O((\log\log N)^{-1/2})
+O((\log N)^{-1/4}).
\]

The triangular-array distribution problem is therefore closed.  What remains is solely value-sensitive relation energy inside matched blocks.

---

## 8. Parity twist and the radial Hamming gap

For a subset history `I`, define

\[
G(I)=(-1)^{|I|}r(q_{a_I}(N)).
\]

Then every Hamming edge satisfies

\[
\boxed{
|G(I\cup\{j\})-G(I)|^2
=|r(q_{a_I})+r(q_{a_{I\cup\{j\}}})|^2.}
\]

Thus signless residual energy is an ordinary parity-twisted Dirichlet form.

On the ideal product-bounded degree-`R` shell carrier:

- top slack is `Beta(1,R)`;
- continuation-weighted leaf slack is `Beta(2,R-1)`;
- the radial up/down singular values are
  \[
  \boxed{
  \sigma_j(R)=
  \sqrt{\frac{R-1}{(j+1)(R+j-1)}}.}
  \]

The first nonconstant value is

\[
\sigma_1(R)=\sqrt{(R-1)/(2R)}<1/\sqrt2.
\]

Therefore every nonconstant radial channel has a uniform Poincare gap.  The only kernel is the common parity-twisted constant, exactly corresponding to the persistent arithmetic `-1` mode.

This rules out further searches for a missing nonconstant spectral gap.  The sole mathematical obstruction is anchoring that constant mode to zero in the finite atomic endpoint system.

---

## 9. Two equivalent final bridge targets

V16 leaves two concrete, equivalent-strength routes.

### Route A — adaptive matched-block energy

Prove

\[
\boxed{
\mathcal V_{N,k,K}(r)
\le
\text{retained }S_3\text{ standard history energy}
+\text{summable lower-scale terms}.}
\]

All distributional errors are already explicit and tend to zero uniformly at growing depth.

### Route B — product-bounded shell anchor

For the actual top and continuation-leaf endpoint measures, either:

1. construct a common exact-endpoint submeasure of uniformly positive mass; or
2. dominate the block/cell mismatch by the parity-twisted radial incidence Dirichlet form.

The uniform radial gap then controls every nonconstant component, and the common endpoint anchor kills the remaining constant mode.

---

## 10. Correct current boundary

Closed:

1. prime birth/winding determinant geometry;
2. qualitative PNT by the already recorded real-smoothing route;
3. V14 complete provenance and density bridge;
4. V15 parity-fold scalar readout and profile kernel;
5. arithmetic transfer of the profile kernel;
6. critical polynomial and conditional exponent;
7. scalar-recanonicalization no-go;
8. exact persistent standard-channel intertwiner;
9. exact one/two-history block coupling;
10. all-depth Gamma/Poisson overlap;
11. uniform growing-depth Wasserstein/block approximation;
12. parity-twisted radial shell spectrum and uniform nonconstant gap.

Open:

1. one value-sensitive constant-mode anchor/intertwiner, in either Route A or Route B form;
2. a closed retained two-channel recurrence;
3. a terminal one-time scalar coercive readout;
4. a promoted native quantitative bound for `psi(N)-N`;
5. any RH-scale conclusion;
6. Working Truth or Foundation promotion.

The V16 compression is:

\[
\boxed{
\begin{aligned}
\text{all nonconstant history modes}
&\longrightarrow\text{uniformly gapped},\\
\text{distributional parity mismatch}
&\longrightarrow\text{explicitly vanishing},\\
\text{remaining obstruction}
&\longrightarrow\text{one atomic constant-mode anchor}.
\end{aligned}}
\]

No quantitative prime remainder is claimed yet.  The program has nevertheless reduced the former broad density/spectral problem to one sharply stated positive finite-energy comparison.
