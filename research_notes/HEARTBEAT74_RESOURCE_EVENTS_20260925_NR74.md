# Heartbeat74 — native residual resource records, two-way exclusivity and a three-way obstruction

Progress-Event-ID: HEARTBEAT74-RESOURCE-EVENTS-20260925
Status: SAME_AUTHOR_CONDITIONAL_MODEL / ACTUAL_TYPED_BRC / NOT_ADMITTED
Researcher-ID / Research-Activity-ID: none newly assigned. Earlier safety-blocked registration is not retried, bypassed or replaced by a closed identity. Persistence is provenance, not admission.

## 0. Frozen native-residual scope

Continue Stage73 cumulative e21c8d650c246e724ecfc8c69d92c96e4fe091f1 from the actually mounted bundle, SHA256 2a31c4eb1cf60340537d388e630e3be462feb40728714a44e6ad268a99ee5796 verified before LOCAL clone. Global immutable read5cb74aba578d5b7e3a9e5c5fce31f0c62c47b16f; Source readabade8618188294d91c9f4f04ff30d347546003e. Unchanged bootstrap/manual/project/P000 and BRC-only blobs were verified and their full rules reused. Six native axes and independent time type remain unchanged.

This continues the user's residual-first direction, NOT the older quantum-instrument branch. Reuse Stage73 C1:a_i+b_i=phi and C2:sum g_i(b_i-a_i)=s. Source-free closure preserves Q=sum g_i a_i^2. This field is not automatically the older material-bearing Gauss field. No rho, Born sampler, complex phase gate or old quantum propagator is imported. Counter initial distributions are ordinary explicitly declared classical preparations, not squared amplitudes.

New assumptions are explicit: matched local receiver modes; finite ready receiver slots; a fixed resource tick delta and threshold Q0=M*delta; additive integer carry; for the two-counter construction, a correlated initial residue constraint; for single-shot probability, a uniform initial residue distribution. Q is not calibrated energy, a count is not a conserved particle identity, and neither the threshold nor the correlation follows automatically from C1/C2. These assumptions are tested rather than hidden in the word readout.

## 1. Actual receiver and integer resource accounting

The same two-port equal-weight closure exchanges (a,d)->(d,a). An arriving native link residual coupled to a ready internal slot transfers its COMPLETE signed fibre to that slot, returning zero on that link; other native ports are untouched. Matching and inverse use actual Stage73 Junction((1,1)) BRC columns. The apparatus tape retains source/cell/port/event/sample data and has finite capacity. A used slot is not silently reset: receiving4 into a slot holding3 returns3 to the field and leaves4 stored, so storage increased7, not16. This actual counterexample is checked.

For deposited q_k/delta=n_k an integer, residue0<=h<M, define x_k=floor((h+n_k)/M), h'=h+n_k-M*x_k. Each fixed-q transition is compiled as an actual positive BRC deterministic graph, preserving old residue, incoming budget, new residue and mark increment. Undefined tick divisibility is rejected rather than rounded.

Exactly delta*(M*C_N+h_N)=delta*h_0+sum q_k. Thus |C_N-sum q_k/Q0|<1. With unbounded total exposure, common threshold and no loss, long-run count ratios follow deposited Q ratios. Since Q is quadratic in residual, squared-field count rates can arise WITHOUT Born event sampling. This is an additional ideal resource-counter law, not a derivation of all detector physics. The raw receiver tape and C_N*Q0+h_N*delta are two representations of the SAME stored resource; they must not be added as two energies.

Chunking an already received resource into independent deposits leaves final count/carry unchanged. It does not mean overlapping incoming amplitudes can be squared separately. If a nonnegative rational response f respects exact resource refinement f(q+r)=f(q)+f(r), then f(q)=f(1)q. This supplementary additivity rules out f(Q)=Q^2 in that class only; it is not a universal detector/Born theorem.

## 2. Two detectors: exactly one record from complementary residuals

Suppose each pulse genuinely deposits total Q0, so n1+n2=M. PREPARE h1+h2=M-1. Two local counters act without reading each other's present reports. Their total pre-carry input is2M-1: neither can both remain belowM (sum<=2M-2), nor both crossM (sum>=2M). Therefore x1+x2=1 and h1'+h2'=M-1. The condition persists through repeated complete pulses. No input renormalization, loss exclusion or postselected click sample is used.

For an independently specified uniform initial h1 in0..M-1, h2=M-1-h1, exactly n1 seeds click at detector1. Hence P1=n1/M=q1/Q0 and P2=q2/Q0. The uniform prior is generated by an actual positive BRC preparation graph; no code samples a squared wave norm. Uniformity is an additional preparation or finite-cycle symmetry assumption. A fixed biased seed also gives exclusivity but different first-event probabilities.

Actual native packet(-1,-3,1,...,1) through the inherited12-port closure emits(2,4,0,...,0), totalQ20. Real native transport sends the two nonzero modes to-e1 and+e1. With delta4,M5, every complementary seed produces one actual stored-resource record and the uniform seed probabilities are1/5,4/5. Every seed's field+receiver budget and full net-field inverse pass. An independent balanced packet(-2,-2,1,...,1) emits(3,3,0,...), Q18, delta9,M2. Different pulse resources/ticks are stated rather than called the same calibrated photon energy. These are declared signed residual preparations; their material-source origin remains open.

## 3. Correct frequency still does not give independent single events

Fixed per-pulse n1=q gives h1(N)=(h1(0)+Nq)modM and C1(N)=floor((h1(0)+Nq)/M). Balanced M even, q=M/2 forces strict alternating left/right records. Each shot has marginal1/2 under the uniform seed, but P(left then left)=0, not1/4 for an independently prepared Bernoulli-event comparator. Actual six-shot histories are the two alternating sequences, each weight1/2. M5,q1 gives exactly two left counts in ten shots, variance0; M7,q3 gives six in fourteen. These are complete classical seed histories from actual BRC transition columns, not independent resampling.

Biased seed(4,0) atM5,q1 yields first left probability1, while five total shots still have exactly one left record. Thus even perfect long-run squared-resource frequencies plus exclusion do not uniquely establish a one-shot probability law. Fresh independent complementary preparation on each trial would remove this specific temporal dependence, but is a NEW correlated refresh mechanism with physical preparation/control/reset costs and records, not deletion of a log.

For balanced M2 independent uniform local residues, the four patterns00,01,10,11 each have probability1/4. The single-detector marginals equal the correlated construction, but exclusion fails. A mixture(1-epsilon)*complementary+epsilon*independent with epsilon1/100000000 keeps the local marginals and gives no-click and double-click each1/400000000. This perturbation keeps average precharge but changes sharp-total precharge; it is not silently treated as satisfying h1+h2=M-1.

## 4. Universal three-way impossibility for this threshold class

THEOREM: for M>=2,d>=3, no input-independent distribution on residuesh_i in0..M-1 makes sum_i floor((h_i+n_i)/M)=1 with probability1 for EVERY integer allocationn_i>=0,sum n_i=M.

Proof for a fixed residue vector: concentrating input on any pair(t,M-t) forces h_i+h_j=M-1, because the two threshold intervals must partition all t0..M without overlap or gap. Three pair constraints force all threeh=(M-1)/2. EvenM is impossible over integers. For oddM>=3 the split(h,h,1) has totalM and every counter remains below threshold. More counters reduce to a triple. A random preparation cannot repair this: finitely many input constraints of probability1 have a probability1 intersection, which has no residue vector. M1 already makes incoming resource indivisible and is an explicit excluded trivial case.

Finite complete BRC-column checks cover all seeds/splits forM2..8, but the all-M theorem comes from the proof. It is NOT an impossibility theorem for arbitrary residual fields, adaptive thresholds, material tokens, added interactions or input-dependent preparations.

Real three-port packet has three incoming entries-1/2 and nine+1/2, generating exactly three outgoing1s,Q3. Three actual neighbouring receivers withM3,delta1,h=(1,1,1) produce no records on the first equal pulse and THREE records on the second. Complete resource accounting holds. A long-time mean of one record per pulse is not per-pulse exclusivity.

## 5. Sharp quantitative finite-model bound, not merely a failed seed

AtM3,d3, ANY input-independent joint residue law has at least one allocation with non-single-record probability>=1/4. The lower bound is attained, so the best guaranteed exactly-one success is3/4 for this class.

Use six probe permutations(2,1,0), each weight1/8, and probe(1,1,1) weight1/4. For a residue vectorh, letf be its failure count among the six unequal probes andt its0/1 failure on the equal probe. The complete27-vector BRC certificate satisfiesf+2t>=2. If t1 this is immediate; if t0 exactly one residue equals2 and the others are0 or1, and those four cases each givef2. Thus probe-average failure>=1/4 for every seed, hence every seed distribution.

An attaining BRC preparation usesh111 weight1/4 and all sixh012 permutations weight1/8. ALL seven microstates have the same sharp precharge sum3. Every six unequal probe and the equal probe has failure1/4; the three all-in-one allocations have failure0. All10 allocations were executed. The number25% concerns this M3 class, not a physical constant or a universal photon-detection limit.

## 6. Residual interference actually enters the integer record

Stage73's two positive source injections are reused, not called a new interference theorem. At the selected outgoing native link, individual contributions are-5,+5 and the simultaneous net is0 with formal excess/deficit lanes5/5 preserved. A true local matched receiver followed byQ0=25 counter with initialh0 turns the three experiments into marks1,1,0. Full field+receiver Q remains432,10800,11232 respectively. Source strengths and total pulse resources differ, no click-only normalization is applied. This demonstrates discrete resource marks fed by a native dark outlet, NOT a single particle through two paths.

ResourceCounter keeps finite receiver tape and full signed sample provenance, uses actual counter columns, treats retries passively, and rejects conflicting events, exhausted ready slots or unresolved ticks before state mutation. Retained receiver values permit exact net-field reversal. No claim that a software record supplies a physical apparatus or erases all residual content is made.

## 7. Executed evidence and delivery

Final fresh process115 named checks/56 actual unchanged canonical BRC calls. Corepinbc7babbb9e890f6d5a7094430a5fbdccf66c77ad:src/enterprise_math/brc_weighted_recurrent.py,blob4e6b3132580e3cd70a20a0d8bd4d28792b961afb,SHA2567520822074b8f29e1c54f57b9543c043202db8bd7c6e27f3f1d6176028ac4a26. NativeJunction reused unchanged; counter transitions and preparation probabilities are positive typed finite graphs. No quantum propagator, trigonometry, eigenvalue solver or numerical optimization.

Cumulative5d25ff21cf9549746eef4a19716e8d1f82228b84; entrySTART_HERE_STAGE74.md; stage74/PROOF.md,resource_reader.py,run.py,RESULTS.json,BRC_CONTRACT.json,PROVENANCE.json,IMPLEMENTATION_NOTES.md,MANIFEST.json. Fresh LOCAL clone replay matched all science fields excluding onlyelapsed_ns recursively. Original352712225ns,replay313993075ns retained.1344 inherited files unchanged,8 manifest entries checked,git fsckPASS. A first replay invocation used the parent workdir and failed to find the module; the clone had succeeded, then execution in that clone completed. No scientific data were edited to repair that command.

BundleBRC_Heartbeat_resource_events_stage74_20260925.bundle:38275165bytes,SHA256b8533e7e55f00c4772d0c791c5b2a0879a5c63f56393ddbfc87b01ea76842646. Drive1n2vneFM82jj3pMsi_paM-7H33vwKw3hj uploaded,metadata and raw bytes fetched; actual local cmp and hash identical. ProofSHA256a7444ec0b2e90e47553cd7582f9e958e0640f6d2ee90d5115d161a0afbe89d13;code00f2efc462e13b36578f2fd50072de4fa31ba35742ba632b790b5d91d7259ff7;results837b270048fda2b2ef60103225a2f520d47c36877af52fa540deb72ef0f31e0f. Local delivery/mnt/data/heartbeat74/DELIVERY.json.

Official primary abstracts/metadata read: Jacques etal EPJD35,561–565(2005),arXiv2011.12664v1 uploaded2020; it reports fringe buildup and a separate absence-of-coincidence experiment. Brida etal JPSJ82,034004(2013),DOI10.7566/JPSJ.82.034004,NIST author page reports an experimental distinction for a PARTICULAR event-based model, not a test of this new construction. Grangier1986 IOP access was robots-blocked; no full text claimed. Generic threshold counting and anticorrelation are not asserted new. No exhaustive novelty or empirical data fit. Current inspected KQB capabilities blobc0737f0940fdb6bc814cb8cb35cdbf55dbfa1dfc still has no scholarly operation; no unlisted query or provider success invented.

NEXT: create or rule out a local material-identity-bearing interaction which provides the joint event constraint without input-aware seed preparation or unaccounted refresh, while retaining the residual field and passing a three-output AND temporal test. Do not repeat this two-counter law as a Born derivation. A count is not yet an indivisible material object. Full photon phase/tensor composition, source-to-residual bridge, autonomous timing, thermal amplification/reset and experimental calibration remain open. Same-author proof/code/replay, no independent review/Lean or remote server. Parent physics objective and activity registration remain incomplete.
