# Native Enterprise sharp-nine scan through r=350000: Hardy-Littlewood-type null match

Status: `FREE_RESEARCH_FINITE_NULL_MODEL_DIAGNOSTIC / NOT_CANONICAL_FOUNDATION / NO_NOVELTY_CLAIM`

Date: `2026-08-23`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Depends on:

- `NATIVE_ENTERPRISE_SHARP_NINE_ENDPOINT_HOLOGRAPHY_AND_DUAL_TANGENT_SIEVE_20260823.md`;
- `research_notes/data/NATIVE_ENTERPRISE_SHARP_NINE_SCAN_R350000_20260823.csv`.

## 1. Frozen finite scan

The exact mod-30 sharp channels through central shell `r<=350000` contain

`408333333`

valid `(r,h)` candidates before q>=7 filtering.

The exhaustive deterministic scan found exactly

`20`

sharp nine-prime islands:

- `11` with chi=+1;
- `9` with chi=-1.

## 2. Conditional small-prime normalization

Every candidate in the two exact mod-30 channels already forces all nine Cell labels to avoid primes 2,3,5.

Relative to an unrestricted nine-prime heuristic, this supplies the fixed conditional factor

`(30/phi(30))^9=(30/8)^9`.

For each q>=7 define the local factor

`beta_q^chi = (N_q^chi/q^2)/(1-1/q)^9`,

where `N_q^chi` is the exact number of q-avoiding `(r,c)` residue pairs in the nine-line dual-tangent arrangement.

The exceptional q<=53 counts are taken exactly from the classified line arrangement; for q>53,

`N_q=q^2-9q+36`.

## 3. Finite local products

Multiplying the exact factors over primes `7<=q<=1,000,000` gives

`S_+ ~= 1.14216477556237`,

`S_- ~= 1.15249711093452`.

At q=53 the partial products are

`1.13854542144223` and

`1.14884501514576`,

so the common generic tail beyond53 contributes only about

`1.00317892818`.

The ratio of the two products is already fixed by q=13 and23:

`S_+/S_- = 29736/30005 ~= 0.9910348275`.

## 4. One-scale finite expectation

For a candidate at central shell r, the nine labels are all asymptotic to

`X_r=(3/2)r^2`.

Using the finite diagnostic probability

`Prob_chi(r) ~= (30/8)^9 * S_chi / log(X_r)^9`

and summing over the exact number of allowed h values in each sharp mod-30 channel through r=350000 gives

- chi=+ expected count: approximately `10.28`;
- chi=- expected count: approximately `10.41`;
- total expected count: approximately `20.69`.

The actual counts are `11`, `9`, total `20`.

This is a close null-model match.

## 5. Interpretation boundary

The finite comparison supplies no evidence that the observed sharp-nine frequency exceeds or departs materially from a classical Hardy-Littlewood / singular-series explanation.

Therefore the current research claims should distinguish:

### Exact native structural results

- sharp global component cap9;
- prime5 unique connectivity breaker;
- mod30 tangency channels;
- prime-valued curvature/recurrence code;
- endpoint interior reconstruction;
- finite dual-tangent exceptional spectrum ending at53;
- long high-dimensional connectivity plateau witnesses.

### Not currently supported as new statistical law

- anomalous abundance of sharp-nine islands;
- nonclassical chirality bias;
- prime-count enhancement beyond local singular-series effects.

The coordinate system organizes the prime set very strongly, but the current finite event rate remains compatible with classical local prime-tuple statistics.

## 6. Boundary

This is a finite heuristic/null comparison, not a proof of a Hardy-Littlewood asymptotic and not an external novelty claim.
