# Free Research — Prime-Winding Partition Geometry of the Full-Turn Completion

Status: `FREE_RESEARCH_FRONTIER / ANCHOR_EXPOSED / NOT_WORKING_TRUTH / NOT_FOUNDATION`
Date: `2026-09-04`
Parent: `FREE_RESEARCH_PI_PRIME_GEOMETRY_SYNTHESIS_20260904.md`

## 1. Purpose

The birth-block channel identifies primes as irreducible multiplicative directions and gives

\[
\tau^2=3!\lim_{M\to\infty}Z_M(2),
\qquad
Z_M(2)=\prod_{p\le M}(1-p^{-2})^{-1}.
\]

This note gives the Euler determinant a direct finite branch/occupation meaning and thereby extends the geometry from primes to **all composites and prime powers**.

The construction is deliberately finite before completion so that every branch weight is positive rational and compatible with current Weighted-BRC typing.

---

## PWG-T01 — Finite prime-winding state space

Let

\[
\mathcal P_M=\{p\le M:p\text{ prime}\}.
\]

Fix an exponent cutoff `K>=0`. Define the finite winding state space

\[
\boxed{
\mathcal E_{M,K}
:=\{e:\mathcal P_M\to\{0,1,\dots,K\}\}.
}
\]

For `e in E_(M,K)`, define its multiplicative state

\[
N(e):=\prod_{p\in\mathcal P_M}p^{e_p}
\]

and quadratic branch weight

\[
\boxed{
w_2(e):=N(e)^{-2}=\prod_{p\in\mathcal P_M}p^{-2e_p}.}
\]

All weights are exact positive rationals.

Typed interpretation:

- `p` is a prime-birth direction;
- `e_p` is the winding/occupation number of that direction;
- `p^k` is a pure `k`-winding state;
- a composite integer is a multi-direction occupation state.

This is an arithmetic holonomy/occupation fiber, not a new spatial coordinate system.

---

## PWG-T02 — Exact finite factorization of the winding mass

Define the finite total mass

\[
Z_{M,K}^{\rm wind}
:=\sum_{e\in\mathcal E_{M,K}}w_2(e).
\]

Because the state space is a finite Cartesian product and the weight factors coordinatewise,

\[
\boxed{
Z_{M,K}^{\rm wind}
=\prod_{p\le M}
\left(\sum_{a=0}^{K}p^{-2a}\right).
}
\]

Each geometric sum is exact rational:

\[
\sum_{a=0}^{K}p^{-2a}
=\frac{1-p^{-2(K+1)}}{1-p^{-2}}.
\]

Hence

\[
\boxed{
Z_{M,K}^{\rm wind}
=\prod_{p\le M}
\frac{1-p^{-2(K+1)}}{1-p^{-2}}.
}
\]

No logarithm, floating spectrum or infinite branch space is needed at this stage.

---

## PWG-T03 — Remove the winding cutoff

For fixed finite `M`, the finite masses increase monotonically in `K` and

\[
\lim_{K\to\infty}Z_{M,K}^{\rm wind}
=\prod_{p\le M}(1-p^{-2})^{-1}
=Z_M(2).
\]

Equivalently,

\[
\boxed{
Z_M(2)
=\sum_{e\in\mathbb N_0^{\mathcal P_M}}
\prod_{p\le M}p^{-2e_p}.
}
\]

By unique factorization, the map

\[
e\longmapsto N(e)
\]

identifies these winding vectors with positive integers all of whose prime divisors lie in `P_M`. Therefore

\[
\boxed{
Z_M(2)
=\sum_{\substack{n\ge1\\p\mid n\Rightarrow p\le M}}
\frac1{n^2}.
}
\]

So the finite prime-birth determinant completion already sums the complete composite tower generated by the born directions.

---

## PWG-T04 — Remove the prime cutoff: `tau^2/3!` is total winding mass

As `M` increases, every positive integer eventually enters the smooth-number state space. Hence

\[
\lim_{M\to\infty}Z_M(2)
=\sum_{n\ge1}\frac1{n^2}.
\]

The current internal #1159 completion gives

\[
\sum_{n\ge1}\frac1{n^2}=\frac{\tau^2}{3!}.
\]

Therefore

\[
\boxed{
\frac{\tau^2}{3!}
=\sum_{e\in\mathbb N_0^{(\mathbb P)}}
\prod_p p^{-2e_p}.
}
\]

Here `N_0^(P)` means finite-support prime occupation vectors.

This gives the strongest current occupation-language reading:

> `tau^2/3!` is the total quadratic mass of all finite winding configurations built from the arithmetic prime-birth directions.

The factor `3!` is separately realized by the six-history Hamming shell-3 provenance fiber in the same #1159 parent carrier.

---

## PWG-T05 — Finite normalized branch ensemble

For finite `M,K`, define

\[
\boxed{
\mu_{M,K}(e)
:=\frac{w_2(e)}{Z_{M,K}^{\rm wind}}.
}
\]

Then

\[
\sum_{e\in\mathcal E_{M,K}}\mu_{M,K}(e)=1.
\]

Because both the weight and normalization factorize,

\[
\boxed{
\mu_{M,K}(e)
=\prod_{p\le M}
\frac{p^{-2e_p}}
{\sum_{a=0}^{K}p^{-2a}}.
}
\]

Thus different prime-birth directions are exactly independent in the finite quadratic occupation observer.

This is a statement about this particular positive rational weighting. It does not imply that all Enterprise prime observables factor independently; the separate C3 orientation channel is signed/character-valued and remains differently typed.

---

## PWG-T06 — Infinite-cutoff local winding law

For fixed finite `M`, let `K -> infinity`. The marginal occupation law at a prime `p<=M` becomes

\[
\boxed{
\Pr(e_p=a)
=(1-p^{-2})p^{-2a},
\qquad a\in\mathbb N_0.
}
\]

So the number of repeated windings in each prime direction has an exact geometric distribution under the quadratic completion measure.

In particular,

\[
\Pr(e_p\ge1)=p^{-2},
\]

and

\[
\mathbb E[e_p]=\frac1{p^2-1}.
\]

These are derived probabilistic readouts of the positive weighted completion, not primitive branch counts.

---

## PWG-T07 — Primorial versus saturated winding envelope

The determinant of the finite birth block records one copy of every born direction:

\[
\det B_M=\prod_{p\le M}p.
\]

A different exact integer records the **largest allowed winding of every direction below scale `M`**:

\[
\boxed{
\operatorname{lcm}(1,2,\dots,M)
=\prod_{p\le M}p^{\lfloor\log_p M\rfloor}.
}
\]

The exponent

\[
a_p(M):=\max\{a:p^a\le M\}
\]

can be defined without logarithms. Thus the exact native arithmetic carrier is the valuation vector

\[
(a_p(M))_{p\le M},
\]

while the classical Chebyshev function

\[
\psi(M)=\sum_{p^a\le M}\log p
=\log\operatorname{lcm}(1,\dots,M)
\]

is only its derived logarithmic readout.

Geometrically:

- primorial = first-winding envelope;
- lcm = maximally saturated prime-winding envelope at cutoff `M`.

---

## PWG-T08 — Von Mangoldt as derived winding-event readout

The logarithmic derivative of the infinite prime determinant has the classical expansion

\[
-\frac{\zeta'(s)}{\zeta(s)}
=\sum_{p}\sum_{a\ge1}
(\log p)p^{-as}
=\sum_{n\ge1}\frac{\Lambda(n)}{n^s}.
\]

In the current typing this means:

- the exact combinatorial event is a pure winding `p^a`;
- its primitive direction label is `p`;
- `log p` is a derived real weight;
- `Lambda(n)` vanishes unless `n` is a pure repeated winding of one prime-birth direction.

No logarithm is required to state the underlying winding-event inventory.

This gives a clean route from the finite birth block toward prime distribution without changing the underlying geometry.

---

## 9. Relation to earlier project work

Historical Draft #333's primitive-generator / Omega-word compiler shares the arithmetic fact that a positive integer is encoded by its prime exponent vector. This note does not claim novelty for that compiler substrate.

The new typed role here is different: the exponent vector is used as the occupation state of the exact Krawtchouk arithmetic birth block, and the quadratic mass is tied to the internal full-turn completion `tau^2/3!`.

Relation: `COMPOSABLE_INDEPENDENT / SHARED PRIME-EXPONENT CARRIER`.

---

## 10. Next asymptotic frontier

The exact finite state now naturally exposes the classical prime-distribution observables:

\[
\pi(M)=\text{birth rank},
\]

\[
\vartheta(M)=\text{derived log determinant of first windings},
\]

\[
\psi(M)=\text{derived log mass of the saturated winding envelope}.
\]

The prime number theorem would become the asymptotic statement

\[
\psi(M)\sim M
\]

for this winding geometry. No such asymptotic is proved in this note.

This is the next natural frontier if the research is continued toward zeta zeros and prime distribution: first derive a native/spectral statement controlling the saturated winding envelope, rather than jumping directly to the Riemann hypothesis.

---

## Current classification

- finite prime-winding state space: `CLOSED / FINITE`.
- finite rational factorization `Z_(M,K)`: `CLOSED`.
- fixed-M infinite winding completion: `CLOSED / GEOMETRIC SERIES`.
- all-prime completion `tau^2/3!`: `RESEARCH-PROVED GIVEN #1159 PRODUCT`.
- finite factorized probability ensemble: `CLOSED / DERIVED WEIGHTED READOUT`.
- prime-power winding inventory / lcm envelope: `CLOSED / EXACT ARITHMETIC`.
- von Mangoldt interpretation: `CLASSICAL DERIVED LOG READOUT`.
- prime number theorem in this geometry: `OPEN`.
- Riemann-hypothesis consequence: `NOT CLAIMED`.
