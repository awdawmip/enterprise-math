# Three raw X6 frontier entries: mathematical L4 transport

Channel: `MATHEMATICAL_L4`.
Scope: `NO_NEW_MATHEMATICS_SOURCE_TRANSPORT`.
Status: integration candidate; the actual execution result is recorded separately.

This package transports eight immutable source files and a complete three-entry
registration shard onto main `0242e5ccbc6b134241f083e6204de2bd47393e78`, tree
`1f3f3a5bc7c4a2b64c273a99bdac4bc1f8bfd926`. It grants no Task, claim, formal
Result/review, WorkingTruth or Foundation status. `NO_NEW_MATHEMATICS` describes
source transport here; it is not a control-maintenance exemption from
mathematical L4 admission.

The exact publication source is owner commit
`f67c8d991cb8781f03285925a30cfefee743d3c9`, tree
`f1d40c9c6d5e7e7338d023caec1a1cb65a3fc592`. Its equivalent local preparation is
`8bc4a78a53a29865841bacc332e213f92d41a36a`. The compression package and p=3 probe
were published in `c0495fd71a557ed78eef7ab62a8b2bf08feee535`; the equality paper
was already in `d11e10126170335cc520fb9ce809682d76aa8bfe`. The copied shard
retains these original publication distinctions and separately records the narrow
Path-join compatibility revision from immutable parent
`ad2798bcc9d6f5b3c57bbd0fd292015f67390583`.

## Exact twelve-path closure

Eight files retain their source bytes:

- `experiments/owner_positive_support_compression_20260908/`:
  `check_positive_support_compression.py`, `certificate.json`, `README.md`;
- its `independent_review_20260908/`: `review.py`, `review.json`, `REVIEW.md`;
- `research_notes/OWNER_TWO_POSITIVE_EQUALITY_CLASSIFICATION_REVIEW_20260908.md`;
- `research_notes/OWNER_THREE_POSITIVE_RAW_STABILITY_PROBE_20260908.md`.

The other four paths are the complete unchanged
`research_method_inventory_addenda/20260908_owner_raw_stability_extensions.json`,
the main adapter and actual receipt in
`experiments/owner_raw_stability_extensions_harvest_20260908/`, and this note.
The shard SHA256 is
`506bc35b11c211f351d26ac0f19e1f3ba09b841521cfb49302067f69b293b571`;
all three method objects therefore retain their exact newly published source
bytes. The two RESULT objects are also unchanged from the earlier publication;
the consumer entry updates only its three independent-package evidence pins.

The original compression proof already exists on main and remains an exact
source pin. All fourteen runtime dependencies already exist on the baseline
and are verified in place. The old 20260907 main shard stays at two tools,
and the earlier 20260908 raw-result shard stays at two paper results. Their
full bytes, four objects, source packages and old validators are protected
with every other baseline entry. Other owner candidates are not part of this
closure.

## Registered scope

`candidate.x6.positive_support_raw_compression_certificate` exposes only
`audit_compression(case_id, mu_atoms, nu_atoms, denominator=1)`. It checks the
twenty raw three-axis tables after Jordan cancellation. Low-level `pushforward`
does not guarantee preservation for an arbitrary specification. The paper's
all-coordinate-subset theorem is broader than the executed table set.

`result.x6.two_positive_equality_rectangle` has no API. For nonzero p<=2,
`D=4M` is equivalent to an equal-weight rectangle on original raw coordinates.
`result.x6.three_positive_raw_mass_boundary` also has no API: `M=7,D=26`
supplies an arbitrary-mass lower bound; the equal-mass `D>=4M` and equality
interval remain confined to the specified symmetric family.

All entries remain candidates, with null family and formal acceptance.
Original papers and evidence keep their historical status text. The current
integration neither re-proves their general statements by examples nor
extends the p=3 upper-bound scope.

## Preserved initial failure and exact source correction

The earlier local input commit `6b35ff4546ed090ffbd0731f765f6fbbdc6f44c4`
(tree `0183bf57ab11847733883a51005121df2acfe4c0`) passed registration,
the old API check and the original independent replay. Its selected static
arithmetic check failed on exactly five `pathlib.Path` `/` expressions in the
independent script. That input commit and its failure/preservation evidence
remain unchanged; it did not produce a main PASS receipt.

The source owner then published an exact five-expression correction to
`joinpath`, with no other AST change, and honestly regenerated the independent
receipt and explanation. The source registration pins were updated after a
fresh bounded replay. The author consumer/certificate/README, all fourteen
runtime dependencies, both RESULT objects and mathematics were unchanged.
This integration now copies the revised source bytes and reruns the same
bounded four-command composition on newly committed inputs. The current
checker is unchanged, and the selected static step is retained.

The old source review receipt SHA256
`8ee1608a8e32d442fa788d797936278b50752c84439c28c9af3f1373ac5fd43e`
remains historical in the immutable parent publication. The current copied
source receipt is the new `2b69f5e837c8067bb266931e438b4207e394aff16f0f40c56d4d759dddeb844a`
receipt. The main run writes its own independent output only to fresh TEMP.
Neither historical execution is relabelled as execution on the new main input.

## Reproducible bounded composition

The eleven inputs are committed before execution. Only the new main receipt
is added in the subsequent commit. Its `actual_input_head` and
`actual_input_tree` identify the tested input; it does not claim its own later
receipt commit was the input.

Run from the integration checkout with a fresh external evidence directory
and the caller's actual global-knowledge read SHA:

```powershell
python -B -X utf8 experiments/owner_raw_stability_extensions_harvest_20260908/validate_registration.py --evidence-dir D:/em/TEMP/owner-raw-extensions-main-20260908-run-02 --global-read-sha eb09a0ac3a329cdb2b854619784e3c3339f9ef70
```

The driver runs four bounded commands covering three requested checks:

1. The source registration `validate()` function is copied verbatim, SHA256
   `b45892f3ec1716583f529ea8a1379baa028ece469684514c24d464d90f6d3a13`.
   It validates three unique IDs, six queries, the one real API/signature,
   source pins and fourteen runtime pins. The main adapter additionally
   requires actual prior shard counts `[2,2]` and preserves all four objects.
   It executes the unchanged old L4 `API_CHECK` string to inspect the old
   seven APIs, without invoking the old L4 mathematical consumers.
2. A fresh TEMP directory receives the published independent `review.py`
   byte-for-byte. It is run once with this main root explicitly supplied:
   one fresh author-default replay plus the five independent cases and
   three rejection checks. Only TEMP receives the new `review.json`; the
   original source review receipt with SHA256
   `2b69f5e837c8067bb266931e438b4207e394aff16f0f40c56d4d759dddeb844a`
   remains unchanged on main.
3. The current-main arithmetic checker examines only the new compression
   consumer and its independent mathematical review script.

Each command records its actual argv, cwd, input head/tree, elapsed time,
exit code and output log hashes. The master `validation.json` binds these
results, the new TEMP independent receipt, and a full baseline byte proof.
All baseline mode/type/blob identities and actual working bytes must match
the base before and after execution. No broad suite, old long consumer,
remote action or CI polling occurs in this driver.

The source registration receipt's `[19,2]` observation remains historical at
[immutable owner source](https://github.com/awdawmip/enterprise-math/blob/f67c8d991cb8781f03285925a30cfefee743d3c9/experiments/owner_raw_stability_extensions_harvest_20260908/validation.json).
The current main receipt records its own actual `[2,2]` observation. The
existing `registration_authoring_global_knowledge_sync` describes source
authorship; the main execution records the supplied current snapshot in a
separate field. Future replays must use their actual snapshot instead of
treating this example's SHA as a perpetual lease.

This note specifies the input and execution contract, not a pre-execution
PASS. Local combination evidence remains separate from root's independent
review, actual CI results and expected-head main admission.

Global-Knowledge-Sync: main@eb09a0a / GLOBAL_KNOWLEDGE_V1
