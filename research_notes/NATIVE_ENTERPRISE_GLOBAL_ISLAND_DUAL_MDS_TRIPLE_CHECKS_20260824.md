# Native Enterprise global prime islands: dual MDS triple checks

Status: `FREE_RESEARCH_EXACT_DUAL_CODE_GEOMETRY / NOT_CANONICAL_FOUNDATION`

Date: `2026-08-24`

Researcher-ID: `EM-FREE-NEPS-239A6D`

Depends on:

- `NATIVE_ENTERPRISE_GLOBAL_PRIME_ISLAND_AFFINE_MDS_CODE_SPECTRUM_3_TO_9_20260824.md`;
- `NATIVE_ENTERPRISE_TRIPLE_CELL_DISCRETE_CURVATURE_CODE_20260823.md`.

## 1. Flattened primal code

For one curvature mode chi, subtract the geometry offset

`eta_j^chi=(3*j^2+chi*epsilon_j)/2`.

Every island packet becomes

`W_j=a+b*j`.

Thus the fixed-mode packet is an affine translate of the `[k,2,k-1]` Reed-Solomon code.

Its linear dual is an `[k,k-2,3]` MDS code.

The dual minimum support is exactly three coordinates.

## 2. Arbitrary triple check

For any three distinct positions

`i<j<l`,

every affine word satisfies

`(l-j)*W_i + (i-l)*W_j + (j-i)*W_l = 0`.

This is the unique three-point affine-dependence relation up to scalar.

Returning to the unflattened values gives a fixed geometry syndrome:

`S_i,j,l^chi = (l-j)*V_i + (i-l)*V_j + (j-i)*V_l`.

The affine placement parameters cancel completely.

## 3. Exact syndrome formula

The quadratic contribution satisfies

`(l-j)*i^2 + (i-l)*j^2 + (j-i)*l^2`

`=(j-i)*(l-i)*(l-j)`.

Therefore

`S_i,j,l^chi`

`= (3/2)*(j-i)*(l-i)*(l-j)`

`+ (chi/2)*[(l-j)*epsilon_i+(i-l)*epsilon_j+(j-i)*epsilon_l]`.

This depends only on the three relative positions and the curvature mode, not on shell, transverse coordinate or absolute prime scale.

## 4. Primitive incidence curvature is the nearest triple check

For consecutive positions

`(i,j,l)=(m,m+1,m+2)`,

the coefficient word is

`(1,-2,1)`.

The exact syndrome reduces to

`V_m-2*V_{m+1}+V_{m+2}=3-chi*(-1)^m`,

which is exactly the primitive incidence curvature pair

`2 / 4`.

Thus the original triple-cell curvature invariant is not an isolated coincidence. It is the nearest-neighbor generator of the dual MDS check space.

## 5. Why triple-cell incidence is algebraically minimal

The primal value packet has two free affine placement parameters.

Therefore:

- one coordinate carries no relation;
- two coordinates determine the packet;
- three coordinates are the smallest support on which a nontrivial consistency relation can exist.

This matches the native carrier fact that coordinate vertices are triple-cell incidence events.

Freeze the bridge:

`NATIVE TRIPLE-CELL INCIDENCE`

`<->`

`MINIMUM-WEIGHT DUAL CHECK OF THE CURVATURE-FLATTENED VALUE CODE`.

## 6. Long-range virtual incidence checks

Nonconsecutive triples provide exact long-range weighted curvature checks.

For example, the symmetric triple `(-j,0,+j)` has coefficients `(j,-2j,j)`, and after dividing by j its syndrome is the multiscale curvature already frozen for the sharp nine.

Hence the square-curvature ladder is the symmetric subfamily of the complete dual-check system.

The full set of triple checks spans all `k-2` independent parity constraints of the fixed-mode code.

## 7. Detection and localization

If one recorded Cell value is perturbed by an error e, the consecutive-check residuals form the local signature

`e,-2e,e`

on the three adjacent dual checks, truncated at a boundary.

Thus the value packet is internally self-auditing.

For a known curvature mode, the MDS distance `k-1` gives the standard correction capacity

`floor((k-2)/2)` symbol errors.

For the maximal nine-Cell packet this is three symbol errors. If the curvature mode is forgotten, the two-coset union distance is four and guarantees one-symbol correction.

This coding interpretation does not alter the arithmetic primality claim; it classifies the redundancy generated by the native incidence geometry.

## 8. Interpretation

The current exact hierarchy becomes

`TWO BOUNDARY VALUES = PRIMAL INFORMATION SET`

`THREE CELL VALUES = MINIMAL DUAL CONSISTENCY CHECK`

`ALL TRIPLE CHECKS = COMPLETE CURVATURE SYNDROME SYSTEM`.

The endpoint holography and the triple-cell incidence curvature are therefore dual aspects of one geometry-selected affine MDS code.
