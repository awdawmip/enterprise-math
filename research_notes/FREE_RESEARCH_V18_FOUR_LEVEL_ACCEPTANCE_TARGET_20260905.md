# Free Research — Four-Level Commutator-Jet Acceptance Target

Status: `FREE_RESEARCH_ACCEPTANCE_TARGET / EXACT MARGIN 58 OVER 625 / FINITE CHANNEL INVENTORY / PROOF OPEN / NOT WORKING TRUTH / NOT FOUNDATION`
Date: `2026-09-05`
Parent: `FREE_RESEARCH_PI_PRIME_GEOMETRY_FRONTIER_V18_20260905.md`
Researcher-ID: `EM-FREE-PI-PRIME-20260904`

## 1. Purpose

The V18 frontier identifies the first finite block that can overcome the one-time reconstruction cost without approaching the critical Mellin exponent: depth four at energy exponent `beta=1/3`.

The exact ideal block multiplier is

\[
\boxed{q_4=567/625.}
\]

The available error budget is therefore

\[
\boxed{1-q_4=58/625.}
\]

This note freezes the finite theorem that must be proved before any `(log N)^(-1/6)` prime remainder is promoted.

---

## 2. Required state

At each top cutoff `N`, retain:

1. root/history-mean and `S_3` standard relation channels;
2. `D_(N,1)=[S_N,J_N]-J_N^2`;
3. `D_(N,2)=ad_(S_N)^2(J_N)/2-J_N^3`;
4. `D_(N,3)=ad_(S_N)^3(J_N)/6-J_N^4`;
5. the positive endpoint measures of each of the six operators `K_(N,k)` and `J_N^(k+1)`;
6. complete weighted relation fields on the corresponding ordered provenance fibers;
7. transported residual channels `H_N r` and moving-tail endpoint channels.

No product label may be collapsed before its relation energy is formed.

---

## 3. Exact algebraic block

For `beta=1/3`, the one-step Mellin entries are

\[
A=B=3/5,
\qquad
9C=9/10.
\]

After four ideal steps and one terminal conversion,

\[
\boxed{
(1,9)\mathcal M(1/3)^4(1,0)^T
=(3/5)^3\left(3/5+36/10\right)
=567/625.
}
\]

This calculation is exact over the rationals.

---

## 4. Required finite inequality

Let `E_N^(4)` denote the positive quadratic norm on the full depth-four jet. The acceptance theorem must establish, for all sufficiently large `N`,

\[
\boxed{
E_N^{(4)}
\le
\left(\frac{567}{625}+\varepsilon_N\right)
\frac1{A(N)}
\sum_{a\le N}u_a
E_{q_a(N)}^{(4)}
+F_N,
}
\]

where

\[
\varepsilon_N\to0,
\qquad
F_N=O((\log N)^{-\gamma})
\]

for some `gamma>1/3` after normalization by the chosen barrier.

Sufficient explicit thresholds are

\[
\varepsilon_N\le29/625
\]

and a forcing allocation using the remaining half-margin `29/625`.

---

## 5. Mandatory coefficient audit

The proof must separately account for:

1. the three commutator defects;
2. the two-body and higher-history endpoint mass discrepancies;
3. residual transport at each of the four levels;
4. moving-cutoff tail descendants;
5. terminal conversion from retained standard energy to unmixed variance;
6. endpoint disintegration and provenance recoalescence.

The terminal factor `9` may occur exactly once. Any proof paying it inside the block fails the one-step identity

\[
(1,9)\mathcal M(\beta)(1,0)^T=1/(1-\beta)>1.
\]

---

## 6. Promotion consequence

If the acceptance theorem holds, standard barrier induction gives

\[
E_N^{(4)}=O((\log N)^{-1/3}).
\]

The one-time odd-simplex or higher-fold square-root readout then gives

\[
\boxed{
\psi(N)-N=O\left(N(\log N)^{-1/6}\right).
}
\]

No such bound is presently asserted. This note only freezes the exact finite target and its coefficient budget.
