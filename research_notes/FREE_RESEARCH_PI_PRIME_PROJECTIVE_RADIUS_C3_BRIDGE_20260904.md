# Free Research — Projective Radius Characterization and C3 Completion Bridge

Status: `FREE_RESEARCH_FRONTIER / ANCHOR_EXPOSED / NOT_WORKING_TRUTH / NOT_FOUNDATION`
Date: `2026-09-04`
Parent: `FREE_RESEARCH_PI_PRIME_NATIVE_C3_CHIRAL_TRACE_20260904.md`

## 1. Motivation

The native-C3 trace frontier reached the exact cross-layer identity

\[
\mathcal O_3=\frac{\tau R_{\rm cell}}3,
\qquad
R_{\rm cell}=1/\sqrt3,
\]

but still left open whether the appearance of the native radius is causal or merely compatible with the classical character evaluation.

This note isolates a stronger geometric fact: `1/sqrt(3)` is uniquely selected by a projective rotation step that partitions the positive Wallis completion coordinate into three equal invariant-measure sectors.

The projective coordinate introduced below is a **derived completion coordinate**. It is not silently identified with a primitive Enterprise point address.

---

## PR3-T01 — Projective rotation family and invariant one-form

For `r>0`, define the fractional-linear map

\[
\boxed{
T_r(x):=\frac{x+r}{1-rx}
}
\]

on the real projective line, away from its pole `x=1/r`.

Its derivative is

\[
T_r'(x)=\frac{1+r^2}{(1-rx)^2}.
\]

Also

\[
1+T_r(x)^2
=\frac{(1+r^2)(1+x^2)}{(1-rx)^2}.
\]

Therefore the Cauchy/Wallis one-form

\[
\omega:=\frac{dx}{1+x^2}
\]

is exactly invariant:

\[
\boxed{T_r^*\omega=\omega.}
\]

This calculation is purely algebraic and does not require a pre-known circle constant.

---

## PR3-T02 — The current cell radius is the unique three-interval step

Always

\[
T_r(0)=r.
\]

Demand the reciprocal-symmetric next boundary

\[
T_r(r)=1/r.
\]

Then

\[
\frac{2r}{1-r^2}=\frac1r
\iff 2r^2=1-r^2
\iff 3r^2=1.
\]

For positive `r`, this uniquely gives

\[
\boxed{r=1/\sqrt3.}
\]

At that value,

\[
T_r(1/r)=\infty.
\]

Thus

\[
\boxed{
0\xrightarrow{T_r}r
\xrightarrow{T_r}r^{-1}
\xrightarrow{T_r}\infty
}
\]

holds **iff** `r=1/sqrt(3)`.

The current Enterprise cell radius is exactly

\[
R_{\rm cell}=1/\sqrt3.
\]

Hence it has a second characterization:

> `R_cell` is the unique positive projective-rotation step whose first three boundary images cut the positive completion ray at `0, R_cell, 1/R_cell, infinity`.

This does not replace the existing gap-free circle-cover derivation of the radius; it is an independent compatible characterization.

---

## PR3-T03 — The three intervals have exactly equal Wallis measure

Set `r=R_cell=1/sqrt(3)`. Since `T_r` preserves `omega` and maps the consecutive intervals

\[
[0,r]\to[r,r^{-1}]\to[r^{-1},\infty],
\]

we obtain

\[
\boxed{
\int_0^r\omega
=\int_r^{1/r}\omega
=\int_{1/r}^{\infty}\omega.
}
\]

Therefore

\[
\boxed{
\int_r^{1/r}\frac{du}{1+u^2}
=\frac13\int_0^\infty\frac{du}{1+u^2}.
}
\]

No arctangent evaluation is needed for the equality of thirds; it follows from the exact projective symmetry and the radius equation `3r^2=1`.

This is the first causal bridge in the current pass connecting:

`THREE SECTORS -> R_CELL=1/sqrt(3) -> THREE EQUAL COMPLETION CELLS`.

---

## PR3-T04 — Order-three projective rotation is the square of the radius step

The family composes by

\[
T_a\circ T_b=T_{(a+b)/(1-ab)}
\]

whenever written away from projective poles.

At `r^2=1/3`,

\[
\frac{2r}{1-r^2}=\frac1r,
\]

hence

\[
\boxed{T_r^2=T_{1/r}.}
\]

In matrix form use

\[
M(a)=
\begin{pmatrix}
1&a\\
-a&1
\end{pmatrix},
\]

whose projective action is `T_a`. For `a=1/r=sqrt(3)`, direct multiplication gives

\[
M(1/r)^3\sim I
\]

(up to a nonzero scalar), so `T_{1/r}` has projective order three. Consequently

\[
\boxed{T_r^2\text{ is an order-three projective rotation}.}
\]

Thus the same `r=1/sqrt(3)` selected by the native cell covering is a projective **half-step** whose square realizes a `C3` rotation.

This is structurally compatible with the native sector cycle `rho^3=id`, but no intertwining theorem between primitive sector addresses and this derived projective coordinate is asserted yet.

---

## PR3-T05 — The C3 orientation series follows from the middle completion cell

The orientation completion is

\[
\mathcal O_3
=\int_0^1\frac{dx}{1+x+x^2}.
\]

Set

\[
u=r(2x+1),
\qquad r^2=1/3.
\]

Then `x=0` gives `u=r`, `x=1` gives `u=3r=1/r`, and

\[
1+u^2=\frac43(1+x+x^2).
\]

Therefore

\[
\boxed{
\mathcal O_3
=2r\int_r^{1/r}\frac{du}{1+u^2}.
}
\]

Using PR3-T03,

\[
\boxed{
\mathcal O_3
=\frac{2r}{3}
\int_0^\infty\frac{du}{1+u^2}.
}
\]

If the Wallis completion is normalized by

\[
\tau=2\int_0^\infty\frac{du}{1+u^2},
\]

then immediately

\[
\boxed{
\mathcal O_3=\frac{\tau r}{3}
=\frac{\tau R_{\rm cell}}3.
}
\]

The important advance is that the factor `1/3` and the radius `1/sqrt(3)` are now generated by one exact projective symmetry; the only remaining analytic boundary is the identification of the Wallis completion integral with the internally constructed `tau`.

---

## PR3-T06 — Prime orientation product acquires a geometric normalization

Combining with the native chiral trace character

\[
\chi_3(p)=\frac13\operatorname{Tr}(JP^p),
\]

and the conditional Dirichlet Euler product gives, at analytic-completion strength,

\[
\boxed{
\prod_p
\left(1-\frac{\operatorname{Tr}(JP^p)}{3p}\right)^{-1}
=\mathcal O_3
=2R_{\rm cell}
\int_{R_{\rm cell}}^{1/R_{\rm cell}}
\frac{du}{1+u^2}
}
\]

and therefore, by PR3-T03,

\[
\boxed{
\prod_p
\left(1-\frac{\operatorname{Tr}(JP^p)}{3p}\right)^{-1}
=\frac{2R_{\rm cell}}3
\int_0^\infty\frac{du}{1+u^2}
=\frac{\tau R_{\rm cell}}3.
}
\]

The product-to-series step remains the standard conditional Dirichlet completion. The geometric normalization itself is exact from PR3-T01..T05.

---

## 7. What this changes about the research direction

Before this note, `R_cell` in `O3=tau R_cell/3` could still be dismissed as a rewritten `sqrt(3)`.

Now `R_cell` is independently characterized by an exact projective dynamical property:

\[
\boxed{
T_r(r)=1/r
\iff
r=R_{\rm cell}=1/\sqrt3.
}
\]

At precisely this value:

- the positive completion ray splits into three equal invariant-measure intervals;
- the square of the radius step is an order-three projective rotation;
- the middle interval is exactly the transformed `C3` orientation integral.

This is a genuine structural coupling among `3`, `R_cell`, and the `C3` prime character. It still lives in a derived projective/analytic completion layer, not primitive G0 address dynamics.

---

## 8. Next discriminating theorem

Two gaps remain:

1. **Wallis-to-projective completion:** derive
   \[
   \tau=2\int_0^\infty\frac{du}{1+u^2}
   \]
   from the same finite #1159/#1161 branch/spectral construction, without importing a classical circle integral as an axiom.

2. **Native-sector intertwining:** construct a finite map from the current native sector-cycle `rho` to the projective order-three action `T_{1/R_cell}` that respects the typed native/derived boundary.

Closing both would turn the present exact projective compatibility into a native finite-refinement derivation of the prime orientation channel.

---

## Current classification

- `T_r^* dx/(1+x^2)=dx/(1+x^2)`: `PROVED / ALGEBRAIC`.
- `T_r(r)=1/r iff r=1/sqrt(3)`: `PROVED / ALGEBRAIC`.
- three equal projective completion intervals: `PROVED / CHANGE OF VARIABLES`.
- `T_r^2` order-three at `r=1/sqrt(3)`: `PROVED / PROJECTIVE MATRIX ALGEBRA`.
- orientation integral is middle-cell readout: `PROVED / ALGEBRAIC SUBSTITUTION`.
- `O3=tau R_cell/3`: `PROVED GIVEN WALLIS-INTEGRAL COMPLETION`.
- `WALLIS-INTEGRAL COMPLETION FROM CURRENT NATIVE BRANCH MODEL`: `OPEN`.
- `NATIVE rho <-> PROJECTIVE C3 INTERTWINER`: `OPEN`.
