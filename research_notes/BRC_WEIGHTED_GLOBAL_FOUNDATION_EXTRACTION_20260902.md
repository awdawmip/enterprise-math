# Weighted/Log BRC — Global Foundation Tool/Theorem Extraction

Status: `FOUNDATION INTEGRATION PACKET / NO NEW MATHEMATICS`
Date: `2026-09-02`
Steward-ID: `EM-STW-B9F4C2`
Authority: explicit user instruction to extract tools/theorems and make the layer part of every research foundation.

## Result

The main-backed Weighted/Log BRC research sequence has been refactored into four reusable layers:

1. **minimal all-research substrate** — `definitions/ENTERPRISE_BRC_WEIGHTED_GLOBAL_SUBSTRATE_20260902.json`;
2. **detailed canonical foundation** — `definitions/ENTERPRISE_BRC_WEIGHTED_LOG_FOUNDATION_20260902.md`;
3. **stable theorem interface** — `definitions/ENTERPRISE_BRC_WEIGHTED_LOG_THEOREM_LEDGER_20260902.json`, with `WBRC-T01..T11` and `WBRC-N01..N05`;
4. **reusable executable T0 subtool** — method `t0.weighted_brc_cwm`, implementation `src/enterprise_math/brc_weighted.py`.

The minimal substrate is now routed into FREE Phase A as a type discipline without preloading successful applications or theorem menus. TASK/Driver/Steward/FREE Phase B may use the full theorem and tool surfaces normally.

## Tool extraction

The production tool exposes exact positive-rational operations for:

- CWM construction;
- branch recoalescence and serial propagation;
- exact positive-path realizability;
- Boolean support readout;
- effective multiplicity and symbolic BRC-LN surplus;
- all-prefix future-CWM equality;
- projective/gauge scaling and incoming compensation;
- exact one-state recurrent closure.

It rejects signed weights and has an explicit deterministic degeneration `E=1`, `Delta=0`.

## Theorem extraction

The ledger separates positive theorems from reusable negative boundaries. In particular it keeps:

- Boolean support homomorphism iff zerosumfree + no zero divisors;
- `Delta=ln(W/M)` and equal-branch `Delta=ln k`;
- all-prefix CWM quotient;
- projective/gauge quotient;
- one-state recurrent stability `sum q_i<1` and equal-loop `ln q<-ln k`;
- signed-cancellation, Boolean-too-coarse, local-bisimulation-not-necessary, deterministic-degeneration and no-general-SCC boundaries.

## Anti-anchoring resolution

“All research foundation” is implemented as **universal but lazy**:

`ALL_RESEARCH_HAS_THE_TYPED_CAPABILITY != ALL_RESEARCH_MUST_USE_THE_CAPABILITY`.

FREE Phase A sees the minimal typing commitments because the user explicitly promoted them to project substrate. It does not receive the old arithmetic/selector/holonomy success examples or a suggested Weighted-BRC question menu. Detailed theorem/tool lookup remains downstream of a self-generated relevant problem.

## Validation

Dedicated CI checks:

- reusable tool unit tests;
- theorem/substrate/method/router integrity;
- old-research retrofit regression;
- actual toolbox discovery of both `t0.weighted_brc_cwm` and executable module `brc_weighted`.

This packet contains no new theorem beyond the canonical PR #1102-#1108 evidence; it is extraction, routing, and executable refactoring only.
