{
  "name": "enterprise-math-cloudflare-supervisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-contract.mjs && wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "smoke:live": "node scripts/smoke-live.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "0.22.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "wrangler": "4.127.1"
  }
}
