{
  "name": "enterprise-math-cloudflare-supervisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm test && node scripts/check-contract.mjs && wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "test": "vitest run",
    "smoke:live": "node scripts/smoke-live.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "0.22.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@modelcontextprotocol/client": "2.0.0",
    "vitest": "4.1.11",
    "wrangler": "4.127.1"
  }
}
