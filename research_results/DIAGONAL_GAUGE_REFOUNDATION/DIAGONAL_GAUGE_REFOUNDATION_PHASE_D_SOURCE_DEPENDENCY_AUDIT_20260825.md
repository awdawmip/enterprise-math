# Diagonal Gauge Refoundation — Phase D source dependency audit

Status: `RAW INTEGRATION AUDIT / NO CANONICAL SOURCE EDIT`
Date: `2026-08-25`
Researcher-ID: `EM-DGR-8C2D41`
Owner branch: `research/diagonal-gauge-refoundation`

Primary disposition:

`SMALL_TYPED_FOUNDATION_CORRECTION_IDENTIFIED__R061_R062_CORE_MATHEMATICS_PRESERVED__DRIVER_INTEGRATION_REQUIRED`

## 1. Audit principle

Phase A-C did not start from the superseded pre-2026-08-20 vector relation as an axiom.

An independent current-source route exists:

1. current Stage 2 supplies `delta_I(P,Q)=(r,s) in Z^2`;
2. current Stage 2 supplies `D_E(r,s)=(r-m,s-m,-m)` with `m=min(r,s,0)`;
3. define `chi(a,b,c)=(a-c,b-c)`;
4. prove `chi(D_E(r,s))=(r,s)` and `D_E(chi(z))=can(z)`;
5. infer `ker(chi)=Z(1,1,1)`.

Therefore the diagonal gauge quotient is reconstructed from the **current decoder**. It is not imported merely because the historical Euclidean carrier once used the same kernel.

Freeze candidate:

`CURRENT_DECODER_INDEPENDENTLY_RECONSTRUCTS_DIAGONAL_GAUGE_KERNEL = true`.

---

## 2. Current foundation statements requiring correction if integrated

File:

`definitions/ENTERPRISE_THREE_POSITIVE_AXIS_OVERLAPPING_CIRCLE_CELL_PLANE_20260820.md`

### A. `NO_NATIVE_DIAGONAL_SHIFT_QUOTIENT`

Audit verdict:

`OVERSTRONG_AS_GLOBAL_PROHIBITION`.

Reason: current Stage-2 decoder/composition already define an exact derived lifted-displacement quotient with kernel `Z(1,1,1)`.

Safe replacement must preserve the N0/G1 distinction:

`NO_PRIMITIVE_UNTYPED_NATIVE_POINT_DIAGONAL_QUOTIENT`;

`DERIVED_LIFTED_DISPLACEMENT_DIAGONAL_GAUGE = ALLOWED/ESTABLISHED_G1`.

### B. `(a,b,c) IS_NOT_EQUIVALENT TO (a+k,b+k,c+k)`

Audit verdict:

`NEEDS_DOMAIN_RETYPE`.

Correct split:

- two **canonical native addresses** in `A_E` are never distinct diagonal shifts, because each class has one min-zero representative;
- arbitrary lifted displacement labels in `Z^3` that differ by `k(1,1,1)` have the same derived displacement decode.

Suggested typed replacement:

`CANONICAL_NATIVE_ADDRESS_IS_UNIQUE_MIN_ZERO_REPRESENTATIVE`;

`LIFTED_DISPLACEMENT_DIAGONAL_GAUGE_EQUIVALENCE_IS_DERIVED_NOT_PRIMITIVE_POINT_ONTOLOGY`.

### C. Section-9 prohibition against reintroducing the deleted quotient

Audit verdict:

`REPLACE WITH METRIC/ONTOLOGY GUARD`.

The necessary protection is not “never use a diagonal gauge.” It is:

- do not infer a native Euclidean vector relation from the gauge;
- do not infer the old symmetric `Delta` metric;
- do not treat noncanonical lifts as additional native points;
- do not bypass explicit trace/sector typing.

---

## 3. Current Stage-2 statements requiring clarification, not mathematical replacement

Files:

- `definitions/ENTERPRISE_ARBITRARY_POINT_DIRECTED_LINE_GAUGE_20260821.md`;
- `driver_reviews/R061_STAGE2_ARBITRARY_POINT_DIRECTED_LINE_GAUGE_DRIVER_REVIEW_20260821.md`.

Current wording says the decoder is “not a native common-diagonal quotient.”

Audit verdict:

`MATHEMATICS_KEEP / SEMANTIC WORDING NARROW`.

Recommended future wording:

> The decoder is not a primitive native-point diagonal quotient and does not restore the old carrier-Euclidean vector ontology. Algebraically, however, it is exactly the canonical min-zero section of a **derived lifted-displacement diagonal gauge** whose kernel is `Z(1,1,1)`.

All current formulas remain unchanged:

- decoder;
- translated sector selection;
- trace identity;
- path fiber;
- directed gauge;
- triangle inequality;
- reversal formula;
- reversal asymmetry.

Freeze candidate:

`R061_STAGE2_FORMULAS_REQUIRE_ZERO_MATHEMATICAL_CHANGE`.

---

## 4. Current source statements that remain fully intact

### Plane / geometry

Retain:

- `O_E=0`;
- triple-intersection origin;
- overlapping circle cells;
- nearest center spacing `1`;
- radius `1/sqrt(3)`;
- three positive native rays;
- no primitive native negative axes;
- three glued positive sectors;
- `ENTERPRISE_RIGHT_ANGLE=120_DEGREES`;
- sector-local `L_E^2=a^2+b^2`.

### Native line

Retain:

- line identity = typed native component trace;
- `X_iX_j ~ X_jX_i` only within component-preserving trace language;
- one trace has many path representatives;
- same endpoint does not imply same line;
- reverse-third shortcut remains outside the line trace.

### Arbitrary-point line

Retain all formulas; diagonal refoundation explains rather than replaces them.

### Unoriented segment

Retain:

`BSEG_E(P,Q)={T(P->Q),T(Q->P)}`

and

`SPEC_E={ell(g),ell(-g)}`.

### BRC multipath bridge

Retain:

`PATH_FORMAL_BRC -> N_BRC -> BOOLEAN_BRC`

and

`COMPONENT_TYPED_FIRST -> CARRIER_ENRICHED_SECOND`.

The diagonal endpoint quotient is a different forgetful map and does not replace BRC.

---

## 5. Geometry policy audit

File:

`GEOMETRIC_TOOL_REFOUNDATION_POLICY.md`.

The policy says the historical A2/C6 carrier may remain an I0 implementation/combinatorial carrier but must not manufacture native coordinate equivalence or metric.

Audit verdict:

`POLICY CORE RETAIN`.

Reason: this refoundation does **not** derive the gauge from historical Euclidean carrier geometry. It derives the kernel from the current Stage-2 decoder and retypes it as a G1 endpoint/displacement object.

A future integration may add a narrow clarification:

`I0 carrier relation cannot by itself promote a native quotient; an independently reconstructed derived displacement gauge is allowed when it passes the native-semantics gate.`

No policy reversal is required.

---

## 6. Foundational/native-semantics audit

The packet/path N0 base does not declare `Z^3` displacement addition as primitive.

Therefore:

`DIAGONAL_GAUGE_AS_N0_PRIMITIVE = NOT ESTABLISHED`.

But current G1 line/gauge semantics already contain enough exact structure to reconstruct it as a derived endpoint object.

Verdict:

`DIAGONAL_GAUGE = CANONICAL_DERIVED_G1 / EXACT_RECOVERY_OF_CURRENT_STAGE2`.

This respects the project rule `REFOUND, NOT REJECT`.

---

## 7. Native dimension versus derived group rank

`G_D ~= Z^2` has algebraic rank two.

Current project policy separately uses three positive native axes and in some policy text labels Enterprise plane dimension as `3`.

No current theorem establishes

`NATIVE_DIMENSION = RANK(DERIVED_DISPLACEMENT_GROUP)`.

Packet/path foundation explicitly treats dimension as relational.

Therefore Phase D does not declare a contradiction, but freezes an integration warning:

`NATIVE_DIMENSION_VS_DERIVED_DISPLACEMENT_RANK = SEMANTICALLY_DISTINCT_UNLESS_FUTURE_THEOREM_IDENTIFIES_THEM`.

---

## 8. Historical `Delta` status after refoundation

Keep the current rejection of

`Delta=a^2+b^2+c^2-ab-bc-ca`

as the native Enterprise Pythagorean metric.

Phase-A metric-fork theorem reclassifies `Delta` more precisely:

- it is diagonal-gauge invariant;
- it is the unique globally quadratic, S3-symmetric, unit-calibrated quadratic scalar on the derived quotient;
- it fails the current native sector requirement `q(1,1,0)=2`, because `Delta(1,1,0)=1`.

Thus it may remain a carrier/optional symmetric quadratic readout, never automatically the current native metric.

Freeze candidate:

`DELTA_REHABILITATED_AS_DERIVED_SYMMETRIC_QUADRATIC_OPTION__NOT_NATIVE_METRIC`.

---

## 9. Minimum safe integration surface

If Driver review accepts the refoundation, the smallest coherent source transaction is expected to touch only:

1. `definitions/ENTERPRISE_THREE_POSITIVE_AXIS_OVERLAPPING_CIRCLE_CELL_PLANE_20260820.md` — retype the overstrong no-quotient wording;
2. add one new exact definition, suggested name:
   `definitions/ENTERPRISE_DERIVED_DIAGONAL_DISPLACEMENT_GAUGE_20260825.md`;
3. `definitions/ENTERPRISE_ARBITRARY_POINT_DIRECTED_LINE_GAUGE_20260821.md` — add a compatibility note, with formulas unchanged;
4. `definitions/00_CURRENT_NATIVE_FOUNDATION.md` — route the derived displacement gauge;
5. possibly a narrow clarification in `GEOMETRIC_TOOL_REFOUNDATION_POLICY.md` if Driver judges it necessary.

Do **not** modify native line formulas, Stage-3 segment spectrum, BRC definitions, or path-valued square-root definitions merely for this integration.

---

## 10. Driver gate recommended before canonical edits

Because this changes the interpretation of a foundational freeze, do not directly edit canonical source from this researcher branch.

Recommended hard review target:

`DIAGONAL_GAUGE_REFOUNDATION_TYPED_CORRECTION_ACCEPTED_OR_NARROWED_OR_REFUTED`.

Driver/independent review should specifically attempt to refute:

1. `ker(chi)=Z(1,1,1)` as the exact current decoder kernel;
2. current Stage-2 composition = canonicalized quotient addition;
3. current reversal = quotient group inverse in min-zero section;
4. separation between quotient structure and current sector gauge;
5. line/BRC non-collapse;
6. Phase-B zero-displacement closed path versus identity-path distinction.

---

## 11. Phase-D verdict

`NO_NATIVE_DIAGONAL_SHIFT_QUOTIENT` is not supported as a blanket prohibition once current Stage-2 mathematics is included.

The narrow statement still supported is:

`NO_PRIMITIVE_UNTYPED_NATIVE_POINT_DIAGONAL_QUOTIENT`.

The stronger derived result is:

`LIFTED_DISPLACEMENT_DIAGONAL_GAUGE = EXACT_CURRENT_G1_STRUCTURE`.

No current canonical source has been modified by this research branch.
