# R063 Stage 2 — Final Classification

Status: `COMPLETE / READY FOR DRIVER REVIEW`

Researcher-ID: `EM-R063S2-52118B`

## Final classification

`MULTIPLICATIVE_PATH_NORM_ROOT_PROVENANCE_TOWER_CLASSIFIED_WITH_UNIT_QUOTIENT_MONOID_ORIENTATION_CONDITIONAL_TRACE_PRODUCT_AND_CHOICE_FREE_PATH_LIFT_NO_GO`

Hard target:

`MULTIPLICATIVE_PATH_NORM_ROOT_PROVENANCE_TOWER_CLASSIFIED = true`.

## Tower verdict

`factorization provenance`

→ **exact graded commutative multiplicative algebra on supported factors**;

`SRoot`

→ **exact surjective signed Gaussian multiplication on supported factors**;

`URoot=SRoot/U`

→ **exact graded commutative quotient monoid**;

`full ordered GRoot_E`

→ **not globally bijective to URoot on squares; exact oriented product exists only after retaining an ordered sector orientation**;

`native component trace`

→ **exact orientation-conditioned multiplicative trace product, distinct from frozen additive trace concatenation**;

`native path fiber`

→ **native path multiplicity is not multiplicative; no single-valued multiplicative path operation is generated by the frozen R061 path-operation closure; exact relation-valued whole-target-fiber lift survives after orientation**;

`N_BRC`

→ **downstream target path multiplicity only; not a root-product multiplicative homomorphism**;

`Boolean BRC support`

→ **exact downstream nonempty-support shadow after target root/trace is already fixed**.

## Central exact formulas

For split prime `p`:

`m_p(alpha,beta;t)`

`= max(0,min(alpha,t)-max(0,t-beta)+1)`.

For a fixed signed target:

`PreimageCount = 4 * product_p m_p(alpha_p,beta_p;t_p)`.

The factor `4` is proved from the four ordered unit factorizations of each target unit.

For a fixed unit-orbit target:

`PreimageCount_URoot = product_p m_p(alpha_p,beta_p;t_p)`.

## Mandatory discriminator

`A=B=2`, `AB=4`, source root `(1,1)`:

- oriented target root: `(0,2)`;
- fixed-signed-target multiplicative provenance pair count: `4`;
- source native path pair count: `4`;
- target native path multiplicity: `1`;
- frozen additive-concatenation trace target `(2,2)` path multiplicity: `6`.

Therefore

`NATIVE_PATH_MULTIPLICITY_IS_NOT_MULTIPLICATIVE_UNDER_ROOT_PRODUCT = true`.

The `5 x 5` witness separates the accidental `4=4` coincidence:

`4 provenance preimages`, `9 source path pairs`, `35 target native paths`.

## Ordered-component orientation boundary

The component swap `sigma(a,b)=(b,a)` fixes `(1,1)`. But the norm-4 unit-orbit target has the two Stage 1 ordered nonnegative representatives `(0,2)` and `(2,0)`, exchanged by `sigma` and neither fixed.

Hence a single-valued ordered component/trace lift of the `URoot` product cannot be component-swap equivariant. The exact oriented products `star_i` and `star_j` are therefore classified as `CONDITIONAL_DERIVED`, not as orientation-free native multiplication.

## Semantic scope

`R063_STAGE2_CLAIM_SCOPE = FROZEN_R061_SECTOR_LOCAL_PYTHAGOREAN_TRACE_SEMANTICS`.

`GLOBAL_FULL_PLANE_MULTIPLICATIVE_STRUCTURE = NOT_CLAIMED`.

Gaussian multiplication remains a sector-local factorization/component algebra in this stage.

## Acceptance gates

1. `SEMANTIC_SCOPE_CLAIM_LEDGER_COMPLETE = PASS`
2. `R063_STAGE1_FROZEN_DEPENDENCY_REPLAY_INTACT = PASS`
3. `PROVENANCE_MULTIPLICATION_EXACT_OR_FALSIFIED = EXACT`
4. `PROVENANCE_ASSOCIATIVITY_COMMUTATIVITY_UNIT_CLASSIFIED = EXACT`
5. `EVALUATION_MULTIPLICATIVITY_AND_SIGNED_ROOT_SURJECTIVITY_CLASSIFIED = EXACT_ON_SUPPORTED_FACTORS`
6. `PROVENANCE_FIBER_COUNT_FORMULA_EXACT_OR_MINIMAL_COUNTEREXAMPLE_WITH_CORRECTED_STATEMENT = EXACT_FORMULA`
7. `UNIT_QUOTIENT_COMPONENT_ROOT_MULTIPLICATION_WELL_DEFINED_OR_FALSIFIED = UROOT_EXACT; FULL_GROOT_ORIENTATION_CONDITIONAL`
8. `TRACE_PRODUCT_CLASSIFIED = ORIENTATION_CONDITIONAL_EXACT / ORIENTATION_FREE_NO_GO`
9. `CANONICAL_PATH_MULTIPLICATIVE_LIFT_OR_STRONGEST_NO_GO_CLASSIFIED = FROZEN_OPERATION_CLOSURE_NO_GO + RELATION_SURVIVOR`
10. `NATIVE_PATH_MULTIPLICITY_NONMULTIPLICATIVITY_PROVED = PASS`
11. `ALL_MULTIPLICITY_LAYERS_SEPARATED = PASS`
12. `BRC_DOWNSTREAM_COMPATIBILITY_OR_NO_GO_CLASSIFIED = PASS`

Deterministic checker status: `PASS`, mismatch count `0`, exhaustive ordered pairs `16,384`.

## Stop rule

R063 Stage 2 stops here for Driver review.

`R063_STAGE3 = NOT_OPENED`.
