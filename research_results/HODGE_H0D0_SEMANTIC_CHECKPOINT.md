# HODGE H0D0 Semantic Checkpoint

Date: `2026-08-17`  
Researcher-ID: `EM-HODGE-H0D0-5C7E21`  
Task: `RS-HODGE-H0D0-PROOF-LEVERAGE-ATTRIBUTION-REPAIR`  
Driver: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`  
Owner branch: `research/hodge-h0d0-proof-leverage-attribution`  
Taskbook source: `49a8a2bb3084be72b37562792fa0986beb9c61a9`  
Parents: H0A0 `f4e6cf84eb191e0b7442913e018e1f6347e9584e`, H0B `d93d050542dac6074ec7343e534b3854811ff7fd`, H0C `80ea55d0043ceb0c4c31c484944dd2cc14ce1859`

## Frozen disposition

`H0D0_ATTRIBUTION_CRITERION_FROZEN_NONVACUOUS`

Hard target:

`ENTERPRISE_PROOF_LEVERAGE_ATTRIBUTION_CRITERION_IS_WELL_TYPED_AND_NONVACUOUS = PASS`

H0D0 is a criterion addendum only. It does not search a new carrier, does not prove Hodge, and does not open H1.

## Four-axis repair

H0D0 freezes four independent axes:

1. information / recoverability;
2. abstract operational rank R0/R1/R2/R3;
3. proof-leverage attribution relative to a predeclared source baseline;
4. novelty / prior-art status.

Frozen invariants:

- `NOVELTY_STATUS_DOES_NOT_DETERMINE_OPERATIONAL_RANK`;
- `PRIOR_ART_IS_NOT_A_NEGATIVE_R2_GATE`;
- `SOURCE_INHERITED_LEVERAGE_DOES_NOT_EARN_TRANSFORM_ATTRIBUTION`;
- `KNOWN_PRIOR_ART_TRANSFORM_CAN_PASS_R2`;
- `NOVEL_THEOREM_NOT_REQUIRED_FOR_R2`;
- `BASELINE_MUST_BE_PREDECLARED`;
- `POST_HOC_WEAKENING_OF_SOURCE_BASELINE_IS_FORBIDDEN`.

A future Hodge-special R2 claim must additionally pass `R2_ATTRIBUTION_ADDENDUM_PASS`. This addendum does not rewrite the candidate's abstract H0A0 operational rank.

## Positive attribution control — DFT

On the predeclared original-basis baseline, the two-point convolution operator is `A=[[1,1],[1,1]]`. The invertible transform `F=[[1,1],[1,-1]]` gives `F A F^-1 = diag(2,0)`.

The predeclared off-diagonal-nonzero count drops `2 -> 0`. The source baseline contains the dense source-basis operator but not the DFT diagonal normal form. Therefore the transform is load-bearing for this credited normal-form differential:

`TRANSFORM_ATTRIBUTED_LEVERAGE`.

Novelty remains `CLASSICAL_PRIOR_ART`. Prior-art status therefore does not erase operational R2.

## Negative controls

Bijective renaming remains R0 with no leverage to attribute. Arbitrary hidden color remains R0 despite information enrichment relative to a weak readout. Thus neither equivalence, enrichment, nor novelty decides leverage.

## H0C attribution retype

Historical H0C remains frozen as `H0C_PRIOR_ART_ONLY_NO_ENTERPRISE_CREDIT`; H0D0 changes only the future qualification semantics.

### S1 — spanning forest

The source baseline already contains rational states `rho`, support graph `E`, equality, graph paths and a spanning-forest rule. Equality on a spanning forest implies equality on every support edge directly at source. Since normalized `Z` is `rho_i-rho_j`, the `3 -> 2` triangle reduction is:

`SOURCE_INHERITED_LEVERAGE`.

### S2 — signed path telescoping

Source rational differences already telescope along paths. The diamond `2 -> 1` endpoint-token reduction is therefore already present in the source `rho` language:

`SOURCE_INHERITED_LEVERAGE`.

### S3 — zero-class quotient

`Z_ij=0` iff `rho_i=rho_j`. The Enterprise zero-class quotient is therefore the source equality/kernel quotient, and the diagonal-support normal form already exists at source:

`SOURCE_INHERITED_LEVERAGE`.

All three retain their historical abstract R2-control certificates, but fail the new Hodge-special attribution addendum. Their strongest Hodge-special qualification under the addendum is R1 derived reorganization.

## H0B Candidate B attribution retype

H0B's frozen source theorem-critical operation is already the one-step final-support behavior `sigma_src(b)=O[relImage(R12,{b})]`.

On the four exact middle states the source values are already:

- `b00,b10 -> {c0}`;
- `b01,b11 -> {c1}`.

Hence the source behavior map itself has exactly two fibers, and those fibers are the unique coarsest classes preserving the frozen one-step output language. The future-signature quotient packages this kernel, but on H0B's exact frozen scope it does not create the `4 -> 2` reduction.

Attribution:

`SOURCE_INHERITED_LEVERAGE`.

This conclusion does not project to a larger multi-step/compositional future language. A future enlarged-scope theorem would require a new attribution audit.

## Historical preservation

H0B remains historically `H0B_R2_PRIOR_ART_CONTROLS_ONLY`.

H0C remains historically `H0C_PRIOR_ART_ONLY_NO_ENTERPRISE_CREDIT`.

H0D0 does not rewrite either disposition and does not retroactively claim Enterprise novelty. It freezes the corrected future rule: novelty is reported independently; leverage credit is decided by source-baseline attribution.

## Route decision

`NO_EXISTING_ATTRIBUTED_R2_COMPONENT__SEARCH_NEW_OPERATIONAL_SOURCE`

No existing H0B/H0C strict-leverage component passes transform attribution on its frozen scope. The next search should target a Hodge-adjacent algebraic realization/operation for which the exact realization map is load-bearing in producing a normal form, dependency reduction, obstruction basis, compositional factoring, or layer lowering that is absent from the predeclared source baseline.

Zero external novelty is not a reason to reject such a future component.

## H1 firewall

`H1_ADMISSIBLE = false`.

No H1 work was started.

`CI_NOT_REQUIRED_FOR_RESEARCH`

## Semantic checkpoint digest

`HODGE_H0D0_SEMANTIC_CORE_SHA256 = c199a976a1b998eb5f98bda0ce0b8e59c47a0aeeee3a5f1c90aad7e951f67b2f`

This is the SHA-256 of the canonical semantic-core JSON for this stage, not a Git commit hash.
