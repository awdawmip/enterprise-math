# FQ010 — Final Admission Classification

Researcher-ID: `EM-FQ010-CA2555`

Task: `RS-FQ010-LINE-SCALE-SEMANTIC-ADMISSION-COMPARATIVE-REFOUNDATION`

Hard target:

`LINE_SCALE_SEMANTIC_ADMISSION_COMPARATIVE_REFOUNDATION_CLASSIFIED`

## Final classification

`HYBRID_RELATION_ADMISSIBLE_BUT_SCALE_ROLE_REQUIRES_SEPARATE_DECLARATION_OR_CALIBRATION`

Requested disposition category:

`COEXIST / HYBRID`.

This is a positive structural admission of the relation route, not a rejection of relation cardinality. It is also not a claim that `|R_type|` is N0-forced squared line scale.

## 1. Theorem equivalence

Let

`R_type={(x,y):tau(x)=tau(y)}`

and

`Q_K=|R_type|`.

For component multiplicities `(n_c)`,

`Q_K=sum_c n_c^2`.

On every declared two-channel sector:

- K implies FQ008 axis square calibration;
- K implies zero mixed second difference / transverse independence;
- FQ008 axis square calibration plus transverse independence uniquely reconstructs the same scalar field;
- therefore `Q_FQ008=Q_K` on the full declared sector domain.

So the scalar theorem content is exactly equivalent.

`THEOREM_EQUIVALENCE = EXACT`.

## 2. Relation information strength

The scalar equivalence does not extend to relation information.

`R_type` retains the same-component partition before scalarization. `Q_K` and FQ008 `Q` retain only one integer.

Explicit inequivalent partition collisions include:

- `(0,0,5)` and `(0,3,4)`, both with scalar `25`;
- `(0,1,7)` and `(0,5,5)`, both with scalar `50`;
- `(0,1,8)` and `(0,4,7)`, both with scalar `65`.

Hence

`primitive typed content > relation/partition information > Q_K = Q_FQ008`

at presentation-independent information strength.

`RELATION_INFORMATION_STRENGTH = STRICTLY_STRONGER_THAN_SCALAR`.

## 3. Relation admissibility

`R_type` is exactly `ker(tau)` and is the unique greatest equivalence relation on the token carrier through whose quotient the exact component observation still factors.

The proof uses no metric, length, norm, orientation or target scalar.

It is invariant under token renaming and equivariant under full component relabeling.

Therefore:

`R_type = N0_DEFINABLE_DERIVED`.

This relation-level admission is clean and final.

## 4. Cardinality readout strength

Once the ordered-pair relation carrier is selected, ordinary finite cardinality is exact and gives

`|R_type|=sum_c n_c^2`.

But relation canonicity does not uniquely select cardinality among all scalar valuations.

Even imposing:

- relation-isomorphism invariance;
- disjoint-union additivity across equivalence blocks;
- unit normalization;

leaves an arbitrary block valuation `g(n)` with `g(1)=1`. Examples `g(n)=n^2`, `g(n)=n`, and `g(n)=1` all survive.

Therefore:

`Q_K = N2_READOUT_COLLAPSE`,

not an N0 primitive or uniquely forced N0 scalar.

## 5. Observation-resolution result

Three canonical neighboring resolutions give:

- occurrence identity: `Q_fine=sum n_c`;
- component identity: `Q_comp=sum n_c^2`;
- full component erasure: `Q_coarse=(sum n_c)^2`.

The FQ008 scalar conditions jointly isolate the component-resolution cardinality from these two neighboring countermodels:

- axis square rejects the finer occurrence count;
- transverse independence rejects the coarser universal-pair count.

The current line/trace source supplies an independent **non-scalar** calibration fact:

`ENTERPRISE_LINE_IDENTITY = NATIVE_COMPONENT_TRACE`.

That fact favors component resolution without using the current sum-of-squares output as a construction premise.

However, it still does not prove that ordinary relation cardinality is the unique scalar valuation carrying the scale role.

## 6. Primitive/semantic assumption budget

Interface K achieves real ontological compression:

- the relation exists before any scalar;
- one relation plus one readout yields axis-square behavior and transverse independence as theorems;
- the relation simultaneously explains shuffle invariance and component provenance;
- occurrence-count and fully-coarse alternatives become explicit observation-resolution countermodels.

This is stronger than notational repackaging.

But K does not eliminate all semantic assumptions. It still requires:

1. component resolution to be selected as the scale-relevant observation resolution;
2. finite relation cardinality to be selected as the scalar readout;
3. that readout to be identified with squared native line scale.

R065 proves that such scalar/semantic selection is not forced by the primitive substrate alone.

Therefore the comparative budget is:

`ONTOLOGICAL_COMPRESSION_WITH_THEOREM_EQUIVALENT_SCALAR_CONTENT_BUT_RESIDUAL_N2_SEMANTIC_SELECTION`.

## 7. FQ008 source status versus theorem status

These are recorded separately as required.

### Source status

The exact FQ008 refoundation source supplied to this task is

`definitions/ENTERPRISE_THREE_POSITIVE_AXIS_OVERLAPPING_CIRCLE_CELL_PLANE_20260820.md@b6ec6eb58f1c724657db7c5bd7deb82827774439`.

It contains the `axis-square + transverse-independence -> derived sum-of-squares` formulation.

The FQ010 owner branch currently resolves the same definition path to the earlier `9866e523...` formulation in which the sector Pythagorean/sum-of-squares law is primitive. FQ010 therefore does not silently report the integration-candidate source as the currently integrated owner-base Foundation definition.

### Theorem status

The FQ008 scalar theorem is exact on the declared sector domain, and FQ010 proves it is bidirectionally equivalent to `Q_K` at scalar strength.

Thus:

`FQ008_SOURCE_STATUS != FQ008_THEOREM_STATUS`.

## 8. Explanatory-compression calibration

Using current line/trace semantics strictly downstream, Interface K obtains:

- shuffle invariance: `EXACT_RECOVERY`;
- component provenance at relation strength: `EXACT_RECOVERY`;
- relation-level explanation of word-count versus squared-readout distinction: `EXACT_RECOVERY` on the origin-based sector domain;
- axis quadratic scaling: `EXACT_RECOVERY`;
- two-component additivity/transverse independence: `EXACT_RECOVERY`;
- current origin-based sector length output: `DOMAIN_RESTRICTED_RECOVERY` because arbitrary point-to-point/cross-sector line semantics remain open in the downstream source.

This is explanatory compression beyond pointwise formula recovery, but it is not a proof that the scale role is N0-forced.

## 9. Target-leakage and circularity disposition

`TARGET_LEAKAGE_AND_CIRCULARITY_AUDIT = PASS`.

Specifically:

- R065 clean relation evidence remains typed as blind relation evidence;
- `Q_K` remains a Driver/FQ010 post-freeze readout consequence, not retroactive blind selection;
- current `LENGTH` and sum-of-squares outputs were not used to construct `R_type`;
- current line/trace semantics was used only after construction as downstream calibration;
- no Formula -> premise -> same Formula circular derivation is claimed.

## 10. Why the final disposition is HYBRID rather than REPLACE

A full `REPLACE` verdict would require treating the cardinality/scale identification as already justified strongly enough to discard the scalar-admission boundary. FQ010 does not have that theorem.

The exact remaining datum is:

> an explicit Foundation semantic declaration or an independent calibration principle selecting the cardinality of the component-kernel relation as the squared-line-scale readout among the other intrinsic scalar valuations that survive the same N0 substrate.

FQ008's scalar conditions provide an exact characterization/calibration of that choice and discriminate nearby observation-resolution alternatives. Therefore retaining them as the scalar admission/checking layer is mathematically useful even though K gives the stronger structural explanation.

The recommended architecture is:

`typed component content`

`-> N0-definable R_type=ker(tau)`

`-> N2 candidate readout Q_K=|R_type|`

`-> explicit semantic declaration/calibration for squared-line-scale role`

with FQ008 retained as an exact scalar characterization/calibration theorem rather than misreported as relational information.

If a later Foundation disposition explicitly declares the N2 role `squared line scale := |R_type|`, then FQ008 axis-square and transverse-independence conditions can be reclassified downstream as derived characterizations. That future mutation is outside FQ010.

## 11. Acceptance gates

1. `R065_PROVENANCE_TYPED_WITHOUT_RETROACTIVE_REWRITE = PASS`
2. `RELATION_DEFINABILITY_AND_MAXIMALITY_CLASSIFIED = PASS`
3. `CARDINALITY_READOUT_TYPED_SEPARATELY_FROM_RELATION = PASS`
4. `FQ008_BIDIRECTIONAL_SCALAR_EQUIVALENCE_CLASSIFIED = PASS`
5. `RELATION_VS_SCALAR_INFORMATION_STRENGTH_CLASSIFIED = PASS`
6. `PRIMITIVE_AND_SEMANTIC_BUDGET_COMPARED = PASS`
7. `OBSERVATION_RESOLUTION_ALTERNATIVES_PRESSURE_TESTED = PASS`
8. `SQUARED_LINE_SCALE_ROLE_ADMISSION_EXPLICITLY_DECIDED_OR_LEFT_WITH_EXACT_MISSING_DATUM = PASS`
9. `EXPLANATORY_COMPRESSION_TESTED_BEYOND_POINTWISE_FORMULA_RECOVERY = PASS`
10. `TARGET_LEAKAGE_AND_CIRCULARITY_AUDIT_PASS = PASS`
11. `CURRENT_SEMANTIC_CLAIM_LEDGER_COMPLETE = PASS`
12. `DETERMINISTIC_CHECKER_PASS_OR_RESIDUALS_PRESERVED = PASS`
13. `CURRENT_SOURCE_STATUS_DISTINGUISHED_FROM_THEOREM_STATUS = PASS`
14. `NO_FOUNDATION_DEFINITION_MUTATION_PERFORMED_INSIDE_RESEARCH_TASK = PASS`

## 12. Deterministic regression

The exact checker covers all sector-supported component multiplicities `0..8`:

- 217 sector-supported states;
- 1302 component relabeling checks;
- 434 token-renaming regression checks;
- 4675 exhaustively enumerated equivalence partitions on carriers of at most six tokens;
- 1609 component-preserving equivalence partitions;
- 27 axis-square checks;
- 192 mixed-second-difference checks;
- 243 FQ008 recurrence reconstructions;
- 217 observation-resolution checks;
- 3 orbit-level scalar-collision groups;
- 0 mismatches.

General proofs, not finite checking, carry the theorem claims.

## Driver return

Hard target achieved:

`LINE_SCALE_SEMANTIC_ADMISSION_COMPARATIVE_REFOUNDATION_CLASSIFIED = ACHIEVED`.

Return to Driver with exact final classification:

`HYBRID_RELATION_ADMISSIBLE_BUT_SCALE_ROLE_REQUIRES_SEPARATE_DECLARATION_OR_CALIBRATION`.

No current Foundation definition was modified. Stop here per FQ010 stop rule.
