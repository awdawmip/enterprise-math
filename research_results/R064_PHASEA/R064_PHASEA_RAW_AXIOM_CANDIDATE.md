# R064 Phase A — Raw Axiom Candidate Freeze

Freeze: `2026-08-22T15:49:42+08:00`

## Raw candidate retained

`COMPONENT_COMPLEMENT`

On the primitive three-axis component carrier `A`, define a binary relation/function:

- equal inputs return that axis;
- distinct inputs return the unique third axis.

Status:

`N0_DEFINABLE_DERIVED_COMPONENT_RELATION`

This is retained because it has an explicit N0 definability DAG and exact `S3` equivariance.

## What is **not** frozen as an axiom

The candidate is **not** promoted to Foundation and is **not** activated as Working Truth. In particular Phase A does not add:

- `PROCESS_FACTORS_THROUGH_COMPONENT_TAGS`;
- an output-event sector selector;
- a source/position creation rule;
- a preferred orientation;
- an identity/associativity/group axiom.

The full N0 local-event context admits an exact nonunique family. Therefore the raw candidate is frozen only as one intrinsic component-level relation inside that family, not as the unique native local process.

## Exact missing-axiom statement

To obtain a unique nontrivial axis-output process from the full local event context, an additional operational constraint must at least force factorization through component tags. If the output must be an elementary event object, a further single-valued event-lift/placement rule is necessary.

Phase A stops here.
