# HODGE H0A0 Semantic Checkpoint

Date: `2026-08-17`  
Researcher-ID: `EM-HODGE-H0-2F8C71`  
Task: `RS-HODGE-H0A0-REALIZATION-COMPARISON-CRITERION-REPAIR`  
Driver: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`  
Owner branch: `research/hodge-h0a0-realization-criterion-repair`  
Taskbook source: `7ebcccda1fb5550c2db6d204bbf381469e2ea366`  
Parent H0 frozen head: `b30bd1f662597f1819d7e96934bca2069036dcf4`

## Frozen disposition

`H0A0_CRITERION_V2_FROZEN_NONVACUOUS`

Hard target:

`HODGE_RELEVANT_REALIZATION_CRITERION_IS_WELL_TYPED_AND_NONVACUOUS = PASS`

H0A0 repairs the realization classifier only. It does not construct a new Enterprise realization, does not prove Hodge, and does not open H1.

## 1. Comparison-strength repair

The overloaded comparison symbol `C(X)` is removed from load-bearing classifier use. Later stages must name the exact comparison carrier:

- `C_full^alg(X)`: declared full algebraic source structure;
- `C_full^an(X)`: analytification as a complex analytic locally ringed space, with coherent-sheaf/morphism/cohomology categories added only when explicitly invoked;
- `C_top(X)`: declared topological/homotopy reduct only;
- `C_H(X)`: exact cohomological/Hodge readout actually declared;
- `C_state(X;O)`: deliberately limited observation/readout.

Frozen conclusion:

`NO_SINGLE_UNTYPED_CLASSICAL_COMPARISON_OBJECT`.

No total linear strength ordering is assumed between these objects unless an exact comparison map is proved.

## 2. GAGA boundary

For a complex projective algebraic variety, Serre's GAGA gives the projective coherent-sheaf/coherent-cohomology comparison and algebraization results recorded in the dedicated boundary matrix. This is strong evidence that "information absent from full analytification" is not a suitable universal necessary qualification condition for a Hodge realization.

The exact negative boundary is nevertheless:

`GAGA_DOES_NOT_SOLVE_RATIONAL_HODGE_CONJECTURE`.

GAGA algebraizes analytic/coherent objects that already exist under its hypotheses; it does not construct an analytic subvariety or algebraic cycle representing every rational Hodge class. The missing existence/lifting obligation therefore remains outside GAGA.

## 3. Information axis separated from operational axis

Information/recoverability is now a secondary tag relative to an explicitly typed comparison target. Operational proof leverage is the primary realization rank.

Frozen invariants:

- `INFORMATION_NONFACTORIZATION_IS_NOT_A_NECESSARY_OPERATIONAL_R2_GATE`;
- `WEAK_READOUT_NONFACTORIZATION_DOES_NOT_EARN_R2`;
- `INVERTIBILITY_DOES_NOT_FORCE_R0_OR_R1`;
- `INFO_ENRICHED_DOES_NOT_IMPLY_R2`.

Thus an information-equivalent realization may be operationally R0 or R2, while an information-enriched construction relative to a weak readout may remain R0.

## 4. Realization Classifier V2

### R0 — `INERT_REPARAMETRIZATION`

Only labels/coordinates/theorem-critical structures are transported item by item. No independently defined operational structure and no strict proof-leverage certificate survives.

### R1 — `DERIVED_REORGANIZATION`

A genuine derived representation/computational organization exists, but no theorem-critical strict proof-leverage certificate has been established.

### R2 — `NONTRIVIAL_OPERATIONAL_REALIZATION`

Information enrichment is not required. R2 requires exact typed comparison, an independently generated operational structure, an exact comparison/correctness theorem, a non-tautological strict `PROOF_LEVERAGE_CERTIFICATE`, the required naturality/functoriality scope, target-leakage PASS, and explicit prior-art/novelty status.

### R3 — `HODGE_RELEVANT_OPERATIONAL_REALIZATION`

R2 plus a typed, non-tautological interface

`rational Hodge-class condition -> Enterprise operational constraint -> algebraic-cycle lifting interface / exact unresolved lifting obligations`.

R3 does not itself assert the final lifting theorem.

## 5. Exact nonvacuity controls

The classifier passes both directions of the stress test:

1. **Bijective renaming:** information-equivalent but operationally `R0`.
2. **Arbitrary hidden color:** information-enriched relative to a deliberately weak `C_state`, but operationally `R0`.
3. **Invertible two-point DFT:** information-equivalent, yet the exact invertible transform changes 2-point circular convolution by `(1,1)` from matrix `[[1,1],[1,1]]` to `diag(2,0)`. The predeclared off-diagonal-nonzero count strictly drops `2 -> 0`, giving a prior-art `NORMAL_FORM` control eligible for operational R2 while earning zero Enterprise novelty credit.
4. **Finite graph potential control:** global existence of a vertex potential is equivalent to vanishing on the finite fundamental-cycle basis of a spanning tree; this supplies exact `FINITE_OBSTRUCTION_BASIS + LOCALIZATION + COMPOSITIONAL_FACTORING` prior-art leverage, again with zero Enterprise novelty credit.
5. **Weak-readout gaming:** explicit non-factorization relative to a coarse observation is rejected as R2 evidence.
6. **Full analytic GAGA boundary:** used only to delimit the comparison criterion, never as a Hodge conclusion.

Therefore the repaired classifier is nonvacuous rather than a rule that labels every invertible transform R2.

## 6. Legacy H0 retype without history rewrite

Historical H0 remains frozen and true under its own taskbook:

`the audited candidates did not satisfy legacy information-nonfactorization R2 under all H0 gates`.

It did **not** prove that those candidates have no theorem/proof leverage under V2.

Using old artifacts only, without new carrier research:

- Candidate A / precision-collapse: highest established V2 class `R1_DERIVED_REORGANIZATION` on its chosen-presentation scope; presentation/naturality remains unresolved and no complete V2 leverage certificate exists.
- Candidate B / Boolean BRC relation: highest established V2 class `R1_DERIVED_REORGANIZATION`; independent branch/recoalescence operations exist but no Hodge-theorem-critical strict leverage certificate or R3 bridge was established.

No retroactive R2 is manufactured.

## 7. Hodge leverage interface

Any future R3 candidate must type and prove the interfaces, without using the desired cycle answer as generator data:

1. `alpha in H^{2p}(X,Q) ∩ H^{p,p}(X)` at `C_H(X)`;
2. a typed realization/comparison map into an Enterprise carrier;
3. independently defined Enterprise constraint `K_E^p`;
4. an exact Hodge-side-to-Enterprise theorem;
5. at least one strict operational leverage theorem;
6. an explicit algebraic-cycle/Chow lifting interface with existence, descent/functoriality, cycle-class compatibility, rational-coefficient and choice-independence obligations;
7. a later lifting correctness theorem. This final arrow is not assumed here.

## 8. Suspended H0A decision

Driver-facing recommendation:

`RETYPE_H0A1_AS_PRESENTATION_NATURALITY_SUBPROBLEM`.

The historical H0A equivariant-presentation task must **not** execute unchanged. Equivariance/naturality remains necessary for any presentation-dependent global R2/R3 claim, but it is only a qualification sub-gate. The main search must first identify an independently defined Enterprise operational structure plus a strict V2 leverage theorem.

No H0A1 starts automatically.

## 9. H1 firewall

`H1_ADMISSIBLE = false`.

H0A0 cannot open H1. Under the V2 default, R2 alone is not enough; an accepted R3-class Hodge leverage/lifting interface or an explicit later Driver override is required.

## Semantic checkpoint digest

`HODGE_H0A0_SEMANTIC_CHECKPOINT_SHA256 = d408ac4915913590ff10ca796cb3fb2af9921e2920af100df7b8338e303256fa`

This SHA-256 is over the canonical semantic-core JSON used to freeze the stage's disposition and invariants; it is not a Git commit hash.
