# R059D Stage AK — Target fixed-length native turn-orbit certification report

Researcher-ID: `EM-R059D-AK-3C7E52`

Task-ID: `RS-R059D-STAGE-AK-TARGET-FIXED-LENGTH-NATIVE-TURN-ORBIT`

Owner branch: `research/r059d-stage-ak-target-fixed-length-native-turn-orbit`

Frozen source main: `e28a11f6a3e94ec1bf1e1bf7ddbc0a062eef5183`

Taskbook source: `8628970bd3f072b7a8823873ec4597c1e31c4652`

## Primary disposition

`TARGET_FIXED_LENGTH_NATIVE_TURN_ORBIT_THEOREM_PROVED__KAPPA_NATIVE_CIRCLE_CONSTANT`

## What AK proves

Stage AH already generated the accepted N boundary autonomously, but its implementation built a first-sector word and then reflected/completed it. AK exposes the same object as a genuine one-step target dynamics.

For every integer `r>=1` and center `O`, define a target segment state

`S=(O,r,sector,phase,a,b,z)`

with free endpoint

`O+R^sector(a,b)`, `R(a,b)=(-b,a+b)`.

The anchor state is

`S_r(O)=(O,r,0,L,r,0,-4)`.

The integer `r` is the primitive-axis step count of the canonical anchor segment. Target equal length is the orbit-class equivalence generated by translation and legal turns from the same anchor radius, not a Euclidean equal-distance predicate.

## One-step machine

The left phase uses the accepted AH residual `rho`.

When the path reaches the midline, AK derives a direct forward right residual

`sigma=rho+9*b+3`.

No left-half history is stored.

Right phase:

- if `sigma>=0`: emit `2`, set `sigma<-sigma+3*(a-b-2)`, `(a,b)<-(a-1,b+1)`;
- if `sigma<0`: emit `3`, set `sigma<-sigma+3*(2*a+b-2)`, `(a,b)<-(a-1,b)`.

When the emitted edge lands at local `(0,r)`, the resulting state is normalized to the next sector anchor. The equality

`R^k(0,r)=R^(k+1)(r,0)`

means the normalization moves no endpoint and consumes no extra turn.

Thus every application of `tau` emits exactly one legal Enterprise adjacency edge using a constant number of integer registers.

## All-radius endpoint-orbit theorem

AK proves the first-sector one-step orbit equals the frozen AH first-sector path edge for edge. Six copies are transported by one D6 law rather than six tuned machines.

The complete free-endpoint orbit is therefore exactly the accepted AH D6 boundary.

The center `O` never moves and the Enterprise length class `r` never changes.

Within each sector, `a` never increases and `b` never decreases, with nonzero movement every turn. Interior vertices have `a>0,b>0`. Rotated open-sector interiors are disjoint. Hence no endpoint repeats before final closure.

## Period = circumference

One sector has exactly

`|W_N(r)|=r+J_N(r)`

turns. Therefore the minimal positive segment-state period is

`T_r=6*(r+J_N(r))=C_N(r)`.

With AG,

`T_r=6*(r+floor(alpha*r+1/3))`.

With AI,

`lim T_r/(2r)=kappa_E`,

`kappa_E^2=12`, `kappa_E>0`.

Therefore native circumference is now operationally the number of elementary legal turns required for the fixed-length segment endpoint to complete one full orbit.

The increment law `T_r-T_(r-1) in {6,12}` makes the period strictly increasing, so the closed orbit itself distinguishes the integer length classes.

## Covariance

Translation covariance:

`tau(T_t S)=T_t(tau(S))`.

Fixed-endpoint D6 rotation covariance:

`tau(Rot_j S)=Rot_j(tau(S))`.

`R^3` supplies sign inversion. D6 reflections reverse the simple cycle and conjugate forward turns to inverse turns at orbit level.

## State-size boundary

A sufficient online state is

`r + (a,b) + one signed residual + sector + phase`,

plus the carried center `O` for endpoint projection.

No unbounded prefix/history is needed.

True information-theoretic minimality remains open because the residual is algebraically reconstructible from coordinates if the frozen support polynomial is recomputed. Such recomputation is excluded by the target runtime firewall, so the correct freeze is

`SMALLEST_CURRENTLY_PROVED_SUFFICIENT_ONLINE_STATE_UNDER_RUNTIME_FIREWALL`.

## C_s compatibility

AJ's C_s family shares the same anchor radius label and differs from N by at most one shell / six circumference turns. AK records this as bounded finite-precision phase/readout compatibility.

AK does not claim the C_s endpoint path is the identical N tau orbit and does not build a C autonomous turn machine.

## Semantic firewall

The native target theorem uses no Euclidean norm/equidistance, source-circle query, source Q lookup, standard pi, square root, floating point, trigonometry, occupancy table, full word table, boundary table, or radius-specific tuning.

`kappa_E` remains the Enterprise-native constant; no theorem about the standard real number `pi` is asserted.

## Verification

The deterministic checker verifies:

- every `r=1..4096` first-sector AK symbol/endpoint sequence exactly equals the independent frozen AH construction;
- factorized full D6 equality and no-early-repeat conditions for every radius;
- exact `T_r=C_N(r)` count and AG shell threshold;
- legal adjacency and preserved length class;
- translation, fixed-endpoint rotation, and chart-rotation covariance;
- full materialized endpoint orbit equality, exact period, and no early return at `r=8192,16384`;
- runtime firewall.

Pre-history-gate checker result:

`73745/73745 PASS`.

Checker digest:

`aee36414f23757bf859639eb0aba413c5797a8ed8f94ce94f604e1cd8178426d`.

Finite replay validates the implementation; the all-radius theorem is the symbolic proof in `R059D_STAGE_AK_PROOF.md`.

## Remaining open boundaries

AK does not prove:

- information-theoretic minimality of the online state;
- a full autonomous C_s fixed-length turn machine;
- N/C unique canonical resolver selection;
- pointwise identity of N and C_s endpoint orbits;
- any theorem identifying `kappa_E` with the standard real `pi`;
- any AL or later result.

`STOP_FOR_DRIVER_REVIEW` after final checker/history/manifest/checkpoint freeze.
