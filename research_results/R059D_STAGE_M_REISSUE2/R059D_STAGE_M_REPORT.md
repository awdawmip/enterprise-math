# R059D Stage M REISSUE2 — Three-Axis Vector Carrier / Geometric-Tool Refoundation / Microgrammar Robustness

Researcher-ID: `EM-R059D-9C6B2A`  
Taskbook source: `823c5d8aaa0d63f9914e14a4375ad8fb3876f76f`  
Frozen parent: Stage-L `da350b7b1e2ae21491e6251fdf2ba9cf0d4557ca`

## Project semantic mode

`REFOUND, NOT REJECT`.

VECTOR, LENGTH, DISTANCE, ANGLE, NORM, DOT/PAIRING, SIN/COS/TAN, PI, AREA, VOLUME and EUCLIDEAN GEOMETRY remain admitted mathematical concepts/tools. Stage M only withholds automatic inheritance of undeveloped classical definitions into the BRC6 selector premise.

Freeze for this task only:

- `LENGTH_NOT_USED_AS_BRC6_SELECTOR_PREMISE`
- `ANGLE_NOT_USED_AS_BRC6_SELECTOR_PREMISE`
- `TRIG_NOT_USED_AS_BRC6_SELECTOR_PREMISE`

`SEGMENT_PACKET_COUNT=4` is the common experimental endpoint boundary and is not declared to be `GEOMETRIC_LENGTH=4`.

## Three-axis vector carrier

Freeze

`Lambda={(a,b,c) in Z^3 : a+b+c=0}`

with

`u=(1,-1,0)`, `v=(0,1,-1)`, `w=(-1,0,1)`, `u+v+w=0`.

`PLANE_DIMENSION=2`, `AXIS_COUNT=3`, and

`D6={+u,-u,+v,-v,+w,-w}`.

BRC6 is retyped as `NEXT_STEP_VECTOR in D6`; integer labels are serialization keys only.

For every `x in Lambda` and `d in D6`, `x+d in Lambda` because both coordinate sums are zero.

Freeze:

`DISCRETE_THREE_AXIS_VECTOR_CARRIER_ESTABLISHED`.

## Declared four-packet vector transport

Use the same four declared packet-cell states `P0,P1,P2,P3` for every candidate. Let `rho` cyclically map axes while preserving orientation. The frozen canonical transition word is

`W_transport(d)=[d,d,rho(d),-rho(d)]`.

Thus the exact vector endpoint displacement is derived, not assumed:

`S_d=sum W_transport(d)=2d`.

The word contains a repeated vector step and an immediate reversal. `P1` and `P3` may have the same Lambda coordinate while remaining different declared packet states, so coordinate equality is not silently identified with packet identity.

The tested transport symmetry is generated by cyclic axis permutation `rho` and global inversion. Full fixed-rho covariance under odd S3 reflections is explicitly not claimed; an odd reflection conjugates `rho` to `rho^-1` unless the oriented companion datum is transformed as additional structure.

## Microgrammar registry and raw-history oracle

Six grammars were frozen before witness scoring and the same grammar is used for all six candidate vectors in one evaluation.

Their direct unit-seed endpoint history counts are:

- G0 `AFFINE_UV_REPLAY`: `(alpha_A,beta_I)=(1,3)`
- G1 `SINGLE_SPLIT_RECOALESCE`: `(1,2)`
- G2 `DELAYED_SPLIT_RECOALESCE`: `(1,2)`
- G3 `TWO_STAGE_SPLIT_RECOALESCE`: `(1,4)`
- G4 `LAUNCH_CLASS_SPLIT_CONTROL`: `(2,1)`
- G5 `VECTOR_INTERNAL_REWRITE_ENDPOINT_EQUIVALENT`: `(1,2)`

All multiplicities arise from explicitly distinct raw histories. W0/WD/WQ/WDQ exhibit repeated steps, immediate reversals, zero-sum loops, packet-state revisits and distinct vector words reaching the same endpoint. G5 has distinct internal lineages with the same vector word. All branch histories reaching the endpoint recoalesce by natural-number addition; support-only collapse is forbidden.

For all six grammars, all six D6 candidates, and every tiny `(A,I) in {0,1,2,3}^2`, direct raw-history multiplicity equals exact CPBC endpoint count: 576/576 seed/candidate cases.

## Endpoint functionals

Raw histories derive the exact endpoint functions:

- G0: `B=A+3I`
- G1: `B=A+2I`
- G2: `B=A+2I`
- G3: `B=A+4I`
- G4: `B=2A+I`
- G5: `B=A+2I`

No coefficient is an arbitrary weight.

G1/G2/G5 have different internal organization but the identical endpoint functional `A+2I`. Therefore they produce identical six-vectors and identical BRC6 winner/unresolved status for every state.

Freeze:

`BRC6_ENDPOINT_FUNCTIONAL_EQUIVALENCE_THEOREM`.

## Mandatory witness replay

`W_ASYM_BASE` is strong consensus: all grammars select `-w`.

`W_CONSENSUS_DOMINANT2` is strong consensus: all select `+v`.

`W_S1_TIE_S2_RESOLVE` is strong consensus: all select `-w`.

`W_FULLY_SYMMETRIC` and `W_SIGNATURE_INSUFFICIENT_PAIRS` are all-unresolved.

The pre-frozen 3840-template exact search box contains all four requested classes:

- `MICROGRAMMAR_STRONG_CONSENSUS_RESOLVED`: 2610 templates
- `MICROGRAMMAR_COMPATIBLE_WITH_TIES`: 180
- `MICROGRAMMAR_DEPENDENT`: 810
- `MICROGRAMMAR_ALL_UNRESOLVED`: 240

Thus both robust and dependent domains are nonempty.

Freeze:

`BRC6_VECTOR_ENDPOINT_COUNT_MICROGRAMMAR_ROBUST_DOMAIN_ESTABLISHED`

and

`BRC6_VECTOR_ENDPOINT_COUNT_MICROGRAMMAR_DEPENDENCE_ESTABLISHED`.

## Explicit dependence certificates

Cross-axis conflict:

`A=(0,0,1,0,0,0)`, `I=(1,0,0,0,0,0)`.

G0/G1/G2/G3/G5 select `+u`, while G4 selects `+v`.

A second state `A=(0,1,0,0,0,0)`, `I=(1,0,0,0,0,0)` gives a same-axis orientation conflict: the first five classes select `+u` and G4 selects `-u`.

Hence equal packet cells and a common endpoint do not uniquely determine the endpoint-history functional or native BRC6 law.

`BRC6_NATIVE_CANONICALITY = NOT_ESTABLISHED`.

## Microgrammar-independent sufficient condition

Every frozen functional is `alpha_g A + beta_g I` with `alpha_g,beta_g>0`.

If one candidate d satisfies `A_d>=A_e` and `I_d>=I_e` for every competitor, with at least one strict coordinate against each competitor, then every frozen grammar uniquely selects d. This supplies an exact nonempty grammar-robust cone/domain without hiding the dependence counterexamples.

## Axis/orientation factorization

The direct six-way unique endpoint-count argmax admits an exact lossless two-stage factorization.

For each undirected axis define

`Q_u=max(B_+u,B_-u)`, and similarly for v,w.

First require a unique maximal axis score; then require a unique orientation winner within that axis. This partial staged selector is exactly equivalent to direct six-way unique argmax, including all tie/unresolved cases.

Freeze:

`BRC6_AXIS_ORIENTATION_FACTORIZATION_ESTABLISHED`.

This is not true for arbitrary axis aggregation. Explicit control: `B=(4,0,3,2,0,0)` directly selects `+u`, but pair SUM gives axis totals 4,5,0 and incorrectly selects v-axis / `+v`.

## True vector-state dynamics

At each resolved decision use Stage-K/L count updates and also update the discrete coordinate by

`X_(k+1)=X_k+S_(d_k)=X_k+2d_k`.

All six endpoint counts are recomputed from the new accumulated state.

On W_ASYM_BASE:

- Carrier A: all endpoint-functional classes give `-w,-u,-v` repeating for 48 decisions. The three-step vector sum is zero, so the Lambda coordinate returns to zero every three decisions. This is not a full-state cycle because `TOTAL_O` increases.
- Carrier B: all classes give `-w,+v` alternating; final 48-step coordinate is `(48,48,-96)`.
- Carrier C: G3 reaches exact tie at epoch 8; G0 at epoch 12; G1/G2/G5 keep `-w` with constant margin; G4 keeps `-w` with increasing margin.

Closed-form Carrier-C margins are:

- G0: `B_-w-B_+u=12-e`
- G1/G2/G5: `8`
- G3: `16-2e`
- G4: `19+3e`

On the cross-axis dependence witness, G0 first moves by `+u` to coordinate `2u=(2,-2,0)`, while G4 first moves by `+v` to `2v=(0,2,-2)`. Thus grammar-dependent endpoint counts create genuine vector-coordinate trajectory divergence.

Freeze:

`BRC6_VECTOR_ENDPOINT_COUNT_TRUE_STATE_MICROGRAMMAR_DYNAMICS_ESTABLISHED`.

## Perturbations

Frozen true-state perturbations were rerun for four endpoint-functional classes, three carriers, all six vector channels, and 96-decision windows.

G0/G1-equivalence/G3 have the same first-step pattern on W_ASYM_BASE: count token and tagged-vector-step controls leave first output `-w`; incidence in relative class 3 creates an immediate unresolved tie.

G4 is more sensitive because launch-class histories have multiplicity two. Its count-token table is `[-w,-w,UNRESOLVED,-v,-w,-w]`; its tagged-vector-step table is `[UNRESOLVED,-v,-w,-w,-w,UNRESOLVED]`.

For every endpoint-functional class, the W_S1 incidence control creates an exact `-v/-w` maximal tie. From the fully symmetric state, one count token or incidence event resolves to each of the six vectors, while the tagged vector-step transfer resolves according to rho: `+v,-v,+w,-w,+u,-u`.

Count-token and incidence perturbations cannot same-epoch full-state recoalesce because TOTAL_O or TOTAL_I has a permanent +1 offset. Tagged vector-step perturbations show no exact recoalescence in the frozen 96-decision window; no infinite-horizon impossibility is claimed.

## Large-N symbolic stress

For a fixed grammar `F_g=alpha_g A+beta_g I`, adding a common system background N to every candidate launch count gives

`B_d -> alpha_g*N + B_d`.

The added integer is candidate-common within that grammar, so every unique winner and exact tie is unchanged for all frozen probes near `10^36`.

N remains system/tag/packet scale only; it is not geometric length, norm, angle, vector magnitude or precision. No huge object/history enumeration is used.

## Geometric refoundation interface

Stage M does not define LENGTH, ANGLE, NORM or SIN/COS/TAN. Those concepts remain admitted and future tasks may rebuild them from the current vector/path/count substrate and then classify relation to classical Euclidean tools as:

`EXACT_RECOVERY`, `FINITE_PRECISION_RECOVERY`, `ASYMPTOTIC_RECOVERY`, `DOMAIN_RESTRICTED_RECOVERY`, `SYSTEMATIC_DEVIATION`, or `NONRECOVERY`.

Classical values, if later recorded, must be typed as calibration/readout unless independently refounded.

## Validation

Deterministic checker: `7589 / 7589 PASS`, zero failures.

Digest: `2ff4207c7cb47117216b3124357abcb6cef69c11642488ded0dca772e4235877`.

Parent immutability pre-manifest gate passed: Stage J/K/L artifacts are unchanged.

## Final interpretation boundary

Stage M establishes a structured vector carrier, exact vector-history bookkeeping, a nonempty endpoint-count microgrammar-robust domain, an explicit microgrammar-dependent domain, endpoint-functional equivalence, exact axis/orientation factorization, and true vector-coordinate dynamics.

It does not establish a unique native BRC6 law, physical direction calibration, physical probability, physical rigidity/elasticity, or a native classical length/angle/trigonometric definition.

`STOP_FOR_DRIVER_REVIEW`.
