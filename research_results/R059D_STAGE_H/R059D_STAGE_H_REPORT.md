# R059D Stage H — Relay Regeneration / Attenuation / Localization

Researcher-ID: `EM-R059D-4C7E21`  
Taskbook: `89c19be9fdf26a5c2b3eadda8689d34879db680c`  
Frozen parent: `a9dcb2ac0190b5fdd972ca8f7a561836317e350a`

## Disposition

`RELAY_REGENERATION_AND_ENDOGENOUS_LOCALIZATION_FAMILY_FOUND`

## Exact Stage-G regeneration

For verbatim `G1_RELAY_H_RECURRENT_ALIGN`, every preclosure recruitment event has the same canonically relabeled local front:

`incoming ingress H, resident START, S_SELF=2, L_SELF=2, M_FRONT=1, actions V/H`.

The old front settles through `V -> V_INV`; the resident becomes the unique outgoing `H` front with `M_FRONT=1`. Hence

`Phi(rho_G)=rho_G`

exactly. The H_INV mirror is identical under `H <-> H_INV`.

Freeze:

`EXACT_RELAY_FRONT_REGENERATION = ESTABLISHED`.

This self-loop alone re-derives system-spanning response: a nonzero transmitting canonical front can repeat through arbitrarily many homogeneous recruitment sites, so a finite N object is exhausted by N-1 transfers. Recruitment remains `e=q*i-1`.

After first closure the resident class is `V_INV`, but the postclosure front is again an exact transmitting self-loop, explaining Stage-G recurrence.

## Finite-state relay theorem

For a finite controller-relevant canonical relay graph:

- a reachable transmitting cycle gives an arbitrarily repeatable CPBC branch and therefore system-spanning causal-support closure on every nondegenerate finite homogeneous object;
- if no reachable transmitting cycle exists, the transmitting graph is acyclic after SCC condensation, so a finite graph-derived `K0` bounds participant closure independently of N;
- if recurrent and extinct branches coexist, preserve `BRANCHING_MIXED`: causal-support closure spans because the recurrent branch is retained, although not every history transmits.

Freeze:

`FINITE_STATE_RELAY_REGENERATION_LOCALIZATION_DICHOTOMY = ESTABLISHED`.

## Endogenous relay integer M

`M_FRONT` is the exact current number of source-tag lineages at the front packet whose ingress equals the relay direction. At a canonical one-resident collision:

`S_SELF=M_FRONT+1`.

It is a current exact relational/source-support readout, not tag identity, time, a countdown, remaining range, N, or q. Therefore:

`LARGE_INTEGER_RELAY_STATE_LANE = OPENED`.

## Transfer classes

- Stage-G G1: reachable `1 -> 1 -> ...`; generalized collision `M -> 1`. `REGENERATIVE / NONZERO_RECURRENT_CYCLE`.
- `H_AMPLIFY_COHORT`: `M -> M+1`. `AMPLIFYING`, system-spanning.
- `H_NONZERO_PERIOD_12`: `1 -> 2 -> 1 -> 2 ...`. `NONZERO_PERIODIC`, system-spanning.
- `H_EVENTUAL_ZERO_IMMEDIATE`: `M -> 0`; real I3 closure `min(N,2):N`.
- `H_BRANCHING_MIXED_01`: `Phi(M)={0,1}`; extinct and recurrent branches coexist; causal-support union spans.

### Finite-cap localization family

For each pre-frozen `C in {1,2,3,5,8,13}`, one stationary local rule uses only current ingress, current `S_SELF`, and C:

`Phi_C(M)=M+1` for `1<=M<=C`;

`Phi_C(M)=0` for `M>=C+1`.

Starting from the real tagged perturbation (`M0=1`):

`1,2,...,C+1,0`.

The terminal collision still recruits its resident through an exact local count-signature delta, but leaves no outgoing relay front. Thus:

`RESPONSE_PARTICIPANT_COUNT_CLOSURE=min(N,C+2)`.

For every fixed C and `N>C+2`:

`BOUNDED_LOCALIZED_RELAY_RESPONSE`.

Localization is generated by current exact local cooccupancy crossing the fixed rule predicate. There is no external K/T, timer, tag counter, or remaining-range state.

The H_INV mirror has identical formulas.

## Large-M stress

At `M≈10^36` and frozen neighbors:

- G1: `M -> 1 -> 1...`;
- amplifier: `M -> M+1`;
- period-12: huge `M -> 1 -> 2 <-> 1`;
- immediate zero: `M -> 0`;
- mixed: `M -> {0,1}`;
- every frozen finite-cap C: huge `M -> 0`.

Scale-down of `H_CAP_C` gives the exact boundary `M=C+1`. It is only:

`RELAY_STATE_INTEGER_CROSSOVER_CANDIDATE`.

The boundary moves with C (`2,3,4,6,9,14`), so it is controller-parameter dependent, not an intrinsic N or physical transition.

## Important limitation

A single parameterized controller family with one arbitrary-N regenerative endpoint and another arbitrary-N localized endpoint was **not** established. Stage-G G1 and the finite-cap attenuation family are distinct stationary rule representations. Stage H does not merge them into a fictitious coupling axis.

## Scheduler and firewalls

`S_SYNC` and `S_ALL_ORDERS_SNAPSHOT` have identical canonical transfer classes. Order/Cartesian multiplicity differences are diagnostic only.

`INTRINSIC_N_MACRO_MICRO_CROSSOVER = NOT_IDENTIFIED`.

`PHYSICAL_ELASTICITY_FROM_RELAY_COUPLING = NOT_ESTABLISHED`  
`PHYSICAL_RIGIDITY_INTERPRETATION = NOT_ESTABLISHED`  
`PHYSICAL_PROBABILITY_FROM_COUNTING = NOT_ESTABLISHED`  
`QUANTUM_BRIDGE = NOT_ESTABLISHED`

Deterministic checker: see frozen checker output.

`STOP_FOR_DRIVER_REVIEW`
