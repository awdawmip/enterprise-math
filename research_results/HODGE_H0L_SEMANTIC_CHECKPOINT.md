# HODGE Stage H0L — Coniveau / Support-First Downward-Collapse Semantic Checkpoint

Status: `FROZEN / RETURN_TO_HODGE_CONTROL_PLANE`

Researcher-ID: `EM-HODGE-H0L-8D4C27`
Task-ID: `RS-HODGE-H0L-CONIVEAU-SUPPORT-DOWNWARD-COLLAPSE`
Taskbook source: `e74eeb9966f0adb7deb74408b74336c2aa20c542`
Owner branch: `research/hodge-h0l-coniveau-support-collapse`

## Final disposition

`H0L_SUPPORT_FIRST_EQUALS_CYCLE_GYSIN_NORMAL_FORM_R1`

Hard prerequisite A:
`DEGREE_2P_ALGEBRAICITY_CONIVEAU_EQUIVALENCE_TYPED_WITHOUT_OVERCLAIM = PASS`

Primary hard target B:
`SOURCE_DERIVED_CONIVEAU_DOWNWARD_COLLAPSE_ADDS_ROBUST_ATTRIBUTED_LEVERAGE = NOT_ESTABLISHED`

Preferred target C:
`CONIVEAU_SUPPORT_COLLAPSE_ENTERPRISE_R3_PRESEED = NOT_ESTABLISHED`

`H1_ADMISSIBLE=false`
`Hodge_proved=false`

## Phase-A firewall

Phase A used only the Fermat equation/symmetry and H0K cycle-independent rational Hodge carrier inputs. H0K final class-first lift, plane-cycle class matrix, blockwise coefficients, and Gram inverses were not read until the H0L support certificates, support Gram, source-only pivot support, and predeclared metrics had frozen.

## Exact rational H4 control

For the Fermat cubic fourfold, the cycle-independent character census gives
`dim_Q H^4=23`, with `dim_Q V_Hdg,Q=21` and a two-dimensional rational non-Hodge block `V_nonHdg,Q` of types `(3,1)+(1,3)`. Every codimension-2 degree-4 support Gysin image is `(2,2)`, so every nonzero vector of `V_nonHdg,Q` is an exact false-positive control.

## Degree-2p coniveau theorem

For smooth projective complex `X` with rational coefficients,
`N^p H^{2p}(X,Q)` equals the rational span of codimension-p algebraic cycle classes. Localization identifies the support kernel with `im H_Z^{2p}->H^{2p}`; purity/Gysin gives fundamental classes for smooth codimension-p components; singular support is typed by resolution/stratification and top Borel-Moore homology. No generalized-Hodge-conjecture claim is made.

## Source-generated support theorem

The Fermat equation regenerates 405 target-independent planes. Their exact support/intersection grammar has:
- 4860 unordered pairs meeting in a line;
- 31590 meeting transversely in a point;
- 45360 disjoint pairs;
- 4050 unique line strata;
- 3690 unique point strata.

The exact Gysin/intersection rule is `3,-1,1,0` for same-plane/common-line/point/disjoint. The full support Gram has rank 21 and SHA-256
`99607a1ee33bd020ca59a74d9896ea901ff3fdb354c29cdd6cdab5e2a3ecf8ba`.

Hence for the universal support `Z_all`,
`ker[H^4(X_F,Q)->H^4(X_F-Z_all,Q)] = V_Hdg,Q`.

A source-only RREF pivot selects plane indices
`[0,1,2,3,4,9,10,12,13,27,28,36,37,81,82,84,85,108,109,189,190]`.
The corresponding 21x21 principal Gram determinant is `27`, so the union `Z_21` of those planes has the same restriction kernel.

Moreover, any union of `m` irreducible codimension-2 surface components has top support carrier dimension at most `m`; therefore 21 components are necessary to support all of `V_Hdg,Q`, and `Z_21` attains this component-count lower bound.

## Downward collapse

The source-localization carrier has exact strict reductions

`36855 -> 405 -> 21`

where 36855 counts 405 components plus 4860 line-pair and 31590 point-pair obligations, 405 is the top local-cohomology/Borel-Moore generator count, and 21 is the exact Gysin-image basis count.

This is genuine `LAYER_LOWERING + LOCALIZATION + DEPENDENCY_REDUCTION + COMPOSITIONAL_FACTORING + NORMAL_FORM` against `B_raw^support`.

## Attribution

Against fair `B_std^support`, the same reductions are standard source mathematics:
- `36855 -> 405` is top local-cohomology/purity/Cousin collapse;
- `405 -> 21` is ordinary Gysin-image rank / exact linear algebra;
- class-sensitive support transport is finite-group representation plus Gysin/localization.

Thus all positive candidates are `SOURCE_INHERITED_LEVERAGE`; robust transform-attributed candidate count is `0`.

After Phase-A freeze, the H0K final lift and class matrix were read only as checker/fair-attribution controls and reproduced the same rank-21 Gram result.

## Multiplicity and rationality firewalls

`SUPPORT_2(alpha;Z)` depends on the closed support/complement. A doubled cycle has the same support truth while its Gysin coefficient doubles:
`RADICAL_SUPPORT_TRUTH != CYCLE_MULTIPLICITY`.

Negative and denominator-bearing rational classes transport Q-linearly; no effectiveness/positivity is introduced.

## Route decision

`CONIVEAU_SUPPORT_FIRST_FORMULATION_IS_SOURCE_COMPLETE_AT_AUDITED_FERMAT_SCOPE__CHANGE_ENTERPRISE_MECHANISM`

Do not rerun with more Fermat planes, larger support unions, or a weakened fair coniveau/local-cohomology baseline.

## Deterministic evidence

The published checker is a deterministic packaging wrapper around the frozen checker payload and recomputes the full finite source.

Final checker result: `83528/83528 PASS`.

`CI_NOT_REQUIRED_FOR_RESEARCH`
