# Enterprise Math Research Tool Surface

Status: `ACTIVE / STABLE ROUTER / HOT-PATH V6`
Purpose: current tool/protocol entrypoint without remote-preflight tax, historical preload or agenda preload.

## Liveness

`RESEARCH_HOT_PATH > REMOTE_PREFLIGHT`.

For a selected TASK:

`MAX_ROUTINE_SOURCE_READS_BEFORE_SUBSTANTIVE_WORK = 3`.

## GitHub route

In ChatGPT/Project execution:

`CONNECTED_GITHUB_PLUGIN = PRIMARY_REMOTE_GITHUB_PATH`.

Use the connected GitHub capability for remote files/search/commits/branches/PRs/issues and allowed workflow/status operations.

Do not use chat/container networking as a fallback remote GitHub transport when the connector supports the operation.

A pre-existing local checkout is for local execution/tests, not remote synchronization.

## Research modes

`FREE_AXIOM_DISCOVERY` uses the primitive discovery substrate before candidate freeze.

`TASK_RESEARCH` uses exact-task-first execution.

This tool surface does not choose the research question.

## TASK hot start

1. `AGENTS.md` if needed;
2. exact task entry;
3. first exact dependency required to begin;
4. work;
5. triggered expansion only when needed.

Common Surface is a lookup, not a default context dump.

## FREE hot start

Phase A begins from:

1. FREE role mechanics when needed;
2. `definitions/00_FREE_AXIOM_DISCOVERY_SUBSTRATE.md`;
3. only exact primitive definitions actually required.

Do not preload the general current-result router, project-control context, or a suggested question/discovery-lens menu before candidate freeze.

After freeze, current-result/project context may be opened for Phase-B audit. After candidate maturity, FREE may publish a derived task into Scheduler V2 without becoming an automatic scheduler worker.

## Scheduler V2 trigger

When the active function is task publication, dispatch review, claim, progress, handoff, return, orphan registration or recovery, use:

- `research_scheduler_v2.json`;
- `docs/RESEARCH_SCHEDULING_PROTOCOL.en.md`;
- `docs/RESEARCH_SCHEDULER_V2_QUICKSTART.md`;
- `tools/research_scheduler.py`.

Freeze:

`PUBLISH != READY`.

`RETURN != DONE`.

`V2_LEASE_EXPIRY -> ORPHANED`.

An unregistered legacy taskbook is automatically surfaced as a non-dispatchable orphan migration record; it is not silently executed.

## Triggered reads

Load only when material:

- Common Surface — cross-owner theorem/tool/conflict lookup;
- `docs/GITHUB_INTERACTION_BUDGET.md` — expanded remote/PR/Issue/CI operations;
- identity state machine — unresolved identity/mode mechanics;
- foundational/native-semantic policy — when such claims are being frozen;
- geometry/refoundation policy — geometry/refoundation work;
- exact current definitions — when their objects are used;
- Scheduler V2 / Relay / Foundation / owner-isolation / test / Lean diagnostics — only when that function is active.

## Current mathematical authority

TASK/Driver/FREE Phase B:

`definitions/00_CURRENT_NATIVE_FOUNDATION.md`.

FREE Phase A:

`definitions/00_FREE_AXIOM_DISCOVERY_SUBSTRATE.md`.

Exact current canonical definitions control over router summaries.

## Taskbooks

A supplied current taskbook is the normal second TASK read. Use its frozen source/ref/owner lane and current origin/lineage/successor contract.

Taskbook policy PASS permits scheduler publication only. Runtime READY is a Scheduler V2 Driver-review state.

## Computation / tests / Lean

Use diagnostics only when actual formal/test/tool work requires them.

Do not poll CI.

Tool availability is evidence/mechanics support, not a FREE research-topic selector.

## Read stability

Reuse immutable content already fetched at a known SHA/ref during one execution phase.

Do not reread unchanged routers/PR metadata for reassurance.

## History rule

This router contains current behavior only. Historical task/governance compatibility belongs to Git history or explicit historical records, not the hot path.
