# 先行研究——R004 A3 p-adic exterior target bridge

状态：`RESEARCH PRIOR-ART MAP / NOVELTY_UNVERIFIED`

补充 21 是 compiler bridge，不主张 exterior algebra、Smith normal form、finite-module duality 或 p-adic projective ultrametric 为新发明。

## 1. Exterior algebra 与 alternating maps 属于先行数学

Mathlib 已形式化 module exterior algebra、universal property、exterior powers 与 alternating-map lifts [SRC-R004-A3PET-MATHLIB-EXTERIOR]。R004 这里只使用最基础的 `c -> m wedge c` 以及其 determinant/minor coordinates。

## 2. Smith normal form 与 p-adic module profile 属于先行数学

补充 20 已登记 Smith normal form、local ring 与 matroid-over-ring 先行来源。新的“`gcd(m)` 重复 `d-1` 次”Smith profile 只是 unimodular basis change 加 exterior functoriality 的 elementary specialization，不宣称一般 Smith/exterior theorem 为新结果。

## 3. p-adic/projective ultrametricity 属于先行数学

由 valuation 诱导的 nested congruence/projective agreement 及 non-Archimedean ultrametric behavior 属于标准 p-adic geometry。R004 不主张 p-adic ultrametric 的存在为新发现。当前 project-local result 是：**两个 primitive A3 exterior fields 之间的 compiler missing-target exponent mass** 恰好等于 wedge-minor projective depth defect。

## 4. 当前 project-local addition under test

补充 21 仅保留以下 Enterprise Math package：

1. 把 A3 weighted relation field 识别成 Structural Target compiler 的直接实例；
2. 给出其完整 Smith specialization：所有非零 invariant factors 都是 `gcd(m)`，重复 `d-1` 次；
3. 把 guard-score visibility 的 Structural Target defect 精确识别成 A3 既有 guard-image module `W(ker A)` 的 finite dual，包括完整 p-group exponent-profile equality；
4. 把 primitive A3 exterior fields 之间的 target-repair mass 精确识别成 integer projective wedge-valuation ultrametric distance；
5. 保留 semantic boundary：hidden-guard branch-effect exactness 仍属于 A3 piecewise semantics，不能由 linear defect module 单独决定。

这套 compiler/A3 packaging 的历史 novelty 仍为 `NOVELTY_UNVERIFIED`。
