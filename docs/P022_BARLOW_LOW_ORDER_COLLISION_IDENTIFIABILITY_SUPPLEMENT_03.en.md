# P022 — Consolidated Low-Order Collision Identifiability Certificate Through Segment Length 150

Status: `ACTIVE RESEARCH NOTE / EXACT FINITE CERTIFICATE / GLOBAL EXTENSION OPEN`  
Owner: `program/p022-geometry-v2`  
Parent chain: low-order collision identifiability base theorem and finite certificate supplements

## 1. Current certified frontier

The original exact certificate covered observed segment lengths through `50`.  Incremental p-adic row-space extension has now pushed the same theorem to

\[
\boxed{1\le\ell\le150.}
\]

The hidden tail remains unrestricted.

This is not a bounded schedule enumeration.  The theorem covers arbitrary nonnegative multiplicities of every certified segment generator.

---

## 2. P022-LI08 — 151-dimensional exact determinant certificate

For each observed segment length `ell`, use the two moment generators

\[
A_\ell=\binom{2\ell}{\ell},
\qquad
F_\ell=\sum_j\binom{\ell}{j}^3.
\]

Adjoin one hidden-tail generator contributing `(4,8)` to `(M_2,M_3)` per invisible step.

The current certificate chooses `151` p-adic valuation rows and forms

\[
V_{150}\in M_{151}(\mathbb Z),
\]

with columns

\[
(t_1,\ldots,t_{150},u).
\]

Every row is recomputed directly from the binomial/Franel definitions; no external factor table is part of the certificate.

Modulo the prime

\[
q=1000003,
\]

exact Gaussian elimination gives

\[
\boxed{
\det V_{150}
\equiv973381
\pmod{1000003}.
}
\]

Therefore

\[
\boxed{
\det V_{150}\ne0
}
\]

as an integer.

---

## 3. P022-LI09 — `J_1,J_2,J_3` identify the entire certified checkpoint geometry

If two Barlow selected-layer geometries have all observed segment lengths at most `150` and have equal `(M_2,M_3)`, their multiplicity/tail difference vector lies in the nullspace of `V_150`.  Since the determinant is nonzero, that difference vector vanishes.

Hence

\[
\boxed{
(M_2,M_3)
\Longrightarrow
(t_1,\ldots,t_{150},u).
}
\]

Using the exact P011 transforms

\[
M_2=2J_2+J_1,
\]

\[
M_3=6J_3+3M_2-2J_1,
\]

we obtain

\[
\boxed{
(J_1,J_2,J_3)
\Longrightarrow
(t_1,\ldots,t_{150},u)
}
\]

throughout the certified class.

Thus the first three collision coefficients already carry the same unordered checkpoint-geometry information as the complete collision polynomial on this large finite generator family.

---

## 4. How the certificate was extended without full factorization

The important methodological change is that a new segment length does **not** require full factorization of its Franel number.

Suppose the existing certificate through length `L-1` has full row rank.  After appending the new length-`L` column, extend every existing valuation row and compute the resulting rank-`L` row space inside `L+1` columns (including tail).

Then search for any new valuation functional

\[
v_p(A_\ell)
\quad\text{or}\quad
v_p(F_\ell)
\]

whose row is outside that span.

If one exists, append it and regain full rank.

The new prime need not divide the newest generator.  For example, length `67` was extended by the existing-style row

\[
v_{337}(F_\ell),
\]

even though the role of that row is global linear separation rather than a primitive-prime certificate for `F_67` itself.

At length `125`, the initial candidate pool below `50,000` stopped increasing rank, but the row

\[
v_{231947}(F_\ell)
\]

restored full rank.  The apparent boundary was therefore a row-search cutoff, not a multiplicative dependence.

The same incremental method then continued through `150`.

---

## 5. Why this matters for the global conjecture

The finite evidence is now much stronger than “no alias found in schedules up to horizon N”.

Full rank of `V_150` means there is no nonzero integer vector

\[
(c_1,\ldots,c_{150},c_u)
\]

such that simultaneously

\[
4^{c_u}\prod_{\ell=1}^{150}A_\ell^{c_\ell}=1
\]

and

\[
8^{c_u}\prod_{\ell=1}^{150}F_\ell^{c_\ell}=1.
\]

The exponents are unrestricted integers.  So the theorem rules out every possible multiplicative alias generated by the first 150 segment types, no matter how large the total horizon or checkpoint count becomes.

This strongly shifts the global research question toward a structural number-theory problem:

\[
\boxed{
\text{Are the joint generators }(A_\ell,F_\ell)_{\ell\ge1}
\text{ multiplicatively independent after adjoining }(4,8)?
}
\]

A global proof would imply that `(J_1,J_2,J_3)` always identify Barlow checkpoint geometry.  A single exact multiplicative relation would give the first global counterexample.

---

## 6. Prior-art search status

A targeted search found established Franel-number literature on congruences and p-adic properties, but no directly matching theorem establishing the required global primitive-divisor or joint multiplicative-independence statement for the pair

\[
\left(\binom{2\ell}{\ell},\ \sum_j\binom{\ell}{j}^3\right).
\]

Absence from that search is not evidence of novelty.  The global prior-art status therefore remains

`NOVELTY_UNVERIFIED / STRUCTURAL THEOREM NOT FOUND IN CURRENT SEARCH`.

The finite determinant theorem itself is internally certified regardless of that historical-priority question.

---

## 7. Current stopping rule for certificate growth

The certificate is deliberately stopped at `150` for this research pass.

The reason is not rank failure.  Rank continued to grow through every tested segment length.

Further mechanical extension would add confidence but diminishing mathematical insight.  The next high-value step is to explain the persistent rank growth using structural divisibility, primitive factors, recurrences, or another invariant, rather than increasing the finite cutoff indefinitely.

So the current state is

\[
\boxed{
\ell\le150:\text{exactly certified},
\qquad
\ell>150:\text{global theorem/counterexample open}.
}

---

## 8. Executable certificate

Added:

- `src/enterprise_math/p022_barlow_low_order_identifiability_150.py`;
- `tests/test_p022_barlow_low_order_identifiability_150.py`.

The module stores only the selected valuation-row labels and expected modular determinant residue.  Every matrix entry is regenerated from exact binomial and Franel formulas and the determinant is recomputed by modular elimination.
