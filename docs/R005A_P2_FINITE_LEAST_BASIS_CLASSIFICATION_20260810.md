# R005-A — Finite Classification of p=2 Least-Basis Failures

Status: `PROVED R005 CLASSIFICATION UNDER DECLARED EXTERNAL GAP TAIL / EXACT LOW-MID COMPUTATION / NOT CANONICAL / LEAN PENDING`  
Date: `2026-08-10`  
Task: `RS-R005-PRIME-ALGORITHM-LAB`

## Main result

For square basins `k^2<n<(k+1)^2`, using exact dangerous-gap enumeration below `387096200`, reciprocal non-forced witness strips, exact pair closure, the selected Oliveira e Silva maximal-gap record tail, and Campbell's published finite `1724` gap consequence, the current certificate classifies every integer

`2 <= k <= 11,661,903,789`.

The divisor-witness language has **no least safe basis** exactly for the 49 values

`25, 47, 62, 123, 130, 151, 157, 162, 196, 217, 308, 364, 365, 479, 556, 888, 924, 935, 1008, 1056, 1078, 1162, 1290, 1345, 1454, 1511, 1541, 1577, 1612, 1627, 1679, 1781, 1790, 1865, 1897, 2073, 2164, 2850, 4412, 5833, 5834, 6339, 7289, 8584, 9369, 11226, 11433, 13006, 35901`.

Hence, under the declared external gap tail,

`35902 <= k <= 11,661,903,789`

has a unique least safe divisor-witness basis.

The upper endpoint is a certificate boundary; no failure is asserted at the next k.

## T-A45 — reciprocal prime-gap strip

Let `A=k^2`, `U=k^2+2k`, q prime, and `p<r` consecutive cofactor primes. The strict next prime after `A/q` is r and the e=1 cofactor certificate fails exactly when

`q*p <= k^2` and `(k+1)^2 <= q*r`.

Thus the exact integer strip is

`ceil(sqrt(q*p)) <= k <= floor(sqrt(q*r))-1`.

Pure-cube forcing removes the unique basin containing `q^3`, so the actual non-forced strip starts after `floor(sqrt(q^3))`.

This turns the non-forced witness field into an event set generated by consecutive cofactor prime gaps instead of a scan over every k/witness pair.

## T-A44 — dangerous-gap elimination

For cube-root witnesses, if a cofactor prime-gap record starts at P with actual gap g and

`g^4*(P+g) <= 16*P^2`,

then every witness whose cofactor point lies in that region is forced. Therefore only gaps violating this inequality need exact enumeration below the chosen cutoff.

## Exact dangerous-gap sieve

`experiments/r005a_p2_danger_gap_sieve.cpp` performs a segmented Eratosthenes scan through `387096200`.

Exact run:

- primes enumerated: `20,684,332`;
- maximum actual gap: `248`;
- quartic-dangerous gaps: `1,654`.

## Exact low classification: 2..35901

Projecting those dangerous gaps through T-A45 yields:

- `3783` non-forced strip intervals;
- `3577` candidate k values;
- at most `3` non-forced cube-root witnesses at one candidate k.

Exactly one candidate basin has a genuine non-forced witness inside the fourth-root core: `k=121,q=11`. That basin is checked directly and has empty residual fiber.

All other candidate basins have a fully forced fourth-root core, so pair closure is complete. It produces exactly `49` no-least basins and `50` residual blocks. This upgrades the earlier 49-basin collection from a non-exhaustive certificate family to an exhaustive classification over `2<=k<=35901` under the declared gap-tail inputs above the exact cofactor cutoff.

## Exact middle bridge: 35902..1906499

The same strip construction reduces this interval to:

- `2932` non-forced strips;
- `2894` candidate k values;
- at most `2` non-forced cube-root witnesses at one k.

Exact pair closure on those 2894 event points gives `0` residual basins.

## Large tail

Below cofactor cutoff `P0=387096133`, the maximum actual gap is 248. The exact first k satisfying

`248*floor((k^2+2k)^(1/3)) <= 2k`

is `1,906,500`.

Every maximal-gap record from `P0` onward in the selected Oliveira double-check region satisfies T-A44. This forces the cube-root core through `k=894,427,190`.

Campbell's published operational consequence that every real `x<6.8e19` has a prime in `(x,x+1724]` forces the cube-root core from `k=640,503,066` through `k=11,661,903,789`. The intervals overlap.

By T-A20 the large tail has no residual composite.

## Repair complexity classification

The 49 failure basins all have transversal number one; every other certified basin has empty residual hypergraph. Therefore, throughout the whole certified range,

`tau(R_k) <= 1`.

More exactly, `tau=1` exactly at the 49 listed k values and `tau=0` elsewhere. Thus the first possible repair-complexity jump `tau>=2` is pushed beyond `11,661,903,789` under the declared finite-gap evidence.

## Ownership / prior-art boundary

External/prior inputs include ordinary Eratosthenes, maximal prime-gap computations/record tables, and Campbell's finite gap consequence. R005 does not claim a new prime-gap theorem.

The R005-specific content is the forced/non-forced witness semantics, T-A20 cube-root least-basis criterion, T-A44 transfer inequality, T-A45 reciprocal strip representation, pair-closure decision on the sparse event set, and the repair-language classification.

Classification: `R005 PRIME-SPECIFIC EXACT/FINITE SPECIALIZATION / NOT CANONICAL`. This does not reopen generic Foundation ownership. Lean remains `LOCAL_LEAN_PENDING`.