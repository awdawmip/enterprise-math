# P017 — P2 Chen Carry Bridge Checkpoint

Status: `PROVED_WIP + EXECUTABLE_CHECKED + PRIOR_ART_ALIGNED / NOT CANONICAL / NO ALL-K P2 CLAIM`

Captured: `2026-08-25T10:31+08:00`

Owner branch: `research/p017-p2-chen-carry-bridge-20260823`

Parent artifact: `docs/P017_P2_CHEN_CARRY_BRIDGE_20260823.md`

## 1. Corrected route

The earlier exploratory constant high-prime correction `lambda/2=0.415` is not a complete pointwise P2 detector and must not be used as such. The low-low-large three-factor geometry can leave positive weight.

The correct high-end weight has root-normalized linear form

`rho(p) = 1 - log(p)/log(sqrt(X))`,

up to the normalization of the surrounding Chen/Laborde weight. This is independently forced by the three-factor product geometry and matches the classical Chen/Laborde/Matomaki high-prime penalty shape.

## 2. Exact P017-to-Chen remainder transfer

For the square basin `I_K={K^2+1,...,K^2+2K}`, define

`H_m(K)=#{n in I_K : m|n}`

and

`O_m(K)=H_m(K)-H_{2m}(K)`.

With

`r_K(m)=H_m(K)-2K/m`,

one has the exact identity

`O_m(K)-K/m = r_K(m)-r_K(2m)`.

Therefore the binary-carry remainder required by the P2 sieve is exactly the odd-parity projection of the classical short-interval floor remainder. It is not a new independent distribution family.

## 3. Square-root barrier diagnosis

If one keeps Campbell's original explicit per-prime sieve depth `D_p ~ X^0.44/p`, then `pd<X^0.44<X^1/2`. The resulting remainder is absolutely summable, but this regime cannot cross the P2 parity barrier.

A genuine P2 route must deliberately use a shallow super-root total level. Continuous-weight diagnostics put the positivity threshold near

`D = X^(0.54524...)`,

or `D ~ K^(1.09048...)` with `K=sqrt(X)`.

Matomaki's known choice `D=X^(5/9)` is therefore structurally aligned with the independently derived threshold.

## 4. Main-term margin diagnostic at theta=1/2

Re-evaluating the published Iwaniec-Laborde weight model at square-interval length exponent

`theta=1/2`

and total level

`D=X^(5/9)`

with the corresponding optimization gives the numerical diagnostic

`c ~ 5.61945`, `b ~ 4.18055`, `G ~ 0.12805`.

Using conservative rounding of the published constants still leaves approximately

`G > 0.12798`.

For comparison, the original `theta=0.45` parameter package has published margin about `0.00177`. Thus the square interval has roughly seventy times more main-term room under the same normalized model.

This `G` value is a route diagnostic, not yet a theorem statement: it remains to be checked line-by-line against the exact normalization and every hypothesis of the Iwaniec-Laborde bilinear lemma.

## 5. Prior-art boundary

- Chen 1975 and Iwaniec-Laborde 1981 already show sufficiently large short intervals can contain P2 values; Iwaniec-Laborde reaches intervals shorter than the square interval scale. Therefore an asymptotic sufficiently-large consecutive-square P2 statement is prior art and is not a novelty target here.
- The live target is explicit/effective overlap with Campbell-style finite verification and possible simplification from the special square endpoint/root-cutoff coupling.
- Repackaging the carry as Fourier/Ramanujan/roughness discrepancy alone does not break parity; P017's possible extra leverage must use the low-height coupled diagonal generated by the same `K`.

## 6. Exact finite regression status

The owner branch contains bounded exact regression checks for:

1. the corrected P2 root detector;
2. odd-radius/binary carry identities;
3. the Chen remainder transfer `O_m-K/m = r_K(m)-r_K(2m)`;
4. L054 cofactor-window separation / semiprime counting equivalences.

These checks are error detectors only. The proof status of the exact identities comes from their algebraic derivations.

## 7. Current frontier

The next load-bearing task is now precise:

1. recover the exact Iwaniec-Laborde bilinear parameter inequalities;
2. verify them at `theta=1/2`, `D=X^(5/9)` and the optimized square-interval weight parameters;
3. retain the P017 transfer at the remainder layer;
4. turn every `O`, `o(1)`, truncation and exceptional contribution into an explicit error budget;
5. solve the resulting numerical threshold and compare it with Campbell's finite/computational coverage.

No all-K P2 theorem is claimed at this checkpoint.
