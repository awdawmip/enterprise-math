# RSA-270：第 7 轮——代价场 minimax 定理与强制格 j-筛

Progress-Event-ID: `rsa270-round7-minimax-j-sieve-9d4c2e`
At: `2026-09-06T20:20+08:00`
Scope: `enterprise-math / RSA-270 / cost-field consequences (minimax + modular j-sieve)`
Source: `ChatGPT TASK_RESEARCH conversation; exact integer Python; journal 7e3b5d; P000 assumed`
Kind: `PROGRESS / EXACT THEOREMS / QUANTIFIED NO-GO`
Researcher-ID: `EM-DIRECT-5D08CB`

## Event

Round 7: consequences of the Direction-1 cost field (7e3b5d). No factor obtained.

### A. Minimax theorem (cost-field coverage)

Over the construction-family band `r = q/p in [1.765, 2.266]` and directions `(a,b) <= 21` coprime:

- **`(2,1)` is the global minimax single direction** (max band coefficient `c = 0.0083`, at r=2.266);
- multi-direction coverage cannot beat it: any cover set's work = max of members' band-max `c`, and `(2,1)`'s band-max is the smallest possible;
- worst-case add-steps `~ 2^447.5 * 0.0083 ~ 2^440.6`.

### B. Modular j-sieve (forced lattice action on the cost field)

For branch direction `(a,b)`, the endpoint `x* = a*p + b*q` is constrained by the forced lattice `S = 16 mod 72`; per direction the admissible endpoint residues mod 72 are:

- `(1,1)`: `{16}` -> j-sieve keeps **1/72** of steps (full 6.17-bit sieve, consistent with `A = 8 mod 36`);
- `(11,5)`: `{2,38}` -> keeps 2/72;
- `(2,1)`: 12 residues -> keeps 12/72.

The direction lattice itself is **not** sieved: the ratio-shadow congruence `q*p^-1 mod 72 in {7,31,55}` only constrains exact ridge matches (`q/p = a/b` exactly, impossible for odd primes), so every band direction stays viable and `(2,1)` remains minimax. The sieved scan budget is `~ 2^440.6 / 72 ~ 2^434.4` add-steps — **still infeasible**; this is the tightest quantified form of the no-go for the multiplier-branch route.

## Artifacts

- Script: `rsa270_round7.py` (conversation-local).
- Prior frontier: `journal/enterprise-math/2026-09-06/20260906T194000+0800-rsa270-cost-field-geometrization-7e3b5d.md`.

## Next

The multiplier-branch route is now quantified to its optimum (`(2,1)`, mod-72 sieved, ~2^434.4 adds) and closed. Remaining candidate families all reduce to the S-scalar per the dichotomy (8e5f2a). Next rounds need either a new observable class or external input; without one, the blocking condition persists.
