# Canonical Problem Status Index

Status: `CANONICAL`  
Effective: 2026-08-09

This file is the authoritative status ledger for numbered Enterprise Math research problems. `OPEN_PROBLEMS.en.md` preserves the original problem statements and may contain historical wording from the moment a problem was posed; when status wording differs, this index controls.

`RESOLVED` means that the specific numbered problem has a canonical main-branch answer at its stated scope. It does **not** mean the surrounding research area is closed.

| Problem | Status | Canonical scope/result |
|---|---|---|
| P001 | `RESOLVED` | `docs/P001_ROOT_MULTIPLICATIVITY.en.md`: integer roots are supermultiplicative, and multiplicativity holds exactly when the basin-product carry load stays below the next perfect-power threshold; the note also gives the exact carry count, downward-closed no-carry region, and a floor-division boundary. |
| P002 | `RESOLVED` | `docs/P002_COLLAPSE_GAP_BOUND.en.md`: the collapse gap has the sharp basin bound `0 <= G_p(n) <= (k+1)^p-k^p-1`, with equality exactly at the last basin state; the gap coordinate is a bijection across each basin. |
| P003 | `RESOLVED` | `docs/P003_COLLAPSE_COMMUTATION.en.md`: perfect-power collapses commute globally iff their positive exponents are comparable under divisibility. If `p|q`, both compositions equal `C_q`; incomparable exponents have the explicit prime-power witness `2^max(p,q)`. The comparable direction is Lean-checked in `EnterpriseMath/Arithmetic/CollapseCommutation.lean`; the converse remains an ordinary proved theorem with executable witness regression. |
| P004 | `RESOLVED` | `docs/P004_COLLAPSE_FIXED_POINTS.en.md`: for any finite word of positive-exponent collapse maps, the fixed-point set is exactly the perfect `L`-th powers where `L` is the lcm of the exponents (with the empty word using `L=1`). Word order and repetition may change transient action but not the fixed set; exhaustive bounded word regressions audit the classification. |
| P005 | `RESOLVED` | `docs/P005_SCALE_LATTICE_CORE.en.md`: total positive scale factor, divisibility projections, gcd/lcm scale lattice, path independence, and nonunique inverse refinement. |
| P006 | `RESOLVED` | `docs/P006_SIGNED_STATE_EXTENSION.en.md`: ordinary-order odd roots and signed-magnitude quantization are explicitly separated; even powers have no ordinary-order right adjoint on all signed states. |
| P007 | `RESOLVED` | `docs/P007_DISCRETE_DIVISION.en.md`: quotient, same-space multiple collapse, and reversible quotient/remainder state are distinct exact discrete semantics. |
| P008 | `RESOLVED` for the current v0.1 root/quotient/collapse family | `docs/P008_MINIMAL_ORDER_CORE.en.md` plus the Lean core: partial-order equality semantics, greatest-sublevel/right-adjoint existence, order-embedding exact recovery, and reductive idempotent collapse. Richer future operations may justify richer structure separately. |
| P009 | `RESOLVED` for the minimal typed collapse+coarsening system | `docs/P009_TYPED_SCALE_CORE.en.md`: strict trajectories terminate, no nontrivial cycles exist, pure projection is target-confluent, and mixed collapse/projection is generically nonconfluent. Noncanonical inverse lifts define a different system. |
| P010 | `RESOLVED` | `docs/P010_STRICT_HISTORY_MERGE.en.md`: exact reachable-collision criterion and exact multiplicity increment. |
| P011 | `RESOLVED` for finite deterministic maps | `docs/P011_INTEGER_IRREVERSIBILITY_SPECTRUM.en.md` and supplements: superadditive integer observables, complete collision spectrum, collision polynomial, and derived entropy comparisons. |
| P012 | `RESOLVED` at the metric-foundation scope | `docs/P012_INTRINSIC_DISCRETE_GEOMETRY.en.md`: primitive-step graph distance gives an exact integer metric; the supplement explicitly separates metric structure from inner-product/Pythagorean structure. Broader discrete geometry remains open research. |
| P013 | `RESOLVED` | T001 and T005 are Lean-checked in `EnterpriseMath/Arithmetic/IntegerRoot.lean`; the warning-fatal pinned Lean build passes in CI. |
| P014 | `RESOLVED` | T010 is Lean-checked in `EnterpriseMath/Scale/Compatibility.lean`, with `EnterpriseMath.lean` importing the module so CI actually compiles it. |
| P015 | `OPEN / CONTINUOUS` | Prior-art mapping remains an ongoing obligation as new components are added. |
| P016 | `RESOLVED` at the protocol level | `docs/P016_PHYSICAL_FALSIFICATION_CONTRACT.en.md` and `falsification.schema.json` define F1–F9 quantitative kill-test requirements. This does not validate or falsify the physical hypothesis itself. |
| P017 | `OPEN / ACTIVE RESEARCH` | The Legendre pressure test has many canonical structural lemmas but no proof of Legendre's conjecture; continue pressure-testing and counterexample-first work. |
| P018 | `OPEN / ACTIVE RESEARCH` | The finite-precision proof calculus is an expanding foundational program. PR #249 made the P018↔P023 bounded quotient-root future-action basis canonical and Lean-checked: `O_a(q)=R_r(floor(q/a))` separates all exact states on `0,...,N` exactly when the positive action set contains every positive `r`-power-free `b<=N`, yielding the unique least separating action set under inclusion. PR #270 added the canonical executable centered-prime-radius re-expression of the already-established Stage-8 near-diagonal factor-proof slack under explicit left-prime/size hypotheses; it does not assert symmetric prime pairs for every center or prove Goldbach-type statements. See the shared surface for exact assets and prior-art boundaries. The numbered program remains active. |
| P019 | `RESOLVED` | `docs/P019_COLLAPSE_WORD_STABILIZATION.en.md`: a monotone reductive self-map on a well-founded poset stabilizes at the greatest fixed point below the initial state; consequently every fixed collapse word stabilizes exactly at `C_L(n0)`, its eventual basins are the ordinary `L`-basins, and the collapse-word semigroup modulo stable equivalence is the lcm join-semilattice. |
| P020 | `RESOLVED` | `docs/P020_WELL_FOUNDED_STABILIZATION.en.md` plus `EnterpriseMath/Order/WellFoundedStabilization.lean`: Lean proves that finite ordinary iteration of any monotone reductive endomap on a `WellFoundedLT` partial order reaches the greatest original fixed point below the initial state; the selected stabilization map is monotone, reductive, idempotent and has exactly the original fixed points. |
| P021 | `OPEN / ACTIVE RESEARCH` | The finite causal-boundary executable core is canonical on `main` in `src/enterprise_math/causal_boundary.py` with regression coverage in `tests/test_causal_boundary.py`. It provides the program-specific finite undirected-graph plus integer-expansion boundary layer while consuming P018 observation/refinement machinery. Broader causal focusing, direction/witness composition, and physical interpretation remain active research. |
| P022 | `OPEN / ACTIVE RESEARCH` | Canonical executable geometry includes `lattice_geometry.py`; PR #262's exact `A_p`/simple-cubic geodesic multiplicity and integer-coordinate ABAB HCP contact-graph core; PR #288's periodic Barlow stacking core with exact graph distance/geodesic multiplicity, FCC/HCP reconstruction, and root-to-target-layer cumulative interface-sign-count compression; PR #292's task-relative Barlow precision layer, where `(abs(k), delta_k)` exactly recovers the effective interface counts and selected-layer vertical witness polynomial while the full prefix-imbalance trajectory remains strictly richer; and PR #296's periodic-growth layer, which gives the exact drift-class growth equation `(lambda-2)^(2L)=2^(L+|D|)` plus a universal eventual C-finite recurrence class. Equal `(L,|D|)` does not determine the finite shell-total sequence or phase. Coordination-observable and observation-history theories remain outside this promotion; broader geometry and cross-owner interfaces remain active research. |
| P023 | `OPEN / ACTIVE RESEARCH` | `docs/P023_COMPOSITION_SAFE_COLLAPSE.en.md` and supplements: a coarse quotient is legal for a required future computation only when the relevant observables/operations descend to the quotient. Canonical scope includes fiber-constancy factorization, coarsest repair, finite total operation-family closure, exact quotient/multiple-collapse compatibility, one-bit minimal repair, reductive gap-to-borrow transport, borrow telescoping, stable safe-selector semigroup equivalence, PR #249's Lean-checked bounded quotient-root action-basis specialization, and FQ-006's legality-sensitive extension to finite deterministic partial operation families. For partial maps `F_a:D_a->X`, a safe quotient must preserve enabledness/domain membership and target quotient classes; the finite refinement is the coarsest compatible refinement, agrees with legality-sensitive bounded-word signatures, and reduces exactly to the total-family construction when every `D_a=X`. An absorbing `UNDEFINED` sink is only a verification representation when it is distinguished and absorbing, not a world-state assumption. See `docs/P023_PARTIAL_OPERATION_QUOTIENT_SUPPLEMENT_08.en.md`, `src/enterprise_math/partial_operation_quotient.py`, and its regression suite. This FQ-006 extension is `EXECUTABLE_CHECKED` but not Lean-checked. Generic partition refinement, automata distinguishability, partial-transition machinery, Test Cover and power-free arithmetic remain prior art; broader safe-precision structure remains active research. |
| P024 | `OPEN / ACTIVE RESEARCH` | `docs/P024_ACTION_LANGUAGE_PRECISION.en.md`: specializes P023 to integer translation languages and ordered threshold observations. Current proved scope identifies precision cells with reachable boundary orbits `B-M`; gives exact one-sided numerical-semigroup class counts, gcd over-refinement defect as the number of relevant semigroup holes, conductor-localized irregular boundary layers, genuine two-sided group completion to `g Z`, and automatic subgroup completion after finite cyclic periodicization. Broader state-dependent and higher-dimensional action-language precision remains open. |

## Status discipline

A Draft PR, branch theorem, finite computation, or unmerged formalization never changes a numbered problem to `RESOLVED` here. Promotion requires a main-branch result at the stated scope and all applicable final gates.

If a resolved problem later receives a stronger extension, add a new problem or explicitly widen the scope; do not silently reinterpret the old `RESOLVED` label as a universal closure claim.
