# P022 — Observation/History Theorem Lineage

Status: `ACTIVE RESEARCH LINEAGE / WIP`  
Owner: `program/p022-geometry-v2`

## 1. Geometry-to-observation chain

`Barlow stacking signs -> prefix imbalance -> layer geometry -> coordination/geodesic observables`.

## 2. History branch

`coordination shell history -> two-shell local hidden-state decoder -> global multiplicity reconstruction -> terminal stratification equivalence`.

## 3. Collision branch

`checkpoint segments -> binomial fiber profiles -> multiplicative convolution -> power moments -> P011 collision spectrum -> collision-polynomial inversion back to unordered checkpoint geometry`.

## 4. Repair branch

`absolute history -> excursion orientation bits -> unordered two-channel history -> diagonal side-split bits -> repair polynomial -> P011 fiber/collision profile`.

## 5. Optimization branch

`checkpoint geometry -> image/J2 balanced optimum` and independently `checkpoint geometry -> maximum-fiber odd-balanced optimum`; the disagreement is an exact task-relative precision boundary.

## 6. Current non-equivalences

- single terminal shell cardinality does not determine current hidden pair uniformly;
- `J_2` does not identify checkpoint geometry;
- complete collision data does not identify segment order;
- coordination history/global multiplicity do not identify coordinate-labelled orientation geometry;
- two-channel history reconstruction does not generalize automatically to three channels.
