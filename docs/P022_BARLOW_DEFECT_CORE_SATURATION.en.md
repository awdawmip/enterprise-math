# P022 — The Segment-150 Franel Defect Core Is Unimodularly Saturated by Two Additional Valuation Rows

Status: `ACTIVE RESEARCH NOTE / EXACT INTEGER SATURATION CERTIFICATE / PRIOR-ART SENSITIVE`  
Owner: `program/p022-geometry-v2`  
Depends on: 40-dimensional defect core; Smith obstruction vector  
Cross-route relevance: P011 low-order collision identifiability; finite valuation-certificate design

## 1. The Smith index `26622` is not intrinsic to the bounded defect family

The current selected 40 valuation rows give the exact Smith form

\[
\operatorname{diag}(1^{39},26622).
\]

At first sight this leaves one cyclic integer cokernel of order

\[
26622=2\cdot3^3\cdot17\cdot29.
\]

That cokernel belongs to the **selected square row set**.  It need not be the saturation index of the 40 defect columns inside the full prime-valuation lattice.

The primitive obstruction vector makes this distinction testable without constructing new large determinants.

---

## 2. Primitive obstruction and row replacement

Let

\[
M\in\mathbb Z^{40\times40}
\]

be the current Franel defect core, and let

\[
x\in\mathbb Z^{40}
\]

be the primitive vector satisfying

\[
\boxed{
Mx=-26622\,e_{269}.}
\]

Here `e_269` denotes the coordinate of the selected valuation row `v_269`.

Because the Smith form has only one nontrivial invariant, `x` is a primitive adjugate direction for that row.

If the `v_269` row is replaced by any integer row vector `r`, determinant multilinearity gives

\[
\boxed{
\det M[r]=r\cdot x.}
\]

So finding a better exact certificate becomes a one-dimensional pairing problem.

---

## 3. P022-SA01 — two genuine new prime valuations have coprime pairings

Evaluate the 40 core defects at two previously unselected prime valuations:

\[
q_1=73589,
\qquad
q_2=176459.
\]

The exact obstruction pairings are

\[
\boxed{
v_{73589}\cdot x=-13311}
\]

and

\[
\boxed{
v_{176459}\cdot x=2518.}
\]

Factorization gives

\[
13311=3^3\cdot17\cdot29,
\]

while

\[
2518=2\cdot1259.
\]

Hence

\[
\boxed{
\gcd(13311,2518)=1.}
\]

Therefore the 40 columns are already primitive in the enlarged row lattice generated by the historical core rows together with these two genuine prime-valuation observations.

---

## 4. P022-SA02 — explicit Bézout valuation functional

The exact Bézout identity is

\[
oxed{
915(-13311)+4837(2518)=1.
}
\]

Define the integer valuation functional

\[
\boxed{
\lambda
=915\,v_{73589}
+4837\,v_{176459}.
}
\]

Since ordinary prime valuations are additive on positive rationals, `lambda` is an integer additive functional on the multiplicative defect group.

Its pairing with the primitive obstruction is

\[
\boxed{
\lambda\cdot x=1.
}
\]

Replace the historical `v_269` row of the 40-dimensional core by `lambda`.  The exact determinant becomes

\[
\boxed{
\det M_\lambda=1.
}
\]

Thus the residual Franel core has a **unimodular integer certificate**.

No modulus is required.

---

## 5. P022-SA03 — the finite defect lattice is saturated on an explicit 42-row surface

Consider the rectangular valuation surface consisting of:

- the 40 historical core valuation rows;
- `v_73589`;
- `v_176459`.

The two replacement determinants

\[
-13311,
\qquad
2518
\]

are coprime.  Therefore the gcd of the full-rank `40 x 40` minors of this `42 x 40` integer matrix is one.

Equivalently, its Smith invariant factors are all one.

So the 40 defect columns span a primitive/saturated rank-40 sublattice of the explicit 42-coordinate valuation lattice.

The previous cyclic cokernel

\[
\mathbb Z/26622\mathbb Z
\]

is therefore not an intrinsic obstruction of the segment-150 defect family.  It is the index created by the historical choice of only 40 particular rows.

---

## 6. Meaning of the factor split

The old exact determinant decomposes as

\[
26622=2\cdot13311.
\]

The new rows separate the two pieces cleanly:

- `v_73589` sees the odd part `13311=3^3*17*29` up to sign;
- `v_176459` has pairing `2518=2*1259`, which is coprime to the odd part and removes the remaining 2-adic obstruction.

The extra prime `1259` is irrelevant to the gcd because it does not divide `26622`.

This arithmetic separation explains why two rows are enough to saturate the selected finite certificate even though neither row alone gives a unit determinant.

---

## 7. Consequence for the complete `N=150` certificate

The earlier structural reductions were all integral and unimodular at their nontrivial steps:

1. prime-boundary `A` directions have unit valuation pivots;
2. composite/tail relations have triangular leading coefficient one;
3. 49 Franel support pivots are `+/-1`;
4. the remaining 40-dimensional core becomes determinant one after replacing one row by `lambda`.

Therefore the bounded segment-150 identifiability theorem does not fundamentally depend on a nonunit determinant index.

A structurally adapted integer valuation system exists in which the entire remaining defect problem is solved unimodularly.

This is stronger certificate information than mere rational full rank.

It does **not** prove the unbounded defect sequence is multiplicatively independent.

---

## 8. Why this matters for the global problem

The old finite evidence could have suggested that the prime factors

\[
2,3,17,29
\]

represented a deep obstruction that might persist with larger segment cutoff.

SA03 shows that interpretation was too strong: those primes are artifacts of one finite selected row basis.

The global research question therefore remains purely the multiplicative independence of

\[
2,D_n
\]

across all composite odd-boundary indices, not stabilization of the historical Smith factor `26622`.

The better global diagnostic is now:

> at each new composite defect, does the full prime-valuation lattice gain one primitive direction after allowing adaptive valuation rows?

This is a stricter and more invariant question than tracking one fixed square determinant or its Smith factorization.

---

## 9. Prior-art boundary

Bézout identities, determinantal divisors, Smith saturation, additive prime valuations and determinant row replacement are standard integer linear algebra and arithmetic.

No novelty is claimed for those tools.

The P022-specific result is the explicit saturation of the segment-150 Franel defect core using the two valuation primes `73589` and `176459`, together with the exact unit determinant functional `lambda`.

---

## 10. Executable assets

Added:

- `src/enterprise_math/p022_barlow_defect_core_saturation.py`;
- `tests/test_p022_barlow_defect_core_saturation.py`.

The tests recompute the two prime valuation rows from the defect definitions, verify pairings `-13311` and `2518`, check the Bézout identity, construct the derived valuation row, and recompute the resulting `40 x 40` determinant as exactly `+1` by fraction-free elimination.
