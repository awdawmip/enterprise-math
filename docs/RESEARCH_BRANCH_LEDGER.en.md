# Enterprise Math Current Research Branch Ledger

Status: `CANONICAL OWNER-ISOLATION LEDGER / CURRENT WRITE SURFACE`  
Current routing base: `main@85919bbb939ac8155a37c70e088b15b15fab795f`  
Prior audited 11-route snapshot: PR #215 / `main@683d6baaec90f4d59a5c3a64c9d40a6f3a24a337`  
Date: 2026-08-09

This ledger records the current semantic write surface. PR #215 correctly canonicalized an 11-route audited snapshot. After that audit completed, P025 was created as a new independent L2 program owner, so the current surface is now 12 routes. This is a normal Owner-Isolation increment, not evidence that the #215 audit was wrong.

The governing lifecycle remains:

`owner research -> freeze exact payload -> fresh latest-main L4 replay -> exact merge-state gates -> canonical main`.

L1/L2/L3 owners may lag moving main. They do not whole-tree sync main merely for currency. L4 is globally `NO NEW MATHEMATICS`.

## 1. Canonical reusable layers

Current main contains the first canonical cores for A2, A3 and A4; the thin A3->A4 generated-support bridge B01-B06; P021 causal-boundary core; P022 `A_p`/root-lattice geometry core; Owner-Isolation execution rules; scope-aware branch auditing; E001 finite impulse-world integration; and later P018 quotient-channel coalescence.

Important compaction/promotion merges include `87231d29`, `ca923d64`, `e8407882`, `21c1ef66`, `4a4a2fde`, `683d6baa`, and governance merge `85919bbb`.

## 2. Current active write surface: 12 branches

The target range is not a permanent quota. Twelve is currently the upper edge of the intended compact surface and every listed branch has a distinct theorem/application home.

### 2.1 Program owners — 6

1. `program/p017-legendre`
   - square-basin / Legendre pressure-test frontier;
   - active branch-side work remains P017-owned.

2. `program/p018-precision-v2`
   - precision-specific frontier;
   - generic future quotient remains A2/P023-owned.

3. `program/p021-causal-focusing-v3` — Draft PR #213
   - fresh generation after causal-boundary promotion;
   - retains focusing, direction-orbit/causal-role, causal witness and physically bounded application routes.

4. `program/p022-geometry-v2`
   - `A_p` lattice core is canonical;
   - geodesic multiplicity, HCP and Barlow remain same-owner geometry research.

5. `program/p024-action-precision`
   - closed-form action-language / boundary-precision specializations;
   - generic quotient and adjunction remain upstream-owned.

6. `program/p025-abc-support-collapse` — PR #216
   - created after the #215 11-route audit;
   - owns the abc pressure-test program: radical-support collapse, multiplicity residual, exact integer rational-exponent defect, relation-conditioned witness-precision candidates, P025 prior-art, lineage and regressions;
   - radical is explicitly not treated as an addition congruence;
   - generic correspondence / quotient mother theory remains upstream-owned;
   - no abc proof or canonical problem-status closure is claimed.

### 2.2 Cross-owner bridges — 3

1. `bridge/a3-a4-generated-support-v3` — thin A3 relation-state -> A4 generated-support bridge.
2. `bridge/p017-p018-hard-core-v2` — arithmetic / precision hard-core, root-channel and tail-resource bridge.
3. `bridge/a2-e001-material-markov` — future-compatible quotient -> material future-state specialization bridge.

### 2.3 E001 engineering owners — 3

1. `engineering/e001-material-impulse-v2` — finite impulse/momentum, force activation, remainder and reversal-certificate frontier.
2. `engineering/e001-material-pair-impulse` — bounded two-body equal-and-opposite impulse owner, PR #205.
3. `engineering/e001-material-multiaction-protocol` — empirical multi-action protocol -> canonical P023 future partition adapter, PR #185.

## 3. Exhausted or frozen generations

These are provenance, not current write points:

- `core/a2-future-quotient-v2`, `core/a3-relation-state-v2`, `core/a4-admissible-support-v2` — all observed `ahead=0` during the audit; start a new generation only when new owner mathematics actually resumes.
- `program/p021-causal-focusing-v2` — causal-boundary payload promoted; PR #182 closed `PROMOTED / PROVENANCE`; v3 is active.
- `engineering/e001-material-impulse-world` — validated one-body wall-world payload is canonical; PR #194 is frozen provenance and #205 is the declared next pair-impulse generation.

Do not fast-forward old generations merely to make them look current.

## 4. Replay-required / stacked sources

The following retain useful history but are not active mother-theorem homes:

- `research/core/relation-quotient` — broad historical A3 source.
- `research/core/relation-support-bridge` and old `bridge/a3-a4-v2` — B01-B06 has moved through the thin bridge; remaining B07-B58 must be rehomed by actual owner.
- `engineering/e001-material-state-cost` — stacked benchmark/application carrying an upstream `material_future_precision` dependency; later replay only unique benchmark/test assets.

## 5. Selected historical PR audit

### 5.1 Closed as absorbed

- #22 P005 — `ABSORBED / SEMANTIC`.
- #23 P017 mirror support/incidence — `ABSORBED / STRICT_GENERALIZATION`.
- #65 P017 rough/high-band — `ABSORBED / STRICT_GENERALIZATION`.

### 5.2 Remain open

- #21 — `PARTIAL ABSORPTION / CORRECTED / DEPENDENCY-BLOCKED WIP`, Draft. Old L023 is subsumed; old L024 required the later anchor-survival correction; L025 is unvalidated because its historical branch lacks the imported `four_support` dependency.
- #54 — `SPECIALIZATION / STILL-UNIQUE REPRESENTATION / UNVALIDATED DRAFT`. Centered-prime-radius remains a potentially useful near-diagonal coordinate but requires fresh validation, numbering reconciliation and prior-art audit before replay.

## 6. Scope-audit corrections

The current machine routing records:

1. P022 `p022_hcp_*`, `p022_barlow_*`, `p022_geodesic_*`, P022 tests and bilingual registration as legitimate same-owner assets.
2. `bridge/a3-a4-generated-support-v3` rather than broad v2 as the active A3/A4 bridge.
3. A2/A3/A4 v2 as exhausted generations.
4. P021 v3 rather than v2 as the active causal/focusing owner.
5. P025 as a new clean L2 owner created after the #215 snapshot, with only `P025_*`, `abc_*`, prior-art/lineage and registration paths allowed.
6. E001 impulse-world as provenance, while impulse-v2 / pair-impulse / multi-action remain active and state-cost remains replay-required.

## 7. Current P022 publication order

Do not publish the large P022 owner wholesale. The next reviewed slicing order is:

1. **Geodesic Multiplicity core** — `A_p` + simple-cubic distance/interval/path multiplicity, independently checked by recursive and direct-enumeration oracles;
2. **HCP extension** — exact integer HCP contact graph and geodesic-growth layer, explicitly dependent on the GM core;
3. **Barlow family** — only after GM/HCP are separately validated.

## 8. Canonical promotion protocol

For every active owner or bridge:

1. research locally without whole-main synchronization;
2. relay reusable results;
3. freeze the exact publication payload;
4. create a fresh L4 from then-current main;
5. replay only owner-owned frozen assets;
6. run quality, bilingual-sync, reference-integrity and Lean when applicable on the exact merge state;
7. merge only that L4;
8. freeze/close exhausted source generations after every branch-side asset is accounted for.

Moving main is not a research stop condition; it matters at the final L4 combination gate.
