# Enterprise Math Current Research Branch Ledger

Status: `CANONICAL OWNER-ISOLATION LEDGER / CURRENT WRITE SURFACE`  
Routing snapshot base: `main@aec7f625e48eb8f93ba701ba57686a9e225efd17`  
Prior canonical ledger refreshes: PR #196, PR #215  
Date: 2026-08-09

This ledger is a human-readable snapshot of the current semantic write surface. The live machine authority remains:

`branch_governance_overrides.json + research_scheduler.json + Issue #240 runtime events`.

Open PR count, branch age and Git ancestry do not define theorem ownership. The governing lifecycle is:

`owner research -> freeze exact payload -> fresh/latest-main L4 replay -> exact merge-state gates -> canonical main`.

L1/L2/L3 owners may intentionally lag moving `main`. They do not whole-tree sync merely for currency. L4 publication is globally `NO NEW MATHEMATICS`.

## 1. Canonical reusable layers at this snapshot

Current `main` includes, among other already-canonical assets:

- A2 finite-arity / future-compatible quotient machinery;
- A3 weighted relation-state, relation lattice/scale core;
- A4 admissible correspondence / relational-spectrum core;
- the thin A3->A4 generated-support bridge;
- P021 causal-boundary core;
- P022 `A_p` / root-lattice geometry core;
- P022 geodesic-multiplicity + HCP executable core via PR #262 / `fc81a15a`;
- P022 Barlow stacking executable core via PR #288 / `aec7f625`;
- P018 centered-prime-radius executable layer via PR #270 / `b4801960`;
- Owner-Isolation, scope-aware branch auditing, scheduler / handoff control and Foundation Steward rules;
- the current canonical E001 finite material/world slices already registered in the owner registry and shared-surface machinery.

The next bounded P022 publication is task-relative Barlow precision. Its validated two-blob payload from source PR #265 has been replayed onto `main@aec7f625` as fresh L4 PR #292; it remains non-canonical until the exact current-head promotion gates pass and the PR merges.

## 2. Current active semantic write surface: 13 routes

The machine owner registry currently contains thirteen `ACTIVE_OWNER` / `ACTIVE_BRIDGE` routes. The preferred compact operating range remains roughly 8-12 long-lived write refs, so thirteen is a temporary high edge, not a new target. Compaction must follow semantic result conservation rather than arbitrary branch deletion.

### 2.1 Core owner — 1

1. `core/a3-relation-lattice-v3`
   - sparse integer relation-lattice continuation;
   - owns spanning-tree integer coordinates, zero-total orbit/lattice reconstruction and directly dependent A3 invariants only.

### 2.2 Program owners — 6

1. `program/p017-legendre`
   - square-basin / Legendre pressure-test frontier.

2. `program/p018-precision-v2`
   - precision-specific quotient/root-channel and carry frontier;
   - generic future-compatible quotient theory remains A2/P023-owned.

3. `program/p021-causal-focusing-v3`
   - focusing, direction, causal-role/witness and bounded physical-specialization continuation after causal-boundary promotion.

4. `program/p022-geometry-v2`
   - intrinsic discrete geometry owner;
   - `A_p` lattice, geodesic/HCP and Barlow stacking executable layers are canonical;
   - Barlow precision/growth/coordination and later observation/collision/repair work remain same-owner research unless a theorem is rehomed by weakest hypotheses.

5. `program/p024-action-precision`
   - state/action-language precision specializations;
   - generic quotient/adjunction remains upstream-owned.

6. `program/p025-abc-support-collapse`
   - abc/radical-support pressure test and relation-conditioned bounded-witness precision candidates;
   - no abc proof or problem closure is implied.

### 2.3 Cross-owner bridges — 3

1. `bridge/a3-a4-generated-support-v3`
   - thin genuinely cross-owner A3 relation-state -> A4 generated-support bridge.

2. `bridge/p017-p018-hard-core-v2`
   - arithmetic / precision hard-core, root-channel and analytic tail-resource bridge.

3. `bridge/a2-e001-material-markov`
   - future-compatible quotient -> material future-state specialization bridge.

### 2.4 E001 engineering owners — 3

1. `engineering/e001-material-impulse-v2`
   - finite impulse/momentum, force activation, retained remainder and reversal-certificate frontier.

2. `engineering/e001-material-contact-network`
   - bounded contact-network incidence/Gram, cycle-kernel and rank-duality owner.

3. `engineering/e001-measurement-area-refinement`
   - exact measured-polyline area shells, refinement variation and their regressions.

The former pair-impulse and multi-action owner generations are no longer active write points; their bounded results have been promoted and those branches are now provenance.

## 3. Exhausted, provenance or replay-required generations

Do not fast-forward these merely to make them look current.

### Provenance / absorbed

- `core/a2-future-quotient-v2`;
- `core/a3-relation-state-v2`;
- `core/a4-admissible-support-v2`;
- `program/p021-causal-focusing-v2`;
- `engineering/e001-material-impulse-world`;
- `engineering/e001-material-pair-impulse`;
- `engineering/e001-material-multiaction-protocol`;
- the mechanically/semantically absorbed E002 v2 generations recorded by the owner registry;
- absorbed P023 and historical P018 all-power quotient generations recorded by the owner registry.

### Replay-required / stacked sources

- `research/core/relation-quotient` — broad historical A3 source;
- `research/core/relation-support-bridge` and old `bridge/a3-a4-v2` — B01-B06 has moved through the thin v3 bridge; remaining historical metric/count/future-language material must be rehomed by actual owner;
- `engineering/e001-material-state-cost` — stacked benchmark/application source carrying upstream future-state dependencies;
- validation/stacked PRs must not be treated as canonical merge vehicles merely because their tests are green.

## 4. Selected historical PR audit

Result conservation uses semantic categories, not branch age.

### Closed as absorbed / strict-generalization

- #22 P005 — `ABSORBED / SEMANTIC`;
- #23 P017 mirror support/incidence — `ABSORBED / STRICT_GENERALIZATION`;
- #65 P017 rough/high-band resource route — `ABSORBED / STRICT_GENERALIZATION`.

### Still open because unique assets remain

#### #21 — `STILL-UNIQUE / REPLAY-QUEUE`

Unique historical mathematics remains:

- large-support hit for support product `G>2k` via centered carry;
- exact-support closure via smooth half-scale cofactor;
- four-prime support-set reindexing / basin contribution formula;
- `basin_aggregate.py` + tests;
- provenance of the historical Supplement-06 text despite later path reuse.

However the historical executable cannot be replayed unchanged today: `basin_aggregate.py` imports `.four_support`, while neither current `main` nor #21 head contains `src/enterprise_math/four_support.py`. The old quality failure is therefore a real dependency defect in that historical slice. This is **not** a HARD_BLOCK for P017. The owner may later replay the independent large-modulus / exact-support layer separately and must locate, reconstruct or supersede the four-support tail dependency before publishing the aggregate tail layer.

Do not close #21 as absorbed and do not merge it wholesale.

#### #54 — `EXECUTABLE PROMOTED / PROSE-PROVENANCE`

- canonical `prime_gap_slack.py` was already exact-present on main;
- the still-unique centered-prime-radius Stage-9 executable layer was replayed through L4 PR #270 and merged as `b4801960` after all three repository gates passed;
- historical Supplement prose / numbering / lineage was intentionally excluded from #270 and remains provenance until selectively replayed or explicitly classified superseded/rejected.

Do not merge #54 wholesale.

## 5. Current P022 publication chain

P022 remains one geometry theorem home, but publication is sliced by dependency and payload.

1. **`A_p` / root-lattice geometry** — canonical before this snapshot.
2. **Geodesic multiplicity + HCP executable core** — canonical through PR #262 / `fc81a15a`.
3. **Barlow stacking executable core** — canonical through fresh two-blob L4 PR #288 / `aec7f625`; validation PR #261 is closed publication provenance.
4. **Barlow task-relative precision** — source PR #265 is independently three-gate green; its exact two frozen blobs are under current L4 promotion in PR #292.
5. **Periodic growth and coordination observables** — stacked validation PRs #267 and #269 are independently three-gate green, but must remain non-merge sources until their prerequisite layers are canonical; each later publication must be a separate bounded L4.
6. Current observation-history / collision / repair research on `program/p022-geometry-v2` remains independent owner work and is coordinated by scheduler Issue #240.

## 6. Scope-audit corrections relative to the older 12-route ledger

The current machine routing changes the earlier ledger in four material ways:

1. `core/a3-relation-lattice-v3` is now an active A3 owner generation.
2. E001 pair-impulse and multi-action generations are provenance, not active owners.
3. `engineering/e001-material-contact-network` and `engineering/e001-measurement-area-refinement` are the current bounded E001 owner generations replacing those exhausted write points.
4. P022 geodesic/HCP and Barlow stacking are canonical; Barlow precision is the bounded next publication slice rather than another broad-owner merge.

Therefore the live semantic surface is thirteen routes at this snapshot, even though many more Git refs and open PRs exist for validation, provenance or stacked research.

## 7. Canonical promotion protocol

For every active owner or bridge:

1. research locally without whole-main synchronization;
2. publish reusable proved WIP/counterexamples through the Relay when cross-route consumption matters;
3. freeze the exact publication payload;
4. begin one L4 integration from then-current `main`;
5. replay only owner-owned frozen assets;
6. inspect intervening main deltas rather than generating a new replay merely because main moved;
7. run `quality`, `bilingual-sync`, `reference-integrity`, Lean and shared-surface gates when applicable on the final merge state;
8. merge only that L4;
9. mark superseded validation/replay PRs as provenance after asset conservation is explicit;
10. close an historical branch only after every unique result is classified `INTEGRATE`, `SUPERSEDED`, `COMPARATOR-NEGATIVE`, `OWNER_MOVED` or `REJECTED`.

Moving `main`, CI, review, ACK latency or another route's publication queue is not a research stop condition. Only a fully specified hard dependency that eliminates all meaningful owner-local work is a `HARD_BLOCK`.

## 8. Authority rule

This file is intentionally a snapshot. If it disagrees with the current owner registry, scheduler or live runtime claims, use:

`branch_governance_overrides.json -> research_scheduler.json -> Issue #240`

for current routing, and use canonical `main` + `docs/PROBLEM_STATUS.*` / theorem documents for mathematical truth.
