# HODGE H0K — Driver Review

Date: `2026-08-22`
Driver: `EM-DVR-HODGE-4Q7M2K / HODGE_CONTROL_PLANE`
Task: `RS-HODGE-H0K-FERMAT-CUBIC-RATIONAL-HODGE-CARRIER-INTERACTION-TRANSFER`
Taskbook source: `35af8150cc4e7d6a50c26b82a2d0c17a4de784fe`
Owner branch: `research/hodge-h0k-fermat-rational-hodge-interaction`
Researcher-ID: `EM-HODGE-H0K-5A71D2`

## Driver disposition

`H0K_CLASS_FIRST_LIFT_SOURCE_COMPLETE_NO_ENTERPRISE_INCREMENT`

is **ACCEPTED** at the exact returned scope.

Hard prerequisite A:

`FERMAT_CUBIC_EXACT_RATIONAL_HODGE_INPUT_CARRIER_ESTABLISHED_WITHOUT_CYCLE_GENERATOR = PASS`.

Primary hard target B:

`SOURCE_DERIVED_INTERACTION_PROCESS_ADDS_ROBUST_ATTRIBUTED_LEVERAGE_ON_EXACT_RATIONAL_HODGE_INPUT = NOT_ESTABLISHED`.

Stronger target C:

`FERMAT_CUBIC_CLASS_FIRST_ENTERPRISE_R3_PRESEED = NOT_ESTABLISHED`.

`H1_ADMISSIBLE = false`.
`Hodge_proved = false`.

The owner branch is exactly one commit ahead of the taskbook source under the connected compare surface, with only H0K result/checker artifacts added. The connected branch surface did not expose the result head SHA directly; freeze the payload by owner branch plus:

- semantic core SHA-256: `32d795d72ad07391a7cd486b3f7f83605dba0462d8a4c5557a805afc85a9c218`;
- manifest blob SHA: `5055ca394395c5aa425800f632820c586311e2af`;
- checker: `168173/168173 PASS`.

## Accepted load-bearing results

1. **Exact rational Hodge input carrier before cycle search.**
   The diagonal `G=(mu_3)^6/Delta` action on the Fermat cubic gives twenty primitive complex `(2,2)` character eigenlines, paired by `Gal(Q(zeta_3)/Q)` into ten two-dimensional rational blocks. The Galois-fixed projectors `e_[a]=e_a+e_2a` lie in `Q[G]` and act on singular `H^4(X_F,Q)`. Together with `Q*h^2`, this freezes a genuine 21-dimensional rational Hodge carrier before any plane search.

2. **Cycle independence of the carrier.**
   The rational carrier is not defined from a plane, known Chow basis, known Fermat spanning theorem, or known Hodge-conjecture conclusion. Known Fermat algebraicity results remain checker-only.

3. **Full target-independent plane grammar.**
   The source algebraically generates the complete 405-plane grammar from all 15 coordinate pairings and all `3^3` phase assignments before target-specific basis selection.

4. **Arbitrary class-first lift on the entire frozen carrier.**
   After source generation, exact plane class rows and blockwise Gram data yield a rational cycle `Z(alpha)` for every `alpha in V_Hdg,Q`, including cross-block, negative and denominator-bearing inputs, with exact `cl_Q(Z(alpha))=alpha`.

5. **Interaction law genuinely source-derived.**
   The Hodge-side local interaction is `m(t,r)=zeta_3^(-tr)`. In the rational basis `1,zeta_3`, multiplication is `(a,b)*(c,d)=(ac-bd,ad+bc-bd)`. The R063 Gaussian law and C4 table are not imported.

6. **Real abstract operational reduction.**
   Three local pair interactions reconstruct the global plane-to-character Fourier coefficient. The process reduces a 216-entry global coefficient presentation to a six-entry local table plus tensor composition, with real compositional factoring and dependency reduction against `B_raw^Fermat`.

## Attribution verdict

The fair `B_std^Fermat` already contains:

- finite-group representation theory and `Q[G]` projectors;
- Galois descent;
- tensor-product character DFT;
- Griffiths/Jacobian normal forms;
- exact plane-orbit incidence matrices;
- exact sparse linear algebra, SNF/HNF and rational inversion;
- Chow/cycle rational linear combinations and intersection pairing.

Therefore:

- K1 tensor interaction = source-inherited character DFT;
- K2 signed/cyclotomic descent = source-inherited cyclotomic/Galois arithmetic;
- K3 pairing/block interaction = source-inherited representation block decomposition;
- K4 class-first lift = source-inherited exact cycle-class matrix inversion.

Freeze:

`ROBUST_TRANSFORM_ATTRIBUTED_ENTERPRISE_INCREMENT = false`.

H0K satisfies every declared R3-preseed condition except the robust Enterprise increment condition. No R3 preseed is accepted.

## Scientific consequence

H0K closes two recurring ambiguities for future Hodge-special research:

1. **rational-input ambiguity is closed** on a nontrivial degree-four benchmark;
2. **arbitrary class-first lifting is achievable** on the complete declared rational Hodge carrier.

Thus a successor must not return to:

- character DFT repackaging;
- a larger Fermat plane grammar;
- another target basis on the same carrier;
- more bundle/Chern/Chow matrix inversion;
- future-signature/BRC provenance quotienting.

The next theorem-critical mechanism should move from direct cycle construction to **geometric support/coniveau lowering**.

For smooth projective complex `X`, in degree `2p` the codimension-`p` algebraic cycle span is the geometric coniveau piece `N^p H^{2p}(X,Q)` via localization, purity/resolution and Gysin. Hence the Hodge conjecture can be typed as the support-lowering obligation

`H^{2p}(X,Q) cap H^{p,p} subset N^p H^{2p}(X,Q)`.

This classical equivalence is control mathematics, not Enterprise credit. The next stage should ask whether an Enterprise-style downward-collapse mechanism can construct or certify codimension-`p` support **before** cycle coefficients are known.

Freeze route decision:

`DIRECT_CLASS_FIRST_LIFT_IS_CLASSICALLY_SOURCE_COMPLETE_ON_H0K__PIVOT_TO_CONIVEAU_SUPPORT_DOWNWARD_COLLAPSE`.
