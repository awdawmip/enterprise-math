# CBRC F5R — Independent Forgetful-Branch Semantic Replication Driver Review

Status: `ACCEPTED_WITH_NEW_AXIOM_BOUNDARY`
Date: `2026-08-25`
Driver-ID: `EM-DVR-CBRC-F0-7C3A21`
Task-ID: `RS-CBRC-F5R-INDEPENDENT-FORGETFUL-BRANCH-SEMANTIC-REPLICATION`
Taskbook source: `3015cee704b6864c955bf577637383dd8c3dfd19`
Accepted owner branch: `research/cbrc-f5r-independent-forgetful-branch-semantics`
Accepted owner head: `f7532b27e2d3e38486b52f8ae24a78734b5a0053`
Researcher-ID: `EM-CBRC-F5R-8120F1`

## 0. Driver verdict

`F5R_ACCEPTED`.

Primary mathematical verdict accepted:

`F5R_NEW_AXIOM_REQUIRED`.

Hard target:

`FORGETFUL_BRANCH_NONDEGENERACY_SEMANTIC_STATUS_INDEPENDENTLY_CLASSIFIED = ACCEPTED`.

The acceptance is a semantic independence/new-axiom result. It is not an admission of the candidate axiom into Foundation and does not authorize F6 or any rank-two carrier construction.

## 1. Publication-liveness and evidence surface

The mandatory liveness gate passed before mathematical work:

- fresh Researcher-ID allocated;
- remote owner branch created from the exact taskbook source;
- required JSON execution stamp committed at `1c6a03fd55cccad10be608345ab2229f585986f1` before Checkpoint A;
- stamp records `phase = STARTED_BEFORE_MATH` and `mathematical_verdict = null`;
- Checkpoint A return and countermodel drafts were pushed at `f275a313e58e809953ca20da6a78301664f4d654` and `5d58c9215145729fa8dcaaa18f42dc2e58ec98ea`;
- Checkpoint B audit/checker were pushed at `ec852fe1a95a2ed6356ace6f42ed8db3e216755d` and `1f67d0b475efc8d6afd900f2632b280534c26e20`;
- pushed-checker execution was recorded at `cdc3ebf083d45007d7579e74e700d459d28a22f4`;
- final manifest freeze is `f7532b27e2d3e38486b52f8ae24a78734b5a0053`.

All six mandatory artifacts exist at the accepted head. The final manifest reports complete materialization and no local uncommitted worktree state.

## 2. Source firewall

`TARGET_LEAK_AUDIT_PASS` is accepted.

Before raw freeze the research used only:

1. `research_inputs/CBRC_F5_BLIND_FORGETFUL_BRANCH_SEMANTICS_PACKET_20260823.md@a107c133e11597623bbe79ef37397fc8ba5c13f7`;
2. `definitions/ENTERPRISE_BRC_MULTIPATH_ENRICHMENT_BRIDGE_20260821.md@6ec0d73a19e28ec586c59a97d24f5798c9119771`;
3. `definitions/ENTERPRISE_NATIVE_LINE_TRACE_FORMULA_20260821.md@b631242db84c5bd3640e6dc554b19a1d04d464f3`.

No downstream coherent-wave, R063/R064/R065/FQ, external quantum/wave theory, rank-two target, complex/quadratic carrier, phase group, norm, inner product or square law was used as a premise or selector.

## 3. Accepted type-boundary theorem

The allowed sources distinguish:

- a concrete Path-formal witness;
- the formal coefficient attached to a witness;
- a later marked slot;
- an enriched coefficient state in that slot;
- the forgetful retraction `pi : C -> Z e`;
- later marker erasure/recoalescence.

A retained marked slot is not canonically identical to a concrete Path-formal basis witness merely because its enriched coefficient is nonzero.

Accepted label:

`F5R_PATH_WITNESS_VS_MARKED_SLOT_SEMANTIC_BOUNDARY_CLASSIFIED`.

## 4. Accepted independence countermodel

Take the additive bookkeeping carrier

`C = Z x Z/2`,

with `e=(1,0)` and `pi(n,t)=n e`.

Define

`M_B((n,t),(m,s)) = ((n,t),(m,s+n mod 2))`.

This map is additive and involutive. On the elementary input it gives

`M_B((e,0)) = ((1,0),(0,1))`.

Therefore both enriched outputs are nonzero while

`pi(1,0)=e`,
`pi(0,1)=0`.

The same model preserves the total old signed coefficient, old Boolean support, marker distinction, reversibility and no-resurrection. Path-formal provenance may remain on the genuinely old-supported branch while the second branch stores only enrichment-kernel information.

Thus none of the currently accepted retraction/refinement/support conditions forces

`pi(x) != 0 and pi(y) != 0`.

A candidate-true model also exists under the same common conditions. Hence the result is exact logical independence, not merely missing proof technique.

Accepted labels:

`F5R_FORGETFUL_BRANCH_NONDEGENERACY_DERIVABILITY_CLASSIFIED`;

`F5R_BRANCH_SURVIVAL_LOAD_BEARING_AXIOMS_CLASSIFIED`.

## 5. Strongest derived substitute

Without a separately imposed old-coordinate conservation equation, no nontrivial per-branch theorem about `pi(x),pi(y)` follows beyond enriched nonzeroness.

If

`pi(x)+pi(y)=e`

is imposed, then necessarily

`pi(x) != 0 or pi(y) != 0`.

This is sharp:

- the kernel-branch model permits exactly one nonzero projection;
- the conserving free block `[[2,1],[-1,0]]` permits two nonzero projections.

Therefore total conservation selects neither `both nonzero` nor `exactly one nonzero`; it forces only `at least one`.

Accepted label:

`F5R_MAXIMAL_DERIVED_FORGETFUL_BRANCH_CONDITION_CLASSIFIED`.

## 6. Rank consequence

The accepted F4 theorem states that under `GLOBAL_ZERO_SEPARATION`, every successful torsion-free-rank-one model has a signed-permutation free quotient block. Such a first column cannot carry two nonzero old-coordinate projections.

F5R does not derive the missing two-branch projection condition. Therefore no unconditional rank lift follows.

Only after adding a new branch-to-old-support faithfulness/nondegeneracy axiom may one infer

`torsion_free_rank(C) >= 2`.

Status:

`CONDITIONAL_ON_NEW_AXIOM`.

The weaker `at least one nonzero projection` theorem does not close the F4 torsion loophole.

Accepted label:

`F5R_CONDITIONAL_RANK_CONSEQUENCE_CLASSIFIED`.

## 7. Checker review

Pushed checker blob:

`b83995d4d1bf00db3d078fcb349ef5ed5223f8a4`.

Manifest SHA-256:

`f3d77387fe54ee44017af5b76bd6d3b4327b02ca028f3fd07c615e54d6d2df8f`.

Researcher execution:

- result `PASS`;
- mismatch count `0`;
- check count `39`;
- deterministic digest `14201c39734a17782aa7dabb48a22c0e97fc72a002f6f78578cf3645869d9a97`.

Driver independently replayed the pushed checker semantics and reproduced exactly:

- `39` checks;
- `0` mismatches;
- digest `14201c39734a17782aa7dabb48a22c0e97fc72a002f6f78578cf3645869d9a97`.

`F5R_PUSHED_CHECKER_PASS = ACCEPTED`.

## 8. Scope disposition

Freeze:

`FORGETFUL_BRANCH_NONDEGENERACY_IS_NOT_DERIVED_FROM_CURRENT_BRC_SEMANTICS`.

`PER_BRANCH_OLD_SUPPORT_FAITHFULNESS_REQUIRES_A_NEW_AXIOM`.

`RANK_TWO_LOWER_BOUND_REMAINS_CONDITIONAL`.

No F6, rank-two classification, downstream wave comparison or Foundation promotion is authorized by this review.

The only authorized successor is an axiom-admission/minimality audit that decides whether a narrowly typed branch-to-old-support faithfulness rule should be admitted, rejected, narrowed or left model-relative.