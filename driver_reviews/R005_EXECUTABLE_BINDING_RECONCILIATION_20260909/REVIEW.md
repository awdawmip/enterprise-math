# R005 correction recovery and exact-byte Driver review

Driver: `EM-DVR-81273A`  
Authority: `DA-207C4345BD4091D3059F`, authenticated Owner comment `5599483429`  
Reviewed Result: `RR-566554389F6F36576DE2` / publication `TP2-ADD82532ACD19FC01D53`  
Frozen repair source: `1d9823dfe6ba9301e936740da847ac3c682fe0a2` (PR #1201)  
Frozen original source: `f9e2a611b45631c43effce36b7300c6f9a56b77b` (PR #1140)

## Decision and exact strength

The recovered repair satisfies the old task's
`R005_DEFICIT_SHADOW_EXECUTABLE_BINDING_CORRECTED_AND_BYTE_REVALIDATED`
target. The substantive Driver decision is **ACCEPTED** only at
`DEFICIT_SHADOW_STRUCTURAL_REDUCTION_WITH_EXECUTABLE_BINDING_REPAIRED`
strength. This is `NO_MATH_DELTA` executable/evidence acceptance. It grants no
q=78553 seam closure, endpoint extension, Working Truth, Foundation status,
canonical mathematical promotion or parent closure.

This artifact is the source review. The immutable Result-review record and its
typed follow-up must separately satisfy the current write-boundary and
follow-up contracts before task-terminal runtime or downstream release. In
particular, the old Result's task-specific hard-target string remains unchanged;
it is not silently rewritten to `SATISFIED`.

## Recovered completion and ownership

The new conversation recovered actual Issue #240 server envelopes:

- CLAIM `5533593735`, created and unedited at `2026-09-03T23:46:28Z`;
- HANDOFF `5533667142`, created and unedited at `2026-09-03T23:56:06Z`.

Both are by `awdawmip` / user `30957095` / association `OWNER`. They bind
`EM-R005AFIX-754384`, claim `chatgpt-r005a-fix-20260904-0746`, execution
`ER-932CC977692631CF047E` and branch
`research/r005a-fix-chatgpt-20260904-754384`. The branch remains at the reviewed
immutable head. The recorded handoff is complete and awaits Driver review; it
does not prove an active researcher session. No new claim, transfer, adoption,
redating or change of researcher identity was performed.

The Sep-4 Result is a separate immutable Correction Result for the abandoned
Sep-3 package. The Sep-9 relay's requirement for that correction object is
fulfilled by consuming this recovered object under its original author,
publication and parent. It does not require pretending the completed scanner
repair was absent or creating a duplicate repair execution.

## Exact source comparison

The scanner changes `metada` to `metadata` for `max_gap_bound_end` and repairs
the malformed required-band diagnostic f-string. It also removes one
nonexecuting explanatory comment. No mathematical expression, constant or
scan operation changes. The regression removes four comments and adds its
true-attestation fixture, short-max-bound negative test and output fields.
The complete diffs are retained in the review evidence.

All six Result output entries and all five artifact-manifest entries were
checked against immutable connector reads. Git blob SHA-1, SHA-256 and every
declared byte count agree. The original Result record blob is
`aea04ecd76c5f6effe92726ae08d64c512f73131`. Original records, return, transcript,
manifest and source bytes are copied verbatim, including their original dates.

## Executed validation

`source_revalidation.json` records actual argv, cwd, timestamps, exit status,
stdout/stderr paths and the checked scanner/regression digests. On the final
recovered bytes:

- `py_compile`: exit `0`;
- focused regression: exit `0`, same parsed JSON as the stored Sep-4 output;
- gap-shadow checks: `54165`; deficit checks: `1008`; synthetic scan: `963`
  failures on `2188` rows;
- q=78553 constants and the old certified endpoint remain unchanged.

Windows stdout uses CRLF; the original persisted JSON uses LF. The original
bytes are preserved and parsed output is identical. This line-ending
difference is disclosed rather than reported as byte-identical replay output.

The old regression validates true attestation and scans a synthetic case in
separate tests. The additional Driver review test
`check_attested_scan_path.py` joins the actual path:

`load_catalog -> validate_catalog_for_seam(coverage, max_bound, verify_rows=True)
-> scan_gap_shadows`.

It uses a complete **small synthetic catalogue**, with `2188` nonempty rows,
and verifies all `963` scan failures against direct finite enumeration. The
one-unit-short `coverage_end`, one-unit-short `max_gap_bound_end`, and false
attestation cases each fail closed. `attested_scan_run.json` and its actual
output retain the checked source digest and the run evidence. This is reviewer
validation of unchanged source, not a new q=78553 catalogue or theorem proof.

## Publication reconciliation

The two active generation-1 publications have compatible repair intent but
different parent, owner, identity-lane and disposition fields. They remain
immutable and separately referenceable. The formal intake
`PI-R005-81273A-20260909` covers both publications and the recovered Result.
Pass 1 independently checks each route's provenance, scope and evidence; pass 2
challenges ownership, source-set completeness, loss of obligations and authority
leakage. Both use the same exact evidence-set digest and disclose the same
reviewer and shared context. They are not claimed as two independent research
executions.

`PS-R005-81273A-OPERATIONAL-20260909` chooses
`TP2-ADD82532ACD19FC01D53` for Result/review continuity and retains
`TP2-2C524413EC9774383AF1`. The operational resolution and removal of only the
exact R005 fork quarantine are one atomic change. The legacy fields
`canonical_publication_id` and `quarantined_publication_ids` are compatibility
aliases for that operational choice and retained alternate; they are not
mathematical truth selection or rejection of the alternate.

The original parent `OBJ-R005-PRIME-ALGORITHM-LAB` and relay parent
`OBJ-R005-PRIME-ALGORITHM-LAB-RELAY-20260909` retain distinct provenance and
responsibilities. Existing persistent governance, Correction Driver review,
catalogue/seam and terminal-review tasks remain intact. No duplicate task is
created and no downstream task is released by this synthesis.

## Remaining control and mathematical boundary

The current `TASK_SCOPE_CLOSURE_PORTFOLIO_CONTINUATION` helper accepts only the
literal Result label `SATISFIED`. This older successful Result carries the
taskbook's exact named success label. That is a post-review control encoding
boundary, referred to the Owner with an exact-source read-only reproducer. It
does not invalidate the verified source repair and is not permission to rewrite
the immutable Result, fabricate a follow-up task or close either parent.

DSI1/DSI2/DSI3 remain frozen. `q=78553` is open, the certified frontier remains
`k <= 2822453183433`, and a complete independently auditable exact-916
consecutive-prime-gap catalogue is still required for starts
`[1291005053866735,1294364244470160]`. BRC is `NOT_APPLICABLE` to this control and
byte-binding repair; method harvest remains `RESULT_ONLY`. There is no new
general mathematical tool or independent theorem claim.
