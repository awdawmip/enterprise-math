# R063 Stage 0 — Path-Valued Square Root 2500 Driver Review

Status: `ACCEPTED / FROZEN`
Date: `2026-08-21`
Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`

## 1. Reviewed source

Task-ID: `RS-R063-STAGE0-PATH-VALUED-SQUARE-ROOT-2500-DISCOVERY`

Taskbook source:

`e7dd7af0b7f01168afd25e55d04900e841cc399e`

Owner branch:

`research/r063-stage0-path-valued-sqrt-2500`

Repository comparison at review: owner branch is exactly one commit ahead of the taskbook source and zero behind; the added files are confined to `research_results/R063_STAGE0/` and the deterministic checker under `scripts/`.

Frozen discovery-phase SHA256:

`6dd26e75436fb6740134590fc243b59ab9eb287cadbc46d417d5bbbb74022f0d`.

## 2. Discovery/verification barrier

The checker satisfies the anti-target-leakage requirement.

`discover_from_scalar_root(N)` executes before the brute norm fiber is called. Its output is materialized and hashed as `R063_STAGE0_DISCOVERY_PHASE_FROZEN.json`. Only after that freeze does `verify_against_norm_fiber` enumerate the target equation `a^2+b^2=N`.

Source anti-hardcode scan reports no nontrivial discovered component-pair literals embedded in the generator.

Freeze:

`R063_DISCOVERY_PHASE_INDEPENDENT_OF_BRUTE_TARGET_FIBER = true`.

## 3. What sqrt(2500) discovers

Input:

`N=2500`, ordinary scalar root `r=50`.

The complete ordered nonnegative component-root fiber discovered by C3 and independently by C4 is

`{(0,50),(14,48),(30,40),(40,30),(48,14),(50,0)}`.

Hence there are two distinct nondegenerate unordered shapes:

`{14,48}` and `{30,40}`.

This is structurally stronger than the `N=25` reference, which has only one nondegenerate unordered shape `{3,4}`.

## 4. Candidate classification

### C0 — ordinary scalar square root

`2500 -> 50` is exact but discards component-root identity, trace identity, derivation provenance and path multiplicity.

### C1 — inverse norm fiber

Brute enumeration of `a^2+b^2=N` is a verification oracle, not an accepted discovery operator.

### C2 — unscaled square lift

Take `N_E(beta)=r` and square `beta`.

At `N=2500`, C2 discovers only

`(0,50),(14,48),(48,14),(50,0)`

and misses

`(30,40),(40,30)`.

Freeze classified negative result:

`UNSCALED_SQUARE_LIFT_IS_INCOMPLETE_IN_GENERAL`.

Its first square-regression failure occurs already at `r=3`.

### C3 — scaled square lift

For `N=r^2`, enumerate factor channels

`r=k(m^2+n^2)`

and form

`k(m+nJ)^2 = k(m^2-n^2) + 2kmn J`,

retaining the canonical primitive Euclid channels and ordered orientations.

For `r=50`, the nondegenerate canonical channels include

`50=2(4^2+3^2) -> (14,48)`

and

`50=10(2^2+1^2) -> (30,40)`.

C3 exactly recovers the complete component-root fiber with no extras.

The theorem proof shows completeness for every square native norm by gcd reduction to the primitive Pythagorean classification.

### C4 — Gaussian factorization-first lift

Factor `N` in `Z[J]` and distribute exponents between split Gaussian prime/conjugate factors. For

`2500=2^2*5^4`,

the Gaussian exponent allocations generate exactly the same six ordered nonnegative roots before brute verification.

C4 exposes prime-exponent allocation provenance directly.

Freeze:

`C3_SCALED_SQUARE_LIFT_COMPLETE_FOR_SQUARE_NATIVE_NORMS = true`.

`C4_GAUSSIAN_FACTORIZATION_ROOT_FIBER_COMPLETE_FOR_THE_VALIDATED_SQUARE_SCOPE = true`.

## 5. Path-valued square-root formula

For square input `N=r^2`, let `GRoot_E(N)` be the deduplicated ordered nonnegative component-root set generated by the accepted algebraic discovery operator.

For each `(a,b)` define

`Lambda(a,b)=[u^a v^b](uX_i+vX_j)^(a+b)`.

Freeze the square-scope path-valued root:

`PathSqrt_E(N)=disjoint_union_{(a,b) in GRoot_E(N)} Lambda(a,b)`.

After the frozen R061 typed start incidence is attached, each branch is exactly the native trace `T_{a,b}^{(ij)}` with

`|Lambda(a,b)|=binom(a+b,a)`.

Native length remains `sqrt(a^2+b^2)=r`; path-letter count `a+b` is not native length.

## 6. Exact N=2500 path fiber

Branch cardinalities in one fixed sector:

- `(0,50)`: `1`;
- `(14,48)`: `29,078,984,349,975`;
- `(30,40)`: `55,347,740,058,143,507,128`;
- `(40,30)`: `55,347,740,058,143,507,128`;
- `(48,14)`: `29,078,984,349,975`;
- `(50,0)`: `1`.

Exact one-sector total:

`110,695,538,274,255,714,208`.

The astronomical fibers are not explicitly expanded; exact binomial counts, coefficient-extraction formulas and deterministic combinadic rank/unrank certificates are committed instead.

## 7. Two different multiplicities

R063 distinguishes:

1. `ALGEBRAIC_ROOT_DERIVATION_MULTIPLICITY` — multiple factorization/scaled-square channels that can derive the same component root;
2. `NATIVE_PATH_MULTIPLICITY` — the binomial number of noncommuting path linearizations inside the deduplicated native trace.

They are not the same quantity and must not be multiplied automatically.

Freeze root-return policy:

`DEDUP_COMPONENT_ROOT_AND_TRACE; RETAIN_DERIVATION_PROVENANCE_AS_HIGHER_ENRICHMENT`.

## 8. Regression and evidence

Deterministic checker:

`python3 scripts/r063_stage0_validate_path_valued_sqrt_2500.py --out research_results/R063_STAGE0`

Exact regression:

- `1<=r<=512`;
- C3 mismatch count `0`;
- C4 mismatch count `0`;
- combined mismatch count `0`;
- checker mismatch count `0`.

Regression rows SHA256:

`51891fbf9c3a66b1e00dfaa92fa6ae46984e4a6c99325624ec5b88371b66dcae`.

## 9. Scope boundary

This stage validates and proves the path-valued square-root operator on the square-native-norm scope `N=r^2`.

C4 algebraically suggests a broader Gaussian norm-root construction for non-square `N`, but R063 Stage 0 does not freeze that extension. It remains an open question.

BRC remains downstream projection only and does not participate in root discovery.

## 10. Driver verdict

`SQRT_2500_ALGEBRAICALLY_DISCOVERS_COMPLETE_NATIVE_COMPONENT_AND_PATH_FIBER = true`.

`PATH_VALUED_SQUARE_ROOT_FACTORIZATION_OPERATOR_COMPLETE_FOR_SQUARE_NATIVE_NORMS = true`.

`R063_STAGE0_STATUS = COMPLETE_AND_FROZEN`.

No R063 Stage 1 is opened by this review.
