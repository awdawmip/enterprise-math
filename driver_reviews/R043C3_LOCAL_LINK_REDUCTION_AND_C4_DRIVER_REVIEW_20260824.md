# R043-C3 Local-Link Reduction and C4 Driver Review

Status: `DRIVER_REVIEW / C3 ACCEPTED / LOCAL-LINK OBSTRUCTION ISOLATED / C4 AUTHORIZED / NO CANONICAL PROMOTION`

Driver-ID: `EM-DVR-ZX1UEJ / CONTROL_PLANE`

Date: `2026-08-24`

Reviewed return:

- PR `#625`;
- researcher `EM-R043C3-9F21C6`;
- owner head `49877b834c4f15e7f30cb54f03ba5f106dba0342`;
- task `RS-R043C3-UNOCCUPIED-COMPONENT-FRONTIER-CONNECTIVITY`.

## 1. Driver disposition

C3 is accepted at research-checkpoint strength with primary verdict

`REDUCED_TO_LOCAL_LINK_SEPARATOR_LEMMA`.

Accepted exact evidence includes the complete thick-void pressure family `U=N[D]` for all frozen-symmetry FCC connected cores through `|D|<=8` (`158,260` cores, zero disconnected frontier slices) and HCP cores through `|D|<=7` (`55,523` cores, zero disconnected frontier slices). These remain bounded family statements only.

The global frontier-connectivity claim is not accepted as proved.

## 2. Exact remaining object

C3 has reduced the grouping question to a local native incidence statement:

`R043C3-L1 / native interface-to-frontier connectivity`.

The issue is whether a connected interface between one connected unoccupied component and its connected complementary side can ever transfer connectivity only through lower-dimensional close-packed cell incidences while the corresponding unoccupied frontier sites remain disconnected under the original 12-contact graph.

A proof removes C2's component-grouping ambiguity. A counterexample supplies the exact local separator pattern needed to construct a harmful latent-interaction G0 fiber.

## 3. C4 gate

C4 must not expand the generic animal ceiling. It must attack the local statement by either:

1. a purely discrete shortest-path/minimal-separator proof using the actual FCC/HCP site links; or
2. an exact native cell-interface incidence table plus a global connected-interface glue argument.

Every local case must be finite and auditable. FCC and HCP are separate theorem obligations.

## 4. External topology boundary

A Mayer-Vietoris connected-interface argument may be used only as the global glue layer after a precise native thickening is defined. It does not by itself prove frontier-site graph connectivity.

The theorem-critical missing step is the native incidence lift. Do not substitute continuum intuition for that finite combinatorial proof.

## 5. Driver decision

C3:

`RS-R043C3-UNOCCUPIED-COMPONENT-FRONTIER-CONNECTIVITY = DONE / RETURNED / ACCEPTED AT RESEARCH CHECKPOINT`.

Authorized successor:

`RS-R043C4-NATIVE-INTERFACE-LINK-SEPARATOR-CLOSURE`.

Priority: `P1`.

No hard block. No Foundation or canonical theorem promotion.
