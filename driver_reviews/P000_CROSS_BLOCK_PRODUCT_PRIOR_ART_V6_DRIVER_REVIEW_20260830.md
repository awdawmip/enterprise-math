# Driver Review — P000 cross-block product / block-system prior-art V6

Status: `ACCEPTED / CLASSICAL PRODUCT-BLOCK BOUNDARY FROZEN / FRAME-PRIOR-ART OPEN`

Result: `RR-742A5139AD81F0E41917`  
Task: `RS-P000-6D-ROTATION-PRIOR-ART-DUPLICATION-AUDIT`  
Publication: `TP2-E4143818BDA70C70113B`  
Researcher: `EM-P0006DPA6-E61B42`  
Driver: `EM-DVR-7C31A8`

## Verdict

`ACCEPTED`.

The result correctly classifies the Gen7/Gen8 product/block-system layer as substantially classical and prevents novelty overclaim.

## Accepted external boundary

1. Connected Cartesian-product factorization and factor-controlled automorphism structure are classical Sabidussi–Vizing / product-graph theory.
2. The Gen7 whole-block envelope

`W=(S3 x S3) semidirect C2 ~= S3 wr S2`

is the standard stabilizer of a `3+3` block system in `S6`, with `|W|=72`.
3. The required mixed action `b=(E2 E4)(E3 E5)` lies outside `W` because it sends the first 3-block to the mixed set `{E1,E4,E5}`.
4. The equal-block stabilizer `S3 wr S2` is a maximal imprimitive subgroup of `S6`; therefore the naive total-permutation extension satisfies

`<W,b>=S6`.

The return's exact finite checker independently enumerates all `720` elements. This yields an important project guard:

`GLOBAL_BMix_b_AS_TOTAL_PERMUTATION_ON_TOP_OF_FULL_W => S6`.

5. Partial actions / inverse semigroups are standard formalisms for supplied partial symmetries with explicit domains; they do not generate the missing P000 mixed relation by themselves.
6. Cartesian graph bundles are a standard adjacent framework for twisted/fiberwise transport beyond direct products, but do not identify the project-specific native relation.

## Source quality

The ledger uses authoritative book/publisher/journal sources including the Handbook of Product Graphs, Dixon–Mortimer, peer-reviewed group theory, Lawson on inverse semigroups, Exel on partial actions and Imrich–Pisanski–Žerovnik on graph bundles. This is sufficient for the current duplication boundary.

## Strength boundary

This review does **not** grant novelty to `BMix_b`, P000 no-quotient semantics, native Cell identity, or any frame/connection construction. `NO_MATERIAL_MATCH` remains only a search boundary.

## Routing consequence

The product/block-system audit is closed at V6 scope. The next external audit must move with Gen9/Gen10 and examine the newly exposed mathematics of:

- definability/invariance under automorphism groups;
- `S6` torsors and symmetry breaking;
- frame fields / principal bundles / local trivializations;
- graph/discrete connections and parallel transport;
- gauge freedom and loop holonomy;
- seed-frame + connection versus per-Cell frame equivalence;
- whether a frame plus local passage data is a standard construction before P000-specific typing/no-quotient constraints are imposed.

Destination: `TP2-5A7C1D9E3B6042F8D117`.

Final disposition: `ACCEPTED / FOLLOWUP_TASK`.
