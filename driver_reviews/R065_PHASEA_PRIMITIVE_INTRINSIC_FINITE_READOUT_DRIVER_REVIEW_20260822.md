# R065 Phase A — Driver Review

Status: `DRIVER_ACCEPTED / CLEAN_BLIND_STRUCTURAL_REPLICATION / SCALAR_UNDERDETERMINATION_FROZEN`

Driver: `EM-DVR-R63A21 / CONTROL_PLANE`
Date: `2026-08-22`

Task:

`RS-R065-PHASEA-PRIMITIVE-INTRINSIC-FINITE-READOUT-DISCOVERY`

Taskbook source:

`7e6f46a27504ea3cb8867128952047da029643ae`

Blind packet source:

`00765cc76ea71f789481fbe91c29d852bbf6b209`

Owner branch:

`research/r065-phasea-primitive-intrinsic-readout`

Frozen owner head:

`fa9a6a7932afc898a0b834b7d3b0033526ec226f`

Researcher:

`EM-R065A-7E6F46`

Intake comparison:

- frozen owner head is exactly 2 commits ahead of taskbook source;
- owner branch was 0 commits behind the taskbook source at intake;
- all 14 mandatory outputs/checker artifacts are present;
- no taskbook, blind-packet, Foundation or canonical-definition file was modified on the owner branch.

## Driver verdict

The Phase-A hard target is accepted:

`PRIMITIVE_SUBSTRATE_INTRINSIC_FINITE_READOUT_CLASSIFIED = ACCEPTED`.

Strongest accepted blind result:

`CANONICAL_COMPONENT_PARTITION_AND_COMPLETE_S3_ORBIT_RECONSTRUCTED_WITH_NONUNIQUE_INTRINSIC_SCALARIZATION_AND_EXACT_OBSERVATION_VALUATION_SEMANTIC_GAP`.

The packet is clean independent evidence at the declared Phase-A provenance strength.

## Accepted Phase-A theorems

### A. Complete unlabeled state invariant

For a sector-supported content `n=(n1,n2,n3)`, the presentation-independent isomorphism class under token renaming and full `S3` component relabeling is completely determined by

`Lambda(n)=sort(n1,n2,n3)`.

Two contents are isomorphic iff their sorted multiplicity triples agree.

Therefore every presentation-independent scalar factors through `Lambda`.

### B. Canonical component-type relation

The primitive token typing canonically defines

`R_type = {(x,y): tau(x)=tau(y)}`.

Equivalently this is the token equivalence relation whose blocks are the occupied component fibers.

This is a parameter-free definable derived relation and does not require metric, norm, length, pair-capacity, target formula, preferred orientation or target scalar.

### C. Scalar underdetermination

Several inequivalent exact scalars are definable from the same blind substrate, including total token cardinality, occupied-support cardinality, largest block size, same-type unordered-pair cardinality and cross-type unordered-pair cardinality.

In particular TOTAL and MAXBLOCK both satisfy zero normalization, unit normalization, full `S3` invariance, token-renaming invariance and componentwise monotonicity, yet differ already on a two-unit two-component state.

Therefore the primitive substrate does not uniquely force a scalar magnitude/scale readout.

### D. Conditional additive classification

If an extra hypothesis requires an `N`-valued readout to be additive on every admissible typed composition and `S3` invariant, then it is `c*TOTAL`. Unit normalization further gives `TOTAL`.

These are conditional selecting axioms, not primitive consequences.

### E. Orbit-composition obstruction

The complete unlabeled single-state orbit `Lambda` does not inherit typed componentwise composition as a single-valued binary operation without relative component alignment.

Two identical unit orbits may be aligned on the same component or on distinct components and yield different output orbits.

## Driver independent finite audit

The checker contains only the blind carrier declarations and exact finite/integer operations. Driver review found no embedded downstream scalar target.

Accepted tested-domain counts:

- component multiplicities `0..6`;
- `127` sector-supported states;
- all `6` `S3` relabelings;
- `762` state-permutation checks;
- `7057` admissible ordered composition pairs;
- submitted mismatch count `0`.

The general orbit and additive-classification statements are also justified by direct finite-set/group arguments, not by regression alone.

## Post-freeze comparison with LSR-N2-20260822

This section is Driver Phase-B comparison and is not retroactively part of the blind Phase-A evidence.

The previously audited replacement candidate uses the component observation and its kernel relation:

`ker(comp) = {(x,y): comp(x)=comp(y)}`.

R065 independently reconstructed exactly the same relation object at relation strength:

`R_type = {(x,y): tau(x)=tau(y)}`.

Under the blind packet, `tau` is the primitive component-type observation. Hence:

`R_type = ker(tau)`.

Therefore:

`LSR_N0_RELATION_OBJECT_CLEAN_INDEPENDENT_REPLICATION = ACHIEVED`.

### Cardinality readout is inside the frozen blind definability envelope

R065's own definability language explicitly allows finite products/relations followed by cardinality once the finite object is specified. Consequently the scalar

`Q_R = |R_type|`

is already a valid intrinsic scalar generated by the frozen blind mathematics, even though the researcher did not list it as a named retained scalar.

For multiplicities `(n_i)`:

`Q_R = sum_i n_i^2`.

Using the R065 retained scalars:

`Q_R = TOTAL + 2*SAME2`,

because `TOTAL` supplies the diagonal ordered pairs and each unordered distinct same-type pair contributes two ordered pairs.

This is a post-freeze deterministic consequence of the blind packet; it is not evidence that Phase A selected `Q_R` as the preferred scalar.

### Mandatory non-overclaim

The blind run did **not** uniquely select `Q_R`, did **not** assign it a magnitude/scale role, and did **not** independently assert that relation cardinality should be squared line scale.

Therefore do not record:

`LSR_N2_SCALAR_ROLE_BLINDLY_UNIQUELY_RECONSTRUCTED`.

The correct classification is:

`LSR_RELATION_LAYER_REPLICATED; LSR_CARDINALITY_READOUT_DEFINABLE_IN_BLIND_ENVELOPE; LSR_SCALE_ROLE_REMAINS_N2_SEMANTIC_CHOICE`.

## Replication-gate disposition

The previous control-plane formulation requested a clean run that would independently converge to the same N2 component-kernel readout before a Foundation question was considered.

R065 proves a stronger boundary: the primitive substrate itself does not uniquely select any scalar readout or semantic scale role. Repeating blind trials until one researcher happens to choose `|R_type|` would therefore be selective sampling rather than stronger mathematical evidence.

Accordingly the replication gate is reclassified:

- clean independent reconstruction of the theorem-critical N0 relation object: `SATISFIED`;
- mathematical availability of its finite cardinality readout: `SATISFIED`;
- primitive uniqueness of that readout among all intrinsic scalars: `FALSIFIED / NOT REQUIRED`;
- semantic assignment `Q_R := squared line scale`: `OPEN FOUNDATION CHOICE`.

Thus:

`LSR_N2_BLIND_REPLICATION_PROGRAM = MATHEMATICALLY_COMPLETE_AT_RELATION_PLUS_UNDERDETERMINATION_BOUNDARY`.

No additional blind run is justified merely to seek accidental scalar convergence.

## Candidate-family omission note

The Phase-A candidate table did not explicitly name `|R_type|` even though it named `R_type` and admitted cardinality of specified finite relations. This is a minor candidate-table omission, not a failure of the no-go classification.

Adding `|R_type|` supplies one more inequivalent intrinsic scalar and therefore strengthens, rather than weakens, scalar nonuniqueness.

Future citations should distinguish:

- the submitted named candidate list;
- the complete orbit-function envelope;
- the Driver-added post-freeze scalar `|R_type|` obtained directly from the frozen relation object.

## Semantic freeze

Freeze the hierarchy:

`primitive typed content`

`-> N0/derived same-component equivalence relation R_type`

`-> optional numerical valuations/cardinalities at readout strength`

`-> separate semantic role assignment such as magnitude/scale`.

No scalar readout is promoted to N0 by this review.

No Foundation definition is changed by this review.

## Next decision surface

The remaining information gap is no longer mathematical discovery of a unique scalar. It is a Foundation-facing semantic-admission question:

whether to adopt the cardinality of the canonical component-preserving relation as the squared-line-scale readout, and whether that single relation/readout formulation should replace or merely refactor the scalar-first `axis square + transverse independence` interface.

That question must be explicit and comparative. It is not another blind Phase-A search and is not opened automatically by this review.
