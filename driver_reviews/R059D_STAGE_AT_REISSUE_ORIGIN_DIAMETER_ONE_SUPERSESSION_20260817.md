# R059D Stage AT-REISSUE — Origin-Diameter-One Foundational Supersession

Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`
Date: `2026-08-17`

## Disposition

The task

`RS-R059D-STAGE-AT-REISSUE-VOID-FIRST-EXISTENCE-GEODESIC-SPHERE-CIRCLE`

is superseded before execution by the stronger user foundational statement:

`THE_ORIGIN_ITSELF_IS_THE_DIAMETER_ONE_ENTERPRISE_CIRCLE`.

Do not execute the old taskbook as canonical.

## New frozen base circle

Read:

- `definitions/ENTERPRISE_ORIGIN_IS_DIAMETER_ONE_CIRCLE_20260817.md`
- `definitions/ENTERPRISE_VOID_ORIGIN_EXISTENCE_GEODESIC_20260817.md`

Freeze:

`VOID_E=∅` is external pre-coordinate nonexistence.

`VOID_E -> O_E=[+1]=[-1]` is the unique first existence step.

`CIRCLE_E(1)={O_E}`.

`DIAMETER_E(CIRCLE_E(1))=1`.

The base circle is not to be retyped as a classical diameter-zero/radius-zero point-circle.

## Why the previous task is insufficient

The previous AT-REISSUE treated `EXISTENCE_SPHERE_E(1)={O_E}` merely as a special shell and left circle identification open. The user now freezes that shell as the first Enterprise circle and freezes its diameter as one.

This changes the admissible diameter/radius/circumference analysis:

- `DIAMETER_E` cannot be silently identified with internal pairwise graph diameter;
- classical `D=2R` may not be imported;
- a ratio such as `C/(2r)` may not be called circumference/diameter until the Enterprise diameter law is independently derived;
- historical `kappa_E` interpretations must be re-audited under the new diameter typing.

## Preserve from old AT-REISSUE

The following remain valid research targets and should be carried into the replacement stage:

- void-first existence graph;
- all shortest void-to-point paths;
- spatial-tail graph distance;
- geodesic path-family segment ontology;
- local equal-length path deformation;
- fixed-existence-length endpoint sets;
- D6/reversal structure;
- historical supersession map.

## Stop rule

Old owner branch:

`research/r059d-stage-at-reissue-void-first-existence-geodesic-sphere`

must stop under the old task semantics. A replacement taskbook will start from a new source commit and must treat `CIRCLE_E(1)` and diameter one as frozen inputs.
