# PFSSV frozen return: request a bounded revision

Driver: `EM-DVR-01E1D9 / CONTROL_PLANE`.
Disposition: `REQUEST_REVISION`; destination: `NONE`; nonterminal.

This review concerns Result `RR-3287C6124F8D8A1F0901`, task
`RS-PRIME-FACTOR-SEMIPRIME-SHELL-RESIDUAL-VALIDATION`, publication
`TP2-8EE46AA7741B8867DAF3`, restored unchanged from source
`1cac434cb9ac542a38a8e7bb6defa414fefb2e4d` by PR #1406.
Current authority is `DA-3DD1D3F6CAAB01EA5D6C`, derived from actual unedited
Issue #240 comment `5580862447`. The review record must bind the current
immutable Result bytes in its mutation parent. No old Result field is rewritten.

## Decision and retained value

The original report does not establish completion of the published hard target
or its assertion that no unresolved residue remains. Request one revision of
the same task and retain the original generation and all seven source files.
The follow-up spec supersedes the prior publication through the current
transaction; it does not transfer the expired historical claim.

The exact integer shell-enumeration structure is useful. Independent trial-prime
enumeration and direct pair multiplication reproduce the two already-public
discovery-cell raw counts, 237 and 25 at X=100000 for widths 1/100 and 1/1000.
The original coarse-null screen and its recorded holdout numbers remain
historical observations under the implemented procedure. They do not justify
a new arithmetic residual candidate. They also do not prove that all correctly
controlled residuals at the registered scales have been ruled out.

## Evidence that prevents acceptance

1. The checker deletes every p row with zero observed q-prime count before
   constructing the null and smooth baseline. Removing zero-weight rows is
   harmless for the observed total, but changes the support on which the null
   can place mass. In the two independently checked small shells this removes
   15 of 59 and 29 of 41 geometrically nonempty eligible windows; their omitted
   smooth expected masses are 11.840328204718341 and 5.867714049252224. Within
   one actual stratum, p=109 has one q prime and p=139 has zero; the latter still
   has positive smooth expectation and is discarded. A conditional-on-occupied-
   support model is possible, but it must be disclosed and justified. The
   current surrogate cannot be described as depending only on first-order
   density and shell geometry while its position mask uses observed q primes.
2. `prime_rank` is the normalized index of surviving p rows, with no pi(q).
   For the same p=23 the stored coordinate changes with shell width while the
   true prime rank remains 9. This is not the task's required `(pi(p),pi(q))`
   primary view.
3. The serialized 21-cell summary omits the five full profiles and complete
   signed/corrected residual vectors. Selected-bin scores and maxima cannot
   reconstruct the required per-cell raw/corrected occupancy and cross-scale
   phase evidence.

The independent audit also identifies hard-coded scientific PASS/terminal labels,
an overbroad generic 64-bit primality label, and clipping beyond the declared
u<=1/2 interval. The primality counterexample lies outside the original tested
q range and therefore does not invalidate those finite-range counts. The square
boundary issue does not directly refute selected bin 10. Both require explicit
scope handling in the revised implementation, without overstating their effect.

The original signal losing its sign under a different post-hoc correction does
not itself certify a calibrated null or restore blindness. This is why the
decision is a revision request rather than acceptance under a renamed negative
terminal class.

## Exact unfinished unit and execution boundary

The one same-task revision must freeze its correction design, preserve or
explicitly justify the complete eligible-p/null support, provide actual prime-
rank coordinates, retain all required profiles, derive scientific labels from
computed gates, and state scope/exposure limitations. Keep every original scale,
width, hard-target obligation and native progressive-plane interpretation.
The S3 orbit remains a derived diagnostic. No secondary coloring can rescue the
primary gate, and no new global tool family is admitted.

Previously opened holdouts remain exposed. A revised analysis of them is a
post-exposure reanalysis; it must not be called a new blind replication. The
researcher may return a sharply bounded partial outcome if a gate cannot be
discharged. A new second-order direction or genuinely new blind test would need
its own later decision and frozen design. This review publishes no such route.

The six follow-up gates are explicitly recorded in
`research_artifacts/PFSSV_DRIVER_REVIEW_20260908/revision_followup_spec.json`.
Their NOT_REQUIRED decisions avoid additional parallel routes for this single
nonaccepted, result-only repair; they do not certify the eventual corrected
return, waive future review, or close the parent objective.

## Verification and source exposure

The root reviewed the exact original taskbook, return, checker and current
contracts, then the independent audit and actual small-check receipt. The
independent reviewer had source exposure and did not execute a task claim.
The original full-size sieve, original 21-cell run, and original blind chronology
were not replayed or newly certified. This review is mathematical and procedural
assessment; it is not native arithmetic certification or theorem promotion.

The preserved audit, original diagnostic script, actual receipt and invocation
limitations are in `research_artifacts/PFSSV_DRIVER_REVIEW_20260908/`.
The report SHA-256 is
`53db4b935131fe33f9b084ba4014fad29af2c141aab2cb400fee063a1d01ec6a`;
the actual receipt SHA-256 is
`d4f1f3c976c4fd9ed391e673c890eea0d5c8b5bc9c2222889687adcbde005f66`.
