# Prime Fusion Free-Discovery Replication — Driver Intake

Status: `DRIVER_INTAKE_COMPLETE / DERIVED_NOT_AXIOM / INDEPENDENT_REPLICATION_REQUESTED`
Date: `2026-08-23`
Driver-ID: `EM-DVR-R63A21 / CONTROL_PLANE`
Scope: `prime-arrangement free discovery -> theorem package -> blind independent replay`

## 1. Inputs reviewed

Canonical source rollback snapshot:

`88d86e2146c01cbe7a62432e9488b2b4621ec9fa`.

Free-discovery provenance reviewed from GLOBAL_KNOWLEDGE journal checkpoints including:

- `557594c4b8fdeed7a5f58682d3abc8a8566de133` — cyclotomic corridor / local matching classification and post-freeze classical audit;
- `6b5b45c2cfd272b09ed58e3f8b88ac3cb5515812` — fusion algebra / reversible-collapse derivation and Phase-B comparison;
- `d1f92e34d77f82069575105c612617655a5c880b` — reciprocal-trace/idempotent/phase bridge continuation.

Theorem-package review branch:

`research/prime-fusion-theorem-package-clean@e5138e17f8c4009f5e357f43326f2812c9df1359`.

Draft PR:

`#597`.

Blind independent-replication input branch:

`research/prime-fusion-blind-replication-packet@096d7f4f3a6347b79bee58ae0973cea518780efa`.

Blind packet:

`research_inputs/PRIME_FUSION_BLIND_INDEPENDENT_REPLICATION_PACKET_20260823.md`.

## 2. Isolation audit

Relative to source main `88d86e2...`:

- theorem branch is ahead by exactly two added files: theorem Markdown + checker;
- blind packet branch is ahead by exactly one added file: the blind packet;
- source main itself contains neither package nor blind taskbook.

The blind packet exposes the primitive one-sector substrate and the two raw quadratic readouts only. It does not disclose the T1–T15 theorem list, theorem proofs, source checker implementation, or source-run comparison conclusions.

The current source/discovery conversation is not eligible to count as clean independent replication.

## 3. Candidate-lifecycle classification

The theorem-package core is not a new primitive axiom. Its structural statements are derived from the already supplied sector coordinates/readouts plus ordinary algebra/number theory.

Therefore the correct candidate-lifecycle classification is:

`DERIVED_NOT_AXIOM`.

This classification is eligible for Driver intake under `research_axiom_candidate_state_machine.json`, and the selected Driver action is:

`INDEPENDENT_REPLICATION_REQUESTED`.

Do not relabel this derived theorem package as `AUDITED_AXIOM_CANDIDATE` merely to satisfy a taskbook origin enum.

## 4. Taskbook origin/lineage disposition

Canonical V5 permits `FREE_AXIOM_CANDIDATE` taskbooks only when `origin_candidate_state` is one of:

- `AUDITED_AXIOM_CANDIDATE`;
- `AUDITED_REPLACEMENT_CANDIDATE`;
- `EXACT_NEGATIVE_OBSTRUCTION`.

Because this intake classifies the source result `DERIVED_NOT_AXIOM`, the clean replay taskbook must use:

`origin_kind = REPLAY_OR_INTEGRATION`

and

`task_lineage = REPLAY`.

Free-discovery provenance remains explicit in the task-local provenance section and frozen source refs. This preserves origin without laundering a derived theorem into an axiom-candidate lifecycle.

## 5. Replication authorization

Approved hard target:

`PRIME_FUSION_CORE_INDEPENDENTLY_RECONSTRUCTED_OR_REFUTED`.

Approved evidence design:

1. genuinely new context-clean researcher;
2. before return freeze, only the approved blind taskbook and blind packet may select the mathematical target;
3. independent derivation and independently authored exact-integer checker;
4. active counterexample search and explicit degeneracy handling;
5. frozen return before any theorem-package comparison;
6. after freeze, Driver performs theorem-by-theorem comparison against PR #597.

Permitted return classes:

- `FULL_STRUCTURAL_REPLICATION`;
- `PARTIAL_REPLICATION`;
- `MATERIAL_COUNTEREXAMPLE`;
- `NO_RECONSTRUCTION`.

A failure to reproduce the source theorem package is valid evidence and must not be rewritten after comparison.

## 6. Theorem-package status

PR #597 remains:

`DRAFT_REVIEW / NOT_CANONICAL / NOT_DRIVER_ACCEPTED_AS_A_PACKAGE`.

Author-side checker PASS is audit evidence only. Independent replay is being requested precisely so the later Driver comparison can distinguish reproducible structure from source-run-specific derivation choices.

## 7. Driver disposition

`REPLICATION_INTAKE = PASS`

`SOURCE_DISCOVERY_CLASS = DERIVED_NOT_AXIOM`

`DRIVER_ACTION = INDEPENDENT_REPLICATION_REQUESTED`

`TASKBOOK_ROUTE = REPLAY_OR_INTEGRATION / REPLAY`

`CURRENT_CONVERSATION_REPLICATION_ELIGIBILITY = NOT_INDEPENDENT`

No canonical Foundation/source definition is modified by this intake.
