# GEO6 Native Translation and Følner Semantics — Driver Review

Driver-ID: `EM-DVR-G6K2P9`  
Role: `RESEARCH_DRIVER`  
Task: `RS-GEO6-NATIVE-TRANSLATION-FOLNER-SEMANTICS`  
Publication: `TP2-2DACFDB1816BE0DEB532`  
Result: `RR-EBE56954DEFC688B316A`  
Disposition: `ACCEPTED`  
Terminal: `true`

## Decision

The hard target

`GEO6_NATIVE_TRANSLATION_ACTION_AND_FOLNER_DENSITY_INTERFACE_TYPED_OR_EXACTLY_OBSTRUCTED`

is satisfied at current-language strength.

The Result package is writer-conformant. Return, checker, certificate and execution record match every Git blob in the Result manifest, and the branch changes exactly the five authorized paths. The original repository checks were blocked before task-local execution by an unrelated decorated-carrier quarantine-head mismatch; no GEO6 translation/Følner defect was found.

## Accepted mathematical strength

At the frozen accepted P000/Full-Cell evidence cut there is no typed native translation/amenable action. Exact equivalences and automorphisms are presentation symmetry only; observation/reduct maps are wrong-sort for a total action; comparison `Z^6` shifts and finite carrier permutations do not become native translations by notation.

The minimum missing interface is accepted as:

`NATIVE_TRANSLATION_LABEL_GROUP_AND_TYPED_CELL_FULLCELL_ACTION`,

including a native group, total Cell and Full-Cell actions, identity/composition, a nondegeneracy witness, and exact equivariance with every accepted readout/locality relation placed in scope. Period, window, boundary and Følner semantics remain blocked until that action is independently typed.

The finite underdetermination witnesses are accepted at their declared strength:

- on the same eight-state readout carrier, two faithful readout-preserving `C4` actions have generator fixed-point counts `0` and `4`; exhaustive checking of all `576` readout-preserving bijections finds no conjugacy;
- fiberwise-regular readout-preserving `C4` and `V4` actions remain inequivalent because their element-order profiles differ.

These witnesses prove that the observable/fiber fragment does not reconstruct a unique action or translation-label group. The project-level current-language no-go additionally relies on the accepted resolver census; the finite models are not promoted as full models of every P000 axiom.

Once a native action and admissible windows are independently supplied, standard periodic/Følner averaging may be reused conditionally. That classical reuse does not construct the missing native action.

## Selector consequence

- `TRANSLATION_ACTION_SELECTOR` becomes `PARTIALLY_CONSTRAINED_BY_ACCEPTED_DATUM`.
- `TRANSLATION_FOLNER_SELECTOR` becomes `PARTIALLY_CONSTRAINED_BY_ACCEPTED_DATUM`, with its dependency on the action selector preserved.

No native action, Working Truth, Foundation authority, ontology elevation or novelty certificate is granted. The task is terminal, and no density successor may be published until an independently accepted native action satisfies the frozen interface.