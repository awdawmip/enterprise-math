# Toolbox Logic-Closure Audit — 2026-08-23

Driver: `EM-DVR-ZX1UEJ / CONTROL_PLANE`

Status: `DRIVER AUDIT / CLOSURE PATCH ISSUED`

## Scope

End-to-end audit of:

1. research return intake;
2. family registration;
3. method harvest;
4. executable callability;
5. role-timed lookup;
6. strict taskbook-only execution;
7. regression and shared-surface ownership;
8. non-promotion/no-go discoverability.

## Findings

### F1 — six-return intake was integrated

The six A+/A/B returns were merged and classified:

- T10 local redistribution/toppling;
- T11 discrete Morse chain reduction;
- T12 idempotent path closure/Bellman;
- weighted energy as T10 specialization;
- Voronoi/Delaunay as carrier domain operator;
- discrete conformal as extra-structure/no-go result.

This closed family-level routing and prevented three false new-family promotions.

### F2 — base inventory pointer did not describe dated addenda

`tools/enterprise_toolbox.py` already loaded
`research_method_inventory_addenda/*.json`, but the machine invocation policy
and human protocol named only `research_method_inventory.json`.

Consequence: execution through the router worked, but governance documentation
and machine policy did not describe the same lookup surface.

Repair:

- policy V2 names base plus addenda;
- protocol V2 names four lookup layers;
- a machine closure checker verifies pointer equality and duplicate rejection.

### F3 — strict taskbook-only execution lacked a visible tool-invocation marker

Taskbooks already inherited the final-response identity footer, but did not
carry an analogous compact field for tool lookup.

Consequence: a researcher instructed to read only the taskbook could receive a
valid identity contract while never seeing that role-timed reuse-before-
invention was inherited.

Repair:

- taskbook contract V7 requires
  `tool_invocation_policy=INHERIT_GLOBAL`;
- generator writes it;
- review repairs it;
- dispatch audit rejects a missing or altered value;
- policy digest invalidates old stamps on tool-policy change;
- blind-forward timing remains hidden until the named freeze.

### F4 — T12 was routable but not production-callable

The T12 registry/inventory/checker existed, but the family explicitly said no
production module.

Consequence: a researcher could discover the family but had no stable source
API to call, unlike T10 and T11.

Repair:

`src/enterprise_math/idempotent_path_closure.py`

now provides exact finite:

- min/max-plus matrix composition and powers;
- improving-cycle obstruction;
- Kleene closure with path witness;
- finite idempotent-semiring validation;
- left/right residuals;
- Bellman least/greatest fixed points.

No floating arithmetic or inferred weights are used.

### F5 — no single machine gate checked the entire loop

The Common Surface checker correctly owns shared file-index integrity, but it
does not by itself prove that an accepted mathematical tool is simultaneously
registered, harvested, callable, routable and taskbook-visible.

Repair:

`tools/check_toolbox_logic_closure.py`

verifies:

- T0–T12 uniqueness/continuity;
- registry/policy/base/addendum/router pointer consistency;
- production-callable tool source modules;
- registered source/report/review path existence;
- base+addendum method uniqueness;
- T12 production source visibility;
- taskbook inheritance field;
- human protocol/registry markers;
- executable-router rediscovery.

## Closed-loop invariant after repair

`ACCEPTED RETURN`

`-> DRIVER CLASSIFICATION`

`-> FAMILY REGISTRY OR EXPLICIT NON-PROMOTION`

`-> BASE/ADDENDUM METHOD HARVEST`

`-> PRODUCTION MODULE IF CALLABILITY CLAIMED`

`-> ROUTER DISCOVERY`

`-> ROLE-TIMED INVOCATION`

`-> TASKBOOK-ONLY LAST-MILE VISIBILITY`

`-> REGRESSION + MACHINE INTEGRITY GATE`.

## Preserved boundaries

- FREE Phase A remains unanchored by the current catalog.
- Blind-forward TASK lookup remains delayed until its named freeze.
- Tool acceptance does not mutate Foundation.
- Classical theorem packaging is not theorem novelty.
- Weighted incidence does not define energy.
- Carrier nearest-site geometry is not native metric.
- Current overlapping circle cells are not a circle packing.
- P008 retains ordinary adjunction/residuation ownership.
