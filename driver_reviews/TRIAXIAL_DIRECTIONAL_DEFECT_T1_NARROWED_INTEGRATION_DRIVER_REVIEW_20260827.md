# Driver Review — Triaxial Directional-Defect T1 Narrowed Integration

Status: `DRIVER_FINAL / ACCEPTED / T1_GLOBAL_SUBTOOL_MATERIALIZED / NO_NEW_MATHEMATICS / NO_NEW_TOP_LEVEL_TOOL_FAMILY`

Date: `2026-08-27`

Driver-ID: `EM-DVR-K7Q4N8 / CONTROL_PLANE`

Source PR: `#645`

Reviewed PR head: `d4813a4d9ed1efe5a4bf05ddb6ec4b5491c27b41`

Independent mathematical/tool authority retained from:

- controlling review `f364c75d55e8018bcea80bdf00fc025f2ad7dd02`;
- independently verified implementation head `b1f79d2314de2d1ae1511a693cdf37e7c7812cf8`.

Current-main materialization commit:

`b2d4bd80709152b33287999ceddc09da0ab64555`.

## Final disposition

`DRIVER_DISPOSITION = ACCEPTED`.

`CLASSIFICATION = GLOBAL_SUBTOOL`.

`PARENT_FAMILY = T1_SCALE_ENUMERATION_VALUATION`.

`SUBTOOL_ID = T1_TRIAXIAL_DIRECTIONAL_DEFECT`.

`NEW_TOP_LEVEL_TOOL_FAMILY = NO`.

`FOUNDATION_MUTATION = NONE`.

`NEW_MATHEMATICS = NO`.

The integration is accepted as a narrowed executable surface for mathematics already independently verified. It does not reopen or strengthen the source theorem package.

## Materialized exact executable surface

The current-main replay intentionally excludes the stale governance file `research_task_registry.json` from PR #645. Only the reusable integration payload is materialized:

- exact private core blob `5261da8b9e7848ac5cce935f81d7e7e0381b210b` at `src/enterprise_math/_triaxial_directional_defect_core.py`;
- exact public adapter blob `570088fb2a8dc1a19ada303d55ea1cb93718a0c7` at `src/enterprise_math/triaxial_directional_defect.py`;
- exact regression blob `c92da7c8d87a7c0efae477151323435d2ff0f8ec`;
- exact method-inventory addendum blob `422562255c3c6a64e8fc1d263f4ccbbf0d253959`;
- exact integration return and evidence manifest;
- T1 machine registry subtool entry and an incremental human-router mirror update.

No old task-registry runtime state was replayed.

## Accepted scope guards

The following boundaries are binding:

1. `FULL_ADJOINT / CHIRALITY_ADJOINT` remain typed separately from `COMPRESSED_GRAM / GRAM_FACTOR`;
2. frame inputs use primitive/canonical validation and explicit unoriented deduplication;
3. width/uniqueness routing remains native-hex/support-compatible only at the independently verified scope;
4. coefficient-characteristic diagnostics remain restricted to the exercised characteristics `0,2,3,5,7` on the adjoint/Gram integration path;
5. endpoint coalescence is not trace identity;
6. tomography and Hive/rhombus reuse share the same operator core;
7. exposed-vertex augmentation retains the verified unimodular/full-rank certificate behavior;
8. Y–Delta is not exported;
9. binary/nonlinear tomography, Foundation edits, width-theorem enlargement and new top-level family promotion are excluded.

## Validation evidence

At exact PR head `d4813a4d9ed1efe5a4bf05ddb6ec4b5491c27b41`, all three repository checks completed successfully, including the whole-suite test run. The integration return records 1431 tests passing and 13 dedicated new regressions.

The current-main materialization uses the exact verified code/test blobs rather than a rewritten implementation. Machine registry replay was safe because the pre-T1 current registry blob was identical to the PR's original base registry blob (`81ab862925df4946ac41c83cb2b7b048d5909a3c`). Human documentation was updated incrementally rather than overwritten.

## Final freeze

`T1_TRIAXIAL_DIRECTIONAL_DEFECT = DRIVER_ACCEPTED_NARROWED_EXECUTABLE / GLOBAL_SUBTOOL`.

`PR_645 = PROVENANCE_PACKAGE_ONLY_AFTER_CURRENT_MAIN_MATERIALIZATION`.

`SUCCESSOR_TASK = NONE`.

`FOUNDATION = UNCHANGED`.
