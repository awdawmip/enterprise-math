# R059D Stage AT-REISSUE2 — Initial Unit Circle Supersession

Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`
Date: `2026-08-17`

## Disposition

The task

`RS-R059D-STAGE-AT-REISSUE2-ORIGIN-DIAMETER-ONE-CIRCLE-GENERATION`

is superseded before canonical execution.

Reason: it froze only the origin-circle diameter as one and treated radius/perimeter/area as later unknowns. The user clarified the stronger base-circle axiom:

`CIRCLE_E(1)={O_E}`

with

`RADIUS_E=1`

`DIAMETER_E=1`

`PERIMETER_E=1`

`AREA_E=1`

simultaneously.

Canonical foundation:

`definitions/ENTERPRISE_INITIAL_CIRCLE_ALL_UNIT_INVARIANTS_20260817.md`.

## What is preserved

The following remain valid research infrastructure:

- `VOID_E=∅` external non-coordinate start;
- `VOID_E -> O_E=±1` unique first existence step;
- complete all-shortest-path segment ontology;
- spatial-tail graph distance after the origin;
- existence levels `LEVEL_E(n)`;
- D6/reversal;
- no-native-zero firewall;
- historical circle results remain open to retyping/supersession.

## What changes

The replacement task must treat the four base circle invariants as co-primitive initial calibrations.

It may not:

- derive `D_E(1)` from `R_E(1)` or conversely;
- derive perimeter from radius/diameter by pi;
- derive area from radius by pi or square by fiat;
- import classical `D=2R` at any scale;
- privilege one invariant as the generator of the other three without an Enterprise-native theorem.

The replacement task must derive four higher-circle propagation laws independently first, and only then audit relations among them.

## Stop rule

Old owner branch:

`research/r059d-stage-at-reissue2-origin-diameter-one-circle-generation`

must stop under old semantics.
