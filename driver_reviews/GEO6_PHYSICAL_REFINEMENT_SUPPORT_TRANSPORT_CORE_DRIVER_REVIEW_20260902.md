# GEO6 Physical Refinement and Support Transport Core — Driver Review

Driver-ID: `EM-DVR-G6K2P9`  
Role: `RESEARCH_DRIVER`  
Task: `RS-GEO6-PHYSICAL-REFINEMENT-SUPPORT-TRANSPORT-CORE`  
Publication: `TP2-596ED944A7D5C5F8065B`  
Result: `RR-79C65C93052D6857CA9C`  
Disposition: `ACCEPTED`  
Terminal: `true`

## Decision

The hard target

`GEO6_PHYSICAL_SCALE_REFINEMENT_AND_SUPPORT_TRANSPORT_TYPED_OR_EXACTLY_OBSTRUCTED`

is satisfied by a current-language no-go plus an exact minimum interface classification.

The Result package is writer-conformant. Return, checker, certificate and execution record match the Result manifest Git blobs, and the branch changes exactly the five authorized paths. The original repository checks were prevented from reaching task-local execution by an unrelated decorated-carrier quarantine-head mismatch; no task-local defect was identified.

## Accepted mathematical strength

Current accepted P000/Full-Cell semantics does not determine a physical scale-refinement map. In particular, none of the following is sufficient by itself:

- a same-level equivalence or automorphism;
- a slice observation or reduct;
- a finite quotient or graph cover;
- Q17/Q20 abstract refinement grammars;
- Q22 return-profile/1-WL refinement;
- the name “rotation” without typed source, target and action laws.

Any admissible physical-refinement extension must at least provide the four declared interface obligations:

1. distinct typed Full-Cell levels and a non-equivalence state arrow;
2. a native nontrivial Cell-locality fiber map with a stated locality law;
3. an independent physical-scale witness not inferred from cover cardinality;
4. explicit state/locality update, preservation and composition laws.

The finite witness with two coarse Cells, four fine Cells and a fixed parity coarse projection is accepted: the same cover/projection admits exactly `256` compatible deterministic state sections. Thus cover data plus a coarse projection does not select a unique physical state-refinement arrow.

The support-pullback criterion is accepted at typed set-incidence strength. Given a Cell map `q:C_1→C_0`, total support transport with exact incidence pullback exists iff every inverse-image coarse incidence profile is representable by a fine support; it is unique when fine supports are extensional by incidence profile, and unique pullbacks compose contravariantly.

## Scope guards

The four clauses are accepted as necessary semantic interface obligations at the declared P000 typing boundary, not as a choice of Euclidean scale, homothety, bundle, connection or holonomy. The finite section count is an underdetermination witness, not a claim that native P000 states are bit strings.

No current native Cell–Support incidence is supplied by this Result. The pullback theorem is therefore a conditional downstream interface. No immediate native rotation/mixed lift is reopened; that frontier remains externally gated by its P000 owner.

## Selector consequence

- `LOCALITY_REFINEMENT_SELECTOR` remains unresolved.
- `PHYSICAL_REFINEMENT_SELECTOR` remains partially constrained, now with a complete current-language no-go and four-clause extension boundary.
- `REFINEMENT_TRANSPORT_SELECTOR` remains partially constrained and dependency-obstructed by physical refinement plus support incidence.
- the cross-refinement component of `MIXED_DIRECTION_SELECTOR` remains partially constrained.

The task is terminal. No direct successor should select a scale factor or transport law by fiat. Further work requires independently justified native P000 information outside this locked-P000 transfer task.