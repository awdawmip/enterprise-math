# Orphan Frontier Maintenance — R043 Addendum

Status: `DRIVER_CONTROL_PLANE_AUDIT / RECOVERABLE_RESEARCH_ORPHAN / NO_NEW_MATHEMATICS`

Driver-ID: `EM-DVR-ZX1UEJ`

Current-policy comparison snapshot: `main@0aa8824250c609283363c662ed875d661972dd43`

## Finding

The second-pass search over explicit `UNRESOLVED_NEEDS_NEW_TASK` records found a stronger orphan than the initially identified trace-frame candidate:

`RS-R043-NATIVE-SURFACE-FRONTIER-RECONSTRUCTION-MARKOV-CARRIER`.

This is a genuine recoverable research orphan because all four conditions hold:

1. a formal Driver-authored taskbook exists;
2. no runtime `CLAIM`, `PROGRESS`, `HANDOFF`, or `DONE` event exists for this exact task id;
3. the parent R041 result explicitly leaves the same bounded reconstruction/minimal-correlation question as its next frontier;
4. the later R044 semantic impact matrix independently classifies the R043 stationary-Markov/all-horizon carrier hypothesis as `UNRESOLVED_NEEDS_NEW_TASK`.

The old taskbook was not dispatchable under current policy because its metadata/digest predated the current origin/lineage/final-footer contract.

## Parent lineage

Exact parent:

`RS-R041-NATIVE-SURFACE-HORIZON-QUOTIENT-CALCULUS`.

R041 PR #527 / owner head

`688661e76255b3e86df6d5c69695f2932b650740`

establishes the positive horizon-indexed carrier candidate

`M_h = S + weighted induced graph on L0..L_{h-2}`

for the declared Boolean addition future, with recursive update to child `M_{h-1}`.

R041 also records that the stronger weighted-current-frontier-only state `G0` had no bounded abstract weighted-graph collision in the tested FCC/HCP atlases, but this remained diagnostic rather than a theorem. Its explicit next frontier is to kill/prove `G0 -> B3`, then isolate the first hidden L0-L1/shared-future-cell correlation if G0 fails.

Therefore R043 is semantically `CONTINUATION`, not `NEW_DIRECTION`.

## Current recovery action

The existing taskbook path has been re-reviewed on the orphan-maintenance branch rather than duplicated under a new route name:

`research_tasks/R043_NATIVE_SURFACE_FRONTIER_RECONSTRUCTION_MARKOV_CARRIER_20260812.md`.

Current recovery metadata:

- priority: `P1` so it does not preempt the currently active P0 consolidation gates;
- origin: `DRIVER_ROADMAP`;
- lineage: `CONTINUATION`;
- parent: `RS-R041-NATIVE-SURFACE-HORIZON-QUOTIENT-CALCULUS`;
- final-response identity: `INHERIT_GLOBAL`;
- policy digest: current `sha256:9c1f03a5086432f83d1a3821893be5589124293bc5be5b14d4b7e196220271c7` for the compared main snapshot;
- successor gate: complete, with proof/counterexample/strict-repair outcomes and hard kill conditions;
- generic quotient/BRC/symmetry mechanisms explicitly remain owned by existing Toolbox/upstream owners.

No runtime claim is emitted by this addendum. Dispatch should occur only after the re-reviewed taskbook is canonical/current and the ordinary dispatch audit passes at that time.

## Why this orphan is high value

The question is not another representation census. It asks whether an already exact horizon-indexed carrier can collapse further to a **stationary current-frontier state**.

A positive theorem would replace explicit exterior depth with a fixed-form recursively executable state.

A negative theorem is equally valuable if it identifies the first irreducible shared-future correlation and proves that a pair/hypergraph residual is necessary.

Either outcome directly informs the project-wide question:

`HOW_MUCH_LOCAL_RELATIONAL_STATE_IS_ACTUALLY_NEEDED_FOR_EXACT_FUTURE_EXECUTION?`

This is closer to the project's nontraditional-collapse frontier than simply enlarging FCC/HCP enumerations.

## Priority relative to other orphans

1. `R043` — **highest-value true research orphan**; formal continuation, exact parent residue, later audit explicitly says unresolved.
2. `SEGMENT_ROTATION_ORTHOGONAL_TRACE_FRAME` — promising but still candidate-lifecycle Phase B; semantic admission must be audited before a task is opened.
3. `P021` focusing/direction — possible owner-local residue only after narrowing away generic A2/A4/BRC/T7 content.
4. `P022` observation-history — real dormant handoff, but latest-owner lineage must be audited before reactivation.

Old P017/P024/P025 broad scheduler entries are not research opportunities; their exact historical ids were runtime-superseded during this maintenance sweep because later owner work already executed their semantic frontiers.

## Freeze

`FORMAL_TASK + NO_RUNTIME_EXECUTION + LATER_AUDIT_SAYS_UNRESOLVED + COMPLETE_SUCCESSOR_GATE = RECOVERABLE_RESEARCH_ORPHAN`.

`RECOVERABLE_RESEARCH_ORPHAN != AUTOMATIC_DISPATCH_PRIORITY`.

R043 is recovered as P1 behind active P0 consolidation gates, not used to bypass the current portfolio.
