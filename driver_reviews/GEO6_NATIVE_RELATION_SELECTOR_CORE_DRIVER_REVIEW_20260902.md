# GEO6 Native Relation Selector Core — Driver Review

Driver-ID: `EM-DVR-G6K2P9`  
Role: `RESEARCH_DRIVER`  
Task: `RS-GEO6-NATIVE-RELATION-SELECTOR-CORE`  
Publication: `TP2-46E1AB6359CBEBAA6B0D`  
Result: `RR-85C8C9CDB6C3A8B8622C`  
Disposition: `ACCEPTED`  
Terminal: `true`

## Decision

The hard target

`GEO6_NATIVE_RELATION_CORE_CONTACT_EXCLUSION_SUPPORT_TYPED_AND_CONVERSIONS_EXACTLY_CLASSIFIED`

is satisfied at the declared typed semantic boundary.

The Result package is writer-conformant. Its Return, deterministic checker, machine certificate and execution record match every Git blob frozen in the Result manifest. The research branch changes exactly the five authorized paths. Repository checks on the original research surface were blocked before task-local execution by an unrelated decorated-carrier quarantine-head mismatch; that baseline defect neither touches nor falsifies this Result.

## Accepted mathematical strength

Let `K` be a symmetric irreflexive Cell-contact relation, `X` a symmetric irreflexive Cell-exclusion relation and `I` a typed `Cell × Support` incidence.

At the current accepted P000/Full-Cell language:

- none of the six directed off-diagonal implications among `K`, `X` and `I` is forced;
- all six failures have same-carrier, same-readout, nontrivially `C4`-equivariant finite witnesses;
- for unrestricted independent finite relations on `|Cell|=n`, `|Support|=m`, the exact state count is `2^(2*binom(n,2)+n*m)`;
- consequently any lossless **local typed** representation must distinguish four `(K,X)` states on each unordered Cell pair and two incidence states on each Cell–Support slot;
- the five-signature atlas `SIG0`–`SIG4` correctly separates safe independent typing, role-tagged compression, a genuine typed witness-incidence mechanism, explicit Cell–Support self-duality, and explicit support-overlap conflict semantics.

`SIG2` is accepted only as the smallest identified common-mechanism **extension interface**: its role map retains enough information to project to all three selectors and therefore does not prove selector equivalence. `SIG3` and `SIG4` are exact conversion theorems only after their additional typed laws are separately supplied.

## Scope guards

This review does not assert that no future P000 extension can relate contact, exclusion and support. The information lower bound is accepted for the unrestricted independent Boolean relation model and lossless local typed encodings; it is not a universal lower bound against every nonlocal or hypothesis-restricted representation.

Carrier contact is not promoted to native contact. A conflict graph is available only after native exclusion is typed. FCA/Galois closure is available only after native Cell–Support incidence is typed. No self-duality, support-overlap conflict law, Working Truth, Foundation authority, native ontology elevation or novelty certificate is granted.

## Selector consequence

- `CONTACT_SELECTOR` remains `PARTIALLY_CONSTRAINED_BY_ACCEPTED_DATUM`.
- `NONOVERLAP_SELECTOR` becomes `PARTIALLY_CONSTRAINED_BY_ACCEPTED_DATUM` by the exact current-language independence/no-resolver boundary.
- `SUPPORT_RELATION_SELECTOR` becomes `PARTIALLY_CONSTRAINED_BY_ACCEPTED_DATUM` by the same boundary.
- `SELF_DUAL_IDENTIFICATION_SELECTOR` remains unresolved.

The task is terminal. Do not publish a renaming successor for `SIG0` or `SIG1`; reopen only after independently accepted native data supplies one of the missing relation/self-duality/conflict laws or a strictly stronger compression theorem.