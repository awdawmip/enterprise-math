# Quadratic Packet — Observable-Complete Self-Composition Candidate Phase-B Audit

Status: `PHASE_B COMPLETE / AUDITED_AXIOM_CANDIDATE / INDEPENDENT REPLICATION RECOMMENDED`

Candidate-ID: `AX-QP-OCSC-20260825`

Researcher-ID: `EM-FREE-5K7N2Q`

Candidate freeze:

`research_freezes/QUADRATIC_PACKET_OBSERVABLE_COMPLETE_SELF_COMPOSITION_AXIOM_CANDIDATE_FREEZE_20260825.md@087c18666a0c727274cef208974fd9f9e1d8cd93`

Foundation snapshot audited:

`awdawmip/enterprise-math main@1c71c3ee6c4fb483c27f2f72e445ccc83a392824`

## 1. Candidate after Phase-B narrowing

The discovery packet initially arose from the three relation-level labels NC1–NC3. Phase B separates their roles.

- **NC1 / one-clock unique unresolved source** is best treated as a task-local sector declaration specifying what `one-clock` means for the finite deterministic collapse object. It is not proposed here as a universal Foundation axiom.
- **NC2 / well-founded downward exhaustion** is likewise a task-local typing condition for the word `downward precision collapse`. Current Enterprise already has general well-founded finite stabilization machinery, but current PF-N0 does not require every operation to have this type.
- **NC3 / observable-complete self-composition** is the genuine surviving axiom candidate. It excludes a proper nonempty intermediate residual layer inside a sector already declared primitive, one-clock, downward, and complete at its observable state capacity.

Therefore the audited candidate is **not** `NC1+NC2+NC3 as three new universal axioms`.

The audited candidate is:

`AX-QP-OCSC-20260825 = OBSERVABLE_COMPLETE_SELF_COMPOSITION_FOR_PRIMITIVE_ONE_CLOCK_DOWNWARD_PRECISION_COLLAPSE`.

## 2. Dedup against current Enterprise Math

### 2.1 E002 predictive quotient

Current component `EM-COMP-E002-PREDICTIVE-QUOTIENT` adopts Moore-style behavioral distinguishability and Hopcroft-style finite automaton minimization. It constructs the coarsest predictive partition for a declared future language and stable action congruence.

This does **not** imply NC3.

A finite chain of distinct remaining depths can be perfectly predictive: if terminal/no-terminal behavior is observable at future horizons, different depths remain behaviorally distinguishable states in the minimal predictive automaton. Predictive minimization preserves those necessary distinctions; it does not force the chain height to two.

Verdict: `RELATED PRIOR METHOD / NOT DUPLICATE / NOT IMPLYING NC3`.

### 2.2 P023 composition-safe quotient

Current component `EM-COMP-019` requires legal information loss to factor declared operations and preserve future-compatible quotient behavior.

This also does **not** imply NC3. A quotient may be fully composition-safe while retaining several composition-distinct residual-depth states. Operation factorization constrains which distinctions may be forgotten; it does not require the observable state count to be minimal relative to a one-clock declaration.

Verdict: `RELATED SAFE-QUOTIENT PRINCIPLE / NOT DUPLICATE / NOT IMPLYING NC3`.

### 2.3 P020 well-founded finite stabilization

Current P020 machinery proves finite stabilization for suitable reductive/well-founded maps and packages the terminal stabilization as an idempotent operator.

It supports the mathematical legitimacy of NC2-style downward typing, but it places no two-step bound on the transient tower.

A length-three chain is well-founded.

Verdict: `NC2 HAS EXISTING GENERAL MATHEMATICAL SUPPORT / NC3 NOT DERIVED`.

## 3. Contradiction against current Foundation

Bare `PACKET_PATH_FOUNDATION` does not require every native relation or operation to be a precision collapse, one-clock, deterministic, or two-level.

Canonical Boolean BRC is support-level and may discard multiplicity/provenance under different semantics.

Therefore universalizing NC3 to all N0/BRC would conflict with current typing discipline.

The candidate avoids that conflict by living at:

`N1_DERIVED_OPERATIONAL_SEMANTICS`

for a separately declared primitive one-clock downward precision-collapse sector.

Verdict: `NO CONTRADICTION AT TYPED SECTOR STRENGTH / UNIVERSAL N0 VERSION REJECTED`.

## 4. Axiom versus derived theorem

The following consequences are already theorems once the sector semantics are granted:

- free linearization rank identity `rank(E^j)=|R_j|`;
- NC1 -> corank one;
- NC2 -> strict rank drop;
- NC3 -> rank dichotomy;
- NC1+NC2+NC3 -> `E^2=0`, rank two, dual-number one-chain envelope.

Those consequences are **derived**, not axiom candidates.

What is not currently derivable from Foundation is the semantic exclusion of a proper nonempty intermediate residual layer at the declared primitive one-clock capacity.

Verdict: `NC3 SEMANTIC REQUIREMENT REMAINS AXIOM-LIKE / RANK CONSEQUENCES DERIVED_NOT_AXIOM`.

## 5. Implementation-carrier audit

The candidate statement uses only finite states, a pointed terminal state, repeated application of one declared operation, residual-state sets, and type-preserving relabeling.

It does not require:

- a matrix basis;
- a vector space;
- a module/ring;
- dual numbers;
- a coordinate embedding;
- a norm or metric.

The free linearization is a downstream N2 proof/readout carrier. It can be replaced by pure finite-set cardinality arguments for the rank-two conclusion.

Verdict: `NOT AN IMPLEMENTATION-CARRIER ARTIFACT`.

## 6. Prior-art / external analogue classification

No historical novelty claim is made for finite-state minimization, behavioral equivalence, partition refinement, bisimulation-like future equivalence, or well-founded finite iteration.

Current repository lineage already attributes the relevant generic mechanisms to established automata/state-minimization and partition-refinement prior art, including Moore, Hopcroft, and Paige–Tarjan families.

The candidate's project-specific question is narrower:

> whether the **primitive one-clock minimum-capacity precision-collapse semantics** should reject an otherwise predictive/composition-safe intermediate residual-depth state as excess precision rather than merely retain it as another automaton state.

This is a Foundation-selection question, not a claim of inventing state minimization.

Verdict: `PRIOR-ART ANALOGUES PRESENT / NOVELTY NOT CLAIMED / CANDIDATE QUESTION SURVIVES AS PROJECT SEMANTIC CHOICE`.

## 7. Counterexample and small-model pressure

### 7.1 Remove one-clock typing

Two disjoint terminal chains provide multiple unresolved sources. Their free linearization has cokernel dimension greater than one.

This confirms NC1 carries real content but does not justify universalizing it.

### 7.2 Remove downward typing

A unique source feeding a nonzero recurrent/fixed state can preserve a nonzero same-rank residual under iteration. This kills strict rank drop while retaining a one-clock-looking source.

This confirms NC2 is the semantic distinction between precision loss and persistent transport/memory.

### 7.3 Keep one-clock and downward; allow intermediate residual

The length-three chain

`x_2 -> x_1 -> x_0 -> 0`

is finite, deterministic, one-source and well-founded. It has

`R_1={x_1,x_0}`,

`R_2={x_0}`.

Thus it is the exact pressure case for NC3.

It is also predictive and composition-safe if all three states are admitted as distinct observable states. Therefore E002/P023 do not kill it.

The candidate kills it **only** if the declared semantics insist that a primitive one-clock sector is already at its minimal observable precision capacity and may not expose a new residual-depth coordinate under self-composition.

This is not resolved by same-context algebra. It is the exact semantic question requiring independent audit.

Verdict: `J3 IS NOT A MATHEMATICAL COUNTEREXAMPLE TO NC3; IT IS THE DECISIVE SEMANTIC DISCRIMINATOR`.

## 8. Semantic-layer recheck

Strongest valid typing:

- PF-N0 supplies finite discrete relational substrate only;
- NC1 and NC2 are declared N1 sector predicates;
- NC3 is an `AUDITED_AXIOM_CANDIDATE` about minimal observable state capacity under repeated self-composition;
- free linearization/rank/dual-number envelope are N2 consequences;
- no Foundation mutation occurs in this audit.

A claim that NC3 is already N0-native would fail the current native-admissibility gate.

## 9. Relation to active routes after freeze

Only after the Phase-A candidate freeze is the active quadratic-packet route reintroduced for comparison.

The candidate explains the missing semantic premise behind the independently verified QP-R2 rank-two theorem and the higher-jet obstruction:

- the quadratic rank package has no intermediate residual precision;
- higher `J_m`, `m>2`, has genuine intermediate residual/contact layers;
- current Cech/higher-jet work detects the same hidden-precision phenomenon at a later algebraic/readout layer.

This relation strengthens relevance but is discovery/adaptive evidence, not independent validation.

## 10. Phase-B classification

Final Phase-B classification:

`AUDITED_AXIOM_CANDIDATE`.

Exact surviving object:

`AX-QP-OCSC-20260825 / OBSERVABLE_COMPLETE_SELF_COMPOSITION_FOR_PRIMITIVE_ONE_CLOCK_DOWNWARD_PRECISION_COLLAPSE`.

This classification means:

- not falsified by a contradiction in its typed scope;
- not already present under the checked current Enterprise principles;
- not merely the linearization artifact;
- not already a theorem of current Foundation;
- not validated independently;
- not Working Truth;
- not Foundation-admitted.

## 11. Independent-replication target

The recommended independent audit must attack, before seeing the originating proof:

1. whether `primitive`, `one-clock`, `minimal observable capacity`, and `self-composition complete` can be defined without smuggling NC3 into the definitions;
2. whether a defensible future-sufficient/composition-safe `J_3`-like model satisfies all weaker intended semantics and therefore refutes or narrows the candidate;
3. whether NC3 follows from a strictly weaker condition such as diagonal kernel-pair saturation or minimum sufficient predictive quotient plus a separately justified capacity law;
4. whether NC3 should be weakened to a quotient/fiber condition that still yields the required rank dichotomy;
5. whether the linearization faithfully reflects the relation semantics at exactly the claimed strength.

Recommended control-plane action:

`INDEPENDENT_REPLICATION_REQUESTED`.

The current researcher does not possess Driver authority to issue the official READY taskbook in this conversation.
