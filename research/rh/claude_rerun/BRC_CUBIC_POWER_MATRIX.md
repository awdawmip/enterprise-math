# BRC_CUBIC_POWER_MATRIX — exact log-quadratic reduction and monotonicity boundary

Task: `RS-RHR-CLAUDE-RH-RERUN-20260811`  
Researcher-ID: `RHR-9Q6M2K`  
Driver: `EM-DVR-7Q4K2C`

Status:

`CUBIC_POWER_MATRIX_REDUCTION / LOG_QUADRATIC_STRUCTURE_EXPOSED / MONOTONICITY_ONLY_KILLED / LOG_QUADRATIC_SIGN_FRONTIER_OPEN / RH_NOT_CLOSED`

## 1. Exact cubic mixed kernel

Let `D=r-1`, `tau>0`, and let `a3` be the retained centered cubic coefficient. After
removing positive row/column-only factors from the reversed-Hankel comparison block, the exact
mixed exponent is

`L(i,j)=(-tau+6D a3) i j - 3a3(i^2 j+i j^2)`.

Define

`boxed:
 X_i=exp(-tau*i/2+3a3*i*(D-i)).`

Then a direct calculation gives

`boxed: exp(L(i,j))=X_i^j X_j^i.`

Thus the cubic comparison determinant is exactly the determinant of the symmetric power matrix

`M(X)=[X_i^j X_j^i]_(0<=i,j<r)`.

This is equivalent to the Lorentzian feature lift in `BRC_FEATURE_LIFT_INERTIA.md`, but exposes
a different finite invariant.

## 2. Exact log-quadratic structure

Write

`A=exp(-tau/2+3Da3)`,
`B=exp(-3a3)`.

Then

`X_i=A^i B^(i^2)`.

Equivalently, the adjacent ratios satisfy

`X_(i+1)/X_i=exp(-tau/2+3a3(D-2i-1))`

and the second difference is constant:

`Delta_i log(X_(i+1)/X_i)=-6a3`.

So the relevant power matrix is not generated by an arbitrary decreasing sequence; its log
coordinates lie on an exact quadratic curve.

## 3. Monotone linear window

If

`6(r-1)|a3|<tau/2`,

then every adjacent logarithmic ratio is negative and

`X_0>X_1>...>X_(r-1)>0`.

The stronger certified Xi condition from the feature-lift checkpoint,

`12r |a3|/tau<1`,

implies this immediately. Combined with the published `d=3` coefficient bound, the sufficient
explicit linear window

`k>4,608,000 r`

therefore has a strictly decreasing log-quadratic power sequence.

## 4. Exact negative boundary: monotonicity alone is insufficient

The statement

`X_0>...>X_(r-1)>0 => sign det[X_i^j X_j^i]=(-1)^(r(r-1)/2)`

is false.

At rank `r=4`, take the strictly decreasing rational sequence

`X=(11/12, 10/11, 9/10, 7/8)`.

Exact fraction arithmetic gives

`det[X_i^j X_j^i]
 = -560198940167/50155641372672000000 < 0`,

whereas the q-Pascal sign at order four is positive.

Therefore any successful cubic theorem must use more than ordered feature coordinates. In
particular, the exact log-quadratic relation between successive ratios is future-relevant BRC
metadata and cannot be collapsed to monotonicity.

## 5. Log-quadratic sign frontier

This leaves the precise finite theorem target

`LOG_QUADRATIC_POWER_SIGN`:

> for `X_i=A^i B^(i^2)>0` with all adjacent ratios `<1`, determine whether the full consecutive
> determinant `det[X_i^j X_j^i]` always has q-Pascal sign
> `(-1)^(r(r-1)/2)`, or characterize its first failure region.

This target is strictly narrower than `LORENTZIAN_FOCK_DOMINATION` and could supply a direct
cubic inertia theorem if positive.

A bounded exact-rational search over small rational `A,B`, through ranks `r<=6`, found no sign
failure whenever the generated `X_i` were strictly decreasing. This is `EVIDENCE_ONLY` and not
a theorem.

## 6. Why this is promising for BRC

The carrier hierarchy has now exposed three equivalent exact descriptions of the retained cubic
state:

1. a centered cubic polynomial in the Hankel coordinate;
2. a two-feature Lorentzian metric;
3. a decreasing log-quadratic power sequence `X_i=A^iB^(i^2)`.

BRC can choose the cheapest representation whose declared future operation is safe. A proof of
`LOG_QUADRATIC_POWER_SIGN` would let the cubic branch recoalesce without tracking the full signed
Fock expansion; a counterexample would instead identify the missing curvature/correlation token
needed by the next refinement.

Final status: `RH_NOT_CLOSED`.
