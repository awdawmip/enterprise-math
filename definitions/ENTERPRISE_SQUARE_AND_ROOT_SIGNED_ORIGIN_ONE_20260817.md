# 进取平方与平方根：signed-origin-one 校正版 — 历史证明链

Status: `PARTIALLY_SUPERSEDED / HISTORICAL_PROVENANCE`
Original date: `2026-08-17`
Reconciled: `2026-08-21`

本文件曾在 signed-origin-one ontology 下恢复标量平方关系 `ENTERPRISE_SQUARE(n)=n^2` 与 `ENTERPRISE_ROOT(n^2)=n`。

当前三正轴 / origin=0 foundation 已 supersede 其坐标与几何证明前提，因此：

- `+1≡-1≡O_E` 不再 current；
- native zero 不存在不再 current；
- signed negative native coordinates 不再 current foundation；
- 旧 unit quadrilateral / initial circle 证明不再作为当前 foundational proof。

但标量代数关系本身可以在当前 sector-local squared norm 中独立保留：

`r^2 <-> r`。

当前 richer square-root definition：

`definitions/ENTERPRISE_PATH_VALUED_SQUARE_ROOT_OPERATOR_20260821.md`。

它在 `L_E^2=a^2+b^2` 的当前原生 right-sector 语义下，把 scalar root `r` 扩展为全部 component-root branches `(a,b)` 与其 native path fibers。

因此本文件仅保留“旧证明为何曾恢复 n^2”的 provenance，不再是 current canonical foundation。原始完整文本保留在 Git 历史中。
