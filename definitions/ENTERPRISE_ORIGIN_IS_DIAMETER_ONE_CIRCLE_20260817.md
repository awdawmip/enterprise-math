# 进取圆基态：原点本身是直径 1 的圆

Status: `ACTIVE / CANONICAL / FOUNDATIONAL_SUPERSESSION`
Date: `2026-08-17`
Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`
Depends on:
- `definitions/ENTERPRISE_VOID_ORIGIN_EXISTENCE_GEODESIC_20260817.md`
- `definitions/ENTERPRISE_SIGNED_ORIGIN_ONE_COORDINATE_20260817.md`

## 1. Foundational freeze

Freeze the user's foundational statement:

`THE_ORIGIN_ITSELF_IS_THE_DIAMETER_ONE_ENTERPRISE_CIRCLE`.

Let

`O_E=[+1]=[-1]`

be the first occupied/native coordinate state generated from the external pre-coordinate void state `VOID_E=∅` by the unique first existence step

`VOID_E -> O_E`.

Define the first Enterprise circle by

`CIRCLE_E(1) = {O_E}`.

Freeze its Enterprise diameter:

`DIAMETER_E(CIRCLE_E(1)) = 1`.

Thus the Enterprise circle family does not begin with a diameter-zero point-circle. The first existing coordinate state is already the first nonzero-diameter circle.

## 2. Diameter typing

`DIAMETER_E` is not, by definition, the ordinary graph-theoretic diameter of the set of currently visible native vertices.

For the singleton set `{O_E}`, ordinary internal graph diameter would be zero, but the frozen Enterprise diameter is one because the circle is typed by its existence/generation scale from `VOID_E`.

Therefore freeze the type distinction:

`ENTERPRISE_CIRCLE_DIAMETER != INTERNAL_PAIRWISE_GRAPH_DIAMETER` unless a later theorem proves equality in a declared regime.

At the base state:

- internal pairwise graph diameter of `{O_E}` = external count `0`;
- Enterprise circle diameter = native/generative scale `1`.

The symbol `0` in the first bullet is an external cardinal/distance value only; it is never a native coordinate.

## 3. Relation to existence length

The current existence-length foundation gives

`ELL_E(O_E)=1`.

At the base state this agrees exactly with the Enterprise circle diameter:

`DIAMETER_E(CIRCLE_E(1)) = ELL_E(O_E) = 1`.

This equality is frozen only at the base circle. A later theorem must determine whether the full circle family satisfies

`DIAMETER_E(CIRCLE_E(n)) = n`

for all `n>=1`, or whether another native diameter law emerges.

Do not assume a classical radius `1/2` for `CIRCLE_E(1)`. Enterprise radius/diameter relations must be derived independently and may differ from classical Euclidean conventions.

## 4. Circle generation viewpoint

The foundational sequence is now typed as

`VOID_E -> CIRCLE_E(1)={O_E} -> higher Enterprise circle states`.

The first step is simultaneously:

1. the first existence step;
2. the creation of the coordinate origin;
3. the creation of the first Enterprise circle;
4. the assignment of circle diameter one.

This does not make `VOID_E` a center point inside native geometry. `VOID_E` remains outside the coordinate graph.

## 5. Consequences for fixed-length shells

The prior void-first geodesic foundation defines

`EXISTENCE_SPHERE_E(n)={P:ELL_E(P)=n}`.

It is now frozen that

`EXISTENCE_SPHERE_E(1)={O_E}=CIRCLE_E(1)`.

For `n>=2`, whether

`EXISTENCE_SPHERE_E(n)=CIRCLE_E(n)`

as the complete Enterprise circle of diameter `n` is an active theorem question, not an axiom.

The first-circle boundary condition is therefore fixed before any higher-level circumference or circle-constant law is derived.

## 6. Historical typing impact

Any historical Enterprise circle construction whose base object treats the origin as a diameter-zero/radius-zero degenerate circle must be re-audited.

Do not protect historical `kappa_E`, circumference, radius, support-frontier, or turn-orbit interpretations from an exact contradiction with this base-circle axiom.

Algebraic/combinatorial identities may remain valid under a retyped object even if their circle interpretation changes.

## 7. Current canonical summary

Freeze:

`VOID_E = external non-coordinate pre-existence`.

`VOID_E -> O_E` is the first existence step.

`O_E=+1≡-1` is the coordinate origin and first occupied state.

`CIRCLE_E(1)={O_E}`.

`DIAMETER_E(CIRCLE_E(1))=1`.

`0` is not a native coordinate.

Higher Enterprise circles and their diameter/radius/circumference relations must be re-derived from this base state rather than imported from classical geometry.
