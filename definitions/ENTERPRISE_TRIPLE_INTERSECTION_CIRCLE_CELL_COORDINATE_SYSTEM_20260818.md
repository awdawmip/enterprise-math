# 进取平面坐标系重基：三圆交点原点、圆 Cell 与交点数轴

Status: `ACTIVE / CANONICAL / FOUNDATIONAL_SUPERSESSION`
Date: `2026-08-18`
Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`

This definition supersedes the prior signed-origin-one / no-native-zero coordinate foundation and the interpretation of the origin itself as the initial circle.

## 1. Native origin

The native Enterprise plane origin is the real geometric intersection point

`O_E = 0`.

Freeze:

`ENTERPRISE_NATIVE_ORIGIN = 0`.

The former identification `O_E=[+1]=[-1]` is historical and superseded as the native coordinate origin.

The former firewall `ZERO_IS_NOT_AN_ENTERPRISE_COORDINATE` is superseded.

## 2. Cells are circles

Every native geometric cell is a circle.

Freeze:

`ENTERPRISE_CELL = CIRCLE`.

The origin is not a cell center and is not itself a cell. It is a common intersection point of three cells.

Freeze:

`ORIGIN_IS_TRIPLE_CELL_INTERSECTION`.

`ORIGIN_IS_NOT_CELL_CENTER`.

`ORIGIN_IS_NOT_A_CELL`.

The previous `CIRCLE_E(1)={O_E}` ontology is superseded.

## 3. First generation around the origin

Let the first three cell centers be `C_1,C_2,C_3`.

Freeze the first-length calibration

`dist(O_E,C_i)=1`, for `i=1,2,3`.

The three first-generation centers are symmetrically arranged and divide the surrounding plane into three equal sectors. Equivalently, their center rays from the origin are separated by `120 degrees` in the current planar realization.

Because the origin lies on each of these three circles, the first-generation cell radius is also 1.

Thus the primitive geometric calibration is now:

`ORIGIN_TO_FIRST_CELL_CENTER = FIRST_CELL_RADIUS = 1`.

This replaces the previous co-primitive claim `(R,D,P,A)=(1,1,1,1)` for an origin-circle.

## 4. Intersecting packing, not tangent packing

The Enterprise plane is generated by continuing the circle-cell arrangement outward by intersection rather than by close tangency.

The intended local incidence rule is that each native coordinate intersection event is shared by three circle cells.

Freeze the structural target:

`NATIVE_COORDINATE_VERTEX = TRIPLE_CIRCLE_CELL_INTERSECTION`.

The continuation is not a tangent-circle packing and contains no coordinate-defining void/gap center.

Freeze:

`TANGENT_PACKING_IS_NOT_THE_NATIVE_ENTERPRISE_PLANE`.

`VOID_GAP_CENTER_IS_NOT_THE_NATIVE_ORIGIN`.

## 5. Three number axes / six directions

Let the center rays be `O_E C_1`, `O_E C_2`, `O_E C_3`.

The three native number axes are the three lines through `O_E` that bisect the angular sectors determined by the three center rays.

Each line has two orientations, giving six native axis directions.

Freeze:

`ENTERPRISE_NATIVE_AXES = THREE_BISECTOR_LINES_THROUGH_ORIGIN`.

`ENTERPRISE_NATIVE_DIRECTIONS = SIX_ORIENTED_RAYS`.

Critically, the number axes do not pass through any circle-cell center.

Freeze:

`NATIVE_NUMBER_AXIS_NEVER_PASSES_THROUGH_CELL_CENTER`.

The number axes pass through the intersection-point network. Coordinate marks on the axes are intersection events, not cell centers.

Freeze:

`AXIS_COORDINATE_EVENTS_ARE_CELL_INTERSECTIONS`.

## 6. Point layer and cell layer

The new native plane has two distinct geometric object types:

1. `CELL`: circle regions/objects;
2. `COORDINATE_VERTEX`: triple intersections of circle cells.

The coordinate network is carried by intersection vertices and the number axes through them; cell centers are auxiliary geometric generators, not axis coordinates.

Freeze:

`CELL_CENTER != NATIVE_COORDINATE_VERTEX`.

`CELL != COORDINATE_VERTEX`.

## 7. Consequence for vector coordinates and length

The prior three-axis theory assumed three pairwise `ENTERPRISE_ORTHOGONAL` vector axes and therefore froze

`||V||_E^2=v_1^2+v_2^2+v_3^2`

for three independent orthogonal vector components.

The new native coordinate axes are planar three-axis/six-direction bisector lines and are not three independent pairwise orthogonal Cartesian axes.

Therefore distinguish:

- `ORTHOGONAL_VECTOR_COMPONENT_FORMULA`: may remain valid for an independently constructed orthogonal vector basis;
- `NATIVE_THREE_AXIS_COORDINATE_METRIC`: reopened and must be derived from the new intersection-cell geometry.

Freeze:

`NATIVE_AXIS_COORDINATES_ARE_NOT_AUTOMATICALLY_ORTHOGONAL_VECTOR_COMPONENTS`.

`NATIVE_THREE_AXIS_LENGTH_FORMULA = OPEN`.

Do not directly apply `sqrt(x^2+y^2+z^2)` to the new native three-axis coordinate labels until their coordinate constraint / Gram form is derived.

## 8. Consequence for discrete rotation

The discrete-state principle survives and becomes simpler to type:

`ROTATING_SEGMENT_NATIVE_STATE = ONE_CIRCLE_CELL_PER_TRAJECTORY_STEP`.

Intersection vertices provide transition/incidence events between circle cells. A trajectory may branch if several next circle cells are equally legitimate, but each branch occupies one circle cell at each step.

Freeze:

`INTERSECTION_VERTEX = CELL_TRANSITION_EVENT_NOT_CELL_STATE`.

## 9. Superseded foundations

Superseded as current native foundation:

- `O_E=[+1]=[-1]`;
- `ZERO_IS_NOT_AN_ENTERPRISE_COORDINATE`;
- native axis `...,-3,-2,±1,+2,+3,...` as the primary plane coordinate ontology;
- `CIRCLE_E(1)={O_E}`;
- `(R,D,P,A)=(1,1,1,1)` as the origin-circle calibration;
- treating all three native axis coordinates as pairwise-orthogonal vector components;
- tangent close packing / gap-center origin candidates.

Historical results depending on these assumptions remain archived but require retyping before reuse.

## 10. Canonical summary

Freeze the new coordinate construction:

`ORIGIN 0 = TRIPLE CIRCLE-CELL INTERSECTION`.

`EVERY CIRCLE = CELL`.

`FIRST THREE CELL CENTERS ARE DISTANCE 1 FROM ORIGIN`.

`THREE CENTER RAYS DIVIDE LOCAL SPACE EQUALLY`.

`THREE NUMBER AXES ARE THE BISECTOR LINES THROUGH ORIGIN`.

`NUMBER AXES NEVER PASS THROUGH CELL CENTERS`.

`NUMBER AXES PASS THROUGH THE INTERSECTION NETWORK`.

`NATIVE COORDINATE VERTICES ARE TRIPLE CELL INTERSECTIONS`.

`NATIVE THREE-AXIS METRIC MUST BE REDERIVED FROM THIS GEOMETRY`.
