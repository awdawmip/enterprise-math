# 象限 / 符号 chamber 代数向量运算 — 历史路线

Status: `SUPERSEDED / HISTORICAL_PROVENANCE`
Original date: `2026-08-17`
Reconciled: `2026-08-21`

本文件原先以 signed-origin-one、六方向/符号 chamber 为基础冻结局部向量运算。其 theorem-critical dependencies 已被当前三正轴 foundation supersede，因此不能继续标为当前 foundational authority。

当前原生平面：

- `O_E=0`；
- `THREE_POSITIVE_NATIVE_RAYS`；
- `ENTERPRISE_RIGHT_ANGLE=120_DEGREES`；
- `A_E={(a,b,c) in N_0^3:min(a,b,c)=0}`；
- 三个正二轴扇区 chart `S_12,S_23,S_31`。

当前局部代数若要成立，应在上述 sector atlas 内重新表达，而不是使用旧 signed chamber。

保留原则：**局部 chart 运算可以存在，跨 chart 需要显式 gluing/transition；实现坐标不能自动晋升为 native geometry。**

本历史文件中的具体 signed-coordinate formulas 统一状态为 `REQUIRES_CURRENT_FOUNDATION_REDERIVATION`。原始完整文本保留在 Git 历史中。
