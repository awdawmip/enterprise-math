# Enterprise Math — FREE_AXIOM_DISCOVERY Primitive Substrate Router

Status: `ACTIVE / CANONICAL FREE-DISCOVERY SUBSTRATE ROUTER / P000-BOUND / WEIGHTED-BRC-GLOBAL-TYPING`
Effective: `2026-09-02`
Research mode: `FREE_AXIOM_DISCOVERY`
Classification: `PRIMITIVE SUBSTRATE + USER-PROMOTED GLOBAL BRANCH TYPING / INFORMATION-PARTITION ROUTER`

## Purpose

Give a free researcher the **current primitive commitments** without also giving it the project's current successful downstream constructions or a menu of what kind of axiom it should look for.

This file partitions information into:

`P000 ROOT AXIOM + PHASE-A PRIMITIVE SUBSTRATE + MINIMAL GLOBAL BRANCH TYPING`

versus

`PHASE-B CURRENT-RESULT / THEOREM / TOOL COMPARISON KNOWLEDGE`.

Freeze:

`FOUNDATION_FOR_DISCOVERY != CATALOG_OF_CURRENT_ACHIEVEMENTS`.

`P000_ASSUMPTION_PRECEDES_QUESTION_GENERATION=true`.

`GLOBAL_WEIGHTED_BRC_TYPING_PRECEDES_BRANCH_ERASURE=true`.

`PRIMITIVE_SUBSTRATE -> RESEARCHER_GENERATES_ITS_OWN_QUESTION`.

`NO_DEFAULT_DISCOVERY_LENS_MENU`.

## P000 — mandatory, locked, not a discovery target

Before free question generation load `../p000_reality_foundation.json`.

FREE research must assume:

`REALITY_DIMENSION=7`.

`ENTERPRISE_SPACE_DIMENSION=6`.

`ENTERPRISE_TIME_DIMENSION=1`.

`ENTERPRISE_SPACE_KIND=DISCRETE_CELL_SPACE`.

`ENTERPRISE_PLANE_NATIVE_DIMENSION=6`.

`CURRENT_THREE_AXIS_MODEL=RESEARCH_SLICE_OF_6D_SPACE`.

`ENTERPRISE_GEOMETRY_PRIMARY_TRANSFORMATION=ROTATION`.

`TIME_ROLE=TRACE_AND_ORDER_OF_RELATIONAL_CHANGE`.

Machine boundary:

`FREE_MAY_CHALLENGE_P000=false`.

`P000_PROOF_OBLIGATION=NONE`.

`P000_FALSIFICATION_ROUTE=DISABLED`.

`P000_REPLACEMENT_CANDIDATE_ROUTE=DISABLED`.

`P000_RESEARCH_BUDGET_FOR_PROVING_OR_DISPROVING=ZERO`.

P000 is not a downstream current result and is not hidden in Phase A. It is the root premise from which free discovery begins. Only an explicit direct user worldview change may supersede it.

## Universal Weighted-BRC branch typing — mandatory background, not an agenda

By explicit user promotion on 2026-09-02, the minimal branch-typing commitments in

`definitions/ENTERPRISE_BRC_WEIGHTED_GLOBAL_SUBSTRATE_20260902.json`

are part of **all** Enterprise Math research foundations, including FREE Phase A.

A FREE researcher therefore starts with the following type discipline:

- Boolean result support, positive weighted multiplicity/mass, provenance, and signed/amplitude information are different carriers unless an explicit theorem identifies them;
- if the self-generated problem itself contains several branch representatives and declares their multiplicity or positive weights material, that data is not silently discarded to Boolean support before checking what is lost;
- for finite positive-rational branch families, the exact shared summary `(C,W,M)` = supported path count / total positive mass / dominant path mass is available as a background carrier;
- `E=W/M` and `Delta=LN(E)` are derived coordinates, not primitive native state and not a reason to numerical-evaluate logarithms during discovery;
- positive Weighted-BRC cannot replace signed/amplitude cancellation or reflection-odd orientation after positive recoalescence;
- one deterministic positive path has `C=1`, `W=M`, `E=1`, `Delta=0`, so deterministic problems are not forced into a multiplicity interpretation;
- canonical R023 Boolean support remains the base support semantics;
- no general SCC / Perron-Frobenius / spectral-radius theorem is supplied by the one-state recurrent result.

Anti-anchoring freeze:

`GLOBAL_BRANCH_TYPING != SUGGESTED_BRC_QUESTION`.

`MINIMAL_CWM_BACKGROUND != PRELOAD_OF_WEIGHTED_BRC_SUCCESS_CATALOG`.

`SELF_GENERATED_PROBLEM_FIRST -> DETAILED_THEOREM_OR_TOOL_LOOKUP_SECOND`.

Do **not** show the detailed Weighted-BRC theorem IDs, prior successful arithmetic/selector/holonomy applications, or a menu of Weighted-BRC research directions before a FREE Phase-A question has independently arisen. If the question naturally contains the relevant branch structure, the exact definitions may be used without waiting for Phase B because their minimal type is now project substrate; detailed current-result comparison and tool-dedup remain Phase-B functions.

## Phase-A project commitments

A FREE researcher may start from these current project-level commitments:

- P000 above;
- the universal Weighted-BRC branch-typing substrate above;
- finite resolution / precision is endogenous to the research ontology rather than an afterthought;
- integer/discrete/computable structure is preferred at the native layer;
- `N = N_0` includes zero where the natural-number substrate is used;
- definitions are not inherited merely because an effective/classical theory succeeds;
- implementation carriers and readouts are typed separately from native relational claims;
- discovery-stage arithmetic sampling preserves the full declared integer population before stratification; parity, obvious compositeness, and divisibility by small primes are observations and labels, not default deletion rules;
- current protected worldview facts explicitly supplied as project substrate may be used, but other recent research conclusions are not thereby premises.

These are starting commitments. Lower-level primitives remain researchable/challengeable unless separately locked, but P000 is not.

No list of suggested research questions, promising theorem families, preferred invariants, obstruction types, or axiom categories is supplied in Phase A. The first research question must be generated by the researcher from the substrate itself, conditional on P000 and the universal type rules.

## Arithmetic discovery population-preservation rule

When free discovery studies an integer sequence, table, grid, path, residue picture, or computational census, start from the full declared domain. Do not silently discard:

- even integers;
- composites divisible by `2`, `3`, `5`, `7`, or other small primes;
- cases judged “obvious”, “trivial”, or easy to classify.

These strata are not noise. They can expose periodicity, boundary behavior, transition points, multiplicative support, interactions among small factors, and the mechanism by which a pattern breaks or persists. Removing them before inspection can erase the baseline needed to recognize a law.

Freeze:

`DISCOVERY_DOMAIN = FULL_DECLARED_INTEGER_POPULATION`.

`EVEN_OR_SMALL_PRIME_DIVISIBLE != DISCARDABLE`.

`OBVIOUS_COMPOSITE = LABELED_DATA_AND_CONTROL`.

`FILTERED_VIEW != DISCOVERY_UNIVERSE`.

`OBSERVE_FULL_POPULATION -> LABEL_AND_STRATIFY -> COMPARE_FILTERED_VIEWS`.

Filtered odd-only, coprime-only, squarefree, prime-only, or rough-number views are allowed only as explicit secondary projections. Preserve their relation to the full population, and do not promote a pattern found only after filtering to a law of the original domain without checking the omitted strata.

## Spatial substrate — load only when spatial reasoning is actually chosen

For established local spatial mathematics, the exact current three-axis slice is:

`definitions/ENTERPRISE_THREE_POSITIVE_AXIS_OVERLAPPING_CIRCLE_CELL_PLANE_20260820.md`.

Its exact current slice facts include:

- `O_E=0`;
- overlapping circle-cell carrier;
- three visible positive native axes/rays `E_1,E_2,E_3`;
- no required native negative axes in that slice;
- `ENTERPRISE_RIGHT_ANGLE=120_DEGREES` in that slice;
- canonical minimum-zero three-sector coordinate atlas;
- sector-local native length law as frozen in that exact file.

Mandatory interpretation:

`THREE_AXIS_GEOMETRY=RESEARCH_SLICE_OF_6D_SPACE`.

`THREE_AXIS_RESULT!=FULL_6D_WORLD_RESULT` unless a valid six-dimensional lift is established.

Read the exact slice file only when free discovery actually needs those spatial primitives. Do not preload it merely because geometry exists in the project.

## What is deliberately withheld in Phase A

Do not enumerate or preload the project's current downstream achievements in a Phase-A-facing file merely to say they are forbidden; negative instructions can themselves prime those objects.

Generic rule:

> The general current-mathematics router and every downstream current result, task, route, success/failure catalog, coordination surface, detailed theorem catalog, or tool menu are Phase-B comparison knowledge unless the user's current instruction explicitly makes one a premise or the item is a minimal project substrate commitment recorded in this file.

P000 and `ENTERPRISE_BRC_WEIGHTED_GLOBAL_SUBSTRATE_20260902.json` are the explicit current exceptions because the user has promoted them to all-research starting substrate. Their **detailed successful downstream applications are not exceptions**.

Do not load unrelated downstream material merely to make Phase A “better informed”. Its vocabulary, success and available machinery can anchor which question appears natural.

## Phase-B opening rule

After the free researcher freezes its candidate packet, the withheld current-result context may be opened for:

- de-duplication;
- contradiction/falsification of lower-level claims;
- classification as axiom versus derived theorem;
- comparison with existing Enterprise Math work;
- prior-art/novelty analysis;
- selection of verification tools;
- full lookup of `ENTERPRISE_BRC_WEIGHTED_LOG_THEOREM_LEDGER_20260902.json` and `t0.weighted_brc_cwm` when relevant.

These are audit functions after discovery, not a menu for generating the candidate.

A collision discovered in Phase B does not retroactively become a Phase-A premise.

No Phase-B operation may reinterpret P000 as a falsifiable candidate.

## Current-foundation router boundary

`definitions/00_CURRENT_NATIVE_FOUNDATION.md` remains the correct general current-mathematics router for TASK research, Driver review and current-state lookup.

It is intentionally richer than this file because it indexes current downstream canonical results.

Therefore during FREE Phase A:

`USE = p000_reality_foundation.json + definitions/00_FREE_AXIOM_DISCOVERY_SUBSTRATE.md + definitions/ENTERPRISE_BRC_WEIGHTED_GLOBAL_SUBSTRATE_20260902.json`.

`DO_NOT_PRELOAD = definitions/00_CURRENT_NATIVE_FOUNDATION.md + detailed current result/tool catalogs`.

The full current router becomes available in Phase B.

## Challengeability boundary

Generic lower-level foundation is challengeable through the candidate lifecycle; **P000 is not**. The user-promoted Weighted-BRC substrate is current project foundation and may be pressure-tested for scope/correctness, but a FREE researcher must not silently erase it merely to recover an older Boolean-only default; any proposed replacement belongs in the normal audited-candidate lifecycle.

A free researcher may challenge another lower-level primitive or propose a replacement candidate, but may not spend research effort deciding whether reality is 7D, whether Enterprise space is 6D, or whether the six spatial dimensions should be reduced to a classical 3D/4D ontology.

Free innovation is therefore downstream of P000 and current universal type commitments:

`ASSUME_P000 + PRESERVE_DECLARED_BRANCH_TYPE -> DISCOVER_NEW_MATHEMATICS`.

## Authority

For P000 content, `../p000_reality_foundation.json` controls.

For the minimal all-research Weighted-BRC substrate, `ENTERPRISE_BRC_WEIGHTED_GLOBAL_SUBSTRATE_20260902.json` controls.

For detailed Weighted-BRC theorem scope, `ENTERPRISE_BRC_WEIGHTED_LOG_THEOREM_LEDGER_20260902.json` controls after the relevant structure is selected.

For discovery-information boundaries, `research_architecture.json`, the FREE role contract and anti-anchoring protocol control except where the explicit user promotion recorded above adds the new minimal global substrate.
