# Enterprise Math — FREE_AXIOM_DISCOVERY Primitive Substrate Router

Status: `ACTIVE / CANONICAL FREE-DISCOVERY SUBSTRATE ROUTER`
Effective: `2026-08-22`
Research mode: `FREE_AXIOM_DISCOVERY`
Classification: `NO_NEW_MATHEMATICS / INFORMATION-PARTITION ROUTER`

## Purpose

Give a free researcher the **current primitive commitments** without also giving it the project's current successful downstream constructions.

This file creates no new mathematical premise. It only partitions already-current source material into:

`PHASE-A SUBSTRATE` versus `PHASE-B COMPARISON KNOWLEDGE`.

Freeze:

`FOUNDATION_FOR_DISCOVERY != CATALOG_OF_CURRENT_ACHIEVEMENTS`.

`PRIMITIVE_SUBSTRATE -> INDEPENDENT QUESTION/AXIOM SEARCH`.

## Phase-A project commitments

A FREE researcher may start from these current project-level commitments:

- finite resolution / precision is endogenous to the research ontology rather than an afterthought;
- integer/discrete/computable structure is preferred at the native layer;
- `N = N_0` includes zero where the natural-number substrate is used;
- definitions are not inherited merely because an effective/classical theory succeeds;
- implementation carriers and readouts are typed separately from native relational claims;
- current protected worldview facts explicitly supplied as project substrate may be used, but recent research conclusions are not thereby premises.

These are starting commitments, not a closed final axiom set.

## Spatial substrate — load only when spatial reasoning is actually chosen

For native spatial work, the exact current primitive plane/coordinate foundation is:

`definitions/ENTERPRISE_THREE_POSITIVE_AXIS_OVERLAPPING_CIRCLE_CELL_PLANE_20260820.md`.

Its current generation includes the already-frozen primitive spatial facts such as:

- `O_E=0`;
- overlapping circle-cell carrier;
- exactly three positive native axes/rays;
- no required native negative axes;
- `ENTERPRISE_RIGHT_ANGLE=120_DEGREES`;
- canonical minimum-zero three-sector coordinate atlas;
- sector-local native length law as frozen in that exact file.

Read the exact plane file only when the free discovery actually needs spatial primitives. Do not preload it merely because geometry exists in the project.

## What is deliberately withheld in Phase A

The following are **current results/comparison knowledge**, not primitive-discovery input by default:

- `ENTERPRISE_NATIVE_LINE_TRACE_FORMULA_20260821.md`;
- `ENTERPRISE_ARBITRARY_POINT_DIRECTED_LINE_GAUGE_20260821.md`;
- `ENTERPRISE_UNORIENTED_BIDIRECTIONAL_SEGMENT_SPECTRUM_20260821.md`;
- `ENTERPRISE_BRC_MULTIPATH_ENRICHMENT_BRIDGE_20260821.md`;
- `ENTERPRISE_PATH_VALUED_SQUARE_ROOT_OPERATOR_20260821.md`;
- numbered-route taskbooks/results;
- theorem-family/Common-Surface success catalogs;
- current PR/Relay/scheduler/Driver-continuity material.

Do not load these merely to make Phase A “better informed”. Their success, vocabulary and available machinery can anchor which question appears natural.

## Phase-B opening rule

After the free researcher freezes its candidate packet, the withheld current results may be opened for:

- de-duplication;
- contradiction/falsification;
- classification as axiom versus derived theorem;
- comparison with existing Enterprise Math routes;
- prior-art/novelty analysis;
- selection of the best verification tool.

A collision discovered in Phase B does not retroactively become a Phase-A premise.

## Current-foundation router boundary

`definitions/00_CURRENT_NATIVE_FOUNDATION.md` remains the correct **general current-mathematics router** for TASK research, Driver review and current-state lookup.

It is intentionally richer than this file because it indexes current downstream canonical results.

Therefore during FREE Phase A:

`USE = definitions/00_FREE_AXIOM_DISCOVERY_SUBSTRATE.md`.

`DO_NOT_PRELOAD = definitions/00_CURRENT_NATIVE_FOUNDATION.md`.

The full current router becomes available in Phase B.

## Foundation is still challengeable

Current substrate is the canonical starting point, not a sacred final theory.

A free candidate may argue that a current primitive is:

- derivable from something smaller;
- over-specified;
- missing a necessary axiom;
- at the wrong semantic layer;
- replaceable by a sharper primitive.

Such a claim enters the normal candidate lifecycle; it does not silently edit current source.

## Authority

For exact mathematical content, the exact referenced canonical source file controls over this router summary.

For discovery-information boundaries, `research_architecture.json`, the FREE role contract and anti-anchoring protocol control.
