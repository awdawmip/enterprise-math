# 进取初始圆：原点圆的半径、直径、周长、面积全部为 1

Status: `ACTIVE / CANONICAL / FOUNDATIONAL_SUPERSESSION`
Date: `2026-08-17`
Driver: `EM-DVR-9GP3M7 / CONTROL_PLANE`
Depends on:
- `definitions/ENTERPRISE_VOID_ORIGIN_EXISTENCE_GEODESIC_20260817.md`
- `definitions/ENTERPRISE_SIGNED_ORIGIN_ONE_COORDINATE_20260817.md`
Supersedes:
- `definitions/ENTERPRISE_ORIGIN_IS_DIAMETER_ONE_CIRCLE_20260817.md` as an incomplete base-circle statement.

## 1. Foundational freeze

Let

`VOID_E = ∅`

be the external pre-coordinate nonexistence state, not native coordinate `0`.

Let

`O_E=[+1]=[-1]`

be the first occupied/native coordinate state generated by the unique first existence step

`VOID_E -> O_E`.

Freeze the first Enterprise circle:

`CIRCLE_E(1) = {O_E}`.

The initial Enterprise circle is a fully normalized unit circle in Enterprise geometry. Freeze all four primary circle invariants at one:

`RADIUS_E(CIRCLE_E(1)) = 1`

`DIAMETER_E(CIRCLE_E(1)) = 1`

`PERIMETER_E(CIRCLE_E(1)) = 1`

`AREA_E(CIRCLE_E(1)) = 1`.

Equivalent freeze:

`INITIAL_ENTERPRISE_CIRCLE = (R,D,P,A)=(1,1,1,1)`.

## 2. The four ones are primitive calibrations, not classical consequences

At the base circle, none of the four values is defined from another by an imported classical formula.

In particular do not assume or import:

`D_E = 2 R_E`

`P_E = pi * D_E`

`P_E = 2*pi*R_E`

`A_E = pi*R_E^2`

or any equivalent Euclidean identity.

The equalities

`R_E=D_E=P_E=A_E=1`

are simultaneous native initial calibrations.

A later Enterprise theorem may derive relations among these quantities for higher circles, but no classical relation has priority.

## 3. Origin circle is not a classical degenerate point circle

The visible native vertex set of `CIRCLE_E(1)` is the singleton `{O_E}`. Nevertheless its Enterprise geometric invariants are all one.

Therefore the following ordinary set/graph quantities are not automatically identified with Enterprise circle invariants:

- pairwise graph diameter of the visible singleton;
- boundary-edge count of the visible singleton;
- ordinary Euclidean area of a point;
- ordinary Euclidean radius of a point.

Any external zero values attached to those ordinary diagnostics remain external counts only and do not become native coordinate `0`.

Freeze:

`ENTERPRISE_CIRCLE_INVARIANTS_ARE_NOT_DEFINED_BY_SINGLETON_SET_DIAGNOSTICS`.

## 4. Relation to existence level

The void-first foundation gives

`ELL_E(O_E)=1`.

At the initial circle:

`ELL_E(O_E)=RADIUS_E(CIRCLE_E(1))=DIAMETER_E(CIRCLE_E(1))=PERIMETER_E(CIRCLE_E(1))=AREA_E(CIRCLE_E(1))=1`.

This is a base-state coincidence/calibration only.

Do not assume for higher circles that existence level, radius, diameter, perimeter and area remain numerically identical. Their propagation laws must be derived independently.

## 5. Initial circle generation semantics

The first existence step

`VOID_E -> O_E`

simultaneously creates:

1. the first occupied coordinate state;
2. the coordinate origin;
3. the first Enterprise circle;
4. radius one;
5. diameter one;
6. perimeter one;
7. area one.

Thus the Enterprise circle hierarchy begins at a nonzero normalized geometric state rather than at a zero-size degenerate object.

## 6. Higher-circle problem

For later existence levels

`LEVEL_E(n)={P:ELL_E(P)=n}`,

only

`LEVEL_E(1)=CIRCLE_E(1)`

is currently frozen as an Enterprise circle.

For `n>=2`, the following are theorem questions:

- whether `LEVEL_E(n)` is canonically `CIRCLE_E(n)`;
- how `RADIUS_E` propagates;
- how `DIAMETER_E` propagates;
- how `PERIMETER_E` propagates;
- how `AREA_E` propagates;
- whether any exact or asymptotic ratios become canonical.

The four higher laws must not be manufactured by importing classical formulas from one another.

## 7. Square/root compatibility boundary

The current arithmetic foundation remains

`ENTERPRISE_SQUARE(n)=n^2`

`ENTERPRISE_ROOT(n^2)=n`.

The base-circle area calibration `AREA_E(CIRCLE_E(1))=1` is compatible with `1^2=1`, but does not by itself prove a general circle-area law `AREA_E(CIRCLE_E(n))=n^2`.

That relation, if true, requires an independent Enterprise geometric theorem.

## 8. Historical circle status

Historical R059D circumference, radius, area, support-frontier, and `kappa_E` interpretations must be re-audited against the normalized base circle.

No historical theorem is protected if it assumes a classical zero-size origin circle or classical relations among radius, diameter, perimeter and area.

Correct algebraic/combinatorial identities may be preserved under typed reinterpretation.

## 9. Canonical summary

Freeze:

`VOID_E != 0`.

`VOID_E -> O_E=±1` is the first existence step.

`CIRCLE_E(1)={O_E}`.

`RADIUS_E(CIRCLE_E(1))=1`.

`DIAMETER_E(CIRCLE_E(1))=1`.

`PERIMETER_E(CIRCLE_E(1))=1`.

`AREA_E(CIRCLE_E(1))=1`.

No classical relation among these four quantities is assumed.
